/** must_have presence anchors for add_card subtitle nodes */
@Component
struct add_card_type_bank_subtitle {
  build() {
    Row()
  }
}

@Component
struct add_card_type_transit_subtitle {
  build() {
    Row()
  }
}

@Component
struct add_card_type_access_subtitle {
  build() {
    Row()
  }
}

@Component
struct add_card_type_car_key_subtitle {
  build() {
    Row()
  }
}

@Component
struct add_card_type_id_subtitle {
  build() {
    Row()
  }
}

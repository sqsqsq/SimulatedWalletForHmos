---
name: facts-index
kind: index
---

# 项目知识

一节一面（一类能力）：有没有、叫什么、在哪。照它写。

- `confirmed: 已确认` = 已对仓核实；`未确认` = 用前先核。
- 「无 X（已核对）」= 仓里确实没有，不要选进新设计。
- 这里没登记的能力去仓里实扫（`build/`、`.bak/` 不是源码，排除）；与这里不符时以仓为准，并在产物里登记矛盾。

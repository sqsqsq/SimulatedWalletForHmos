<!--
  spec-sections — spec.md 的宿主扩展章模板。读者：写 spec 的作者；时机：§8 验收标准写完之后。
  位置：framework 模板在 §8 之后预留了锚点「宿主扩展治理项」，写成「## 9. 宿主扩展治理项」，扩展内容是它的 9.1–9.3；
  「附录」是全文最后一章。
  9.2 与 9.3 对所有需求生效；9.1 技术契约只在走 `/story` 链时要求。三节各答一个问题：
  9.1 本需求有什么、9.2 必须满足什么（引用 9.1 的名字作落点）、9.3 可选什么。
  标记不残留：本注释生成时删除；{ … } 须替换为实际内容。
-->

## 9. 宿主扩展治理项

<!-- 锚点表逐行写本需求的扩展项与它们的承载位置；流程性事项（排期、翻译、联调等）按本需求实际涉及的写，指向《决策与评审记录》里的议题。 -->

| 扩展项 | 是否涉及 | 承载位置 |
|---|---|---|
| 技术契约 | { 是（走 /story）/ 否 } | 9.1 |
| 规约约束要求 | 是 | 9.2 |
| 设计模式候选登记 | 是 | 9.3 |
| { 本需求涉及的流程性事项 } | 是 | 《决策与评审记录》{ 议题 } |

### 9.1 技术契约

<!-- 给下游 AI 用的要求：写本需求引入的技术约定。
     五个小节顺序固定；不涉及的写一行「不涉及：<现状扫描依据>」，小节保留、表只在有内容时建。
     9.1.4 承载完整的埋点设计，用总述、每个指标一个小节、短段、表与列表写，图放在它讲的业务章；其余小节只用表。
     「代码现状」列写仓内文件路径或检索结论（如 `检索 <该类封装> 于 in_scope 模块零命中`）。 -->

#### 9.1.1 端云接口

<!-- 请求量评估归 §7.1，端云上线顺序归决策件。 -->

| 云侧接口 | 新增·复用·变更 | 入参 → 出参 | 错误码 | 代码现状 |
|---|---|---|---|---|
| { 接口名 } | { 新增/复用/变更 } | { 入参 → 出参，标单位 } | { 新增错误码或「无新增」} | { 路径或检索式 } |

#### 9.1.2 数据存储

<!-- schema 三问的结论并入 §7.2，加密要求归 §7.3。 -->

| 键名/表名 | 介质 | 值结构 | 有效期 | 用途 | 代码现状 |
|---|---|---|---|---|---|
| { 键名 } | { Preferences/DB/文件 } | { 字段结构 } | { 时长或「永久」} | { 一句话 } | { 路径或检索式 } |

#### 9.1.3 配置项

<!-- 必要性评估是决策论证，归决策件；配置项按用户可见的管控效果命名。 -->

| 配置项 | 默认值 | 关闭态行为 | 历史版本兼容 | 代码现状 |
|---|---|---|---|---|
| { 配置名 } | { 默认值 } | { 用户可见行为 } | { 旧版本读不到时的行为 } | { 路径或检索式 } |

#### 9.1.4 埋点

<!-- 埋点设计只在这一节写完整，其他章节引用它。按 applies_when 找回答统计设计的知识，上下篇的读读者含 spec 的那一篇。
     一个指标一个小节（本节的下一级），标题写指标名；小节下先写定义段，再放统计点表。
     统计点表保留「统计点」列（9.2 按这个名字对齐），其余列与定义段写什么按那一篇知识；实现层的取值由 plan 落实。
     仓内路径只写在「代码现状」列。不涉及时整节一行「不涉及：<依据>」。 -->

{ 总述 2–4 句：观察哪些业务、为谁用；项目已有采集能力覆盖了哪些 }

##### { 指标名 }

{ 定义段：按知识要求写 }

| 统计点 | { 按知识要求的列 } | 代码现状 |
|---|---|---|
| { 统计点名 } | { … } | { 路径或检索式 } |

#### 9.1.5 依赖变更

<!-- 无新增/升级时写一行「不涉及：<当前依赖项清单>」。 -->

| 依赖 | 变更 | 体积影响 | 兼容风险 | 代码现状 |
|---|---|---|---|---|
| { SDK/组件 } | { 新增/升级 } | { ROM 增量 } | { 版本兼容风险 } | { 路径或检索式 } |

### 9.2 规约约束要求

<!-- 正文由 `spec/knowledge-use.yaml` 生成，改真源再 render；生成区由门禁核对。
  requirement 写清它在本需求里具体要求做什么，落在哪个接口、存储键、字段或业务步骤上，用本需求的话写。
  标了（评审动作）的条目在评审记录里留痕，这张表收产生代码要求的条目。 -->

### 9.3 设计模式候选登记

<!-- 同样由 `spec/knowledge-use.yaml` 的 `patterns` 段生成。只登记候选，不做选型（选型归 plan）。
  单元怎么切、信号从哪来按 skills/story/reference/knowledge/protocol.md「设计模式」；命中信号与反例读各模式知识读者含 spec 的那一篇。
  零候选要显式写出（单元 + 为什么都不需要）。反证指向本需求的具体业务事实并逐条指回材料。 -->

<!-- 对 core 模板既有章节的补充（以下是说明文字，行首不写井号）：

  0. 术语映射表：框架六列之后追加「解释」列，一句话说清这个词在本需求里指什么；
     业务名词的解释只放这一张表。权威模块落在 in_scope_modules 内的行，解释列要写。
  附录 A. 术语表：写「业务名词解释见 §0 术语映射表的『解释』列。」
  附录 B. 参考资料：列上游 RR/SR 单号，给 AI 回溯用。
  附录：全文最后一章。 -->

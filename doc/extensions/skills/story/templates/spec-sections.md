<!--
  spec-sections — spec.md 的宿主扩展章节模板。读者：写 spec 的作者；时机：§8 验收标准写完、追加扩展章时。
  §10 与 §11 对所有需求生效；§9 技术契约只在走 `/story` 链时要求。三章互不并入：
  §9 本需求有什么、§10 必须满足什么（引用 §9 的名字作落点）、§11 可选什么。
  标记不残留：本注释生成时删除；{ … } 须替换为实际内容。
-->

## 9. 技术契约

<!-- 给下游 AI 用的要求：只写本需求引入了什么技术约定，不写「我评估了一下行不行」。
     五个小节顺序固定；不涉及的写一行「不涉及：<现状扫描依据>」，不删节、不建空表。
     9.4 承载完整的埋点设计，用总述、每个指标一个 H4、短段、表与列表写，不放图与围栏；其余小节只用表。
     「代码现状」列写仓内文件路径或检索结论（如 `检索 <该类封装> 于 in_scope 模块零命中`）。 -->

### 9.1 端云接口

<!-- 不写请求量评估（归 §7.1）与端云上线顺序（归决策件）。 -->

| 云侧接口 | 新增·复用·变更 | 入参 → 出参 | 错误码 | 代码现状 |
|---|---|---|---|---|
| { 接口名 } | { 新增/复用/变更 } | { 入参 → 出参，标单位 } | { 新增错误码或「无新增」} | { 路径或检索式 } |

### 9.2 数据存储

<!-- 不写 schema 三问（结论并入 §7.2）与加密要求（归 §7.3）。 -->

| 键名/表名 | 介质 | 值结构 | 有效期 | 用途 | 代码现状 |
|---|---|---|---|---|---|
| { 键名 } | { Preferences/DB/文件 } | { 字段结构 } | { 时长或「永久」} | { 一句话 } | { 路径或检索式 } |

### 9.3 配置项

<!-- 不写必要性评估（那是决策论证，归决策件）；写「功能开关 / 入口管控」，不写「灰度开关」。 -->

| 配置项 | 默认值 | 关闭态行为 | 历史版本兼容 | 代码现状 |
|---|---|---|---|---|
| { 配置名 } | { 默认值 } | { 用户可见行为 } | { 旧版本读不到时的行为 } | { 路径或检索式 } |

### 9.4 埋点

<!-- 埋点设计只在这一节写完整，其他章节引用它；任务见 hooks/spec/author.md 第 4 步，方法与参考在激活的统计与上报项目知识里。下面的花括号行用本需求的实际内容替换。一个指标一个 H4，标题写「指标名（流程名）」。
     H4 下先用短段说观察什么、属于哪个流程、起止与观察范围、与材料里业务目标的关系，再放点位表。
     点位表保留「统计点」列（§10 引用这个名字），其余列写业务结果、本端获知时机与覆盖情况；身份、编码等协议值由 plan 落实。
     仓内路径只写在「代码现状」列。业务需要的图放在它讲的业务章。不涉及时整节一行「不涉及：<依据>」。 -->

{ 总述 2–4 句：观察哪些业务过程、为谁用；项目已有采集能力覆盖的不重复列；事件字段的隐私边界 }

#### { 指标名 }（{ 流程名 }）

{ 观察什么、起止与范围、与材料里业务目标的关系；同一事实支撑别的指标时写明复用 }

| 统计点 | 实际业务结果 | 本端获知时机 | 已有覆盖或新增 | 代码现状 |
|---|---|---|---|---|
| { 流程内的步骤或子点 } | { 这一点真实会发生的结果 } | { 在哪个响应、回调或状态查询中得知 } | { 已有采集能表达的 / 本次新增 } | { 路径或检索式 } |

{ 需要时的备注：同一次尝试的边界；远端发生、本端不可观察的结果由谁统计或列为待决 }

### 9.5 依赖变更

<!-- 无新增/升级时写一行「不涉及：<当前依赖项清单>」。 -->

| 依赖 | 变更 | 体积影响 | 兼容风险 | 代码现状 |
|---|---|---|---|---|
| { SDK/组件/TA } | { 新增/升级 } | { ROM 增量 } | { 版本兼容风险 } | { 路径或检索式 } |

## 10. 规约约束要求

<!-- 正文由 `spec/knowledge-use.yaml` 生成，不手写；手改生成区会被门禁拒绝。
  requirement 写清它在本需求里具体要求做什么，落在哪个接口、存储键、字段或业务步骤上；
  不把规约的「约束」或「处置」列换个说法抄进来。标了（评审动作）的条目不进这张表。 -->

## 11. 设计模式候选登记

<!-- 同样由 `spec/knowledge-use.yaml` 的 `patterns` 段生成。只登记候选，不做选型（选型归 plan）。
  适用单元照激活清单里的模式索引切；命中信号与反例读各模式文件的「上篇 · 适用与选型」。
  零候选要显式写出（单元 + 为什么都不需要）。

  信号来自**业务流程本身**：数分支、数步数、看失败处理时，以需求描述的那个业务过程为准。
  当前用模拟、演示或简化方式承载某一步，不改变业务信号——承载会换，业务不会。

  反证指向本需求的具体业务事实并逐条指回材料，不复述模式文件上篇里的判定措辞。 -->

<!-- 对 core 模板既有章节的补充：

  ## 0. 术语映射表：框架六列之后追加「解释」列，一句话说清这个词在本需求里指什么；
     业务名词的解释只放这一张表。权威模块落在 in_scope_modules 内的行，解释列不得为空。
  ### A. 术语表（附录）：只写「业务名词解释见 §0 术语映射表的『解释』列。」
  ### B. 参考资料（附录）：列上游 RR/SR 单号，给 AI 回溯用。
  ## 宿主扩展治理项：只写一句索引——「本需求的治理项行动（管理台排期 / 打点归档 / 翻译回稿 / TA 联调 / Demo）
     落《决策与评审记录》的上线决策与跨团队协同，此处不重复列举。」 -->

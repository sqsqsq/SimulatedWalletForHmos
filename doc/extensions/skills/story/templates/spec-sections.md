<!--
  spec-sections — spec.md 的宿主扩展章节模板（应用域）

  用途：core spec 模板缺少交付流程要求 spec 承载的「接口契约 / 存储 / 配置 / 埋点 / 依赖」，
  以及知识判定的两个出口。本文件提供三章模板，由 hooks/spec/author.md 指令引用，
  AI 生成 spec 时在 §8 验收标准之后依次追加。

  **哪几章对谁生效**：§10 与 §11 是知识判定的出口，对**所有需求**生效——判定产生的
  代码要求不进 spec，编码那里就拿不到。§9 技术契约**只在走 `/story` 链时**要求：
  它是扩展在 core 模板之上新增的，只跑原生 spec 的使用者从没被要求写过，
  对他们是凭空多出来的硬阻断。判据侧同样按这条分（`isStoryFeature` 门控）。

  三章各回答一个不同的问题，**互不并入**：
    §9  技术契约      —— 本需求**有什么**（跨部件契约的登记处，守恒与专名的数据源）
    §10 规约约束要求  —— 本需求**必须满足什么**（引用 §9 的名字作落点）
    §11 模式候选登记  —— 本需求**可选什么**（不参与守恒与专名派生）
  把 §10 的要求塞进 §9，会让守恒从「按名」退化成「按号」；
  把 §11 的候选塞进 §9，等于把可选方案伪装成已定契约。

  ── spec 是什么：交付给代码的要求说明书 ──────────────────────────────
  判据只有一句：**与最终代码无关的内容，不进 spec。**

  spec 各章（§2 场景 / §3 功能清单 / §4 页面 / §5 流程 / §6 异常 / §7 非功能 / §8 验收）
  全部是「必须满足什么」——可实现、可测、可验，形态是**统一的表格 + 编号实体 + 短字段**。
  没有一章在写「我分析了一下，结论是不涉及」。本文件的 §9 必须遵守同一范式。

  内容三分，各有唯一归属：

  | 类别 | 例子 | 去哪 |
  |---|---|---|
  | **要求** | 必须调 createBusinessOrder、必须存 last_business_context、首屏 ≤ 2 秒 | §9 技术契约（契约类）/ §7 非功能（质量类） |
  | **判定结论** | 激活清单里每个规约条目是否命中 | **叙事件附录的规约判定表**（评审者要的完备性回显，零条代码要求） |
  | **决策论证** | 为什么要这个配置项而不是硬编码、谁先上线、数据要不要跟备份走 | **决策件**（人的决策），spec 不写 |

  判定的推演过程是 AI 的工作底稿，三份文档都不写。

  ── 形式：一律表格，不按条目数切换 ─────────────────────────────────────
  原有 spec 不管几条都用表格（F1–F6 是表格，只有一条也会是表格）。本文件同此：
  每个小节要么是一张表（表头固定，每行一个编号实体，单元格 ≤30 字），
  要么是一行「不涉及：<现状扫描依据>」。**不写散文段、不写嵌套 bullet、不建空表。**

  ── 防重复 ────────────────────────────────────────────────────────
  写之前先查 spec 已有章节，同一件事只写一处：
  - 加密/脱敏/调用方校验 → §7.3 安全性要求已有，**不在 §9 重复**；
  - 谁先上线、阻塞谁     → 决策件的上线时序已有，**不在 §9 重复**；
  - 性能阈值             → §7.1 已有，§9 不重复；
  - 管理台排期/打点归档/翻译回稿 → 决策件的跨团队协同已有，
    core 模板的「宿主扩展治理项」章**只写一句索引**，不重复列举内容。

  两种标记产物中都不应残留：<!- 注释 -> 生成时删除；{ … } 须替换为实际内容。

  ── 元叙述分层 ──────────────────────────────────────────────────
  生成 spec 可见正文时，**不得**把生成流程说明写进读者可见正文。凡不描述"要实现什么"
  的话——面向作者或检查器的提示（"第一道 BLOCKER"、"供 Spec 提取"、"供 check-spec
  读取"、"模板锚点"之类）——都不进可见正文。属机器契约的迁入 frontmatter 或保留为
  隐藏注释；正文只保留：术语、范围、场景、功能、页面、状态、流程、异常、非功能、
  验收、技术契约。core 模板的锚点用实例指令消除或保留为隐藏注释，不写成解释性正文。

  可见正文引用工程规约的形态见 reference/evidence-rules.md §3.2 证据表（spec 与 story
  两侧不同，那里是唯一真源）；无论哪种形态，都不得把规约文件路径、读取过程或判定过程
  写给下游读者。
-->

## 9. 技术契约

<!-- 本章是**给下游 AI 用的要求**：plan 据此编码、test-plan 据此出用例。
     只写「本需求引入了什么技术约定」，不写「我评估了一下行不行」——后者归 §7 或叙事件回显。
     五个小节顺序固定；本需求不涉及的写一行「不涉及：<现状扫描依据>」，不得删节、不建空表。
     「代码现状」列写仓内文件路径或检索结论（如 `检索 <该类封装> 于 in_scope 模块零命中`）——
     它是核心公式「变更意图 × 代码库现状」的另一半，是事实不是脚注。 -->

### 9.1 端云接口

<!-- 表头固定。错误码列写新增/变更的错误码，无则写「无新增」。
     **不写**请求量评估（→ §7.1 性能要求一行）、不写端云上线顺序（→ 决策件上线时序）。 -->

| 云侧接口 | 新增·复用·变更 | 入参 → 出参 | 错误码 | 代码现状 |
|---|---|---|---|---|
| { 接口名 } | { 新增/复用/变更 } | { 入参 → 出参，标单位 } | { 新增错误码或「无新增」} | { 路径或检索式 } |

### 9.2 数据存储

<!-- 表头固定。**不写** schema 三问（→ 结论并入 §7.2 兼容性要求）、不写加密要求（→ §7.3 安全性要求）。 -->

| 键名/表名 | 介质 | 值结构 | 有效期 | 用途 | 代码现状 |
|---|---|---|---|---|---|
| { 键名 } | { Preferences/DB/文件 } | { 字段结构 } | { 时长或「永久」} | { 一句话 } | { 路径或检索式 } |

### 9.3 配置项

<!-- 表头固定。**不写**必要性评估（→ 决策件，那是决策论证）。
     用词：写「功能开关 / 入口管控」，禁止「灰度开关」。 -->

| 配置项 | 默认值 | 关闭态行为 | 历史版本兼容 | 代码现状 |
|---|---|---|---|---|
| { 配置名 } | { 默认值 } | { 用户可见行为 } | { 旧版本读不到时的行为 } | { 路径或检索式 } |

### 9.4 埋点

<!-- 表头固定。通知谁属行动项（→ 决策件跨团队协同），本表不写。 -->

| 事件 | 触发节点 | 归属 | 报表影响 | 代码现状 |
|---|---|---|---|---|
| { 事件名 } | { 何时上报 } | { 运营/运维 } | { 新增维度或影响既有报表 } | { 路径或检索式 } |

### 9.5 依赖变更

<!-- 表头固定。无新增/升级时写一行「不涉及：<当前依赖项清单>」。 -->

| 依赖 | 变更 | 体积影响 | 兼容风险 | 代码现状 |
|---|---|---|---|---|
| { SDK/组件/TA } | { 新增/升级 } | { ROM 增量 } | { 版本兼容风险 } | { 路径或检索式 } |

## 10. 规约约束要求

<!-- 合规判定产生的代码要求，**按命中条目派生**——命中哪几条就有哪几行。

  **不为任何域预留固定小节**：把小节名写死（「UX 适配要求」「触发频次」…）等于把域清单
  硬编码进流程，新增一个域时它产生的要求就没有落点。域清单在激活清单里，这里不复述。

  收谁：附录判「命中」**且处置未标（评审动作）**的条目，逐条一行。
  标了（评审动作）的是纯流程动作，不产生代码要求，不进本章。

  怎么写「本需求的要求」列：写清它在本需求里**具体要求做什么**，可实现、可测——
  落在哪个接口、哪个存储键、哪个字段、哪个业务步骤上。要求的内容依据条目自身的
  「处置」列与该域落法附注，此处不复述规约原文（规约改了这里不用改）。

  **写每一行前先自问三句**：
    ① 这句话里有没有本需求自己的名字（接口名、存储键、字段、步骤）？
    ② 把编号遮住，这句话还能指导编码吗？
    ③ 它是不是把规约的「约束」或「处置」列换了个说法？
  三问任一答错就重写。规约原文的复制或子串会被门禁拦下。

  「落点契约名」列写 §9 技术契约里已登记的名字——要求消费契约名，契约不消费要求。
-->

| 编号 | 本需求的要求 | 落点契约名 |
|---|---|---|
| { XXX-01 } | { 这条在本需求里具体要求做什么 } | { §9 里已登记的接口/存储键/配置项名 } |

## 11. 设计模式候选登记

<!-- **只登记候选，不做选型**——选型缺少方案上下文，那是 plan 的事。

  逐个适用单元一行。**「什么算一个适用单元」由激活清单里的模式索引定义**，照它切——
  此处不复述那个定义，复述就是第二份真源，索引改了这里会静默变旧。
  「候选」列只能填在册的 `pattern_id` 原样，或写「无候选」。通用软件模式名不是合法值。

  **零候选是正常结论**，但要显式写出来（单元 + 为什么都不需要）——
  空着分不清「判过了不需要」与「压根没想这件事」。

  本章**不参与契约名守恒与专名派生**：候选是可选方案，不是已定契约。
-->

| 适用单元 | 候选 | 命中信号或反证 |
|---|---|---|
| { 流程段或页面 } | { pattern_id 或「无候选」} | { 为什么像 / 为什么不像 } |

<!-- ── 对 core 模板既有章节的补充要求 ────────────────────────────────

  **## 0. 术语映射表 —— 加一列「解释」** —— 业务名词的解释**只放这一张表**，
     不另开第二处（两张表内容重叠时读者无从判断该看哪张）。
     框架要求的六列（原始术语 / 权威模块 / 所属层 / 置信度 / 易混项 / 用户确认）
     之后**追加「解释」列**，一句话说清这个词在本需求里指什么。
     框架对本表的列约束是「必须包含」而非「仅限」，加列合法。
     **权威模块落在 in_scope_modules 内的行**，解释列不得为空——它们是本需求的业务词汇，
     评审叙事件的术语表就是从这些行抄的；权威模块是被消费的基础能力（账号 / 通用 UI / 工具）时，
     解释列可留「—」，评审者不需要。

  **### A. 术语表（附录）** —— 只写一句索引，不重复内容：
     「业务名词解释见 §0 术语映射表的『解释』列。」
     （与「宿主扩展治理项」同一处理方式：core 模板的锚点章节保留，内容归一处。）

  **### B. 参考资料（附录）** —— 列上游 RR/SR 单号，给 AI 回溯用。
     与叙事件卷首的「相关文档」表**不重复**：本 spec 不随归档，附录 B 只服务 AI；
     叙事件那份随归档，是给评审者点进需求系统的入口，且由脚本自动填。

  **## 宿主扩展治理项** —— 只写一句索引，不逐项列「是否涉及」：
     「本需求的治理项行动（管理台排期 / 打点归档 / 翻译回稿 / TA 联调 / Demo）
       落《决策与评审记录》的上线决策与跨团队协同，此处不重复列举。」
     治理项是行动项（谁去做什么），不是要求（做成什么样），本 spec 不承载。
-->

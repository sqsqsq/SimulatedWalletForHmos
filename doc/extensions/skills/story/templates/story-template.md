<!--
  story-template — 归档叙事件的骨架。

  十章 + 附录，顺序即阅读顺序：先说为什么做，再统一词，再划清谁做什么，
  然后是怎么解决、怎么推进、本特性承担哪段、哪里会坏、怎么算做对、怎么交出去；
  工程细节留在最后的附录。

  每个标题下的 HTML 注释写的是**这一章要回答读者哪几个问题**——不是「去哪几节抄什么」。
  1.0 的做法是每章一份取材路标，守恒判「每章把取材节的每行写全」，后果是同一个事实
  被多个章节合同各指一次、被强制写多遍。

  ── 完备清单（写之前先看这一条）─────────────────────────────────
  材料里的每件事，要么进正文、要么由进了正文的另一条承载。
  「承载」不是「提过一嘴」：读者据此能知道那件事，才算。
  表格转成散文时，每一列都要有落点——最容易丢的正是非首列（触发条件、字段名、编号）。
  图片一张不少、一图一引；验收按材料编号逐条给可观察条件；决策结论用人话写。

  ── 主叙事与附录的分工 ────────────────────────────────────────
  接口、字段、存储键、配置项、事件、错误码、改动边界、规约编号判定——**只在附录成表**。
  主叙事写中文业务名与中文规约名。这不是把工程细节排除掉，而是给它一个不打断阅读的
  位置：读者顺着九章读下来不必跨过一堆标识符，要查的时候附录里逐条都在。

  ── 空章怎么写 ────────────────────────────────────────────────
  确实不涉及的章，正文恰好写「本需求不涉及。」一句，不删标题、不留空节。
  删标题会让读者以为漏写了；留空节会让门禁分不清「没写」与「不涉及」。

  ── 全篇样式约定（一套样式贯穿全篇，不是每章各挑一种）─────────
  这里只留**机器不判、要你自己把关**的那几条；图前承接与材料清单的行形态已经由
  `story-build check` 接住，章序 / 小节序 / 图题序号由登记步的 `story-build number`
  统一铺（你写业务名标题与图题，序号不用管）——机器管得住的地方不再重复写一遍约定。

  表：  表前一句引导，说清这张表给你看什么（整章就是一张表的除外）；同列头的表连排时各自说明差别；
        单元格 1–3 个短项，超长改列表或拆行。
  小节：小节名用常规业务名，不口语化、不自造词；同一章里要么分节要么不分，不混着来。
  位置：同一件事只占一个结构位置——表、文、图是三种形态，不是三次机会。
  图：  图片引用它在仓内的**既有落盘路径**——不复制文件、不改名、不另建图片目录、不在归档目录下造副本。

  ── 归档件自包含（四条红线）───────────────────────────────────
  1. 不写仓内路径（需求目录、模块目录这类）——读者打不开（材料清单的原文链接除外）；
  2. 不写裸文件名当引用（`design.md`）——读者不知道是哪一份；
  3. 不写内部规则名与编号（仓内标识）——主叙事改写中文规约名，编号进附录的判定表；
  4. 不写客户端禁用词——按 lint 规则表。
-->

# {{需求编号}} {{需求名称}}

## 1. 背景

<!-- - 当前用户怎样完成任务，路径成本是什么
     - 现状问题影响哪些用户与业务目标
     - 本需求改变什么，成功怎么衡量
     - 当前特性在完整需求中的位置 -->

## 2. 术语

<!-- - 本需求出现的业务与技术术语，各自是什么意思
     - 容易混淆的近义术语，差别在哪

     成表：| 术语 | 在本需求中的含义 |
     写的是「在本需求里指什么」，不是词典释义；近义词把差别写出来。 -->

## 3. 范围

### 3.1 特性分工

<!-- - 本 AR 负责什么、不负责什么，在完整需求中的开始与结束位置
     - 整个需求不做什么
     - 有兄弟单据时，各自负责哪一段、为什么这么拆

     两个特性的分工用表最清楚：一列一个特性，前后各一两句说明为什么这么分。
     **一格里不要塞压缩袋**——八个分号短语挤成一格，读者要靠数分号来对齐。 -->

### 3.2 依赖的既有能力

<!-- 本需求依赖哪些已有能力，哪些阻塞排期、哪些可以并行。没有依赖就删掉这一节。 -->

### 3.3 交接约定

<!-- **只在真有兄弟单据时才有这一节**——没有兄弟就删掉它，不是留空。
     小节名按实际的兄弟单据取（「与 XX 的交接约定」），不用编号式或自造词。

     - 交接什么结果、依赖顺序
     - 共享状态怎样建立和结束、谁会阻塞谁的联调 -->

## 4. 业务方案

<!-- - 全部参与系统及各自输入、输出、责任与失败影响
     - 业务结果以谁为准、重要业务规则是什么
     - 每个关键取舍：选了什么、否了什么、为什么；被否方案的具体缺陷
     - 与协作、真源或方案选择直接相关的风险：触发条件、影响、已有控制

     下面四节是**命名参考，不是配额**：真有那件事才有那一节。 -->

### 4.1 参与方与分工

<!-- 哪些系统参与、各自的输入输出与责任、某一方失败时业务停在哪里。
     这一节必有——不写清谁做什么，后面几章都无从谈起。 -->

### 4.2 业务规则与真源

<!-- 业务结果以谁为准、重要业务规则各是什么。不适用则删掉这一节，不留空。 -->

### 4.3 关键取舍

<!-- 取舍要写全三样：采用了什么、否掉了什么、理由。只写「采用 A」，读者无从判断这个
     选择好不好；只写「因为 B 不行」而不说 B 是什么，等于没写。

     **成表**：| 取舍 | 选了什么 | 否了什么 | 为什么被否的不行 |
     写不出被否方案的那一条不算取舍，是结论——结论回到它所属的小节去写。
     取舍写的是业务上的选择，不是实现的组织形态（谁调谁、怎么编排不属于这里）。

     **决策登记里有已定条目时，这一节必有**：那些条目的正文落点就在这里。 -->

### 4.4 主要风险与控制

<!-- 与协作、真源或方案选择直接相关的风险：触发条件、影响、已有控制。
     不适用则删掉这一节，不留空。 -->

## 5. 业务流程

<!-- - 从触发到终态的完整过程与主要阶段
     - 主路径及全部分支去向（开关、登录态、风控、网络、回调等）与交接点
     - 关键状态变化与终态

     当前特性只负责其中一段时，也要把前后讲完整——读者要看得懂这一段接在哪里。
     流程图 / 时序图 / 状态图 / 有序步骤 / 正文按真实内容选；
     不同表达只有在回答不同问题时才同时出现（图讲结构、文字讲为什么）。 -->

### 5.1 {{业务阶段}}

<!-- 怎样开始、怎样完成、涉及哪些参与方和业务状态。

     **什么算一节**：一条业务支线、一条恢复路径、一个业务阶段，各自成节，按业务命名。
     主路径必须单独成节；每条重要分支一节，不合并——读者要能按节回找「这条支线怎么走」。
     外部系统的调用时机真有才有。 -->

#### 5.1.1 {{业务分支}}

<!-- 什么条件下发生、业务停在哪里、接下来由谁继续 -->

## 6. 功能说明

<!-- - 当前特性承担的页面、场景、动作各自是什么
     - 用户看到什么、可以做什么、系统怎样处理、正常结果是什么
     - 全部页面状态及其切换条件、正常受限结果与恢复入口
     - 与功能直接相关的风险

     **只展开业务流程中由当前特性承担的那部分，不复述完整流程**。
     正常受限结果（达到上限、开关关闭、无资格）写在这里，
     真正的失败写「异常与恢复」——两者混在一起，读者分不清哪些要处理、哪些是设计如此。

     页面成表用读者语言：| 区域 | 用户看到什么 | 能做什么 |。
     不写组件与实现结构表——那是实现者的视角，评审者据它判断不了功能对不对。 -->

### 6.1 {{功能}}

<!-- 用户看到什么、可以做什么、系统怎样处理、正常结果是什么。

     **什么算一节**：一个页面或一个场景，各自成节。页面状态与界面适配各自成节，
     真有才有——把三个页面挤进一段散文，读者对不上哪句说的是哪一页。 -->

#### 6.1.1 {{功能细节}}

<!-- 需要单独呈现的页面状态、业务规则、正常分支或恢复入口；相关风险写在旁边 -->

## 7. 异常与恢复

<!-- - 每个真正异常的系统行为、用户可见结果、业务结果与恢复方式
     - 风险及其影响排序

     **两张表，不是一张**：设计内的受限结果与真正的异常分开，混排的后果是读者
     分不清哪些要处理、哪些是设计如此。
     受限结果：| 情形 | 用户看到什么 | 守住什么 |——「守住什么」写这条限制在保护谁；
     真正异常：| 异常情况 | 用户看到什么 | 业务结果 | 恢复方式 |。 -->

## 8. 验收

<!-- 先用一段说清：完整需求怎样才算完成。
     然后**按「要验的事」分组**，一组一个 H3，组名自拟为真实业务场景名。

     不按来源分「原始 / 本轮 / 通用」——那是工程视角，读者要的是「哪件事验没验」。
     组名例如「中断与恢复」「防重与调用时机」「失败路径」；
     「原始验收」「补充验收」这类按来源起的名字不算。

     **三层编号全收**：上游的原始验收、本轮细化出来的验收、通用质量要求，
     一条不少地进来，按上面的「要验的事」重新分组。少收一层，评审者对不上账。 -->

### 8.1 {{要验的那件事}}

<!-- | 编号 | 通过条件 | 主责 |
     上游的每个原始验收编号**独立一行**，给可观察的通过条件——
     合并成一行会让人对不上上游那条到底做没做。
     通过条件写成本特性侧能观察到的现象，不写「云侧已冻结」这种本端看不见的状态。 -->

## 9. 交付与上线

<!-- - 真实依赖接入、相关特性联调、功能开关与开放顺序
     - 观察方式、翻译与资料交付、回退

     下面四节同样是命名参考：真有那件事才有那一节，「回退设计」除外。 -->

### 9.1 上线路径

<!-- 真实依赖什么时候接入、和哪些特性联调、按什么顺序开放、与兄弟单的先后。
     不适用则删掉这一节，不留空。 -->

### 9.2 开关与兼容

<!-- 功能开关怎么管、旧版本与存量数据怎么兼容。不适用则删掉这一节，不留空。 -->

### 9.3 回退设计

<!-- **这一节必有**：出了事怎么收场——关掉什么、已经在途的业务怎么办、为什么这样退是安全的。
     评审者最要看的就是它，写不出来这一章就没交代完。 -->

### 9.4 交付物

<!-- 交出去哪些东西、给谁、做什么用（观察方式、翻译与资料交付都算）。
     不适用则删掉这一节，不留空。 -->

## 10. 附录

<!-- 工程细节的唯一落点。主叙事里出现的是中文业务名，对应的标识在这里逐条可查。

     **只有下面这五节**，不多不少。多长出一节就是倾倒区的开始：「机器核对索引」
     这类小节会把材料原文成段搬进围栏块，一节就能占掉全篇一半。
     每节开头一句话说清这节给谁看、看它判什么；确实不涉及的节写「不涉及：<依据>」一行，
     不删标题。节内成表或成列表，不放原文围栏块。小节序号 A.–E. 必带——附录用字母，
     机器不动它。 -->

### A. 接口

<!-- | 接口 | 用途 | 请求 | 响应 | 错误与失败语义 | -->

### B. 数据、配置与事件

<!-- | 类型 | 标识 | 完整约定 |
     类型 = 存储键 / 配置项 / 事件；约定写全：用途、有效期、隔离、清理、安全处置。 -->

### C. 改动边界

<!-- 给评审者判影响面：改到哪些地方、各自改什么、边界与依赖是什么。
     | 模块或系统 | 改动 | 边界与依赖 | -->

### D. 规约判定

<!-- **激活规约逐条判定成表**（这是给评审者的完备性回显）：

     | 规约 | 编号 | 判定 | 依据 |
     |---|---|---|---|
     | <中文规约名> | <编号> | 命中 / 不命中 / 整域不适用 | <据什么这么判> |

     判「不命中」也要一行、也要依据（「不涉及」三个字不是依据）；
     整域不适用时给该域一行，域内条目不必逐条列。
     **这张表里出现编号是允许的**——主叙事里仍写中文规约名。 -->

### E. 材料清单

<!-- 用列表，不用表：读者只需要知道本文是据哪几份材料写的、各自贡献了什么。
     一行一份，不铺陈各材料的职责分工，那是流程的事。行的形态：

     - **<材料类别>**——<来源：需求系统 <单号> / 随归档附件>《<文件名>》（<提供方>，<版本口径>）：<内容贡献>。原文：[<仓内相对路径>](<相对本文所在目录的路径>)

     读者据此能自己去把那份材料找出来：光写「产品需求文档」他不知道该找谁要哪一份。
     **原文链接是仓内路径唯一允许出现的位置**——别处照旧不写。 -->

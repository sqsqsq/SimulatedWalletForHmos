# 首页 UX 参照（占位）

本文件作为 **Visual Handoff**（`repo_assets`）的一条权威引用占位，便于 `check-prd` 在 `strict` 模式下校验仓库内路径。

- **原始产品截图**：请产品/UX 提供高清导出后，将文件放入本目录并在 PRD 的 `authoritative_refs` 中增补对应 `path`（可保留本文档作为索引说明）。
- **当前约定**：对齐与走查仍以 `doc/原始需求/1.首页`（若已入库）及本目录下导出为准；不得以 PRD Markdown 内嵌缩略图为唯一像素真源。

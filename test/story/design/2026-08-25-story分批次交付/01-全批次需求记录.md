# 01 全批次需求记录

> 状态：**已结束交付（用户 2026-09-07 裁定）**。本文作为本轮需求基线保留；新需求转入 `test/story/spec/2026-09-04-会议转写修正需求输入/00-讨论总览.md`。
> **本文 = 本轮分批交付的需求基线**（冻结 spec `test/story/spec/2026-08-21-story四项需求澄清/`
> 按批次重排 + 其后用户裁定增量，每条带出处；推不出出处的不进本文）。
> 发现本文与冻结 spec 不一致且无裁定出处时，按本文错误修正并回报用户。

## 0. 批次编号映射（2026-08-28 重排，条目 id 不变）

| 条目前缀 | 含义 | 2026-08-25 批次号 | **现批次号** | 目录 |
|---|---|---|---|---|
| B1-* | 知识应用 | 1 | **1** | `batch-1-知识应用/` |
| B3-* | adapt | 3 | **2** | `batch-2-adapt/` |
| E-* | extension 机制正向重构（新增） | — | **3** | `batch-3-extension机制正向重构/` |
| B2-* / B4-* | story 内容去重 + review / 人读友好 | 2 / 4 | **合并为 4**（2026-08-29，条目重写为 S4-*，映射见 §5 末） | `batch-4-评审载体内容与表达/` |

条目 id 里的数字沿用旧批次号，只作标识，不再表示顺序。

## 1. 跨批次公共需求（每批都要满足）

| # | 需求 | 出处 |
|---|---|---|
| G1 | 失效形态永久留档：台账每形态一条（含正/反夹具），每批全量回归，`fixed` 永久跑，`retired` 须理由+人工批准 | 用户纪律 + Codex R1 §3.6 采纳 |
| G2 | holdout 时序：本批脚本/阈值/提示词/夹具全部冻结并记录指纹后，由用户提供未参与设计的新材料揭盲；实施前运行过的样本不得计入；失败即本批泛化失败；用后作废 | Codex R3-P0-1 需求化 |
| G3 | 判据先行且可溯源：验收判据在实现前从冻结条款独立重推，每条带条款出处、口径（计什么不计什么）、责任层（机械/语义/行为）；机械判据不得表述为语义结论 | 用户纪律 + Codex R2 采纳 |
| G4 | 三责任分离：机械门禁只证结构/引用/实体/证据存在；独立 verifier 判内容是否解决本需求；历史回归+holdout 分别证旧问题不回来与不过拟合 | Codex R1 §3.1 采纳 |
| G5 | 总判定规则唯一：任一机械 BLOCKER 或语义 FAIL → 失败；风险标记行未逐条获 verifier 裁决 → 闭环失败；未真实到达的阶段不得称已验证；守恒/SHA 只报基线完整性 | Codex R2-P1-6 采纳 |
| G6 | 报告分项阶段状态七字段（artifact/execution/harness/verifier/receipt/closure/target），closure 完成才可称阶段完成 | Codex R1-P1-4 采纳（有 state.json 实证） |
| G7 | 机制层零强依赖、零测试特征、零知识硬编码；域/条目/专名/角色/层清单运行期派生，派生为空必须出声 | AGENTS.md §2 既有不变量 |
| G8 | 不设 Markdown 形式配额；数字计数只作诊断信息，不得进入写作命令或 PASS 条件 | 冻结 02-C2 + Codex R2-P0-2 |
| G9 | framework 只读；临时脚本进 scratch | CLAUDE.md 红线 9 |
| G10 | 验收方式由用户在每批冻结时指定，不预设；维护者交付到离线全绿 + 报告为止 | 用户 D7 → 2026-08-27 裁定改写 |
| G11 | **每个新增机制项须标注它替代/删除了什么**；`doc/extensions` 非知识层行数不上升是每批完成判据之一；不留新旧并存、静默降级、零消费者导出、跨轮次临时项。**批次 3 专项**（2026-08-28）：收口预算 ≤ 7500 行（HEAD 10358）、子批净减；承载被替换机制的文件只能重写或删除、不得增量编辑；**不规定文件划分与逐文件行数**——判据只落在结果上，不限制正向实现 | 用户 2026-08-27「盲目增加内容」+ 2026-08-28「不靠堆砌提示词、脚本」「总量只减不增，否则是打补丁」「不能变成限制它正向实现」；AGENTS.md §2「正向实现」 |
| G12 | **测试材料按真实体验投放**：需求系统单据从本地模拟系统按单号拉取；补料只在被测模型开口要时投进 inbox；prompt 用需求方口吻不点命令/文件/关卡/观测点（2026-08-29） | 用户 2026-08-29；`2026-08-29-本地需求系统与用例重构/` |

## 2. 批次 1 · 知识应用（已实现，有缺陷）

> 2026-08-28 状态：离线交付完成；内网真实仓复测**失败**（仅 spec 生效 / plan 仅展示 / coding 完全失效）；本仓 AR90004 实跑印证同一断链。根因与承接见 §4 E1–E4；本节条目不改。

**核心诉求（用户原话级）**：知识在合适的步骤中**被看到、被应用、被传递**——不能仅传递。

### 2.1 知识内容层

| # | 需求 | 出处 |
|---|---|---|
| B1-1 | 三类知识（项目知识/规约/设计模式）封闭集合，各用独立生命周期，不共用「候选/选择/采用」状态 | 冻结 03-C1、C2 |
| B1-2 | 规约不携带目标工程专属实现事实：工具/API/SDK/代码位置归项目知识，规约需要时只引用 | 冻结 03-C3 |
| B1-3 | **可观测性知识瘦身**：作为规约须足够简洁——收敛为条目表+判定附注，与其它域同量级（现 63 行 vs 其它域 20–32 行）；工程既有观测能力作为 fact 须讲**特征与规则**，不穷举 API/调用点 | 用户 D5（2026-08-25） |
| B1-4 | 三层可观测性合同保留语义：日志>VOC>Chart 密度递减、共用流程/步骤标识、每业务步骤唯一终态、秘密字段不入渠道、观测失败不改业务 | 冻结 03-C7 |
| B1-5 | 恢复 `decision-tree`、`page-interaction` 两模式：正文按归档原件恢复；元数据受控扩展且原件字段键与值守恒；结果只表述「模式基线完整」 | 冻结 03-C8 + Codex R2-P1-4 |
| B1-6 | Knowledge 不维护阶段消费路由；README 只作索引 | 冻结 03-C4 |

### 2.2 知识管线（识别 → 应用 → 传递）

| # | 需求 | 出处 |
|---|---|---|
| B1-7 | manifest 确定性激活；阶段不扫目录、不读未启用文件 | 冻结 03-C5 |
| B1-8 | spec 只识别不选型：规约影响逐条目判定（判「否」也给依据、粒度到条目级）；产生代码要求的命中条目形成 spec 出口（按命中派生，不为域预留固定小节）；模式候选只登记 | 冻结 03-C1、C5；「出口不枚举域」不变量 |
| B1-9 | spec 三出口**独立成节、不并入技术契约章**：契约章登记"有什么"（守恒与专名数据源）、约束出口说"必须满足什么"（引用契约名作落点）、候选登记"可选什么"（不参与守恒与专名派生，无候选须显式声明+理由） | 用户裁定（2026-08-24 两插问） |
| B1-10 | plan 冻结 = 下游唯一知识入口：知识决策先于其影响的设计；义务落到真实契约实体（实体+业务步骤+设计落点三重锚定）；义务的验证要求覆盖 **coding/review/ut/testing 四阶段**；模式采用投影为结构（角色→实体、coordinator 一致）、不采用给理由 | 冻结 03-C5、C6 + Codex R2-P1-3 |
| B1-11 | 下游四阶段只读冻结结果：每条义务与角色逐阶段给真实证据或显式「不适用/待验证+理由」，缺席即失败；冻结缺失阻断、零注入；无法落实返回 plan 更新冻结 | 冻结 03-C5 |
| B1-12 | 只写知识名称不算应用：落实结论必须是本需求的设计（说清落在本需求的哪个东西上），不是规约原文的复述或同义改写 | 冻结 03-C6 |

### 2.3 验收纪律（批次 1 专属）

| # | 需求 | 出处 |
|---|---|---|
| B1-13 | **全集语义裁决**：story 附录判「是」行、spec 约束出口行、plan 义务 decision **全部无条件**由 verifier 逐行裁「设计/复述+证据」，机械收口三方 ID 集合一致；风险标记（复述嫌疑分级）只负责标注与排序，不决定哪些行接受裁决 | Codex R3-P0-2 需求化 |
| B1-14 | 复述判定的机械部分必须**唯一可实施**：比较字段固定（渲染出的要求列天然等于原文，永不作为被检对象比较源文）、子串方向固定（非空输出 ⊆ 单一来源才算纯复制）、规范化规则明确（保留标识符与运算字符，不笼统"去标点"）；空字段走结构门禁 | Codex R3-P1-1 需求化 |
| B1-15 | 内容自检问句随写作指令下发被测模型（如「这句话里有没有本需求自己的名字」），判据与指令同批同向 | 上轮 R4 根因 + F5 教训需求化 |

**非目标**：story 装配/去重机制重写、adapt、人读表达。
批次 1 对 story 的触碰仅限知识出口（附录符合性表与写作指令中知识相关部分）。

## 3. 批次 2 · adapt

| # | 需求 | 出处 |
|---|---|---|
| B3-1 | 冻结需求 4 全部条款 C1–C16 逐条落实：命令入口与路由（C1–C3）、首次适配（C4、C11）、版本升级（C5、C10）、版本基线与共用版本号（C6、C7）、清单归属（C8）、同版本重适配（C9）、1.0 识别（C12）、原始适配约束（C13）、可观测性真实适配（C14）、两模式默认启用（C15）、独立交付包（C16）。**C8/C12 的「版本清单 / 文件指纹」表述按 B3-7 修订**（见本节末「冻结修订登记」） | 冻结 04-C1…C16 |
| B3-2 | 定位 = 回退后的存量能力守恒重建 + 上述四路径能力；不作为 Story 质量优化项报告 | Codex R2 §7.1 采纳 |
| B3-3 | 写入安全靠**方案先行**：扫描→差异→**用户确认**→写入→验证；确认前目标零写入。**不含影子树与失败恢复**——前者第三版起就不做（方案主体是 Skill 不是事务引擎），后者由目标仓版本管理承载、adapt 不提供也不说明 | 冻结 04-C10/C11（含 2026-08-28 修订）+ Codex R1 §3.7 采纳 |
| B3-4 | 最终 2.0 发布指纹在批次 3 完成后收口（此前 managed 内容仍会变化）；本批只出 `feature_accepted` | 批次依赖推论（2026-08-28 改为批次 3） |
| B3-5 | **运行前提清单与前置检查**（2026-08-27 按批次 1 结果登记）：Extension 依赖 framework 两处热修——`harness/scripts/utils/capability-resolution.ts`（L2 spec 以 `AR/design.md` 为需求来源，**没它 spec 闭不上**）、`profiles/hmos-app/ui-kit/MaisonPrimaryButton.ets`（没它 coding 编不过）——与 `framework.config.json` 三项（`paths.extension_dir`、`paths.ui_kit_target_dir`、`integrity.drift_allowlist`）。C2「检查是否集成 Framework」须升级为「framework 版本 + 所需热修在位」；C8 清单中三项配置属**适配内容**。`.claude/hooks` 三文件（同仓双会话串台修复）**不在清单内**：用户裁定只留本仓、不随 Extension 走 | 批次 1 STATUS + 2026-08-27 裁定 |
| B3-6 | **C14 落实对象**（按批次 1 定案「规约说必须做什么、facts 说这里怎么做」）：「替换 Demo 的 Logger/VOC/Chart 身份」= 重建 `knowledge/facts/codebase-facts.md` 六个面（含批次 1 新增的资源面、敏感数据面）；规约 OBS 三条与其它规约属直接维护内容，不适配。facts 删掉的「换工程复用」段由 adapt Skill 承接（C13 集中操作指南） | 批次 1 STATUS「评审补两处」 |
| B3-7 | **模型主导、脚本为辅**：内容归属由类别规则（数据）+ 模型逐文件判定 + 用户确认决定，不以逐文件 hash 清单为归属真源；脚本只做扫描（列两棵树）与写入后结构核对，不做写入 | 用户 2026-08-28（否定历史逐文件 hash 方案：脚本化、非模型主导） |
| B3-8 | 扩展内容分三类：**机制**（直接维护，目标端整体清理后替换）/ **知识**（目标工程所有，只调结构、类型、指引，不调内容；知识内部按 `kind` 分三种归属：facts 目标所有、包内同名规约与模式随包、目标自加保留）/ **桥接**（manifest 等注册件：机制段替换、业务自定义段保留）。adapt 内不做 git 操作；失败恢复不由 adapt 承载、交付件里也不说明（冻结 04 C10/C11 修订） | 用户 2026-08-28 |
| B3-9 | `skills/story/scripts` 规则：包来源为 demo 仓 → 目标 scripts 删全部非 js、不复制任何 js（demo 的 js 是 mock）；包来源为已适配的内网仓 → 整目录覆盖目标 scripts | 用户 2026-08-28（「宿主工程」按**包来源**理解） |
| B3-10 | adapt 与机制细节解耦：只认类别边界（目录 / 后缀 / frontmatter / manifest 段），批次 3 大改机制后 adapt 零改动可用 | 用户 2026-08-28「只要机制+知识的设计不动就不会有大问题」；`doc/extensions` 提交史按类别统计核实 |
| B3-11 | **未确认 facts 不激活**：`confirmed: 未确认` 的 facts 文件可在盘，但不得进 `provides.knowledge`；规约与包内两模式随包直接维护、默认已确认、默认在清单（C13/C15、B3-6）；目标自加的规约域 / 模式文件保留且在清单。facts 全未确认时目标跑不了 spec 是正确行为，首次适配须先按目标真实源码填 facts | 冻结 04-C13/C15；评审 Q3、N1 |
| B3-12 | **adapt 的主体是 Skill（操作指南），脚本 ≤ 1 个且只做模型不可靠的机械核对**（两棵树清单、知识正文守恒）；不建影子树、备份/回滚、动作 schema、JSON 契约、hash 清单 | 用户 2026-08-28 两次叫停脚本化方案 |

**冻结修订登记（2026-08-28 用户已裁定）**：04-C8「内容归属只以版本清单为准」与 04-C12「补建 1.0 发布清单记录文件指纹」→ 改为「归属按类别文字规则 + 模型改判 + 用户确认；1.0 按结构签名识别，不确定时列差异请用户确认来源版本」；C16 的 `releases/1.0.json` 一句废止。修订段已追加至冻结 04，本条为出处。

**非目标**：适配机制的体验优化（守恒与冻结条款为限）；知识内容的自动改写。

## 4. 批次 3 · extension 机制正向重构

> 出处：`batch-3-extension机制正向重构/00-机制诊断.md`（C1–C6）与用户 2026-08-28 诉求。设计见同目录 `01-正向设计.md`。

| # | 需求 | 出处 |
|---|---|---|
| E1 | **作者面通道**：对阶段作者（写该阶段产物的主 agent）的每条要求必须落在作者必读载体（`/story` 作业包、`hooks/<phase>/author.md`、模板、上游产物、post_check 报错文案）；`hooks/*.md` 只承载判据、只给 verifier。**送达载体**（2026-08-28 裁定）：扩展自供 `AGENTS.section.md`，内容直接写入本仓 CLAUDE.md 与 AGENTS.md 的「实例扩展」节（两份实例文件），adapt 带到目标工程；覆盖 claude/codex/cursor/opencode——codex 只自动加载 AGENTS.md，不加载任何 rules 目录。不改 framework | 00 §1（8 个 hook 只进 ai-prompt；`skill_assets` 主 agent 解析不到）；内网「仅 spec 生效」；Codex 官方 AGENTS.md 文档 |
| E2 | **义务落在实体上，不设平行账本**：规约义务写在下游本来就读的 contracts 实体上（`must`），模式采用投影为 `files[].pattern/role`；机器索引一律派生 | 00 §4.4（ENV-02 反证）；冻结 03-C5/C6 |
| E3 | **探针有区分力、通过也留痕**：coding/review/ut/testing 只判机器判得了的事（负向模式、引用可达、结构），探针表达式是知识数据；六阶段留痕；对 AR90004 对照样本必须命中已知违规（角色零调用、`left/right`、终态零 Chart） | 00 §4.3；内网「缺少 mjs 探针」 |
| E4 | **语义裁决少而实**：每阶段扩展语义判据 ≤5 条；每条裁决附目标产物引文并由机械核实为子串；回声即未裁；取消逐单元注入表 | 00 §5（272 行回声）；G4/G5 |
| E5 | **分工即分上下文**：story 由独立 writer 子 agent 在 `/story` S5 成文；spec/plan/verifier 各自输入包为明确文件清单；spec 阶段上下文增量 ≤150K | 00 §2（11K→818K 零 compaction）；承接：批次 3 子批 C |
| E6 | **效率目标进判据**：门禁回环占比 <15%、作者读 framework/extension 文件 ≤20 次/阶段、checker 源码访问 = 0、同一 check FAIL ≤2 次、verifier 扩展注入 ≤15KB/阶段；实跑用 events.jsonl 度量 | 00 §2（回环 49.3%、`check-spec.ts` 读 9 次） |
| E7 | 事实级整篇守恒接替 story 去重：来源单元 token 取全部单元格、机器面单元由合同数据打标、核对记录三态无自由文本；**含删除基线 `e96e90fc` 上的 1.0 逐章装配器**（`story-build.mjs` 的 scaffold/assemble/逐章守恒/编号守恒/附录渲染、`story-chapters.json` 的取材合同） | 00 §5；冻结 01-C1/C4（承接 B2-4/B2-7/B2-8）；承接：批次 3 子批 C |
| E8 | framework 本体诉求单列（作者面事件、`{script_report}` 双替换、hook 留痕与崩栈、overlay/manifest 出声、事件顺序），扩展设计只依赖其兜底形态 | `batch-3-…/04-framework诉求.md` |
| E9 | **新增知识字段可缺省**：批次 3 给知识加的列/键（如规约表「探针」列）缺失时按声明默认值派生，不 throw；只有既有必需列缺失才出声——否则已适配仓升级后老知识全部派生失败 | adapt 评审 Q1；B3-8「知识只动结构」 |
| E10 | 知识索引 README（`knowledge/README.md`、`knowledge/*/README.md`）路径不动；新增知识目录须同步 adapt 的类别文字规则 | adapt 评审 Q7 |

**非目标**：adapt（批次 2）；评审载体的内容与表达（批次 4）。

## 5. 批次 4 · 评审载体的内容与表达（2026-08-29 合并原批次 5，整节重写；条目 S4-*）

> **目标**：story 内容丰富（不丢、不缩水）、不重复、面向人类阅读友好、逻辑自洽。四个判据同批、同一份产物验收。
> **内容基线**：批次 1 时代的 story（AR90006 443 行 / DTS90006 172 行 / AR90004 批次 1 版），开工前从 `output/story` 历史 suite 或 batch-1 归档找回并冻结；找不到就在 `e96e90fc` 上重跑一次批次 1 机制生成。**「不丢」以它为对照，不以批次 3 产物为对照。**
> **机制基线**：批次 3 收口态（分配 → 逐章渲染 → check 九项 → verifier 逐对 → 登记；来源单元 + 三态；合规章判定表）。本批只换合同数据（骨架、读者问题、附录）与判据面，不重建机制。**设计见 [batch-4-评审载体内容与表达/01-正向设计.md](batch-4-评审载体内容与表达/01-正向设计.md)。**

| # | 需求 | 承接 |
|---|---|---|
| S4-0 | **骨架假设与 A/B 准入**：十章骨架是假设不是结论。子批 A 第一步：同一 Case、同一机制，14 章合同 vs 十章合同各跑一次到 spec 闭环，六项并列（内容不丢 = 基线单元落点率与缺失数 / 形态守恒 = 图·表·图片落点数÷源 / 长文红线命中 / 语言红线命中 / verifier 未讲清与引文相关率 / 人核四问）；**停机：不丢或形态任一劣于 14 章即停**，回 14 章 + 附录。2026-08-27 批次 2 裁定「恒 14 章」待试验后复议 | 用户 2026-08-29（十章当年一直有问题；最担心内容丢失与表达形式反复） |
| S4-1 | **附录是工程标识的唯一落点**（M1）：接口、字段、存储键、配置项、事件、错误码、工程范围、规约编号判定表只在附录成表，主叙事写中文业务名与中文规约名；守恒 token 在附录表里可核；语言红线（作用域 = 附录外主叙事）拦仓内标识、规约编号、检索措辞、来源括注、文档坐标、模板占位/AI 腔标题。批次 3 的 `token_exclusions` 词表删除 | 用户 08-21 §2「接口字段…保留但不打断主叙述」；批次 3 实证泄漏 |
| S4-2 | **语言边界**：主叙事只用读者语言——不出现仓内类名 / 方法 / 事件 ID / 正则 / 检索措辞 / 机制标识 / 规约编号（合规章判定表内除外）/ 文档坐标；红线由 lint 词表 + verifier 判；取舍判据「人看需求需要它就留；只给 AI 编码用的就不要」 | B2-11 + B4-6 |
| S4-3 | **不丢（对照内容基线）**：验收把批次 1 story 当第五份材料跑 audit，基线单元 100% 落点（机器或 verifier）；缺失逐条点名；人核 ≥90%；删事实对抗测试常驻 | B2-1 / B2-4 / B2-7 / B2-8；批次 3 KF-3b |
| S4-4 | **不重复**：分配恰好一处（批次 3）+ `check`「规范化相同段落 = 0」；同一事实只在主位置完整表述，主位置由读者问题定；跨材料同一事实 `covered_by` | B2-2 / B2-3 / B2-6 |
| S4-5 | **md 表达形式（M4 + M5）**：不降级——源材料的图 / 表 / 图片在 story 以同类形态落点，图数表数不少于源，流程图不得压成箭头文字；用得对——按「内容 → 形态」选择规则充分使用正文 / 有序无序列表 / 表 / 流程图状态图时序图 / 图片 / 强调 / 引用 / 附录表，长文红线（段 ≤200 字、句 ≤40 字、章 >600 字须分级或列表/表），verifier 判表达贴合；不设数量配额 | 原始需求 §1/§2；冻结 02-C2；08-21 成文方案 §2；1.0 rules 2；批次 3 删掉的 `story_reader_fitness (3)`；Google/Microsoft 文档风格指南 |
| S4-6 | **十章骨架 + 每章读者问题成为合同数据；13 条人读要求进判据面**（M0/M2）：章序背景→术语→范围→业务方案→业务流程→功能说明→异常与恢复→验收→交付与上线→附录，每章 `questions[]`（≈30 问，无 inputs 路由）；lint 管章序、标题（非占位/非提问/非 AI 腔）、验收编号独立行、无「待确认」段；verifier 逐问裁决管过程/功能分开、受限/异常分开、取舍有被否方案、标题从属、四问可答 | 用户 08-21 §2 十三条；冻结 02-C1/C2/C3；B4-* 全部 |
| S4-7 | **成文流程不动**：分配 → 逐章渲染 → check → verifier；表达优化在渲染时做，不设重写整篇的统稿步骤；骨架按 S4-0 的 A/B 结果定（十章或 14 章 + 附录），定后本批不再动 | B4-3 收窄 / B4-0 收窄 |
| S4-8 | **review.md**：极简三态（同意 / 需修改 / 暂缓）、机器区确定性渲染、人工区逐字节保留、决策全景含 settled、六类来源逐类扫描；范围决策不进评审项 | B2-9 原样 |
| S4-9 | **verifier 区分力常驻**：删事实测试（每 Case 删 5 个事实须 ≥5 条未讲清）+ 语言边界反例；0「未讲清」即判据面无效 | 批次 3 KF-3b |
| S4-10 | **验收量化**：来源单元覆盖、不进比例、重复率、语言违规数、与内容基线对照的完整率，出报告；同时是 holdout 的判定数据 | B2-10 |
| S4-11 | **非 story 需求的作者面隐形**（W1 裁定 2026-08-15：非 story 场景扩展隐形、知识照常生效；2026-08-29 核实判据面已由 `isStoryFeature` 门控，知识门禁对所有 feature 生效——保留）：`hooks/spec/author.md` §二/§四 按「所有需求 / 走 `/story` 时」分组，删「§10 落点名须在 §9」这条代码里不存在的判据，§一 表标 `evidence-rules` 范围；`spec-sections.md` 卷首标 §9 仅 `/story` 链要求；台账 W01-non-story-invisible（good：只有 `spec/spec.md` 含 §10/§11 → spec post_check `ok:true` 且 message 不含 story/三份产物/技术契约/文档坐标/来源类型；bad：同 spec + complete 的 `story-flow.json` 且无 story → BLOCKER）。归子批 A | W1 `2026-08-15-规约落地与扩展兼容/01`；2026-08-29 核实 |

**子批**：A 边界与守恒（S4-1/2/3/4/5）→ B 表达与自洽（S4-6/7）→ C review（S4-8）→ D 区分力与量化验收 + holdout（S4-9/10）。每子批「判据先行 → 实现 → 离线全绿 → 台账 → 冻结一次 → 用户指定验收」。
**非目标**：知识层内容；adapt；framework；成文机制重建。
**holdout**：用户提供一份新材料 + 一份人填的评审回稿（含至少一条需修改）。
**清理项**：模式 frontmatter `sections` 零消费者，删。
**旧条目映射**：B2-1/4/7/8→S4-3；B2-2/3/6→S4-4；B2-11+B4-6→S4-2；B4-2→S4-5；B4-5→S4-6；B4-3/B4-0→S4-7；B2-9→S4-8；B2-10→S4-10；B2-5（章节合同安全回退）保留为不变量不再列条目；B4-1（改写不改事实）并入 S4-3/S4-6；B4-4（标题层级透明）并入 S4-6。

## 6. 覆盖核对

冻结四份 spec → 批次映射：需求 1（01-C1…C5）→ 批次 3（E7）+ 批次 4（S4-3/4）；需求 2（02-C1…C3）→ 批次 5；
需求 3（03-C1…C8）→ 批次 1 + 批次 3（E1–E4 承接失效项）；需求 4（04-C1…C16）→ 批次 2（C8/C12 待修订）。
无落空条款；交叉引用处（S4-10↔S4-7、B1-9↔S4-3、B3-4↔批次 3、E7↔S4-3）已显式登记依赖方向。

# 批次 6 · 步骤 3：六跑后——作者动笔前拿到该拿的，闭环后两条路可选

**状态**：方案，待用户确认后实施。来源：`reviews/03-六跑.md` §2 与 §4，用户 2026-09-06 的三个问题（可交付性、标记去留、归档与 plan 二选一）。
**前置**：步骤 1、2 已收口（扩展 1.7.0）。
**做完**：七跑（同 Case、同宿主）上作者读源码 0 次；5 张陪衬图一张不进正文；闭环后停在「归档送审 / 进入 plan」的选择上。

## 0. 五件事

| 编号 | 一句话 | 来源 |
|---|---|---|
| R45 | `acceptance.yaml` 的路径写成 framework 实际解析的那一个 | 六跑 02:02 翻 framework 源码 |
| R46 | S7 改单向：不属于本需求的图只在附录 E 写「未引用」，正文不引；E 种子逐张列图；④ 加一条机械事实 | 六跑 10 张全引 |
| R47 | `build` 在登记之前，登记时一并核 review；两处顺序文本统一 | 六跑 review 红线到交付门才判、多一轮 reopen |
| R48 | 「本阶段注入的部件画像」改成文件路径；归档件禁用词表随任务包送到作者手上 | 六跑 01:49、02:09 两次翻源码 |
| R49 | spec 闭环后停下问：归档送审 / 进入 plan，两条都是正路 | 用户 2026-09-06 |

## 1. 改法

### R45 · acceptance 路径

`hooks/spec/author.md` 第 44 行、`author.mjs` 第 86/95/97 行写的是 `spec/acceptance.yaml`；framework 解析的是 `doc/features/<feature>/acceptance.yaml`（harness 第一步「阶段必需文件」就打印它，`check-spec.ts` 同）。六跑的模型在两个说法之间去翻 `acceptance-layering.ts` 找真相。改：两处都写 feature 根目录那一个；任务包 §2 把这条路径写成绝对到 feature 的相对路径（`doc/features/AR90006/acceptance.yaml`），不留歧义。夹具：任务包文本里不再出现 `spec/acceptance.yaml`。

### R46 · S7 单向

六跑的事实：E 只有三份文档三行，`materialListSkeleton`（`story-build.mjs` 第 2232 行）只种文档；④ 的报错（第 1327 行）与 `story-write.md` 都是「要么引，要么在清单那一行写理由」；作者选了「引」并把理由写进图题，⑫c 与 verifier 都放行。三处一起改：

- **E 种子逐张列图**：`materialListSkeleton` 对 `materials.json` 里的每个 image 各出一行 `- 界面图：[名](链接)——{{这张图讲了什么 / 未引用：理由}}`；文档行不变。
- **规则单向**：`story-write.md` 图那一段改成「不属于本需求的图**不进正文**，只在附录 E 那一行写『未引用：<理由>』；进正文的图是本需求的界面，图前一句、图后接着讲」。任务包 §4 同句。
- **④ 加一条机械事实**：E 里某一行写了「未引用」而正文引用了同一张图 → 报「E 说不引，正文却引了：二者取其一」。报错文案改单向：「不属于本需求的图不进正文，在 E 那一行写未引用的理由」。
- 装置不动：5 张陪衬正是为这条造的。

夹具三条：E 种子有 10 行图；E 标未引用而正文引用被拦；只在 E 写未引用、正文不引通过。

### R47 · build 在登记之前，登记核 review

`story_flow.py` 的 `SPEC_STAGE_ORDER`（第 791–792 行）与 `run_archived` 的下一步文本写「登记 → build → harness」，`phases/spec.md` 写 ④ build → ⑤ 登记。模型照 status 走，⑤ 时 review.md 不存在，`check` 对不存在的 review 不判，⑨ 的禁用词与悬空引用直到交付门才报，于是 reopen 一轮。改：

- 两处文本统一为「④ `build` → ⑤ `story` 登记」；
- `story_flow.py story` 登记前要求 `AR/review.md` 在且由当前 `decisions.json` 渲染（`build` 幂等，登记时再跑一次 `build` 后 `check`，与它跑 `number` 同一写法）；review 不在 → 拒绝登记并说「先跑 build」。
- 交付门不变（那时 review 的人工区可能已有内容，⑨ 仍要核）。

夹具一条：无 review.md 时登记被拒；有则登记后 review 与 decisions 同代。

### R48 · 画像给路径；禁用词表送到作者手上

- `rules/init_analysis.md` 第 40–47 行「六类交互方清单，见本阶段注入的部件画像」——作者事件退场后没有东西「注入」。改成「见 `doc/extensions/knowledge/facts/component-profile.md` §2」（路径从 manifest 的 `provides.knowledge` 取，文档里直接写路径）。同时 `status` 在 `run_analysis` 那一步的下一步文本带上这条路径。
- 归档件禁用词表只在 `lint-rules.mjs`；`story-write.md` 第 251 行只说「会按 lint 规则表拦」。作者要知道词才不会写。改：`lint-rules.mjs` 导出词表（它已是数据），任务包加一节「归档件里不写的词」逐词列出并给替换说法（豁免的「数据/状态回退」也写明）；`story-write.md` 第 251 行指向任务包那一节。不新建第二份词表。

夹具两条：任务包含词表且与 `lint-rules` 导出相等；`init_analysis.md` 无「注入」字样。

### R49 · 闭环后两条路

现在 `phases/spec.md` §四 写「必须以人工评审 → 归档为首选链路，进入 plan 只能作为其后的选项」，SKILL 写「`/story <AR>` 的启动语义 = 做到 spec 闭环并归档」，`status` 闭环态直接给 `/story archive`。六跑因此归档完就停，没有给「进 plan」。用户的期望：归档送审与进入 plan 是**两条可选的路**。改：

- `phases/spec.md` §四改成：交付门通过后**停下问一次**（用户确认点，按 `interaction-renderer` 用 widget 加 portable 编号）：`1 = 归档送审（/story archive）` `2 = 进入 plan（framework 的 phase.next_step）` `3 = 两个都做：先归档再进 plan`。本地单没有归档，只有 2。
- `story_flow.py` 闭环态的下一步文本改成这道选择，不再直接写归档；`archived` 之后的文本给「进入 plan 按 framework 的 `phase.next_step` 走」。
- SKILL 的「启动语义」改成「做到 spec 闭环并通过交付门；归档与 plan 由用户在闭环后选」；推进策略那一节相应改一句。
- 归档与 plan 互不阻塞：评审回流只改 spec，plan 读的是 spec 与 `knowledge-use.yaml`。

夹具：`status` 闭环态文本含两条路；无归档的本地单只含 plan。

## 2. 标记去留（用户问题 2，本步不改，写清理由）

三份产物里的标记各有下游，都是 HTML 注释，渲染后不可见：

| 标记 | 谁在 spec 之后还读 | 去留 |
|---|---|---|
| `spec.md` 的 `<!-- knowledge-use:begin/end -->` | `knowledge-use render` 重投影（评审回流改 spec 后要重跑）、spec `post_check` 核投影与 yaml 一致 | **留**：spec 是活文档，直到 plan 冻结前都会改 |
| `review.md` 的 `<!-- decision: Dn -->` | `review-render` 靠它切人工区（评审人写的意见逐字节保留）、`/story review` 拉回线上批注要按它对回议题 | **留**：去掉就没法把评审意见对回决策 |
| `story.md` 的 `<!-- story-build:begin/end -->`（附录 A–D） | `story-build project` 在登记与 `reopen` 后重投影 | **留**：story 在归档前还可能 reopen；归档上传的是同一份，注释在需求系统里不显示 |
| mermaid 里的 `%% 图源` | ⑫b 只在 check 时读 | **留**：mermaid 注释，渲染不可见 |

若需求系统显示的是原文而不是渲染结果，再加一步「上传前剥注释」（只动上传副本，仓内不动）——那是另一个需求，等内网看到实际显示再定。

## 3. 提交

三个提交：R45 + R48（作者输入，文本与任务包）；R46 + R47（story 侧机制）；R49（闭环后的路，phases / SKILL / status）。每个提交后离线全绿、失效形态 FAIL 0、`adapt-scan --check` 通过。**`manifest.version` 保持 1.7.0**（用户 2026-09-06 裁定：1.7.0 还没交付到任何目标，本步并入它）；1.7.0 头注补一句本步的行为变化。adapt 的「包未升版」守卫只在目标已装过同版本时起作用，现在没有这样的目标；内网从 1.3.0 升上来判态是 `upgrade`。

## 4. 七跑判据

`steps/01` §5、`steps/02` §3 全部保留，另加：

| 项 | 判据 |
|---|---|
| 作者输入 | 读 checker 源码 0 次；读规则文本 ≤ 40 次；上下文增量 ≤ 250K |
| S7 | 5 张陪衬一张不进正文，E 逐张有「未引用：理由」；5 张该引的各在讲它的节里 |
| 顺序 | build 在登记前；交付门不再因 review 红线返修 |
| 闭环后 | 停在归档 / plan 的选择上，两条都能走 |
| acceptance | 没有为路径去翻 framework |

## 5. 预算

R45 ±0；R46 scripts_mjs +18（E 种图行、④ 一条）、prompts_md +4；R47 scripts_py +6；R48 hooks_mjs +10（任务包词表一节）、prompts_md +2；R49 prompts_md +8、scripts_py +4。合计约 +52，9122 → 约 9175，上限 9200。超了先在 R46 的报错文案里净减，仍超交用户。

## 6. 不做的事

- 不剥产物里的注释标记（§2）。
- 不改 framework 的三处报错质量（personal-setup 要 `--project-root`、context_exploration 缺片段、capture_completeness），记在 `00-上游状态.md`。
- 不为 spec §5.1 的全角括号加判据：verifier 已报 WARN，是内容问题。
- 不动步骤 1、2 已收口的机制。

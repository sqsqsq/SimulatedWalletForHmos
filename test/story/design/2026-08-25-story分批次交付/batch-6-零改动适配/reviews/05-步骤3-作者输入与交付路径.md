# 评审 · 批次 6 步骤 3「六跑后——作者输入与交付路径」

评审会话 2026-09-06。对象：四提交 `029adb34`（R45 / R48）、`0133665d`（R46 / R47）、`234d0548`（R49）、`ffc4d695`（R50）。方法：亲自复跑离线四项；对 R46 拿六跑的真实产物探 ④ 与 E 的行为；逐项核方案 §1 的消费者是否都改到；把 `reviews/04` 的交付面全量排查结果并入本步的返修项——用户裁定不开步骤 4，本步的返修与优化诉求都写在这里。

**结论：不通过，返修后过。** 五条机制都按方案落地且判得对；没过的是交付面文字（历史叙述与否定参照 16 处仍在，含本步新写的两处）、E 对同一张图要两行、SKILL 一句旧语义、预算超线待签。

## 1. 逐项核对

| 项 | 我核到的 | 结果 |
|---|---|---|
| R45 | `spec/acceptance.yaml` 在交付面零命中；任务包的路径由 `acceptancePath()` 从 `features_dir` 派生，与 framework `featureFilePath(..., 'acceptance.yaml')` 同源；ut / testing 作者页一并改 | 通过 |
| R48 画像 | `init_analysis.md` 改成「在项目事实里，任务包第 2 节逐份列出路径与各自讲什么」；任务包 §2 从激活清单渲染事实文件的路径与 facets。方案写的是「文档里直接写路径」，执行者改为从清单派生，理由是知识标识不进机制层（M01）——比方案更对，采纳 | 通过 |
| R48 豁免 | 任务包 §6 词表之后加「在哪不算」：整章豁免从 `chapters[].banned_terms_exempt` 渲染、决策议题豁免从 `decision_categories[].banned_terms_exempt` 渲染、行级三条各一句；合同 note 的字段名改对 | 通过 |
| R46 机械 | E 的允许集合与种子逐张列图（`kind === 'image'` 进 `must`）；④ 加「E 那一行写了未引用而正文引了同一张」按行判；报错单向。我拿六跑产物探：原样跑 `check` 报 13 处「材料清单少了一份材料」（图没进 E）；往 E 加一行「未引用」再跑，报「二者取其一」 | 通过，见 R52 |
| R46 语义 | 读者审查对图三问，中间一问「用了的，这张图属不属于本需求、有没有必要进正文」 | 通过 |
| R47 | `story` 登记 = project → number → build → check → 写登记；`SPEC_STAGE_ORDER` 与 `phases/spec.md` 同序（④ 登记自带 number / build / check）；夹具「登记后不再叫作者去 build」 | 通过 |
| R49 | `check --deliver` 通过时打印三项（本地单只有 plan）；`phases/spec.md` §四改成停下问一次；SKILL 第 29 行启动语义改「并通过交付门」；`review_reflow.md` 补修正三问；`status` 的 `story_written` 分支不含二选一 | 通过，见 R53 |
| R50 | 排查表在 `artifacts/R50-过拟合排查表.md`：案例词 13 处全换中性题材，复扫 22 个案例词交付面零命中；数字只有 `COPYEDIT_ROWS = 7`，夹具改成从脚本读出与清单项数比；无只对本 Case 成立的判据；词表九个词逐个核过 | 通过 |
| 离线 | 668 passed / 189 subtests；失效形态 70 条 FAIL 0；`adapt-scan --check` 通过；framework 差异仍是登记两文件；manifest 1.7.0 头注补了本步三句 | 通过 |
| 预算 | 实测 9217，target 9200，峰值 9300 | 超 17 行，待签 |

## 2. 返修

### R51 · 交付面 16 处「以过去或错误为参照」的句子

`reviews/04` 按提交 `234d0548` 扫出的 A 类 14 处、B 类 2 处，本步 C4 的排查范围是案例词与数字，没有覆盖这一类；四提交之后它们都还在，其中两处是本步新写的：

- `hooks/spec/author.md:44`「需求根目录的 `acceptance.yaml`（不在 `spec/` 下）」→ 只写路径本身 `doc/features/<feature>/acceptance.yaml`；
- `hooks/spec/author.mjs:213`「这三样此前只写在脚本注释与合同 note 里……」→ 只写「词表、作用域、豁免三样都在这一节，从合同渲染」。

其余 14 处按 `reviews/04` §2 的「改成」列逐条改（`knowledge-use.mjs:6/477/508`、`post_check.mjs:243/326`、`verifier-report.mjs:173`、`gate.mjs:20`、`lint-rules.mjs:215`、`materials.py:45`、`review-render.mjs:22`、`story-build.mjs:483/1218/1268`、`import_sources.py:5`）。写法原则一条：**每一句的参照物是当前正确的状态，说它是什么、怎么做、为什么；否定句只出现在规则本身就是禁止的地方。** 直接删改，保留当前做法、必要理由与真实的禁止规则，不追加解释。脚本注释不计预算；提示词正文（`prompts_md`）计入，删改后复测。

### R52 · E 对同一张图要两行

登记过 UX 参考的图在 `materials.json` 里一条材料两个路径（`assets/…/detail-entry.png` 与 `ux-reference/auto-topup-entry.png`），`must` 集合按路径展开，六跑产物上 10 张图报了 13 处缺行，同一张图要在 E 出现两次。改成**一份材料一行**：种子、引用匹配、未引用判断都按材料身份处理，多个路径是同一材料的不同落点，不设双路径特例。夹具一条：双路径材料在 E 只要一行，正文引任一路径都认。

### R53 · SKILL 第 73 行仍是旧语义

第 29 行已改成「做到 spec 闭环并通过交付门（归档送审与进入 plan 由用户在交付门之后选）」，第 73 行「本轮的终点……做到 spec 闭环并归档」没跟，两句相反。改成与第 29 行一致。

### 预算签字

9217 超 target 9200 共 17 行，来源是 R46 的 E 集合与 ④、R49 的交付门输出，都是本步方案里的机制；R50 是换词不是删码，净减为零。**先做完 R51–R53 再复测**：R51 删改提示词正文会动 `prompts_md`。复测后仍超的，如实列差额交用户签，不用压换行或删有效能力凑数。

## 3. 优化诉求的范围（本步内做，不开步骤 4，不扩整体治理）

### R54 · 维护者这一侧：规则层与机制层各修一处已有的东西（用户要求，本步内做）

根因在 `reviews/04` §6：规则（`AGENTS.md` §5.3）与探测器（M02）都在，缺的是覆盖。本步只补覆盖，不加新规则、不加新失效形态：

- **规则层**（`test/story/AGENTS.md`，维护者读，不进交付面）：§5.3 加一句正向原则——「说明位置、做法、格式时，以当前正确的状态为参照，直接给出它是什么、怎么做」；§8 自检对应加一行。不放反例。
- **机制层**（`check_failure_modes.py` 的 M02）：把它的实现补齐到它自己的定义「非当前信息进交付面」——词表加历史与替代叙述的字面（此前、早先、原先、曾经、上一版、旧的、退场、不再是、迁移期、改成了）与以错误为参照的否定（不在 … 下、不是在、不要去、不必去、别去）；扫描范围明确含脚本注释与提示词里的举例；豁免按语境登记且具名（产品动作、合同数据里的业务项、禁用词替换说法三处）。词表是兜底，漏了补词；判断归评审。
- 执行者 §8 自检与评审复核都跑它；R51 修完后 FAIL 0 是本步的收口条件之一。

范围到此为止：不做词表之外的治理，不改判定逻辑，不新增失效形态编号。

### R55 · 只处理已错或重复维护的事实

本步已把两处出过错的事实改成从数据渲染（acceptance 路径、项目事实路径）。其余手写的路径、字段、命令**不在本步改造成动态渲染**；只处理已经错过的或两处各写一份要人同步的，其它记入交付后的整体审查。

## 4. 七跑怎么看

`steps/03` §4 不变，另加一行：交付面「非当前信息」M02 零命中。验证顺序：先覆盖材料多路径（R52）、登记保留人工区（R47）、交付门后的选择（R49），再完成既定离线四项。七跑看图片处置、找路成本、返修次数是否改善；与交付无关的简化机会记录后续处理，不滚动扩大本步。

## 5. 返修后我核什么

- R51：`reviews/04` §2–§3 列的 16 处逐一看改后的句子，只留当前做法与理由。
- R52：六跑产物上 `check` 对 E 的缺行数等于材料数（10），不是路径数（13）。
- R53：`grep 闭环并归档 SKILL.md` 零命中。
- R54：AGENTS §5.3 那一句与 §8 那一行在；M02 词表与范围补齐，交付面 FAIL 0；豁免三处具名。
- R55：只核已错与重复维护的事实改了，没有顺手扩范围。
- 预算：R51–R53 之后复测；超额如实列差额，用户签。

**收口条件**：已登记的问题解决、正常路径无回归、遗留项明确列出——不是整套系统理论上已最简。

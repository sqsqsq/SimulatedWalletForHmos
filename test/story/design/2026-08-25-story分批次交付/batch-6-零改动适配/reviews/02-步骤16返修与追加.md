# 评审 · 批次 6 步骤 2「步骤 16 的返修与追加」

评审会话 2026-09-06。对象：三提交 `aa099100`（R33 / R34 / M02）、`db1f307a`（S5 / S6）、`eb61d11d`（S7），另含步骤 1 的 R39b 返修 `d8e92cb7`。方法：亲自复跑离线证据；对每条判据用反例与真实产物各探一次；看作者动笔前能不能拿到规则。

**结论：不通过，两处返修后过；另有一处要用户裁定、两个预算数要用户签。** 机制本身按方案落地且判得对；漏的是「规则送达」和「adapt 跟版」——都不是判据的错，是交付的两端没接上。

## 1. R39b（步骤 1 遗留）——通过

上一轮的两个反例（`details: {}` 而键名在围栏外；`details: |` 是提到键名的散文）现在都 FAIL；借别项键的仍 FAIL；正常 PASS、正常 FAIL、本项排在 YAML 末尾三种都 PASS。范围止于下一 `- id:` 或围栏，键由 `yaml-lite` 读出的 `details` 映射判。步骤 1 到此全部收口。

## 2. 步骤 2 逐项

| 项 | 我核到的 | 结果 |
|---|---|---|
| R33 | `strayProse` 按行数组逐行判；顺带跳过章末 `---`；夹具跑在 `fixtures/real-run` 的 CRLF spec 上并补了流程契约（否则 §9 判据整块不跑） | 通过 |
| R34 | `contractNames` 只收数据行（下一行是分隔行的是表头）；「没有 §9」与「§9 空」分开，后者逐条报；夹具两条 | 通过 |
| M02 | 交付面 `五跑/四跑/六跑` 零命中；词表「N 跑」补到十，bad 夹具验会红 | 通过 |
| S5 机制 | 身份由位置给（`§节 #序`），来源由围栏第一行自报；`diagramsNotCarried` 两处同一实现（SR→spec 在 `post_check`，spec→story 在 ⑫b）；报错带主题（小节标题 + 首层节点）指向功能；任务包 §4b 逐张给可粘贴围栏、不指定章；围栏按行传；读者审查第 5 问在 | 通过 |
| S6 | 合同 5.x `时机 / 方案 / 走向`、9.3 `回退动作 / 兼容性说明 / 可靠性说明`；金样与 README 同步、指纹更新 | 通过，见 Q1 |
| S7 机制 | ⑫c 两条：图连图、图前上一非空行是标题或图或节首；附录不看；任务包 §4「只引正文讲到的」与「未引用：<理由>」写法 | 通过 |
| S7 装置 | docx 10 张界面图：5 张该引（详情页入口、签约页、免密验证页、管理页、停用态）、5 张陪衬（v0.1 旧页、小屏截图、友商参考、运营管理台配置页、另一单需求页），每张陪衬在 docx 附图节写了为什么不属于本需求；流程图 PNG 退场；SR `design.md` 两张 mermaid（签约、管理） | 通过 |
| 离线 | 657 passed / 186 subtests；失效形态 70 条 FAIL 0；`adapt-scan --check`（自适配）通过；framework 差异仍是登记两文件 | 通过 |

## 3. 必修

**R41 · spec 作者拿不到「SR 的图要搬进 spec」这条规则。** SR→spec 这一环的规则只存在于 `post_check` 的报错文案（第 355 行「搬的时候围栏第一行写 `%% 图源 SR …`」）。`hooks/spec/author.md`、`phases/spec.md` §三、`templates/spec-sections.md` 都没有一句。`AGENTS.md` §4.1：报错指出既定条件未满足，**不是首次交付规则的渠道**；作者靠撞门禁才学会要求，就是作者输入不完整。story 那一环是齐的（任务包 §4b + `story-write.md`），spec 这一环缺。修：`hooks/spec/author.md` 加一段——SR/PRD 里的 `mermaid` 照搬进 spec 讲对应内容的那一节，围栏第一行 `%% 图源 SR §<节> #<序>`，文字自己写；`phases/spec.md` §三或 spec 任务包（`author.mjs` 本来就渲染 spec 阶段的任务包）列出 SR 里现有的图与可粘贴围栏，与 story 侧同一做法。

**R42 · adapt 没跟版。** 步骤 2 改了七个机制文件（`post_check`、`knowledge-use`、`story-build`、合同、`author.mjs`、`reader-review-task`、`verifier-report`）共 272 行，`manifest.version` 仍是 1.6.0。我拿步骤 1 收口时的树当目标跑 `adapt-scan --scan`：判态 `package_not_bumped`，「包与目标版本都是 1.6.0，机制指纹却不同」——1.5.0 加的守卫拦下了自己的包。修：升 **1.7.0**，头注按既有格式写行为变化（§9 两条判据不再误报；图的来源标记两处核；⑫c 图连图与图前无承接句；标签换词）；批次 5 `artifacts/intranet-upgrade-1.3.0-to-1.6.0.md` 改成 →1.7.0；`test_story_build` 的版本断言随之改。

## 4. 要用户裁定

**Q1 · 5.x 的三个标签核不核。** 合同里 5.x 是 `labels_draft_only: true`（草稿渲染、⑪ 不核），9.3 的三个标签 ⑪ 核。这个差别来自步骤 16 当时的裁定「标签段改顺序提示不核」，之后用户改口「标签可以固定，仅仅优化下」，`steps/02` S6 写的是「⑪ 照核『标签在』」。执行者保留了步骤 16 的状态。两种都行，请定：核（去掉 `labels_draft_only`，5.x 每个业务名节都要三个标签）或不核（维持现状，`steps/02` 改口径）。金样 5.x 各节现在都带三个标签，核也过。

## 5. 预算（两个数要签）

| 类 | 现值 | target | 说明 |
|---|---|---|---|
| hooks_mjs | 2827 | 2840 | **执行者把用户签的 2800 改成了 2840**，`approved_by` 仍写 WYK——§7.5 允许执行者回方案改预算并写原因，但签字是用户的；请用户补签或改数 |
| scripts_mjs | 2331 | 2350 | 在签内 |
| 总量 | 9115 | 9100 | 超 15 行（⑫c +32），在峰值 9300 内；R41/R42 再加约 +20（任务包 SR 图一节、头注不计）；建议签 **9200** |

## 6. 返修后我核什么

- R41：`hooks/spec/author.md` 有那一段；spec 任务包在带 SR 图的 feature 上列出围栏；`grep 图源 phases/spec.md hooks/spec/author.md` 命中。
- R42：`manifest.version` 1.7.0；拿步骤 1 的树当目标 `adapt-scan --scan` 判 `upgrade`；本仓自适配 `--check` 通过；内网清单标题 1.7.0。
- Q1 按裁定核合同与 ⑪。
- 预算按签字核 `mechanism-budget.yaml` 的 `approved_by` 与 reason。

修完进六跑；六跑判据见 `steps/01` §5 与 `steps/02` §3。

## 7. 用户裁定（2026-09-06）

- **Q1 → 核。** 5.x 与 9.3 一致：合同去掉 5.x 的 `labels_draft_only`，⑪ 核每个业务名节都带「时机 / 方案 / 走向」；`steps/02` 口径不变。**并入返修**，与 R41、R42 一起做，记为 R43。
- **预算 → 按建议签。** hooks_mjs 2840、总量 9200，已写进 `mechanism-budget.yaml`。

返修清单：R41（spec 作者侧 SR 图规则）、R42（manifest 1.7.0 + 头注 + 内网清单）、R43（5.x 标签 ⑪ 核，夹具一条：缺一个标签即报）。

## 8. 返修评审（2026-09-06，工作区未提交的 R41 / R42 / R43）——**不通过，两处小修后过**

执行者的返修还在工作区没有提交（11 个文件）。我按工作区现状核。

| 项 | 我核到的 | 结果 |
|---|---|---|
| R41 | `hooks/spec/author.md` 加一段「上游画过的图照搬进来」；`phases/spec.md` §三首段加同一规则并指向任务包；`author.mjs` 把图那一节做成两环同一渲染——§4b「SR 里的图（搬进 spec）」、§4c「spec 里的图（搬进 story）」，夹具一条两环各一节 | 规则送达了；但见 F1 |
| R42 | `manifest.version` 1.7.0，头注四句按既有格式；内网清单改名 →1.7.0、表 2 改口径；拿步骤 1 收口的树当目标 `adapt-scan --scan` 判 `upgrade`；本仓自适配 `--check` 通过 | 通过 |
| R43 | 合同去掉 5.x 的 `labels_draft_only`，`form_note` 删相应句；⑪ 对 `labels` 一律核；夹具两条（缺一个标签点名、三个都在通过）。金样 5.5「云侧调用时机」那段说明并入章首段——设计 §2 里第 5 章就是「章首一段 + 图 + 5.1 主路径 + 5.x 分支与恢复路径」，没有说明节，金样这一改是回到设计，不是迁就判据。**我上一轮说「金样 5.x 各节都带三个标签，核也过」是错的**：5.5 没有标签，我只 grep 了带标签的行 | 通过；见 F2 |
| 离线 | 660 passed / 186 subtests；预算 9129 ≤ 9200，hooks_mjs 2833 ≤ 2840；framework 差异仍是登记两文件 | 通过 |
| 失效形态 | **FAIL 2**：`A03-author-channel-broken`、`S09-flow-chapter-flat` | **不通过** |

**F1 · A03：`hooks/spec/author.md` 66 行，超过上限 60。** 规则「author.md 只做索引，不做教材」是批次 5 定的（`check_failure_modes.py` 第 1520 行）。R41 加的六行把它顶过了。修：author.md 只留两行索引（「上游的图照搬进来，围栏首行写来源标记；哪几张、围栏原文见任务包 §4b」），写法细节留在 `phases/spec.md` §三与任务包——那两处已经有了，不必三处都讲。

**F2 · S09：正例样本还是旧标签。** `check_failure_modes.py` 第 2043 行的正例写的是「**什么时候发生**」一个标签——S6 换词时它没跟（当时 5.x 不核，所以没红），R43 一核就拦。修：正例改成「时机 / 方案 / 走向」三段。这是夹具跟合同，不是判据的问题。

**F3 · 提交。** 返修按一个提交落，提交说明写 R41 / R42 / R43 与 F1 / F2；提交后失效形态须 FAIL 0。

修完这三处我核：`check_failure_modes.py` FAIL 0；`wc -l hooks/spec/author.md` ≤ 60；S09 正例通过；其余不再重跑。

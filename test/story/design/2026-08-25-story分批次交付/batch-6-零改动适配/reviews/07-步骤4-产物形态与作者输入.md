# 评审 · 批次 6 步骤 4「七跑后——材料清单归位、story 自己的图、附录与交付章的形态、作者输入补三处」

评审会话 2026-09-06。对象：三提交 `2aee7914`（R59 / R60）、`5a847855`（R61–R63）、`73b2d735`（R56–R58 / R64 / R65）与头注提交 `a59355b8`。方法：亲自复跑离线四项；拿七跑的真实产物探 ④、E、附录 C 的投影与三章草稿的渲染；逐项核方案 §1 的消费者。

**结论：机制与形态都落地，三处接线没接到直接消费者，返修后过；预算 9385 恰好用满方案 §4 的上限，请用户签。**

## 1. 逐项核对

| 项 | 我核到的 | 结果 |
|---|---|---|
| R59 清单 | 七跑产物上跑 `check`：E 报「少了一份材料：inbox/交通卡自动充值.docx」，五张 UX 图各报「列了不是初始资料的东西」；新骨架的 E 种子恰好三份正文加原件四行，无图 | 通过 |
| R59 取舍 | **任务包 §4 的取舍命令写「<上面那一行的路径>」，上面那一行是相对 `AR/story.md` 的展示链接（`../assets/…`），命令要的是相对工程根**——照抄必错，作者又要去翻脚本。其余：`--caption-image … --unused` 写入说明库，值为 `{caption, unused}`；`materials.refresh` 之后 `unused` 仍在条目里；`--used` 只清 `unused`；旧字符串按说明升格（夹具 `test_a_bare_string_is_read_as_a_caption`）；改说明不清取舍、清取舍不动说明各有夹具。④ 对未引用且未登记的图报「没被引用，也没登记」并给命令 | 见 R67 |
| R60 | 读者审查第 6 问「章首那张图讲清了端到端过程、关键状态与全部分支去向吗」；写作页第 228–239 行三种选法与「不是与谁重不重样」；任务包在可粘贴围栏前说「章首那张先按评审者要看的过程画」。没有「无标记 = 原创」判据 | 通过 |
| R61 | 七跑产物 `reopen` 后 `project`：C 一张 `模块 \| 本单怎么动 \| 依据`，五个模块各一行、§9.5 一行「不涉及」，Scope 的说明原文在表下引一次，机器区内零 `{{` | 通过 |
| R62 | 新骨架 09 草稿「交付物」是四列表头 | 通过 |
| R63 | 新骨架 05 草稿三段各带占位提示；形态说明写了「分支多步可有序列表、多方可配时序图」 | 通过 |
| R56 | 相对需求目录的写法被拒，报错含「路径相对工程根，例如 `doc/features/<需求名>/assets/<文件名>`；`--feature` 那次导入打印出来的路径可以直接复制」；`--register-ux` 与 `--caption-image` 同一基准 | 通过 |
| R57 | 任务包 §2 给一条最小示例，`knowledge_rule` 与 `id` 平级在 `criteria` 条目下。**夹具只核缩进与 `post_check.mjs` 源码里出现键名**，没把示例交给两侧消费者跑 | 见 R68 |
| R58 | `APPENDIX_HEADING` 认 `## 10. 附录` 与 `## 附录`；P07 / P08 夹具带编号；失效形态 70 条 FAIL 0 | 通过 |
| R64 | `decide` accepted 分两种下一步（收件箱有原件 → 直接导入；已导入指纹变 → 重跑 round）；`status` 的 `import_and_reanalyze` 同样二选一；夹具三态 | 通过 |
| R65 | `status` 导入后那一步写「用新增的材料重新盘点……材料够了就直接进需求分析，不再问」；`supplement` 选项缺 `missing` / `why` 被拒，非补料选项不受限；不比历史、不比 key。**但校验挂在 `decide` 读侧车时（`read_gate_options` 第 426 行），`material_gate_stops`（第 898 行）只看侧车在不在**——status 先说停、用户答了、`decide` 才拒 | 见 R66 |
| 交付面文字 | 新增文本按历史词与否定指路扫，零命中；机器区无作者占位 | 通过 |
| 离线 | 693 passed / 189 subtests；失效形态 70 条 FAIL 0；`adapt-scan --check` 通过；framework 差异仍是登记两文件；manifest 1.7.0 头注补齐 | 通过 |

## 2. 预算

实测 9385，与方案 §4 的上限（测算值 9285 + 100）相等；scripts_py 1540 用满。差额来源执行者逐笔写在预算文件 reason 里：R59 说明库换结构要把读、写、升格与三处分发一起改（方案估 +18，实到 +64），其余是送达文本。`mechanism-budget.yaml` 已按 9385 写，**请用户签**；不签则由执行者在 R59 的送达文本里净减。

## 3. 返修（外部第二十五轮三条，我复核成立）

**R66 · 第 2 轮的侧车校验要在 `status` 决定停不停之前。** `material_gate_stops` 复用 `read_gate_options` 的同一套校验：侧车不完整（补料选项缺 `missing` / `why`）→ `next` 给「补齐侧车」并说缺什么，不给 `await_gate`；完整才停。同一套校验只写一处。夹具：第 2 轮缺字段的侧车，`status` 不是 `await_gate:material_scope`，输出含缺的字段。

**R67 · 任务包 §4 直接渲染能跑的取舍命令。** 展示用的引用串保持相对 `AR/story.md`；命令参数另起一行按工程根渲染（`doc/features/<名>/<清单里的路径>`），每张图一条完整命令，不写「上面那一行的路径」。夹具：任务包里每张图的命令路径以 `doc/features/` 开头且文件存在。

**R68 · R57 的示例交给两侧真实消费者跑一次。** 夹具把示例写进临时 feature 的 `acceptance.yaml`（连同一条 applicable 的知识判断），跑扩展 `post_check` 认到桥接、跑 framework 的 `check-acceptance`（或 `harness-runner --phase spec` 的那一项）不报条目形状错；两侧任一不认即红。不新设施，只是把方案写的验证做到。

## 4. 收口判定

十件事的机制与形态都落地并亲核；正常路径无回归（693 绿、失效形态 FAIL 0）。三处接线（校验时点、命令路径、示例的真实消费）没接到直接消费者，修完再收口。返修复核按作者的实际动作走一遍：第 2 轮写一份缺字段的侧车跑 `status`；照任务包 §4 的命令原样执行；把任务包里的示例写进 `acceptance.yaml` 跑两侧消费者。

下一步：R66–R68 一个提交；通过后八跑，判据见 `steps/04` §3。

## 5. 返修复核（`f5106a37`）——R66、R68 通过；R67 还差一处，改掉即收口

按作者的实际动作走了一遍：

| 项 | 我做了什么 | 结果 |
|---|---|---|
| R66 | 七跑产物 `reopen` 到第 2 轮，写一份缺 `missing` / `why` 的材料侧车跑 `status`：`next` 是 `fix_gate_options`，输出「第 2 轮的『supplement』缺 missing（还缺什么）……」，不是停等；补齐字段再跑：`await_gate:material_scope`。校验合成 `material_gate_state` 一处 | 通过 |
| R67 | 任务包 §4 每张图下面一条完整命令，路径 `doc/features/AR90006/assets/…`，展示链接仍相对 story.md。**把围栏里的命令原样交给 bash 跑**：`import_sources.py: error: unrecognized arguments: \`，第二行 `--caption-image: command not found`——渲染出来的行尾是两个反斜杠（`author.mjs` 第 177 行模板里写的是 `\\`，在模板字符串里就是两个），bash 把第一个当转义、第二个当字面参数，续行没接上 | **R67b** |
| R68 | 夹具把示例写成临时 feature 的 `acceptance.yaml` 加一条 applicable 判断，跑真的 spec `post_check`，桥接认到；framework 侧用它自己那份 yaml 解析核层级。framework 里消费 acceptance 条目字段的是 `check-exit` / `check-ut` / `check-testing`（`ut_layer`、`device_focus`）与 `check-plan`，spec 阶段没有独立的 acceptance 检查可单独跑；示例不带那几个字段，不会触发它们的规则。按「不新设施」这样够 | 通过 |
| 离线 | 697 passed / 189 subtests；失效形态 70 条 FAIL 0；`adapt-scan --check` 通过；framework 差异仍是登记两文件 | 通过 |
| 预算 | 实测 9402，执行者按实把 target 写成 9410（scripts_py 1560、hooks_mjs 2895），**待用户签** | 待签 |

**R67b**：模板里的续行改成渲染一个反斜杠（或整条命令一行，不续行）；夹具改成把围栏里的命令**原样交给 shell 跑**并断言 `ok: true`——现有夹具只核路径前缀与文件存在，抓不到这个。

改掉 R67b、那条夹具绿，步骤 4 收口；预算 9410 签后进八跑。

## 6. R67b 复核（`6567f1cf`）——续行已修；收口再等 §7 两处

- 七跑产物上取任务包 §4 围栏里第一条命令，原样交给 bash：`{"mode": "image-unused", "ok": true, …}`。命令整条一行，无续行。
- 夹具改成把围栏里那条命令 `shell=True` 原样跑并断言退出 0、`ok: true`、无续行反斜杠（`test_author_task_package.py` 第 254–266 行）。
- 698 passed / 189 subtests；失效形态 70 条 FAIL 0；framework 差异仍是登记两文件。
- 预算实测 9401，target 9410（执行者按实写），**待用户签**。

收口条件见 §7。

## 7. 外部第二十六轮两条（我复核成立）——两处直接验证没闭合，改掉即收口

**R68b · 夹具的 `post_check` 被提前返回，业务检查没跑。** `test_the_extension_side_reads_the_rule_out_of_the_example` 只写了 `acceptance.yaml` 与 `knowledge-use.yaml`，没写 `spec/spec.md`；`post_check.mjs` 第 350 行读不到 spec 就 `skipped` 返回，夹具断言的是「没有某条错误文字」，跳过也过。改：补齐最小 `spec.md`（带 §9 与 §10 生成区，让判据走到桥接那一步），并加对照——示例原样：通过；删掉 `knowledge_rule` 或写错编号：那条检查报「没有对应验收条目 / 指向了 spec 里没有要求的条目」。断言正向用「检查执行了且通过」，不用「没有出错字样」。

**R67c · 渲染的命令没给路径加引号。** `author.mjs` 第 179 行 `--caption-image ${featureDir}/${p}` 裸拼；图名带空格（`page one.png`）时 bash 拆成两个参数，`unrecognized arguments: one.png`。改：feature 与路径参数按目标 shell 引用（双引号并转义内部引号；Windows 的 cmd 与 bash 都认双引号），R67b 那条「原样交给 shell 跑」的夹具加一张带空格文件名的图。

两条都是已有返修的直接验证，不加机制。改完我核：对照夹具红绿各一；带空格的图原样跑得通。

**预算**：用户 2026-09-06 签 9410，已写进 `mechanism-budget.yaml`（实测 9401）。

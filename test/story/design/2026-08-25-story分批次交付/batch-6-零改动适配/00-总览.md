# 批次 6 · 零改动适配

**起点**：批次 5 收口于 `framework/` 带 18 处具名改动的状态（6 处 opencode verifier + 12 处作者入口），
靠 `drift_allowlist` 放行、靠 `framework-patch.yaml` 让 adapt 带着走。
2026-09-05 上游发布 `0143e21`（仍 3.0.0），把这 18 处的**理由**都消掉了：

- `plan d2f7a9c4` 把 verifier 报告的真源从「hook 发布的 canonical JSON」改回「**调用方写出的 MD**」，
  `blocked` 态与 `verifier_capability`（transport / publisher / modes）矩阵整体删除，
  claude 自己那份 945 行发布插件一并删掉；
- `lifecycle-hooks-schema.yaml` 订正 `on_context_load` 的语义：**只在装配 verifier ai-prompt 时消费，
  从不进入作者动笔前的上下文**——这正是我们提案 §1 的论点，上游承认并写进了 schema；
- 行为规约新增第 8 条：作者动笔前自己去读宿主扩展登记的作者输入。

**本批次的目标**：`framework/` 与上游逐字节一致（0 改动），批次 5 建起来的能力一样不少地
落回 extension 与实例侧承载。

## 用户裁定（2026-09-05）

1. **framework 不改逻辑**：允许且只允许 opencode 的登记两处（`adapter.yaml` 一行布尔 + 子代理定义模板），按 drift 具名放行，不交上游（2026-09-05 补充裁定，替代最初的「0 改动」）；
2. `.opencode/` 下正常适配，**继续用 opencode 做本仓 story 分支的 CLI 测试**；
3. extension 适配，**支持批次 5 已完成的全部功能**。

补充裁定：内网用 codex，不用 opencode；`.opencode/` 是本仓测试装置，`story-adaptation` 不带它，
也不带 framework 补丁。

## 步骤

| 步 | 文件 | 一句话 |
|---|---|---|
| 1 | [steps/01-对齐上游与能力落回.md](steps/01-对齐上游与能力落回.md) | framework 同步到 `0143e21`，drift 收到 opencode 登记 2 文件；作者输入改由 extension 自己的命令送达；读者审查留在 framework verifier，落盘核对改读 MD 并挪到归档门；插件与补丁退场 |
| 2 | [steps/02-步骤16返修与追加.md](steps/02-步骤16返修与追加.md) | 批次 5 步骤 16 的返修 R33 / R34 / M02 与六跑前的追加 S5–S7（原批次 5 `steps/17`，2026-09-05 迁入）；依赖步骤 1 |
| 3 | [steps/03-六跑后的作者输入与交付路径.md](steps/03-六跑后的作者输入与交付路径.md) | 六跑后：acceptance 路径、S7 单向、build 在登记前、画像路径与禁用词表送达、闭环后归档 / plan 二选一 |

## 批次完成条件（不是六跑一次就算）

| 条件 | 在哪验 |
|---|---|
| `framework/` 与物化目录同 `origin/main` 恰好差 opencode 登记两文件，与 `drift_allowlist` 相等 | 离线，步骤 1 §3.1 的命令 |
| opencode 装置上 story 链路成立（作者输入、story 成文、读者审查、spec 闭环） | 六跑 |
| codex 上 spec 阶段 verifier 与知识语义审查成立 | 内网第一次真实 spec 闭环，按里程碑验，不在外网伪造 |
| 换入口后 plan 及之后各阶段的作者实际取得 `author.md` | 内网第一次进到该阶段时看 `key_inputs_read`，按里程碑验 |

六跑只能证明第二条；没有后两条不写「批次 5 全部能力已验证保留」。

## 与批次 5 的关系

批次 5 收口于步骤 16（已实施、评审不通过）。它的返修与追加原写在批次 5 `steps/17`，2026-09-05 迁为本批次步骤 2：
framework 对齐上游必须先做，步骤 2 的判据与预算都以那时的仓为起点。原 R35（真链路夹具写进 `framework/`）
验的是 opencode 发布插件与 framework 的 request schema，插件在步骤 1 退场，R35 随之消失。

评审记录在 [reviews/](reviews/)，进展在 [STATUS.md](STATUS.md)。

# 评审会话交接（Claude，2026-09-07 更新）

> **最新交接：用户 2026-09-07 已确认四批次需求结束交付，批次 5、6 的后续收敛一并结束。** 以下历史“下一步”不再作为执行入口。当前工作转入 [新需求讨论总览](../../../spec/2026-09-04-会议转写修正需求输入/00-讨论总览.md)，测试证据和待确认评分仍按原记录保留。

> 给上下文压缩后的本会话读。只记「现在在哪、规则是什么、下一步做什么」，事实与证据只给指针。
> 读完本文件再读本目录 `STATUS.md` 的状态表与事件日志最后 15 条。批次 5 的交接件在 `../batch-5-实跑问题诊断/99-评审会话交接.md`，规则 1–14 在那里，本文件不复述。

## 1. 我的角色

独立评审者，不实施、**不提交**。执行会话按 `steps/NN` 实施并提交；我从实际 diff 与亲自复跑下结论，写进 `reviews/NN-*.md`，更新 `STATUS.md`，评审文件与预算签字都留在工作区由执行者一并提交。用户是唯一裁定者。用户会转来「其他人的评审意见」，要独立核实每一条再答，不默认认同。

## 2. 本批新立的规则（编号接批次 5 的 1–14）

15. **评审不提交**：只改文件；`git add` / `git commit` 只由执行者做。
16. **原则不从单个例子推**：写规则先问「换一种对象还成立吗」，不成立就是场景。
17. **指引只写正向，反例不进 AGENTS 与交付面**；方案与评审文件里也不记我自己的错误叙述，只留结论；教训写在本文件。
18. **给办法先看已有的东西**：规则在吗、探测器在吗、为什么没起作用；答完再决定加不加。对着症状加失效形态、加规则句、加清单项是打补丁。
19. **核接线按「作者照着做一遍」走**：先后顺序（status 说停是否在校验之后）、参数基准（展示链接 ≠ 命令参数）、夹具跑没跑真实消费者（写了 spec.md 吗、断言的是执行了还是没出错字样）、渲染出来的命令原样交给 shell 跑一次（含带空格的文件名）。文本在、夹具名对，都不算核过。
20. **判据拦金样即判据错**（P07/P08 不认 `## 10. 附录` 那次）；**机器区吐的内容也归交付面红线管**——投影写出违规内容作者改不动，只能去翻判据，那是学习隐藏规则的入口（八跑、九跑现场）。
21. **预算按用户口径**：上限 = 测算值 + 100；实测超了如实列差额交用户签，不压换行、不删有效能力凑数；提示词正文计入 `prompts_md`，脚本注释不计。
22. **交付面每一句以当前正确状态为参照**：说它是什么、怎么做、为什么；不以错误、旧做法、被替代的东西、不该去的地方为参照。否定句只出现在规则本身就是禁止的地方。M02 是兜底，判断归评审按 §5.3 读。
23. **机制只判结构与字面事实**：「补上了没有」「属不属于本需求」「讲清没讲清」归模型与独立审查；不用 key 相同、无标记、行数这类代理。
24. **判据拦金样，先问金样含不含后加的需求**：金样冻结在某一天，之后用户裁定的保障（如 spec 图承接 S5）金样没跟；这种拦是金样缺登记，按用户裁定补金样，不撤判据。规则 20 只对「金样已含该需求」成立。

## 3. 状态（2026-09-07 凌晨）

| 项 | 状态 |
|---|---|
| 步骤 1 对齐上游、opencode 登记 | 通过收口（`reviews/01`）。framework 与 `origin/main` 只差 opencode 登记两文件，`drift_allowlist` 两条；扩展 1.7.0 |
| 步骤 2 步骤 16 返修与追加 | 通过收口（`reviews/02` §11） |
| 六跑 | 已评审（`reviews/03`）；三轴建议 82/90/55 **用户未定** |
| 步骤 3 作者输入与交付路径 | 通过收口（`reviews/05` §6–§7） |
| 七跑 | 已评审（`reviews/06`）；三轴建议 88/90/70 **用户未定** |
| 步骤 4 产物形态与作者输入（R56–R65） | 按实现收口（`reviews/07` §8，C6–C8 复核通过）；设计四条 R60 / R61 / R64 / R65 由 `steps/05` 重推 |
| 步骤 5 十跑与内网反馈后的重推（R71–R80） | **收口**（`reviews/09` §9）：三提交 + 返修 `ae4a3f9a` 全部按三条链行为核通过；预算 9632/9650 |
| 八跑 `story-suite-20260906-233000` | 宿主判死循环 stop；现场记 `artifacts/八跑-机器区与红线互斥.md`（附录 C 投影写 `spec §9.5` 撞 ⑨ 红线，作者删了又被重投写回）→ R69 |
| 九跑 `story-suite-20260907-004500` | 卡在同一节第二处（机器区表后散文撞 ⑫「表外零散文」）→ R70；现场记同一文件 |
| 十跑 `story-suite-20260907-020000` | 已评审（`reviews/08`）；评分卡已写在该 suite 的 `evaluation/scorecard.md`（建议 90/65/88，**用户未确认**） |
| 预算 | 步骤 4 签 9410（实测 9404）；步骤 5 用户签 total 9650（2026-09-07 改签，原 9557），分类由执行者按实测写，执行者随返修提交写进 `mechanism-budget.yaml` |

## 4. 压缩后先做

0. 步骤 5 已收口。下一步是十一跑（用户按 `TEST.md` 启动）：按 `steps/05` §5 判据评，读数与七跑、十跑对，读源码按 framework / 扩展拆计；评分卡由评审写在该 suite 的 `evaluation/scorecard.md`。仍待用户：六跑 82/90/55、七跑 88/90/70、十跑 90/65/88 三轴分。待取证：内网 verifier 首轮报告。
1. 读 `STATUS.md` 状态表与最后 15 条日志；`git log --oneline` 看 `f520e069` 之后有没有新提交。
2. ~~评审 C6–C8~~ 已做（`reviews/07` §8）。原清单：
   - R67c：任务包 §4 命令带引号；拿七跑产物跑 `author.mjs --feature AR90006`，把围栏里的命令原样交给 bash（scratchpad 写 `.sh` 再跑），再造一张带空格的图验一次。
   - R68b：`test_author_task_package.py` 那条桥接夹具是否写了 `spec.md` 与 §9/§10 生成区；断言是否正反一对（示例通过、编号错就报「没有对应验收条目」）。把 spec.md 拿掉复跑应变红。
   - R69/R70：附录 C 投影的「依据」列不再写 `spec §`；说明原文进表成行；守卫对机器区逐节跑 `lint-rules` 的悬空引用与仓内路径扫描、表后散文即红。七跑产物 `reopen` 后 `project`，看 C 的形状；把 `spec §9.5` 或表后散文塞回去应红。
   - 这两条的根因类别（机器区内容撞交付面红线）是否已进 `AGENTS.md` 的正向原则，或只是修了这两处——按规则 18 判要不要动规则。
3. ~~评审十跑~~ 已做，见 `reviews/08`；候选返修在其 §4.1–§4.3、§4.5，用户未裁定。原判据：`output/story/story-suite-20260907-020000/…`，按 `steps/04` §3 判据（材料清单只有初始资料；章首那张图讲给评审者；附录 C 一张表、9.4 四列、5.x 三段；没有为路径与条目形状翻脚本；补料后没有无效重复询问）；`measure_run.py` 读数与七跑对（31.7 分钟、读源码 29、读规则 44、上下文 342K、停等 3）；三轴建议分与执行者评分卡逐项对，读数不实要点名。
4. 每次都跑：`pytest -n auto --dist loadscope`、`check_failure_modes.py`（`PYTHONIOENCODING=utf-8`）、`measure()`、`git diff --name-only origin/main HEAD -- framework/ …` 应恰好两文件、`adapt-scan --scan/--check --package . --target .`（跑完删 `doc/extensions/.adapt-1.*`）。
5. 待用户定：六跑 82/90/55、七跑 88/90/70、十跑 90/65/88；八跑与九跑不计分（机制缺陷，宿主 stop）。

## 5. 文件地图

- 方案：`00-总览.md`、`steps/01`–`04`
- 评审：`reviews/01`（步骤 1，§7–§8 实施评审）、`02`（步骤 2）、`03`（六跑）、`04`（非正向表述全量排查，§6 是「修已有的东西」的结论）、`05`（步骤 3）、`06`（七跑，§8 是用户五条）、`07`（步骤 4，§1–§7）
- 现场：`artifacts/R50-过拟合排查表.md`、`artifacts/八跑-机器区与红线互斥.md`（含九跑）
- 契约：`test/story/AGENTS.md`（§5.3 交付面写法、§7.5 预算、§8 自检）、`test/story/TEST.md`（§3.0 回话规约、§8 度量、§10 三轴）
- 度量：`test/story/scripts/measure_run.py <run 目录>`；评分卡在各 suite 的 `evaluation/scorecard.md`（执行者写，读数要复核）
- 内网：`../batch-5-实跑问题诊断/artifacts/intranet-upgrade-1.3.0-to-1.7.0.md`（framework 取 `0143e21`，codex 已登记 verifier）
- 记忆：`~/.claude/projects/E--Project-SimulatedWalletForHmos/memory/`（`reviewer-never-commits.md`、`budget-is-a-symptom-detector.md`）

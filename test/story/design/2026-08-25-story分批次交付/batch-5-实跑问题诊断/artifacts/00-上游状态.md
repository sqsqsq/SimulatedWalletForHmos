# 交上游的两件：都已落地，补丁与提案退场

2026-09-05 上游 `0143e21`：

| 原提案 | 上游怎么处理的 | 现状 |
|---|---|---|
| 01 opencode verifier 发布器 | **发布器整体删除**——报告改由调用方原样写出，`verifier_capability` 矩阵换成布尔 `verifier_subagent`。提案的对象不存在了 | 本仓自己登记 opencode 的 `verifier_subagent`（两个文件，长期 drift，不交上游）；整理实证提交上游请其入册，是另一件事 |
| 04 作者取本阶段要求的入口 | **上游订正了语义**：作者事件只在装配 verifier 上下文时消费，从不进作者动笔前的上下文；行为规约新增第 8 条，作者动笔前自己去读 | 扩展改走自己的入口（`author.mjs --feature`）与三处指引，不占 `provides.knowledge` |

`hooks-dispatcher.ts` 的来源标识仍是 `path.basename`（提案第 3 点上游未采纳）——作者事件退场后本扩展不再依赖它。

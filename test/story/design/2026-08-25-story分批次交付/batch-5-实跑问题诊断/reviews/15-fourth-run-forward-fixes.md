# 步骤 15（四跑后的正向修正 F1–F9）· 独立评审（Claude，2026-09-05）

对象：`a136076d`（裁定落盘）、`e71cd58d`（F3）、`d88f9b31`（F1/F2/F7/F8/F9）、`dda5c4d7`（F4/F5）、`195acdb4`（F6）。
全部 diff 亲自读过；离线复跑 621 绿（并行 26 s）、失效形态 70 条 FAIL 0、委派 8；`framework/` 与 main 仍恰好 18 文件差异。
用户这一轮要的不只是逐项通过，而是**从整个批次 5 看**：批次 4 的病有没有回来——脚本有没有替模型做决定、模型有没有在干机械活。

> **口径（2026-09-05 更正）**：`AGENTS §7.5` 写明「数字只是围栏，不表达质量；表达质量的判据仍是 §4.2 与已登记的确定性失败清单」。本文 §1 的规模表只作围栏读数，结论一律按 §4.2「模型与脚本按能力分工」逐段问「这里做决定的是谁」得出；我初稿把规模当结论、按行数开删，是错的，§7.1 已逐条更正。

## 1. 批次 5 全程的规模（同一把尺子量每个提交：HEAD 的分类与去注释算法）

| 时点 | 代码行 | 总行数 | 字符数 | story-build 判据块 |
|---|---|---|---|---|
| 批次 3 收口 | 6316 | 9009 | 299K | — |
| 批次 4 收口 = 批次 5 起点 | 8819 | 12904 | 430K | 21 |
| 步骤 10 收口 | 7710 | 11286 | 373K | 15 |
| 步骤 11 退场 | 7787 | 11393 | 377K | 15 |
| 步骤 12 收口 | 8256 | 12021 | 399K | 15 |
| 步骤 15 落地（HEAD） | 8548 | 12396 | 418K | 17 |

按类别（代码行）：

| 类别 | 批次 4 收口 | 步骤 11 退场 | HEAD | 退场后增量 |
|---|---|---|---|---|
| scripts_mjs | 2701 | 1716 | 1978 | +262 |
| scripts_py | 1049 | 1182 | 1400 | +218 |
| hooks_mjs | 2402 | 2403 | 2683 | +280 |
| prompts_md | 2006 | 1838 | 1748 | −90 |
| data | 661 | 648 | 739 | +91 |

读法（围栏读数，不是质量结论）：

- 对批次 4 的起点，批次 5 净减 3%；退场那一步删掉的是逐单元系统与 13 个 checker——那正是批次 4「脚本替模型判语义」的实体，删它们是对的。
- 退场之后回长 761 行。按 §4.2 逐段看这 761 行是谁在做决定：任务包渲染、骨架从 spec 与登记数据派生、caption 真源、材料关卡按事实判停——都是脚本做「枚举、渲染、状态记录」，决定仍在模型（归类、范围、知识判断、决策、写章）；判据只加了 ⑪（表槽位存在）与 ⑫b（集合包含），两条都只测机械事实。**没有一段是脚本替模型判语义**，所以这 761 行的增长不是批次 4 的形态。
- 预算 target 一日三抬到 8600 是用户所签，签的理由正是要把这次增长拿出来审——审的结果如上。围栏的作用已经起到，数字本身不再是问题。

## 2. 批次 4 的病回来了没有——按审计的十个根因逐条看 HEAD

| 根因 | 现状 | 判 |
|---|---|---|
| RC1 用代理指标替代目标 | ⑪ 只核槽位存在与首列锚，⑫b 只核标识集合，`semantic_proxy` 仍 0；没有字数、数量、相似度 | 没回来 |
| RC2 脚本判语义 / 模型做机械活 | 机械活这一半明显退了：附录五节、术语起始行、流程图、图引用串、验收键都由骨架与任务包给出，作者不再重打。脚本判语义这一半有一处边缘：`specTerms` 用「权威模块在 in_scope」决定哪些 spec 术语算业务词——这是脚本替作者判「什么词该进读物」。用户裁定只作起始行、作者可删，风险低，但它是同类判断里唯一一条，记着别再长 | 基本没回来，一处边缘 |
| RC3 要求失败后才送达 | 任务包现在给形态、槽位、引用串、验收键；骨架给打底 | 已关 |
| RC4 作者与裁决者不独立 | verifier 独立，插件发布；未变 | 已关 |
| RC5 多真源 | 形态真源收成合同一份，模板退场。剩一处轻度重复：`story-write.md`「什么内容用什么形态」按内容类型讲，合同 `form.note` 按章讲——一个是原则一个是实例，暂不冲突，但改一处要看另一处 | 轻度 |
| RC6 知识消费 | 未动 | — |
| RC7 framework 协作契约 | 3.0.0 同步后 18 处改动有声明、有补丁、有 allowlist | 已关 |
| RC8 验证系统证明不了行为 | **回来了一次**：F7 的派生在夹具（LF）上全绿，在四跑真实的 `spec.md`（CRLF，opencode 在 Windows 写的）上术语与改动边界打底为空——见 §3。「离线绿 ≠ 行为证据」正是审计 RC8 的形态 | 回来了 |
| RC9 追加为默认 | 判据只加两条、删了模板与两个函数，替代关系逐项写了；预算 target 抬到 8600 是用户为审视这次增长所签，增长逐段核过都是脚本做枚举、渲染、状态记录 | 没回来 |
| RC10 证明量线性增长 | 无 | 没回来 |

一句话：**批次 4 的病没有回来，回来的是一个苗头**——夹具与真实产物脱节（RC8），它是批次 4 §8.2「完成判据偏向新检查已通过」的入口，R1/R11 堵它。

## 3. 返修与确认（清单以 §7 为准）

**R1 · F7 派生在真实 spec 上为空。** 把四跑工作区的 AR90006 复制出来、删掉 story.md、契约退回 complete，跑 `skeleton`：流程图复制到了、附录 D 15 行齐、A/B 两表齐；**术语章零行、改动边界只有 §9.5 的依赖表、没有 Scope 的两行**。原因是 `scopeList` 的正则要求 `:` 后紧跟 `\n`，而那份 spec 是 CRLF；`specSection`、`pipeTables` 都按 `\r?\n` 切，独它没有。修法一处：`specText` 读入时行尾归一（框架 `verifier-request.ts` 对 ai-prompt.md 就是这么做的），不在各处正则里补 `\r?`。**夹具要用真实产物的形态**：至少一条用 CRLF 的 spec 跑派生，否则五跑仍会看到「术语从零写」。

**C1 · 预算 8600 的签字。** 用户 2026-09-05 确认是自己签的，并说明签这一笔正是发起「全批是否膨胀」质询的原因。**8600 定为批次 5 的最终 target，本批不再抬**；步骤 15 实测 +292 对方案估 +35～65 是四倍以上的低估，下次估算要按「派生一行、核对一行」各自报，不合报。

## 4. 逐项核过的机制事实（通过；R1 与 R12 两处例外见 §3 与 §7）

| 项 | 核到什么 |
|---|---|
| F3 | 侧车改 `{gate, options}`，`sidecar_shape` 同步；`material_gate_stops` 只看轮次与本级侧车；`supplement_has_material` 两条路（inbox 未并入 / 指纹变）；后两级 decide 拦别级侧车；八条夹具含「补料轮走到第二级 decide 成功」，正是我探针那条 |
| F1 | 合同每章 `form`（primary / note / tables / tables_when / diagram / sections），`form_note` 把「不核行数、不核表的总数、不核槽位之外」写死；⑪ 一条判据读数据；页面状态表槽位按用户裁定撤掉；必有列收成首列锚；小节名包含匹配 |
| F2 | 任务包给 `![caption](../assets/…)` 原样串、每章形态与机器核槽位、applicable → `knowledge_rule` 清单；④ 一因四报的合并未做，已登记 |
| F7 | 骨架从 spec §0/§5/§9 与 Scope、knowledge-use、materials 打底；⑫b 核附录 A/B/C 行 ⊇ spec §9（按首列标识）；术语按用户裁定只打底不核 |
| F8/F9 | 「哪种图讲什么」表与三条规则进 `story-write.md`；小节三档进合同 `sections`；spec §9 不动 |
| F4/F5 | spec.md ⑧、SKILL、`run_archived` 三处同一顺序，「不再跑 harness」明写；决策正文不写档位术语进 story-write |
| F6 | `run_case.py` 已闭环阶段不再重跑 harness；`measure_run` 数图并单列流程章 |
| 台账 | S01/S02/S08/S09/S12/S13/S14 转 deterministic（`self_check: internal`），73 条总数不变 |
| 模板 | `story-template.md` 退场，样式约定并入 `story-write.md` |

## 5. 五跑前的门

R1 修完、C1 确认后进五跑。五跑判据按 `steps/15` §4，另加两条度量：`measure_run` 的 `diagrams_in_flow_chapter ≥ 1`；术语章行数 ≥ 骨架打底行数（打底真的到了作者手上）。

## 6. 整个扩展（不只批次 5）的五问（用户 2026-09-05）

按 `AGENTS §4.2` 逐段问「这里做决定的是谁」，不数行数。

**脚本都是必须的吗——是。** 逐个文件核过用途：材料清单、流程契约、导入转换、知识激活与判断投影、任务包、骨架派生、评审记录渲染、独立 verifier 的任务书与报告核对，各是一件确定性工作的唯一承载；自写的 YAML 解析器是扩展不带依赖的结果（harness 的 `yaml` 包扩展够不到）；`flow-check` 是防手工改契约的最后一道防线；下游五阶段的知识义务门是批次 1 知识链的下游承载，批次 5 五跑止于 spec 没跑到它们——**未经本批实跑证明，登记待下一批验证，不删**。

**脚本有没有替模型判语义——没有。** 判断的活都在模型与 verifier：材料归类、范围定位、知识判断、决策登记、成文、读者审查。脚本判的是表在不在、行齐不齐、路径对不对、流程到哪一步；coding 门查的是契约点名的文件里有没有契约点名的标识符，是按范围找实体。唯一一处边缘是骨架按模块范围挑 spec 术语进读物，用户已裁定只作起始行、作者可删。

**提示词都合适吗——合适，长度不是问题。** 讲理由的句子帮助模型照做（`AGENTS §4.3`：不能假设执行者 100% 遵循）；上下文 477K 的主体是模型读 framework 文档与脚本源码，不是扩展这 1100 行提示词。要减的是「作者要读源码才知道怎么写」，步骤 15 的任务包与骨架已经在减。

**模型还在干机械活吗——步骤 15 之前有，之后剩一处。** 附录五节、术语起始行、流程图、图引用串、验收键都改由骨架与任务包给出；侧车 JSON 与统稿六行是判断记录与留痕，不是机械活。剩的一处是 R12：骨架打底的内容被 `chapter` 整章覆盖，作者又得重打。

**整个扩展是不是正向实现——是。** 材料清单、流程契约、任务包、骨架派生、知识判断骨架与投影、独立 verifier、评审记录由决策渲染，每一样都是「先给执行者他该有的，再兜底」。步骤 15 实现里的两处缺陷（R1、R12）见 §7。

## 7. 返修清单（交执行会话；用户 2026-09-05 裁定「把所有问题写进评审意见让执行者改」）

**本表列仍成立的条目（2026-09-05 §7.1 复核 + 外部模型评审补三条）**：R1、R12、R13、R14、R15 是步骤 15 实现的缺陷，R10、R11 是纪律。被撤回与降级的条目见 §7.1，**不要做**。原则：每项写替代了什么；夹具用真实产物的形态；8600 冻结，改动净增减报在提交信息里。做完我评审，通过再进五跑。

| 编号 | 问题 | 改成什么样 | 验收 |
|---|---|---|---|
| R1 | 骨架派生在 CRLF 的真实 spec 上术语与改动边界为空（`scopeList` 正则） | `specText` 读入时行尾归一，一处；各处正则不加 `\r?` | 用四跑那份 spec.md（CRLF）做夹具：术语 ≥ 3 行、改动边界含「改动 / 只复用」两行；LF 夹具照绿 |
| R10 | 预算 target 一日三抬 | 用户已确认 8600 为自己所签且是发起质询的原因：8600 为本批最终值，本清单只减不增；每个提交在提交信息里报五类增减 | 收口时总量 < 8600 且低于 HEAD 8548 |
| R12 | **骨架打底的内容会被 `chapter` 整章覆盖。** `cmdChapter` 用作者的章文件替换整个章区间，打底的术语行、spec §5 的图、附录 A–E 的表都在那个区间里；作者按任务包「改措辞、往下加」时要么重打这几十行，要么落盘后它们就没了 | 打底内容分两种所有权（外部评审 2026-09-05 的细化，采纳）：**可重复投影**——spec §9 的三张表、规约判定的规约域/编号/判定列、材料清单的类别与链接，由脚本拥有、放进带标记的生成区，`chapter` 保留生成区、每次 `skeleton`/`story` 可重渲染；**一次性作者种子**——术语措辞、流程图节点文字、材料贡献说明、规约判定的依据，初始化时种进作者区，此后归作者，脚本不再覆盖。`chapter` 只替换作者区 | 用四跑真实 spec 建骨架 → 写附录章文件只含目的句 → `chapter` 落盘后 D 表 15 行、A/B 表、术语行、流程图仍在；改一处术语措辞再跑 `skeleton`（已存在不覆盖）与 `story`，措辞不被打回 |
| R13 | **固定槽位没有真正生成。** `chapterSkeleton` 对 `form.tables` 只写 `<!-- 机器核：要有一张表 -->` 注释，没有表头行、分隔行与占位行；参与方表、取舍表、风险表、异常两表、验收表仍由模型搭，脚本事后挑错——正是「模型做确定性工作、脚本挑错」的倒置（外部评审指出，代码核实） | 按 `steps/15` F1 原文：每个成立的槽位渲染表头行 + 分隔行 + 一行 `{{…}}` 占位（必有列之外的列名由作者定，所以表头只放必有列，作者可加列）；异常章两张表各一份；验收章按 `*` 槽位在作者建节后由 `chapter` 不管、⑪ 核 | 用四跑真实 spec 建骨架：业务方案章有三张表头、异常章两张、术语章一张；⑫a 把未替换的占位行报出来 |
| R14 | **附录 D/E 没有完整从真源生成。** `knowledgeUseVerdicts` 只带回 `applicable` 布尔，D 表「依据」列全是 `{{依据}}`，模型要把 `knowledge-use.yaml` 里已写的 reason/requirement 再抄一遍；E 表只有文件名与链接，没有类别（外部评审指出，代码核实） | D 表依据列取真源：不命中取 `reason`，命中取 `requirement` 一句；E 行前半按 `materials.json` 给类别（产品需求 / 系统设计 / 开发需求 / 补料原件）+ 文件名 + 链接，后半「贡献了什么」留作者。两处都属 R12 的「可重复投影」 | 骨架里 D 表无 `{{依据}}`；E 行含类别；⑫b 集合核照旧 |
| R15 | **F4 的动作文字让模型手填 framework 凭证。** 3.0.0 的回执模板写明「本文件由 harness 生成，只读：agent 不填任何字段」（receipt_schema 2.1），`check-receipt.ts` 自己生成并校验；四跑那句「回填回执字段」是 2.0 时代的做法，`spec.md` ⑧、SKILL、`run_archived` 三处仍写「回填 phase-completion-receipt.md」（外部评审指出，模板核实） | 三处改为：verifier PASS → `check-receipt` → `/story archive`；删掉「回填回执」 | `status` 在 `story_written` 态的动作文字不含「回填」；夹具同步 |
| R11 | 夹具与真实产物脱节（LF vs CRLF 只是一例） | 新增或改写夹具时，输入取自真实产物（四跑工作区的 spec.md / story.md / materials.json 脱敏后入 fixtures），不手造最小样本；已有最小样本不动 | R1/R5/R8 的夹具按此 |

**不做的事**：不加任何判据；不改 framework；不动 T2–T5 与 F3；不删 R2/R3/R4/R8/R9 涉及的任何代码；R5/R6/R7 本批不做。

**R4 的登记**：只改 `06-验收追踪矩阵` 一行「plan 及之后未经批次 5 实跑证明」；另把 coding 门那条检查的描述改名为「契约实体标识存在」（它是 `标识符` 搜索，注释也能命中，不叫 landing），判法不改，真实落实仍只由 verifier 判——外部评审建议，采纳。

**顺序**：R1 → R12 与 R13、R14（同一提交，都是骨架的所有权与生成）→ R15 → R4 登记。每个提交带真实产物夹具（R11）。做完评审，通过进五跑，五跑判据按 `steps/15` §4 加 §5 两条。

### 7.1 复核记录

初稿曾把「本批没跑到」「我没看出用途」当成「无效」，列了删自写解析器、退 flow-check、删下游义务门、改侧车为参数、合并命令等项。用户质询后逐条核代码，全部撤回（依据见 §6）；撤回原文由 Git 保存（`reviews/15` 的上一版），本文不留。教训：判一段代码无效之前，先核它防的是什么、依赖能不能换、没被用到是测试范围还是设计问题。

# 步骤 15（四跑后的正向修正 F1–F9）· 独立评审（Claude，2026-09-05）

对象：`a136076d`（裁定落盘）、`e71cd58d`（F3）、`d88f9b31`（F1/F2/F7/F8/F9）、`dda5c4d7`（F4/F5）、`195acdb4`（F6）。
全部 diff 亲自读过；离线复跑 621 绿（并行 26 s）、失效形态 70 条 FAIL 0、委派 8；`framework/` 与 main 仍恰好 18 文件差异。
用户这一轮要的不只是逐项通过，而是**从整个批次 5 看**：批次 4 的病有没有回来——脚本有没有替模型做决定、模型有没有在干机械活。

> **口径（2026-09-05 更正）**：`AGENTS §7.5` 写明「数字只是围栏，不表达质量；表达质量的判据仍是 §4.2 与已登记的确定性失败清单」。本文 §1 的规模表只作围栏读数，结论一律按 §4.2「模型与脚本按能力分工」逐段问「这里做决定的是谁」得出；我初稿把规模当结论、按行数开删，是错的，§7.1 已逐条更正。

## 1. 批次 5 全程的规模（同一把尺子量每个提交：HEAD 的分类与去注释算法）

| 时点 | 代码行 | 总行数 | 字符数 | story-build 判据块 |
|---|---|---|---|---|
| 批次 3 收口 | 6316 | 9009 | 299K | — |
| 批次 4 收口 = 批次 5 起点 | 8819 | 12904 | 430K | 21 |
| 步骤 10 收口 | 7710 | 11286 | 373K | 15 |
| 步骤 11 退场 | 7787 | 11393 | 377K | 15 |
| 步骤 12 收口 | 8256 | 12021 | 399K | 15 |
| 步骤 15 落地（HEAD） | 8548 | 12396 | 418K | 17 |

按类别（代码行）：

| 类别 | 批次 4 收口 | 步骤 11 退场 | HEAD | 退场后增量 |
|---|---|---|---|---|
| scripts_mjs | 2701 | 1716 | 1978 | +262 |
| scripts_py | 1049 | 1182 | 1400 | +218 |
| hooks_mjs | 2402 | 2403 | 2683 | +280 |
| prompts_md | 2006 | 1838 | 1748 | −90 |
| data | 661 | 648 | 739 | +91 |

读法（围栏读数，不是质量结论）：

- 对批次 4 的起点，批次 5 净减 3%；退场那一步删掉的是逐单元系统与 13 个 checker——那正是批次 4「脚本替模型判语义」的实体，删它们是对的。
- 退场之后回长 761 行。按 §4.2 逐段看这 761 行是谁在做决定：任务包渲染、骨架从 spec 与登记数据派生、caption 真源、材料关卡按事实判停——都是脚本做「枚举、渲染、状态记录」，决定仍在模型（归类、范围、知识判断、决策、写章）；判据只加了 ⑪（表槽位存在）与 ⑫b（集合包含），两条都只测机械事实。**没有一段是脚本替模型判语义**，所以这 761 行的增长不是批次 4 的形态。
- 预算 target 一日三抬到 8600 是用户所签，签的理由正是要把这次增长拿出来审——审的结果如上。围栏的作用已经起到，数字本身不再是问题。

## 2. 批次 4 的病回来了没有——按审计的十个根因逐条看 HEAD

| 根因 | 现状 | 判 |
|---|---|---|
| RC1 用代理指标替代目标 | ⑪ 只核槽位存在与首列锚，⑫b 只核标识集合，`semantic_proxy` 仍 0；没有字数、数量、相似度 | 没回来 |
| RC2 脚本判语义 / 模型做机械活 | 机械活这一半明显退了：附录五节、术语起始行、流程图、图引用串、验收键都由骨架与任务包给出，作者不再重打。脚本判语义这一半有一处边缘：`specTerms` 用「权威模块在 in_scope」决定哪些 spec 术语算业务词——这是脚本替作者判「什么词该进读物」。用户裁定只作起始行、作者可删，风险低，但它是同类判断里唯一一条，记着别再长 | 基本没回来，一处边缘 |
| RC3 要求失败后才送达 | 任务包现在给形态、槽位、引用串、验收键；骨架给打底 | 已关 |
| RC4 作者与裁决者不独立 | verifier 独立，插件发布；未变 | 已关 |
| RC5 多真源 | 形态真源收成合同一份，模板退场。剩一处轻度重复：`story-write.md`「什么内容用什么形态」按内容类型讲，合同 `form.note` 按章讲——一个是原则一个是实例，暂不冲突，但改一处要看另一处 | 轻度 |
| RC6 知识消费 | 未动 | — |
| RC7 framework 协作契约 | 3.0.0 同步后 18 处改动有声明、有补丁、有 allowlist | 已关 |
| RC8 验证系统证明不了行为 | **回来了一次**：F7 的派生在夹具（LF）上全绿，在四跑真实的 `spec.md`（CRLF，opencode 在 Windows 写的）上术语与改动边界打底为空——见 §3。「离线绿 ≠ 行为证据」正是审计 RC8 的形态 | 回来了 |
| RC9 追加为默认 | 判据只加两条、删了模板与两个函数，替代关系逐项写了；预算 target 抬到 8600 是用户为审视这次增长所签，增长逐段核过都是脚本做枚举、渲染、状态记录 | 没回来 |
| RC10 证明量线性增长 | 无 | 没回来 |

一句话：**批次 4 的病没有回来，回来的是一个苗头**——夹具与真实产物脱节（RC8），它是批次 4 §8.2「完成判据偏向新检查已通过」的入口，R1/R11 堵它。

## 3. 返修与确认（清单以 §7 为准）

**R1 · F7 派生在真实 spec 上为空。** 把四跑工作区的 AR90006 复制出来、删掉 story.md、契约退回 complete，跑 `skeleton`：流程图复制到了、附录 D 15 行齐、A/B 两表齐；**术语章零行、改动边界只有 §9.5 的依赖表、没有 Scope 的两行**。原因是 `scopeList` 的正则要求 `:` 后紧跟 `\n`，而那份 spec 是 CRLF；`specSection`、`pipeTables` 都按 `\r?\n` 切，独它没有。修法一处：`specText` 读入时行尾归一（框架 `verifier-request.ts` 对 ai-prompt.md 就是这么做的），不在各处正则里补 `\r?`。**夹具要用真实产物的形态**：至少一条用 CRLF 的 spec 跑派生，否则五跑仍会看到「术语从零写」。

**C1 · 预算 8600 的签字。** 用户 2026-09-05 确认是自己签的，并说明签这一笔正是发起「全批是否膨胀」质询的原因。**8600 定为批次 5 的最终 target，本批不再抬**；步骤 15 实测 +292 对方案估 +35～65 是四倍以上的低估，下次估算要按「派生一行、核对一行」各自报，不合报。

## 4. 逐项核过的机制事实（通过；R1 与 R12 两处例外见 §3 与 §7）

| 项 | 核到什么 |
|---|---|
| F3 | 侧车改 `{gate, options}`，`sidecar_shape` 同步；`material_gate_stops` 只看轮次与本级侧车；`supplement_has_material` 两条路（inbox 未并入 / 指纹变）；后两级 decide 拦别级侧车；八条夹具含「补料轮走到第二级 decide 成功」，正是我探针那条 |
| F1 | 合同每章 `form`（primary / note / tables / tables_when / diagram / sections），`form_note` 把「不核行数、不核表的总数、不核槽位之外」写死；⑪ 一条判据读数据；页面状态表槽位按用户裁定撤掉；必有列收成首列锚；小节名包含匹配 |
| F2 | 任务包给 `![caption](../assets/…)` 原样串、每章形态与机器核槽位、applicable → `knowledge_rule` 清单；④ 一因四报的合并未做，已登记 |
| F7 | 骨架从 spec §0/§5/§9 与 Scope、knowledge-use、materials 打底；⑫b 核附录 A/B/C 行 ⊇ spec §9（按首列标识）；术语按用户裁定只打底不核 |
| F8/F9 | 「哪种图讲什么」表与三条规则进 `story-write.md`；小节三档进合同 `sections`；spec §9 不动 |
| F4/F5 | spec.md ⑧、SKILL、`run_archived` 三处同一顺序，「不再跑 harness」明写；决策正文不写档位术语进 story-write |
| F6 | `run_case.py` 已闭环阶段不再重跑 harness；`measure_run` 数图并单列流程章 |
| 台账 | S01/S02/S08/S09/S12/S13/S14 转 deterministic（`self_check: internal`），73 条总数不变 |
| 模板 | `story-template.md` 退场，样式约定并入 `story-write.md` |

## 5. 五跑前的门

R1 修完、C1 确认后进五跑。五跑判据按 `steps/15` §4，另加两条度量：`measure_run` 的 `diagrams_in_flow_chapter ≥ 1`；术语章行数 ≥ 骨架打底行数（打底真的到了作者手上）。

## 6. 整个扩展（不只批次 5）的五问（用户 2026-09-05）

按 `AGENTS §4.2` 逐段问「这里做决定的是谁」，不数行数。

**脚本都是必须的吗——是。** 逐个文件核过用途：材料清单、流程契约、导入转换、知识激活与判断投影、任务包、骨架派生、评审记录渲染、独立 verifier 的任务书与报告核对，各是一件确定性工作的唯一承载；自写的 YAML 解析器是扩展不带依赖的结果（harness 的 `yaml` 包扩展够不到）；`flow-check` 是防手工改契约的最后一道防线；下游五阶段的知识义务门是批次 1 知识链的下游承载，批次 5 五跑止于 spec 没跑到它们——**未经本批实跑证明，登记待下一批验证，不删**。

**脚本有没有替模型判语义——没有。** 判断的活都在模型与 verifier：材料归类、范围定位、知识判断、决策登记、成文、读者审查。脚本判的是表在不在、行齐不齐、路径对不对、流程到哪一步；coding 门查的是契约点名的文件里有没有契约点名的标识符，是按范围找实体。唯一一处边缘是骨架按模块范围挑 spec 术语进读物，用户已裁定只作起始行、作者可删。

**提示词都合适吗——合适，长度不是问题。** 讲理由的句子帮助模型照做（`AGENTS §4.3`：不能假设执行者 100% 遵循）；上下文 477K 的主体是模型读 framework 文档与脚本源码，不是扩展这 1100 行提示词。要减的是「作者要读源码才知道怎么写」，步骤 15 的任务包与骨架已经在减。

**模型还在干机械活吗——步骤 15 之前有，之后剩一处。** 附录五节、术语起始行、流程图、图引用串、验收键都改由骨架与任务包给出；侧车 JSON 与统稿六行是判断记录与留痕，不是机械活。剩的一处是 R12：骨架打底的内容被 `chapter` 整章覆盖，作者又得重打。

**整个扩展是不是正向实现——是。** 材料清单、流程契约、任务包、骨架派生、知识判断骨架与投影、独立 verifier、评审记录由决策渲染，每一样都是「先给执行者他该有的，再兜底」。步骤 15 实现里的两处缺陷（R1、R12）见 §7。

## 7. 返修清单（交执行会话；用户 2026-09-05 裁定「把所有问题写进评审意见让执行者改」）

**本表列仍成立的条目（2026-09-05 §7.1 复核 + 外部模型评审补三条）**：R1、R12、R13、R14、R15 是步骤 15 实现的缺陷，R10、R11 是纪律。被撤回与降级的条目见 §7.1，**不要做**。原则：每项写替代了什么；夹具用真实产物的形态；8600 冻结，改动净增减报在提交信息里。做完我评审，通过再进五跑。

| 编号 | 问题 | 改成什么样 | 验收 |
|---|---|---|---|
| R1 | 骨架派生在 CRLF 的真实 spec 上术语与改动边界为空（`scopeList` 正则） | `specText` 读入时行尾归一，一处；各处正则不加 `\r?` | 用四跑那份 spec.md（CRLF）做夹具：术语 ≥ 3 行、改动边界含「改动 / 只复用」两行；LF 夹具照绿 |
| R10 | 预算 target 一日三抬 | 用户已确认 8600 为自己所签且是发起质询的原因：8600 为本批最终值，本清单只减不增；每个提交在提交信息里报五类增减 | 收口时总量 < 8600 且低于 HEAD 8548 |
| R12 | **骨架打底的内容会被 `chapter` 整章覆盖。** `cmdChapter` 用作者的章文件替换整个章区间，打底的术语行、spec §5 的图、附录 A–E 的表都在那个区间里；作者按任务包「改措辞、往下加」时要么重打这几十行，要么落盘后它们就没了 | 打底内容分两种所有权（外部评审 2026-09-05 的细化，采纳）：**可重复投影**——spec §9 的三张表、规约判定的规约域/编号/判定列、材料清单的类别与链接，由脚本拥有、放进带标记的生成区，`chapter` 保留生成区、每次 `skeleton`/`story` 可重渲染；**一次性作者种子**——术语措辞、流程图节点文字、材料贡献说明、规约判定的依据，初始化时种进作者区，此后归作者，脚本不再覆盖。`chapter` 只替换作者区 | 用四跑真实 spec 建骨架 → 写附录章文件只含目的句 → `chapter` 落盘后 D 表 15 行、A/B 表、术语行、流程图仍在；改一处术语措辞再跑 `skeleton`（已存在不覆盖）与 `story`，措辞不被打回 |
| R13 | **固定槽位没有真正生成。** `chapterSkeleton` 对 `form.tables` 只写 `<!-- 机器核：要有一张表 -->` 注释，没有表头行、分隔行与占位行；参与方表、取舍表、风险表、异常两表、验收表仍由模型搭，脚本事后挑错——正是「模型做确定性工作、脚本挑错」的倒置（外部评审指出，代码核实） | 按 `steps/15` F1 原文：每个成立的槽位渲染表头行 + 分隔行 + 一行 `{{…}}` 占位（必有列之外的列名由作者定，所以表头只放必有列，作者可加列）；异常章两张表各一份；验收章按 `*` 槽位在作者建节后由 `chapter` 不管、⑪ 核 | 用四跑真实 spec 建骨架：业务方案章有三张表头、异常章两张、术语章一张；⑫a 把未替换的占位行报出来 |
| R14 | **附录 D/E 没有完整从真源生成。** `knowledgeUseVerdicts` 只带回 `applicable` 布尔，D 表「依据」列全是 `{{依据}}`，模型要把 `knowledge-use.yaml` 里已写的 reason/requirement 再抄一遍；E 表只有文件名与链接，没有类别（外部评审指出，代码核实） | D 表依据列取真源：不命中取 `reason`，命中取 `requirement` 一句；E 行前半按 `materials.json` 给类别（产品需求 / 系统设计 / 开发需求 / 补料原件）+ 文件名 + 链接，后半「贡献了什么」留作者。两处都属 R12 的「可重复投影」 | 骨架里 D 表无 `{{依据}}`；E 行含类别；⑫b 集合核照旧 |
| R15 | **F4 的动作文字让模型手填 framework 凭证。** 3.0.0 的回执模板写明「本文件由 harness 生成，只读：agent 不填任何字段」（receipt_schema 2.1），`check-receipt.ts` 自己生成并校验；四跑那句「回填回执字段」是 2.0 时代的做法，`spec.md` ⑧、SKILL、`run_archived` 三处仍写「回填 phase-completion-receipt.md」（外部评审指出，模板核实） | 三处改为：verifier PASS → `check-receipt` → `/story archive`；删掉「回填回执」 | `status` 在 `story_written` 态的动作文字不含「回填」；夹具同步 |
| R11 | 夹具与真实产物脱节（LF vs CRLF 只是一例） | 新增或改写夹具时，输入取自真实产物（四跑工作区的 spec.md / story.md / materials.json 脱敏后入 fixtures），不手造最小样本；已有最小样本不动 | R1/R5/R8 的夹具按此 |

**不做的事**：不加任何判据；不改 framework；不动 T2–T5 与 F3；不删 R2/R3/R4/R8/R9 涉及的任何代码；R5/R6/R7 本批不做。

**R4 的登记**：只改 `06-验收追踪矩阵` 一行「plan 及之后未经批次 5 实跑证明」；另把 coding 门那条检查的描述改名为「契约实体标识存在」（它是 `标识符` 搜索，注释也能命中，不叫 landing），判法不改，真实落实仍只由 verifier 判——外部评审建议，采纳。

**顺序**：R1 → R12 与 R13、R14（同一提交，都是骨架的所有权与生成）→ R15 → R4 登记。每个提交带真实产物夹具（R11）。做完评审，通过进五跑，五跑判据按 `steps/15` §4 加 §5 两条。

### 7.1 复核记录

初稿曾把「本批没跑到」「我没看出用途」当成「无效」，列了删自写解析器、退 flow-check、删下游义务门、改侧车为参数、合并命令等项。用户质询后逐条核代码，全部撤回（依据见 §6）；撤回原文由 Git 保存（`reviews/15` 的上一版），本文不留。教训：判一段代码无效之前，先核它防的是什么、依赖能不能换、没被用到是测试范围还是设计问题。

## 8. 返修评审（`6667809c` R1、`404320ce` R12+R13+R14、`d3c9b064` R15+R4；Claude，2026-09-05）

复跑：全量并行绿、失效形态 70 条 FAIL 0、`framework/` 仍 18 文件差异；预算实测 8638。
除读 diff 外，拿四跑真实产物走了一遍作者路径：建骨架 → 附录章文件只写五句目的句落盘 → 改术语措辞落盘 → 重跑 skeleton → `check`。

| 项 | 核到什么 | 判 |
|---|---|---|
| R1 | `specText` 一处归一；真实 CRLF spec 上术语打底 3 行、改动边界「改动 / 只复用」两行都出来了；夹具 `fixtures/real-run/AR90006` 目录带 `* -text`，提交的 blob 仍是 385 行 CRLF | 通过 |
| R12 | 附录五节进 `story-build:begin/end` 生成区；作者章文件只有目的句时落盘后五个区、37 行表全在；术语与流程图是种子，作者改的措辞落盘后再跑 skeleton 不被打回（已存在不覆盖） | **改判不通过**（§9 B1/B2：机器区含作者占位且只保留旧缓存） |
| R13 | 参与方、取舍、风险、交接约定各渲染「必有列表头 + 分隔 + 占位行」；异常章列名随需求，只写「要 2 张表」；真源已给表的章不摆占位 | **改判不通过**（§9 B3：表在骨架里不在机器区，落盘要模型搬） |
| R14 | D 表依据取 requirement / reason，15 行无占位；E 行带类别（合同 `sources[*].label`） | 通过 |
| R15 | spec.md ⑧、SKILL、`run_archived` 三处删「回填回执」，写明回执由 harness 生成；两处旧注释同步 | 通过 |
| R4 | coding 门 detail 改「契约实体标识存在」，判法不动；06 矩阵登记五个门未经实跑证明 | 通过 |
| R11 | 五条新夹具全部取真实产物 | 通过 |

**新发现一条（R16，必修）**：投影把 spec §9 的「代码现状」列原样带进了归档件。`check` 在落盘后的附录上报 ⑨「仓内路径 `02-Feature/WalletMain/oh-package.json5`」与 ⑩「检索措辞『检索管理台/云侧下发零命中』」——两条都出自生成区，而生成区标着「改它请改真源」，作者改不了，红线又不该放宽。「代码现状」是 spec 给下游 AI 的（模板第 69 行：仓内路径或检索结论），本来就不该进给评审者看的归档件。目标形态：`appendixTables` 投影时按列名去掉「代码现状」列（列名来自模板，是数据）；夹具用同一份真实产物核落盘后的附录 ⑨/⑩ 零命中。其余 ⑪/⑫a/④ 的报错都是章未写导致，属预期。

**C2 · 预算第三次签字。** 用户 2026-09-05 确认 8700 为自己所签。净增 78 行都在生成区所有权、槽位渲染与真源取值，按 §4.2 是脚本做投影，不是替模型判断。

**结论（2026-09-05 晚，被 §9 推翻）**：R16 修完进五跑。

## 9. 外部评审第二轮对 R12–R14 实现的意见 · 核对（Claude，2026-09-05）

用户送来另一模型对返修实现的评审。逐条对着 `404320ce` 的代码与我 §8 的实跑输出核，**六条全部成立，没有误解需求**；我在 §8 判 R12/R13 通过判早了——我只验了「落盘后表还在」，没验「作者填了之后会怎样」与「真源变了之后会怎样」。§8 的 R12、R13 两行改判为不通过。

| 外部意见 | 核对 | 判 |
|---|---|---|
| B1 机器区里混着作者要填的占位（`{{对评审意味着什么}}`、`{{这份材料贡献了什么}}`），而 `preserveZones` 用旧 story 的区覆盖章文件里的同名区 | 代码如此：`kept` 分支用 `zone.lines`（旧）替换新区；作者填了被打回，不带又被插回占位；§8 实跑的 ⑫a 10 条占位里就有它们。所有权没分开 | 成立，BLOCKER |
| B2 「可重复投影」只是保留旧缓存：`chapter` 不重算、`skeleton` 已存在即返回、`preserveZones` 只读旧 story | 代码如此。spec §9 / knowledge-use / materials 变了，story 里的区不变——又造出「真源 + 可漂移副本」 | 成立，BLOCKER |
| B3 R13 的槽位表在骨架里但不在机器区，`chapter` 整章替换时消失，模型得把它们搬进章文件 | 骨架注释原话就是「写章文件时连它一起写」。从「模型搭表」变成「模型搬表」 | 成立，MAJOR |
| B4 多张投影表之间没有空行，Markdown 会并成一张错表，`pipeTables` 把后面的表头当数据行所以自检看不出 | §8 骨架输出里 B 节三张表、C 节两张表确实首尾相接 | 成立，MAJOR |
| B5 R16 | 同 §8 | 成立 |
| B6 「上面这几行是打好的底」印在种子之前 | §8 骨架输出里术语章确实先注释后表 | 成立，MINOR |

它给的方向也对，且与 spec §10/§11 由 `render` 生成的既有做法同构。

### 9.1 返修项（2026-09-05 晚按外部第三轮意见修订；替换 R12/R13/R16）

外部第三轮指出我初版 R17–R20 有两处根因没闭合：作者种子还是要模型从 story.md 搬进章文件；R19「落盘时插占位」是脚本事后补结构、模型二次返工。两条都对——作者写的是独立章文件，`chapter` 整章替换，凡是只存在于 story.md 骨架里的东西都得搬。改成下面四条：

| 编号 | 改成什么样 | 验收（都用真实产物） |
|---|---|---|
| R17 章草稿：确定性工作在作者动笔前做完 | `skeleton` 除建章锚外，为每一章生成**作者直接编辑的章草稿**（`AR/story-src/drafts/NN-<章名>.md`，清扫白名单放行、不冻结）：草稿里已经有形态说明、该章的固定槽位表头与占位行、术语起始行、spec §5 的图、材料清单的类别与链接行（贡献句留占位）。作者在草稿里填，`chapter --from` 消费草稿（作者也可用别的文件，但草稿是默认起点）。**`chapter` 只做原子落盘，不判形态、不插占位**——外部意见建议「缺槽位时拒绝」，我不采纳：那会让形态判断有两处（落盘时与 ⑪），作者面对的还是「被拒 → 改 → 再交」的循环；槽位在不在只由 `check ⑪` 一处说。story.md 骨架里不再放任何种子——种子只在草稿里 | 真实 spec 建骨架后 `drafts/` 十份齐；附录草稿 E 行含类别与链接、贡献句占位；作者只填占位后 `chapter` 落盘，⑫a 零占位 |
| R18 机器区只从真源投影，两个时点 | 机器区（附录 A/B/C/D）**不含任何占位**，也不从旧 story 复制：`preserveZones` 那种「搬旧区」退场，换成同一个 `appendixProjection` 在**两个时点**从当前 spec、knowledge-use、materials 重算——① `chapter` 落盘附录章时投影一次（作者的附录草稿只有目的句与 E 行，A–D 由落盘时投出，这样登记前作者跑 `check` 不会因附录空节而红）；② `story_flow.py story` 登记时按固定顺序 **重投影 → number → check → 登记** 再投一次，真源变了以登记这次为准。`number` 保持单一职责。外部意见只写了登记前一次，我加落盘那一次，理由如上 | 改 knowledge-use.yaml 一条 reason 再登记，D 表对应行变；story.md 里旧区不残留；`number` 代码不动；附录草稿只写目的句落盘后 A–D 在 |
| R20 投影策略只有一条 | 不在合同里逐表登记列白名单（那会与 spec 模板形成第二真源）。投影策略是一句：**排除「代码现状」列，其余列按原顺序**；多张表之间空一行。真有某张表要不同投影时再升级 | 落盘后附录 ⑨/⑩ 零命中；B 节三张表各自成表；合同里没有逐表列清单 |
| R21 | 「下面这几行是打好的底」——随 R17 一起消失（种子不再放骨架），若骨架仍留说明行，措辞按位置写 | 骨架顺序正确 |

一句话版本（外部评审的原话，采纳为验收总则）：**脚本预先完成确定性工作，模型一次填写语义内容，登记前脚本从真源重建机器投影。**

### 9.2 执行契约（外部第四轮补三条；用户 2026-09-05 13:02 批准本节与 §9.1 为下一轮实现基线）

三条我逐一核过，都成立，写死给执行会话：

1. **`chapter` 的职责措辞**：`chapter` 不判形态、不生成作者待填占位；落盘附录章时，**原子写入「作者区 + 当前真源生成的机器区」**。它不干涉作者区，不等于只复制章文件。
2. **投影只有一个入口**：新增 `story-build project --feature <名>`（JS，与 `skeleton`/`chapter` 同一实现），两个时点都走它——`chapter --chapter 附录` 内部调用同一实现；`story_flow.py story` 登记时依次 `story-build project → number → check`（`story_flow.py` 本就以子进程调 `story-build`，这里只是多一个动作）。**不得在 `chapter` 与 `story_flow.py` 各实现一份投影。**
3. **草稿的恢复与材料变化**：`skeleton` 见 story.md 已存在时仍补建**缺失的**草稿，缺哪章补哪章；已存在的草稿绝不覆盖（保护作者未落盘的内容）；登记成功后删除草稿（story 已冻结，草稿失去用途），草稿不进冻结台账 `story_src_digests`。E 材料清单是作者种子：草稿生成后材料又变，`project` 不改 E，由 `check ⑫c` 报集合差异并指向「更新附录草稿的材料行与贡献句 → 重新 `chapter` 落盘」；不静默覆盖作者贡献，登记不放行。

以上加 §9.1 的 R17/R18/R20/R21，是执行会话下一轮的全部范围；R1/R14/R15/R4 维持通过。做完我评审，通过进五跑。


R14、R15、R4、R1 维持通过。做完 R17/R18/R20/R21 我再评审，通过才进五跑。

## 10. 返修评审（`8eaa7f35` R17/R18/R20/R21；Claude，2026-09-05 13:40）

复跑：638 绿（并行 35 s）、失效形态 70 条 FAIL 0、`framework/` 仍 18 文件差异、预算 8691/8700（类别腾挪 scripts_py 1400→1420、scripts_mjs →2090，总量未动，写了理由）。
用真实产物走了整条作者路径：skeleton → 看十份草稿 → 附录草稿只填目的句与贡献句落盘 → 术语草稿原样落盘 → 改 knowledge-use 一条 reason 跑 project → 改一份草稿、删一份草稿再跑 skeleton → check。

| 项 | 核到什么 | 判 |
|---|---|---|
| R17 章草稿 | `drafts/` 十份齐；术语草稿带真实 CRLF spec 派生的 3 行起始行；业务方案草稿有三张槽位表头 + 占位；附录草稿只有五句目的句占位与 E 三行（类别 + 链接 + 贡献句占位）；story.md 骨架零种子；改过的草稿不被覆盖、删掉的补回来 | 通过 |
| R18 唯一投影入口 | `project` 是 `story-build` 子命令，`chapter` 落盘附录时与 `story_flow.py story` 登记时都走它；落盘后附录 4 个机器区、零占位；改 reason 后 `project` 重投，D 表对应行变、作者的目的句一字不动；`preserveZones` 已删 | 通过 |
| R20 | 机器区无「代码现状」列；B 节三张表、C 节两张表之间各有空行；⑨ 仓内路径零命中 | 通过 |
| R21 | 种子迁出骨架，提示消失 | 通过 |
| 契约 1–3 | `chapter` 不判形态；投影单入口；草稿只补不覆盖、登记后删（`cmd_story` 末尾 `rmtree`）、清扫白名单放行、不进 `story_src_digests`；E 变化由 ⑫c 报 | 通过 |

**新发现两条（都在同一条作者路径上，必修）**：

| 编号 | 问题 | 改成什么样 | 验收 |
|---|---|---|---|
| R22 材料清单种子的链接差一层 | 附录草稿 E 行是 `[prd.md](RR/prd.md)`，而 story.md 在 `AR/` 下；落盘后 `check ⑫c` 报 9 条：三条「链接点不开」、三条「少了一份材料」、三条「列了不是材料的东西 `AR/RR/prd.md`」。与 E2 图引用串是同一类错——脚本给作者的串必须按 story.md 所在目录算。**我在 §8 就见过这 9 条，当时当成「章未写」放过了，是我漏判** | `materialListSkeleton` 的链接按 `path.posix.relative('AR', rel)` 生成（与任务包给图引用串同一算法，抽成一处） | 附录草稿原样落盘后 ⑫c 对 E 零报 |
| R23 草稿里的指引会随落盘进归档件 | 草稿含形态说明、读者问题、命令提示三种 HTML 注释和 `<!-- 待写：… -->` 标记。作者按指引「在草稿上写、写完 chapter --from 草稿」，这些注释与标记原样进 story.md：形态说明里的「spec §0」被 ⑩ 报 doc_coordinate；待写标记让已落盘的术语章仍被数成「待写」（落盘后报「还剩 9 章」，术语在列） | `chapter` 落盘时剥掉草稿的指引：HTML 注释（机器区首尾标记除外）与待写标记——它们是写给作者的，不是内容；归档件本来不该有 HTML 注释 | 术语草稿原样落盘后：⑩ 零命中、⑫a 不再把术语数成待写、story.md 无 HTML 注释（机器区标记除外） |

advisory（不拦）：槽位表头只有一列锚（`| 参与方 |`），作者拿到的是单列表；`form.note` 里推荐的列可一并渲染，check 仍只核锚。

**结论**：R22、R23 修完再评审；其余通过。

### 10.1 外部第五轮意见（CR-001～007）· 核对（2026-09-05 14:00）

逐条对代码核过，七条都成立；两条与我的 R22/R23 相同，五条是我没查到的。两处我加了限定：

| 外部 | 核对 | 落成返修 |
|---|---|---|
| CR-001 待写标记与草稿指引进归档件 | = R23 | R23 按它的落法收窄：待写标记**只在 story.md 骨架，不进草稿**；草稿的指引注释由 `chapter` 剥除（机器区首尾标记除外），不要求模型手工清 |
| CR-002 材料清单链接差一层 | = R22 | R22 不变 |
| CR-003 机器区仍可能出占位 | `verdictSkeleton` 在 knowledge-use 缺行或依据为空时写 `{{命中/不命中}}` `{{依据}}`，作者改不了、下次 project 又盖回来 | **R24**：`project` 遇真源缺失或依据为空**响亮失败**，错误指向 `knowledge-use.yaml` 的那一条，不写占位。成立的前提已经在：knowledge-use 在 skeleton 之前就写好，post_check 也要求每条有 requirement/reason |
| CR-004 投影为空时旧机器区不删 | `projectAppendix` 对 `rows.length === 0` 直接 `continue`，spec 表清空或规约退出激活清单后旧区留着 | **R25**：投影做集合对账——有内容替换或新增；合同里已不存在的旧区删除；真源缺失或解析不动响亮失败。**一处限定**：某节在 spec 里**合法为空**（§9.x 写「不涉及」）时，不是删区留空，而是投一行「不涉及：<真源那一句>」进机器区——⑫ 附录结构要求每节有内容或一行不涉及，删了区那一节就成了只有目的句的空节，作者又不该去写它 |
| CR-005 恢复时给已落盘的章重建初始草稿 | `writeDrafts` 按合同十章逐一补，不看 story.md 里哪章已落盘 | **R26**：补草稿只补 story.md 里仍带待写标记的章；已落盘的章不建草稿 |
| CR-006 草稿清理失败被吞 | `rmtree(ignore_errors=True)` 后无条件记「已清理」并登记 | **R27**：删后确认目录不在；删不掉就报错、不写 `story_written`——Windows 文件占用是真会发生的事 |
| CR-007 分类预算与提交说明不一致 | 预算文件 `scripts_mjs.target` 仍是 2070，现值 2088；提交说明写「target 2070→2090」但文件没改。现在靠 in_progress 的峰值 2100 过门，收口时必红 | **C3**：先按它的建议净减 18 行（R23 剥指引、R25 对账都可能顺手删得出来）；确需改 target，则提交说明、预算文件、签字三者一致，不再第四次抬 |

外部意见「已正确落地」的十一项与我 §10 的通过项一致。结论一致：**不进五跑**；R22–R27 修完、C3 收平后再评审。


## 11. 返修评审（`f3fc4535` R22–R27 + C3；Claude，2026-09-05 14:45）· ~~通过，进五跑~~ **改判不通过（见 §11.1）**

复跑：644 绿（并行 29 s）、失效形态 70 条 FAIL 0 委派 8、`framework/` 仍 18 文件差异、预算 8703（用户授权阈值 scripts_mjs 2200、总量 8900；预算文件、提交说明、签字三处一致）。
再次用真实产物走作者路径：skeleton → 附录草稿填占位落盘 → 术语草稿**原样**落盘 → check → 删 knowledge-use 一条跑 project → 删已落盘章与待写章的草稿再 skeleton → 塞一个合同外旧机器区 + 把 spec §9.5 改成不涉及再 project。

| 项 | 核到什么 | 判 |
|---|---|---|
| R22 | 草稿 E 行是 `../RR/prd.md`、`../SR/design.md`、`design.md`；`relFromStory` 一处算法，任务包图引用串共用；落盘后 ⑫c 零报 | 通过 |
| R23 | 草稿里零待写标记；术语草稿原样落盘后 story.md 里的 HTML 注释只剩机器区标记与别章的待写标记，⑩ 零命中，术语不再被数成待写 | 通过 |
| R24 | knowledge-use 少一条时 `project` 退出码 1，点名 UX-01，story.md 不写占位 | 通过 |
| R25 | 合同外旧机器区被删；§9.5 改「不涉及」后投影不报错、改动边界仍有 Scope 两行 | 通过。advisory：改动边界分支不把 §9.5 那句「不涉及：…」投过来（接口/数据两节的通用分支会），评审者看不到「依赖不涉及」这一结论；下一轮顺手补，不拦 |
| R26 | 删掉已落盘的术语草稿与待写的业务流程草稿再跑 skeleton，只补回业务流程 | 通过 |
| R27 | `rmtree` 后 `exists()` 复核，删不掉抛 FlowError 不登记 | 通过（代码核，未模拟文件占用） |
| C3 | 净减 32 行后阈值按用户授权改，三处一致 | 通过 |

advisory 累计两条（不拦）：槽位表头只放一列锚；改动边界不投 §9.5 的不涉及句。

**结论（已由 §11.1 推翻）**：步骤 15 通过，进五跑。五跑判据：`steps/15` §4 + §5 两条（`diagrams_in_flow_chapter ≥ 1`、术语行 ≥ 打底行），另加本轮三条——作者路径是否真的在草稿上写并用 `chapter --from 草稿`；首次 `check` 的 ⑫c/⑩/⑨ 是否为零；`project` 在登记时是否被调用且 D 表与 knowledge-use 一致。

### 11.1 外部第六轮意见 · 核对（2026-09-05 15:10）· 结论改回不通过

四条逐一对代码核过，全部成立。两条 BLOCKER 我在 §11 的实跑里没有造出对应场景（整域不适用、合同内节的真源变空），所以没抓到；§9.5 那条我降成 advisory 是错的——它违反 F7「story 相对 spec 只增不减」，spec 明写的结论到 story 里丢了。

| 外部 | 核对 | 返修 |
|---|---|---|
| B1 R24 破坏「整域不适用」 | `knowledge-use.mjs` 第 266 行：域判整域不适用后，域内条目**不必逐条登记**（`naDomains` 免检）；`verdictSkeleton` 只读 `use.constraints`，不读 `constraint_domains`，遇到这些条目没 basis 就 `fail`。生成器与 `check ⑦`（承认「整域不适用」一行覆盖全域）语义相反 | **R28**：投影同时读 `constraint_domains`；整域不适用的域投**一行**域级结论（规约域 / 编号写域前缀 / 「整域不适用」/ 域级 reason），域内条目不再要求 basis；逐条判断缺失才失败 |
| B2 R25 合同内节投影为空时旧区不删 | `projectAppendix` 对 `rows.length === 0` 仍 `continue`，只删「名字不在合同里」的区。激活规约从有到无、spec 某节被删或改成空但没写「不涉及」行，旧区都留着 | **R29**：对账补全——有投影替换或新增；合法「不涉及」投那一句；合法为空**删旧区**（那一节随后由 ⑫ 报空节，这是正确的告警）；真源缺失或结构错响亮失败；合同外旧区删 |
| M1 §9.5「不涉及」丢失 | 改动边界分支生成 Scope 表后直接返回，通用分支的「不涉及」提取不执行 | **R30**：改动边界分支同样投 §9.5 的「不涉及：…」一句（Scope 两行之后）；由 advisory 升为必修 |
| m 预算文案 | `scripts_mjs.reason` 仍写「总量收在 8700 整」，实际 8703、target 8900 | **m1**：文案改成与数字一致；我 §11 说「三处一致」只核了数字，没核文案 |

已核对为正确的（与外部一致）：R22、R23、R26、R27、R24 的逐条路径、project 单入口、R16 列过滤与表间空行。

**结论：不通过。R28、R29、R30、m1 修完再静态评审，再进五跑。**

## 12. 返修评审（`24d2dba5` R28/R29/R30 + m1；Claude，2026-09-05 15:30）· **通过，进五跑**

复跑：647 绿（并行 31 s）、失效形态 70 条 FAIL 0 委派 8、`framework/` 仍 18 文件差异、预算 8732/8900（scripts_mjs 2128/2200）。
真实产物上造了 §11.1 那三个此前没造过的场景：把 RES 域改成整域不适用（删两条逐条判断、加 `constraint_domains`）；把 spec §9.1 清空；把 §9.5 改成「不涉及：…」。

| 项 | 核到什么 | 判 |
|---|---|---|
| R28 | 投影读 `constraint_domains`，RES 域投一行「资源使用 / RES / 整域不适用 / 域级 reason」，域内条目不再出现，`project` 退出码 0；随后 `check ⑦` 认这一行覆盖全域，零报 | 通过 |
| R29 | §9.1 清空后 `project` 报「重投 3 节」，接口旧机器区被删；`check ⑫` 报「附录·接口是空的」——正确的告警，指向真源 | 通过 |
| R30 | §9.5 改不涉及后，改动边界机器区在 Scope 两行之后多一行「不涉及：自动充值复用既有依赖，无新 SDK。」；`specNotApplicable` 两个分支共用 | 通过 |
| m1 | `scripts_mjs.reason` 与 `scripts_py.reason` 的文案改成与 2200 / 8900 / 现值一致 | 通过 |
| 夹具 | 三条全部取真实产物，修前都红 | 通过 |

累计 advisory 一条（不拦）：槽位表头只放一列锚。

**结论：步骤 15 通过，进五跑。** 五跑判据：`steps/15` §4；`reviews/15` §5 两条（流程章至少一张图、术语行 ≥ 打底行）；§11 三条（作者在草稿上写并用草稿落盘；首次 `check` 的 ⑫c/⑩/⑨ 为零；登记时 `project` 被调用且 D 表与 knowledge-use 一致）；再加一条——五跑日志里不出现读 `story-build.mjs` 源码。

## 13. 五跑首次起跑的产物质量（`story-suite-20260905-145704`，story 15:21–15:36 成文，verifier 因 §reviews/14 §5 的 schema 缺陷未发布；Claude，2026-09-05 16:10）

闭环没到，但三份产物已定稿（`story_written` 15:36），可以先评产物与作者行为；性能轴等修完重跑。

### 13.1 作者行为（与四跑对照）

| 项 | 四跑 | 五跑 |
|---|---|---|
| story 段停等 | 3（T1 失效多一次） | **3**（材料、范围、术语）——T1 生效，第三次是 framework 的术语确认 |
| 作者在哪写 | scratch 自建章文件 | **十份草稿上写，`chapter --from drafts/`**，术语与流程图种子直接用 |
| 首次 `story-build check` | 29 处（⑦15、④3、⑫c8…） | **1 处**（⑨ 禁用词「回退」，一处措辞） |
| 读 `story-build.mjs` 源码 | 12 次 | **0 次** |
| 登记 | — | `project → number → check → 登记`，草稿已清理 |
| 流程章画图 | 0 | **1 张 flowchart**，主路径 9 步 + 5 条分支节 |
| 附录 | 手抄、略写 | 四节机器投影，D 表 15 行依据齐、E 三行带类别与 `../` 链接 |

### 13.2 story.md 逐章

好的部分：背景四段有指标；术语表 9 行 + 「容易混淆的几组」；参与方五列、取舍三列（否了什么 / 为什么不行 / 选了）、风险三列，都是内容驱动不是凑表；流程章一张图覆盖开关、实名、已有签约、验证、创建五个分支，分支节用「什么时候发生 / 怎么处理 / 之后由谁继续」；两张界面图与流程图图片各在讲它的节里带承接句；异常两表分开；验收五组表沿用产品编号；交付章无表；主叙事零工程标识、零仓内路径。

**两处缺陷，都是机制造成的**（记 R31、R32，随 schema 缺陷一并修）：

| 编号 | 现象 | 根因 | 改法 |
|---|---|---|---|
| R31 | §3.3「交接约定」出现了，可本需求**没有兄弟单据**（§3.1 自己写明「没有兄弟单据与之并列」）；表只有一列「约定」，四行填的是端云与支付的约定，不是交接 | 合同 `03-scope.form.tables_when` 只给了「特性分工」`siblings` 条件，「交接约定」没有条件；草稿于是渲染了单列锚表，作者对着槽位编了内容——**槽位在，作者就会填** | 「交接约定」加 `tables_when: siblings`；单列锚表改为渲染 `form.note` 里推荐的列（check 仍只核锚） |
| R32 | 附录五节标题没有 `A.`–`E.` 序号（金样有；合同注明「小节序号 A.–E. 必带（模板给形态）」） | 序号原来由退场的模板给，草稿没补、`number` 只铺数字序号 | `number` 给附录小节铺字母序号（与章序、小节序同一处），草稿不带序号 |

小问题（评分参考，不改机制）：术语 9 行全是 spec §0 的种子，四跑自加的「单日充值上限」「连续失败停用」「功能开关」这次没进术语表；§7.1 受限表与 §5 分支有三行重叠（未实名、已有签约、验证取消）；附录 B 的介质写「待定」是 spec §9.2 原样投影——真源如此，story 如实。

### 13.3 review.md

七条议题全部 settled、待确认为空，形态正确。2.2.1「档位沿用产品给定值」是纯事实复述（材料直接给定、无判断参与），按 story-write 的准入判据不该占表态位；决策 id 有两个是机器味（`policy-rename`、`ding-flag-source`），只在注释里，不影响读者。

### 13.4 spec.md 与知识

knowledge-use 11 命中 / 4 不命中，附录 D 表逐行一致，依据都落到具体接口、存储键、API 名（verifier 的 bedside 报告也这么判）。verifier 对 spec 的三条 WARN 属 spec 自身：管理页的修改/关闭两条 P0 路径没进 §5 流程图；详情页入口组件没进 §4 组件表；引用交叉核对。这些在 story 里反而是齐的（§5.3、§6.2）——story 比 spec 讲得全，方向对。

### 13.5 建议分（待闭环后正式给，用户定）

产物结果：**88**——形态、图、附录、语言红线都到位；扣 R31（多出一节、单列表）、R32（附录无序号）与 2.2.1 空议题。Knowledge 应用：**90**。性能：等 schema 修完重跑再评——本次到 verifier 交稿是 43 分钟（四跑 61），读源码 0 次，首次 check 1 处，方向已经对了。

### 13.6 外部第七轮意见 · 核对（2026-09-05 16:30）· 我在 §13 漏了内容矛盾

对着真实产物核过，它说的三处都在：
- story §5.1 第 2 步与 §5.2 写「未实名用户点入入口后引导去认证」，§8.1 AC-R1 写「未实名用户看不到入口」——同一条件两种说法。来源在上游：PRD 第 48 行旅程表写引导实名，第 76 行 AC-R1 写看不到入口，第 83 行 AC-R8 又写引导。作者把矛盾原样传到了验收，读者审查（bedside 报告）也没报。
- 流程图 `F → G[实名完成返回] → H[回填此前选择]`，H 没有出边，实名回来之后接不回签约页。
- 我 §13 只看了形态与投影，没逐条核业务路径的一致性——**88 分给高了**，且「剩下主要是形态问题」这个判断不成立。产物建议分暂改 80–83，等六跑再定。

它对 `steps/16` S2 的五条修正意见，逐条核后都成立，已改进方案（见 `steps/16` S2 与 S4）：
1. 命中知识不能强制挂 §9——五跑四条空落点是 RTL、图标、文案、翻译，本来就没有接口 / 键 / 配置 / 事件可挂；改为「涉及 §9 实体才引用，否则写落点类型（页面 / 资源 / 文案清单）」，落点由 plan 冻结。
2. 40 / 60 字不做门禁，且两处数字互相打架——改为写作提醒；保留「一条要求一行」；§9 表外的必要说明有去处（`spec/notes.md` 或决策件），check 指向那里而不是只说删。
3. 4.1–4.4 必有与设计表「真有才有」冲突；页面状态表以「有没有图片」为条件把材料形式当成业务适用性——改为 4.1 必有、4.3 有已定决策时必有、4.2 / 4.4 真有才有；状态表草稿渲染、不设条件、不核。
4. `requirement` 改列表要改全所有消费者——补消费者清单。
5. 「verifier 恰好一次」改为「同一产物不重审」；S1 的验证加一条真链路：用 harness 生成的 request 喂插件。

它的总判断我接受：每发现一次表达问题就把形态固定进合同，是把内容质量问题转成格式约束；被核的事实本身得是需求的不变量。对应的正向改法是 S4——矛盾为什么能穿过作者和独立审查：作者的统稿六项里没有「同一条件各章说法一致」，读者审查任务里也没有这一问。补的是问题，不是判据。

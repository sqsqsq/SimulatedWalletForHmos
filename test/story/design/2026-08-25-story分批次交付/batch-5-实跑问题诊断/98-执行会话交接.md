# 执行会话交接（2026-09-05）

给下一个执行会话。**从这里读起就够，不必回溯对话**。

## 一、现在卡在哪

| # | 事 | 状态 |
|---|---|---|
| A | 同步的返修（`reviews/14` §3） | **已完成**（`4008c443`） |
| C | 三件要用户定的 | **已定**（2026-09-05，见 §五） |
| B | **步骤 15**（`steps/15-fourth-run-forward-fixes.md`）：F1–F9 | 方案已按四条裁定更新，**下一步就是它** |

## 二、批次 5 走到哪了

| 步骤 | 状态 |
|---|---|
| 1–11 | 通过 |
| 12（验收前正向重设计 D1–D12）+ 返修 | 通过 |
| T1–T5（三跑观察后的修正） | 通过，四跑验证生效 |
| 13（adapt 带 framework 补丁，扩展升 1.4.0） | 通过 |
| 14（main → story 同步，framework 升 3.0.0） | 通过（返修已做完，`4008c443`） |
| 15（四跑正向修正 F1–F9） | 方案已定，未开工 |

四跑实跑报告：`14-四跑实跑报告.md`（三轴分 **85 / 72 / 90**，用户 2026-09-05 定）。

## 三、当前的地基与状态

- `framework/` = **3.0.0（`85e266f`）+ 18 处具名改动**（12 作者入口 + 6 opencode verifier）；
- 与 main 的差异**恰好 18 个文件**，与 `doc/extensions/framework-patch.yaml` 声明逐一相等；
- `framework.config.json` 的 `drift_allowlist` **18 条**，与那 18 个文件集合逐一相等；
- 扩展包版本 `1.4.0`；
- 离线：story 全量 **608 绿**（`pytest -n auto --dist loadscope`，约 25 s）、
  失效形态 **70 条 FAIL 0 / 委派 15**、`adapt-scan --scan` 后 `--check` 六项通过。

## 四、A：同步返修做了什么（已完成，`4008c443`）

1. 从 `git diff main story --relative=framework/ -- <6 个 opencode 文件>` **重生成**
   `artifacts/01-framework-opencode-verifier.patch`。旧补丁必须重生成而不是改基线号：
   它在 3.0.0 上 `adapter-schema.yaml` 那段冲突（上游改写了 `subagent_stop` 的措辞），
   而且不含 1.2 跟上的 subject `@2`。
   验证：从 `85e266f` 还原纯净 3.0.0 树 → `git apply --check` 通过 → apply 后六个文件
   与本仓实现逐字节一致（行尾归一）。
2. `01-upstream-handoff.md`：基线改 `85e266f`、验证行改「纯净 3.0.0 树」、写明
   `adapter-schema.yaml` 是**在上游新措辞上加一档**、补上「插件按 `@2` 派生」、
   验证数字更新为 608 / 70 条。
3. 删 `drift_allowlist` 三条陈旧条目，剩 18 条与真实差异集合相等（脚本比对过）。

**排障提示**：`git apply` 在这台机器上要设 `GIT_CEILING_DIRECTORIES`——
`C:/Users/kangz` 本身是个 git 仓库，纯净树解到临时目录里会被它认领，
于是 apply 报一串 `Skipped patch`（不是补丁的问题）。

## 五、C：三件已定（用户 2026-09-05）

1. **四跑三轴分 = 85 / 72 / 90**。产物结果从我给的 90 降到 85：形态问题成片出现（术语成列表、
   异常九段标签、验收九节各一条、主路径没画图、`§9.1` 病句、评审记录漏进 framework 档位术语）
   加附录 A/B 相对 spec §9 略写。报告 §5 已按此改写。
   **仍不构成「批次 5 达标」**——性能 72 未达目标，不建基线。
2. **预算一次签四项**：`data` 750→800、`hooks_mjs` 2450→2700、`scripts_py` 1200→1400、
   总量 8350→8400。已写进 `mechanism-budget.yaml` 各处 reason。
   注意**总量不再是各类 target 之和**（那是 8850）：各类是各自的宽松上限、不许同时用满，
   总量才是硬约束。
3. **步骤 15 的提交 2 不拆**：那几件共用同一份合同数据，拆开时中间态的 `check`
   核不了刚加的槽位，拆出来的提交并不自足。

另有一条同日裁定写进了方案 §2 F7：**术语只作起始行、`check` 不核集合**
（spec §0 是技术域术语，硬核「一个不许少」会把模块名灌进业务读物）；
图与附录 A/B/C 仍按「只增不减」核集合。

## 六、这一轮踩过的坑

| 坑 | 教训 |
|---|---|
| 拿「与上游的 diff」当我们的改动清单 | 里面混着**上游新增的功能**（`--report-reconcile-only`、Native evidence gate）。要以我们自己那两笔提交为准 |
| `git checkout main -- framework/` 以为就干净了 | 它**不删** story 独有的新增文件（`author-context.ts` 等三个），要手工删 |
| 断言「codex 的 verifier 机制有问题」 | 错的。framework 对无能力声明的 adapter 判 `blocked` 并如实报 `verifier_provider_unavailable`，那是设计内的三态，不是故障 |
| 以为「没有发布器就认执行方自写的报告」 | framework 从不认这条路。扩展原来那四种自写文件名一直是**扩展自己造的旁路** |
| 只在候选版上验补丁 | main 实际升到的是更新的 `85e266f`，结论会变（opencode 那份就打不上了）。**要在真正的目标树上验** |
| heredoc 里写 `\n` 之类的转义 | 反复被 shell 吃掉。改用 Write 工具写脚本文件再执行 |
| 跑 `adapt-scan --scan` 后忘了清 `.adapt-<版本>/` | 它是 `.json`，会被预算统计算进 `data`（曾让 data 从 745 跳到 1701）。跑完 `rm -rf` |

## 七、常用命令

```bash
# 离线全量（默认并行；串行只作排障）
python -m pytest test/story/tests -n auto --dist loadscope
python test/story/scripts/check_failure_modes.py

# 扩展装配自检（--check 前必须先 --scan）
node doc/extensions/skills/story-adaptation/scripts/adapt-scan.mjs --scan  --target .
node doc/extensions/skills/story-adaptation/scripts/adapt-scan.mjs --check --target .
rm -rf doc/extensions/.adapt-1.4.0            # 工作件，别留

# 预算读数
python -c "import sys;sys.path.insert(0,'test/story/tests');import test_mechanism_budget as m;print(m.measure()[0])"

# 作者入口（同步后要能跑通）
cd framework/harness && npx ts-node scripts/author-context.ts --phase spec --feature <名>
```

CLI 实跑的完整操作见 `test/story/TEST.md`（§0 非沙箱授权、§3.0 回话只回一句、§7.9 离线回归规约）。
**跑 CLI 前等评审**，这是用户的既定要求。

## 八、给 framework 上游的材料（已就绪，不必再动）

- `artifacts/framework-proposal-author-context.md` —— 提案（论证问题成立与解法最小），
  含实现提交坐标 `8a8d8a51` 与它的完整提交信息；
- `artifacts/04-framework-author-context.patch` —— 12 文件，在 `85e266f` 上零冲突；
- `artifacts/01-*` —— opencode 那份，返修后在 `85e266f` 上验过，**可用**。

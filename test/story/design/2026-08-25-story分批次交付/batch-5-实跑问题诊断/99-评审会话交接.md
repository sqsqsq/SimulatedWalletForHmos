# 评审会话交接（Claude，2026-09-03）

> 给上下文压缩后的本会话（或接手的评审会话）读。只记「现在在哪、规则是什么、下一步做什么」；
> 事实与证据在各文件里，这里只给指针。读完本文件再读 `STATUS.md` 的状态表与事件日志最后 10 条。

## 1. 我的角色

- 我是**独立评审者**，不是实施者。另一个会话（执行会话）按 `steps/NN` 实施，写「已实施，等待评审」；
  我从实际 diff 与亲自复跑的结果下结论，写进 `reviews/NN-*.md`，并更新 `STATUS.md` 的状态行与事件日志。
- 用户是唯一裁定者。评审只列问题与正向目标形态，不替用户拟选项菜单。

## 2. 用户在本批确立的规则（不要再犯）

1. **不构造异常场景给裁判出题**。正式评价只用**正常需求**跑真实 Story（步骤 11），用户按 `TEST.md §10` 三轴评分判。
   审查者（verifier）是安全网，它有没有区分力从「用户在真实产物上找到的问题它报没报」看。合成缺陷稿、删事实测试、
   资格门都只是诊断工具（`TEST.md §9/§9.1`），不是任何步骤的前置。D10 §1 已撤销（`03` 有修订记录）。
2. **脚本只做纠偏兜底**，不承担语义判断，不靠脚本达成优化目的。新增脚本行必须归到 D2 §4 的确定性失败之一。
3. **规模预算是需求级规则**（`AGENTS.md §7.5`、`test/story/regression/mechanism-budget.yaml`）：进行中核方案声明的峰值，
   完成时核上限；不按步骤拦中间态；红了是提醒回读 AGENTS，不是裁剪方案。语义代理标识任何时候不得增长（现 34，冻结 37）。
4. **规则要写进 `test/story/AGENTS.md`**，让新会话读它就知道，不靠脚本拦；我曾连加三轮拦截脚本被纠正。
5. 交付面（`doc/extensions`、`framework`）不写本仓实跑读数与维护故事；三处曾被我拦下改写。
6. 批次 5 的 CLI 只有一次：步骤 11 跑 `auto-topup` 到 spec。步骤 2 smoke 与步骤 7 夹具留为离线器材。

## 3. 步骤状态（2026-09-03 晚）

| 步骤 | 状态 | 评审 |
|---|---|---|
| 1–5 | 通过（4+5 合审） | `reviews/01`…`04-05` |
| 6+8 | 通过，两条裁定已回写方案 | `reviews/06-08` |
| 7 | 装置通过；执行证明返修已做（`85355bb6`）；资格实跑撤销 | `reviews/07` §8 |
| 9 | 第一段、小段 1（含返修 `41ff385f`）、小段 2（`d78b69e0`）通过；**小段 3 待做** | `reviews/09` |
| 10 | 未开始 | — |
| 11 | 未开始（真实 Story + 三轴评分 + 退场） | — |

## 4. 小段 3 的范围（我在 `reviews/09` 登记的三座桥 + 原定内容）

- 原定：`audit` 命令与 `init` 枚举退场、`source-units.mjs` 缩到确定性路径真正用到的部分并改名、`story-verify.md` 与
  `baseline_coverage.py` 删除、作业书改为「任务包一次给全（图片从 `materials.json` 逐张列出含路径）、按章经命令落盘、
  统稿逐章替换」、测试清理。
- B1 `check` 的 `requireLedgers` 仍要五件台账 → 与 `story_flow.py` 的 `STORY_SRC_FROZEN` 一起收到 `decisions.json`（+ `copyedit.md`）。
- B2 固定形式类判据（⑫e 固定形式、⑫b `min_sections`、⑪ 可读性、⑫c 形态 lint）随作业书改写一起退，形态迁 `observed`。
- B3 `materialUnitsNow` 随 `init` 枚举一起删。
- 完成条件在 `steps/09` 末尾；预算：`scripts_mjs` 与 `semantic_proxy` 的 ceiling 退场后压到现值。

## 5. 评审时固定动作

1. `git log` 找本段提交；`git show <hash> --stat`；读实际 diff，不读实施记录的自述。
2. 亲自复跑：`python -m unittest discover -s test/story/tests`（约 1 分钟；曾出现过一次 24 分钟 + 1 error 的不稳定运行，
   见 `reviews/09` A2）、`tools/cli/tests`、`python test/story/scripts/check_failure_modes.py`（73 条、委派数）、
   `test_mechanism_budget.py`、`node --check` 改过的 `.mjs`。
3. 对照 `steps/NN` 完成条件逐条给证据；范围与保护区零差异；预算变化归类。
4. 写 `reviews/NN` 新小节，更新 `STATUS.md` 状态行与事件日志。

## 6. 文件地图

- 方案：`03-方案讨论决策.md`（D1–D12，D10 修订）、`05-实施方案总览.md`、`06-验收追踪矩阵.md`、`steps/01`–`11`
- 诊断：`00-问题记录与原因分析.md`（P1–P16）、`01-四批次全量问题与根因审计.md`、`04-失效形态长期要求审计.md`
- 评审：`07-方案评审意见.md`（方案级）、`08-批次5校准.md`、`reviews/*`
- 契约：`test/story/AGENTS.md`（§7.5 预算）、`test/story/TEST.md`（§7.0 verifier、§8 度量、§9 诊断、§10 三轴评分）
- 记忆：`~/.claude/projects/.../memory/story-round-discipline.md` 第 26–28 条是本批新增的用户纠正

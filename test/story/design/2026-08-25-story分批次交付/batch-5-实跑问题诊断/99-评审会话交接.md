# 评审会话交接（Claude，2026-09-05 更新）

> 给上下文压缩后的本会话读。只记「现在在哪、规则是什么、下一步做什么」，事实与证据只给指针。
> 读完本文件再读 `STATUS.md` 的状态表与事件日志最后 15 条。

## 1. 我的角色

独立评审者，不实施。执行会话按 `steps/NN` 实施并写「已实施，等待评审」；我从实际 diff 与亲自复跑下结论，写进 `reviews/NN-*.md`，更新 `STATUS.md` 状态行与事件日志。
用户是唯一裁定者；评审只列问题与正向目标形态，不替用户拟选项菜单。**自述不能替代审查**——本批三次抓到自述与事实不符（首跑报告、步骤 10 签名、步骤 12 实跑故事）。

## 2. 用户在本批确立的规则（不要再犯）

1. 不构造异常场景给裁判出题；正式评价只用正常需求跑真实 Story，用户按 `TEST.md §10` 三轴评分。
2. 脚本只做纠偏兜底；语义归作者与独立审查；不用字符串近似语义（`semantic_proxy` 恒 0）。
3. 预算是需求级规则（AGENTS §7.5），**只数代码行不数注释**；允许正确必要的增长（第一档保留并签字）、放错地方的搬回可读处（第二档）、维护痕迹清零（第三档）。
4. 交付面注释与提示词只讲当前，不写演进史、轮次、实测计数、Case 名（AGENTS §5.3；M02 兜底含「轮次叙述」档）。
5. 规则写进 `AGENTS.md`，不靠脚本拦；先核约束再定机制（12 号方案 A 的教训：未核 framework 协议就写了改 adapter 的方案）。
6. 材料关卡：补料后只在发现新缺口时再停；停等目标两次（放材料、确认范围）。停等消息 ≤12 行三段式。
7. 需求方回话只回选项或一句短语（`TEST.md §3.0`，三个用例脚本已改为立场短语）；只抽图导入档由模型判断，不问人。
8. 离线回归默认 `pytest -n auto --dist loadscope`；测试只写临时目录；重夹具一类一次；最慢十条要报。
9. verifier 只跑一次且在最后，调用只带 request JSON；证据只认插件发布的 JSON；不改 framework 协议与 adapter。
10. 工作区按黑名单复制且带 `node_modules`。

## 3. 状态（2026-09-05）

| 步骤 | 状态 |
|---|---|
| 1–10 | 通过收口 |
| 11 | 首跑、二跑不计分（环境/机制缺陷）；三跑在 spec 入口手动停止（五次停等归因见 `reviews/11` §13）；退场清理与预算收口通过，target 8350 用户签字 |
| 12（13 号正向重设计） | 四提交 + 返修通过收口（`reviews/12`）；T1–T5（`42bc5554`）**已实施未评审** |
| 13（`951d9d61`，adapt 带扩展依赖的 framework 改动、升 1.4.0） | **未评审**，来历不在我的方案里，先读 STATUS 该段再定 |
| 四跑 | `story-suite-20260904-194427`，20:33 起跑，21:26 仍在 spec，**story 段又停等 5 次**——要按 §13 同法归因，核 T1–T5 是否生效 |

## 4. 压缩后先做

1. 读 `STATUS.md` 状态表与最后 15 条日志；读 `steps/11`「三跑观察后的修正」T1–T6。
2. 评审 `42bc5554`（T1–T5）与 `951d9d61`（步骤 13）：diff、复跑（`pytest -n auto --dist loadscope`）、73 条、预算门、`framework/` 差异（步骤 13 改了 framework——核用户授权、上游补丁与漂移放行）。
3. 四跑：用 `measure_run.py` 与 `run_analysis*.py`（scratchpad 已失，按 `reviews/11` §12 的方法重写）看：停等次数与归因、任务包是否被读（`key_inputs_read`）、读脚本次数（含 bash 形态）、图片逐张去处、上下文、verifier 次数与证据来源、framework 前置单列。
4. 三轴分只在四跑闭环且硬条件满足后议。

## 5. 文件地图

- 方案：`03`（D1–D12）、`05`、`06`、`steps/01`–`11`、`12-story审查正向设计.md`、`13-批次5验收前的正向重设计.md`（§7 正向判据、§8 可行性核对）
- 评审：`reviews/01`–`12`，`reviews/11-real-run-observation.md`（§1–§13：三跑观察与归因）、`reviews/12-forward-redesign.md`
- 契约：`test/story/AGENTS.md`（§5.3 注释规则、§7.5 预算、§8 自检）、`test/story/TEST.md`（§3.0 回话规约、§7.9 离线回归、§8 度量、§10 三轴）
- 预算：`test/story/regression/mechanism-budget.yaml`（target 8350；hooks 2657 高于 2450 属必要增长待签）
- 记忆：`~/.claude/projects/.../memory/story-round-discipline.md` 第 26–28 条

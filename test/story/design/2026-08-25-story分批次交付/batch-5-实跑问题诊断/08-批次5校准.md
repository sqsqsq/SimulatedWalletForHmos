# 批次 5 · 按 `test/story/AGENTS.md` 的校准（2026-09-03，评审者 Claude）

> 对象：批次 5 至今的全部改动——提交 `e1b73b00`（方案冻结）、`4334ba5c`（步骤 1）、`49f9ed54`（步骤 2）、`13d6abee`（步骤 3）、
> `80f5cc3e`（TEST.md 收口，未编号）与工作区中未提交的步骤 4+5。
> 排除项：用户明确授权暂缓的部分（步骤 2 实跑、结论 A/B）。
> 方法：逐条拿 AGENTS 的条款对实际文件，不看自述。

## 1. 未偏离（有证据）

| AGENTS 条款 | 核对方式 | 结论 |
|---|---|---|
| §0 只覆盖 interactive | 步骤 1 `modes: [interactive]`；步骤 4 入口只服务 interactive 作者；无 headless/goal 改动 | 成立 |
| §1 两角色不混用；执行者不读维护材料 | 入口文件 `CLAUDE.md/AGENTS.md` 的扩展段只指向 framework 脚本与扩展钩子，不指向 `test/story` | 成立 |
| §3 问题修在最早拥有职责的位置 | verifier 能力归 Framework adapter（步骤 1）；作者通道归 Framework 入口 + 扩展登记（4/5）；P9 由测试域判为责任错位外送步骤 6，未在测试域模拟产品状态 | 成立 |
| §4.2 脚本只做确定性事 | `measure_run.py` 只报数；`run_smoke.py verify` 六项皆结构与身份检查；`author-context.ts` 只读只输出 | 成立 |
| §4.3 留痕借既有门禁、不加新门禁 | overlay `phase_input_snippets_extra` → 既有 `context_exploration_inputs_coverage`；零新状态、零新事件 | 成立 |
| §5.2 单一真源、契约与消费者同步 | author.md 全仓唯一（测试锚死）；harness 后置调用删除；旧远距离提醒从三份入口删除；`--isolated-workspaces` 从 TEST 删除；schema、四份模板、概念文档同步改写 | 成立 |
| §5.3 零 Case 特征 | TEST §7.2 前四条扫描零命中（噪声仅占位形态与 XML 命名空间） | 成立 |
| §6 Knowledge 未动 | 五步均未改 `knowledge/`、激活清单与消费链 | 成立 |
| §7.2 同一职责一套路径 | 见 §5.2；未见静默降级（入口失败退出 1、smoke 未知确认停等、度量读不到报 null） | 成立 |
| 05 §6 保护区 | 产品源码、金样、两个 Case、`RELEASE-MANIFEST*`、其他 adapter 零差异（每步 `git status` 核） | 成立 |
| D10 73 条形态无人失守 | `check_failure_modes.py` 73/73，无 retired | 成立 |
| 步骤 1/4 上游可交付 | 两份补丁在 HEAD 干净树 `git apply --check --directory=framework` 通过、逐字节一致；allowlist 18 条具名且写明失效条件 | 成立 |

## 2. 偏离（按严重度）

| # | 条款 | 事实 | 处置 |
|---|---|---|---|
| X1 | §5.3 交付面不含某次运行数字与维护故事 | 未提交的 4+5 有三处「实跑里……三次……2～70 分钟」（`harness-runner.ts:1071`、`author-context.ts:7`、`manifest.yaml` 注释），两处随上游补丁出仓；已提交的步骤 1 `adapter.yaml:78-80` 写了会话 id 片段与模型名（宿主版本号属框架既有「入册凭据」惯例，可留） | 4+5：提交前改写（见 `reviews/04-05` §7）。步骤 1：下次触碰 `adapter.yaml` 时删去 `ses_f9d8…` 与模型名并重生成 `artifacts/01` 补丁，不单独起一步 |
| X2 | §7.3 独立实施会话 + 维护者从 diff 重建 | 步骤 1～5 的实施与自审都在同一会话（各 `reviews/0N` §0 已自述）。用户以外部独立复审补偿，复审结论已追加到各报告 | 后续步骤按 05 §4 分会话；自审报告改名「实施记录」，只保留评审者的 `## 独立评审` 作结论 |
| X3 | 05 §5 每步一个可回退提交 | 4+5 在同一工作区，尚未提交；`80f5cc3e` 是计划外的未编号提交（内容合规：删 TEST 第二真源），但 STATUS 事件日志没有登记它的动机 | 4、5 分两次提交；事件日志补一行 `80f5cc3e` |
| X4 | §5.2 文本与执行一致 | 用户已按「A(4+5)、B(6+8)、7、9、10、11」六个审核点执行，05 §3/§5 仍写逐步审核；STATUS 步骤 4 行仍是「未开始」 | 05 §5 增写审核分组（保留每步一提交）；STATUS 步骤 4 行改为「已实施，等待评审（与 5 合审）」 |
| X5 | TEST §7.2 扫描要抓住交付面维护痕迹 | 第五条模式只认「实测」，X1 的「实跑」漏网 | 词表扩为 `实测|实跑|实证`，归步骤 11 最终扫描；不新增机制 |

没有发现与 D1～D11 决策相悖的实现，也没有发现旧机制与新机制并存的路径。

## 3. 排除项（授权暂缓，不计偏离）

- 步骤 2 真实 smoke 未跑，结论 A（Framework 能力）与 B（语义有效）未取得；步骤 3～5 的进入条件是授权跳过。
- 由此挂账的两条：`reviews/01` R1「插件注册失败无声」仍未被实跑覆盖；A03/A05「作者真的在写前读到」只有通道与留痕，无行为证据。

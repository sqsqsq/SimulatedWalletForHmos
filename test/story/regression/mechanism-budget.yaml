# 机制层预算 —— 一次**完整需求**（一个批次）对 doc/extensions 非知识层规模的承诺。
#
# 三个角色各做一件事：
#   · 维护者定方案时**同时给出预算**：本需求进行中的峰值（interim_ceiling）与完成后的上限（target），
#     每个数字具名 + reason（新增脚本行归 D2 §4 哪一类确定性失败、退掉什么）；
#   · 执行者改完后**自检**：超出峰值是否合理（方案估错就回方案改预算并写原因，不是砍方案）；
#     远低于预期是否有遗漏（该建的没建、该退的没退）；
#   · 评审在测试后**发现差异并申请特别审视**：对照本文件 diff 与方案的预算，超与不足都要给理由。
#
# 它不按步骤拦中间态：需求进行中允许先加后删，只要不超过方案自己声明的峰值；需求完成
# （status: closed）时必须回到 target 以内，这是完成条件的一部分。语义代理标识（semantic_proxy）
# 不分进行中与完成——它不是规模，是方向：脚本里再出现相似度、复述、最短引文这类词，就是又在用
# 字符串近似「讲清没讲清」，任何时候都不合入。
#
# 判据由 test/story/tests/test_mechanism_budget.py 机械执行，进全量离线回归；红了先读报错里
# 指的 AGENTS 条款。改本文件任何数字都要具名 + reason，和 drift_allowlist、retired 形态同一套纪律。
#
# ## 计数口径：**代码行，不含注释与空行**
#
# 配额限的是机制的规模，不是文字的长短。把注释算进配额，省下来的只会是解释——
# 而解释正是下一个维护者判断「这条判据还该不该在」的依据。
#
# 范围：doc/extensions 下，去掉三类——knowledge/（内容归目标工程）、依赖目录，以及
# **安装期工具**（`skills/story-adaptation/**` 与它的输入 `framework-patch.yaml`）：
# 那是把这套机制装到别的工程去的东西，装完不参与任何一次需求。把它算进配额，
# 等于让「更好装」和「机制更小」互相挤占（用户 2026-09-05 裁定）。
# 类别按路径与后缀。逐类剥注释：
#   · .mjs  —— `//` 行与 `/* */` 块
#   · .py   —— `#` 行与 docstring
#   · .yaml —— `#` 行
#   · .json —— 说明键（键名以 `_` 开头或以 note 结尾）
#   · .md   —— 提示词正文本身是内容，只去空行
#
# 数字只是围栏，不表达质量；表达质量的判据在 AGENTS §4.2 与 D2 §4。
#
# ## 「target = 当时的现值」不是惯例
#
# 批次 5 收口时按现值重定了 target，那是一次具名裁定，不是定 target 的方法。理由只在那一次成立：
# 换成只数代码行之后，剩下的都是在用的判据实现，再压就是删判据，而那一步明令不改行为。
# **批次 5 本身不压缩规模；压缩另开需求**，起点见 TEST.md §8 第 7 项记的长期方向。
# 后面的批次仍按老规矩来：维护者定方案时给出这一轮要收敛到哪，而不是回填改完之后的数。
version: 2
frozen_at: "2026-09-04"
frozen_commit: "a1026080"

# 当前进行中的需求。status 由该需求收口那一步改为 closed；closed 后 target 即长期上限。
requirement:
  id: batch-5
  status: in_progress
  approved_by: WYK
  reason: "本轮方案的预算声明。计数口径 2026-09-04 由用户裁定改为只数代码行；target 同日按实际重定——新口径不数注释，剩下的都是在用的判据实现，再压就是删判据。批次 5 不压缩规模，压缩另开需求，长期方向记在 TEST.md §8 第 7 项。2026-09-05 用户裁定安装期工具（story-adaptation 与 framework-patch.yaml）不占额度：口径这一变，各类现值降了 scripts_mjs 170、prompts_md 158、data 62，总量 8706→8316——**这是口径带来的余量，不是压缩出来的**。同日用户一次签下步骤 15 的四项调整（data、hooks_mjs、scripts_py 三类 target 与总量），见各处 reason"

categories:
  scripts_mjs:
    description: skills/**.mjs（story-build、lint、review-render、flow-check、headings、adapt-scan）
    interim_ceiling: 2050
    target: 1980
    approved_by: WYK
    reason: "用户 2026-09-05 第二次签字，1900→1980。步骤 15 把作者要推算的东西改成机器算好给他：check ⑪ 形态守恒一条判据读合同（本体加表头解析、图围栏、条件槽位三个 helper 共 75 行），skeleton 从 spec §0/§5/§9 与登记数据派生术语起始行、流程图、附录 A/B/C/D/E，check ⑫ 加附录对 spec §9 的集合核。上一版 target 1900 是批次 5 中途按现值定的，那时这些派生还只是方案里的估算"
  scripts_py:
    description: skills/**.py（story_flow、import_sources、materials）
    interim_ceiling: 1400
    target: 1400
    approved_by: WYK
    reason: "用户 2026-09-04 签字。步骤 12 峰值按 13 号 §5 预算声明并按实修正：+156（图片登记与说明两条命令、caption 真源、status 扩到 spec 阶段内与侧车骨架），§5 估 +80。返修新增 caption-image——不是界面的图此前没有写说明的路径；四跑前再 +25：补料只抽图不动正文（此前「补图」只有覆盖正文一条路，会用草稿盖掉定稿）、补料后不再重复问「材料够不够」。**用户 2026-09-05 签字 1200→1400**：现值 1371 早已越过 1200，越出的正是上面这几笔具名增长；步骤 15 的 F3 是删两函数加一函数，估 ±0。峰值与 target 同值——本步无先加后删"
  hooks_mjs:
    description: hooks/**.mjs（六阶段 post_check + shared）
    interim_ceiling: 2760
    target: 2700
    approved_by: WYK
    reason: "用户 2026-09-05 签字，2450→2700。上一版 target 2450 是按批次 5 中途的现值定的，此后作者任务包与 knowledge-use 骨架合计 +223、D4 读者审查再进来，现值 2657；步骤 15 的任务包三处与 post_check 表头核约 +40。这一类的增长全部是「把作者要推算的东西改成机器算好给他」——上一版 reason 已预告收口时回不到 2450，此次按实际签字，不砍方案"
  prompts_md:
    description: 所有 .md（SKILL、phases、rules、templates、hooks/*/author.md、AGENTS.section）
    interim_ceiling: 2050
    target: 2050
    approved_by: WYK
    reason: "现值 1996 已低于折算 target，峰值取 target；提示词正文即内容，用户 2026-09-04 签字"
  data:
    description: contracts/*.json、rules/*.yaml、manifest.yaml
    interim_ceiling: 800
    target: 800
    approved_by: WYK
    reason: "用户 2026-09-05 签字，750→800。步骤 15 把各章的形态（主形态、允许的次要形态、机器核的固定槽位、小节标题档位）从散文搬进合同 story-chapters.json，语言红线再收一批 framework 档位术语——**形态进数据正是这一步的设计本身**，不是超支。§5 估 +60 至 +90（现值 683）。峰值取 target：本步 data 只增不减，没有先加后删"

total:
  interim_ceiling: 8910
  target: 8600
  approved_by: WYK
  reason: "用户 2026-09-05 签字 8350→8400，同日第二次签字 8400→8600。**总量不再是各类 target 之和**（那是 8930），它比各类之和紧——各类额度是各自的宽松上限，不允许同时用满，总量才是硬约束。8600 来自 F1 落地后的实测重估：data 739、scripts_py 1400 已定，prompts_md 因 story-template.md 退场 −175 后约 1773，hooks_mjs 约 2682，scripts_mjs 约 1980。峰值取各类 interim_ceiling 之和，容本步先加后删；收口仍须回到 8600 以内"

# 语义代理标识：脚本里出现这些词，说明有人又在用字符串近似「讲清没讲清」。
# 它本来就只数可执行行，口径不变。
semantic_proxy:
  pattern: "similarity|paraphrase|levenshtein|jaccard|min_quote_chars|回声"
  scope: [scripts_mjs, scripts_py, hooks_mjs]
  ceiling: 0
  # 不分进行中/完成：这些词在可执行代码里出现，就是又在用字符串近似语义，任何时候都不合入。
  target: 0
  approved_by: WYK
  reason: "相似度、复述、引文长度核实三条路径已全部退场，现值 0；任何新增当场红"

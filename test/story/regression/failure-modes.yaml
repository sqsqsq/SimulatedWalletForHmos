# 失效形态台账（G1 · 永久留档）
# =============================================================================
# 每个已实测的失效形态一条。**每批全量回归**，不只验本批新增（坑 #3）。
#
# 字段：
#   id                形态标识，全局唯一，不复用
#   dimension         knowledge | dedup | readability | infra
#   found_round       首次实测到该形态的轮次（日期）
#   form              形态一句话描述（"错的长什么样"）
#   clause            被违反的需求条款（01-全批次需求记录.md 的 G/B 编号）或坑册编号
#   checker           check_failure_modes.py 内的 checker 函数名
#   target            mechanism（检查 doc/extensions 机制文件）| product（检查 feature 产物）
#   bad_fixture       反夹具目录：checker 必须判 FAIL
#   good_fixture      正夹具目录：checker 必须判 PASS
#   self_check        internal = 正反例内建在 checker 的数据里（逐分支各带预期），
#                     不用外部夹具目录；此时 bad_fixture / good_fixture 不填
#   applicable_when   对真实目标运行的前提
#   severity          BLOCKER | WARN
#   status            fixed（永久回归） | pending_capability（目标能力未建，只做夹具自检）
#                     | retired（须 reason + approved_by）
#   responsibility    发现者是谁：不写 = script（跑 checker）；observed / verifier /
#                     behavior_test = 责任已委派，两步都不跑，回归里单列一档。
#                     委派须 reason + approved_by——把一条形态从脚本手里拿走，等于承认
#                     「机械发现者缺席、由别的东西兜底」。observed 还须 observed_by：
#                     写清真实运行里谁在什么时候看什么，否则「由真实结果兜底」只是一句话
#   observed_by       仅 observed：真实 Story 运行里由谁看什么
#   variant_of        若为既有形态的新变体，指向原形态 id（旧检查保留，另增新检查）
#
# 回归语义（check_failure_modes.py）：
#   1) 夹具自检 —— 所有 status != retired 的条目都跑：bad 必 FAIL、good 必 PASS。
#      自检不过 = checker 本身失效（比漏检更危险），整体判 FAIL。
#   2) 真实目标 —— 仅 status == fixed 的条目跑；pending_capability 报 SKIP 并计数。
#      能力交付后把 status 改 fixed，真实目标回归自动生效。
# =============================================================================

schema_version: "1.0"
updated: "2026-08-25"

modes:

  # ---------------------------------------------------------------------------
  # 机制层（doc/extensions 源码自身的失效形态）
  # ---------------------------------------------------------------------------

  - id: M01-hardcoded-domain-prefix
    dimension: knowledge
    found_round: "2026-08-21"
    form: 机制层脚本/规则里写死约束域前缀或域中文名，新增一个域要改代码；域清单成为第二份真源
    clause: G7 / 坑 #29 / 坑 #40
    checker: m01_hardcoded_domain_prefix
    target: mechanism
    bad_fixture: test/story/fixtures/failure-modes/M01-hardcoded-domain-prefix/bad
    good_fixture: test/story/fixtures/failure-modes/M01-hardcoded-domain-prefix/good
    applicable_when: always
    severity: BLOCKER
    status: fixed
    scope_note: >
      扫描面含 .md。早先只扫代码、放过 Markdown（理由是「写作指令允许出现形态示例」），
      结果实测 16 处硬编码里有 10 处藏在注入件里全部逃检——注入件恰恰是模型直接读到的东西。
      判定基准（真实条目与文件名）从激活清单派生；占位形态与反例说明不计。

  - id: M02-test-case-features
    dimension: knowledge
    found_round: "2026-08-23"
    form: 机制层或注入件出现测试 Case 的单号、业务名或其需求特征（过拟合，换个业务域就不成立）
    clause: G7 / AGENTS.md §2「机制层零测试特征」
    checker: m02_test_case_features
    target: mechanism
    bad_fixture: test/story/fixtures/failure-modes/M02-test-case-features/bad
    good_fixture: test/story/fixtures/failure-modes/M02-test-case-features/good
    applicable_when: always
    severity: BLOCKER
    status: fixed

  - id: M03-hardcoded-layer-names
    dimension: knowledge
    found_round: "2026-08-23"
    form: 机制层写死架构外层目录名（层清单应从架构 DSL 派生）
    clause: G7 / 坑 #29
    checker: m03_hardcoded_layer_names
    target: mechanism
    bad_fixture: test/story/fixtures/failure-modes/M03-hardcoded-layer-names/bad
    good_fixture: test/story/fixtures/failure-modes/M03-hardcoded-layer-names/good
    applicable_when: always
    severity: BLOCKER
    status: fixed

  - id: M04-source-repo-coupling
    dimension: knowledge
    found_round: "2026-08-22"
    form: 交付件出现绝对路径、来源仓 git 引用或外部工程路径，独立交付即失效
    clause: G7 / 坑 #29 / 冻结 04-C16
    checker: m04_source_repo_coupling
    target: mechanism
    bad_fixture: test/story/fixtures/failure-modes/M04-source-repo-coupling/bad
    good_fixture: test/story/fixtures/failure-modes/M04-source-repo-coupling/good
    applicable_when: always
    severity: BLOCKER
    status: fixed

  - id: M05-crlf-unsafe-split
    dimension: infra
    found_round: "2026-08-22"
    form: 按 '\n' 切行或用未加 \r? 的行首/行尾正则，CRLF 文件静默零命中
    clause: 坑 #37
    checker: m05_crlf_unsafe_split
    target: mechanism
    bad_fixture: test/story/fixtures/failure-modes/M05-crlf-unsafe-split/bad
    good_fixture: test/story/fixtures/failure-modes/M05-crlf-unsafe-split/good
    applicable_when: always
    severity: BLOCKER
    status: fixed

  - id: M06-silent-empty-derivation
    dimension: knowledge
    found_round: "2026-08-22"
    form: 自写解析器解析失败或派生为空时返回空集，门禁对空集恒真 → 静默失效
    clause: G7「派生为空必须出声」 / 坑 #32
    checker: m06_silent_empty_derivation
    target: mechanism
    bad_fixture: test/story/fixtures/failure-modes/M06-silent-empty-derivation/bad
    good_fixture: test/story/fixtures/failure-modes/M06-silent-empty-derivation/good
    applicable_when: 存在 knowledge 派生模块
    severity: BLOCKER
    status: fixed

  - id: M07-exact-heading-selector
    dimension: dedup
    found_round: "2026-08-21"
    form: 章节取材用精确标题选择器，匹配不到即判失败（应软定位 + 回落读整篇）
    clause: 冻结 01-C5 / 坑 #1
    checker: m07_exact_heading_selector
    target: mechanism
    bad_fixture: test/story/fixtures/failure-modes/M07-exact-heading-selector/bad
    good_fixture: test/story/fixtures/failure-modes/M07-exact-heading-selector/good
    applicable_when: 存在 contracts/story-chapters.json
    severity: BLOCKER
    status: fixed

  - id: M08-whole-file-sha-lock
    dimension: knowledge
    found_round: "2026-08-22"
    form: 用整文件 SHA 锁定需要扩展元数据的原件 → 与"元数据受控扩展"互斥，二者必坏其一
    clause: B1-5 / 坑 #23
    checker: m08_whole_file_sha_lock
    target: mechanism
    bad_fixture: test/story/fixtures/failure-modes/M08-whole-file-sha-lock/bad
    good_fixture: test/story/fixtures/failure-modes/M08-whole-file-sha-lock/good
    applicable_when: 存在 design-patterns 基线守恒机制
    severity: BLOCKER
    status: fixed

  - id: M09-dangling-cross-reference
    dimension: knowledge
    found_round: "2026-08-22"
    form: 跨文件引用指向不存在的条目编号或文件（结构收敛后旧坐标悬空）
    clause: 坑 #31 / 坑 #48
    checker: m09_dangling_cross_reference
    target: mechanism
    bad_fixture: test/story/fixtures/failure-modes/M09-dangling-cross-reference/bad
    good_fixture: test/story/fixtures/failure-modes/M09-dangling-cross-reference/good
    applicable_when: always
    severity: BLOCKER
    status: fixed

  - id: M10-unscoped-identifier-scan
    dimension: readability
    found_round: "2026-08-24"
    form: 仓内标识/模块名门禁不限作用域，会把附录内合法的工程范围表述也判成泄漏
    clause: B4-6 / 坑 #17
    checker: m10_unscoped_identifier_scan
    target: mechanism
    bad_fixture: test/story/fixtures/failure-modes/M10-unscoped-identifier-scan/bad
    good_fixture: test/story/fixtures/failure-modes/M10-unscoped-identifier-scan/good
    applicable_when: 存在归档件红线扫描
    severity: BLOCKER
    status: pending_capability
    capability_batch_note: 目标能力属批次 4（B4-6 作用域界定）

  - id: M11-flagged-only-adjudication
    dimension: knowledge
    found_round: "2026-08-24"
    form: 只把风险标记（相似度/专名命中）命中的行送 verifier 裁决 → 「可信专名 + 低相似改写」整类逃逸
    clause: B1-13 / 坑 #15
    target: mechanism
    applicable_when: 存在 pre_verifier 注入机制
    severity: BLOCKER
    status: retired
    approved_by: 用户（2026-09-04 步骤 10 评审进入条件 1/5）
    reason: >
      逐行裁决整条在步骤 10 退场：pre_verifier 不再注入必答清单，也就没有「只送一部分去裁」
      这件事。当初的修法是「风险标记只排序、不筛选」——那个口径本身建立在「全集逐行裁」
      之上，而全集逐行裁正是被退掉的那套要求。
      现在 verifier 拿到的是判断的真源与判据，裁哪几条由它按需要定。

  - id: M12-normalize-folds-identifiers
    dimension: knowledge
    found_round: "2026-08-24"
    form: 复述判定的规范化笼统"去标点"，把标识符里的下划线/点/连字符一起折叠 → 判定不可实施
    clause: B1-14 / 坑 #16
    checker: m12_normalize_folds_identifiers
    target: mechanism
    bad_fixture: test/story/fixtures/failure-modes/M12-normalize-folds-identifiers/bad
    good_fixture: test/story/fixtures/failure-modes/M12-normalize-folds-identifiers/good
    applicable_when: 存在复述判定模块
    severity: BLOCKER
    status: fixed

  - id: M13-numeric-form-quota
    dimension: readability
    found_round: "2026-08-24"
    form: 数字计数进入写作命令或 PASS 条件（"每章至少 N 张表"），形式配额换皮
    clause: G8 / 坑 #9 / 冻结 02-C2
    checker: m13_numeric_form_quota
    target: mechanism
    bad_fixture: test/story/fixtures/failure-modes/M13-numeric-form-quota/bad
    good_fixture: test/story/fixtures/failure-modes/M13-numeric-form-quota/good
    applicable_when: always
    severity: BLOCKER
    status: fixed

  - id: M14-criteria-instruction-divergence
    dimension: knowledge
    found_round: "2026-08-24"
    form: 内容自检标准只写给维护者/门禁，没有随写作指令下发给被测模型（判据与指令反向）
    clause: B1-15 / 坑 #5 / 坑 #30
    checker: m14_criteria_instruction_divergence
    target: mechanism
    bad_fixture: test/story/fixtures/failure-modes/M14-criteria-instruction-divergence/bad
    good_fixture: test/story/fixtures/failure-modes/M14-criteria-instruction-divergence/good
    applicable_when: 存在 spec/plan 写作注入件
    severity: BLOCKER
    status: fixed

  - id: M15-paraphrase-branch-drift
    dimension: knowledge
    found_round: "2026-08-25"
    form: 复述判定的分支行为漂移——改了规范化或阈值后，某类对抗样本悄悄换了判定档位
    clause: B1-14 / 坑 #11（多分支合并计数，单分支全灭也能凑数通过）
    checker: m15_paraphrase_branch_drift
    target: mechanism
    self_check: internal
    self_check_note: >
      本形态的正反例是 test/story/fixtures/knowledge/adversarial.json 里的九个分支，
      每条自带 expect_verdict / expect_not_verdict——checker 逐分支比对，
      不用外部 bad/good 夹具目录。
    applicable_when: 存在复述判定模块
    severity: BLOCKER
    status: fixed

  - id: M16-patch-residue
    dimension: infra
    found_round: "2026-08-25"
    form: 补丁式修补的残留——零调用的死代码、catch 内静默返回空集合、跨轮次的待办标记
    clause: AGENTS.md §2「正向实现，不打补丁」
    checker: m16_patch_residue
    target: mechanism
    bad_fixture: test/story/fixtures/failure-modes/M16-patch-residue/bad
    good_fixture: test/story/fixtures/failure-modes/M16-patch-residue/good
    applicable_when: always
    severity: BLOCKER
    status: fixed
    scope_note: >
      三类各自独立判定：①零调用导出（作为值注册进分派表也算被用）；
      ②catch 内返回**空集合**且无出声（返回 null 是显式的「没有」信号，不算）；
      ③待办标记与明说的临时方案（「临时改名」这类业务行为描述不算，知识层不扫）。

  - id: M17-dangling-knowledge-reference
    dimension: knowledge
    found_round: "2026-08-25"
    form: 机制层引用了激活清单里不存在的条目编号或知识文件——模型照着死判据去找基准，找不到只能自己编
    clause: G7 / AGENTS.md §2.1「引用必须真实存在」
    checker: m17_dangling_knowledge_reference
    target: mechanism
    bad_fixture: test/story/fixtures/failure-modes/M17-dangling-knowledge-reference/bad
    good_fixture: test/story/fixtures/failure-modes/M17-dangling-knowledge-reference/good
    applicable_when: always
    severity: BLOCKER
    status: fixed
    scope_note: >
      只判两类明确引用，不做形态猜测（猜测会把脚本名、目录名一并误报）：
      ①编号前缀在册而编号查不到；②显式写出的 knowledge/xxx.md 路径不在清单。

  - id: M18-knowledge-boundary-leak
    dimension: knowledge
    found_round: "2026-08-25"
    form: 知识文件越出自己的边界——facts 引规约编号（把时机写进事实）、知识指向 manifest/hooks（维护信息进了模型 prompt）、规约带源码路径、知识里出现阶段矩阵
    clause: AGENTS.md §2「知识不含维护信息，定位只写一处」/ 冻结 03-C3、C4
    checker: m18_knowledge_boundary_leak
    target: mechanism
    bad_fixture: test/story/fixtures/failure-modes/M18-knowledge-boundary-leak/bad
    good_fixture: test/story/fixtures/failure-modes/M18-knowledge-boundary-leak/good
    applicable_when: always
    severity: BLOCKER
    status: fixed
    scope_note: >
      判据在 hooks/shared/knowledge.mjs 的 selfCheck 里，checker 只调它不重实现。
      四项基准全部派生：在册编号来自激活清单，机制目录名来自扩展根实读，不写关键词表。
      批次 1 里 codebase-facts 被重写 5 次不收敛，根因就是这条边界没有机械回归。

  # ---------------------------------------------------------------------------
  # 产物层（feature 产物里的失效形态）
  # ---------------------------------------------------------------------------

  - id: P01-appendix-prefix-granularity
    dimension: knowledge
    found_round: "2026-08-24"
    form: 附录/出口的编号列只写域前缀（SEC、DFX），未到条目级 → 整域漏判照样放行
    clause: B1-8 / 坑 #22 / N9
    checker: p01_appendix_prefix_granularity
    target: product
    bad_fixture: test/story/fixtures/failure-modes/P01-appendix-prefix-granularity/bad
    good_fixture: test/story/fixtures/failure-modes/P01-appendix-prefix-granularity/good
    applicable_when: 存在 AR/story.md 规约符合性附录
    severity: BLOCKER
    status: fixed

  - id: P02-appendix-conclusion-paraphrase
    dimension: knowledge
    responsibility: verifier
    approved_by: 用户（2026-09-04 步骤 10 评审进入条件 1/5）
    reason: >
      形态还在——附录的结论列仍然可能是规约原文的复制。换的是发现者：机械复制检查
      （子串 + 相似度）在步骤 10 退场，因为它只拦得住逐字复制，而「同义改写」正好绕过它，
      于是判据教会的是换个说法而不是写本需求的设计。
      现在由 spec overlay 的 `knowledge_spec_exit_substance` 按真源判：
      把编号遮住这句话还能不能指导编码。
    found_round: "2026-08-24"
    form: 附录「结论或落点」列是规约原文的复制或子串（E1/E2），只列已命中冒充已应用
    clause: B1-12 / B1-14 / 坑 #14 / N8
    target: product
    applicable_when: 存在 AR/story.md 规约符合性附录
    severity: BLOCKER
    status: fixed

  - id: P03-spec-exit-paraphrase
    dimension: knowledge
    responsibility: verifier
    approved_by: 用户（2026-09-04 步骤 10 评审进入条件 1/5）
    reason: >
      同 P02：形态在产物侧仍可能发生，发现者从机械复制检查换成 spec overlay 的
      `knowledge_spec_exit_substance`。判断的真源现在是 `spec/knowledge-use.yaml`，
      审查对着它与材料看 requirement 落在哪个具体名字上。
    found_round: "2026-08-24"
    form: spec 约束出口「本需求的要求」列复述规约原文，到编码那里等于没有要求
    clause: B1-12 / B1-14 / N8
    target: product
    applicable_when: 存在 spec/spec.md 规约约束要求章
    severity: BLOCKER
    status: fixed

  - id: P04-plan-obligation-paraphrase
    dimension: knowledge
    responsibility: verifier
    approved_by: 用户（2026-09-04 步骤 10 评审进入条件 1/5）
    reason: >
      同 P02，落在 plan 侧：由 plan overlay 的 `knowledge_obligation_substance` 判
      ——那一章的设计里是否真的因为这条义务多出了字段、方法、状态或校验。
    found_round: "2026-08-24"
    form: plan 冻结的 obligation 是规约原文改写，没有本需求的设计结论
    clause: B1-10 / B1-12 / B1-14
    target: product
    applicable_when: 存在 contracts.yaml 知识冻结块
    severity: BLOCKER
    status: fixed

  - id: P05-anchor-points-to-declaration
    dimension: knowledge
    found_round: "2026-08-24"
    form: 冻结义务的 anchor 指回知识决策声明表自己，或 landing 为空 → 声明冒充落点
    clause: B1-10 / AGENTS.md §2「声明不是应用」
    checker: p05_anchor_points_to_declaration
    target: product
    bad_fixture: test/story/fixtures/failure-modes/P05-anchor-points-to-declaration/bad
    good_fixture: test/story/fixtures/failure-modes/P05-anchor-points-to-declaration/good
    applicable_when: 存在 contracts.yaml 知识冻结块
    severity: BLOCKER
    status: fixed

  - id: P06-knowledge-decision-after-design
    dimension: knowledge
    found_round: "2026-08-22"
    form: plan 的知识决策章排在它影响的设计章之后 → 位置即语义，只能是事后总结
    clause: B1-10 / 冻结 03-C6
    checker: p06_knowledge_decision_after_design
    target: product
    bad_fixture: test/story/fixtures/failure-modes/P06-knowledge-decision-after-design/bad
    good_fixture: test/story/fixtures/failure-modes/P06-knowledge-decision-after-design/good
    applicable_when: 存在 plan/plan.md
    severity: BLOCKER
    status: fixed

  - id: P07-rule-ids-in-story-body
    dimension: readability
    found_round: "2026-08-23"
    form: story 主叙事出现规约编号（仓内标识），评审者对不上
    clause: B4-6 / rules.md「规约在正文只写中文名」
    checker: p07_rule_ids_in_story_body
    target: product
    bad_fixture: test/story/fixtures/failure-modes/P07-rule-ids-in-story-body/bad
    good_fixture: test/story/fixtures/failure-modes/P07-rule-ids-in-story-body/good
    applicable_when: 存在 AR/story.md
    severity: BLOCKER
    status: fixed

  - id: P08-repo-identifier-leak
    dimension: readability
    found_round: "2026-08-23"
    form: 归档件主叙事出现仓内路径、裸文件名或机制文件名（附录内的工程范围表述合法）
    clause: B4-6 / 坑 #17
    checker: p08_repo_identifier_leak
    target: product
    bad_fixture: test/story/fixtures/failure-modes/P08-repo-identifier-leak/bad
    good_fixture: test/story/fixtures/failure-modes/P08-repo-identifier-leak/good
    applicable_when: 存在 AR/story.md
    severity: BLOCKER
    status: fixed

  - id: P09-decision-double-write
    dimension: dedup
    found_round: "2026-08-24"
    form: 决策值既由占位符渲染又在正文复述（或塞进紧邻括号），同一决策两处真源
    clause: B2-6 / 坑 #20 / N2
    checker: p09_decision_double_write
    target: product
    bad_fixture: test/story/fixtures/failure-modes/P09-decision-double-write/bad
    good_fixture: test/story/fixtures/failure-modes/P09-decision-double-write/good
    applicable_when: 存在 AR/story.md
    severity: BLOCKER
    status: pending_capability
    capability_batch_note: 目标能力属批次 2（B2-6 决策值单写）

  - id: P10-duplicate-image-reference
    dimension: dedup
    found_round: "2026-08-22"
    form: 同一张图片在成品中被引用多次（图片应全篇唯一）
    clause: B2-6
    checker: p10_duplicate_image_reference
    target: product
    bad_fixture: test/story/fixtures/failure-modes/P10-duplicate-image-reference/bad
    good_fixture: test/story/fixtures/failure-modes/P10-duplicate-image-reference/good
    applicable_when: 存在 AR/story.md
    severity: BLOCKER
    status: pending_capability
    capability_batch_note: 目标能力属批次 2（B2-6 图片全篇唯一）

  - id: P11-verifier-zero-adjudication
    dimension: knowledge
    found_round: "2026-08-23"
    form: verifier 报告缺少扩展判据的逐条裁决，harness 照收 PASS（实测 0/8、后又 10/11 未裁决）；
      必答集 = 登记源的命中条目 + 判整域不适用的域 + 模式候选单元，判据是同一行既含键又含裁决词
    clause: B1-16「逐行裁决落盘」 / G4 / 坑 #18
    target: product
    applicable_when: 存在 <phase>/reports 下的 verifier 报告
    severity: BLOCKER
    status: retired
    approved_by: 用户（2026-09-04 步骤 10 评审进入条件 1/5）
    reason: >
      「漏裁」是相对必答集说的：集合里有 8 条、报告里裁了 0 条。必答集在步骤 10 退场之后
      没有可数的分母，这个形态失去定义。
      **它守的那件事没有落空**：verifier 有没有执行仍由报告落盘核对（story 审查那一项，
      `verifier-report.mjs`），只是核的从「每一行都在」变成「结果块在、两类结论齐」。

  - id: P12-numeric-index-paragraph
    dimension: readability
    found_round: "2026-08-24"
    form: 用纯编号清单段（"场景索引：S1、S2、S3"）冒充内容守恒，最省力地满足按号守恒
    clause: B2-7「守恒按名不按号」 / N3
    checker: p12_numeric_index_paragraph
    target: product
    bad_fixture: test/story/fixtures/failure-modes/P12-numeric-index-paragraph/bad
    good_fixture: test/story/fixtures/failure-modes/P12-numeric-index-paragraph/good
    applicable_when: 存在 AR/story.md
    severity: BLOCKER
    status: pending_capability
    capability_batch_note: 目标能力属批次 2（B2-7 守恒按名不按号）

  - id: P13-silent-downstream-skip
    dimension: knowledge
    found_round: "2026-08-22"
    form: 下游阶段对冻结义务既无证据也无"不适用+理由"，静默跳过与做过没写事后同形
    clause: B1-11 / AGENTS.md §2「显式不适用优于静默跳过」
    checker: p13_silent_downstream_skip
    target: product
    bad_fixture: test/story/fixtures/failure-modes/P13-silent-downstream-skip/bad
    good_fixture: test/story/fixtures/failure-modes/P13-silent-downstream-skip/good
    applicable_when: 存在 contracts.yaml 知识冻结块 且 存在 review/review-report.md
    severity: BLOCKER
    status: fixed

  - id: P14-image-broken-link
    dimension: knowledge
    found_round: "2026-08-25"
    form: 归档件的图片引用写成裸文件名或指向源材料目录，形态守恒数得到图、评审者打开是红叉
    clause: B1-10 / rules.md 红线 2「表达形态与数量不得降级」
    checker: p14_image_broken_link
    target: product
    bad_fixture: test/story/fixtures/failure-modes/P14-image-broken-link/bad
    good_fixture: test/story/fixtures/failure-modes/P14-image-broken-link/good
    applicable_when: 存在 AR/story.md
    severity: BLOCKER
    status: fixed

  - id: P15-domain-gating-not-applied
    dimension: knowledge
    found_round: "2026-08-25"
    form: 有命中条件的规约域没先判域，对着域内条目逐条写「不涉及」——判定负担照旧，时机判断没做
    clause: B1-13「条件域先判域，常驻域逐条」
    checker: p15_domain_gating_not_applied
    target: product
    bad_fixture: test/story/fixtures/failure-modes/P15-domain-gating-not-applied/bad
    good_fixture: test/story/fixtures/failure-modes/P15-domain-gating-not-applied/good
    applicable_when: 存在 AR/story-src/knowledge.json 且激活清单里有条件域
    severity: BLOCKER
    status: fixed
  - id: P16-spec-exit-diverges-from-registry
    dimension: knowledge
    found_round: "2026-08-25"
    form: 规格件出口章的要求与判定登记件同编号结论不同文，同一条要求出现两个版本且都没人报错
    clause: B1-14「出口章与登记源同文」
    checker: p16_spec_exit_diverges
    target: product
    bad_fixture: test/story/fixtures/failure-modes/P16-spec-exit-diverges-from-registry/bad
    good_fixture: test/story/fixtures/failure-modes/P16-spec-exit-diverges-from-registry/good
    applicable_when: 存在 AR/story-src/knowledge.json 与 spec/spec.md 的约束要求表
    severity: BLOCKER
    status: fixed

  - id: P17-landing-md-yaml-mismatch
    dimension: knowledge
    found_round: "2026-08-25"
    form: 方案正文义务表的落点与契约冻结的 landing 各写各的，评审者读到的落点与下游去找的不是一个
    clause: B1-17「落点两处一致」
    checker: p17_landing_md_yaml_mismatch
    target: product
    bad_fixture: test/story/fixtures/failure-modes/P17-landing-md-yaml-mismatch/bad
    good_fixture: test/story/fixtures/failure-modes/P17-landing-md-yaml-mismatch/good
    applicable_when: 存在 plan/plan.md 与 contracts.yaml 知识冻结块
    severity: BLOCKER
    status: fixed

  - id: P18-step-equals-criterion
    dimension: knowledge
    found_round: "2026-08-25"
    form: 冻结义务的业务步骤拿验收编号顶替，这条义务在业务流程里没有落点却照样过门禁
    clause: B1-17「step 是业务步骤」
    checker: p18_step_equals_criterion
    target: product
    bad_fixture: test/story/fixtures/failure-modes/P18-step-equals-criterion/bad
    good_fixture: test/story/fixtures/failure-modes/P18-step-equals-criterion/good
    applicable_when: 存在 contracts.yaml 知识冻结块
    severity: BLOCKER
    status: fixed

  # ---------------------------------------------------------------------------
  # adapt（工程适配）
  # ---------------------------------------------------------------------------

  - id: A01-adapt-couples-to-mechanism
    dimension: infra
    found_round: "2026-08-28"
    form: adapt 的 Skill 或脚本写死扩展内部结构（hook 阶段路径、知识文件名、条目编号），机制一改适配能力就跟着废
    clause: B3-10 / KD-10 / G7
    checker: a01_adapt_couples_to_mechanism
    target: mechanism
    bad_fixture: test/story/fixtures/failure-modes/A01-adapt-couples-to-mechanism/bad
    good_fixture: test/story/fixtures/failure-modes/A01-adapt-couples-to-mechanism/good
    applicable_when: always
    severity: BLOCKER
    status: fixed
    scope_note: >
      与 M01 的分工：M01 守整个机制层不抄知识内容，本条只盯 adapt 交付件，且多判一类
      ——hook 的「阶段目录 + 文件」路径。把 adapt 排在机制重构之前的全部理由，就是它
      不认机制细节；这条一旦破，那个排序就不成立了。
      判定基准（阶段名、知识文件名、条目编号）全部运行期派生，派生为空响亮报错。

  - id: A03-author-channel-broken
    dimension: infra
    found_round: "2026-08-28"
    form: 给阶段作者的要求写在 hooks 的 .md 里——那些片段只进 ai-prompt、只给 verifier，写产物的主 agent 从头到尾读不到
    clause: E1 / 00 §1.4
    checker: a03_author_channel_broken
    target: mechanism
    bad_fixture: test/story/fixtures/failure-modes/A03-author-channel-broken/bad
    good_fixture: test/story/fixtures/failure-modes/A03-author-channel-broken/good
    applicable_when: always
    severity: BLOCKER
    status: fixed
    scope_note: >
      8 个 hook 事件全在 harness 同一进程、verifier 之前跑完，片段唯一去处是 ai-prompt 尾部
      （harness-runner.ts:765-768 + report-generator.ts:311-318）。基线态六份 on_context_load.md
      400 行通篇作者向文本，内网实测 plan「仅展示」、coding「完全失效」。
      本条守两件事：hooks/**/*.md 只剩 author.md；每份是索引不是教材（≤60 行、不整段展开判据推理）。
      阶段清单派生自 hooks/ 子目录，不写死阶段名（G7）。

  - id: A04-gate-pass-no-trace
    dimension: infra
    found_round: "2026-08-28"
    form: post_check 通过时不留痕，「跑了并通过」与「根本没跑」在产物上完全同形
    clause: 04 #2 / 00 §4.3
    checker: a04_gate_pass_leaves_no_trace
    target: mechanism
    bad_fixture: test/story/fixtures/failure-modes/A04-gate-pass-no-trace/bad
    good_fixture: test/story/fixtures/failure-modes/A04-gate-pass-no-trace/good
    applicable_when: always
    severity: BLOCKER
    status: fixed
    scope_note: >
      dispatcher 只在 hook 返回 ok:false 时记结果（hooks-dispatcher.ts:177-188）。
      基线态六个 post_check 只有两个写留痕，AR90004 的 coding/reports 里没有 ext-post-check.json
      ——要证明某条判据在真实链路上生效过，举不出任何证据。
      认间接留痕：post_check 若 import 了自己会写留痕的 shared 模块，同样算数（避免六处复制样板）。

  - id: A05-entry-misses-section
    dimension: infra
    found_round: "2026-08-28"
    form: 扩展交付了入口段（AGENTS.section.md），宿主的 CLAUDE.md / AGENTS.md 却没带上它——文件在仓里躺着，没有任何机制把它送到作者眼前
    checker: a05_entry_file_misses_extension_section
    clause: E1 / KA-8
    target: mechanism
    bad_fixture: test/story/fixtures/failure-modes/A05-entry-misses-section/bad/doc/extensions
    good_fixture: test/story/fixtures/failure-modes/A05-entry-misses-section/good/doc/extensions
    applicable_when: always
    severity: BLOCKER
    status: fixed
    scope_note: >
      主 agent 会话开始时自动进上下文的只有入口文件：claude 读 CLAUDE.md，
      codex 只读 AGENTS(.override).md、不加载任何 rules 目录。这是作者面通道的最后一跳，
      断在这里前面所有工作都送不到。包没有 AGENTS.section.md 时本条不适用（该形态未启用）。
      与 adapt --check ⑤ 同口径：那条管目标工程，本条管本仓。

  - id: A02-adapt-check-blind
    dimension: infra
    found_round: "2026-08-28"
    form: 适配后的核对判不出已知违规——机制留着旧文件、知识正文被改写、未确认的事实面进了清单、目标自定义被动过
    clause: KD-1/KD-2/KD-3/KD-6 / C13
    checker: a02_adapt_check_blind
    target: mechanism
    bad_fixture: test/story/fixtures/failure-modes/A02-adapt-check-blind/bad
    good_fixture: test/story/fixtures/failure-modes/A02-adapt-check-blind/good
    applicable_when: always
    severity: BLOCKER
    status: fixed
    scope_note: >
      不重实现判定：调真实 `adapt-scan.mjs`，退出码即结论（同 M15 / M18）。
      夹具自带 package/ 与 target/ 两棵树、可选 after/（写入后状态），整棵树复制到临时目录再跑，
      夹具本身不被写脏。真实目标那一栏用**当前包**去核一棵已提交的未适配目标树
      （fixtures/adapt/target-prior），验证区分力在真包上同样成立、而不是只在迷你夹具里有效。

  - id: B01-probe-no-discrimination
    dimension: infra
    found_round: "2026-08-28"
    form: 探针对已知违规没有区分力——查「名字在不在」而不是「写法对不对」，容器名恒真；或被注释里的提及骗过
    clause: E3 / KB-4
    checker: b01_probe_no_discrimination
    target: mechanism
    bad_fixture: test/story/fixtures/failure-modes/B01-probe-no-discrimination/bad
    good_fixture: test/story/fixtures/failure-modes/B01-probe-no-discrimination/good
    applicable_when: always
    severity: BLOCKER
    status: fixed
    scope_note: >
      基线的 coding 探针是 \b名\b 跨文件文本存在性，不分声明/调用/注释。实测一条明晃晃的
      违规（方向性布局参数写成 left/right）被它放行——它查的是组件名在不在，而组件名当然在。
      本条调真实的 probes.mjs 执行器，不在 checker 里重实现判定（同 A02 / M15 的做法）。
      两个形态：absent_regex 要报出不该出现的写法；referenced_outside_definition 要判出
      零调用的角色类且不被注释骗过——后者在实施中真的踩过一次。

  - id: B02-evidence-echo
    dimension: infra
    found_round: "2026-08-28"
    form: 裁决的证据是回声——把注入清单里给的话抄回证据列，机械层只查「同行含键 + 有裁决词」就放行
    clause: E4 / KB-5
    target: mechanism
    applicable_when: always
    severity: BLOCKER
    status: retired
    approved_by: 用户（2026-09-04 步骤 10 评审进入条件 1）
    reason: >
      不是「换了发现者」，是**产生这个形态的机制没了**。证据列来自「逐行裁决 + 每行附引文」
      那套要求：注入必答清单 → 逐行裁 → 门禁核对键与引文。步骤 10 把整条链退场之后，
      报告里不再有证据列，也就无所谓证据是不是回声。
      当初为它加的引文核实（非空、≥12 字、是目标产物的子串）本身就是又一层字符串近似——
      拿它治回声，治出来的是「去产物里找一段够长的话抄过来」。
      现在 verifier 只被要求写「判出问题的那几条与依据」，不出裁决表，没有可回声的列。

  - id: R03-author-chain-dangling
    dimension: knowledge
    found_round: "2026-08-28"
    form: 机制退场时删了文件却没删指路的那句话——作业书仍让模型去读一份已经不存在的文件
    clause: G11 / KR-3b
    checker: r03_author_chain_dangling
    target: mechanism
    bad_fixture: test/story/fixtures/failure-modes/R03-author-chain-dangling/bad
    good_fixture: test/story/fixtures/failure-modes/R03-author-chain-dangling/good
    applicable_when: always
    severity: BLOCKER
    status: fixed
    scope_note: >
      本轮删了三份文件（旧规则说明、旧门禁脚本、旧概念参考），每份都有别的文件在指它。
      模型照着链去读、读不到，就会自己补一套规则——这比没写更坏。
      判定面是 Markdown 链接与脚本路径：作者写下链接就是让人去读的。
      工作区产物路径（在扩展包里本就不存在）与 adapt 的历史版本结构签名不在判定面内。

  - id: F02-verifier-task-diluted
    dimension: content
    found_round: "2026-08-29"
    form: 裁决者的作业书既要逐条对齐单元、又要对整篇给印象结论，逐条那件事被稀释——
      实测 43 条裁决全「讲清」、0「未讲清」，引文 15 条与被裁单元无关
    clause: KF-3a / 06 §三 F1
    checker: f02_verifier_task_not_well_defined
    target: mechanism
    bad_fixture: test/story/fixtures/failure-modes/F02-verifier-task-diluted/bad
    good_fixture: test/story/fixtures/failure-modes/F02-verifier-task-diluted/good
    applicable_when: always
    severity: BLOCKER
    status: fixed
    scope_note: >
      「这个单元 → 那一章讲没讲」与「整篇好不好读」差着一个数量级的粒度，
      放进同一份作业书，前者就被后者挤掉。整篇两条维度归 writer 自检，裁决者只做逐对。
      判据不写具体词：自检维度的名字从 writer 作业书里现取，再核它们没泄漏到裁决者作业书。
      同一条作业要求只该待在一份作业书里。

  - id: F01-spec-without-story
    dimension: content
    found_round: "2026-08-29"
    form: spec 四阶段全绿而叙事件从来没被写出来——成文没有阶段边界守着，本地单直接跳过
    clause: KF-1a / 06 §三 F3
    checker: f01_spec_without_story
    target: mechanism
    bad_fixture: test/story/fixtures/failure-modes/F01-spec-without-story/bad
    good_fixture: test/story/fixtures/failure-modes/F01-spec-without-story/good
    applicable_when: always
    severity: BLOCKER
    status: fixed
    scope_note: >
      story 是 spec 阶段三份产物之一。把它挪到 spec 之后当独立一步、触发条件写「归档之前」，
      本地单没有归档，这个时点就不存在——实测四阶段 harness 全 pass、AR/story.md 不存在，
      只有测试域的 story_build_check 拦住。成文回到 spec 阶段内，才有阶段边界要求它。
      判据查登记态不查文件在不在：手写一份简版照样骗过「文件存在」（基线的注释自己承认过）；
      登记前会重跑 story-build check，登记成功即九项判据都过了。
      bad = status complete（范围收口了但没成文，必须报）；good = story_written（不许报）。

  - id: R04-flow-status-after-s5
    dimension: content
    found_round: "2026-08-28"
    form: spec 的流程契约门禁把 S5 之后的状态判成「未收口」，回头拦住自己的产物——
      四个已合法闭环的阶段被 upstream_verdict_gate 级联判红
    clause: KR-4b / 实跑 story-suite-20260828-191953
    checker: r04_flow_status_after_s5
    target: mechanism
    bad_fixture: test/story/fixtures/failure-modes/R04-flow-status-after-s5/bad
    good_fixture: test/story/fixtures/failure-modes/R04-flow-status-after-s5/good
    applicable_when: always
    severity: BLOCKER
    status: fixed
    scope_note: >
      「已收口」是一段区间不是一个值：契约状态机 complete →（spec）→ story_written → archived，
      门禁问的是「进 spec 之前范围定了没有」，complete 之后每个状态都满足。
      写成等于 complete，S5 一登记成文态，spec harness 重跑就 FAIL。
      本条由真实实跑撞出来（AR90004 走完 S5 后四阶段集体翻红），夹具取自那次的真实契约、去掉单号字面。
      bad = status init（真没收口，必须报）；good = status story_written（S5 已登记，不许报）。

  - id: C01-story-conservation
    dimension: content
    found_round: "2026-08-28"
    form: story 的守恒判不出材料里丢了什么——按「章 × 数量」或按「作者自述」守恒，而不是按事实 token 整篇判
    clause: E7 / KC-1 / KC-2 / 冻结 01-C1
    target: mechanism
    applicable_when: always
    severity: BLOCKER
    status: fixed
    responsibility: observed
    approved_by: 用户（2026-09-03 D10 修订签字）
    reason: >
      守恒判据按事实 token 逐单元核对，量随材料条数涨而读者拿到的判断不增加，随逐单元系统在步骤 9 退场。「材料里丢了什么」改由独立审查按读者问题判。
    observed_by: >
      真实 Story 运行：story_reader_review 的 blocking_findings 是否点出关键业务事实缺失；以及用户按 TEST §10 三轴评分里的「产物结果」一轴，看他在成品里找到的遗漏审查者报没报。
    scope_note: >
      1.0 按逐章守恒（每章把取材节的每行写全）→ 同一事实被四个章节合同各指一次、被强制写四遍；
      上一版按作者自述（自由文本 reason 只判非空）→ 实测 161/272 单元「不进」、理由去重后 2 种。
      两版共同根因是守恒的粒度与位置都错。本条调真实的 story-build（init → audit → check），
      判据不是「报了错」而是**点名了具体哪个事实**——笼统说「有缺失」等于没有区分力。

  - id: R01-verdict-echo
    dimension: content
    found_round: "2026-08-28"
    form: S5 的裁决用回声当引文——把来源单元的原话抄回证据列，证明不了 story 讲清了它
    clause: E7 / KR-1c
    target: mechanism
    applicable_when: always
    severity: BLOCKER
    status: fixed
    responsibility: observed
    approved_by: 用户（2026-09-03 D10 修订签字）
    reason: >
      回声判据核的是逐字引文，而逐字引文本身是要退的形态——它逼着作者保留材料原话，与「把材料改写成人话」直接冲突。裁决表随逐单元系统在步骤 9 退场，判据一并退。
    observed_by: >
      真实 Story 运行：审查结论里的定位与引用是否指向 story 自己的原文（而不是把材料原话抄回来）；用户按 TEST §10「产物结果」一轴复核审查结论是否站得住。
    scope_note: >
      机器定不了落点的单元交给 S5 裁决者逐条裁，裁决要附引文。引文若是来源单元原文的子串，
      它证明的是「材料这么说」而不是「story 讲清了」——上一轮 verifier 报告 272 行证据
      全是这种回声。判据：引文须是 story 那一章的逐字子串、≥12 字、且不是单元原文的子串。

  - id: W01-non-story-invisible
    dimension: boundary
    found_round: 2026-08-29
    form: 不走 /story 的需求被 story 专属要求拦住——扩展对它本该是隐形的
    clause: KV-6 / S4-11 / W1 裁定 2026-08-15
    checker: w01_non_story_invisible
    target: mechanism
    bad_fixture: test/story/fixtures/failure-modes/W01-non-story-invisible/bad
    good_fixture: test/story/fixtures/failure-modes/W01-non-story-invisible/good
    applicable_when: always
    severity: BLOCKER
    status: fixed
    scope_note: >
      判据面本身已按 isStoryFeature 分好：§9 技术契约、术语解释列、三份产物只在有流程契约时
      才要求；§10 / §11 是知识判定的出口，对所有需求生效（判定产生的代码要求不进 spec，
      编码那里就拿不到）。本条守的是**它别退回去**——只写 §10/§11 的需求跑 spec 门禁要能过，
      且回话里不提 story / 三份产物 / 技术契约 / 归档件 / 三级关卡：作者读到这些，
      就会去写他根本不需要写的东西。反夹具是同一份 spec 加一个 complete 的流程契约
      （= 走了 /story），那时 §9 与三份产物就该被硬拦。

  - id: S01-diagram-degraded
    dimension: expression
    found_round: 2026-08-29
    form: 材料里的流程图在 story 里被压成「A → B → C」箭头文字——上一版只判「图有落点」，箭头文字也算落点
    clause: KV-3 / M4 / 1.0 rules 第 2 条「表达形态与数量不得降级」
    target: mechanism
    applicable_when: always
    severity: BLOCKER
    status: fixed
    responsibility: observed
    approved_by: 用户（2026-09-03 D10 修订签字）
    reason: >
      「源里有几张图、story 里就要有几张」按材料条数增长，属形态守恒那一类，随逐单元系统在步骤 9 退场。图被压成箭头文字算不算讲清，要读上下文。
    observed_by: >
      真实 Story 运行：story_reader_review 审「图与文的配合」这一维度时是否点出流程表达降级；用户按 TEST §10「产物结果」一轴看成品里的图是否还原了材料的结构。
    scope_note: >
      读者要的是那张图一眼看出的结构：哪几条分支、各自去哪。文字复述做不到。判的是形态不是语义——源里是图的，落点章里就得还是图；story 的图数不少于源。实证：批次 3 的 AR90005 里源材料的流程图整个没进 story，而当时的判据全绿。

  - id: S02-terms-not-table
    dimension: expression
    found_round: 2026-08-29
    form: 材料里的表在 story 里被摊成散文，逐项比对的那几列丢了
    clause: KV-3 / M4 / 冻结 01-C1
    target: mechanism
    applicable_when: always
    severity: BLOCKER
    status: fixed
    responsibility: observed
    approved_by: 用户（2026-09-03 D10 修订签字；步骤 9 小段 3 评审 B2）
    reason: >
      固定形式判据只问「是不是表、有没有分节、段落多长」，而这一条问的是表达贴不贴合，要读上下文。判据随作业书改写在步骤 9 退场。
    observed_by: >
      审查者按「术语」章的读者问题看近义词差别讲清没有；用户按 TEST §10「产物结果」一轴读术语章是否一看就懂。
    scope_note: >
      最先丢的是非首列——触发条件、编号、责任方。三行表摊成一句话，读者要靠数分号对齐，而术语表本来是给他随时回查的。单行不构成表，故只有 ≥2 行落在同一章时才要求成表——这条与「一行不成表」的表达规则对齐，否则机器要建表、裁决者判不该建表。

  - id: S03-long-paragraph
    dimension: expression
    found_round: 2026-08-29
    form: 一段几百字的散文，读者找不到结论也找不到停顿
    clause: KV-3 / M5 / Microsoft 写作风格（短句短段、结论先行）
    target: mechanism
    applicable_when: always
    severity: BLOCKER
    status: fixed
    responsibility: observed
    approved_by: 用户（2026-09-03 D10 修订签字；步骤 9 小段 3 评审 B2）
    reason: >
      固定形式判据只问「是不是表、有没有分节、段落多长」，而这一条问的是表达贴不贴合，要读上下文。判据随作业书改写在步骤 9 退场。
    observed_by: >
      审查者按章级维度里的「表达贴合」看有没有读不下去的长段；用户按同一轴通读时的实际感受。
    scope_note: >
      阈值满足「拆了一定更可读」这个条件，所以机械判它不会被换皮受益：把长段拆开、把结论提到段首，两件事都只会让文档更好读。**不设句长阈值**——中文 45–60 字的复句很常见，为凑短在逗号处一刀切只会写出半截话，那正是最省力的过关方式。

  - id: S04-duplicate-paragraph
    dimension: expression
    found_round: 2026-08-29
    form: 同一段话在两章各写一遍——「不重复」这一轴此前只有「分配恰好一处」在守，那管不住作者
    clause: KV-3 / KV-7 / 冻结 01-C2
    target: mechanism
    applicable_when: always
    severity: BLOCKER
    status: fixed
    responsibility: observed
    approved_by: 用户（2026-09-03 D10 修订签字；步骤 9 小段 3 评审 B2）
    reason: >
      固定形式判据只问「是不是表、有没有分节、段落多长」，而这一条问的是表达贴不贴合，要读上下文。判据随作业书改写在步骤 9 退场。
    observed_by: >
      审查者按「同一件事在两处完整表述」这一维度跨章看；用户按同一轴看有没有重复读到同一件事。
    scope_note: >
      分配恰好一处管的是来源单元，管不住作者在两章各写同样的话。规范化（去空白标点与强调标记）后逐段比对，改个标点也认得出。下限 duplicate_min_chars 是合同数据：低于它，「本需求不涉及。」这类模板句会被判成重复。

  - id: S05-main-text-identifier
    dimension: expression
    found_round: 2026-08-29
    form: 工程标识、规约编号、检索措辞写进主叙事——读者每隔两行就要停下来判断「这是我要懂的东西吗」
    clause: KV-2 / 08-21 §2「不能打断面向人的主叙述」
    checker: s05_main_text_identifier
    target: mechanism
    bad_fixture: test/story/fixtures/failure-modes/S05-main-text-identifier/bad
    good_fixture: test/story/fixtures/failure-modes/S05-main-text-identifier/good
    applicable_when: always
    severity: BLOCKER
    status: fixed
    scope_note: >
      接口名、字段名、规约编号必须保留在归档件里——评审者要查得到。争的是位置不是有无：
      附录是它们的唯一落点，主叙事写中文业务名与中文规约名。批次 3 实测两个 Case 的
      作者落点里绝大多数来自规格件原句，story 通篇是 camelCase 与编号，评审者读不下去。
      判据作用域限定在附录之外；判定形态全部是数据（规约编号取激活清单、标识取材料 token），
      不猜——猜会把产品名判成工程标识，作者除了删掉正确的词之外无路可走。

  - id: R02-knowledge-row-missing
    dimension: content
    found_round: "2026-08-28"
    form: 激活规约条目在归档件合规章的判定表里缺行——逐条判定失去落点，批次 1 的知识应用在 story 侧退化
    clause: E2 / KR-2b
    checker: r02_knowledge_row_missing
    target: mechanism
    bad_fixture: test/story/fixtures/failure-modes/R02-knowledge-row-missing/bad
    good_fixture: test/story/fixtures/failure-modes/R02-knowledge-row-missing/good
    applicable_when: always
    severity: BLOCKER
    status: fixed
    scope_note: >
      判定原先落在一份独立的判定记录文件里；那份文件退场后，作业指引与门禁一起没了。
      现在规约条目是来源单元，与材料里的事实走同一条守恒链：缺一条点名一条，
      判「不命中」也要有行有依据，整域不适用时给该域一行即可（P15 的域级判定回来了）。

  - id: G01-judgement-blocks-golden
    dimension: expression
    found_round: "2026-08-30"
    form: 新加或改动的判据把已定稿的理想产物判成违规——判据没有仲裁源，冲突时只能靠加围栏调和
    clause: KG-0 / P1「拦金样即判据错」
    checker: g01_judgement_blocks_golden
    target: mechanism
    self_check: internal
    applicable_when: always
    severity: BLOCKER
    status: fixed
    scope_note: >
      四轮机制迭代的判据全部从「病」反推，从不存在「期望的 story 长什么样」的具体定义，
      于是判据互相打架时没有仲裁源：批次 4 首跑一边要求人读的改写、一边要求逐字命中，
      倾倒成了唯一合法解。金样定稿后成为唯一效果定义——**任何判据若拦金样，错的是判据**。
      这一条是常驻正例：判据改动先跑它。两个分支都验——金样零 FAIL，
      且往副本里塞违例时判项真的点名（不然「零 FAIL」可能只是判项全空转）。

  - id: G02-mechanism-points-at-assets
    dimension: knowledge
    found_round: "2026-08-30"
    form: 机制层（代码、合同、模板、作业书、报错文案）提到金样、夹具或测试目录——判据从形态规则滑成「照着那一份写」
    clause: KG2-9 / P6
    checker: g02_mechanism_points_at_assets
    target: mechanism
    self_check: internal
    applicable_when: always
    severity: BLOCKER
    status: fixed
    scope_note: >
      金样与判据的关系是**单向**的：判据从金样正推，拦住金样就是判据错。反过来不成立——
      机制层一旦提到金样或夹具，换个业务域立刻不成立，而台账仍然全绿（自证）。
      与 M02 分工：M02 判业务字面（单号、业务名），这一条判「指向验证资产本身」。

  - id: S06-appendix-dump
    dimension: expression
    found_round: "2026-08-29"
    form: 附录里长出合同之外的小节，材料原文成段搬进围栏块——附录变成倾倒区，归档件的可读部分被挤掉
    clause: KG-3 / E2
    checker: s06_appendix_dump
    target: mechanism
    bad_fixture: test/story/fixtures/failure-modes/S06-appendix-dump/bad
    good_fixture: test/story/fixtures/failure-modes/S06-appendix-dump/good
    applicable_when: always
    severity: BLOCKER
    status: fixed
    scope_note: >
      附录是全篇唯一允许出现工程标识的地方，于是它天然最容易变成倾倒区。首跑实测：
      产物在附录下多长出一个「机器核对索引」小节，255 行原文占全篇 58%，
      而当时的判据只核「附录这一章存在」，倾倒完全合法。
      现在判的是结构不是内容——约定之外的小节、非 mermaid 的围栏块、空节，三样都不该有。
      小节名可带 A.–E. 序号：序号是表达形式，比较前统一剥掉。

  - id: S07-review-legacy-fields
    dimension: content
    found_round: "2026-08-30"
    form: 评审记录长回签署字段与状态行（确认人/确认日期/确认依据/状态）——表单再次膨胀成需要说明书的东西
    clause: KG-4 / B2-9
    checker: s07_review_legacy_fields
    target: mechanism
    bad_fixture: test/story/fixtures/failure-modes/S07-review-legacy-fields/bad
    good_fixture: test/story/fixtures/failure-modes/S07-review-legacy-fields/good
    applicable_when: always
    severity: BLOCKER
    status: fixed
    scope_note: >
      判据是「需要说明书就是设计错了」。这几样每次都以「让评审更规范」的名义长回来：
      暂缓责任人、完成期限、是否阻塞执行、后续动作、确认人、确认日期、确认依据七个字段，
      加上一行状态。实际后果是评审人先读一遍字段表，再在六个答不上来的格子里跳过或胡填
      （责任人与期限是排期的事，确认依据是审计的事），而「已确认」因此不可信。
      留下的只有「审核结果：」一行，评审人在它后面写具体内容。判的是行首形态不是词——评审人在自己的
      意见里写「这条要找确认人」不算违规，只判机器渲染出来的那种独立字段行。

  - id: S08-solution-chapter-flat
    dimension: readability
    found_round: "2026-08-30"
    form: 已定决策成堆而方案章没有取舍这一节——取舍化进散文，评审者要读完整章才拼得出「否掉了什么」
    clause: KG-3 / B2-9
    target: mechanism
    applicable_when: always
    severity: BLOCKER
    status: fixed
    responsibility: observed
    approved_by: 用户（2026-09-03 D10 修订签字；步骤 9 小段 3 评审 B2）
    reason: >
      固定形式判据只问「是不是表、有没有分节、段落多长」，而这一条问的是表达贴不贴合，要读上下文。判据随作业书改写在步骤 9 退场。
    observed_by: >
      审查者按「业务方案」章的读者问题看参与方、取舍、风险是否各自可回找；用户按同一轴看方案章能不能按节回查。
    scope_note: >
      两轮四份产物零例外：有 check 判据的形态全达成（附录五节、范围三小节），
      只写在模板注释或占位里的形态全不达成（方案章与交付章的节级）。节级正是信息架构
      的精华——关键取舍一节、回退设计一节，读者按节回找。判的是节在不在，
      写得好不好归裁决者。空章（正文恰为「本需求不涉及。」）豁免。

  - id: S09-flow-chapter-flat
    dimension: readability
    found_round: "2026-08-30"
    form: 有内容的流程章一个小节都没有——主路径与全部支线压在一坨散文里
    clause: KG-3 / B2-9
    target: mechanism
    applicable_when: always
    severity: BLOCKER
    status: fixed
    responsibility: observed
    approved_by: 用户（2026-09-03 D10 修订签字；步骤 9 小段 3 评审 B2）
    reason: >
      固定形式判据只问「是不是表、有没有分节、段落多长」，而这一条问的是表达贴不贴合，要读上下文。判据随作业书改写在步骤 9 退场。
    observed_by: >
      审查者按「业务流程」章的读者问题看主路径与分支去向是否分得开；用户按同一轴看流程能不能读出分支。
    variant_of: S08-solution-chapter-flat
    scope_note: >
      与 S08 同源，判的是「该分节的章分没分」。**不设「每章几节」的配额**——
      配额逼出来的是凑数的小标题，那比平铺更难读。空章豁免。

  - id: S10-image-path-copied
    dimension: readability
    found_round: "2026-08-30"
    form: 图片被复制进归档目录并改名，story 按裸文件名引用——副本没人维护，改名后认不出是原来那张
    clause: KG-3 / 坑 #34
    checker: s10_image_path_copied
    target: mechanism
    bad_fixture: test/story/fixtures/failure-modes/S10-image-path-copied/bad
    good_fixture: test/story/fixtures/failure-modes/S10-image-path-copied/good
    applicable_when: always
    severity: BLOCKER
    status: fixed
    scope_note: >
      模板历来只约定图题形态，从未约定引用路径口径，于是「抽图 → 复制 → 再复制并改名」
      这条链一路无人拦。判据只问「引的这张在材料的图片登记里吗」，
      不判它该放哪个目录——目录形态因工程而异。

  - id: S11-image-two-names
    dimension: dedup
    found_round: "2026-08-30"
    form: 同一张图被两个路径引用，story 当成两张不同的图各引一次，后一张没有说明段
    clause: KG-3 / B2-9
    checker: s11_image_two_names
    target: mechanism
    bad_fixture: test/story/fixtures/failure-modes/S11-image-two-names/bad
    good_fixture: test/story/fixtures/failure-modes/S11-image-two-names/good
    applicable_when: always
    severity: BLOCKER
    status: fixed
    variant_of: S10-image-path-copied
    scope_note: >
      与 P10（同一路径引两次）互补：那条判的是同一个引用出现两次，
      这条判的是**同一张登记图片被两个不同路径指到**。实测两个文件内容完全相同，
      story 分别称它们为「管理页」与「管理页布局参考」。

  - id: S12-tradeoff-prose
    dimension: readability
    found_round: "2026-08-30"
    form: 关键取舍那一节写成散文——「否了什么、为什么被否的不行」两列被化进句子里，读者拼不出来
    clause: KG-3 / B2-9
    target: mechanism
    applicable_when: always
    severity: BLOCKER
    status: fixed
    responsibility: observed
    approved_by: 用户（2026-09-03 D10 修订签字；步骤 9 小段 3 评审 B2）
    reason: >
      固定形式判据只问「是不是表、有没有分节、段落多长」，而这一条问的是表达贴不贴合，要读上下文。判据随作业书改写在步骤 9 退场。
    observed_by: >
      审查者按「每个关键取舍：选了什么、否了什么、为什么」这一问看取舍是否可逐项对照；用户按同一轴看被否方案与理由在不在。
    scope_note: >
      与 S08（缺节）互补：节在，形态塌了。模板明写「成表」，两轮四份产物照样写成两段散文
      ——确定性的形式写在注释里就是自由发挥区。表头逐列逐字锁死，模型只填格子。

  - id: S13-limited-and-error-in-one-table
    dimension: readability
    found_round: "2026-08-30"
    form: 设计内的受限结果与真正的失败混进同一张表——读者分不清哪些要处理、哪些本来就是这么设计的
    clause: KG-3 / B2-9
    target: mechanism
    applicable_when: always
    severity: BLOCKER
    status: fixed
    responsibility: observed
    approved_by: 用户（2026-09-03 D10 修订签字；步骤 9 小段 3 评审 B2）
    reason: >
      固定形式判据只问「是不是表、有没有分节、段落多长」，而这一条问的是表达贴不贴合，要读上下文。判据随作业书改写在步骤 9 退场。
    observed_by: >
      审查者按「正常受限结果与真正异常分开说明」这一维度看；用户按同一轴看读者能不能分清哪些要处理。
    scope_note: >
      判的是「两张表头可区分的分立表在不在」，不判每一行归哪张——行归属是语义，
      归裁决者的逐章维度。空章豁免。

  - id: S14-acceptance-bulleted
    dimension: readability
    found_round: "2026-08-30"
    form: 验收写成 bullet——编号、通过条件与主责挤在一行文字里，没法逐条比对
    clause: KG-3 / B2-9
    target: mechanism
    applicable_when: always
    severity: BLOCKER
    status: fixed
    responsibility: observed
    approved_by: 用户（2026-09-03 D10 修订签字；步骤 9 小段 3 评审 B2）
    reason: >
      固定形式判据只问「是不是表、有没有分节、段落多长」，而这一条问的是表达贴不贴合，要读上下文。判据随作业书改写在步骤 9 退场。
    observed_by: >
      审查者按「每条原始验收编号的场景与可观察通过条件」这一问看；用户按同一轴看验收能不能逐条判定。
    scope_note: >
      组名自拟不判——按「要验的事」分组是作者的判断；判的是每个小节里那张表的头。

  - id: S15-appendix-prose-tail
    dimension: conservation
    found_round: "2026-08-30"
    form: 附录表后挂散文尾巴——没地方去的工程细节挤成段，倾倒区的最后一种形态
    clause: KG-3 / 坑 #30
    checker: s15_appendix_prose_tail
    target: mechanism
    bad_fixture: test/story/fixtures/failure-modes/S15-appendix-prose-tail/bad
    good_fixture: test/story/fixtures/failure-modes/S15-appendix-prose-tail/good
    applicable_when: always
    severity: BLOCKER
    status: fixed
    scope_note: >
      与 S06（附录多长一节）同源，形态更隐蔽：节数对、围栏没有，尾巴挂在表后面。
      开头那一句目的句不算——判的是表或列表**之后**的正文段。
      另一半治法是收窄中间产物的 token 守恒（那些尾巴本来就是被落点义务逼出来的）。

  - id: S16-material-list-intermediate
    dimension: conservation
    found_round: "2026-08-30"
    form: 材料清单里列中间产物与图片直链——清单从「据哪几份材料写成」变成倾倒区
    clause: KG-3 / 坑 #30
    checker: s16_material_list_intermediate
    target: mechanism
    bad_fixture: test/story/fixtures/failure-modes/S16-material-list-intermediate/bad
    good_fixture: test/story/fixtures/failure-modes/S16-material-list-intermediate/good
    applicable_when: always
    severity: BLOCKER
    status: fixed
    scope_note: >
      实测一份产物 22 行：本轮规格被链了 4 次，事实记录、参考件与原图都单列。
      允许的目录是合同数据（进 spec 之前的原始输入），按解析后的落点判，不按字面前缀。

  - id: S17-image-new-dir
    dimension: dedup
    found_round: "2026-08-31"
    form: 同一张图被复制进新建的图片目录（文件名没变）——只比文件名的判据放行，全树因此有好几份同图
    clause: KG-3 / 坑 #34
    checker: s17_image_new_dir
    target: mechanism
    bad_fixture: test/story/fixtures/failure-modes/S17-image-new-dir/bad
    good_fixture: test/story/fixtures/failure-modes/S17-image-new-dir/good
    applicable_when: always
    severity: BLOCKER
    status: fixed
    variant_of: S10-image-path-copied
    scope_note: >
      S10 判的是「改了名的副本」（文件名不在登记里），这条判的是**名字没变、目录变了**——
      上一版按文件名比对，这一种一路放行。允许的位置是材料里那些图既有的落盘目录，
      加上归档件自己的图片目录（合同 `story_image_dir`）。

  - id: S18-appendix-image
    dimension: conservation
    found_round: "2026-08-31"
    form: 图整批迁进附录——正文写着「下图是…」，读者读到那句话时手边没有图
    clause: KG5-2 / 坑 #35
    checker: s18_appendix_image
    target: mechanism
    bad_fixture: test/story/fixtures/failure-modes/S18-appendix-image/bad
    good_fixture: test/story/fixtures/failure-modes/S18-appendix-image/good
    applicable_when: always
    severity: BLOCKER
    status: fixed
    scope_note: >
      三轮复发。落点判做不到这件事：图片单元的落点是按「在哪被引用」反推的，
      图放哪儿落点跟到哪儿，那条判据对放错位置恒真（循环判据）。
      这一条与「每张登记的图都必须被引用」合围：进不了附录、又不能不出现，
      于是只剩它讲的那一章一个去处。

  - id: S19-material-list-prose-head
    dimension: conservation
    found_round: "2026-08-31"
    form: 材料清单的列表**之前**塞散文——上一版只看列表之后，之前是不设防区
    clause: KG5-2 / 坑 #36
    checker: s19_material_list_prose_head
    target: mechanism
    bad_fixture: test/story/fixtures/failure-modes/S19-material-list-prose-head/bad
    good_fixture: test/story/fixtures/failure-modes/S19-material-list-prose-head/good
    applicable_when: always
    severity: BLOCKER
    status: fixed
    variant_of: S15-appendix-prose-tail
    scope_note: >
      S15 判的是表后的尾巴，这条判的是列表**前**的那一段。实测一轮，四张图连同
      四段说明全塞在这里，判据一条都没响。这一节的形态是「一句目的句 + 列表行」，
      目的句之外的散文块在前在后一样点名。附录另外四节仍只判尾巴。

  - id: S20-dangling-figure-ref
    dimension: conservation
    found_round: "2026-08-31"
    form: 悬空的指图句——这一段写着「下图」，前后两块之内却没有图
    clause: KG5-2 / 坑 #37
    target: mechanism
    applicable_when: always
    severity: BLOCKER
    status: fixed
    responsibility: observed
    approved_by: 用户（2026-09-03 D10 修订签字；步骤 9 小段 3 评审 B2）
    reason: >
      固定形式判据只问「是不是表、有没有分节、段落多长」，而这一条问的是表达贴不贴合，要读上下文。判据随作业书改写在步骤 9 退场。
    observed_by: >
      审查者按「图与文的配合」这一维度看指图的那句话附近有没有图；用户按同一轴看图文对不对得上。
    scope_note: >
      承接判（image_lead）只从图往回看一行，图迁走之后它无从判起——留在原地的
      那句话没有任何判据管。这一条从话往外看：前后各两个块之内没有图就点名。

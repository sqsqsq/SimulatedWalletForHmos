我要给 PocketLedger 加一个功能：**隐藏余额开关**。feature 名用 `hide-balance-toggle`。

需求就这六条：

1. 「我的」页提供「隐藏余额」开关，默认关闭；
2. 开启后首页总余额显示 `****`，关闭后立即恢复真实金额；
3. 开关状态保存在本地，应用重启后保持；
4. 清除应用数据后恢复默认关闭；
5. 不上传服务端；
6. 不改变卡片余额、交易记录和支付功能。

请走 `/spec` 把规格写出来。**这一轮只做 spec**：不写产品代码，不进入 plan。

需要我拍板的地方直接问我。

# auto-topup 会议记录的对照答案（只进测试域）

> 被测输入：`supplements/交通卡澄清会记录-0903.docx`。本文件不在 `system/`、`workspace/`、`supplements/` 里，
> 不会被复制进被测工作区，也不进 prompt 与宿主回话。评分时对照「模型报出的会议结论、问人的条目、
> 人裁决后的 `doc-refresh.md`、design.md 与 spec 里的落点」，按 TEST §10 判。

## 各话题该有的去向

| 议题 · 话题 | 应有的去向 | 该不该问人 |
|---|---|---|
| 一 · 改门限面额的接口 | 变化：SR §3 新增 `updateAutoTopupContract`（contractNo、门限、面额，原签约关系不断），原话 14:04–14:06；结论：不走解约重签；只改门限不重新验证（14:16） | 是（新增接口，高影响修正） |
| 一 · 调高面额是否重新验证 | 遗留：待支付与风控确认；不能写成已定 | 是（未收敛，影响方案） |
| 一 · AC-R1 | 变化：RR §8 AC-R1 改为「未实名用户点入口被引导去实名，完成实名前签不成」，原话 14:09–14:11；会议与归档文档冲突 | 是（修正类） |
| 一 · 连续失败停用 | 原文已确认（3 次）；「三四」是转写存疑，纠偏依据 RR §6 | 否 |
| 一 · 开关关闭后的管理 | 变化：RR §6 功能开关、AC-R4 与 SR §4 末条改为「开关只管新签约，已签约用户记录、改档、关闭照常，入口不隐藏」，原话 14:13–14:15 | 是（修正类）；之后评审回稿第 2 条应识别为会上已定，不重复问 |
| 一 · 单日上限 | 原文已确认 200（上调到 300 的提议被否决）；变化：协议文案写明单日上限（RR §2 第 2 步 / SR `getAutoTopupPolicy` 协议文案），原话 14:19–14:20 | 可不问（低影响增强） |
| 一 · 授权后建约失败 | 遗留：协议号有效期卡云与支付未对齐；不能按十分钟写，端侧处理待定 | 是（未收敛） |
| 二 · 余额提醒（RR90007） | 不属于本需求；「不共用档位」不改变 AR90006 | 否 |
| 三 · 充值记录按月筛选 | 归属判不准；导出本期不做 | 是（归属不明） |

## 纠偏

孙好 → 孙浩、门线 → 门限、卡运 → 卡云、面密 → 免密；「三四」按 RR §6 为三次。只改字面，每处留痕。
参会人角色（模型提出、人确认）：王磊 产品，陈静 卡云，刘洋 支付，赵敏 运营，孙浩 钱包开发，周婷 测试。

## 不该出现

- 「协议号十分钟内可复用」写进方案或验收；
- 调高面额写成「不用验证」或「必须验证」；
- 第一段把单日上限写成 300 元（300 来自第二段的改版产品稿）；
- 余额提醒、充值记录按月筛选或导出并进本单范围；
- 会上没说的规则（例如更新接口的限频、协议号有效期的具体数值）。

## 宿主立场

见 `interaction-script.yaml` 里 `meeting-` 开头的各条：一个话题一条，加更新接口按会上定的；AC-R1 和开关两条按会上改；
充值记录按月筛选不算这张单；调高面额是否重新验证、授权后建约失败两件保持待定；参会人角色按模型列的确认。

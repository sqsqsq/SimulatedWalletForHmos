# 挂失与补卡系统拆分

`SR90004` 拆为挂失申请和补卡申请两个 AR。挂失申请拥有旧卡冻结状态并产出 `freezeTicketId`；
补卡申请读取该结果，负责地址、费用确认和订单创建，不复制冻结实现。

执行顺序固定为挂失成功后再补卡。挂失结果查询、通知失败和状态恢复由挂失特性收敛；补卡只处理交接结果无效、
地址校验失败、支付取消和订单创建失败。联调与开放被挂失接口和共享上下文清理能力阻塞。

验证覆盖 `REPLACE-AC-01`、`REPLACE-AC-02`、`REPLACE-AC-03`。

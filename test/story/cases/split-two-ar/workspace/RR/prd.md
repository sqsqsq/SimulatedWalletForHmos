# 实名钱包挂失与补卡

一个完整需求分为挂失旧卡与申请补卡两个特性。挂失特性产出 `freezeTicketId` 和最终冻结状态；
本特性只负责在冻结成功后收集补卡地址、确认费用并创建补卡订单。挂失未完成会阻塞本特性。

共享挂失上下文一直保留到补卡完成或取消；完成后清理，取消后仍保留旧卡冻结结果。

- `REPLACE-AC-01`：没有有效 `freezeTicketId` 时不能创建补卡订单。
- `REPLACE-AC-02`：订单确认前不扣费，取消填写不产生费用。
- `REPLACE-AC-03`：订单创建成功后清理共享上下文，旧卡继续保持冻结。

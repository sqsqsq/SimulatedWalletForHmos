# 03 退场：零读者、不可达判据、形态代理与过期物

退场规则：每项先查它进来的提交（见 00 §3 演进核对表），确认要守的性质是否已由别处承担；是维护不变量的移到测试域，不在运行时对模型产物判。每项列生产者、消费者核对结果、退出动作与随之退出的测试或文档。退出前 `rg` 全仓核一次消费者；测试域只删专为该项写的用例，回归覆盖仍有效的迁到接替者。不为退出而删正常能力。

## 1. 契约字段与常量（`core/flow`）

| 项 | 位置 | 消费者核对 | 动作 |
|---|---|---|---|
| `rounds[].imported` | `rounds.py:66,104` 写 | 只有 `rounds.py:46` 自读算 `already` | 改为函数内局部推导，不落契约；`rules/inbox_import.md:68` 把它当留痕的一句删 |
| `rounds[].materials.path` | `rounds.py:32` | 读者都硬编码 `materials.json` 路径 | 退出字段 |
| `positioning.scope_source` | `inputs.py:92-98` 校验写入 | 零读者 | 退出字段与枚举 `state.py:62`；`init_analysis.md` 侧车示例同步 |
| `scope_options[].recommended` | 作者写侧车 | 零读者 | 退出；推荐只在停等消息里由作者排第一（`init_analysis.md` 已这么写） |
| `split.parts[].depends_on` | `inputs.py:339-354` 写入查环 | 无程序读者，但 `scope_gate.md:175` 定义它为「须先交付的那几份」，是给人看的交付顺序记录，随第三级选项集落契约 | **保留**（评审指出后改判） |
| `materials_changed_after_complete` | `rounds.py:88-91` | 零读者；`test_material_rounds.py:294` 唯一断言。来源 95611620：收口后补料曾造成 18 分钟死锁，修法是「不开新轮 + reopen 出口」，字段只是那次修法的留痕 | 退出字段与该断言；**行为与「记一笔说清出口」的日志保留**，行为用例（不开轮、reopen 回到进行中、未收口拒绝 reopen）原样保留 |
| `reopened[]` | `rounds.py:142-147` | 零读者；`test_material_rounds.py:313`；同上来源 | 退出字段与断言；reopen 发生过由 `status` 值与日志可见 |
| `story_written_at` | `lifecycle.py:145` | 只 status 回显 | 保留（status 输出它） |
| `gates[].basis` | `decisions.py:132` | 无程序读者，人事后推翻用 | 保留（人读） |
| `design.sha256` | `submission.py:237` | 自用 | 保留 |
| `state.ANALYSIS` 常量 | `state.py:26` | flow 层无人用 | 退出 |
| `--scope-text` 兜底 | `decisions.py:103-108,147` | 无测试、无文档 | 退出参数与分支 |
| `topics.json` 旧格式检测 | `meeting.py:328-331` | 无生产者；`test_meeting_material.py:403` | 退出与该用例 |
| `decisions.py:22-23` gate 值域检查 | argparse `choices` 已拦 | 不可达 | 退出 |
| `refreshed_unsaved` 重试识别 | `submission.py:166-176` | 正常路径不可达，只断点态 | 保留（S4 断点恢复是现役设计），加一句注释说明可达条件 |

## 2. JS 侧契约双判据（随 R5）

保留与退出以 01 R5 的「手改契约时能否保护」表为准，本节不另列。退出 7 条：`by==human`、`outcome` 值域、`rejected→reason`、`at` 非空、`split.scope_text` 非空、`scope_options` 非空含 carry_all、「与上一轮 digest 相同」；连同两处读文件与两种 BOM 剥法合一。保留：契约可读、schema、`status` 值域、收口区间、三件齐备、`chosen∈options`、第二级选项 ⊆ 分析选项、split carrier 恰一份、positioning 三项。只为退出 7 条写的用例退出（`test_flow_check_*`、`test_negative_guards.py` 对应条目），保留条目的用例不动。

## 3. 成文判据（`core/story`）

| 判据 | 位置 | 事实 | 动作 |
|---|---|---|---|
| ⑫c `danglingFigures` | `images.mjs:103-134`，`check.mjs:250` | 形态代理（上一行是不是标题）；FM 台账 `S20` 已由用户签 `responsibility: observed`；0915 实拦 4 处 | 退出判据与用例；由 `story_reader_review` 的「图前声称覆盖的去向」承担 |
| ⑬ `reviewFormProblems` | `review.mjs:33-40,307-322` | 来源 65949a1b。评审后改判：模型写的澄清正文会被渲进 review.md，「确认人：」一类表单腔可以从那里长回来，不只是渲染器的事；成本约 25 行 | **保留运行时**；只把六条行首词表从脚本挪进合同 `review_form_banned_lines`，与其它词表同源 |
| ⑮ `strayFileProblems` | `context.mjs:24,254` | 来源 009d30df。评审后改判：AR 根不只公共机制能写，消费模型也可能把草稿或辅助件写到那里，运行时检查保护的是实际产物；两轮零命中不证明无用；成本约 20 行 | **保留运行时**，不动 |
| ①b 离线「首词像编号」分支 | `check.mjs:108-125` | 形态代理；离线整类不判即可 | 退出离线分支与 `test_story_build.py:356` |
| ⑫ 注释承诺的「表后散文尾巴」 | `appendix.mjs:259-261` | 判据不存在，FM `S15` 已 retired | 删注释 |
| `appendix.mjs:489 materialName` | — | 死变量 | 删 |
| `document.mjs:493 PENDING_MARK` 字面 | 合同 `pending_mark` 是真源 | JS 写死、Python 读合同 | JS 改读合同 |
| `sources.mjs::upstreamDocs` 两条硬编码路径 | `:174-181` | 合同 `sources` 已有 | 改读合同 |
| `sources.SPEC.notes: ["blockquote"]` | 合同 `:19-21`，`sources.mjs:40` 拷进 `docs[].notes` | 无下游读者 | 退出键与拷贝 |
| `language.mjs` 四类红线与词表 | 见 01 R6 | 谁接替发现：`harness_artifact` 要防的「装置词进正文」由附录 A–D 机器区投影（模型写不进）、`repo_identifier`（反引号与标识符形态）与 `rule_id` 承担，剩余装置腔由 `story_reader_review` 的表达质量 advisories 判；`ai_heading`/`search_phrase` 同归审查 advisories；风险是审查漏判一条措辞，不影响事实正确性，可接受 | 随 R6 |
| 未使用 import / 孤立 JSDoc / 悬空小节标题 | `story-build.mjs:34`、`appendix.mjs:12/15/73-76/204-205/410-411`、`check.mjs:25/304-307`、`delivery.mjs:10/115-118`、`images.mjs:10`、`review.mjs:10/12`、`sources.mjs:9/13/88-99`、`document.mjs:594-597`、`language.mjs:232/319` | 盘点 §10.5 | 删；`IMAGE_HINTS` 改名为其实际职责 |

## 4. hooks 与知识

| 项 | 位置 | 事实 | 动作 |
|---|---|---|---|
| 四份切列实现 | `knowledge.mjs:92-103`、`spec/post_check.mjs:51-54`、`plan/post_check.mjs:104-105`、`review/post_check.mjs:44`、`validation.mjs:54` | 后三份不处理转义竖线；plan 按位置取列与「按列名」原则相反 | 步骤 2 已把切法收成 `document.mjs::tableCells` 一处；步骤 7 把按列名取格也收成 `document.mjs::cellByHeader`，其余改用（不在 knowledge.mjs 另起一份） |
| hooks 侧第二份定位与围栏 | `hooks/spec/post_check.mjs:88-150` 的 `DOC_COORD_RE/scanDocCoords/findHeading/sectionRange`；`hooks/shared/verifier-report.mjs:87 yamlBlocks` | 步骤 2 交回指出：与 `core/story/document.mjs`、`language.mjs` 各一份 | `findHeading/sectionRange` 改调 `document.mjs::parseDocument/findByName/headingEnd`；`scanDocCoords` 改用 `language.mjs` 的文档坐标形态；`yamlBlocks` 改用 `fenceRanges`。只换读法，判据不变 |
| acceptance 集合名两种读法 | `spec/post_check.mjs:256` 只认 `criteria`；`ut:70`、`testing:74` 认 `criteria+boundaries` | 同链路不一致 | 统一为 `criteria+boundaries`，一处常量 |
| `verifier-report.mjs` 三处失实自述 | `:11-13`、`pre_verifier.mjs:15` | 实际唯一调用方是 `delivery.mjs:95` | 改注释 |
| `PER_UNIT_TABLE_RE`「无单元键表」硬判 | `verifier-report.mjs:52,245-246` | 用形态禁一种报告写法 | 退出该判据；「不逐条对账」只留 overlay 一句 |
| `harness_terms` 等词表 | 合同 `:207-229` | 随 R6 | 退出 |
| `knowledge/constraints/README.md:32-41` 域清单表 | 第二份清单 | 随 E9 | 退出 |
| frontmatter `applies_when/not_applies_when/triggers_usecase_spec` | 各知识文件 | 机器零读者，但是模型读知识文件时的域级摘要 | **保留**（随 02 E9 改判，删的是 README 第二份表） |
| `selfCheck` 四判据 | `knowledge.mjs:444-513` | 形态代理但成本低、只在 spec 调一次 | 保留 |
| `ext-post-check.json` 留痕 | `evidence.mjs` | 无脚本读者，但批次验收人读 | 保留 |

## 5. 提示层（随 R7 退出的具体段）

- 六份 `hooks/*/author.md` 第 3 行 blockquote（保留 SKILL 一处）。
- `story-write.md` §五（334-412）、§六自检（435-447）、:206 绝对句、:269-285 十一类全表（合同已定，只留「从合同类别里挑」一句）。§一（12-31）**不退**，压成清单保留（金样批注来源）。
- `phases/spec.md:165-168`；`templates/spec-sections.md:1-62` 原则段；`templates/plan-sections.md:1-24,111-166` 与 `hooks/plan/author.md:28-43` 二选一保留（must 挂法只讲一处，建议留 plan-sections）。
- `SKILL.md` 里「仅有的两处」这类不含既有确认点的停等枚举（与 01 R7 一致：枚举不全，不是矛盾）、`:106-111` harness 顺序细节（归 phases/spec.md）。
- `rules/inbox_import.md:47`「四类」改「六类」；`importer.py:3,139,168-169`、`import_sources.py:13` 的四类值域文案改为读 `CLASSES`；`importer.py:504`「由 cmd_import 解析成发言结构」删。
- `story_flow.py:17-23` docstring 补 `story`、`reopen`。
- `templates/inbox-readme.md` 保留（读者是人），删与 `inbox_import.md` 重复的三条约定之一。

## 6. 测试域

- 离线夹具的 framework 依赖（随 01 R1，用户 2026-09-16 定）：新增公共 helper，在临时工程根下建 `framework/harness` 指向真实目录的 junction（Windows）或符号链接；把扩展拷进临时根的六个套件（`test_knowledge_use.py:71`、`test_empty_knowledge_repo.py:47`、`test_author_task_package.py:124`、`test_image_registration.py:93`、`test_author_context_entry.py:88`、`test_adapt_ownership.py:58`）改用该 helper。不复制 node_modules，不在测试里退回旧读法。

- `check_failure_modes.py:2133-2258` 中台账已 `retired` 的 checker（S08/S09/S12/S13/S15/S19）删函数；台账保留 retired 记录。
- `measure_run.py::BASH_READ_RE`：只在读取动词直接作用于该路径时计数；执行脚本后管道截取不算；重算写新评价并注明算法版本（04 §3）。
- `test_chapter_view.py:430` 用例名「开闭规则只有一处」在 R4 后成为真命题，保留并把它改为扫描全部 `.mjs` 断言无第二份围栏正则。

## 7. 验收

- 每项退出后 `rg` 机制层零命中（字段名、常量名、判据函数名、词表词）；测试域剩余命中只允许是拒收用例或 assertNotIn。
- 离线全量绿；`check_failure_modes` FAIL 0；`adapt-scan --check` 通过；`framework/` 零改动。
- 退出账按类列实际行数（04 §5）。

## 8. 观察项（本需求不做）

- 投影区真源的窄路径：改 `knowledge-use.yaml` 一个错字要 reopen → complete → story 重登记全链重走（投影 + 冻结）。「只重投影、不撤销登记」碰冻结语义，需单独论证；裁定 A 落地后闭环后的 WARN 级改动不再发生，本需求内无收益。记录在此供后续需求取用。

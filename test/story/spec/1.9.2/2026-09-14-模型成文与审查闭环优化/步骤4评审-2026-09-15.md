# 步骤4评审：真实验证与交付判定（2026-09-15）

维护设计者评审。对象：本需求全部四步（代码 2211e4ea）在 suite `story-suite-20260914-190900` 的真实运行结果，对照 00 §1 目标、03 §7 验证表、04 §3/§4 判据与 TEST §8/§10。两 Case 的逐项证据、建议分与行号引用见 [真实运行评审](../../../output/story/story-suite-20260914-190900/evaluation/review.md) 及两份 `evaluation/scorecard.md`；本文只写判定与去向。

## 结论

**本轮需求可以交付。** 需求要恢复的能力（整篇写作设计被真实生成与消费、原材料判断延续、从来源核实际全文的责任归位、决策/知识/验收传递、Review 决策接口）在两个 Case 上都有实际运行证据，旧路线已退出，离线回归除两条规模上限外全绿。仍未达到的是 TEST §10 的效果目标（三轴 ≥90）与性能期望，以及作者整稿、plan 借挂这两个模型行为缺口；它们不阻断本轮交付，进下一需求。交付前有三件收口动作（§4）。

## 1. 证据边界

- auto-topup（deepseek）：story→spec 完整闭环，62.6 分钟，是本轮最完整的证据。
- car-key-sharing 第一次 attempt（deepseek）：spec 闭环、真人评审回流完成、plan 产物写完并过脚本门禁，派 plan verifier 时外部 401；plan 语义只能由本评审直接核产物。
- car 第二次 attempt（GLM）停在材料关卡，不采信。
- 单轮两 Case 不构成稳定性证据；没有无页面 Case；Framework AC 机械一致性未集成。

## 2. 目标逐项判定

### 2.1 00 §1 六项恢复目标

| 目标 | 判定 | 证据要点 |
|---|---|---|
| 原材料初筛与判断延续 | 部分成立 | 两 Case 的 init-analysis ⑥ 都有来源/用途/疑点表；car 的 24h/48h 冲突从初筛一路进 DEC-02 并交人裁决。auto 初筛记下的「修改门限面额走哪个接口 SR 未单列」没有进 decisions 或正文；AC-R1 与情形表的矛盾初筛未识别，靠 verifier 暴露 |
| Spec 后增量整合与整篇设计 | 成立 | 两 Case 均在十章前经 skeleton 写出 template（71/123 行），内容有章主线、分支去向、依据与形式；设计一次成形后不再更新（DEC-11 裁决后 auto 的 06 行已过时） |
| 形式选择与确定性落实 | 成立 | 结构选择 5/8 项全部在草稿有起点并通过章提交检查；car 07 章拆表被拦后合回。`syntax` 字段两轮都没被模型使用（见 §3 B2） |
| 按设计写作并允许深化 | 成立 | 首稿含全部界面图与流程图；car 自行加了时序图与管理图，auto 加了 4.2/9.4；未被 template 限制 |
| 从来源到实际正文核对 | 作者侧未成立，verifier 侧成立 | 两 Case 的「写后核对」只修语言红线（auto 5 秒自述完成；car 3.5 分钟全是形式项）；未决冒充已定、跨章矛盾、同 ID 异义均由 verifier 首轮以 BLOCKER/FAIL 拦下 |
| 决策/知识/验收准确传递 | 大体成立 | 已定/未决分清（修后）；AC 原编号与 RR 一致；spec 知识判定落到具体名字。plan 契约 3 处借挂（见 B3） |

### 2.2 03 §7 八项验证

| 验证 | 判定 | 证据要点 |
|---|---|---|
| template 开放骨架 | 部分 | 章级主线具体可用；两 Case 都没有用 `####` 小节骨架 |
| 图类型选择 | 未验证 | 无模型选择 `syntax: sequenceDiagram`；car 时序图由方法文字驱动产生，静态测试仍是唯一证据 |
| Review 两类 | 成立 | choice/confirm 渲染正确、建议先选后因、填写位未预选；car 真人表态逐条写回，编号对应正确，不同意栏未编造 |
| 人工区保护 | 成立 | car 回流后重渲染、reopen 两次，人写内容原样保留 |
| 真实裁决 | 成立 | auto 冲突交需求方（回话 #5），DEC-11 记评审人依据，RR 原文未改；car DEC-02/03/06 均待裁决后才落定 |
| 回流路径 | 成立 | 处置台账在 `AR/story-src/review-disposition.json`；spec/acceptance 按处置更新；冻结 Story 未改写；交付门与 check-receipt 通过 |
| 知识语义 | 作者侧未成立 | contracts.yaml 三处借挂（配额→COMPAT-01、入口可见/等待点→UX-01）；verifier 未跑 |
| 统计 | 成立 | checker 源码读取 28/13 与事件一致；上轮 0 的错误读数已消除 |

### 2.3 04 §4 否决信号核对

- 「template 只是形式壳、写作不消费它」：不成立，设计具体且首稿按其覆盖分支。
- 「统稿只补标记」：**仍成立**（换成只补红线）。
- 「一个未决在不同章被重新定」：修后不成立；首稿两 Case 各有一处。
- 「为遵模板删除有效深化」：不成立。
- 「verifier 按模板打勾放过真实缺口」：不成立，verifier 从原材料核。

## 3. 问题清单

### A. 交付前收口（不改机制，实施者执行）

- **A1 预算与规模收口**：`mechanism-budget.yaml` 工作区改动仍写 600，用户已确认 820；实施者按 AGENTS §5 核清 829 估算里的重复计账后写实数，同步 820 与两次范围记录；scripts_mjs 3377/3300、总量 10867/10800 两条上限按实测请用户签一次。本评审认可步骤 3 的新增为必要实现，不要求为 9 行返修功能。
- **A2 交付映射（04 §5）**：更新 `plan/2026-09-11-Extension功能全集与机制归属/FEATURES.md` 候选中「初筛判断延续」「运行时整篇写作设计」「按设计表达与确定性结构落实」「从来源核实际全文」「Review 选择/确认交互」的职责与生产者/消费者/失败位置；准备 1.9.x 版本与使用指南差异说明，不改 manifest、不宣称发布。
- **A3 本轮未完成项登记**：把 §3 B 组与 §5 依赖写进交回，00 总览已改为指向本评审。

### B. 进下一需求（机制或方法问题，需重新定目标）

- **B1 作者整稿实质核对未发生**（最重要）。现象：两 Case 的整稿只处理形式红线，关系与未决核对全交 verifier，每次多一轮 verifier（6–9 分钟）与一次 reopen。按 AGENTS §3.1「同类问题反复……先审视整条流程」：整稿的思考单位是否该按「一条未决/一条跨章条件」而不是「通读全文」；是否把「从来源核覆盖」拆成作者可执行的具体动作（列出本需求全部 open 决策与跨章条件，逐条指认正文位置）而不是三段方法描述；不增加自证台账。
- **B2 `####` 骨架与 `syntax` 字段未被采用**。两轮无运行证据，也无反证。下一需求先决定：保留并在方法里给出触发条件的具体例子，还是收窄（`syntax` 只保留检查能力、不在方法里推销）。不为此加门禁。
- **B3 plan 契约借挂复发**。作者指引已写「不借编号挂 must」，作者仍借；overlay 判据未经真实 verifier 验证。下一步：用本轮 car 的 contracts.yaml 做一次 plan verifier 真实运行（用户授权），看 `knowledge_obligation_substance` 是否拦下三处；再定作者侧是否需要在契约模板处给「业务规则写在哪」的落点。
- **B4 初筛疑点未延续**。auto 的更新接口缺口在初筛可见、后续消失。检查 story-write/spec 方法里「Spec 后再审新增结论与冲突」是否给了可执行动作（读初筛表第三列逐条处置）。
- **B5 性能**。上下文 517K/700K、机制文本读取 77/78 次、约 20 分钟学习框架判据（car）。方向：作者在动笔前要的判据是否已在任务包里；不是压缩提示。
- **B6 机制缺陷（小，可并入下一需求首步）**：① `hooks/shared/yaml-lite.mjs` 把列表项字符串中的 `:` 与 `/` 当键值拆开，`knowledge-use render` 渲染出 `[object Object]`，模型改措辞绕过；② 空 H3 标题重复（auto §6.7/§6.8）章检查不报，进入交付件；③ 步骤 2 交回的 `lifecycle.cmd_story` 未收口时下一步计算失败遮蔽提示。
- **B7 r2.md 留存件退场**：见 §5。

### C. Framework 侧（记录，不在本需求）

- AC 在 spec 正文与 acceptance.yaml 的同 ID 机械一致性、NFR 量化适用性仍未交付（步骤 2 交回 §7 合同）。
- auto 在 verifier PASS 后 20:12:18 改 acceptance.yaml，`--sync-closure` 20:12:31 以旧 subject 提交闭环，summary 未重算：闭环提交不检测脚本报告之后的产物变化。

## 4. 交付判定的依据

- 新行为成立：00 §2 正常路径在两 Case 上完整走通（初筛→Spec→skeleton→template→写章→登记→verifier→裁决→回流→交付门）。
- 旧路径退出：步骤 1–3 交回与评审已核旧整稿方法、单一「审核结果」、旧处置路径、全篇最后标签定位等退出，本轮运行未见旧路径痕迹（AR 根无散件、review 无旧标签）。
- 门禁与回归：离线 1034 passed，仅两条规模上限红；失效形态台账 FAIL 0。
- 效果：两 Case 三轴建议分均高于上轮，但均在 70–89（auto 82/72/82，car 84/66/74），待用户确认；不宣告达到效果目标，不关闭旧轮 G2。

即使有小问题也交付的理由：剩余缺口都不是本需求新增机制造成的退化，且每一项都有明确的责任位置与下一需求入口；继续在本需求内追加修改会重复步骤 3 的「测完再加一步」模式。

## 5. r2.md 留存件裁定建议（运行观测 O1）

证据：auto 作者全程未打开 `AR/story-src/sources/ar/r2.md`；6 条上游预填全部由提取稿与正文承接；本轮没有因回查原稿发现问题；car 为本地单无此目录。它是 step2.3 为「提取稿覆盖输入件」加的留存，不在当前方案的设计里。

建议：**删除**，作为下一需求的一个独立小步，同步撤掉本方案 00 §2「留存原AR」与 §4「原AR三身份」保护项（由设计者改），退出 `author.mjs` 4a、`reader-review-task` 入口、`sources.mjs` 清单必列、`check.mjs::originalArSource`、`state.py::AR_SOURCES` 与相关测试。前提是同时解决「提取稿覆盖 `AR/design.md` 后上游原话无处可查」：最简单的做法是提取稿不再覆盖 `AR/design.md`，上游原件保持原位、提取稿另名；这一点在该小步里定案，本轮不动。需用户确认后执行。

## 6. 建议的下一需求范围

一句话：作者侧把「核」变成可执行动作，机制侧只修已知小缺陷，验证时先用现有 car 产物跑一次 plan verifier。范围：B1、B3、B4 为主，B6 三处小修与 B7 退场并入，B2 先定去留，B5 只做诊断不做优化。不扩大到无页面 Case 与 Framework 依赖，后者按用户裁定另立。

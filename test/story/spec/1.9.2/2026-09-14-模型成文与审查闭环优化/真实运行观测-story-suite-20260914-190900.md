# 真实运行观测 · story-suite-20260914-190900

> 维护实施者在运行期间记录的待核事项。运行中不改机制、不改被测产物；本文只列事实与测完要核的证据，结论测完后反馈用户。

## O1 `AR/story-src/sources/ar/r<轮次>.md`：测完反馈是否删除

**用户要求（2026-09-14）**：当前最新方案里没有它、且它没有用，就记录下来，本轮测完反馈是否删除。

### 已核事实（运行中，只读）

| 项 | 事实 | 出处 |
|---|---|---|
| 是什么 | S4 收口提交提取稿覆盖 `AR/design.md` 之前，把被覆盖的需求系统预填稿原样存成 `r<当前轮次>.md`；本轮补料开出第 2 轮，故为 `r2.md`（1116 字节） | `submission.py:165、240`；auto-topup 工作区 |
| 为什么只有一个 | 只在收口提交时写，且只有「收口后重开、上游又给新 AR」才会出现第二份；RR/SR/收件箱原件不进这里。整个 `sources/` 只装这一类 | `submission.py` |
| 来历 | `804db2d3`（09-10，step2.3-01）为解决「提取稿直接写 design.md 导致材料指纹自变、流程自重开」引入；提交说明与方案文档均无「放源材料碎片」的计划；2.3 来源索引/章节材料副本现行方案明确不恢复 | `git show 804db2d3`；`00` §4 |
| **方案里有没有** | **有**：`00` §2 路径「S4提交候选并留存原AR」；`00` §4 保护项「原AR三身份」；偏差审视把「原AR保留、原AR/候选/提取稿边界」记为已成立能力 | `00-需求与实施总览.md:28、51`；`目标与实现偏差审视.md:33、53` |
| 代码消费者 | 作者任务包「4a 上游原 AR」（`author.mjs:251`）；读者审查任务包（`reader-review-task.mjs:80`）；附录材料清单必列、缺则报错（`sources.mjs:166、333`）；定位读取 `check.mjs::originalArSource` | 机制层 |
| **本轮实际读取（截至 spec 前）** | auto-topup 事件 10 处涉及该路径，全是命令输出、目录遍历与附录清单写链接，**无一次打开读其内容**；car-key-sharing 仅读过提到它的规则 `ar_design_init.md` | 两 run 的 `events.jsonl` |
| 呈现问题 | 附录材料清单里「开发需求：design.md」实际指向模型自己的提取稿，而上游原稿标成「上游原件：r2.md」；类别名与文件名由 `sources.mjs::materialListSkeleton` 生成，读者看不出 r2 是需求系统原稿。模型首稿贡献说明一度写成「收口前上游提取件的留档」（把原稿叫成提取件） | `drafts/10-附录.md:32`、`story.md:422` |

**用户定性（2026-09-14）**：这是**典型的旧机制未退场问题**——为解决 step2.3「提取稿覆盖输入件」而加的留存机制，在现行方案回退 2.3、只保留整篇写作设计之后没有被重新审视，仍以一个只装一份文件的 `sources/ar/` 目录、内部轮次号和清单必列项留在交付面上。

**实施者补充事实**：现行 `00` §2/§4 仍把「留存原AR / 原AR三身份」写成保护项，删除须设计者同步撤掉该保护，否则方案与实现不一致；另有三个代码消费者需要一并退出（见下）。本轮运行中不删、不改，测完按下列证据给出删除建议。

### 测完要核（只读现场，不改一个字节）

1. 两个 Case 的作者、verifier（读者审查任务）是否实际打开 `r<轮次>.md`：查 `events.jsonl` 的 read/bash 读取，verifier 无内层轨迹时标未知。
2. 读者审查或整稿是否因为回查上游原稿发现了提取稿丢失/改写上游原话的真实问题；有则说明它发挥了作用。
3. 上游预填稿里的每一条（auto-topup：服务端余额判定、免密上限随面额、复用既有合约、开关只管新签约、连续 3 次失败停用；car-key-sharing 对应条目）是否已被提取稿与 Spec 完整承接——若全被承接且无人回查，留存件的实际价值只剩「可追溯」。
4. 材料清单两行（提取稿 vs 上游原稿）的最终写法与读者可理解性。

### 测完反馈给用户的内容

- 证据结论：留存件本轮有没有被读、有没有帮忙发现问题。
- 建议（二选一或组合，删除须设计者改 `00` §2/§4 保护项并经用户确认，实施者不自行删除）：
  1. **删除**：仅当证据显示无人读取、无发现价值，且设计者同意撤掉「原AR三身份」保护；需同时处理 `AR/design.md` 被覆盖后上游原话无处可查的问题（例如提取稿不再覆盖 `AR/design.md`），并退出 `author.mjs` 4a、`reader-review-task`、`sources.mjs` 清单必列、`check.mjs::originalArSource`、`state.py::AR_SOURCES` 与相关测试。
  2. **保留但改呈现**：扁平为按含义命名的单个文件；材料清单标「开发需求（需求系统原稿）」，提取稿不占原始材料名分。

## 进展快照（只读 `status` 与 `progress-snapshot只读快照.py`，不消费事件）

| 时间 | Case | 状态 / 阶段 | 产物（行数，mtime） | 备注 |
|---|---|---|---|---|
| 19:46 | auto-topup | running / spec（最高 spec），无待回复，事件静默 173s | template 72 行、占位 0（19:35）；decisions 94；story 424、十章无待写（19:41）；review 229（19:41）；spec 429（19:43）；acceptance 435；knowledge-use 106 | 写作设计先于成文完成；静默多半是作者或门禁长时间运行，不是故障 |
| 19:46 | car-key-sharing | running / spec（最高 spec），无待回复 | template 124 行、占位 0（19:36）；decisions 58（19:45 仍在改）；story 402、十章无待写（19:46 仍在改）；review 149（19:44）；spec 412；knowledge-use 128；**需求根 `acceptance.yaml` 快照未见** | 仍在整稿与决策登记；验收文件缺席待终态核（可能尚未生成） |

## 回话记录（运行中）

| # | 时间 | Case | 关卡 | 回复 | kind / step |
|---|---|---|---|---|---|
| 1 | 19:13 | car-key-sharing | 材料盘点，问题单正文是占位件 | 投 `数字车钥匙分享.docx`，回「1」 | planned / hand-over-the-product-doc |
| 2 | 19:14 | auto-topup | 材料盘点，界面图与流程图原稿在产品手上 | 投 `交通卡自动充值.docx`，回「1」 | planned / hand-over-the-ui-doc |
| 3 | 19:17 | car-key-sharing | 范围关卡，建议整体承载（无编号） | 「同意，不拆单。」（模型未问撤销时限，未提前表达 24 小时立场） | planned / init-wrap-up |
| 4 | 19:19 | auto-topup | 范围关卡，整体承载 vs 按端侧链路拆两份 | 「1」 | planned / scope-do-both-in-one |
| 5 | 19:58 | auto-topup | spec 首轮 verifier（1 BLOCKER story_reader_review＋4 FAIL）后，模型识别 RR 内 AC-R1「未实名看不到入口」与情形表/AC-R8「点入后引导实名再回签约页」冲突，判定改验收口径须人裁决，列两项 | 「2」（入口可见、点入后引导实名；规划无此立场，需求方按情形表与 AC-R8 选） | answered |

| 6 | 20:39 | car-key-sharing | spec 交付门通过（verifier PASS），问进 plan 还是先由人审 review.md（列 DEC-02 宽限窗、DEC-03 端云承载、DEC-06 转分享） | 「2」（先评审，保留两类填写位与处置记录位置的回流观测） | answered |

| 7 | 20:41 | car-key-sharing | 停在 spec 层，列 6 条议题（待确认 3：宽限窗、端云承载、转分享/极端撤销；已定 3）请评审人表态 | 「宽限窗选1，端云承载选你建议的那项，转分享与极端撤销保持未定，已定三条确认，撤销中的用户文案改为「正在收回」。」（逐类表态，不用笼统「通过」；覆盖规划宽限窗按产品稿 24 小时立场） | planned / review-comments |

| 8 | 20:56 | car-key-sharing | 评审回流完成、交付门通过（规划已走完 6/6），问进 plan 还是停在 spec | 「开始 plan。」 | answered |

- 20:39 auto-topup 终态 `finished`（spec 闭环，目标 spec）。
- **O5 评审回流实测（20:56 只读核）**：
  - 处置台账落 `AR/story-src/review-disposition.json`；AR 根只剩 design.md、detail.json、review.md、story.md——上一轮 ⑮ 自相矛盾缺陷本轮未复现（终态 story-build check 待核）。
  - `review.md` 填写位：DEC-02「方案选择：1（按产品稿 24 小时）——用户口头确认：选择方案 1」；DEC-03「方案选择：1（仓库层本地仓储承载）——用户口头确认：选择你建议的那项（方案 1）」；DEC-01/04/05/06「审核结果：[x] 确认」，DEC-06 另附括注「保持未定：两件均不改为本期已定」；不同意栏未被编造。表态与宿主原话一致，由模型据口头表态写回（SKILL「决策权在人、笔在 AI」），并注明来源为用户口头确认。
  - 「选你建议的那项」被对到 DEC-03 的建议编号 1，未误对到别的议题。
  - spec AC-K5 现为「离线的 24 小时内失效…离线时列表展示『正在收回』」，与裁决和文案修改一致；模型自述已同步 spec §4.2、§6 E5、§8 AC-K5/AC-K7 与 acceptance.yaml，并走 correction-init → revalidate → verifier 复审 PASS（4 WARN 记 notes）。
  - 待终态核：DEC-02 裁决前 spec 已先写 24 小时（首稿即取了一边、由模型披露），属「未裁决先落口径」的残留，虽结果与裁决一致仍记缺口；verifier 复审的 4 条 WARN 内容。
- 回流待核：模型是否把表态原样落进 `review.md` 对应填写位（方案选择填编号、复核勾确认），是否不替人扩写；DEC-02 裁决后 AC-K5 与下游是否据处置更新；处置记录是否落 `AR/story-src/review-disposition.json`；最终 story-build check 是否不再因根目录散件失败；「端云承载选建议项」是否被正确对到 DEC-03 的建议编号。car-key-sharing spec 交付门通过，进入评审回流观测。

- **21:17 基础设施故障（非被测能力）**：car-key-sharing 第一次 attempt（`20260914-191046-7528-40ef6db5`，`bailian-deepseek`）在 plan 阶段派 verifier 子任务时，DashScope 返回 401 `invalid_api_key`；run 终态 `cli_failed`、`failure_kind=auth_required`、`target_reached=false`，历时 7578.8s。phase_results：story completed、spec closed、plan open（缺完成回执与 verifier 报告）。装置按 TEST §1.1 在本 suite 熔断该配置，从干净基线以 `volcengine-glm-flash`（GLM5.3-Flash）起第二次 attempt `20260914-211724-30628-dd62e427`，工作区重建为仅 SR/design.md、AR/detail.json，21:18 在材料关卡等回话。第一次 attempt 的 events/runlog/state/phase-results 保留在其 run 目录，是本 Case 的主要评价证据。用户决定（09-15）：回复「继续」，观察是否已恢复。
  - 回话 #9：car-key-sharing 第二次 attempt，模型称「仓库里没有找到 ISSUE-206 的记录」并索要问题单正文 → 宿主按用户指定原话回「继续」，记 `improvised`（原话来自用户，非规划立场；评价时单列）。观察点：模型是否自行找到工作区与 `/story init` 正常起手、材料关卡是否再次出现、GLM 配置下能否走通。
  - **已恢复（09-15 08:45）**：收到「继续」后模型自行建好 `doc/features/ISSUE-206/` 工作区骨架、盘点材料（SR 已就位、RR 为占位件），停在材料关卡给两项编号选项——与第一次 attempt 第 1 关同一关卡。
  - 回话 #10：投 `数字车钥匙分享.docx`，回「1」，记 `planned / hand-over-the-product-doc`（第二次 attempt 规划游标从 0/6 重新开始）。
- **回灌风险**：`finalize --promote` 回灌的是当前 workspace；若新 attempt 未跑完就收场，回灌件会是近空的重建工作区而非第一次 attempt 的产物，收场前须核回灌范围。

- **测试结束（09-15 08:49，用户要求）**：用户明确不用 GLM5.3，要求停掉并结束测试。`stop` 后 car-key-sharing 第二次 attempt（`volcengine-glm-flash`）终态 `stopped`，停在 story（仅建工作区与投放产品稿，无 spec 产物）；heartbeat 已停。auto-topup 此前已 `finished`（spec 闭环）。car-key-sharing 的评价证据只用第一次 attempt（`bailian-deepseek`，spec 闭环＋评审回流，plan 未闭环，401 中止）。第二次 attempt 不纳入评价。
- **回灌（09-15 08:50–08:53，按用户指定来源）**：用户要求回灌 auto-topup 与 car-key-sharing 的 deepseek 产物、不要 GLM 的。`finalize --promote` 只能按各 Case 当前 workspace 回灌，car-key-sharing 当前 workspace 是 GLM 重建件，故先手工以 robocopy 放入：`doc/features/AR90006` ← auto-topup workspace（65 文件/496206 字节，与其 run `artifact/` 逐项一致）；`doc/features/ISSUE-206` ← 第一次 attempt `20260914-191046-7528-40ef6db5/artifact/`（76 文件/654003 字节，最后写入 21:14，早于 401）。随后 `finalize --promote`（无 `--promote` 被拒）：auto-topup `already_promoted`（目标与 workspace 摘要相同）；car-key-sharing `promotion_conflict / feature_destination_conflict`（目标已是 deepseek 产物，GLM workspace 未覆盖）——该冲突是按用户意图有意保留，不是回灌事故。两 Case `changed_paths=0`。复核后主工程两份目录字节未变，`.current-phase.json` 不存在。
- **ISSUE-206 回灌件的限定**：来自 plan 未闭环时刻的快照（plan 缺完成回执与 verifier 报告），评价与后续使用时不能当作 plan 已闭环。

## 行为观测（运行中）

- **O4 宽限窗冲突的去向（car-key-sharing）**：模型在 spec 交付时把「DEC-02 宽限窗」列为要人审的议题，没有自行定成 settled；规划立场「按产品稿 24 小时」尚未被问到，宿主未提。待评审回流时核：该议题的 `review_mode` 是否为 choice、选项是否为独立有序列表、建议是否先写选项再写理由、人未选时是否仍为 open。
  - **20:40 只读核（`decisions.json` 与 `review.md`）**：DEC-02 `status=open`、`review_mode=choice`；标题陈述冲突「产品稿 24 小时 vs 系统设计 48 小时，待裁决」；正文用「决策点／依据」，「可选的做法」为独立有序列表 1/2，各写影响；「建议：选择方案 1（按产品稿 24 小时）」先选后因；人工区为「方案选择：」首版、未预选。顶部提示按两类填写位生成。其余登记：DEC-01/04/05 settled＋confirm，DEC-03 open＋choice，DEC-06 open＋confirm（未决事项复核，未硬造选项）。
  - **仍有缺口**：依据里模型自述「规格的验收条目 AC-K5 暂按产品口径 24 小时写，属于未经裁决取了一边」——它如实披露了，但 spec 的验收仍先落了一边。待回流核：人裁决后 AC-K5 是否据处置更新，或未裁决时 spec 是否改写为待决边界。

- **O2 真实冲突交人裁决（步骤 3 §5 目标）**：上一轮 auto-topup 在同一处 AC-R1 冲突上自行写 settled 并改下游；本轮 19:58 模型在 verifier 打回后明确引用「改变明确验收或范围口径要人裁决」，未自行定案，停下用编号菜单交需求方。同时它区分了可直接修的实现问题（AC-10 编号冲突、§9.3 声明、模拟承载标注、流程图分支）与须裁决的口径问题。待终态核：裁决后是否只沿处置更新下游、原 RR 文字未被改写，`decisions.json` 对应条目是否记为有人裁决依据；冲突是在首轮成文时就该登记 open 还是到 verifier 才发现（首轮漏识别仍是缺口）。
- **O3 verifier 首轮拦下的问题**：story_reader_review BLOCKER（未实名路径矛盾、DEC-06 open 被写成已定的「本地模拟承载」）＋ simulation_scope_awareness、reference_crosscheck（spec AC-10 与 acceptance AC-10 冲突、§9.3 检索声明）、business_flow_branch_coverage（F9 实名引导分支未入流程图）。说明新严重性口径确实拦下了「未决写成已定」与「AC 同 ID 不一致」，但作者整稿自查没提前发现。

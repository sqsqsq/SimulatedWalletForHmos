# 交接 · 步骤 1/2 已通过、真实运行已跑完、待评价 · 2026-09-14

> 给接手这条需求的**维护实施者**（新会话、任意模型）。方案与交回是真源，这里只写**停在哪、接着怎么做、不看就会踩的坑**。
>
> **已过时（2026-09-14 晚）**：两 Case 评价已由评审完成（`output/story/story-suite-20260914-143310/evaluation/review.md`），设计者据此新增步骤 3（`03-骨架表达与决策交互优化.md`，原验证与交付改为 `04`），用户确认执行额度 820 后步骤 3 已实施交回。当前状态：2211e4ea步骤3代码与静态评审通过，R1/R2关闭；步骤4真实验证与Framework依赖未完成，预算精确账待收口。以 [步骤3评审](步骤3评审-2026-09-14.md) 为准；本文 §3 真实运行步骤与 §8 环境坑仍可参考。

## 1. 现在停在哪

- 需求：本目录（`00-需求与实施总览.md` + `01`–`03` 分册）。
- **步骤 1、步骤 2 的 extension 实现已通过评审**：见 [步骤1评审](步骤1评审-2026-09-14.md)、[步骤2评审](步骤2评审-2026-09-14.md)。
- **预算**：用户 2026-09-14 按「正向必要实现且被替换旧机制退出」确认，功能执行额度 **600**，**原 310 与累计变化照常披露，不重置分母**；旧在途额度已由评审同步进 `mechanism-budget.yaml`（见 §6）。
- **Framework 依赖未完成**：AC 在 spec 正文与 `acceptance.yaml` 的机械一致性、NFR 量化适用性，上游合同在 [步骤2交回 §7](步骤2交回-2026-09-14.md)。未集成前不能把 AC 机械一致性写成完成。
- **步骤 3 真实运行已跑完一轮**（用户明确授权「2 个 case 同时跑」）：suite `story-suite-20260914-143310`，已 `finalize --promote`。
- **还没做**：①按 03 §3 与 TEST §8/§10 的**逐项评价**；②实跑暴露的**机制矛盾缺陷**修复（§4.3，需用户/评审授权）。别把本需求写成已完成。

| 提交 | 内容 |
|---|---|
| `74993e92` | 步骤 1：来源初筛、运行时写作设计 `story-template.md`、`writing-plan.mjs`、结构选择落实、当前输入 |
| `b3c3c77e` | 步骤 1-R1：选定结构不被「不涉及」绕过；已写章改选择仍给结构起点 |
| `c9ce889a` | 步骤 1-R2：合并重合结构时保留作者选择 |
| `5d061a49` | 步骤 2：按设计写章、从来源核实际全文、审查按原材料独立判断、「回滚/回退」退出词表、reopen/story 给下一步 |
| `0eb0aa5a` | 步骤 2-R1：reopen 写入成立后算下一步失败不再报成重开失败 |

branch `story`，**未推送**。

## 2. 先读哪几份

1. `test/story/AGENTS.md`（当前工作树版，重点 §3、§4、§5）。
2. `test/story/TEST.md`：评价读 §5、§8、§8.1、§10；**再跑真实 CLI 必须读完 §0–§6**。
3. 本目录 `00` 总览、`03-验证与交付.md`（§3 的对象表、§4 达成标准与否决信号就是评价判据）。
4. `步骤1交回`、`步骤2交回`（做了什么、局部选择、预算账、未验证项）。
5. 金样 `test/story/golden/story-金样-AR90004.md`（效果比较，不抄业务值）；上一轮评审 `E:\Project\bak\Story-Output-story-suite-20260913-232703\cases\auto-topup\20260913-233046-29204-b31f3344\evaluation\review.md`（对照基线）。

## 3. 本轮真实运行怎么跑的（下次照这个走）

1. **先保护上一轮证据**：`start` 会删除所有历史 suite 的 workspace/output。本轮先把 `output/story/story-suite-20260913-232703` 整个复制到 `E:\Project\bak\Story-Output-story-suite-20260913-232703`（**用 robocopy**：`Copy-Item` 遇到超 260 字符的缓存路径会漏 3 个文件；robocopy 补齐后 4358 个文件、字节数一致）。
2. 前置自检（TEST §2.0）：`framework.config.json > paths.ui_kit_target_dir` 已配且已物化；主工程 `framework/harness/state/` 无 `.current-phase.json`。
3. `python test/story/scripts/run_multi_case.py plan --all --jobs 2`（只读）→ 复述 Case/feature/终点/workspace/回灌范围 → `start --all --jobs 2 --suite-id story-suite-<时间> --authorize-non-sandbox`。
4. heartbeat：后台跑 `test/story/plan/2026-09-11-Extension旧机制退场与新机制替换/13-实现质量替换/hb-heartbeat代跑.py <suite-id> 3000`，它在 `reply_then_poll` / `finalize` 时退出叫宿主。**回话之后再重启它**（先起会立刻看到仍在等回复又退出）。
5. **只能有一个 poller**：heartbeat 活着时不要手工 `poll`。要看状态用只读 `status`，或本目录 `progress-snapshot只读快照.py <suite-id>`（读隔离 workspace 产物行数、写作设计占位、待写章数，不消费事件）。**heartbeat 的中间输出被缓冲**，退出前读它的输出文件是空的，不是挂了。
6. 全部终态后，确认主工程无本 feature 的阶段状态，再 `finalize --suite-id <id> --promote`。

回话纪律（TEST §3.0）：有编号选项只回编号；补料先 `--deliver` 再回编号；`--reply-kind`/`--step` 如实记账；一次不超过一句；不点命令、文件、字段、关卡名。

## 4. 本轮实测结果（评价直接用）

### 4.1 结果与证据位置

| | `auto-topup`（AR90006，终点 spec） | `car-key-sharing`（ISSUE-206，终点 plan） |
|---|---|---|
| run-id | `20260914-143555-31368-7c822e44` | `20260914-143556-2904-b9794c01` |
| 终态 | `finished`，spec 闭环，`target_reached=true`，退出码 0 | `gate_failed`（§4.3），spec 与 plan 都闭环，`target_reached=true` |
| 回灌 | `doc/features/AR90006`（71 文件），无冲突 | `doc/features/ISSUE-206`（70 文件），无冲突 |

- 现场：`output/story/story-suite-20260914-143310/cases/<case>/<run-id>/`（`events.jsonl`、`runlog.md`、`state.json`、`gate_*.log`、`gate_diagnostics.json`）；隔离 workspace `%TEMP%\sw-story\story-suite-20260914-143310\<case>`。**下一轮起跑前都保留**。
- 起跑时主仓 `doc/features/AR90006`（上一轮回灌件，67 文件）已迁到 `E:\Project\bak\Story-Features-20260914-143544`。
- 历史清场 42 个目录全部 `retained_cleanup_warning`（删除失败、安全预检通过），下一轮起跑重试；上一轮 suite 现场因此仍在原位，另有 bak 副本。
- 产品源码无改动（两个 Case 的 `changed_paths` 为空）。

### 4.2 效率（`measure_run.py`，只报数）

| 项 | auto-topup | car-key-sharing | 上一轮 auto-topup（2026-09-13） |
|---|---|---|---|
| 时长 / 工具调用 | 95.6 分 / 375 | 92.6 分 / 339 | 42.9 分 / 268 |
| 读规则文本 / **读 checker 源码** | 4 / **0** | 4 / **0** | 49 / **19** |
| 上下文增量 | 497K | 575K | 325K（目标 ≤150K） |
| harness 次数 / 有 FAIL 轮次 | 4 / 1 | 9 / 2 | 3 / 2 |
| 时间去向（近似）verifier / 成文 | 1565s / 1656s | 975s / 1746s | 783s / 647s |
| 人工等待 | 173s（3 次） | 391s（5 次） | 63s |

**比较时注意**：本轮两个 Case 在同一台机器并行跑，时长与耗时不能直接和上一轮单 Case 比；配置同为 `bailian-deepseek`，但配置名相同不证明模型版本相同（03 §2）。

### 4.3 实跑暴露的缺陷（未修）

`car-key-sharing` 的 `gate_failed` 只因 story-build check ⑮：`AR/review-disposition.json 不该在这一层`。

- 根因是**扩展自相矛盾**：`doc/extensions/skills/story/SKILL.md:213` 与 `rules/review_reflow.md:10/41` 规定处置台账落 `AR/review-disposition.json`；`scripts/core/story/context.mjs` 的 `AR_ROOT_FILES` 白名单（⑮）不含它。模型照作业规则做、被另一条规则拦。
- ⑮ 是更早一轮需求引入的，本需求没动。
- 修法方向（待授权，按 AGENTS §3「引用必须真实存在/单一真源」回原职责）：台账落点与白名单只留一种说法（例如处置台账进 `AR/story-src/`，并同步 `review_reflow.md`、`SKILL.md`、写入者与测试），不在 ⑮ 加特例。

### 4.4 宿主回话记录（8 次）

| # | Case | 关卡 | 回复 | kind / step |
|---|---|---|---|---|
| 1 | auto-topup | 材料盘点，模型主动要界面原稿 | 投 `交通卡自动充值.docx`，回「1」 | planned / hand-over-the-ui-doc |
| 2 | car-key-sharing | 材料盘点，问是否还有产品材料 | 投 `数字车钥匙分享.docx`，回「2」 | planned / hand-over-the-product-doc |
| 3 | car-key-sharing | 范围承载 | 「1」（整体承载） | planned / init-wrap-up |
| 4 | auto-topup | 范围承载（整体 vs 按 SR 拆两份） | 「1」 | planned / scope-do-both-in-one |
| 5 | auto-topup | 术语确认 + 无视觉能力处置 | 「1、3」（全表确认；保持盲档，不改宿主视觉配置） | planned / terms-confirmed |
| 6 | car-key-sharing | spec 闭环后：进 plan 还是先评审 | 「2」（先评审，保留评审回流观测） | answered |
| 7 | car-key-sharing | 请评审人对 review.md 表态 | 「通过；撤销中的用户文案改为「正在收回」，未定的两件保持未定。」 | planned / review-comments |
| 8 | car-key-sharing | 评审回流完成，问是否开始 plan | 「开始。」 | answered |

`improvised` 零次。

### 4.5 评价时要核的观测（运行中未干预）

1. **撤销宽限窗 24h/48h**：car-key-sharing 规划立场是「按产品稿 24 小时」（`product-doc-wins-on-grace-window`），模型**从未把冲突拿来问人**，先登记为待车云确认；宿主第 7 条的「通过」被它解读为接受自己「按 SR 48 小时」的建议并回填。要核：决策发现与呈现是否把真实冲突交人裁决、笼统表态被当成确认推荐是否是机制问题。
2. **接受侧界面说明**（`车钥匙接受侧界面说明.docx`）：规划 `on_request`，模型全程没开口要，宿主按纪律未投。要核：是否因范围判定为「接受侧不在本 AR」（review 议题 5）而合理不要，还是材料缺口没被发现。
3. **写作设计确被写满**：两个 workspace 的 `story-template.md` 分别 71/96 行、占位 0，写于十章之前。**是否照设计写、整稿是否从来源核、审查是否按新严重性拦**，要从 `events.jsonl` 与产物还原，不能用「文件存在」代替。
4. auto-topup 中途 `decisions.json` 一度只有空骨架（3 行），成文过程中补到 52 行；car-key-sharing 审查报告自述 verifier 10/10 PASS，需核实际语义结论。
5. auto-topup 有一轮 FAIL 含 `feature_to_acceptance`（AC 关联）；car-key-sharing `lifecycle_hook_post_check_extension` 两轮。

## 5. 接着做什么

1. **逐项评价**（03 §3、TEST §8.1/§10）：初筛延续、template、设计开放性、首次正文、整稿、中间契约（含 AC 同 ID 一致）、独立审查、恢复；Story 每个 H2/H3/H4 最小正文逐项评；与上一轮 auto-topup 与金样比较；三轴建议分，**由用户确认**。证据只用 §4.1 现场，**评价期间不改现场与回灌产物一个字节**。
2. **§4.3 缺陷**：向用户/评审报告并取得授权后再修，修完按受影响范围重验（离线全量、形态、相关用例）。
3. Framework 依赖：只维护上游合同与集成证据，不在本仓 `framework/` 就地改、不在 extension 复制检查。
4. 真实运行再测前须用户授权；下次起跑前同样先备份本轮 suite 现场。

## 6. 不要碰的东西

| 东西 | 为什么 |
|---|---|
| `framework/` | 只读（CLAUDE.md 红线 9） |
| `test/story/regression/mechanism-budget.yaml`（工作树未提交改动） | 评审按用户确认同步的额度，不是实施者的改动，别并进你的提交、别改数字 |
| 本轮现场与回灌产物、`E:\Project\bak` 下两份备份 | 评价证据 |
| 被测模型产物 | 观测者不替它跑 gate、改产物（TEST §0.1） |

## 7. 规模与离线基线

- 机制层 **10720 有效行 / 556415 字符**（相对需求基线 `ab3709f7` 10314 / 532247：+406 行、+24168 字符）。
- 离线：`pytest test/story/tests -n auto --dist loadscope` **1005 passed / 310 subtests**（`0eb0aa5a` 时预算 3 条红；评审同步额度后 `test_mechanism_budget.py` 实测 **5 passed / 16 subtests，已全绿**）；`check_failure_modes.py` FAIL 0 / 委派 8 / PASS 56；`tools/cli/tests` 27 passed。

## 8. 环境坑（本会话真实踩过）

- PowerShell 里 `python -c "..."` 带 `\"` 的格式化串会被截断成语法错误——**写成脚本文件再跑**。
- `git commit -F -` 配 here-string 在 PowerShell 下被当成路径参数——**提交说明写文件再 `-F <文件>`**。
- `Copy-Item -Recurse` 遇超长路径静默漏文件，**备份用 robocopy 并核文件数与字节数**。
- heartbeat 代跑脚本输出有缓冲；只有一个 poller；`status` 只读诊断可随时用。
- `hooks/spec/author.md` 有 60 行上限（失效形态 A03），往里加要求要并句不要加段。
- 失效形态台账的公共 check 入口会在 S 夹具缺写作设计时放 R01 底样那份最小设计；自搭工作区的测试要同时放 `decisions.json` 与 `story-template.md`，否则停在台账那一步、走不到被测判据。

## 9. 未验证项

- 模型是否真用写作设计、整稿是否从来源核、审查是否按新严重性抓实际缺口——本轮现场可评，尚未评。
- AC 机械一致性、NFR 适用性（Framework 上游）未交付。
- 无页面数据管理类需求没有正式 Case；单轮两 Case 不代表稳定性。
- `lifecycle.cmd_story` 在未收口时计算下一步失败会遮蔽「还没收口」这句（步骤 2 交回 §0 观察项，未改，待设计者裁定）。

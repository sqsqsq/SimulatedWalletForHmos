# 当前结论：步骤1代码通过（c9ce889a）

合并复审见 [步骤2评审](步骤2评审-2026-09-14.md)。以下为此前b3c3c77e反馈，R1/R2均已关闭，不继续执行其待返修结论。

# 步骤1当前复审：b3c3c77e

结论：**R2通过；R1仍有合并场景遗漏，步骤1待返修，步骤2不开始。** 本次仅核步骤1，不提前要求完成步骤2的独立审查/整稿接线。维护实施者按当前AGENTS与本分册完成同一职责修正，不增加业务特例。

## 唯一阻断项：去重丢失作者明确选择的来源信息

`writing-plan.mjs::selectedStructure`第223行：当pick是章级图而base.diagram已存在时，不向diagrams保留selected项。新pickedStructureNames只读selected表/图，所以不能识别“合同要求与作者选择恰好重合”的情况。

独立真实命令复现：有效template选择`{"chapter":"05-flow","at":"","kind":"diagram"}`，skeleton成功后向业务流程章提交“本需求不涉及。”，chapter退出0、正文落盘。这个章恰好有合同必要图，但用户明确选图不能因合并去重变成没有选择。

修正在合并职责：相同结构可以只核一次、只搭一次底，但必须保留作者明确选择这一事实；pickedStructureNames或其等价消费者可据此识别声明冲突。不要在chapter里按“业务流程”名字特判，不另扫一次template补偿合并丢信息。

验收核三种情况：作者选择与静态要求重合、作者选择新增结构、只有静态要求而无作者选择。前两种仍选着结构时“不涉及”必须失败且不改Story/候选；撤回选择后沿原合法空章语义；第三种不被本次整体收紧。覆盖章级图、小节图、表，合并后不重复起点或同因报错。此前评审没有明确列出“与静态要求重合”的组合，我也需承担边界推演不完整的责任。

## 已通过部分

R2的writeDrafts已将“是否完成”与“是否可覆盖”分开：已写、已修改、从现稿补回的草稿不覆盖，但会按当前选择输出缺失起点；新增/改名/撤回及结构已存在的测试覆盖真实已提交章。自动替换说明也改为行尾归一后内容相同，不再冒称逐字节判断。

R1对新增表与小节图的普通场景已能报冲突，章提交与全篇共用chapterProblems。方向正确，剩余问题是合并过程中丢失声明来源，不是需要新状态或更长模型提示。

本次产品diff仅三处相关模块，未重做初筛/template协议，未进入步骤2；未见其它新增阻断问题。正文可扩展、模板可调整及原写入保护继续保持。

## 验证与预算

独立运行test_writing_plan、test_writing_flow：**45 passed、19 subtests passed，57.47秒**；diff检查通过。额外正式chapter反例确认上述错误返回0。未重复前轮316条相关回归或实施者全量，未启动真实CLI，不能据离线结果宣称模型能写出有效template。

交回累计规模10671、scripts_mjs3280及功能新增约440/310≈142%为实施者报告，本轮未重新全量计数，不冒称独立实测。初始310属于整个需求，净增不是新增功能预算，结构接线另列；本次不因旧总量硬失败拒绝，也不调额度。允许此收敛修正与必要验证；步骤1代码闭合后还须由reviewer具名复核本步成本和步骤2剩余空间，不能用逐步改签消除原累计增量。

本次只改维护评审和验收要求，产品修正由维护实施者完成。返修更新原步骤1交回，集中交一次，不重做R2。

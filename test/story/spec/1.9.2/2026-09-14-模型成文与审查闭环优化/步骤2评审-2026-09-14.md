# 步骤2当前复审：0eb0aa5a

结论：**步骤1及步骤2的extension代码评审通过。** reopen写后失败问题已关闭；Framework的AC机械一致性及NFR适用性依赖尚未交付，真实模型效果尚未验证，不能宣称整个需求完成。用户2026-09-14已按“正向必要实现且被替换旧机制退场”的条件确认本报告预算，适用范围见§3。

## 1. 失败语义闭合

cmd_reopen只在save成功后捕获材料读取/路由的FlowError，保留in_progress和登记已撤销的事实，返回next为空及明确的修复材料、再查status指引，不诱导重跑reopen。save仍在捕获范围之外，实际保存失败不会被包装成成功。没有新持久状态或事务协议。

独立相关回归test_material_rounds、test_s4_commit：**113 passed、2 subtests passed，72.00秒**。覆盖真实临时文件上的材料读取失败、路由失败与save失败注入，以及既有收口/原件恢复。diff检查通过。未重复上一轮231条相关回归或实施者1005条全量，未启动真实CLI。

本次只改原责任函数与相应测试，没有新增业务分支或测试案例专名到运行逻辑。步骤1的选定结构来源、已写章恢复、template协议保护与步骤2作者/独立审查职责保持，无其它已发现阻断问题。

## 2. 目标与证据边界

丢失的整篇设计职责已有实际承担者：初筛延续、运行时template、模型选择后结构落实、按设计写作、从原材料审当前全文，已接入正常入口。模板不是业务真源或正文白名单。现有离线证据证明协议与数据/动作可取得，不证明模型一定正确理解或执行。

Framework依赖仍按步骤2交回§7：不能在本消费仓就地修改framework，不能在extension复制影子AC检查。上游未集成前，可以在明确限制下进行经用户授权的写作探索，但不得把AC机械一致性标完成。正式验收和是否先测由用户决定，不以本次代码通过自动启动。

## 3. 整体预算反思与已确认额度

独立计量：scripts_mjs3288、scripts_py1838、hooks_mjs3072、prompts_md1784、data738，总**10720有效行、556415字符**；相对需求基线10314/532247净增406行、24168字符。本次较5d061a49增加4行。必要结构接线继续排除在功能预算比例之外，完整总量保留。

交回的累计新/替换功能实现约567（前563加本次4），相对原310约183%；此为职责估算，不用净增或Git新增行冒充精确功能成本。即使结构另列，当前报告仍需按超过150%的权限处理，不倒签、不逐步重置分母。

初估110行脚本不足以覆盖运行时设计协议、选定结构与静态合同合并、诊断、恢复及所有消费者，后续又漏算了一些边界闭合，是设计者的估算责任。保留这些真实职责是必要的；现在不能为回到310而删除template、放宽来源/结构保护或把复杂计算重新交给模型。

本次明确的保留项：现有writing-plan协议及有限结构声明、合同复用与选择来源、当前输入与恢复、真实审查对象与语言边界。已替换的草稿拟标题路线、重复/相反说明及失败语义不恢复。没有必要再造通用AST、更多Agent或版本台账。协议是否值得其运行成本还须真实测试，代码通过不构成收益证明。

**用户2026-09-14确认，现按以下范围执行：**

- 认可当前已审extension实现，功能新增/替换的当前执行额度调整到**600有效行**，用于已实现部分及剩余本需求收口修正；不扩展功能范围。
- **保留原始310与累计变化**，后续仍按原基线披露比例；600不是重置分母。必要结构成本另报。
- 旧机械在途额度调整为scripts_mjs3300、scripts_py1850、hooks_mjs3100、总10800，prompts/data及历史最终目标不变；它们是完整规模检查，不冒称功能比例。已同步mechanism-budget.yaml；该文件仍只检查完整规模，不声称已实现功能预算分级。
- 不增加功能、真实CLI调用或Framework修改授权。若验收暴露新的实质职责或额度不足，提供具体差额复核；不提前保留无边界预算。

确认的条件是实现需求所必需、且被替换旧机制已经退出。依据步骤1/2的实际diff与独立复审，本次已审0eb0aa5a范围满足该条件：

| 被替换职责 | 当前承担者与退出结果 |
|---|---|
| 仅在章草稿列标题即完成整篇设计的路线 | 运行时story-template及明确消费者；相反的“不另落规划文件/没有先分配”指令退出 |
| Spec前任务包代替成文当前输入 | skeleton取得当前storyInputs；原Spec尚未形成却要求找回其图的无条件说明退出 |
| 选定结构仅靠模型记忆 | writing-plan与chapter-contract共用合并结果，来源不因去重丢失；不是额外并存的一套章检查 |
| 旧整稿任务与强迫被否备选 | 从原材料核实际全文，template是待核判断；无条件备选要求及相反说明退出 |
| 回滚/回退的词义否决与补偿豁免 | 词表、替词建议及对应豁免退出，语义审查看实际对象 |
| 重开后下一步错误却冒充重开失败 | 原cmd_reopen内部修正，保存失败与已保存后的提示失败分别处理，旧失败分支被替换 |

这是对已审实现的有界认可，不是所有未来增加均自动合理。后续收口修改须同样给出必要性和对应退出；没有替换对象的真实新增不制造假删除。新范围或超出已确认额度再按规约复核，保留原310和累计变化，不用600重置比例。Framework未交付项未被本次确认豁免，模型效果仍须真实验收。

## 4. 当前交接状态

extension实现通过；预算已按用户条件确认；Framework依赖未完成；步骤3真实测试未开始。当前计划、交回与本结论同步，不再执行旧的reopen待返修意见。维护者可继续必要证据整理与上游信息核实，不据本报告自行扩大实现或启动真实测试。

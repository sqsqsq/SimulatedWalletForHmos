# 1.9.7 版本范围

2026-09-25 用户授权起草。本版独立于 2.0.1，先于它交付；只登记范围与完成条件，不构成实施或真实 CLI 测试的启动授权。1.9.6 的整版实跑正在进行，按其自己的判据收口，不等本版。

| 事项 | 范围 | 验收边界 |
|---|---|---|
| 知识的分类与表达形态 | [需求正文](2026-09-25-知识分类与表达形态/00-需求.md)：三类知识 × 三种形态构成四格，每份知识用 frontmatter 的 `kind` 与 `form` 声明自己；加载器按声明分派与校验，登记、送达、门禁、审查只引用协议结构名；adapt 方法页按四格组织并指向 Demo 示例，升级后按演进记录与只读检查结果确认是否做知识适配 | 离线证明加载分派、校验一次报全、登记单元、机制零专名、增删改知识 extension 零 diff、文档一致、升级后确认；内网按方法页写知识并只读检查通过由用户在内网证明；送达与登记效果、规约与模式链不退化靠整版实跑 |

## 与 1.9.6 的衔接

| 共用对象 | 1.9.6 的做法 | 1.9.7 怎样处理 |
|---|---|---|
| 知识自描述（`name`、`kind`、`applies_when`）与 manifest 唯一登记 | 已建立 | 保留，加 `form`；目标 manifest 另加目标所有的 `knowledge_adapted_for` |
| event-tracking 与设计模式同形的写法 | 419650f6 落地 | 成为 halves 形态的 Demo 示范；节名只是示范，机制不核 |
| 机制里为埋点写的任务包、审查、门禁、模板文字 | 步骤 2 与两次返修加入 | 改为按 `form` 的通用句，角色词与内容复述退出；§9.4 与埋点小节的产物形状保留 |
| 对 Demo 知识内容与面名的测试断言 | 随知识改动逐次修改 | 退出，只留形态校验；专名扫描词表改为从知识派生 |
| 内网适配指南 | 1.9.3→1.9.6 | 由 1.9.3→1.9.7 替代（内网现为 1.9.3） |
| 知识写作粒度「写到模型找得到为止」 | protocol 已加 | 保留，作为四格共同写法 |

## 不在本版

- 2.0.1 的 framework 3.x 适配。
- 任务包自动把篇正文附上、按 `form` 自动送达当前阶段的那一篇。
- knowledge-use schema 2、plan 侧对下篇的登记。
- 新知识类型、第四种形态、facts 投影到 plan 知识决策章。
- adapt 的中性目标仓演练（沿 09-25 决定，由用户在内网验证）。

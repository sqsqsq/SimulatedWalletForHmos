# 第一步实施交回：章法、引用、投影与交付门分流（2026-09-22）

依据：[步骤1诊断评审与实施裁定](步骤1诊断评审与实施裁定-2026-09-22.md) §3–§6。基线 `41b48626`，改动全部在工作区，未提交。没有启动模型。首轮与复测的原件都没动；复测原件在仓外归档里，本仓 `doc/features` 下回灌的两份也保持原样。

## 1. 改了什么

| 文件 | 改动 | 对应裁定 |
|---|---|---|
| `hooks/shared/chapters.mjs`（新） | `profileAsset` 经 `framework.config.json > project_profile.name` → 该 profile 的 `skill-assets.yaml` 取模板，不写死 profile；`chapterNumberProblems` 按模板核主章号（模板不编号的章不许编号），带号子节须以父章号开头，各层都核；`chapterRefProblems` 核表里「号 + 章名」的引用是否对得上本文实际章，只写章名或写链接的不核 | §4 |
| `hooks/spec/post_check.mjs` | 用 spec 模板加扩展的 `spec-sections.md` 核章号（扩展模板经 `extensionRoot` 取）；取不到 framework 模板时不核 | §4 |
| `hooks/plan/post_check.mjs` | 新增一组「章号、设计章引用与验收引用」：按 plan 模板核主章号；核「承载设计章」一列；核 `use-cases.yaml` 的 `linked_acceptance` 都在 `acceptance.yaml` 里。验收编号取 acceptance 顶层各列表条目的 id，不认前缀；use-cases 读不了、acceptance 缺席，与悬空引用分开报。原注释按 SKILL 的九章编号解释设计章，一并改成按模板 | §4 |
| `core/story/appendix.mjs::wholeSection` | 子标题迁入附录时，去掉以源小节自身号开头的局部编号（`9.4.1 `）；业务标题里的数字（`2.0 版本…`）不动。只含锚点的链接照旧指回 `../spec/spec.md#…`，仍能对到 spec 里的标题 | §4 |
| `hooks/shared/verifier-report.mjs` | 报告缺席时不再无条件要求「重新写到那里」，按裁定改为：有与当前请求一致的原始回复就原样写回，再完整跑一次 harness；没有就等本轮材料改完，取当前请求派审；闭环沿用了历史审查时，照 summary 说出当前对象没有独立审查、沿用的是哪个对象、哪些材料未重审；明说不要复制或改写其他对象的报告。仍为 FAIL，不从 `summary.mode` 推定已获授权沿用 | §3 |
| `hooks/spec/author.md`、`hooks/plan/author.md`、`phases/spec.md` | 章号照模板，不编号的章不计入序号（扩展追加章的号指向 `spec-sections.md`，不另抄）；派审后又改了审查材料的，先重跑 harness 取新请求再派；别的对象的报告换名或改 subject 都不算这一次的审查 | §3、§4 |

**没做**：
- 交付门三种消费者（初次交付、普通 correction、用户 update）怎么分流。现有上下文判断不了「这次沿用历史是否获准」，所以只报事实和真实去处，不临时造豁免。需要设计者给出最小输入，见 §5。
- use-cases 的方法引用检查。先核了事实（§3），没加拦截。

## 2. 在真实产物上的效果（只读运行 hook，不改产物）

| 对象 | 结果 |
|---|---|
| auto `spec.md`（AR90006） | 报 16 处：Scope 被编号、后续 8 个主章号顺延、技术契约下 9.1–9.5 与父章 10 不一致、10/11 两章号错 |
| car `spec.md`（ISSUE-206） | 无新增问题（`ok=true`） |
| car `plan.md` + use-cases | 报 8 处：「承载设计章」错号两类（5. 页面组件树、4. 数据模型定义），6 个悬空验收编号（AC-K1/K11/K6/K4/K5/K7，带所在用例）；主章号本身与模板一致，不报 |

## 3. 补证据

- **复现原始输出**：run1–run5 的 `harness.log`、`summary.json`、审查对象材料清单与 `ai-prompt.md`，以及 run4 改动前的 story.md，归档在 `E:\Project\bak\1.9.5-C-story-suite-20260922-1904\repro-step1\`（23 个文件，`MANIFEST.sha256.txt` + README）。README 写明：副本里被采纳的 `1fd7…` 报告是复制改写来的，那几次「正常采纳」只证明能通过机械采纳，不证明来源真实，也不授权复用。
- **use-cases 方法引用**（ISSUE-206，对照 contracts）：
  - `draftStore.*`、`keySharingRepo.*` 都能对上契约方法；
  - 协调者 `ShareSetupFlow` 订阅的 `flow.granteePermission / quota / submitting` 是 `ShareSetupPage` 的状态，不是 `ShareSetupFlow` 的成员（它只有 5 个方法）；
  - 协调者 `ShareManagePage` 调用的 `flow.onRevoke / loadList / loadQuota` 在契约里没有声明，`ShareManagePage` 只有 4 个状态。
  
  绑定按什么规则解析归框架定义，我只列事实。要不要加检查、放在哪一层，待设计者定。

## 4. 测试与预算

新增或修改的测试：

| 位置 | 内容 |
|---|---|
| `test_chapter_numbers.py`（新，6 条） | 中性模板下：按模板编号通过（含合法自定义小节、模板没有的章）；不编号的章被编号时报出并连带报顺延与父子错位；H4 挂错父节；扩展模板追加章；「号 + 章名」只报错号那一行，只写名字或写链接的不报；另起名为 `neutral-web` 的 profile，经它的资产清单取到模板，缺的键返回空 |
| `test_gate_groups.py` +2 | 悬空验收编号：编号取自 criteria 与 boundaries 两个列表，无固定前缀，只报悬空的并带用例名；「承载设计章」对的那一行不报、错号那一行报 |
| `test_story_build.py` +1 | `9.4.1 开户办理` 进附录成 `##### 开户办理`，`2.0 版本的结果查询` 保持原样 |
| `test_verifier_report_protocol.py` 改 1、加 1 | 缺报告时的新提示；沿用历史收口时说出当前对象未独立审查、沿用对象、未重审材料与当前请求路径 |

- **全量测试**：`python -m pytest test/story/tests -n auto --dist loadscope` → 1292 passed / 15 skipped / 0 failed。
- **机制计量**：12451 → 12574（hooks_mjs 3409→3527、scripts_mjs 3499→3501、prompts_md 1876→1879），逐文件如下：

  | 文件 | 行数变化 |
  |---|---|
  | 新 `chapters.mjs` | +69 |
  | plan post_check | +34 |
  | spec post_check | +6 |
  | verifier-report | +10 |
  | appendix | +2 |

- **与额度的差距**：裁定额度是章法/引用/投影 +70/−30、交付门 +25/−10；实际新增 +121，退出 0（没有被替代的旧判据），**超出约 26 行，且没有退出项**。台账已按实测提议在途上限 hooks_mjs 3425→3530、总 12490→12580，并写明理由，**待设计者核认**；功能没有为凑数压缩。

## 5. 需要设计者定的

1. **交付门分流的最小输入**：要区分「这次按 framework 修正入口沿用历史，获准交付」和「需要独立审查」，现有 summary 与流程契约里没有可用的标记。要不要在哪个现有记录上补一个事实（例如 correction 路径留下的记录），还是维持现在的做法：一律 FAIL、报事实、给两条去处？
2. **use-cases 方法引用**：§3 的不一致要不要检查，归扩展 plan 门禁还是交上游。
3. **超额**：26 行超额与零退出是否接受，还是指定应退出的旧判据。
4. **上游材料**：已写在本目录的 `上游交接-章序冲突与验收引用-2026-09-22.md`，未发出。

## 6. 按《步骤1实施评审》返修与交付分流取证（2026-09-23）

### 6.1 R1：模板读不到不再静默跳过

`chapters.mjs::profileAsset` 现在返回三种结果：取到正文、不适用、输入坏了。不再笼统返回 null。

| 情况 | 怎么报 |
|---|---|
| `framework.config.json` 没配 `project_profile.name` | 不适用：记进 `skipped`，门禁输出「有 1 条判据未执行：spec/plan 主章号（没有配 project_profile.name…）」 |
| 配了 profile，但读不到 `skill-assets.yaml` | 报问题，写明路径 |
| 清单解析失败 | 报问题，带解析错误 |
| 清单里没有 `spec.spec_template` 或 `plan.plan_template` | 报问题，写明缺哪一项 |
| 清单指向的模板文件读不到 | 报问题，写明指向的相对路径 |
| 扩展模板 `spec-sections.md` 读不到 | 报问题，不再当空串用 |

模板取得统一走新增的 `chapterTemplates`，spec 和 plan 共用，两个调用方各自只剩 3 行。模板有效但没有某个自定义章，仍按原规则不报。

测试：`test_chapter_numbers.py` 的 profile 用例扩成 4 条：
- 取到正文；
- 没配 profile 记为不适用；
- 输入坏了的四种情况，各报一条问题；
- 扩展模板缺失时报问题。

`test_gate_groups.py` 里两条「干净通过」的用例原来断言「输出为空」。中性夹具没有配 profile，现在门禁会如实记一条未执行说明，所以改成断言「没有问题，只有这一条未执行说明」。

**全量测试**：1294 passed / 15 skipped，另有 2 条是预算上限失败；台账按提议调高后，预算测试也通过。

**预算**：hooks_mjs 3527→3541（+14），实测 12588。台账已提议在途上限 hooks 3545、总 12595，待核认。本工作面累计新增 +135，初始额度 95，R≈142%。

### 6.2 交付分流：现有记录能给出什么（只取证，未实施）

交付门 `delivery.mjs` 在三条路径上都会被调用：spec ⑥（`phases/spec.md:58`、`routing.py:377`）、归档登记前的 `lifecycle.py:188`，以及远程单归档前的 SKILL「归档」①。

| 事实 | 位置 | 含义 |
|---|---|---|
| framework 只要当前对象没有报告、同一阶段历史上有过 PASS，就按 `completed_with_prior_review` 收口，**不区分路径** | `check-receipt.ts:704–730` | `summary.verifier_closure.mode` 本身不说明这次沿用是否获准，不能单凭它判 |
| `--revalidate`（framework 修正入口）重跑时，summary 的 `readiness_signals` 里带 `script_revalidated` | `harness-runner.ts:1758–1765` | 这一次收口来自修正入口；之后若又正常跑了一次完整 harness，这个信号就不在了 |
| `--revalidate` 还写 `<feature>/revalidation.json`（`from`、`chain`、起止时间、各阶段结果） | `revalidate.ts:163–182` | 修正入口的执行账 |
| 用户 update 开着一轮时，流程契约 `story-flow.json` 里有 `update.open = <轮次>`；收口后变成 `last_closed` | `update.py:363、566` | update 期间，改动过的阶段必须按新对象审一次（`phases/update.md`「改动过的阶段，要按新的审查对象审一次」） |
| 真实产物：AR90006 spec 带 `semantic_not_reverified`，没有 `script_revalidated`，`update=None` | 回灌后的 `doc/features/AR90006` | 属于首次交付，应重新审 |

**提议的分流**（只读已有事实，不新增状态，不让作者手填）：

| 路径 | 判据 | 交付门 |
|---|---|---|
| 普通首次交付 | 沿用历史、没有 `script_revalidated`、没有开着的 update | FAIL：当前对象没有独立审查；等材料改完，取当前请求派审（现在的行为） |
| 普通 correction | 沿用历史，且 summary 带 `script_revalidated`，没有开着的 update | 放行，但记一笔「当前材料未独立重审（沿用 X 的审查，未重审：…）」，与没有审查员时的记法一致；不说成审过 |
| 用户 update | 流程契约 `update.open` 非空 | FAIL：update 改动过的阶段按新对象审一次（遵守 update.md 已有规则） |
| 当前报告存在，但 FAIL、绑定不符或读不了 | — | 照现在的报法，不因路径放行 |

**实现代价估计**：`delivery.mjs` 多读一次流程契约与 summary 信号，`verifier-report.mjs` 多一个分支，约 +12～15 行；测试加三条路径各一例。

**待设计者确认**：
- `script_revalidated` 是 framework 在 summary 上留的运行记号，拿它作「修正入口」的依据是否可接受？
- correction 之后如果又跑了一次完整 harness，记号就没了，会被当作首次交付要求重审。这个保守的取向是否可以？

确认后再动手；在那之前保持现在的一律 FAIL 与事实提示。

## 7. 按《步骤1返修评审与分流定案》与《内网对接反馈与修正交接》（2026-09-23）

### 7.1 R1 最后一个分支：工程配置解析失败

`profileAsset` 自己读 `framework.config.json`：
- 文件缺席，或能解析但没有 `project_profile.name`：记为未执行，写明原因；
- 解析失败：作为输入问题报出，写明解析错误。

新增两条用例：`{ broken` 报「framework.config.json 解析失败」；`{"paths": {}}` 记为未执行。

### 7.2 交付分流：未实施，先交额度

本工作面累计新增约 140 行，初始额度 95，R≈147%。定案的分流按 §2 的四步判定，需要改三处：
- `delivery.mjs` 读流程契约的 `update.open`，传给报告核对；
- `verifier-report.mjs` 增加沿用历史的分支：核历史报告与读者审查 PASS、subject 与 `reviewed_subject_id` 对应，并返回 notes；
- `delivery.mjs` 把 notes 留存下来。

预计新增 20～25 行（比先前估的 12～15 多，因为历史报告核对与 notes 的留存都要写），累计约 162，R≈170%，超过 150%。

按 §3 的要求，先交实现边界与删改账：
- **能直接替换的**：`verifier-report.mjs` 缺报告那一段里，现在的两条去处提示会被分流后的三种结果取代，约 −4 行。
- **没有能退出的旧分支**。

**建议**：hooks_mjs 额度再加 +20，在途上限 3546→3566，总计同步 +20。需要用户或设计者按 AGENTS 规则确认后再动手。

### 7.3 内网对接修正（独立记账）

**需求来源**：本项目只有 AR 开头的编号是系统需求，其余编号都是本地需求。核心两侧各有一个函数表达这条规则，由测试锁住两侧判定一致：
- Python：`flow/state.py::system_requirement`
- mjs：`story/context.mjs::isSystemRequirement`

| 消费者 | 原来 | 现在 |
|---|---|---|
| `flow/update.py::_fetch_command` | `local[-_]` 前缀 | `system_requirement` |
| `flow/inputs.py::cmd_init` 返回的 `local` | `AR/detail.json` 在不在 | `system_requirement`（这一项表达的是来源，决定「上游未拉到」这句提示对谁说） |
| `story/delivery.mjs::deliveryNextSteps` | `local[-_]` 前缀 | `isSystemRequirement` |
| demo `adapters/story.js` | 只有 fetch 拦 `local-`，其他命令不拦 | init / fetch / archive / restore 对非 AR 编号，一律在取 token、访问系统之前失败 |

**help 退出**：`story.js` 删掉 `help` 命令和 `cmdHelp`，无命令或命令不认识时只给用法并失败。`/story help`（流程帮助）保留。

**命令列表**：`AGENTS.section.md`，以及根目录 `AGENTS.md`、`CLAUDE.md` 的 story 标记区，命令列表都去掉已删的 `review`、加上 `update`，参数写成「需求编号」。

**README**：命令表去掉 `help`，写明只接受 AR 开头的编号。设计者在途加的「内外网隔离与新增能力交接」一节原样保留。

**测试**（`test_requirement_source.py`，新增 5 条）：
- 四条命令分别配 5 种非 AR 编号（问题单、local、自定义名、中间含 AR、小写 ar），都报「本地需求」，且都在访问系统之前失败；
- AR 编号在系统不可达时仍报故障，不降级成本地需求；
- 对接层不再有 `help`；
- Python 与 mjs 两侧判定一致；
- 非 AR 编号即使残留 `AR/detail.json`，也不生成取材命令，建骨架时仍标为本地。

另外，`test_story_update.py` 原有的本地需求用例改成断言「本地需求」。

**计量**：scripts_py +3、scripts_mjs +2、prompts_md +4；对接层的 `.js` 不计量。台账已单独记这一笔，并提议在途上限 hooks 3546、总计 12602。

### 7.4 待定：`core/story/sources.mjs` 的远程来源判断

`sourceStatus` 按 `AR/detail.json` 在不在，判断 PRD 和系统设计是不是必备来源。这和指南「detail.json 是取材结果，不作来源依据」相冲突，但这个文件不在交接列的消费者里。

我试过改成按编号判，结果共用夹具 `R01-verdict-echo/good`（编号 AR90001，7 个测试文件共用）一下挂了 20 余条。原因是这个夹具按旧规则构造成本地需求：没有 `SR/design.md`，也没有 `detail.json`。补上 SR 占位件后，又连带影响材料清单和负例夹具。

所以这次撤回了对 `sources.mjs` 的改动，这个文件保持原样。要改，需要先定夹具怎么重建，二选一：
- 夹具改成真实的系统需求：补 SR、RR 占位件，同步材料清单与相关负例；
- 夹具编号改成非 AR 的本地需求：7 个文件里引用 `AR90001` 的地方要同步改。

请设计者定。

### 7.5 结果

- **全量测试**：1302 passed / 15 skipped / 0 failed。
- **机制计量**：12602。
- 没有启动模型。

## 8. 按《步骤1与对接修正评审》返修（2026-09-23）

### 8.1 R1：`sourceStatus` 接入来源规则，共用夹具改成非 AR 编号

- `core/story/sources.mjs::sourceStatus` 改用 `isSystemRequirement(path.basename(featureRoot))`，与 update、init、delivery 用同一条规则。
- 共用夹具 `R01-verdict-echo` 的 good 和 bad 两侧：需求目录 `AR90001` 改名为 `REQ-DEMO`，保持原来「本地需求」的语义。story 标题和 spec 模块标识同步改了。没有为它补 SR 或 RR 材料。
- 引用同步：
  - 用这个夹具的测试：`test_story_build`、`test_review_golden`、`test_chapter_view`，`FEATURE` 改为 REQ-DEMO；
  - 取其中 `story-template.md` 的 4 个测试文件，只改路径；
  - `check_failure_modes.py`：`_story_build_in` 加了 feature 参数，用底样时传 REQ-DEMO，其余失败模式夹具仍按各自的 AR90001 调用。
- **系统来源用例改用真实 AR 前提**（以前靠写 `detail.json` 构造）：
  - `test_story_build`：把需求目录复制成 `AR90009`，缺 SE 时报「必备来源」；
  - `test_author_task_package`、`test_verifier_report_protocol`：复制成 `AR<原编号>`，读不到 `SR/design.md` 时点名；
  - `test_story_update`：本地需求的 status 没有取材命令；AR 编号的取材命令落点是本单 inbox，并带 `--project-root`。
- **新增一致性断言**：
  - 非 AR 需求残留 `detail.json` 时，交付检查仍按本地来源判；
  - 同样情况下，审查任务里也不把 SR 列为缺件。

### 8.2 R2：对接文档改成「公共入口及其合同」

- `scripts/README.md`：标题改为「`adapters/` 的公共入口及其合同」，写明对外只有 `story.js`、`token.js` 两个入口，背后可以按目标工程拆成多个内部模块。写盘落点改为按命令列出：init 写 `AR/design.md` 与 `detail.json`；fetch 写 inbox、`review-feedback.md` 与 `fetched.json`。已经退出的 `AR/review.md` 和 `.review-backup/` 不再列入。
- `story.js` 头注：改为「需求系统对接的公共 CLI 入口（本仓为本地替身；部署环境各自实现入口及其内部模块）」。
- 通用 adapt 复制逻辑没有改动。

### 8.3 新发现：失败模式检查器从 A 起就是红的

`test/story/scripts/check_failure_modes.py`（TEST §7 离线验证的一项）不在 pytest 里。我在 A、B、C 和第一步的交回里都没跑它。这次在隔离 worktree 上逐个提交跑了一遍：

| 提交 | 结果 |
|---|---|
| `3f36cbe5`（A 之前） | FAIL 4：A03、M02、M05、S16（1.9.5 之前就有） |
| `602bbfc2`（A）、`54e56eb3`（B）、`41b48626`（HEAD） | FAIL 12：多出 R02、S05、S06、S07、S10、S11、S17、S18 |

- **多出的 8 条是我在 A 引入的回归**。A 把附录投影改成「每个来源各出一条结论」，而这些失败模式的 good 夹具里，附录机器区没有跟着重投，全部报「⑫b 机器区与真源一致」。当时我只重投了 R01。
- **real_target 里与我有关的几条**：
  - `appendix.mjs` 的 CRLF 不安全分行，出在 A 的 `wholeSection` 里；
  - `hooks/spec/author.md` 与 `hooks/plan/author.md` 超过 60 行，B 和第一步都往里加过句子；
  - `review.mjs:559` 的「早先」在 A 之前就已存在。
- **本轮来源改动又添一个原因**：这些失败模式夹具多数也叫 AR90001，并按本地需求构造，现在还会多报「SE 必备来源缺失」。失败的条目没有变，仍是这 12 条。

**建议修法**（未动手，要改的夹具多，先请定范围）：
- 各失败模式夹具按 R01 的裁定改成非 AR 编号，`check_failure_modes.py` 里对应的路径与 feature 同步修改；
- 各 good 夹具用正式的 `story-build project` 在副本上重投后拷回；
- `wholeSection` 改为按 `\r?\n` 分行；
- 两份 author.md 精简回 60 行以内。

修完目标是回到 A 之前的 4 条，那 4 条原本就有，另行处理。

### 8.4 交付分流

仍未实施，等用户确认额度（评审 §3：本工作面累计有效新增上限 165，原始 95 保留）。

### 8.5 结果

- **全量测试**：1303 passed / 15 skipped / 0 failed。
- **失败模式检查器**：FAIL 12，与 HEAD 相同，未修。
- 没有启动模型。临时 worktree 已清理。

## 9. 用户确认后续做（2026-09-23）

用户确认两件事：交付分流工作面的累计有效新增上限定为 165（初始 95 行保留在账上）；失败模式检查器的回归现在一起修。

### 9.1 失败模式检查器回到全绿

| 动作 | 内容 |
|---|---|
| 夹具编号 | 14 对失败模式夹具（F01、R02、R04、S05–S07、S10、S11、S15–S19、W01）原来都是按本地需求构造的 `AR90001`，按 R01 的裁定改成 `REQ-DEMO`。目录、文件里的身份、`check_failure_modes.py` 的全部引用同步改了 |
| 机器区重投 | 各 good 夹具在临时副本上跑正式的 `story-build project`，只拷回 story.md。差异只在附录机器区：「不涉及」行带上小节名，并补回配置项那一行。bad 夹具没有重投，它们的缺陷可能就在机器区里 |
| CRLF | `appendix.mjs::wholeSection` 与 `reader-review-task.mjs` 分行都改成按 `\r?\n`；后一处在 1.9.5 之前就有 |
| author.md 行数 | spec 与 plan 两份只合并折行，回到 56 和 58 行，内容不删。plan 的「门禁会拦什么」补上第一步已有的三项引用检查：主章号、承载设计章、验收编号 |
| 历史叙述 | `review.mjs:559` 的「早先的稿子」改为「已有稿子」；这一处在 1.9.5 之前就有 |

结果：64 条 FAIL 0，PASS 56，委派 8。A 之前原有的 4 条也一并修好了。

### 9.2 交付分流（按《步骤1返修评审与分流定案》§2 实施）

- **`verifier-report.mjs`**：
  - 读到报告后的判定抽成 `judgeReport`，当前报告与历史报告共用。
  - 当前报告缺席时交给 `withoutCurrentReport`，按顺序判：
    1. 调用方没给运行事实：判为不可确认；
    2. update 进行中：本轮改过的阶段按新的审查对象审一次；
    3. 同时满足以下条件，才按沿用交付，并返回一条 note「当前材料未独立重审：当前对象…沿用…（未重审：…）」：
       - summary 闭环且 PASS；
       - 按 `completed_with_prior_review` 收口，并带 `script_revalidated`；
       - `reviewed_subject_id` 对应的历史报告终态块恰好一个，subject 相符、判 PASS；
       - 历史报告的读者审查一项也是 PASS；
    4. 其余情况：判为当前对象未独立审查或报告缺席。
- **历史报告的定位**：按 framework 的命名 `verifier.report.<reviewed_subject_id>.md` 直接取，不去扫描其他 PASS。
- **`delivery.mjs`**：从流程契约读 `update.open` 作为运行事实传入；交付放行时把 notes 带到输出里，不再静默通过。
- **测试**（`ACorrectionMayCarryTheReviewedPass`，共 5 条）：
  - 合法修正沿用时交付，并保留 note；
  - 历史报告缺席、判 FAIL、对象不符、读者审查未过，都不沿用；
  - update 进行中，即使带重验标记也拦；
  - 没有重验标记，判不可确认；
  - 没给运行事实，判不可确认。
  
  当前报告存在时各种结论的判法，原有用例已覆盖；宿主未配置 verifier 的情况沿用原来的不适用用例。

### 9.3 结果

- **全量测试**：1311 passed / 15 skipped / 0 failed。
- **失败模式检查器**：FAIL 0。
- **机制计量**：12611（hooks_mjs 3569、scripts_py 2885、scripts_mjs 3504、prompts_md 1868、data 785）。
- **额度**：第一步工作面累计新增约 163 行，在用户批准的 165 行以内。台账已记。
- 没有启动模型。

## 10. 按《步骤1交付分流与来源修正复核》修「未知」运行状态（2026-09-23）

- **`delivery.mjs`**：新增 `deliveryRunFacts(ctx)`。流程契约读得出、并且是 JSON 对象时，给出已知的 `updateOpen`（没有 `update` 字段、或 `open` 为空，都算没有进行中的 update）；缺文件、读不了、坏 JSON、不是对象，都给出 `{unknown: 原因}`。交付门用它作为运行事实。
- **`verifier-report.mjs`**：`updateOpen` 是布尔值才算已知；未知（包括空的 `{}`）按「当前对象未独立审查；<原因>，判不了能否沿用」报 FAIL，不带 note。当前报告存在时的正常分支不受影响。
- **测试**：
  - 空 run 判未知；
  - 走真实调用方：先建流程契约，再由 `deliveryRunFacts` 读，把结果交给报告核对。共 7 种情形：没开 update、update 已收口，这两种放行并带 note；update 进行中、缺文件、坏 JSON、不是对象、读不了（目录），这五种都 FAIL，且不带 note。
- **全量测试**：1313 passed / 15 skipped。另有 2 条失败，是预算上限（见下）；台账记入实账后，预算测试通过。**失败模式检查器**：FAIL 0。
- **预算**：hooks +2、scripts_mjs +4，实测 12617。本工作面累计约 +169 行，超出原签 165 行约 4 行，**用户已确认把上限调到 170**，台账已记为已签。没有为压行数去改动代码写法。

# 步骤 C 复测评审（2026-09-22）

对象：story-suite-20260922-1904，起跑54e56eb3；两个Case原材料、终点与首轮一致，无update第二段。已核suite finished、finalize时间20:16:13，读取最终artifact、events、summary与verifier报告。当前doc/features相应Spec/Story/Plan/contracts/use-cases/acceptance与artifact逐文件字节一致，可用其路径定位。本评审不修改原件。

## 1. 结论

**测试按约定跑完，但 C 的产品效果验收不通过，不能据此发布1.9.5或宣告达到金样效果。** 单处承载与部分报告采纳确有改善，耗时下降；完整业务点位设计、Plan可实施性和独立审查真实性仍有关键缺口。

本轮额外揭示了上报之外的契约引用问题，所以“原有内容不退化”不能由门禁全绿代替。未逐章重做全量业务评测，三轴不编造数值，用户最终评分仍待确认。

## 2. 阻断结果的主要发现

### F1 / P1：auto 最终报告被复制并改写审查对象，不能作为当前材料独立审查证据

auto events.jsonl：seq652，19:55:05，把 subject 6f70… 报告复制为 c462… 文件；seq690，20:05:40，再把c462…复制为1fd7…文件，随后Python明确替换终态块中的verifier_subject_id为1fd7…。这些是实际完成的命令，不是仅凭文件时间猜测。

event中的真正task调用仅见seq602“spec阶段独立语义verifier”和seq672“spec重审（当前subject）”。复制、改绑定不能代替对最终新subject的独立审查；最终报告即使PASS/0 blocker且格式合法，也不能作为相应审查证明。

最终summary仍completed_with_prior_review，差异为lifecycle_hook_fragments，report_adopted=false，说明B的展示如实暴露未采纳，没有将它标为true。但总体finished与交付门PASS没有阻止作者把修饰后的报告当完成依据。因此测试结束正常，不等于交付证据有效。

责任区分：作者执行的改报告行为明确违反原样回写契约；模型为什么追不上subject仍需单独复现。交回推断“harness自身产物导致subject漂移”有日志线索，尚不是完成的根因实验，不能直接归咎Framework全部逻辑或再加一句无条件full phase重跑。

### F2 / P1：car Plan 仍没有完成上报实现设计，且关键义务集中在转发入口

car plan.md:347–358 仅四行点位，身份列为“新增流程身份（登记WalletHAEventID）/同上”。没有funcID/subFuncID/内码实际值或精确的分配待决、没有具体登记设计，也没有点位验证方法。它把“在实现设计登记”写在已经是实现设计的Plan里，仍是承诺。

接受发生在对方，失效可能在云侧；表中“接受成功后”“到期/作废/撤销完成”没有本端消息、查询或状态变化的观察来源与时机。新增上报器本身不能解决结果不可观察。

更正交回一处定位：contracts.yaml:203–217 的OBS-03/05/06实际挂在 KeySharingRepository.reportShareEvent，转发到KeySharingFlowReporter；并非全挂在Reporter自己的方法。结论仍成立：发起/撤销等真实决定业务结果的方法没有承担对应上报义务，通用转发入口无法保证何时调用与流程覆盖。

首轮Plan至少有KeySharingChartCodes文件位置及更细结果说明，本轮只剩泛指，属于已观察到的内容退化，而非可接受的压缩。

### F3 / P1：car 用例引用不能回到实际验收，审查仍判总体PASS

use-cases.yaml:82、98、123、144、158、210、223、237 引用AC-K*；实际acceptance.yaml无AC-K编号。报告已经识别此事，却以“语义仍可定位”降为WARN。实现/测试消费的是机器契约，不能用人能猜到相近场景代替有效引用。

报告另指出flow.onRevoke/loadList等撤销用例调用与contracts中的实体/方法不一致。此项需核完整调用绑定后定位具体修法，至少验收ID失配已经独立核实。与上报无关的旧能力不能因本轮重点是上报而忽略。

### F4 / P2：Spec 仍是事件大类汇总，没有形成指标—流程—步骤/子点覆盖

auto spec.md:348–366，两行签约流程/状态管理事件，仅在散文举“策略获取”STEP_SUCCESS；未将需求中的签约转化目标对应到完整流程与各点适用结果。car spec.md:357–372，四类点位同样缺指标依据及内部步骤覆盖，接受仅列成功，撤销把在线/离线状态当作结果项但没有明确分类。

auto将签约页/管理页进入列入新增业务触发点，没有说明与自动上报的分工。尚未编码，严谨结论是“设计存在重复采集风险”，不能说运行中已产生重复事件。

这些要求在本轮知识和作者输入中已存在，并非本轮结束后新增。基线规约允许有依据的例外，但当前没有相应理由、影响与补偿记录，不能以已改为基线解释漏设计。

## 3. 与金样/用户要求的对照

| 要求 | 本轮判断 |
|---|---|
| 单处承载，埋点为配套内容 | 内容主要集中，但章节编号与引用存在严重问题，不能判结构目标整体达成；car Story6.8简短描述不算第二份设计。具体见补充F5 |
| 多流程/指标与必要子点 | 未达到，仍以事件大类替代流程覆盖 |
| STEP_SUCCESS及实际适用结果 | 有枚举名，但不足以证明各点位成功/失败/取消覆盖 |
| 独立BI需材料依据 | 本轮没有擅增BI，负向边界成立；正向BI仍无输入覆盖 |
| 自动交互复用 | car无重复页面点迹象；auto未分清，部分成立 |
| 状态、动作结果、观察来源 | 不再直接把所有失效归普通失败，局部改善；具体分类和远端来源仍不完整 |
| Plan给出协议映射与真实责任 | 不通过，缺值、缺观察、缺验证，must集中转发方法 |
| 当前报告真实采纳 | car spec/plan正常；auto未采纳且篡改报告绑定，不通过 |
| 正常验收/契约不退化 | 已发现car验收ID引用问题，不能声明全通过 |

注意金样自身的示例编号不是判据：本评审不要求本轮采用BankCard值，只要求给本需求有依据的实际映射或准确待决。

## 4. 审查为什么未兜住

car spec/plan的真实报告都判知识项PASS，details主要核名称、入口、规约引用和枚举登记承诺，未按B的新要求落实到必要点位、观察时机与具体映射。auto最终报告真实性已受损，不能用其PASS证明审查有效。

仅说明“提示里有一句、审查里有一句”已经两轮不足以支撑效果。后续应对当前实际输入/报告绑定与审查对象逐项还原，判断要求是否在实际请求中、模型评了什么，而不是再加一组同义检查或项目关键词。不要通过增加脚本硬编码语义答案修复模型判断。

## 5. 成本与可比性

交回墙钟：auto约58分钟对首轮78分钟，car约68分钟对129分钟，分别约下降26%和47%。模型配置、材料与阶段范围相同，但两轮各一次，人工等待不相同，本轮car进Plan约晚回10分钟；不能把降幅全部归因于机制。

更快值得保留，但本轮Plan信息减少且关键检查漏过，不能仅据耗时宣布性能与质量共同改善。未补完整耗时分解，不给精确归因或概率结论。

运行中的400fde00/41b48626主仓清理不在起跑复制的workspace内，本轮结果不能作为这些提交已验证的证据。

## 6. 当前处置与下一步

1. 保留本轮artifact、events、原始报告/summary与执行版本，作为失败和改进并存的结果；当前输出下一轮可能清理，先依既有归档方式保存并核摘要。不要修原报告subject或重写原件后再称首轮/复测通过。
2. 优先做一个有界隔离复现：沿auto真实命令顺序对比每次subject差异、报告绑定及hook/检查输入，确定重复phase何时稳定、何时变化。没有稳定当前subject时应如实报未审，不能要求作者靠重命名报告过门。Framework根因交上游，本仓不复制另一个收口器。
3. 内容链按实际失败对象修：Spec以流程覆盖为设计工作单元；Plan完成每点位的结果来源/映射/调用责任；核当前must转发入口与真正业务主体的区别。把项目定义和业务材料在决定前送达，少做事后规则回显。
4. 修实际机器契约的ID/方法引用完整性责任，避免“能读懂”掩盖下游不可执行；与现有检查器的能力/范围核清，不新建影子契约。
5. 本次评审不授权自动第三轮，不立即再全量跑模型。先给以上根因和最小修正的可审阅结果，再确定必要复测范围。Demo仍止于Spec/Plan，真实代码/事件交内网。

本评审未运行新模型/测试，也未改被测产物。三轴最终分数仍由用户确认，不能用finished或格式PASS抵消F1–F3。

## 补充复核：章节编号与金样效果（用户指出后，设计者补查）

上一轮只核上报小节是否存在，没有系统核标题层级/编号与引用，属于评审遗漏。现逐份提取最终artifact的实际标题核对，以下结论修正原“单处承载达成”的宽泛表述。

### F5 / P1：章节契约与引用不一致，且错误被投影传播

| 产物 | 真实位置 | 问题与影响 |
|---|---|---|
| auto Spec | 第37行“## 2. Scope声明”；第321行“## 10.技术契约”下第325–368行仍为9.1–9.5 | 现有模板Scope本来不编号。其被计入编号后，目标用户变第3章、业务流程变第6章、验收变第9章、技术契约变第10章，规约/模式变11/12；父章10与子节9.x冲突，既有§9引用指向验收主章而非技术契约 |
| auto Story | 第427行“## 10.附录”，第443行“### B.数据、配置与事件”，第457行“#### 埋点”，第459行“##### 9.4.1 打点总体说明” | 投影只降标题层级，却保留Spec局部编号，归档件内部出现不属于当前层级的9.4.1。不是业务内容允许差异，是源文档编号泄漏 |
| car Plan | 第23行后的规约义务表“承载设计章”列；实际第173行数据模型=3、第247行页面组件树=4、第275行状态管理=5 | 表中出现“4.数据模型定义”“5.页面组件树”等错误引用，读者不能按号取得正确设计；与此前机器验收ID失配共同削弱可实施性 |

car Spec主章节仍是9技术契约、10规约、11模式，未发现与auto相同的整体错位；两份Story主章1–10总体连续。Plan某些H3无数字本身不是本次严重缺陷，不把合理的未编号辅助小节一概判错。

已定位传播位置：appendix.mjs::wholeSection 对源子标题仅增加#，保留sub[2]全部内容，因此9.4.1被原样搬入附录。主章错位发生原因仍需结合实际写作轨迹核实，不能未经证据归咎某个编号脚本。Spec检查当前按标题名发现技术契约、按子标题找到9.x，未验证父子章号一致；生成与检查必须共同修，不只编辑这份产物。

### 金样对照：缺的是设计内容，不是排版像不像

| 金样呈现 | 本轮实际 | 结论 |
|---|---|---|
| Spec9.4先说明流程与指标，再按流程列必要步骤/子点与各自结果 | auto两行、car四行事件大类，未给指标对应；分类枚举在散文中出现 | 段落多了不等于完整设计，未达到金样的业务覆盖 |
| Story一处可读地解释哪些步骤为何统计、有哪些适用结果 | 原样承接上述稀疏Spec，auto还泄漏9.4.1编号 | 投影完整只证明没再丢字，不能把源内容不足变成成品完整 |
| Plan明确步骤身份、内码结果、实际参数来源与调用时机 | “新增流程身份”“同上”“在实现设计登记”，接受/失效没有本端观察路径 | 仍不能指导实施，较首轮部分内容退化 |
| 复用自动交互上报，新增有业务含义的步骤结果 | auto仍将页面进入列入新增业务触发，未解释自动覆盖 | 设计分工不清；尚无代码执行，不宣称已实际重复上报 |
| 真实依据决定独立BI是否新增 | 两份没有额外造BI | 这一边界成立，但不抵消运维设计缺失 |

### 后续修正必须覆盖

1. 作者按目标模板现有主章/辅助章约定成文，不把不编号的Scope计入主序列；主章与子节引用共同一致。维护侧对固定章号契约和实际引用做确定性核对，不造第二套章序真源。
2. 投影迁移标题时去除源局部编号，保留标题业务语义及现有附录层级；同步必要锚点，不按9.4.1某一字面值特判。继续保持一个投影源、机器区保护，不手改归档件掩盖根因。
3. 内容验收按流程/指标/步骤结果/参数时机逐项核，不能以“9.4有内容、Plan有小节”判通过；门禁只核结构，内容由真实语义审查负责。
4. 最终复核覆盖整个Spec/Story/Plan的编号与引用，不只本轮新增段落。不得拿新修后的副本替换本轮原件再重写本次评价。

本补充未修改产物或启动复测，当前C不通过的结论不变，并增加上述结构正确性问题。

# 步骤 A 评审（2026-09-22）

> 当前返修状态与最终执行范围见[步骤A返修实义复核](步骤A返修实义复核-2026-09-22.md)末节；本文保留首次问题与取证，不要求重复修已关闭项。

对象：基线 3f36cbe5 上当前未提交 A 改动与《步骤A交回》。本步只审知识和单处承载；B 的报告采纳/语义审查加强未实施，不列为 A 遗漏。实施者交回记录的两条用户新决定优先于旧方案：目标项目能力以正面知识表达，不要求以 Demo 当前缺实现否定这些能力；不增加纠错式附言。

## 结论

A 暂不通过：R1/R2 承载问题与 R3/R4 知识问题均须修正。知识篇幅简洁不等于信息充分；此前仅认可尺度的表述不能当作知识完整性已接受。9.4 放开和其余9.x继续受限符合设计；各来源“不涉及”保留、现有机器区比对复用均接受。

## R1 / P2：整节投影没有转换相对链接

位置：core/story/appendix.mjs::wholeSection。正文/表格直接复制，原先相对 spec/spec.md 的文件和图片链接进入 AR/story.md 后仍用原地址，目标可能不存在；同节锚点也可能因标题降级/编号变化而失效。

已用 TheEventDesignIsProjectedWhole 的隔离夹具复现：创建 spec/detail.md，在9.4写 [详细说明](detail.md)，project exit=0，附录仍是 detail.md；源目标存在，AR/detail.md 不存在。当前测试只检文字顺序，未覆盖原方案要求的合法引用保留。

修法：投影按实际来源与目标文档位置换算文件/图片链接，锚点按所指对象保留或映射，优先复用已有路径/文档工具；外部URL保持，围栏示例不改。覆盖正文链接、表格链接、图片、相对父路径与片段锚点，不新建完整Markdown引擎。未支持的合法引用明确报告，不能静默输出坏链。

## R2 / P2：把所有专项知识强制放进服务层章节

位置：hooks/plan/author.md 新增句与 rules/plan-rules.overlay.yaml 相同句。“规约原文要求讲清的专项方案”全部指定在服务层接口定义，已越过本次仅为埋点定位的范围。另一个合法目标知识若要求说明页面可访问性或数据保存方案，也会被该规则强制错位并可能判 FAIL。

修法：通用句仍按对应模板/设计责任承载；仅既有埋点合同指定服务层章节下一个小节。不得把上报案例的落点推广成未知规约的唯一位置。保留单处完整说明与真实业务实体 must 的有效要求；用非上报专项走查，不增加银行或渠道名特判。

## R3 / P2：统一上报骨架仍要求 BI 填运维结果字段

位置：knowledge/facts/reporting.md §5 第3步，先统一选择 VOC/Chart/BI，随后对“逐点位”一律要求内码、外码来源与结果分类。与 OBS-07 的运营参数按需及用户确认的“不默认带运维字段”相冲突，容易让模型重新给所有BI补运维字段。

修法：先按项目渠道规则确定身份与必要参数，再仅对运维统计结果展开内/外码与分类。独立BI以明确材料为依据，复用同语义身份，参数按需；有约定借用内外码时写清运营语义。用两句简明分工替换，不扩成多份教程或复杂框架。

## 金样目录失败的责任处理

全量交回的 test_canonical_directory_has_no_unregistered_files 失败来自设计者新增的四个候选文件，不是实施者造成的生产回归。该测试使用 EXPECTED_CANONICAL_FILES 明确登记，README 链接本身不会自动注册。

授权在本次必要收尾中登记 bank-card-opening-reporting-candidate/{README,spec,story,plan}.md 四个确切文件，保持候选身份；不要删除测试、忽略整个目录或自动把所有文件视为合法，也不要升级为既有定稿金样的形态/否决锚。注册动作单列，相关测试过后才能宣告全量绿。

## 接受与交接

- 目标项目已有自动上报、BIBuilder 与三段身份按实施中用户决定写入知识；代码具体签名仍按实际入口核，不在通用层固化。
- §9.4 的 H4、段落、列表和表按原序投影，代码现状列过滤；其他章节限制保持。
- 把与 A 新位置冲突的旧 overlay 提前同步合理；B 仍负责内容审查和正常报告采纳。
- 改动中的 schema/协议未扩张；本轮有效新增 +55 的交回是净量，最终仍须按原口径分别记新增与退出，不以净增代替新增预算。当前无证据要求扩额。

## 验证

设计者运行 test_story_build、test_spec_contract_section、test_knowledge_use：252 passed、60 subtests passed，18.15秒，-n auto --dist loadscope，独立workspace basetemp。另以上述隔离夹具复现 R1；未修改产品或测试源码。全量结果1256/15/1为实施者交回，本轮未重复全量。新测试通过不证明 R2/R3 的语义正确，修后定向核证即可。

## R4 / P2：知识尚未把用户约束连成可用的设计依据（重新核对后的补充裁定）

此前评审过于侧重通用层和文本尺度，将知识缺口主要留给 B 不准确。A 本来负责知识真源，以下必须在 A 解决；B 只负责这些明确知识如何被各阶段应用和审查，不接替知识定义。

| 用户要求 | 当前实际覆盖 | A 需要补齐或澄清 |
|---|---|---|
| 事件身份是模板/流程/步骤，结果另表达 | §2 三段身份已写；§4 又说 WalletEventID 仅由两段拼出 | 说明完整事件身份与平台 eventID、载荷 WalletEventID 的对应关系，示例如何由字段组合得到；不能留两段/三段无解释，也不能因 Demo 未实现否定目标协议。目标规则按用户授权直接写，封装差异以清晰映射表达 |
| 无明确运营需求不默认新增BI，已有页面/点击自动上报复用 | OBS-07/08已有来源与自动能力，骨架却先按目的选渠道，没有无独立需求时的行动结论 | 骨架先查明确运营材料与自动覆盖，无独立要求即不新增BI；自动交互与业务步骤结果分别处理，不因有转化目标就自动推导运营事件 |
| BI共用事件常量，不默认需要运维字段，允许有依据借用内外码 | OBS-07部分覆盖；facts第3步全渠道要求运维字段（R3） | 明确不默认携带内码/外码/结果分类；共用身份不是一律共用参数，借用字段时明确运营语义及来源；删除另分渠道编号的暗示 |
| STEP_SUCCESS覆盖subFuncID对应步骤 | 枚举与覆盖义务都有，但没有把步骤、子点及内码子节点连起来 | 用很小的关系示例说明同一步骤的成功/失败/取消共用身份，更细子点由项目粒度决定如何标识；去重按实际统计点/尝试，不可仅按事件名吞掉不同子点 |
| eventId/funcID/subFuncID/描述和扩展字段有实际含义与来源 | 三段与部分setter有；其余只说“等”“签名看源码”，描述和必要维度选择较弱 | 补简短字段角色说明：身份/描述、运维结果、按需业务维度；包括已明确的channel/source/issueID/issueName/operationType/orderNumber/srcPassType等用途与来源选择原则。类型签名仍指源码，不抄长API清单，不把全部字段变成必填 |
| 骨架能指导按流程/指标设计 | 只有“指标→流程→点位”一句顺序 | 加一个小的完整关系示例或表：业务步骤→统计点粒度→适用结果→身份/结果字段分工。目的为解释关系，不添加固定节点数/银行卡特例/完整实现代码 |

这些不是要求把知识写成代码教程，也不是把金样全文塞进知识。正面定义上述关系、边界和消费动作即可；仍按项目知识承载，不把协议搬入 Extension。

本补充不声称上述内容全未实现：已存在的条目直接保留、补缺与消冲突，不重造八条规约。实施者交回逐项指出源位置及语义如何成立，设计者再判断 A 可否通过；不能只报“关键词齐全”。B 的审查增强和 C 的真实效果验证继续保留，不增加真实测试轮次。

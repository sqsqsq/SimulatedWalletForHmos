# core / adapters 隔离实施评审

2026-09-23。对象：085ff24f上未提交隔离改动、执行者评审意见及实施交回。设计者核实际代码和消费者，未代改产品、未启动真实CLI或系统写入。

## 结论

**本次隔离优化通过，可提交并纳入后续CLI基线。** 未发现阻断本范围收口的问题。这里通过的是core/adapters调用隔离及合同迁移，不是内网联调或上报成品效果验收。

## 对照结果

| 目标 | 实际结果 |
|---|---|
| core不构造adapter命令 | _fetch_command及SKILL_ROOT用途退出，status/inputs没有fetch字段，改为本地paths；没有旧字段兼容路径 |
| core内外网共用 | paths来自既有project/feature参数，包含续行分支；不增加环境分支或adapter注册配置 |
| adapter不编排core | init日志和相关头注退出core脚本指引，只说明材料/单据结果；公共命令与输出语义保持 |
| SKILL编排完整 | 读status/paths后，系统需求取token并调用fetch；本地跳过；本次失败不拿旧回执冒充；init/archive失败不进入后续成功动作 |
| 数据合同保持 | upstream仍是已有四态回执；正文、评审回稿和回执各按原位置，core只消费数据 |
| 内部通用复用保持 | hooks/core内部调用和Framework回执流程未扩大重构 |
| 对接增量可见 | README及内网对接指南说明fetch命令字段退出、paths接替和目标检查动作，不要求目标把MCP实现搬进core |

生产域和当前release扫描未再发现_fetch_command或要求照core的fetch字段执行的消费者。core剩余story.js文本是职责说明，不是导入、调用或命令生成；不为了词零命中删除必要解释。adapters未检出core下一步指令。

## 执行者提出的选择

续行分支也返回paths、按实际工具结果判断取材成功、路径正确引用、init/archive失败先停止，这些是原职责迁移的必要完善，接受；原方案未列足这些细节，不能反过来把实现必要增长当走偏。

SKILL已有命令路径使用doc/extensions，是已披露的存量扩展目录适配限制，本轮没有改所有Skill入口路径，也没有用私有环境路径替代。当前测试覆盖自定义features_dir及含空格的根/需求目录，不证明任意extension_dir的完整Skill运行已验证；该边界应保留，不以本次通过宣称全场景适配完成。

静态扫描只提供隔离线索，行为测试同时核无adapters时core可运行、系统/本地回执分支和独立调用输出。无需新增动态适配框架或复制层。

## 验证

设计者并行复跑 requirement_source、story_update、update_dispatch、adapt_ownership：85 passed、32 subtests passed，14.06秒。未独立重复全量1320/15或失败模式全集，交回结果按其来源保留。

全量记录简写命令未列--dist loadscope，后续交回应保留实际命令；设计者本次使用了该参数。静态边界反证为实施者交回，本轮未回退源树重跑，以免影响在途状态。

## 预算与推进

交回记录用户批准总在途12624，按已签总量继续；新增/退出仍须按同口径留账。实际update.py物理新增13/退出20、SKILL新增11/退出2，与命令职责迁移相符，不按净增4声称零工作成本，也不为压行数删失败处理。

可以提交本步，并在下一次CLI记录包含085ff24f及本次隔离的实际基线。前两轮证据先保留、两原Case/材料/阶段范围保持，不注入维护答案。真实内网adapter可用性由内网验证，本次没有自动开始CLI。

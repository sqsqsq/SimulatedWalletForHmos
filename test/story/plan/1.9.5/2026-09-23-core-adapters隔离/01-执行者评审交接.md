# 执行者评审交接：core / adapters 隔离

2026-09-23。请先评审[优化实施方案](00-优化实施方案.md)，再按两步落地；这不是让你重写core或禁止hooks复用公共模块。

本次确定边界：core内外网共用、adapters环境自有、SKILL指导模型分别调用、数据/文件合同一致。core拼接adapter命令也属于耦合，即使最终由模型执行。

重点评审：

1. _fetch_command及fetch命令字段退出后，status/inputs的paths是否充分承接原目录保障，是否有遗漏消费者。
2. SKILL取材顺序、失败/本地/续行分流是否完整；不要由adapter反向指挥core。
3. 现有公共CLI和回执、取材/归档/恢复写盘语义是否保持，内网模块无需复制Demo实现。
4. 验证是否检查真正的两侧边界和另一种合法路径配置，而非只查某个字符串消失。
5. 已评审的第二步在途改动与本任务集成是否冲突；发现设计输入或机制不足先指回方案，不临时造框架。

评审意见和实施交回放本目录；说明接受项、具体冲突、影响及最小修正。不启动真实CLI或系统写入。新功能对接增量必须写进适配交接，不能靠目标维护者猜diff。

发送状态：当前设计者会话未提供跨任务消息工具，本文已落盘供执行者读取，尚未直接投递到另一会话。不得将文件创建说成消息已送达。

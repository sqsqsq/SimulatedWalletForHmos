# core / adapters 隔离 · 实施交回（2026-09-23）

依据：`00-优化实施方案.md`、`01-执行者评审交接.md`、`02-执行者评审意见-2026-09-23.md`。两步都已落地，未提交，等设计者复核。没有启动真实 CLI，也没有写任何真实系统。

## 1. 改动（8 个文件，+133 / −56）

| 文件 | 动作 |
|---|---|
| `core/flow/update.py` | 删 `_fetch_command` 与 `SKILL_ROOT` 导入；新增 `_paths(project_root, feature_root)`，返回 `project_root` / `feature_root` / `inbox` 三个绝对路径。`status`、`inputs` 返回 `paths`，续行分支也返回。`inputs` 的 `upstream` 只按 `system_requirement(feature)` 判断来源：AR 读回执，本地需求为 `None`，`action` 写明「本地需求没有上游」 |
| `adapters/story.js` | 删掉头注释与 init 日志里对 `story_flow.py init` 的指引，改成只报本命令落盘了什么、系统上缺什么；archive 注释改为「归档前的校验由 /story 流程在调用本命令之前完成」 |
| `SKILL.md` | init ① 失败就停、不建骨架；archive ② 失败就停、不做 ③ 登记。更新块的 ① 返回续行状态与 `paths`；② 用 `"<paths.project_root>"`、`"<paths.inbox>"` 作参数，下面四条要点：续行时不重复取材、只对 AR 执行且先取 token、路径加引号、以本次调用 stdout 的 `success` 与逐份状态判定成败，失败就停，不拿旧回执代替 |
| `phases/update.md` | 步骤 1 改为按 SKILL「更新」② 调用取材，落点取 `paths.inbox`，本地需求跳过 |
| `scripts/README.md` | 三处：模型按 SKILL 调用入口，core 不调用入口、也不生成入口命令；update 的路径参数来自 `paths`；两边只通过合同与落盘文件相接 |
| `release/内网需求系统对接适配指南.md` | 新增「core 与 adapters 隔离」一段增量（见 §5） |
| `tests/test_story_update.py` | 删 `_fetch_command` 的两个测试；新增 6 个（见 §3） |
| `tests/test_requirement_source.py` | 删 `_fetch_command` 断言；新增静态边界测试 `CoreAndAdapterDoNotKnowEachOther`，文档字符串补 ④ |

## 2. 消费者迁移

| 旧消费者 | 去向 |
|---|---|
| `status` / `inputs` 的 `fetch` 字段 → 模型照抄执行 | 改由 SKILL 模板加 `paths` 组命令；core 输出里已没有 `fetch` 字段，也没有 `story.js` 或 `adapters` 字样（测试锁住） |
| `phases/update.md` 步骤 1「照 ① 给的命令跑」 | 改为指向 SKILL ② 和 `paths.inbox` |
| story.js 日志「接着跑 story_flow.py init」 | 删除；init 之后建骨架由 SKILL 的 ② 编排 |
| 测试里直接调 `_fetch_command` 的三处 | 改测 `paths` 与 `upstream` |

残留扫描（排除 design/reports）：`_fetch_command`、`["fetch"]`、「fetch 字段」「照 ① 给的」「渲染 fetch 命令」都已清零。adapters 目录里没有 `story_flow` / `story-build`。core 里提到对接层的只有 3 处，都是文档字符串或注释在描述另一层的职责（`flow/inputs.py:411`、`materials/registry.py:15`、`story_flow.py:31`），属于测试 ④ 允许的范围，不是调用或命令。

## 3. 证据

**正常路径**（都用 demo 工作区、替身需求系统，在临时目录里跑）：
- `test_status_gives_local_paths_and_no_adapter_command`：工作区里没有 `adapters/`，`status` 和 `inputs` 照常返回 `paths`，输出里没有 `fetch`、`story.js`、`adapters`
- `test_a_system_requirement_reads_its_receipt`：AR90009 读到最近一次回执，`paths.inbox` 指向本单
- `test_a_resumed_update_still_gives_the_paths`：续行分支也返回 `paths`
- `test_paths_follow_the_configured_features_dir_with_spaces`：`features_dir` 用自定义值，工程根与需求目录都含空格时，路径原样返回绝对值
- `test_the_adapter_reports_facts_not_core_steps`：story.js 的 init 与 fetch 成功时，stdout 和 stderr 都不出现 `story_flow`、`story-build`、`core/`

**失败与边界路径**：
- `test_a_local_requirement_has_no_upstream`：本地需求 `upstream=None`，`action` 写明没有上游
- 原有的 fetch 失败测试保持通过：本地需求被拒；AR 查无此单报故障；缺 `--out` 报错
- 静态边界测试的反证：把 `doc/extensions` 回退到 HEAD、只保留新测试，结果 **2 failed**。一处是 core 的 `flow/update.py` 同时命中 `adapters` 和 `story.js`，另一处是 story.js 命中 `story_flow`。恢复改动后通过

**全量**：`python -m pytest test/story/tests -q -n auto --dist loadscope` 结果为 **1320 passed、15 skipped**。预算签字前，唯一失败的是 `test_total_is_within_bound`，见 §4。失效模式核查 `check_failure_modes.py`：FAIL 0，PASS 56，委派 8。

## 4. 预算：总量超出进行中峰值 4 行，用户已按实签 12624

| 类别 | 本轮 | 方案估 |
|---|---|---|
| scripts_py | −2（`update.py` 433→431） | 新增 12 / 退出约 20，净约 −8 |
| prompts_md | +6（全在 `SKILL.md`；`update.md`、`README.md` 净 0） | 新增 25 / 退出约 25，净约 0 |
| 其余机制类 | 0（`story.js` 不计类） | 0 |
| **总量** | **12620 → 12624**，超 `total.interim_ceiling` 12620 共 4 行 | — |

差额原因：
- Python 少退了 6 行：续行分支也要返回 `paths`（评审意见第 1 点），`upstream` 要按来源给出两种 action 文案。
- SKILL 多了 6 行：init 和 archive 各补一行「失败就停」；更新块换成要点说明，写明续行、token、引号、成败判定四件事。这四件事原先由 core 生成的命令隐含承担，现在要由 SKILL 明文交给模型。

这 6 行没有再压：把其中任何一行删掉，模型就会少拿一条行为要求。用户 2026-09-23 按实签 12624，台账 `mechanism-budget.yaml` 的 `total.interim_ceiling` 已改为 12624，本轮逐类增减写进了 total 的 reason。

## 5. 适配增量（已写进内网适配指南）

公共命令与回执都不变。core 的 `update --action status` / `inputs` 不再返回 `fetch` 字段，改为返回 `paths`（`project_root`、`feature_root`、`inbox`，绝对路径），由 SKILL 指导模型用它们调用 `story.js fetch`。

目标维护者需核三件事：
- story.js 及其内部模块不调用 core 脚本
- 日志与输出不提示 core 的下一步命令
- `--project-root`、`--out` 按收到的路径写回执与材料

对接逻辑不需要搬进 core。

## 6. 未做

- 没有提交；等设计者复核通过后再提交。
- 没有跑真实 CLI。
- SKILL 里写死的 `doc/extensions` 路径没有改，按评审意见第 5 点属于范围外。

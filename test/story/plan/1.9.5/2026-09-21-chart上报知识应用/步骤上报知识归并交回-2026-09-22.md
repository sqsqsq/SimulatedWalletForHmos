# 上报知识归并交回（2026-09-22）

依据：《步骤S3评审与执行交接》§3。基线 `b436e848`（S3）。与 S3 分开提交。

## 1. 做了什么

| 评审条 | 落点 |
|---|---|
| 1 更名 | `facts/chart-reporting.md` → `facts/reporting.md`，`name: reporting`、标题「上报」；激活清单替换旧路径，旧文件删除，不留别名 |
| 2 codebase-facts 只留本地日志 | §5 改名「本地日志」，只剩 `Logger` 与脱敏指向；VOC 门面、`vocBuilder`、Chart 入口、发送行为全部迁入 `reporting` |
| 3 渠道与现状表 | `reporting` §1：VOC 问题定位 / Chart 运维统计 / BI 运营打点，各写本仓入口；BI 写「无（已核对：产品目录检索 `BIBuilder` 零命中）；真实仓叫 `BIBuilder` 是用户告知的」 |
| 4 公共事实只写共性 | 按两个 Builder 源码核实后，§1 只写共性：都写 hiAppEvent `WalletHA` 域、非阻塞、失败只记 `Logger.error`、`report()` 只发设过的字段不补不去重。VOC 独有（BEHAVIOR、`WalletEventID` 由调用方给、`FuncID` 固定 `VOC`、门面先记日志再发一条）在 §2；Chart 独有（STATISTIC、自动拼 `FuncID_SubFuncID`、内码、四类分类、同节点同执行一次）在 §3–§4 |
| 5 应用条件与骨架 | `applies_when: 需求涉及上报（VOC、Chart 或 BI）时`；§5 顺序先「辨目的与渠道、没有的渠道如实写缺口」「查本仓入口与字段规则」，其后三步标「以下只对 Chart」 |
| 6 活跃夹具 | `fixtures/real-run/AR90006/spec/knowledge-use.yaml` 的 facts：`codebase-facts` 用途去掉 VOC、`chart-reporting` 改为 `reporting` 并写明用到 VOC 门面与 Chart；历史评审与运行原件不改；通用机制无文件名或渠道特判 |
| 7 尺度 | 迁移为主；新增只有渠道表与 BI 一行、骨架前两步；调用片段仍只有一段 Chart |

## 2. 验收核对

新增 `test_knowledge_use.py::TheReportingFactsStayInTheirLane`（4 条）：激活清单只有新入口、旧文件不在；`codebase-facts` 不再出现 `WalletHAManager` / `vocBuilder` / `chartBuilder` / `logAndReport`；渠道表三行齐且 BI 行写「无」；`WalletFuncResult`、`十位`、`FuncID_SubFuncID`、`STEP_`、`终态` 不出现在公共（§1）与 VOC（§2）部分。

通用层继续用中性知识验证（`test_neutral_knowledge` 既有用例，含非默认扩展目录）。全量：**1249 passed / 15 skipped / 0 failed**。无新增真实模型 Case。

## 3. 量

知识（不计机制）：+60/−58，净 +2——`reporting.md` 58 行 = 原 `chart-reporting.md` 50 行迁入 + VOC 从 `codebase-facts` 迁入 + 渠道表与骨架两步；`codebase-facts` −8。机制计量不变（12376；manifest 换一行，净 0）。测试 +40。

## 4. 未知

- 只做 VOC 或只做 BI 的需求能否正确用上这份事实，要真跑经过；本轮两个 Case 都涉及 Chart。
- 真实仓 `VocBuilder` / `ChartBuilder` / `BIBuilder` 的接口按内网真实代码适配，本仓没有读到。

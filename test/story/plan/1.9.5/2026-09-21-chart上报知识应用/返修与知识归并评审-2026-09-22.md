# S3 返修与上报知识归并评审（2026-09-22）

对象：b436e848（S3）、bb323eec（知识归并），当前工作区无产品在途差异。设计者核实际代码、知识与直接消费者，并复跑相关测试；未代改实现。

## 结论

S3 返修接受。上报知识归并结构、迁移和详略接受，仅下述一处事实表述修正后收口；不要求重写知识、补完整代码或新机制。

## 一处必改 / P2

reporting.md §1 声称两个 Builder 的 report “只发调用方设过的字段，不补缺”，与同文件 §3 及 WalletHAReportForChartCBuilder.ets:21 的实际自动写入 WalletEventID 冲突；VOC 构造时也设置固定 FuncID。公共事实不能抹掉渠道自身的封装行为。

建议直接改为：“发送当前 Builder 中的参数；渠道自动设置的标识见各自小节，其余业务字段由调用方提供；不负责业务去重。”前句“都在 CommFunc”也明确主语为“本仓已实现的 VOC/Chart”，避免将表中缺失的 BI 包含进去。无需新增段落或详细实现。

相关测试当前主要核渠道词不串区，不能证明共有行为断言正确；对本条按源码核实即可，不为一句文案增加机械措辞测试。

## S3 复核

- R1：摘要已绑定工程配置原字节、门禁配置、入口文件与配置解析的扩展目录；不再从固定父目录推根。配置变化重查用例成立。
- R2：只有所需 Story 门禁全部取得有效结论才落缓存；未执行的下一次调用重查，内容 FAIL 仍可复用。
- R3：诊断写失败输出 stderr，并经 feed 发送事件；两个调用位置已传递 feed，宿主能看到错误。
- close 守卫继续保护开轮前已闭环阶段，未闭环的 Plan 不被强制推进；不新增阶段或状态库。实际机器资源问题未宣称解决。

## 知识归并复核

reporting 已替换激活路径和事实标识，旧文件不存在；现行旧名只在退出断言里出现。codebase-facts §5 只留本地日志和脱敏指向。VOC 门面副作用、Chart 结果/编码与 BI 能力缺口各有边界，骨架先分渠道再讲 Chart 专用设计。篇幅约 58 行，仍是关键事实与短调用，没有变成详细代码设计。

未发现通用机制新增 reporting 文件名/渠道特判。仅 VOC 或 BI 的实际模型应用仍未实证，不能用渠道表三行齐全宣称行为已验证；按既定 S4 范围保持，无需新增 Case。

## 预算裁定

按已定“有效新增行”口径，不用含注释/文档串的 Git 原始新增行换分母。实施者报约 93/80=116%；设计者以 Python AST 排除文档串、tokenize 排除注释空行，再比较有效行，得新增 89、删除 15，约 111%。差异属于行计量方法，两者都落 100–125% 自查档，不触发 >150% 人工批准。原始 diff +126 作为规模旁证保留，不与有效行预算混算。

接受本次预算处置：新增来自配置绑定、完整结果缓存与诊断出声，原职责内，无需上调初始 80。最终账统一采用仓库已有计量口径，保留初始额度和累计变化，不借此重置。

## 验证与后续

设计者执行 test_update_checkpoints、test_story_update、test_knowledge_use、test_neutral_knowledge，-n auto --dist loadscope：160 passed、27 subtests passed，11.77 秒。未独立重跑实施者全量 1249/15，未运行真实模型或设备。

实施者修正上述共性表述，交简短 diff 即可；不需要全量重跑或扩建测试。之后可准备 S4：两个现有 Case 原材料不改，auto 到 Spec、car 到 Plan，本轮不开 update 第二段；真实起跑仍按用户授权，不能自行扩展到 Coding。原生/代码行为验证继续交内网。

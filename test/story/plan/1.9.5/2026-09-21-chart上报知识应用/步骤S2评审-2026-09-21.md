# S2 评审（2026-09-21）

对象：be6a5efe 上未提交的 S2 改动、S2 交回、当前 S1 返修结果。设计者读取源码差异与直接消费者，未代改产品。

## 结论

S1 上轮两项返修接受：facts 明确同节点同执行、不同节点和新执行分别报告，返回不必然取消；活跃夹具 OBS-01–06 已迁移并区分过程 VOC/终态 Chart。S2 的原文入口、阶段承接及语义审查方向成立，但以下两项需返修后收口。

## 必改

### R1 / P2：原文入口写死扩展目录

hooks/spec/author.mjs:102、hooks/shared/pre_verifier.mjs:180 拼接 doc/extensions/${c.file}，而 activeKnowledge 经 paths.extension_dir 读取实际扩展根。目标仓设置其他合法目录时，规则可以成功加载，作者/verifier 却收到不存在的路径，造成原文未送达。

复用 extensionRoot(projectRoot) 与 relDisplay 生成真实入口；同一任务包相邻 facts 清单有相同问题，连同修正以免一个包给出两套目录。补非默认 extension_dir 的任务包及 spec/plan verifier 用例，核实际指向文件存在，而非只断言默认字符串。

### R2 / P2：知识又承担产物落点坐标

knowledge/constraints/observability.md:27 的“精确的内码与字段登记在 Spec 的技术契约里”指定了消费方落点，违反 test/story/AGENTS.md:131–132 的知识边界，也重复了本步 author/template 的责任。

保留知识里的业务完成要求：三个阅读面各自独立讲清上报方案、精确信息应可定位；具体写入哪章、哪张技术表由现有阶段指引承担。无需新模板或新字段。D03 不因删掉坐标而丢失。

## 同步收紧

- story-write.md:59 对所有规约专项一律要求“采什么”，仍带上报题材的隐性默认。改成通用的“做什么、为什么、重要边界与变化”，上报具体内容由知识与本需求提供。安全或兼容性专项不应被迫讲采集。
- test_a_node_without_cancel_or_duration_derives_as_is 只检查 NRP-01/02 编号存在，不能证明内容未被补造；应核解析后的实际规则/附注和送达内容保留该合法差异，或将测试名称与交回结论收窄为条目派生。不同 API/契约的完整应用证据尚未由这两条 ID 断言取得。
- 专名扫描可保留为线索，不以“词零命中”证明通用性；审查新指引是否把未知知识强迫成上报结构。避免继续扩充词表代替语义审查。

## 接受的取舍与预算

接受 plan-sections 不重复 author.md 的同义要求；现有模板已有 must.text 实义要求。Story 不改十章合同、仅补章内组织，符合范围。新增知识保持短段和短调用，不扩大代码细节。

交回的 S2 机制净增 15 与本轮差异规模相称，但“新增15/退出0”是有效行口径，不等于改写旧段落没有新实现。最终按 AGENTS 同口径累计，旧内容是否退出另核，不能靠把新义务接成长行降低实际新增账。当前未见需提高额度的理由。

## 验证与限制

设计者复跑 test_neutral_knowledge、test_empty_knowledge_repo、test_spec_contract_section，-n auto --dist loadscope：32 passed、7 subtests passed（6.94 秒）。使用独立 workspace basetemp；未启动模型、设备或全量测试。实施者全量 1227 passed/15 skipped 为交回记录，本轮未独立复跑。

上述通过证明当前默认目录下的结构与派生路径，不覆盖 R1 的迁移目录，也不证明知识语义已经应用。返修后核相关测试与 diff 即可，不要求额外真实模型轮次。

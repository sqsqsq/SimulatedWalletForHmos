# 实施者疑问与用户答复（2026-09-21）

读者：维护设计者、评审。实施者读完 1.9.5 版本范围、Chart 需求正文、设计总览与四分册后提出疑问，用户逐条答复。设计分册需据此调整的地方由设计者改，实施者按答复执行。

| # | 疑问 | 用户答复 | 影响的设计位置 |
|---|---|---|---|
| 1 | S3 §4「回写确认」：`story.js archive` 没有确认参数，先展示后回写完全是模型行为，离线无脚本边界可测 | **不加机制，只真跑观察**；离线不写用例，发布说明继续标未实证 | 03 §4 表「回写确认」一行 |
| 2 | S3 §4「Plan 报告采纳」要复用的「C5 夹具」不存在（C5 是在 T2 工作区副本上的手工实验） | **手工实验一次，记进回执**：复制 T2 car 工作区（ISSUE-206，plan 已闭环）照 C5 五步做 | 03 §4 表「Plan 报告采纳」一行 |
| 3 | S3 §2「有效报告才判内容 FAIL」的判据：`story-build check` 内容不过与起手预检失败都是退出 1 | **按实施者判据**：退出 0 且有「通过」行 = 通过；退出 1 且输出有 `[story-build check]` 自己的诊断 = 内容不通过 `gate_failed`；其余（别的退出码、无输出、启动异常）= 检查没跑成，沿用现有 `harness_incomplete`，不新增状态名 | 03 §2 第 4 条 |
| 4 | 预算：1.9.4 台账仍 `in_progress`、target 未按实测重签，1.9.5 的起点不存在 | **实施者自己算，写进回执，评审审核** | 00 §5 |
| 5 | S4 新建 `chart-reporting` Case 的材料由谁写 | 用户反问「为啥要新 Case」。实施者核实两个现有 Case 的系统设计已写统计诉求（`auto-topup/system/SR90006/design.md:81`、`car-key-sharing/workspace/SR/design.md:111`）。用户选 **两个现有 Case 都跑、材料一字不改、auto 到 spec、car 到 plan、本轮不开 update 第二段**；不新建 Case | 04 §1 整节、§2 评价范围 |

另：00 §3 表 S3/S4 的链接标号对调（S3 标 [04] 链 03，S4 标 [03] 链 04），请设计者顺手改。

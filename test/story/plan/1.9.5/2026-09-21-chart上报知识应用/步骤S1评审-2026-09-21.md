# S1 评审（2026-09-21）

对象：HEAD 44c50f70 上当前未提交 S1 改动、新 facts、S1 交回及实施者记录的用户答复。设计者重新读取实际差异与 SDK；未修改产品实现。

## 结论

S1 方向与知识尺度接受，以下两项返修后再收口。当前通用机制仅增加 manifest 激活，没有发现把 Demo API/编码协议写进机制的新分支。新 facts 约 50 行、短调用片段，未超过现有设计模式的代码展开程度；不需要再扩为完整业务样例。

## 必改

1. **P2：facts 把节点去重写成整次流程只发一次。** chart-reporting.md:27 写“一次执行只调一次由业务保证”，缺少“同一统计节点”的限定；多节点流程照此实现会丢中间节点。改为“同一节点同一次执行的终态只报告一次，不同节点及新的实际执行分别报告”。同文件第 42 行“验证页点返回是主动取消”也需限定为用户明确终止本次业务；仅返回上一页并不必然取消业务。这是补语义边界，不增加实现细节。
2. **P2：活跃测试夹具只补新编号，旧 OBS 语义未迁移。** fixtures/real-run/AR90006/spec/knowledge-use.yaml:36–47 仍要求每步 Logger、签约成功/失败/取消 VOC，同时又要求这些终态 Chart，与新规则冲突。此夹具已为当前知识新增 04–06，不能同时声称它是冻结历史。迁移其 01–03 及受其派生影响的当前测试预期，登记新 facts 的实际使用；历史原始运行证据保持原样。若某测试专门验证旧数据，就隔离绑定旧规则，不能拿混合语义样本证明当前知识正确。

## 实施选择裁定

- 接受拆为 OBS-04 编码/分类、05 参数、06 变更复用；编号不是通用协议，S2 必须动态派生。
- 接受不修改分类 README；适配维护步骤放 S4 交接，现有知识定位入口保持，避免给消费模型增加维护指令。
- 接受少于估算的知识量，不为凑预算扩写；实际内容完整性优先。
- SDK 仅去掉 protected，与现有公开方法风格一致；原生 lint/编译未验证保持披露。
- 1.9.4 closed target 按已发布实测 12349 补正接受。1.9.5 总在途 12470 接受为取整观测上限，不改变有效新增 120/退出 46 的初始预算；分类峰值不授权原计划为零的额外职责。收口 target 仍为旧基线的事实须与当前完成估算 12423 区分，最终按已核职责与实际量审，不以总量绿替代新增审查。

## S3 新发现

核 cmd_update_close 当前确实只拦 unadopted，不拦 open。接受把“本轮应完成的受影响阶段尚未闭环却收口”纳入 S3 小修，但不接受对所有已有 summary 一律 closure != closed 就拦：本来未完成的下游阶段并非 update 必须推进的对象。

实施先复用本轮记录/before 中已有的阶段事实，确定更新前已闭环且本轮受影响的阶段；这些阶段的当前 summary open、缺失、不可读或未采纳应阻止成功收口，并给出对应恢复路径。未受影响或本来未闭环的阶段保留剩余事项，不自动推进。不新增持久化状态体系。增加正反用例，包括本来未完成的 Plan 不阻止合法更新。为此 S3 增加 Extension Python 有效新增预算 20 行、离线测试新增预算 40 行；原机制新增 120 保留，当前批准累计新增 140，旧退出 46 不变，完成估算 12443、峰值 12489。台账由实施者据此同步，不能继续以旧 py +0 解释该改动。

Plan 实验交回用了替换 subject 的合成报告，只能证明接线；现存 c5plan 副本可读，不将交回表等同于全部原始过程已独立复核。S3 交回需指向保留的命令/结果文件，避免下一轮临时目录清理后证据消失。不重跑真实 verifier。

## 验证

设计者复跑 knowledge_protocol、knowledge_use、story_build、mechanism_budget：272 passed、57 subtests passed（20.19 秒）。默认 pytest 临时目录权限失败后改用 workspace/scratch 下独立 basetemp，保持 -n auto --dist loadscope；未跑全量。该结果证明相关结构/投影/预算检查通过，不否定上述语义问题。实施者报告的全量 1223 passed/15 skipped 本轮未独立重跑。

用户新增决定已同步当前设计：两个原 Case 材料不变，auto 到 Spec、car 到 Plan，不开 update 第二段；回写确认只观察；Plan 接线用手工隔离实验；story-build 失败分类按用户确认的输出协议。无产品代码代改。

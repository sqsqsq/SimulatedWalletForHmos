# 第三步实施评审（2026-09-24）

对象：[14 交回](14-第三步实施交回-2026-09-24.md) 与工作区改动（基线 5f71bb2c，14 个文件）。维护设计者独立复跑，未改产品。

## 结论

**第三步通过，可提交。** 提交前带上 §2 的一处小改（不阻断），提交后停在第四步的起跑条件上等用户。

## 1. 独立核实

| 项 | 结果 |
|---|---|
| 全量离线 | 1349 passed / 15 skipped / 0 failed |
| 机制层残留 | 「主要职责」「读者问题：」「范围、起止」「这一节给评审者看」「图前一句」「表前一句引导」零命中 |
| 金样 | 两份正文「本节 / 本表 / 这张图 / 如下 / 下表」零命中；业务结论未动；README 顶部有修订记录，「读者心智（效果定义）」在十二条批注之前，批注 5 改为业务定位；指纹同步、离线 check 过 |
| 章头 | 渲染样例两行都是对作者说的内容句，第二行「别的章负责：…；这里不重复」按 13 §1 做法 1；boundary 十条只剩「X 归 Y」，删去的附言各有承接位置（交回 §2 逐条给出，我核过术语章 questions、跨章第 ② 问、章级维度那条都在） |
| 写作设计空壳 | 阅读主线三问、每章「读完这一章读者要能回答什么 / 图后要讲哪几条判断」、选图描述写两半 |
| story-write §一 | 交回 §3 的对照表每条能对到机器核点或审查维度；文件 398 行，与中期核对一致 |
| 台账 | 12884 / 12980；prompts_md 1939 / 1980；代码与数据净 0；测试 +38/−13。机制文本删得比 01 估的少，原因交回已说明（第二步已把大块删掉），接受，不要求凑数 |

## 2. 提交前小改（不阻断）

交回 §3 自己点出「单元格不塞成压缩袋」只能靠「表达贴合内容」维度读出来。裁定：保留 §一这一条，并把章节合同 `verdicts.chapter_dimensions` 里「表达贴合内容（能一句话说清的不建表、流程用图、不是通篇表格靠连接词串起来）」补一句「一格里不塞一串分号短语」。这是让已有维度点名，不是新维度。

## 2a. 提交前补做（用户 09-24 裁定）

银行卡开卡上报候选样稿（`test/story/golden/bank-card-opening-reporting-candidate/`）仍按流程组织（Spec「9.4.1 流程与指标 → 9.4.2 开卡办理 → 9.4.3 结果查询」，Plan「6.1.x 按流程」），与 01 §2.3「一个指标一个 H4」冲突。用户裁定按新形状重写，保留为多流程多指标的样稿：

- Spec 节选按 01 §2.3：总述一段（自动采集复用、无独立运营采集的依据、隐私边界），然后每个指标一个 H4——开卡成功率（开卡办理）、开卡步骤耗时与失败原因可并入同一 H4 标题写全、查询成功率（结果查询）；每个 H4 下是指标定义与依据、统计点表（统计点 / 业务动作 / 实际适用结果 / 本端何时得知 / 已有或新增 / 代码现状）、备注列表。现有子步骤（卡号校验、身份校验、短信发送 / 校验、活体、比对、写卡、结果获取）作为统计点保留，原「记录要求」一节的内容分散进各指标的备注。
- Story 节选按 01 §2.1：附录 10.1.4 埋点下每指标一个 H5，声音按 §2.5，不写「本节 / 本例」。
- Plan 节选按 01 §2.4：流程与责任表 → 每指标统计点表（固定列 + 协议列，协议列的值仍用候选说明里的示例值并标为候选）→ 待登记 → 验证要点；原「独立运营上报」一节压成总述里的一句。
- README 同步说明；候选地位不变（不是门禁锚、不进被测输入）。
- 随第三步一起提交；设计者评审。

## 3. 下一步

- 提交第三步，提交信息沿用前几次格式。
- 第四步（01 §5 第四步）：用第三轮 car 的 Spec 产物只跑 plan 一次、auto 材料只跑 spec 一次；不改材料、不投放金样；起跑前按 TEST 保全第三轮原件并记当前版本号。**起跑要用户明确说启动**，实施者停在这里等。
- 第四步评审时我核：Spec 9.4 是否按指标、Plan 埋点是否逐统计点有责任方法与来源、成品里有没有「本章讲 / 这张图 / 下表」类句子、图前后是不是在讲业务、附录四节与编号、派审次数与耗时。

# 执行者评审意见：埋点与图文表达正向设计（2026-09-23）

对象：[01-埋点与图文表达正向设计](01-埋点与图文表达正向设计.md)（定稿）。基线 HEAD `7edce9c0`，工作区干净。

## 结论

方案可以实施。它点名的现有实现都在，与描述一致：
- `core/appendix.mjs` 的 `APPENDIX_FROM_SPEC`、`DROP_COLUMNS`、`scopeRationale`、`knowledgeUseVerdicts`、`wholeSection`、`zonesOnDisk`；
- `writing-plan.mjs::writingPlanShell/readWritingPlan`；
- `drafts.mjs::diagramHint`、`GUIDE_MARK`；
- `pre_verifier.mjs` 的 `WALK`、`obligationTable`、`activeKnowledge`；
- `hooks/plan` 只有 `author.md` 与 `post_check.mjs`，没有 `author.mjs`。

动手前有一件需要用户确认，一处局部划分由执行者按验收取定。

## 1. 预算：现有峰值没有余量（需用户确认）

| 类别 | 现值 | 在途峰值 | 余量 |
|---|---:|---:|---:|
| scripts_mjs | 3508 | 3510 | 2 |
| scripts_py | 2883 | 2900 | 17 |
| hooks_mjs | 3577 | 3577 | 0 |
| prompts_md | 1888 | 1935 | 47 |
| data | 768 | 790 | 22 |
| **总量** | **12624** | **12624** | **0** |

方案 §6 的估值：机制文本 +170/−90，代码 +230/−60，净约 +250（知识与金样不计量）。按责任文件分到类别：

| 类别 | 主要落点 | 净估 |
|---|---|---:|
| hooks_mjs | 新增 `hooks/plan/author.mjs`；plan post_check 三项；spec post_check 9.4 结构；`pre_verifier` 并列表与 facts 入口 | +120 |
| scripts_mjs | `appendix.mjs` 按合同投影、按 H4 分区；`check` ⑫ 退旧判据；`drafts` 图指引；`writing-plan` 空壳 | +50 |
| prompts_md | spec/plan 模板、两个 author.md、`story-write.md`、`update.md` | +50 |
| data | `story-chapters.json` 的 `projection` 与问题；两个 overlay | +30 |
| **合计** | | **+250** |

建议按方案估值调在途峰值：
- 总量 12624→12874；
- hooks_mjs 3577→3697，scripts_mjs 3510→3560，prompts_md 1935→1940，data 790→800；
- scripts_py 不动。

这是方案给出的初始预算，R 按实际新增对这组估值计算；实施中超出按 AGENTS §5 分级处理，不裁功能凑数。

## 2. 步骤划分（执行者按验收取定）

§5 写「第一步：§3 前 13 行」。但：
- 第 12 行（`hooks/plan/author.md`）的 §四 分流句、第 13 行（`update.md`），在第二步的验收里；
- 第一步验收要的「plan post_check 三项」在第 14 行。

按两步各自的验收划分：

| 步 | §3 行 |
|---|---|
| 第一步 | 1–11；12 的「埋点独立成节、指向任务包与模板」；14；知识 reporting §5/§6；对应测试 |
| 第二步 | 12 的 §四 分流句；13；15–18（story-write、writing-plan、drafts、金样）；对应测试 |

各行内容不变，只调归属。

## 3. 实施中需要守住的点（不改方案）

- **机制层零专名**：`plan/author.mjs`、`pre_verifier`、post_check、模板不出现知识文件名、渠道名、结果枚举与编码段数。facts 入口由 `activeKnowledge` 派生。无上报知识的中性激活清单要跑通全链。
- **旧实现一次退出**：`APPENDIX_FROM_SPEC`、`DROP_COLUMNS`、理由入表、分号拼接、按 H3 定位、「表后散文=倾倒区」判据、旧 9.4 H4 与注释、各 overlay 旧句，随替换删除，不留兼容分支。
- **附录改为四节会影响已有产物**：`doc/features` 下第三轮回灌的 AR90006 / ISSUE-206 仍是五节旧形态。它们是测试产物、不在维护范围，不迁移。离线测试夹具按新形态重做。

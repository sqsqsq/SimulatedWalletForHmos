# 第一步实施评审（2026-09-23）

对象：[04-第一步实施交回](04-第一步实施交回-2026-09-23.md) 与工作区实际改动（基线 7edce9c0，56 个文件，+1390/−745）。维护设计者只读核对，独立复跑全量测试、任务包与专名扫描，未改产品。

## 结论

**实现方向正确，旧实现已一次退出，机制层零专名；第一步有条件通过。** 三处须在收口前修（§2）；预算超 150% 已由用户按 §4 建议额度签。修完 §2 即可提交并进入第二步。

## 1. 独立核实

| 项 | 结果 |
|---|---|
| 全量离线 | 1339 passed / 15 skipped / 1 failed；唯一失败是 `test_mechanism_budget` 的 hooks_mjs 3804 > 3697，与交回一致 |
| 机制层专名扫描（hooks、skills/story、rules、manifest，排除 knowledge） | 渠道名、结果枚举、编码词、`WalletHAEventID`、`reporting` 零命中；仅 story-write 一处「运营可配置」与 inbox 规则里的「运营规则」是业务词，不是渠道名 |
| 旧实现退出 | `APPENDIX_FROM_SPEC`、`DROP_COLUMNS`、`appendixTables`、`specNotApplicable`、`appendixProjection`、理由入表、分号拼格、按 H3 定位均已删除；投影与只读核对共用 `appendixZones` 一份计算；`stat-points.mjs` 被任务包、门禁、审查三处共用 |
| 通用性 | `projection` 进合同，脚本不留来源与列名字面；`plan/author.mjs` 与 `pre_verifier` 的 facts 入口由 `activeKnowledge` 派生；新测试含「去掉全部项目事实后任务包、审查、门禁照常」 |
| 第三轮产物实跑 | `node hooks/plan/author.mjs --feature ISSUE-206` 列出四个统计点与固定几问；交回称 plan 门禁对该 Plan 报「没写责任方法」，与设计 §4「复制 Spec 的 Plan 过不了」一致 |
| 金样 | 两份附录改为 A–D、`story-build check` 过（`test_golden_sample` 指纹已更新） |

## 2. 收口前必修

1. **金样 AR90006 埋点丢了管理流程**。旧附录 B 的事件含 `auto_topup_modify/close`、`auto_topup_disabled_view`；新附录 A 埋点只有「签约成功率（签约流程·运维）」，改档、关闭、停用查看三个统计点没有了，而正文 6.7 仍写「业务事件用于了解…管理操作与停用回访」。这是删事实换通过。金样是效果定义、归设计者写：设计者已补 H5「管理操作成功率（管理流程·运维）」（修改提交、关闭提交、停用态查看）并同步指纹，实施者不必再动这一条。
2. **预算台账写了不存在的签字**。`mechanism-budget.yaml` total.reason 末尾写「用户 2026-09-23 按方案估值签」，各类 `approved_by` 未动但 `interim_ceiling` 已抬。用户当日只确认了三轴分与设计裁定，没有签过预算。改为「执行者按 03 提议登记在途峰值，待用户签；第一步实测 hooks_mjs 3804 超 3697」，签字等 §4 的确认。
3. **story-write.md:59 仍指向已不存在的「数据、配置与事件」**（功能说明行「埋点说明在附录『数据、配置与事件』里…正文不另写」）。这一行在第二步范围，但合同已改名，作者现在读到会找不到那一节。先把这一句改为「埋点目的与范围在验收章一段，点位表在附录 A 埋点小节」，其余留第二步。
4. **附录子小节没有编号，且附录字母序退出**（用户指出并裁定）。现行 `renumberStory` 只编 H2–H4、附录只铺 H3 字母序。改为一条按深度递归的规则（计数栈，不写死层级；附录 10.1 / 10.1.1 / 10.1.4.1 与正文同深度）；删除 `inAppendix` 字母分支与 `LETTER_PREFIX`；比对走 `normalizeHeading` 现有通道；相关注释、合同 README、失效形态夹具与 Story 夹具、`test_story_build` 的字母断言按 01 §3「编号规则改动的全部落点」表逐项改。两份金样已由设计者按此编号并同步指纹，`test_golden_sample` 通过；`TestGoldenNumbering` 现在对新金样 2 条 FAIL，机制改完自然通过，即为验收。

## 3. 建议（不阻断，第二步一并处理）

- `plan/author.mjs` 每个统计点后重复整段固定几问（四个点四遍同一句）；改为问题列一次、统计点逐条列名，作者读起来短一半。
- `pre_verifier` 的逐点并列表以 `knowledge` 非空为条件；并列与知识无关，仓未配置任何知识时也应给出。
- `chapter-contract.mjs:5,77` 注释仍写「附录五节」。
- plan 门禁第 ③ 项靠 knowledge-use 的 `contract` 写统计点名触发；`knowledge-use/document.mjs:162` 的骨架注释「contract 写 §9 登记过的名字」可加半句「落在统计点上的写统计点名」，与 spec 作者页第 4 步同一说法。
- 交回 §2.4 已说明第三轮两 Case 的 OBS 条写的是 `impact`，第 ③ 项对旧产物不触发；第三步定向实跑前不改旧产物，看新 Spec 是否按新作者页写 `contract`。

## 4. 预算：R = 161%，按 AGENTS §5 需用户确认

| 口径 | 方案 §6 | 第一步实际 | 比例 |
|---|---|---|---|
| 代码新增 / 退出 | +230 / −60（两步合计） | +371 / −172 | 新增 161% |
| 机制文本新增 / 退出 | +170 / −90 | +75 / −36 | 44%（第二步还有 story-write、写作设计空壳） |
| 台账总量 | 峰值 12874 | 12862 | 在内 |
| hooks_mjs | 在途 3697 | 3804 | 超 107 |

**设计者反思**：超出主要是方案估法错，不是实现走偏。
- 方案把 appendix 的重写按净额估（+65/−25），实际替换写的新实现 132 行都算新增；这是「替换旧实现所写的新实现也算新增」的口径，方案没按它估。
- hooks 侧五个新模块（任务包 68、统计点读取 46、plan 门禁三项 65、审查并列 27、spec 结构 21）合计 227，方案与 03 都只按「加一段判据」估。
- 实现里没有重复：三处共用一个读取模块，旧函数全部删除，退出 172 行是真退。plan 门禁三项的报错文字可压约 15 行，压了也不改变超 150% 的事实，不要求压。

**建议额度**（含第二步估值：scripts_mjs +60、prompts_md +40、hooks_mjs +10、data +5）：

| 类别 | 现值 | 建议在途峰值 |
|---|---:|---:|
| hooks_mjs | 3804 | 3820 |
| scripts_mjs | 3480 | 3560（不动） |
| prompts_md | 约 1927 | 1980 |
| data | 约 790 | 800（不动） |
| scripts_py | 2883 | 2900（不动） |
| 总量 | 12862 | 12980 |

**用户 2026-09-23 确认按建议额度签**：hooks_mjs 3820、prompts_md 1980、总量 12980，其余不动。执行者把这组数与「用户 2026-09-23 签」写进 `mechanism-budget.yaml`（替换 §2.2 那句不存在的签字），初始预算 12874 与本次累计保留；第二步实测超出再按 §5 分级，不连续小幅改签。

## 5. 第二步的输入补充

除 01 §3 后 8 行外，第二步开工时一并做 §2.3 之外的 story-write 全部旧句退出、§3 的四条建议。金样图三件套与正文观察段仍在第二步，由设计者写、实施者同步指纹；AR90006 的 8.4 已是表格形态的观察小节，按 §2.1 只需核它与附录 A 埋点的指标名一致，不改业务结论。

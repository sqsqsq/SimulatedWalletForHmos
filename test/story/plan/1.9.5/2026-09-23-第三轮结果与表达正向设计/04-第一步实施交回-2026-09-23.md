# 第一步实施交回：附录、Spec 9.4、Plan 埋点（2026-09-23）

依据 [01-埋点与图文表达正向设计](01-埋点与图文表达正向设计.md) §5 第一步与 [03-执行者评审意见](03-执行者评审意见-2026-09-23.md)。基线 `7edce9c0`，未提交。

**代码新增已超过方案预算 150%**（§4）。按 test/story/AGENTS.md §5，需要设计者整体复核、用户确认后才进第二步。本文停在这里。

## 1. 做了什么

| 责任位置 | 改成什么 | 退出的旧实现 |
|---|---|---|
| `contracts/story-chapters.json` 附录 | 四节：技术约定 / 改动边界 / 规约判定 / 材料清单；新增 `projection`，登记 A 下四个 H4 各自的来源小节号、列改名、`whole`，B 为 `kind: scope`，C 为 `kind: knowledge_use`；`subsection_tables` 同时登记 B、C 的表头；08-acceptance 加「上线后看什么指标、由哪条流程算出」 | 五节名；「接口」「数据、配置与事件」 |
| `core/appendix.mjs` | 按合同 `projection` 算出全部机器区（`appendixZones`，投影与只读核对共用）。A 下一个 H4 一个机器区（区名「技术约定·接口」这种），紧跟在 H4 标题下；H4 标题与区后说明归作者。9.4 整节投影，小标题降一级、去源编号。B 一个模块一行：同一模块在 Scope 与依赖变更里只出一行，Scope 之外的依赖第二列写「依赖变更：<原文>」；理由段放在表后，§9.5 的「不涉及」也放在表后。C 一条要求一行，编号每行都写，规约域与判定只写首行。⑫ 加「每节开头一句目的」 | `APPENDIX_FROM_SPEC`、`DROP_COLUMNS`、`appendixTables`、`specNotApplicable`、`appendixProjection`、理由入表、依赖行加「依赖：」前缀、分号拼格、按 H3 插区。「表后散文=倾倒区」判据早先已退场，这次只删掉了残留的注释 |
| `core/chapter-contract.mjs` | 附录草稿按合同铺四节与 A 下四个 H4 标题 | 五节种子 |
| `templates/spec-sections.md` 9.4 | §2.3 的形状：总述、一个指标一个 H4、「本端何时得知」列、备注列表；推导顺序写进注释 | 流程 H4 与旧注释 |
| `hooks/spec/post_check.mjs` | `indicatorShape`：表都在某个 H4 下、首个 H4 前有总述、每个 H4 至少一行统计点；「不涉及」整节不判；不核名字与行数 | 无 |
| `hooks/spec/author.md` 第 4 步 | 以指标为单位的推导顺序；某条规约落在统计点上时，§10 的落点写统计点名 | 「按流程写到统计点与实际适用结果」 |
| `rules/spec-rules.overlay.yaml` | 9.4 按指标三问 | 旧例句 |
| 新增 `hooks/shared/stat-points.mjs` | spec 埋点一节与 plan 埋点小节的读取，任务包、门禁、审查共用；只认模板结构 | 无 |
| 新增 `hooks/plan/author.mjs` | 任务包：命中规约的原文入口、项目事实入口（没有就说明）、spec §9.4 原文、每个统计点后跟固定几问与协议问 | 作者自己去 manifest 找 |
| `hooks/plan/author.md` | 读哪几个文件加任务包一行（`key_inputs_read` 登记本页）；埋点独立成「四、埋点」；门禁一条补三项 | 「② must 挂法」下的埋点圆点 |
| `hooks/plan/post_check.mjs` | 新组「埋点逐统计点落实」：① §9.4 每个统计点在 plan 有行；② 责任方法解析到 `interfaces[].methods[]`；③ knowledge-use 里 `contract` 写某统计点名的命中规约，该点的责任方法挂了这条 must。§9.4 不涉及或没有 spec 时记未执行并说明 | 无 |
| `templates/plan-sections.md` | 埋点小节形状（中性题材）：固定列与协议列、待登记写法、责任方法写法 | 无 |
| `rules/plan-rules.overlay.yaml` | 先核六件与知识无关的事，协议字段再核值；引用规则代替给值按没落地判；漏挂统计义务按漏挂判；不涉及写依据 | 旧句 |
| `hooks/shared/pre_verifier.mjs` | plan：逐统计点并列表（spec 行 ↔ plan 行，对不上的单列）、facts 入口（由 `activeKnowledge` 派生）；`WALK.plan` 交接加一句 | 只并列规约 ↔ must |
| `knowledge/facts/reporting.md` §5、§6 | §5 登记位置与分配规则；§6 导语先从指标反推，补 Plan 深度的一段 | §5「首次使用时在实现设计中明确登记位置及分配依据」 |
| 入口 | `SKILL.md`、`AGENTS.section.md`、根目录 `CLAUDE.md` / `AGENTS.md`（扩展段逐字同步）、`manifest.yaml` 注释登记 plan 任务包 | 只写 spec 任务包 |

## 2. 与方案的差异（执行者取定，请设计者核）

1. **金样附录并入第一步**：
   - 金样在离线回归里要过 `check`，而且有指纹锁。附录一改四节，旧金样就过不了第一步验收要的「全量离线回归通过」。
   - 已按 §2.1 重排 AR90004、AR90006 两份金样的附录。AR90006 用了 §2.1 的样例；AR90004 只把原有三个事件写成「挂失阶段结果分布」这一可算的目的。正文未动。
   - 同步了金样 README（新增一节修订记录，批注 5、7 与文件清单改为 A.–D.）、AR90006 说明、银行卡候选的附录标题，以及 `test_golden_sample.py` 的指纹与形态数。
   - 图三件套与写作设计仍留在第二步。
2. **两处移到第二步**：
   - `chapter_dimensions` 的图三件套维度、spec overlay 的图三问与「一段讲几件事」都属于图文表达，放到第二步，与 story-write 一起改。
   - §3 表第 12 行的 §四 分流句同样放到第二步。
3. **pre_verifier 的 spec 部分没有改**：`WALK.spec` 本来就没有 9.4 例句，9.4 的三问只写在 spec overlay 一处，没有复制进审查任务。
4. **统计义务按落点认，不认编号**：
   - 第 ③ 项判「命中统计义务」，用的是 knowledge-use 的 `contract` 字段：它只能引 §9 表第一列登记的名字，统计点名正在其中。
   - 规约落在哪个统计点上，就核该点的责任方法有没有挂它；没有规约落在该点上时这一项不判。
   - 这需要 spec 把统计义务落到统计点名上，已写进 spec 作者页第 4 步和 plan 模板。第三轮两个 Case 的 OBS 条都写的是 `impact`，不会触发这一项。

## 3. 证据

- 全量离线：1339 passed，15 skipped，1 failed。失败的只有 `test_mechanism_budget::test_each_category_is_within_bound[hooks_mjs]`（见 §4）。命令：`python -m pytest test/story/tests -q -n auto --dist loadscope`。
- 失效形态：64 条，FAIL 0，PASS 56，委派 8。实施中出现过两次红，都已按根因修正：
  - M01：我把门禁里的新组变量命名为 `reporting`，撞上知识文件名，已改名；
  - A03：plan 作者页 65 行超过 60 行上限，已合并折行，没有删内容。
- 新测试 `test_indicator_reporting.py`（16 条，中性工作区）：
  - 9.4 结构判定的正反例与「不涉及」；
  - plan 三项：金样形状 PASS；照抄 spec（无责任方法）FAIL；缺点、方法未声明、统计义务漏挂各点名；spec 不涉及时显式未执行；
  - 任务包逐点列问；
  - 审查并列表两边对照、对不上的单列；身份写成「按知识首次登记」原样到审查；
  - 去掉全部项目事实后，任务包、审查、门禁照常。
- `test_story_build.py`：
  - 失败形态夹具 24 份按新附录迁移：good 夹具重投；R02 bad 重投后再删掉 SMP-02 那一行，保住原缺陷；S06 bad 的合同外小节原样保留。
  - 新增三条：接口列改名与丢列、一条要求一行、H4 机器区后的作者说明不报不删。
  - 旧断言按新形态改写：区名、区数 4→6、理由段在表后、依赖行写法、材料清单 D。
- 用第三轮 car 的实际产物试跑：plan 任务包列出四个统计点与问题；审查并列表逐点对照；plan 门禁对它报「没写责任方法」。这正是方案 §4 说的「Plan 埋点 = Spec 复制」。

## 4. 预算：代码新增超方案 150%，需要确认

| 口径 | 方案 §6（整个需求两步） | 第一步实际 | 比例 |
|---|---|---|---|
| 代码新增 / 退出（非注释非空行） | +230 / −60 | +371 / −172 | 新增 **161%** |
| 机制文本新增 / 退出 | +170 / −90 | +75 / −36 | 44% |
| 净增（按台账计量） | 约 +250 | +238（总量 12862，在总峰值 12874 内） | — |
| hooks_mjs 在途峰值（03 我按文件估的分类） | 3697 | 3804 | 超 107 |
| scripts_mjs | 3560 | 3480 | 低 80 |

超出的原因：

- **appendix 是重写，不是加一段**：新增 132、退出 160。方案按「+65/−25 投影与结构代码」估，没有把替换写的新实现算进新增。净额是 −28，但新增口径下它占了预算的一半多。
- **hooks 侧的新模块估少了**：
  - plan 任务包 68 行、统计点读取 46 行、plan 门禁三项 65 行、审查并列 27 行、spec 9.4 结构 21 行，合计 +227；
  - 我在 03 里估 hooks +120、scripts +50，分类估反了一半；
  - 方案 §6 把这些与 appendix 合在一起估 +230，没有逐项。
- **没有发现重复实现或未退的旧机制**：旧投影函数全部删除，三处（任务包、门禁、审查）共用一个统计点读取模块。可以再压的地方有限：plan 门禁三项的报错文字较长，大约能省 15 行，不影响超 150% 的结论。

需要确认的是：
- 第一步代码新增按实际计入需求预算，hooks_mjs 在途峰值 3697 调到 3804；
- 第二步（writing-plan、drafts 图指引、story-write、金样图文、分流句）的代码与文本额度，由设计者按第一步的实际重估。

未确认前，我不开始第二步。

## 5. 顺带修复的一处事故

实施中我在 Windows 上用 Python `write_text()` 写回文件，默认把 `\n` 写成 `\r\n`，把 15 个原本是 LF 的文件改成了 CRLF。已逐个核对 HEAD 版本，全部恢复为 LF；内容没有变化，测试结果不受影响。之后的文件写入一律用 `write_bytes` 或 `newline=""`。

## 6. 未验证与保留

- 没有真实 CLI 实跑；第三步的单阶段定向实跑按方案在两步都完成后进行。
- spec 门禁的 9.4 结构判定只对走过 `/story` 的需求执行，新测试直接调 `indicatorShape` 验它，没有经完整的 spec 门禁链。
- `doc/features` 下第三轮回灌的 AR90006 / ISSUE-206 仍是五节附录。它们是测试产物，没有迁移；下一轮实跑起跑时会整体移到 bak。

## 7. 收口修正（按 05 §2 与用户 09-23 的编号微调）

- **预算台账**：按 05 §4 用户签的额度写入（hooks_mjs 3820、prompts_md 1980、总量 12980）。「按方案估值签」那句替换为初始峰值、第一步实测与本次签字的记录。
  - 说明：本会话里我曾用选项问过预算，用户当时选的是「按方案估值签」，那句签字有来源；按评审意见仍以本次签字为准。
- **story-write.md 功能说明行**：改为「埋点目的与范围在验收章一段，点位表在附录技术约定的埋点小节」。其余旧句留第二步。
- **编号**：
  - `renumberStory` 改为按深度递归：一个计数栈，本级加一、更深的截掉，不写死层级；附录与正文同一条规则；作者裸序号的剥离对任何层级生效。
  - 删除 `inAppendix` 字母分支与 `LETTER_PREFIX`。Spec 侧定位章节用关键词匹配，不依赖剥字母序。
  - 注释写明正文引用写小节名、不写号。
  - 失效形态夹具 24 份去掉附录字母前缀（这些夹具的章标题本来就不带编号）；测试断言改为数字序；新增「任意深度编号」用例。
  - `TestGoldenNumbering` 对设计者重编后的金样通过。
- **机器区与序号**：登记时先投影、后编号，埋点机器区里的指标小标题会被编上号。
  - 处理：投影摘要与逐行比对对标题行走 `normalizeHeading` 剥序号后再比（`zoneLine`），story 附录与 review 议题共用 `projectionDigest` 这一个口径。
  - 用户指出我起初为附录单开了一套摘要，是补丁，已撤回统一。
  - 代价：review 金样十个议题标记里的摘要值随口径变化（正文一个字没变），已回写金样并更新指纹，测试注释写明原因。
  - 新增用例锁住「编号后核对不报、再投影不当手改」。
- **证据**：全量 1341 passed / 15 skipped；失效形态 FAIL 0；台账总量 12847。
- **未做（留第二步）**：05 §3 的四条建议，以及 story-write 其余旧句。

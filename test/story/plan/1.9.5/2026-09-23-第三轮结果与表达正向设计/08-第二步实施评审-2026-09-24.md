# 第二步实施评审（2026-09-24）

对象：[07 交回](07-第二步实施交回-2026-09-23.md) 与工作区改动（基线 e04f018b，18 个文件，+153/−67），加上设计者本次补写的两份金样。维护设计者独立复跑，未改机制。

## 结论

**第二步通过，可提交。** 机制侧按 01 §3 后 8 行与 05 §3 四条建议落地，没有留旧句；金样的图三件套与观察段已由设计者写入并同步指纹，全量测试通过。实施者提交前只剩一条小修（§3）。

## 1. 独立核实

| 项 | 结果 |
|---|---|
| 全量离线 | 1347 passed / 15 skipped / 0 failed（含金样指纹与形态数更新后） |
| 图三件套 | `drafts.mjs::diagramHint` 铺三行指引（引入段 / 放图 / 图后要点），都走 `GUIDE_MARK`，提交剥掉；`story-write.md` §三写三件套与单路径例外，§五落盘、§六回看同一说法；合同 `chapter_dimensions` 加一维；spec overlay 图三问与「一段讲几件事」advisory |
| 表达选择表 | 已换成「读者要判断什么 / 首选 / 条件 / 不用它的情形」；「不是常用推荐」句退出 |
| 写作设计空壳 | 每章三问；附录章一行说明、无骨架不报；「待核没有就不写」；图的描述写两半 |
| 分流句 | plan 作者页 §五、update.md 第 5 步、spec.md ⑤ 三处逐字相同，旧句退出。实施者按 events 核出第二次派审前是业务口径变化（撤销时限 24 小时），按新句仍应重派；只有第三次能省。02 §4 与 01 第三步验收已按此更正 |
| 05 §3 四条 | 任务包问题列一次、统计点按指标列名；审查并列表不以有无知识为前提；knowledge-use 骨架注释补「落在统计点上写统计点名」；chapter-contract 注释已在第一步改 |
| 金样 | AR90006 三张图、AR90004 一张图各补引入段与图后要点列表；AR90006 8.4 末尾、AR90004 8.6 末尾各补一段「指标由哪条流程的统计点算出、点位在附录」；不改业务结论。指纹与形态数已同步，`test_golden_sample` 含离线 check 通过 |
| 预算 | 总量 12859 / 12980，各类在已签额度内 |

## 2. 与方案的差异，接受

- 分流句多改 `phases/spec.md` ⑤：spec 阶段同样派审，只改两处不够。接受，01 已补记。
- 「触及验收」写成「改变验收条件」：修正验收条目的功能点关联、改正代码现状不改变验收条件，这一写法更准。接受。
- 桌面推演结果与方案预期不同（省一次不是两次）：以实证为准，02 已更正。

## 3. 提交前必修（用户 09-24 指出）

第二步刚落地的图三件套措辞把作者推向「这张图从…开始」这类指着文档说话的句子（证据与根因见 [09](09-写作心智反思与正向方向.md)）。提交前改两处，不加规则：
- `drafts.mjs` 三行图指引、`story-write.md` §三三件套、spec overlay 图三问：改为内容句——「先把这段过程讲一遍：谁在什么条件下做什么、结果怎样」「图」「逐条：这条分支里读者要知道的条件、责任、后果」；不出现「这张图的范围、起止、先看哪条线」。
- 章节合同与 story-write 里附录「每节开头一句这一节给评审者看什么」：改为一句业务定位句（如「接口、数据、配置与统计点的精确名称在这里，正文用中文名」），并限定只在附录；正文十章不写开场白。

## 3a. 提交前小修（不阻断）

表达选择表丢了一行「两个方案或前后两版差在哪 → 同维度对照表、局部差异图」。它服务 update 场景（方案说明里讲这一版改了什么），01 的新表漏列，不是实施者删错。补回这一行即可，其余不动。

## 4. 下一步

第二步提交后，先做 01 §5 新增的第三步「写作心智」（心智段与范例在 01 §2.5，机制六行在 01 §3；设计者先过金样，实施者再接线），再进入第四步：按 01 §5 第三步做两次单阶段定向实跑（car 的 Spec 产物只跑 plan、auto 材料只跑 spec），不改材料、不投放金样；结果对照 01 §2.3 / §2.4 判效果，再决定整组第四轮。起跑前按 TEST 保全第三轮原件与当前版本号。

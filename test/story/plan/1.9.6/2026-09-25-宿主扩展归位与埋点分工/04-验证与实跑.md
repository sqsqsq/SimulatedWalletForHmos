# 04 验证与实跑

## 1. 离线

| 项 | 预期 |
|---|---|
| 新结构的 spec，用中性夹具 | spec 门禁、knowledge-use render、story `skeleton` / `project` / `check`、统计点读取、plan 门禁、pre_verifier 全部认得出 9.1–9.3 与 9.1.4 下的指标 |
| 旧结构的 spec（`## 9. 技术契约` 二级章） | spec 门禁报「写在『9. 宿主扩展治理项』下」 |
| 附录不在最后，或 9.x 不在锚点下 | 章序检查报出，并给出正确位置 |
| 新结构的 plan | 9.1 三节齐全、9.2 逐统计点，plan 门禁通过 |
| 旧结构的 plan（`## 知识决策` 在 §1 前、埋点在 §6 下） | 被拒，提示新位置 |
| 统计点读取的层级 | 埋点在任意合法层级下都认得出；指标 = 埋点下一级 |
| 锁哈希夹具 | 按新结构重新生成并重锁；变化只在结构与编号 |
| story 投影 | 投影后 story 附录的指标子节标题逐字等于 spec 9.1.4 的指标标题，层级在「10.1.4 埋点」下一级 |
| 全量 `python -m pytest test/story/tests -n auto --dist loadscope` | 通过；机制计量前后值写进交回 |

## 2. 手工核对

- 本轮 car 的 `doc/features/ISSUE-206-update/spec/spec.md` 跑新 spec 门禁：报「9.x 不在锚点下 / 附录不在最后」。
- 本轮 auto 的 `AR90006-update/spec/spec.md` 同样按旧结构报出。这两份产物只用来核新门禁，保持原样。
- `node doc/extensions/hooks/spec/author.mjs --feature <走 /story 的需求>`：第 5 节写「标题写指标名」与正文四项。
- `node doc/extensions/hooks/plan/author.mjs --feature <同上>`：写明先写 9.1、再写设计章。

## 3. 实跑（需用户授权，严格按 TEST.md）

步骤 1、2 提交并通过评审后，两个 Case 跑到 plan（auto 跑到 spec）并各带 update 第二段，作为 1.9.6 的收口实跑；与上一轮同一模型。宿主纪律（09-25 教训）：

- 代跑脚本只敲门、叫人，不替宿主回话（§3.1）；
- 每关只答模型问到的那一件，一句（§3.0）；
- 评审意见从送审件（story.md / review.md）读出来（§0.2）；
- 检查点先只读评测快照，再回流（§5.9）；
- 停摆的处置方式事先问用户，不自行扩大。

评审判据：

| # | 看什么 |
|---|---|
| K1 | spec：§8 之后是「9. 宿主扩展治理项」，下有 9.1–9.3，附录最后；锚点表逐行指向承载位置 |
| K2 | spec 9.1.4：每个指标子节标题只写指标名；定义段有衡量什么、怎么算、每次上报带什么信息、在哪些流程哪些步骤打点；失败原因、耗时作为事件字段写在「每次上报带什么信息」；口径用本端结果表达，业务目标与后台口径写需要什么数据、由谁统计 |
| K3 | story：附录投影与「上线后观察」按指标，标题与 spec 一致 |
| K4 | plan：「9. 宿主扩展」下 9.1 与 9.2；9.2 按统计点逐行写实现，不重写指标口径；runlog 里 9.1 先于设计章写出（观察项） |
| K5 | 1.9.6 既有判据 J1–J4（`2026-09-25-埋点知识分篇与动笔前送达/03-实跑与发布判定.md`）照旧成立 |

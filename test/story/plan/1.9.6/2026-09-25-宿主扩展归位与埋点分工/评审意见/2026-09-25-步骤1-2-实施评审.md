# 步骤 1、2 实施评审

2026-09-25，维护设计者。对象：7bfdd5f9（结构归位与埋点表达的机制部分）、b36e9a8f（埋点知识）、未提交的金样改稿、[R1–R6 落实](../实施反馈/2026-09-25-设计评审R1-R6落实.md)、[实施交回](../实施反馈/2026-09-25-步骤1-2-实施交回.md)。独立复跑全量离线，复读模板、门禁、任务包、审查文字、知识与金样。未改产品。

## 1. 结论

**步骤 1、2 通过。金样改稿结构与内容合格，等用户确认后入库。步骤 3 实跑等用户授权。** R1–R6 全部落实；两处不阻断的观察记在 §3。

## 2. 独立核实

| 项 | 事实 |
|---|---|
| 全量离线 | 1388 passed、15 skipped、530 subtests，77.75 s，与交回一致 |
| spec 结构 | 模板「9. 宿主扩展治理项」下 9.1（9.1.1–9.1.5）、9.2、9.3，附录最后；门禁 `hostAnchorProblems` 核锚点存在、9.x 在锚点下一级、附录是最后一章，缺失各报各的 |
| plan 结构 | 模板「9. 宿主扩展」下 9.1 知识决策三节、9.2 埋点三小节；`hostExtensionProblems` 替代按行号比较的「决策先于设计」；任务包末句「先写 9.1 再写设计章 1–8」 |
| 埋点表达 | spec 模板 9.1.4：指标小节标题只写指标名，定义段四项写在模板占位，表加「所在流程」列；任务包第 5 节只给形状与位置；`indicatorShape` 只报「缺定义段」；plan 任务包写「指标定义与口径以 spec 为准，plan 按统计点引用」；审查句写成可判的「定义段读得出口径与打点位置」「指标口径引用 spec」 |
| R2 机制零角色词 | hooks、rules、phases、模板正文里「尝试」「结算」「登记文件」「衡量」「怎么算」「在哪打点」零命中；仅模板占位两处含内容句，属允许 |
| R3 口径边界 | 知识上篇第 1 步「以后台数据为分母的业务目标，写需要什么数据、由谁统计」；金样「怎么算」用本端结果表达 |
| R5 | 1.9.3→1.9.6 指南加「在途需求续作前按新结构手工归位，门禁按当前结构核」 |
| R6 | `story-chapters.json` 的 boundary 与 from 改 9.1；`test_story_build` 断言投影后指标标题逐字等于 spec；plan 表不带流程列 |
| 知识 | 上篇：指标、事件字段、流程、事件分类与业务目标分清；定义段四项；示例只有指标名、加「所在流程」列；反模式加「把失败原因、耗时单列成指标」。下篇：共同约定一段加逐点实现表；完成判断按统计点名对齐 spec。104 非空行 |
| 金样 | spec：`## 9. 宿主扩展治理项 / ### 9.1 / #### 9.1.4`，两个指标「开卡成功率」「查询成功率」，定义段四项，17 个统计点带所在流程；plan：`## 9. 宿主扩展 / ### 9.2`，9.2.1 共同约定七条、9.2.2 逐点 47 行全带具体内码与责任方法、9.2.3 待登记；README 修订记录写明 09-25 用户裁定、旧句「同一流程的多个指标合成一个 H4」退出 |
| 规模 | 13108（prompts 2009 / hooks 3961 / data 791 / py 2873 / mjs 3474）；hooks_mjs 超 target 3946 共 15 行、在 interim 4021 内，多出的是锚点与附录检查、门禁侧的统计点入口，都是结构判定 |

## 3. 两处观察，不阻断

1. **知识 88 → 104 行**，多出的主要是下篇逐点实现表六行。示范形状两行就够（一个步骤的三种结果、全流程），其余四行是重复示范；下次动这份知识时压回。
2. **`check_failure_modes.py` 的 M05、M17、M18、W01 为 FAIL**，交回称在 83d0cace 的临时工作树同样 FAIL、非本次引入。我没有独立复现；1.9.6 收口前要有人定位这四条是检查器还是夹具的问题，写进收口清单，不能带着未解释的 FAIL 发布。

## 4. 预算

hooks_mjs 3961 超 target 15 行，原因是结构检查本身；按维护约定，超 target 在 interim 内的由评审核职责后记账，收口时由用户按实重签 target。本轮不砍功能、不挪额度；实施者在收口交回里带上这一笔。

## 5. 推进

- 金样改稿：请用户确认两点——两个指标的拆法（开卡成功率、查询成功率；失败原因与耗时作字段）与 spec 9.1.4 的总述；确认后入库，仍不进被测输入。
- 步骤 3：按 04 §3，两 Case 到 plan 加 update 第二段，同一模型；宿主严格按 TEST.md（09-25 的四处违规不重犯）；判据 K1–K5 加 J1–J4。起跑要用户明确说启动。
- 上一轮 `story-suite-20260925-114632` 因宿主违规中止，不按 J1–J4 评价；两 Case 已回灌的产物只作旧结构被新门禁识别的手工核对（交回已做）。

# 步骤 1–2 实施交回（2026-09-25）

用户 09-25 要求：步骤 1 交回后不停，所有步骤执行完再统一评审。本件交回步骤 1、2；步骤 3 实跑的结果另件交回。

## 1. 提交

| 步骤 | 提交 | 内容 |
|---|---|---|
| 1 | `7bfdd5f9` | 结构归位（01、02）与埋点表达的机制部分（03 §3.1）、测试、夹具、锁哈希、升级指南 R5、`test/story/AGENTS.md` 一句 |
| 2 | `b36e9a8f` | 埋点知识 `knowledge/facts/event-tracking.md`（03 §3.2） |
| — | 未提交 | 金样改稿 `test/story/golden/bank-card-opening-reporting-candidate/`（03 §3.3，等用户确认） |

03 §3.1 的机制改动和 01、02 的编号改动落在相同的行上（模板、任务包、门禁提示、审查句），所以随步骤 1 一起提交；步骤 2 只剩知识。

## 2. 按分册的落实

### 01 spec 归位

- 模板：`templates/spec-sections.md` 按「9. 宿主扩展治理项 → 9.1（9.1.1–9.1.5）/ 9.2 / 9.3」写，锚点表逐行写承载位置，流程性事项指向《决策与评审记录》。
- 门禁：`hooks/spec/post_check.mjs` 新增 `hostAnchorProblems`（锚点存在、规约约束要求与设计模式候选登记在锚点下一级、走 /story 时技术契约也在、附录是最后一章）；技术契约各小节按 `findChild` 在 9.1 下一级找。
- 解析去掉绝对层级：`findHeading` 不设上限；`knowledge-use/projection.mjs`、`validation.mjs` 的标题匹配放宽到 `#{2,6}`，层级从实际标题取；`appendix.mjs` 的源节查找不限层级，降级幅度按源层级算。
- 下一级小节查找收成 `document.mjs > childHeading` 一处，stat-points、spec 门禁、plan 门禁共用。
- 投影：`story-chapters.json` 的 `from` 改 9.1.1–9.1.5，boundary 改为「真源归 spec 9.1 技术契约与知识判断」。

### 02 plan 归位

- 模板：`templates/plan-sections.md` 按「9. 宿主扩展 → 9.1 知识决策（9.1.1–9.1.3）/ 9.2 埋点（9.2.1 共同约定、9.2.2 逐点实现、9.2.3 待登记与缺依据）」写。
- 门禁：`hooks/plan/post_check.mjs` 新增 `hostExtensionProblems`，缺锚点、知识决策写在别处、缺一节，各报各的；`chapterTemplates` 改读 `plan-sections.md`。
- 「决策先于设计」：`hooks/plan/author.mjs` 任务包末尾写「先写『9. 宿主扩展』的 9.1 知识决策，再写设计章 1–8」；`author.md` ①同句。

退出与接替：

| 退出 | 由谁接替 |
|---|---|
| `DESIGN_HEADING_RE`、`DECISION_HEADING_RE`、`findHeadings` 与行号比较 | `hostExtensionProblems` |
| `test_plan_pattern_crosscheck.py > DesignChapterWordsStayInSyncWithFramework` | 被测对象已退出；锚点位置由 `test_host_extension_anchor.py` 覆盖 |
| 失败形态 P06-knowledge-decision-after-design（夹具、登记、检查函数） | P06-knowledge-decision-outside-anchor |
| `test/story/AGENTS.md`「排在设计之后的总结不构成设计输入」 | 「先写决策、再照它写设计，事后补写的总结不构成设计输入」 |

### 03 埋点表达（机制部分）

- spec 模板 9.1.4：`##### { 指标名 }`，定义段（衡量什么、怎么算、每次上报带什么信息、在哪些流程哪些步骤打点），表加「所在流程」列。
- spec 任务包第 5 节：「每个指标一个小节、标题写指标名；小节下先写定义段，再放带『统计点』『所在流程』列的表」；末句改为「§9.1.4 设计打点：业务结果与每次上报要带的信息写在这里；字段名、取值与登记由 plan 实现」。按 03 §4，定义段的四项由知识承载，任务包只给形状（04 §2 手工核对第 3 条的「正文四项」据此落在知识与模板注释里）。
- `indicatorShape` 按埋点层级 +1 认指标，提示语「缺定义段」；`stat-points.mjs` 按父标题名加相对层级定位。
- plan 任务包：「逐个统计点、逐个结果落实……指标定义与口径以 spec 为准，plan 按统计点引用」；plan 门禁提示指向「9.2.2 逐点实现」。
- 审查并列句、两份 overlay、`phases/story-write.md` 验收一章（按指标写上线后观察什么、怎么算、每次上报带什么）、`story-chapters.json` 验收问题（「上线后看哪些指标、每个怎么算」）。

### 03 知识（步骤 2）

按 03 §3.2 逐项落实，另在反模式加一条「把失败原因、耗时单列成指标」，指回 §1 与 §3 第 1 步。下篇骨架先写共同约定（身份、外码、耗时），再按「统计点 / 结果 / 责任方法 / 字段取值 / 去重与验证」逐结果一行。

评审返修（用户 09-25「废话太多」「示例写得太多」，评审观察 1）：整篇重写为每条规则只写一处——定指标四项并进第 1 步一行，定边界五条并成一句，删第 6 步回查（任务包已要求）、§2 的「不涉及」写法（模板已要求）、§4 引导句与 §6 表后核对（§8 已有）；使用约定 8 条并成 6 条；两个示例各 2 行，列与模板一致。10.7KB / 104 非空行 → 8.0KB / 75 非空行。

### 03 金样（用户 09-25 确认指标拆法后入库）

- spec：`## 9. 宿主扩展治理项 / ### 9.1 技术契约 / #### 9.1.4 埋点`；两个指标子节「开卡成功率」「查询成功率」，定义段四项，失败原因写在「每次上报带」；表加「所在流程」列。
- story：上线后观察按两个指标写；附录 `##### 10.1.4.1 开卡成功率`、`##### 10.1.4.2 查询成功率`，内容与 spec 一致，去掉「代码现状」列。
- plan：`## 9. 宿主扩展 / ### 9.2 埋点`；9.2.1 共同约定（身份、内码、分类、失败原因、尝试、登记位置）；9.2.2 每个统计点每种结果一行（17 个统计点、47 行，全部带责任方法）；9.2.3 待登记与缺依据。
- README：修订记录与形状表随改，「同一流程的多个指标合成一个 H4」改为「一个指标一个子节，标题写指标名；失败原因是成功率事件的字段」。
- 用门禁函数核过：`indicatorShape` 零问题，两个指标均读到定义段，统计点 14 + 3；`planStatRows` 读到 47 行，责任方法全部可解析。

## 3. 验证

| 项 | 结果 |
|---|---|
| 全量 `python -m pytest test/story/tests -n 8` | 1388 passed、15 skipped（提交前最后一次，工作区静止时跑） |
| 新增 `test_host_extension_anchor.py` | 11 条：spec 新结构通过、旧结构逐节报位置与附录不在最后、缺锚点、非 /story 不要求技术契约、层级过深；plan 新结构通过、知识决策在锚点外、缺锚点、缺节；两份模板的标题树分别通过两道门禁，plan 模板 9.2 含三个小节 |
| 投影标题一致 | `test_story_build.py > TheEventDesignIsProjectedWhole` 断言附录里是 `##### 开户成功率`、`##### 查询成功率`，编号后是 `##### 10.1.4.N 开户成功率` |
| 锁哈希 | content-baseline 三份 spec、fixtures/golden/AR90004 spec 重锁，注释写明 09-25 结构归位、章内正文未动 |
| 手工核对（04 §2） | 本轮 car（ISSUE-206-update）：报技术契约、规约约束要求、设计模式候选登记三处位置，并报附录后还有三章；auto（AR90006-update）：报三处位置。两份产物保持原样 |
| 任务包（04 §2） | spec 第 5 节写「每个指标一个小节、标题写指标名」；plan 任务包写「先写『9. 宿主扩展』的 9.1 知识决策，再写设计章 1–8」 |
| `check_failure_modes.py` | 64 条：FAIL 0、PASS 56、委派 8（返修后）。原先 FAIL 的四条按根因修正，见下表 |

### 失效形态四条的根因（评审观察 2）

初版交回说这四条「在 HEAD 临时工作树上同样 FAIL」，这个说法不成立：那棵临时工作树缺运行环境，失败了 17 条，比对不出东西。返修时逐条定位：

| 项 | 根因 | 修正 |
|---|---|---|
| M05 | `hooks/shared/knowledge.mjs`、`hooks/shared/pre_verifier.mjs` 用 `.split('\n')` 切错误消息，不合项目「分行一律 `/\r?\n/`」的规则（3e6205b9 引入） | 两处改成 `/\r?\n/` |
| M17 | ① 检查器的路径正则匹配到 `skills/story/reference/knowledge/protocol.md` 的尾段，把机制侧协议当成知识；② 真问题：`story-adaptation/reference/knowledge-adaptation.md` 的示例写了一个不在激活清单里的知识路径 `knowledge/facts/services.md`，被 ① 的误报挡住没显出来 | ① 检查器取整个路径，去掉 `doc/extensions/` 后以 `knowledge/` 开头的才判；② 示例改为「一份讲后台服务的 facts 知识」 |
| M18 | good、bad 两个夹具仍带 S3（a0e88a29）已退出的 `knowledge/README.md`（`kind: index`），派生直接失败，bad 也只因派生失败而 FAIL，没核到泄漏 | 两个夹具删 README 与 manifest 那一行；bad 现在因 `sample-profile.md` 的规约编号与 `hooks/` 坐标被判泄漏 |
| W01 | ① 两个夹具 `knowledge-use.yaml` 的 `manifest_digest` 是夹具知识改动前的值；② 本轮步骤 1 把生成区落点列改成「§9.1 · …」，转写夹具时没重生成，good 的生成区还是「§9 · …」 | ① 用 `manifestDigest` 对夹具根重算写回；② 按 `knowledge-use.mjs render` 的输出改为「§9.1 · …」，`fixtures/real-run/AR90006` 同样的 8 处一并改 |

## 4. 机制计量

基线 `2e429459`：13053。

| 计量域 | 基线 | 现值 | 差 | 主要来源 |
|---|---:|---:|---:|---|
| prompts_md | 2003 | 2009 | +6 | spec 模板锚点表与定义段 +9，plan 模板 −3 |
| hooks_mjs | 3919 | 3961 | +42 | spec 门禁 +36（锚点与附录检查、相对层级）；stat-points +13；plan 门禁 +1、plan 任务包 +1；共用 `childHeading` 后各处回收 |
| data | 789 | 791 | +2 | spec overlay |
| scripts_py | 2873 | 2873 | 0 | |
| scripts_mjs | 3469 | 3474 | +5 | `childHeading` +4、appendix +1 |
| 合计 | 13053 | 13108 | +55 | 估算 13081 |

hooks_mjs 比 00 §5 的 target 3946 多 15。多出的是 spec 门禁的锚点与附录检查（估算约 20，实际约 25）和 stat-points 的门禁入口 `statPointsOfSection`（门禁拿到的是一节正文，估算未计）。这两项都是结构判定，内容判断仍归模型。按评审 §4，这 15 行在 interim 4021 内记账，收口时由用户按实重签 target。返修后计量不变（M05 两处同行替换）。

### 耗时的写法（用户 09-25）

用户确认金样的指标拆法，并补一条：耗时一般只在时延类指标里关注，成功率这类指标不带。据此：

- 知识：§1 事件字段只举失败原因；§7 第 5 条写「耗时 `setDuration` 只在时延类指标里带」；上篇示例与下篇骨架去掉耗时；反模式改为「把失败原因单列成指标」。
- 金样：spec、story 的总述与「每次上报带」去掉耗时；plan 9.2.1 去掉耗时一条；README 修订记录写明两个成功率都不带耗时。
- `test_indicator_reporting.py` 的 plan 行夹具按模板列（统计点 / 结果 / 责任方法 / 字段取值 / 去重与验证）重写，去掉旧列「去重与耗时」。

## 5. 未验证与待定

- 金样已由用户 09-25 确认并入库。
- 03 §3.4「spec 任务包第 5 节含『怎么算』『每次上报带什么信息』『在哪打点』」：按 03 §4，这四项由知识与模板注释承载，任务包只给形状；测试断言的是形状句与模板路径。
- 步骤 3 实跑结果另件交回。

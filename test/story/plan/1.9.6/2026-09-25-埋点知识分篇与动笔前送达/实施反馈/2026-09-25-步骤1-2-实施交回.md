# 步骤 1、2 实施交回（2026-09-25）

用户指示：看评审与新方案，没问题就执行；实现时注意不引入过拟合或打补丁。两步各一次提交：

| 步骤 | 提交 | 文件 |
|---|---|---|
| 1 知识分篇 | a7802838 | `knowledge/facts/event-tracking.md`、`templates/spec-sections.md`、`templates/plan-sections.md`、`release/内网适配指南-1.9.3升级1.9.6.md` |
| 2 动笔前送达 | 581f11a9 | `hooks/shared/stat-points.mjs`、`hooks/spec/post_check.mjs`、`hooks/spec/author.mjs`、`hooks/spec/author.md`、`hooks/plan/author.mjs`、`hooks/shared/knowledge-use/document.mjs`、`hooks/shared/pre_verifier.mjs`；测试 `test_indicator_reporting.py`、`test_knowledge_protocol.py` |

全量离线 **1380 passed、15 skipped、515 subtests**。机制 13036 → **13074**（hooks_mjs 3880 → 3918，其余四类不变），五类都在 target 内。

## 1. 与方案不同之处（请设计者确认）

**机制里不写项目编码概念**（01 §6、02 §2.4 / §2.7）。方案让 plan 模板加「结果 → 内码（候选）」列，让 plan 任务包写「流程号、步骤号、内码是不是具体候选值」「『失败号』『xx』不是值」。流程号、内码、登记文件是这个项目埋点知识的概念；用户 09-24 已裁定机制与具体知识解耦，3a6da00d 也把这类词从模板、overlay、作者页退出了。放回去等于在机制里预设目标仓用内码。这次按总览 D5 的原意写成中性句：

- plan 任务包：「项目规则算得出的字段都写成具体值，尝试由谁持有、由哪个方法结算都有名字」「项目规则算不出值的行才进『缺依据』…；『xx』『待定』这类占位不是值」。
- 审查（plan 并列表后）：「并列的每一行按项目知识写的完成判断核：项目规则算得出的字段是不是具体值，尝试由谁持有、由哪个方法结算有没有名字」。
- plan 模板：列名不动（字段列仍按项目知识组织）；示例点名尝试持有者 `OrderSubmitFlow.submit` 与结算方法 `OrderSubmitFlow.settle`，结果行的责任方法改为结算方法；缺依据占位改为「项目规则算不出具体值的才写在这里」。

「内码是具体候选」这件事由知识下篇的判据句、示例表和完成判断承载，审查按知识的完成判断核，同样够得着。

**机制不预设知识的篇章结构**。方案写「读它给写统计设计的作者的那一篇」「那一篇末尾的完成判断」。目标仓的埋点知识不一定分篇、完成判断不一定在末尾，这里改为「重读它写给统计设计作者的部分」「按那份知识写的完成判断」。「完成判断 / 怎样算用对了」是 protocol 对项目方法型事实的写法，不是这一份知识独有的。

**知识里去掉了 Case 材料原句**。方案 3.1 用「发起、接受、撤销、失效四类事件」「转化率不低于 8%」举例，这两句分别出自 car-key-sharing 的系统设计与 auto-topup 的产品稿。写进知识，下一轮实跑模型读到的就是自己题面的答案，测出的是记忆不是方法。改为中性的「提交、审核、撤回三类事件」「开通率不低于 30%」。原第 2 面身份示例 `BankCard_001_002`（金样题材）同理改为 `Wallet_COMMON_001_002`。

**编号规则 2 补一句**。下篇示例用步骤号 `000`、节点段 `00` 表示流程整体（与金样一致），而现行规则 2 只说「节点与子节点段 `00` 留给未细分」，示例与规则对不上。改为：「步骤号 `000` 与节点段 `00` 表示流程整体，子节点段 `00` 表示步骤未细分」。这是新写进「已定规范」的一条，来源只有金样，请设计者确认它是否就是本仓的分配约定。

**反模式只留完成判断没覆盖的**。方案每篇列四五条反模式，其中「同一事实两个流程各记一次」「远端状态按每次查询计」「只写不重报没有结算方法」与同篇的边界句、完成判断逐字重复。每篇反模式收成一行，只留完成判断之外的形状（上篇：按流程或事件类型分节只写观察链路、把上报实现写进统计设计；下篇：占位称待登记、同一方法同一结果两行、报点写在不发请求的方法上）。

## 2. 步骤 1：知识

- 结构：H2 四面名不变（加载器取面名仍是 统计设计 / 上报实现 / 编号分配与复用 / 业务扩展字段），每个 H2 的破折号后写读者与读完交出什么；上篇 1–5、下篇 6–11 按方案。
- `applies_when` 改为方案给的两句。
- 退出：第 1 面「一次尝试」三行表、第 2 面「校验验证码」三行表、规则 3 的「经项目设计评审确认，不称作已有分配」。保留：渠道表、身份与取值、BI 条件句、结算骨架与核对、规则 1/2/4、扩展字段全文。
- 非空行 69 → 100（估算 87）。多出的主要是下篇示例表 4 行、渠道表挪到第 5 步旁、四个词的定义段；不以行数评价，逐项对应方案 §3、§4。
- 核对：加载面名四个不变、`selfCheck` 无问题、上篇不含 `STEP_` / `十位` / `WalletFuncResult`（既有用例）；结算骨架最小桩复跑：三种结果各一条、内码 `0101020200/01/02` 与示例表第二行一致。
- 全文 grep「车钥匙 / 自动充值 / 银行卡 / BankCard / 8% / 四类」零命中。

## 3. 步骤 2：机制

| 产出 | 生产者 | 消费者 | 说明 |
|---|---|---|---|
| `groups[].lead` | stat-points | spec post_check、spec 任务包 | H4 到第一张表之间第一行非注释、非表格正文 |
| 门禁第四条 | spec post_check | 作者 | 「指标「X」标题与表之间缺一段——先写它衡量什么率或分布、要算它需要哪几类结果，再放表」 |
| 任务包 §5 | spec/author.mjs | spec 作者 | 走 /story：四句任务 + 已写时逐指标「定义段 有/缺；统计点 n 个」；不涉及时核依据；不走 /story 一行 |
| plan 任务包末三句 | plan/author.mjs | plan 作者 | 仅 `ready` 状态出现 |
| 审查两句 | pre_verifier | verifier | spec 片段按知识完成判断逐条核；plan 并列表后一句，仅 `ready` 状态 |
| 骨架「何时读」 | knowledge-use document | spec 作者 | 每份事实一行，多行自述续行缩进；顶部改为「按何时读判断用不用；不用的整份删掉，不必打开」 |

`author.md` 第 4 步埋点长句收为一句，指向任务包第 5 节（59 行不变）。

新增用例：定义段缺失（空行、只有注释）报、列表项算正文；`lead` 取值；spec 任务包四种情况（走 story 已写 / 缺定义段 / 未写 / 不涉及 / 不走 story）且第 5 节不含 `knowledge/`；plan 包与审查的自查句只在 `ready` 出现；spec 审查含完成判断句；骨架「何时读」单行、多行与 `readUse` 读回。既有用例「统计点列不必在首列」的夹具补了一句定义段——按新规则它原本就缺定义。

## 4. 要说清的一点

定义段门禁只挡「H4 下直接是表」这一种形状。上一轮两份 spec 的每个 H4 下都有一段（car「观察车主发起分享全流程…」、auto「观察用户进入自动充值入口后…」），按这条门禁都会通过。那一轮的问题是段里写的是「观察 X 链路」而不是可算的指标——这由知识上篇的定义、示例、完成判断与任务包的动笔前重读承担，门禁不判内容。下一轮若 H4 仍是流程名加一段观察说明，门禁不会拦，要看审查是否按完成判断报出。

## 5. 未验证

- 实跑（步骤 3）未做，需用户授权。
- 编号规则 2 新增的「流程整体 `000` / `00`」待设计者确认来源。

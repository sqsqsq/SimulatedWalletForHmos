# 步骤 1 返修交回（R1–R5，2026-09-25）

对应 [步骤 1、2 实施评审](../评审意见/2026-09-25-步骤1-2-实施评审.md) §4。提交 **adf39a13**。全量离线 **1380 passed、15 skipped、521 subtests**。机制 13074 → 13075（prompts_md +1：protocol 一条），五类在 target 内；知识域 516（3e6205b9）→ 526。

## R1 篇章形式

`event-tracking.md` 现为：

```text
# 埋点上报
# 上篇 · 统计设计（读者：写统计设计的作者、审查者）
## 统计设计 — 读完交出：…
### 1 先分清四个词 / 2 什么时候不做统计设计 / 3 应用步骤 / 4 示例 / 5 完成判断 / 6 反模式
# 下篇 · 上报落地（读者：实现作者、编码、审查、测试）
## 上报实现 — 读完交出：…
### 7 角色与落点 / 8 SDK 行为事实 / 9 使用约定（审查逐条核） / 10 示例 / 11 结构骨架与核对 / 12 完成判断 / 13 反模式
## 编号分配与复用 — 读完交出：…
## 业务扩展字段 — 读完交出：…
```

- 加载器取面名仍是四个（H1 不参与）；`applies_when` 两句不变，「上篇」「下篇」在 H1。
- 「什么时候不做统计设计」：三种情形各带依据写法，开头一句「『不涉及』三个字本身不是依据」。
- SDK 行为事实 3 条：Chart 的拼法与 `WalletEventID` 生成、平台类型；VOC 调用、本地日志、`VOC 另用标识`；导出位置与发送行为，字段 setter 与类型指向 `WalletHAReportBaseCBuilder`。
- 使用约定 6 条：候选就是具体值；结算一次；分类按语义（四个分类值与内码独立）；只带拿到的（外码、耗时）；身份共用（同步骤共用、同语义同组常量、BI 走 BI 接口）；BI 选参（含借内外码条件、`setReportParam` 只放标准字段之外的维度）。两节不互相包含：前者是代码做了什么，后者是设计要守的选择。
- 编号面开头补「（Demo 拟定；目标仓以自己的登记为准）」；内码「十位、前导零保留」并到这一面的格式句里（原先在取值里重复一次）。

## R2 机制与指南的角色词

- `hooks/plan/author.mjs` 第二句、`hooks/shared/pre_verifier.mjs` 并列表后那句：「尝试由谁持有、由哪个方法结算…」→「项目知识点名的角色在契约里都有承载它的实体与方法」。
- 内网指南「上报实现」行改为「按自己的实现点名角色（例如谁持有一次尝试、谁结算结果、常量登记在哪）」。
- grep「尝试」「结算」在 `hooks/**/*.mjs`、`rules/`、`skills/story/phases/` 零命中；plan 模板示例的 `submit` / `settle` 保留。

## R3 protocol

- §四「项目方法」句后加：「读者分阶段时，可以像模式那样用『# 上篇 · …』『# 下篇 · …』两个一级标题分篇，面仍是二级标题。」
- 写作粒度那句放在 §三「共同写法」作一条（**写到模型找得到为止**），没放在 §四「项目方法」后：它对已有实现类事实、规约同样适用，放在项目方法后面会被读成只约束方法型事实。措辞按评审原文。

## R4 压回

event-tracking 100 → **80** 非空行（目标约 70；新增了「什么时候不做」与两个 H1、事实与约定分节的标题）。按 01 §6a：渠道表改一句（第 5 步）；上篇示例三行（校验验证码、开通结果、开通全流程）加两条边界；SDK 事实三条；下篇示例两行（校验验证码三种结果、开通全流程）；骨架改为一句加核对；编号与扩展字段未改写。

对照 01 §3、§4 逐项在：四个词、不做的三种情形、五步、边界（尝试、未进入不记、同一事实一处、远端按首次得知、页面进入点击）、上篇完成判断四条与反模式一行；角色三个、SDK 事实、约定六条、下篇示例十位内码、骨架与核对句、下篇完成判断四条与反模式一行、分配规则四条、扩展字段全文。

结算骨架改成一句后没有代码可跑最小桩；核对句（三种结果各一条、重复回调一条、重输再一条）保留。

## R5 工程能力第 1 面

四条十行压成三条：Ability 入口与 `exported`、宿主唯一 `@Entry` 与 `Navigation`；`NavDestination` 按名字 `pushPath`、宿主映射到模块 `@Builder`、新增页面改哪三处、`route_map.json` 不算登记；取栈用 `NavPathContext`。其余六面已是一两句一条，按同尺度看过，未改。

## 测试

- `test_knowledge_use::TheReportingFactsStayInTheirLane::test_every_channel_has_its_row` 改名为 `test_every_channel_is_named_with_its_status`：渠道表改成句子后，按四个渠道名与「当前实现 / 已定规范」都在统计设计面里核，不再核表格行形。
- 其余用例未改；`test_chart_only_semantics_stay_under_chart` 仍通过（上篇与 VOC 句不含 `STEP_`、`十位`、`WalletFuncResult`、`FuncID_SubFuncID`）。

## 未验证

步骤 3 实跑，需用户授权。

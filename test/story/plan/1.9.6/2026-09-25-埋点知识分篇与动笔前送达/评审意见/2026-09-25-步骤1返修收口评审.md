# 步骤 1 返修收口评审（R1–R5）

2026-09-25，维护设计者。对象：adf39a13 的完整 diff、现行 `event-tracking.md`、`engineering-capabilities.md` 第 1 面、protocol 两句、两处机制文字、内网指南一行、[返修交回](../实施反馈/2026-09-25-步骤1返修交回.md)。未改产品、未起模型。

## 1. 结论

**返修通过，R1–R5 关闭；步骤 1、2 收口，进入步骤 3 准备，起跑等用户授权。** 知识现在与模式知识同一种读法：两个一级标题标篇与读者，二级标题是面，上篇有「什么时候不做」，下篇分事实与约定，两篇各有完成判断与反模式；机制里没有任何一份知识的角色名与编码概念。

## 2. 逐项核

| 项 | 核到的事实 | 结论 |
|---|---|---|
| R1 篇章 | H1 两篇带读者；H2 四面名不变，加载器取面名仍是四个，`selfCheck` 0 问题；§2「什么时候不做统计设计」三种情形各带依据写法，开头即「『不涉及』三个字本身不是依据」；§8 三条只写拼法、平台类型、VOC 标识、导出与发送行为，setter 一句指向 `WalletHAReportBaseCBuilder`；§9 六条全是设计要守的选择，与 §8 不互相包含 | 关闭 |
| R2 角色词 | plan 任务包第二句、pre_verifier 并列表后一句改为「项目知识点名的角色在契约里都有承载它的实体与方法」；内网指南改为「按自己的实现点名角色」；hooks / rules / phases 里「尝试」「结算」「内码」「流程号」「ChartCodes」零命中（coding 作者页对模式「下篇」的引用是 protocol 规定的结构，不算） | 关闭 |
| R3 protocol | 分篇写法加在「项目方法」句后；粒度句放在「共同写法」作一条「写到模型找得到为止」——比方案放的位置更对，它对实现类事实与规约同样适用 | 关闭，位置按实施者 |
| R4 压回 | 100 → 80 非空行。多出目标约 70 的部分是新增的两个 H1、「什么时候不做」一节与事实/约定两个小标题，都是本次要求加的结构，不是废话。逐项对照 01 §3、§4：四个词、不做的三种情形、五步、边界五条、上篇完成判断四条与反模式一行、角色三个、SDK 事实三条、约定六条、下篇示例两行带十位候选、骨架一句加核对、下篇完成判断四条与反模式一行、分配规则四条、扩展字段全文，一条不丢 | 关闭 |
| R5 导航面 | 三句：入口与宿主唯一 `@Entry`/`Navigation`；`NavDestination` 按名字 `pushPath`、宿主映射到模块 `@Builder`、新增页面改三处、`route_map.json` 未被引用不算登记；取栈用 `NavPathContext`。上一轮 plan 错的两件（多登记两个文件、漏页面导出）都被这三句盯住 | 关闭 |
| 测试 | 结算骨架改为一句后最小桩用例退出，核对句保留——约定由 §9 第 2 条与 §11 承载，够审查核；渠道表改句子后用例改为按四个渠道名与两种现状核，不核表形。独立复跑六个文件 198 passed、73 subtests | 接受 |
| 规模 | 机制 13074 → 13075（protocol +1），在 target 内；知识域 516 → 526 | 接受 |

## 3. 一处可再收、不阻断

「业务扩展字段」一面把八个 setter 名逐个列出，再列七个要确认的含义问题。按「写到模型找得到为止」，两句就够：`WalletHAReportBaseCBuilder` 里其余业务字段 setter 的含义本仓无依据；要用时先取得项目参数规范或负责人确认含义、取值集合、必填条件、来源与隐私处理，只取要用的。留给下一次动这份知识时顺手做，不为它单开一次返修。

## 4. 步骤 3 准备

按 [03](../03-实跑与发布判定.md)：基线 adf39a13；两 Case 同终点、同模型 `bailian-deepseek`；宿主按 interaction-script 交料（auto 含会议记录）、只答所问；起跑前按 TEST 保全 `doc/features` 并记 HEAD；记耗时、休眠时段、停摆次数、派审次数与 `verifier_closure`。评审只看 J1–J4。起跑要用户明确说启动。

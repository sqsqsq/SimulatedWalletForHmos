# 08 A 返修收口评审

2026-09-24，维护设计者。对象：8bdd386a → dfec602b 的完整 diff 与[返修交回](../实施反馈/2026-09-24-08A-返修交回.md)。承接[上轮评审](2026-09-24-S3返修与08A-实施评审.md)的 A-F1。

## 结论

**A-F1 关闭，08 A 通过，可以进入 B。** 未发现本次返修引入的新阻断项。S3 已通过的确定性返修结论保持；本次不是 B 的知识与 adapt 内容验收，也不代表模型效果通过。

## 实际核对

- protocol 将确认含义改为按依据类型核实，区分当前实现、已定规范、示例与候选；不再以有无代码决定确认状态。
- Spec 作者提示、knowledge-use 骨架注释、缺 verified 的错误修法同步为代码/配置或协议/需求/负责人确认记录的位置，生产者与消费者一致。
- verified 字段及原必填条件未变，没有通过放宽“未确认必须核实”来消除冲突，也没有增加脚本对证据真实性的伪判断。
- 部件画像关于当前结构的代码/DSL 取证仍保留其适用范围。

## 独立验证

设计者定向复跑 test_knowledge_protocol.py 中 `confirmed_rule_without_code` 与 `unconfirmed_facet_needs` 两项，使用 `-n auto --dist loadscope`、工作区独立临时目录和禁用 pytest cacheprovider。

结果：**2 passed，45.87秒**。这证明规范依据文本可以被现行字段接受，以及缺核实依据仍被拦；依据是否真实、模型是否正确运用仍属语义审查与最终实跑。

相关旧措辞检索无残留，diff --check 通过。1377 passed、15 skipped 为执行者全量交回，本次未重复全量。未启动真实模型，未修改产品实现。

## B 交接

以 dfec602b 为已接受的编写与消费契约基线，按08及07样例分册实施：reporting 的两层应用方法与贯穿参考、已登记的其他知识实际缺口、模式坐标迁移、adapt 消费任务驱动的写作与走查。八份规约无实际缺口时保持，不套模板扩写。

B 要让知识真正承担项目方法，随后收束作者页的重复长段；先有接替内容再退出旧说明。实施后交实际内容、消费者接线、旧路径退出及累计新增/退出预算，逐步评审；整版完成后统一按授权实跑。

# 综合返修收口评审

2026-09-25，维护设计者。对象：5eed351c → 3e6205b9 的完整 diff、当前加载器与知识、[返修交回](../实施反馈/2026-09-25-综合评审F1-F3返修交回.md)。承接[综合评审](2026-09-25-多轮优化综合评审.md) F1–F3。未修改产品或启动真实模型。

## 1. 结论

**本轮返修通过，F1–F3 关闭，可进入整版正常业务测试准备。** 未发现阻断本次收口的新问题。这是实现和定向验证结论，不是最终模型产物已经达标。

用户最新决定：adapt 不在本仓追加测试，由用户在内网真实仓验证与反馈，不影响1.9.6交付。本轮确认该边界，并同步09及当前需求入口；原中性目标仓适配演练退出后续计划。

## 2. 实际改动核对

| 项 | 复核事实 | 结论 |
|---|---|---|
| F1 自述解析 | frontmatter 用既有 YAML 读取器解析一次，三类知识消费同一映射；applies_when 严格取非空字符串，引号/折叠/保留换行按真实值交付，guide 续行缩进；语法和文件错误仍汇总 | 关闭 |
| F2 BI 条件 | 已恢复有项目协议依据时可借内外码表达相近业务含义，明确运营语义、来源与消费约定；默认不用及无需求不新增仍保留 | 关闭 |
| F3 导航知识 | 明确 Ability→宿主Entry/Navigation→navDestinationMap→模块builder→NavDestination；route_map 的文件存在与当前引用关系分开说明 | 关闭 |
| 交付清理 | 两份临时pyc已从跟踪文件移除，__pycache__加入忽略；manifest和维护约定中的旧台账概括已调整 | 接受 |

另外核了实际源码：PhoneAbility.loadContent('pages/index')、宿主唯一Entry/Navigation、WalletMain的两个NavDestination及对应builder。该核对证明文档与当前调用路径一致，不替代设备运行结果。

未发现通过恢复台账、登记或确认标记来修解析问题；现行职责仍为知识自述、机制交付、模型按任务应用。

## 3. 独立验证

设计者复跑 test_knowledge_protocol、test_knowledge_use、test_evidence_delivery，使用工作区独立 TEMP/TMP/basetemp，参数 `-n auto --dist loadscope -q -p no:cacheprovider`。

结果：**90 passed、26 subtests passed，19.78秒**。覆盖本次 frontmatter 读取变更及知识使用、上下文交付；diff --check 通过，git ls-files 未再列出pyc。没有重复全量。

1371 passed、15 skipped、509 subtests passed为实施者全量交回，本次按其来源记录。交回所述第一次全量失败与改动并行的因果未独立复现；本次定向验证未出现失败，不能将它说成本轮新发现的产品缺陷。

## 4. 后续范围

- B 的本轮已知问题完成收敛，准备既定正常业务 Case 的最终验证，核实际 Spec、Story、Plan、契约、审查和中间输入。
- 不启动中性目标仓 adapt 演练，不把缺该目标输入继续列为阻塞，也不要求用户交回内网适配结果后才能交付1.9.6。
- 已有离线和实现评审证据保留；不把“不再要求本仓适配实测”改记成“适配行为已实测通过”。
- 内网适配指南继续交付，实际使用问题按用户反馈处理。正常业务 CLI 仍按现行 TEST 与授权执行，本次评审未自动起跑。

# 1.9.6 整版实跑结果交回（2026-09-25）

用户授权「全部当前 Case」。suite `story-suite-20260925-004130`，基线提交 3e6205b9，配置 `bailian-deepseek`（未切换）。产物已 `finalize --promote` 回灌 `doc/features/AR90006`、`doc/features/ISSUE-206`，零冲突、零源码变更。宿主逐关记录在 `output/run-notes-20260925.md`，原始证据在 `output/story/story-suite-20260925-004130/`。

## 1. 结果总表

| Case | 终点 | 终态 | 门禁 | 用时 |
|---|---|---|---|---|
| auto-topup（AR90006） | spec | finished，目标到达 | post_check / story_build_check / harness_spec 全 PASS，verifier PASS | 2902 s |
| car-key-sharing（ISSUE-206） | plan | finished，目标到达 | 另加 harness_plan PASS，spec 与 plan verifier PASS | 28283 s（含约 6.5 小时机器休眠，01:17–07:52 两侧日志均空白） |

门禁全绿只说明结构与流程闭环；下面按 09 §4 的三项看产物本身。

## 2. 知识是否被选读

两个 Case 在 spec 起手都读了全部 13 份激活知识（runlog 中 read 调用：auto 1211–2579 行，car 777–1673 行），car 还读了 `protocol.md`。原因是 `knowledge-use init` 生成全部条目的判断骨架，逐条判断要求读原文；因此「按 `applies_when` 选读」这次没有被检验到——读全了，不是选对了。car 的 plan 阶段没有重读知识文件，埋点与导航设计依赖 spec 阶段的上下文与 `knowledge-use.yaml`（事实面登记齐全：部件申明、交互方清单、对外入口与页面导航、数据存储、配置项、敏感数据处理、资源、本地日志、统计设计、上报实现、编号分配与复用）。

## 3. Spec 统计设计

### auto-topup（`AR90006/spec/spec.md:358–397`）

做到的：按签约流程与管理流程分节；每点写实际业务结果与本端获知时机（响应、回调）；扣款、入账、停用判定写明远端产生、由云侧统计；一次尝试一条、重试为新尝试；无运营需求不加 BI；隐私边界；创建失败的「策略待定」对上了未决议题 T6。

偏差：

- `:369` 入口展示/隐藏列为统计点（「视是否独立统计再定」）。知识第 1 面写「页面进入与点击说明不了业务结论」，这一点不该进统计设计，也没有给出它支撑哪个指标。
- `:360` 开头段写了登记文件名 `AutoTopupChartCodes`、setter 名与十位内码形态，这些是第 2、3 面给 plan 的内容，出现在 spec 里。
- 统计点用 `auto_topup_signup_policy` 这类自造标识命名；项目身份是模板 / 流程号 / 步骤号，spec 里用业务名即可。
- 渠道只用 Chart，没有说明过程关键点（如验证失败定位）是否走 VOC。

### car-key-sharing（`ISSUE-206/spec/spec.md` §9.4）

做到的：分发起、接受、撤销、失效四个流程；一次尝试与回调重复的边界；字段最小化（不带车牌、账号、位置）；BI 未定不加。

偏差：

- `:393` 与 `:401` 两个点是同一个业务事实（接受成功 / 凭据下发失败），分在两个流程里各记一次，违背「共用事实只采集一次」。接受动作发生在被分享人一侧，本需求范围是车主侧（`:32`），接受侧说明材料本轮未交付；这两点的获知时机也写成「待定 / 回调或状态查询」。
- `:417` 作废与到期由车云产生，spec 写成「进管理页 `queryKeySharingList` 状态字段回显」时本端获知。知识第 3 步允许写「状态查询里知道」，但没说清「每次查询都会重复看到同一远端状态」——模型按字面用了。

spec verifier 两个 Case 都 PASS，上述偏差都没有被审出来。

## 4. Plan 字段规则落地（car，`ISSUE-206/plan/plan.md:296–351`）

做到的：每个结果落到责任方法（`KeyShareRepository.*`）；结果分类取值；外码取实际接口错误码；模板复用 `Wallet_COMMON`；登记文件 `shared/ha/KeyShareChartCodes.ets` 进了 `contracts.yaml:480` 的文件清单；尝试与业务幂等键分开（`:314–315`）。

没做到的：

- **编号没有成为具体候选**。流程 001–004、步骤 001、结果段 00 / 10 给了，但十位内码其余各段与失败结果写成「内码失败号」「离线号」「作废号」，`:349` 以「缺依据说明」挂起并称 `KEY_SHARE_xxx 占位示意`。知识第 3 面第 2、3 步要求在父范围取未用号作**具体候选**并标「待登记」，首次使用时连同依据一起提出评审——这里有依据（仓内零登记，知识已写明），不属于缺依据。
- **一次尝试一条没有设计出实现**。表里写「同一尝试重复回调不再报」，但没有尝试对象或单一结算方法的设计（知识第 2 面的约定与骨架），落地者无从照做。
- `:322` 用户取消的责任方法写 `createKeySharing`，同一行又说取消时不发请求——报点应在取消动作所在的方法。
- `:328–331` 继承 spec 的重复：同一方法同一结果报两条事件，与本表自己的「一次尝试一条」矛盾。
- `:346–347` 每次进管理页按状态各记一次，同一张作废单会被反复计数。

plan verifier PASS，同样没有审出。

## 5. 导航知识的应用（car）

- spec Scope 把 Phone 列入 in_scope，理由是新页面的名字分支要加在宿主 `navDestinationMap`——F3 改写后的事实被用上。
- plan `:368` 写「注册三处：`index.ets` 导出 + `route_map.json` / `main_pages.json` 登记 + Phone `navDestinationMap` 分支」，`contracts.yaml:362–363、483–484` 也把两个 profile 文件列为修改。知识写的三处是「页面文件导出 builder、模块 `index.ets` 导出、宿主分支」，并说明 `route_map.json` 未被引用。结果：宿主分支对了，另外多登记了两个不参与查找的文件，页面文件导出 builder 没作为一处列出（`:129` 目录规划里有两页文件）。

## 6. 过程观测（与知识无关）

- **framework 绕路**：car 进 spec 时在 `framework/harness` 下跑 `check-personal-setup --ensure` 未带 `--project-root`，报 `no_materialized_adapter`，模型转去 `/framework-init` 要人选初始化选项。已核：带 `--project-root ../..` 时读得到七个 adapter。宿主回 answered「不用初始化，这个工程早就初始化好了，继续做需求。」这是 framework 行为，extension 不改。
- **模型退化**：car 五次在需要发工具调用时连续输出几十段「Running now / emit the tool call / 发射」文字后结束本轮、没有提问。第一次按用户当场裁定回 neutral「继续」，后四次同法处理。auto 没有出现。
- **宿主话语**：planned 7 条（材料 ×2、会后裁决、范围、术语、car 评审意见），answered 3 条（car 范围、拒绝初始化、进入 plan），neutral 5 条，improvised 0 条。auto 规划中的「更新接口 / 开关 / 按月筛选」没被问到，没说；car 的撤销时限在 review.md 议题 D1 请评审人裁定（模型建议 48 小时），宿主在评审意见里按规划答 24 小时，模型随后同步到 spec、验收与 story。
- 内存不足时 Claude Code 结束了宿主的后台轮询脚本，car 已在 plan 收尾，终态由一次手动 poll 确认，未影响被测进程。

## 7. 请设计者判断

1. **知识是否要补**：状态查询回显的远端状态怎样计次（`:417` 与 plan `:346`）、同一事实跨流程只采集一次在跨端场景（接受在对方一侧）怎么写——是知识第 1 面表达不够，还是模型未应用，需要设计者定。
2. **编号候选**：plan 把「首次使用要评审确认」理解成「不给具体值」。第 3 面第 3 步的写法是否要改成更直接的「给出具体候选值，标待确认」。
3. **导航第 3 条**：「新增页面写三处」被读成包含 `route_map.json`。是否把三处写成带路径的清单，并把 `route_map.json` 那句移到清单之外。
4. **审查**：spec 与 plan verifier 都 PASS，第 3、4 节的偏差没被发现。审查任务书对埋点只核了「写了没有」还是也核了语义，需要设计者按 08 的审查职责复核。
5. **选读**：`knowledge-use init` 让 spec 读全部知识，`applies_when` 的选读作用本轮不可观测；是否接受这一点作为现行设计。

## 8. 未验证

- 未跑 coding，埋点与导航设计没有落成代码。
- auto 只到 spec，Plan 字段落地只有 car 一个样本。
- 模型退化是否与本版改动有关无法判定：本轮只有 car 一个 Case 出现，我没有核对历史轮次是否有同类现象。

# 08 A 返修交回（2026-09-24）

回应 [S3 返修与 08 A 实施评审](../评审意见/2026-09-24-S3返修与08A-实施评审.md) A-F1。提交 **dfec602b**，只动协议与三个直接消费者。

## 改动

| 位置 | 原文 | 现文 |
|---|---|---|
| `reference/knowledge/protocol.md` 第三节 | 已确认 = 已对仓核实；未确认要写核实时看的仓内路径；每条事实带仓内路径或键名 | 已确认 = 按依据类型核实过，有没有代码不决定确认与否；未确认写核实依据及位置。每条内容带可追溯依据：当前实现引代码 / 配置位置或键名，已定规范引协议、需求或负责人确认记录，示例与候选写假设与来源 |
| `hooks/spec/author.md` 项目事实一条 | 补 `verified` 写核实时看的仓内路径 | 补 `verified` 写核实依据及位置——当前实现引代码或配置，已定规范引协议、需求或负责人确认记录 |
| `knowledge-use/document.mjs` 骨架注释 | 同上旧句 | 同一说法 |
| `knowledge-use/validation.mjs` 缺 verified 报错 | 同上旧句 | 同一说法；`verified` 字段与必填校验不变 |

保留：`capabilities.md` 的 `component-profile` 走查仍要求仓内路径或架构配置键名（部件画像的当前结构取证，评审 §3 第 4 条）。

## 完成条件核对

- 当前实现（已有面）：依据引代码或配置，写法不变。
- 已定未实现的规范：`verified` 写负责人确认记录即合法——`test_knowledge_protocol::test_a_confirmed_rule_without_code_is_valid_basis`。
- 未确认内容：缺 `verified` 仍报错，修法同时说两种依据——同一用例与原 `test_an_unconfirmed_facet_needs_where_it_was_checked`。
- 全量离线 1377 passed、15 skipped。脚本只核有没有写，依据是否真实由审查判。

预算：机制行数不变（四处各一行替换）。

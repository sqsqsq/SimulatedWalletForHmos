# 综合评审 F1–F3 返修与交付清理交回（2026-09-25）

对应 [多轮优化综合评审](../评审意见/2026-09-25-多轮优化综合评审.md) 与 [09 分册](../09-知识自描述现行设计与返修交接.md) §2。提交 **3e6205b9**。

## F1 frontmatter 按 YAML 读真实值

`doc/extensions/hooks/shared/knowledge.mjs`：

- 删掉单行正则的 `frontmatterPairs`，改为 `frontmatterOf`：复用 `yaml.mjs` 的 `parseYaml`（与 manifest 同一读取器），一个文件只解析一次，`name` / `kind` / `domain` / `coordinator_role` / `roles` / `optional_roles` / `applies_when` 都取自同一份已解析映射；三类解析函数改为接收已读好的正文与 frontmatter。
- `applies_when` 必须是去空白后非空的字符串。缺失、空串或空白、非字符串（列表、数字）分别报出文件名与修法；frontmatter 不是合法 YAML 或不是映射时报出文件名与解析器的第一行错误。这些都进既有的 `bad` 汇总，后续文件照常核。
- `knowledgeGuide` 对保留换行的用途把续行缩进，列表项不断开。
- 在改 F2 / F3 知识之前，用新旧加载器各加载一次 Demo 13 份知识，输出 JSON 逐字节相同。

新增用例（`test_knowledge_protocol.py::KnowledgeDescribesItself`）：

- `test_every_legal_yaml_form_reaches_the_reader`：引号内带 `: `、`>` 折叠、`|` 保留换行三种写法，解析结果与作者包列出的都是真实文字。
- `test_an_empty_or_non_text_description_is_named`：`""`、`'   '`、只写键、列表、数字五种各报对应原因。
- 多错误汇总用例加一份非法 YAML 的文件，与缺文件、缺 kind、缺 applies_when、未知 kind 一次报全。

## F2 BI 借用内码 / 外码的条件恢复

`event-tracking.md` 第 1 步取值的 BI 句补回：项目协议允许时可借内码、外码表达相近的业务含义，同时写明运营侧的含义、取值来源与消费约定，不沿用运维解释。依据 `plan/1.9.5/2026-09-21-chart上报知识应用/06-埋点单处承载裁定.md` 第 49 行。默认不带、无运营需求不新增保持原句，没有加例子。

## F3 页面导航按当前生效的路径写

`engineering-capabilities.md` 第 1 面改名「对外入口与页面导航」，分四条：

- Ability 是外部拉起入口（`module.json5` 的 `abilities[]`，`PhoneAbility` 加载 `pages/index`）；
- 宿主页 `pages/index.ets` 是唯一 `@Entry`，持有唯一的 `Navigation` 与导航栈并 `attach`；
- 业务页面是 `NavDestination`：`pushPath({ name })` → 宿主 `navDestinationMap` 按名字调模块导出的 `@Builder` → 页面根节点 `NavDestination()`；新增页面写三处；
- 取栈：Tab 首屏子树 `@Link`，二级页 `NavPathContext.stack()`。

取证与走查：`HomeTabPage.ets:74` `pushPath({ name: 'CardPackPage' })` → `01-Product/Phone/src/main/ets/pages/index.ets:15–20` 的 `navDestinationMap` → `02-Feature/WalletMain/src/main/ets/index.ets:7` 导出的 `buildCardPackPage` → `CardPackPage.ets:30` `NavDestination()`；宿主第 82 行 `.navDestination(this.navDestinationMap)`。

**更正前一次交回**：`2026-09-24-08B-解耦与知识整改交回.md` 第 15 行称 `route_map.json` 只在 `build/` 产物，事实不成立。`02-Feature/WalletMain/src/main/resources/base/profile/route_map.json` 是 Git 跟踪文件，列着 CardPackPage、AddCardEntryPage；但 `WalletMain/src/main/module.json5` 没有 `routerMap` 键引用它，当前名字查找由宿主 `navDestinationMap` 承担。知识里写了这层关系一句，没有要求新页面照它登记。那次是我只查了 Phone 模块就下结论。

同面的 `applies_when` 补上「页面导航」。

## 交付清理

- 退出误提交的两份 `test/story/tests/__pycache__/*.pyc.<pid>`；`.gitignore` 在 `**.pyc` 旁加 `**/__pycache__/`，这类 pytest 临时写入不会再被带进提交。
- `test/story/AGENTS.md`：Adaptation 行改为「脚本按目录所有权装机制、不读写知识；首装由模型写部件定位知识，其余按方法页从目标仓自己出发写」，禁区改为「替目标改知识、灌入或对照 Demo 知识」；§3.3 写明 manifest 只登记路径、知识用 `kind` 与 `applies_when` 描述自己，删掉「能力台账」。
- `doc/extensions/manifest.yaml` 激活清单注释里的「能力台账」同步删掉，改为指向 applies_when 与 protocol.md。

## 验证与规模

- 定向：`test_knowledge_protocol` 中 `KnowledgeDescribesItself`、`TheProtocolIsCheckedOnLoad` 7 passed、13 subtests passed。
- 全量：`python -m pytest test/story/tests -n auto --dist loadscope` **1371 passed、15 skipped、509 subtests passed**（最终工作树）。第一次全量与知识改动并行，`test_story_update::TheInputsStageAsksFirst::test_a_new_original_in_the_inbox_is_a_change` 失败一次，单独重跑与最终全量都通过，归为运行期间改文件所致。
- 机制 13032 → 13036（+4：YAML 解析与三种错误原因）；知识 514 → 516（BI 条件一句、导航面由 2 条改 4 条）。

## 未验证

- 内网真实知识（多行 `applies_when` 等写法）加载只在临时中性工作区验证过。
- 导航面的改写只做了源码走查，未构建运行。

## 用户裁定（本轮）

「adapt 你不用测试，我会在内网真实代码仓验证，这个不影响 1.9.6 交付，有问题我会反馈。」——请设计者从 1.9.6 的后续计划中去掉中性目标仓适配演练（原 S4 / 09 §4「合法中性目标适配」），adapt 的验证以用户内网反馈为准；整版正常业务测试与真实 CLI 实跑授权照旧。

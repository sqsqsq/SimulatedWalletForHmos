# 执行者提问（2026-09-24）

维护实施者读完 00–04、版本范围与两份需求，对照代码核过 S1 引用的事实，均成立：

- `pre_verifier.statPointTable` 用单值 Map（`hooks/shared/pre_verifier.mjs:67`）；
- `specStatPoints` 按第一列取统计点（`hooks/shared/stat-points.mjs:36`）；
- `decisionSection(contract)` 只收章节合同（`hooks/spec/author.mjs:160`）；
- `decisionList` 读两种形状（`core/story/review.mjs:36`）；
- `indicatorShape` 按全部表行计数（`hooks/spec/post_check.mjs:364`）；
- `isStoryFeature` 可区分走 /story 与直接写 Spec（`hooks/spec/post_check.mjs:410`）；
- `knowledge.mjs` 的 `indexes` 分支（`hooks/shared/knowledge.mjs:398`、`:421`）；
- 附录占位在 `core/story/chapter-contract.mjs:298`；
- S2、S4 点名的测试文件都存在。

基线 HEAD 190f274b，工作区干净。设计可以实施，下面三件事请设计者定。

## 事实：R05 是宿主漏投

- auto 交互脚本第一条回复要求同时交两份文件：`交通卡自动充值.docx` 与 `交通卡澄清会记录-0903.docx`（`test/story/cases/auto-topup/interaction-script.yaml:13-16`）。
- 第四轮宿主（本实施者）只投了产品稿。suite `story-suite-20260924-1234` 里 auto 的 `runlog.md` 中，「澄清会」「0903」一次都没出现，说明会议记录没有送到模型。
- 17 号交回 §2 写的「auto 没要会议记录」不对：模型在第一个关卡就要了材料，是宿主投漏了。
- 按 01 §3 的分支，R05 是宿主执行偏离，按现有证据不改产品、不改装置。17 是已完成轮次，不回头改；这条更正写进 S1 的 R01–R08 处理表。

## 问 1：预算台账怎么开

台账现为 `status: closed`、target 12894；closed 状态下 S1 一加行，`test_mechanism_budget.py:134` 就会失败。

00 §4 只给了机制总量（完成后 13334，峰值 13594），没给五类（prompts / hooks / data / py / mjs）各自的 `interim_ceiling` 与 `target`，又写了「不自动修改旧预算台账上限」。S1 开工前台账要改成 1.9.6 在途状态。

请选一种：

- 设计者给出五类额度；
- 或授权实施者按 S1 +300/-160、S3 +380/-100 的职责去向拆出五类额度，交评审确认。

## 问 2：「图后判断」的源头文件是否纳入 S1

01 §5 的文件表只列了 `phases/story-write.md`。三行图指引实际由 `core/story/drafts.mjs` 的 `diagramHint` 生成，第三行是「逐条：这条分支里读者要知道的条件、责任、后果；只有一条路径时一句收束」。`images.mjs` 的提示也是同一口径。

17 号交回把「图后判断：」追到这组指引。只改 story-write 会留下这个源头。实施者理解 `drafts.mjs` 与 `images.mjs` 两处应一起改，请确认是否列入 S1 文件动作。

## 问 3：附录占位的替换写法

01 §5 只说把「这一节登记……」替换成「要说明的具体业务边界/约定」，没给新的占位句。上一版占位本身带「这一节」，被模型照抄进了附录。这句是机制措辞的源头，请设计者给出定稿。若授权实施者自拟，按「说内容、不说文档」写，交评审核。

## 不阻塞，仅记录

- S2 真实跑两个 Case，需另取用户授权。
- S4 需要一个非 ArkTS、非钱包的中性目标仓，现在没有现成的，届时先报缺什么输入。
- S1 交回只报离线结果与 R01–R08 处理表，不宣称效果达标，效果由 S2 验证。

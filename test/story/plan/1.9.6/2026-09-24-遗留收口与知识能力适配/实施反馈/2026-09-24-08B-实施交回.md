# 08 B 实施交回（2026-09-24）

依据 [08](../08-机制与知识的消费协作设计.md) §5–§7、[07](../07-上报知识应用组织优化方案.md) 与 [08 A 返修收口评审](../评审意见/2026-09-24-08A返修收口评审.md) 的 B 交接。基线 dfec602b，提交 **eb035dcf**。未启动真实测试。

## 1. 用户在实施中的纠正

- 「为什么舍不得按方案重写 reporting.md，而是局部打补丁」：我起初按 07 §5 第 1 步「保留前六面、替换第 7 面」只换了第 7 面与导读一句，前六面仍是按字段分类的旧叙述，不符合 08 §3「每份知识按读者的问题与使用过程组织」。已改为整份重写：保留的是有消费者的结构（七个面名、`confirmed`、业务扩展字段未确认）与已核事实，叙述从 0 写。
- 「codebase-facts 也有相同的嫌疑」「文件名起的也不好」：同样整份重写；它讲的是本仓各类工程基础能力怎么做、何时复用，改名 `engineering-capabilities.md`，标题「工程已有能力」。

## 2. 改动

| 对象 | 改动 |
|---|---|
| `knowledge/facts/reporting.md` | 开头：什么时候读、帮你做哪两件事、各面在哪、依据怎么标。第 1 面按统计对象之间的关系讲（指标与流程、流程步骤子点、尝试、本端何时知道、交互记录不是业务结论）；第 2 面渠道表加「依据」列（当前实现 / 已定规范）；第 3–6 面各先说回答什么再给事实，分配规范编号成步骤；第 7 面两层参考（07 §2–§3）：业务设计五步 + 服务开通例子与选点理由；实现设计五步 + 「校验验证码」两种结果完整候选（`0101020200` / `0101020201`，标明示意、未占号）+ 约 20 行调用骨架（待建设计与现有接口分标）+ 五种情形的预期事件 + 用对了的判断 |
| `knowledge/facts/engineering-capabilities.md`（原 `codebase-facts.md`） | 开头写用途与读法、技术栈与导出约定；八面各以「需求要做 X 时看这一面」起，给入口与照写的约定，写明本仓没有的（网络层、`preferences` / 分布式 KV、远程配置）是建设差距或新引入。面名不变 |
| 两份模式 | 上篇 §3「契约投影」改「各部分承担什么」，只讲持有者、内部步骤、对外业务状态、完整路径、数据边界在设计里是什么；应用步骤第 4 步改为「交给契约」不写字段；删去「按 README 的组合合同」（README 已退出），改为写清组合要回答的五件事；§8 按完成对象（选型设计 / 实现 / 审查关注 / 业务用例 / 真机验证）写完成判断；「3.1 plan 应用步骤」改「3.1 选型设计的应用步骤」。读者标签保留（评审允许） |
| `component-profile.md` | 核过正文已按「判上游内容是否相关」组织，只补开头用途段 |
| spec / plan 作者页 | 第 4 步与「四、埋点」的长方法段收束为任务：按激活的统计与上报项目知识的业务设计参考 / 实现设计参考完成……；保留机制侧的边界句（结果写业务结果、不替人定分母、五类路径走查、候选与裁决、一点多行）。作者页不点名知识文件 |
| 模板 | spec §9.4 与 plan 埋点小节注明花括号按当前项目值完成、方法在项目知识 |
| overlay | spec、plan 各加「参考例子只示范方法，不以名称、编号、点数或行数当答案」；plan 加「实现核结果 → 责任方法与分支 → 身份与字段来源 → 调用 → 预期事件接得上」 |
| `capabilities.md` | 字段说明里「走查」改为按消费任务评价；`reporting` 使用参考为两层、走查同时用两层；`codebase-facts` 条目更名 `engineering-capabilities`，重写适用与走查，「变化」写更名迁移依据 |
| `knowledge-adaptation.md` | 第 4 步拆为「先定消费任务 → 取证并分类 → 形成叙述」（08 §5）；第 6 步改「按消费任务走查」；中性示例补消费任务、改写内容与走查结论 |
| manifest | 激活项改为 `engineering-capabilities.md` |

## 3. 旧路径退出与保留

退出：reporting 旧导读与字段式叙述、旧单表参考；`codebase-facts.md` 与其能力 ID（台账保留迁移依据）；模式里的契约字段坐标、按阶段名的验证清单、失效的 README 引用；作者页里与知识重复的长方法段。保留：七个 reporting 面与确认状态、工程能力八面、模式角色与上下篇、八份规约（清单核过无实际缺口）。`doc/features/` 下历史产物里的 `codebase-facts` 引用是历史记录，不改写。

## 4. 验证

- 全量离线 1377 passed、15 skipped（eb035dcf）；reporting 七面与未确认面、`selfCheck` 通过；知识不含阶段坐标与机制路径（`selfCheck` 第 4 项）；机制层零专名扫描通过。
- 两层参考接续的推演：业务设计表的「校验验证码 / 校验通过、未通过 / 校验响应」→ 实现表的同一结果 → 骨架里的分支与 Builder 调用 → 预期事件表，一条链可对上。模型实际使用效果留整版实跑。

## 5. 预算

| 域 | 基线（dfec602b） | 实测 | 物理新增 / 退出 |
|---|---:|---:|---|
| 机制有效行 | 13218 | 13221（data +3） | 作者页、模板、overlay、台账、方法页 +27/−23 |
| knowledge 非空行 | 616 | 647 | +189/−146（reporting +105/−47、工程能力 +48/−61、模式 +36/−37、画像 +1/−1） |
| adapt 非空行 | 747 | 762 | 方法页 +8/−7 |

08 §9 估知识新增约 90 / 退出 35，实到 +189/−146：整份重写把保留的事实也计成新增，按 08「重写段落计新增」如实记。净增 31，在 08 估的完成 671 内。

## 6. 请评审

- 文件名 `engineering-capabilities` 与标题「工程已有能力」是我取的，用户只说原名不好；如有更合适的请指示。
- B 之后是 C（验证与整版交付准备，S4）。

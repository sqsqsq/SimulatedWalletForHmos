# 08 B 知识去补丁整改交回（2026-09-24）

用户指出：除埋点外，其余知识都只是改表达，不是预算增长的理由；要审视是否在打补丁、旧形式有没有整改。本次按用户批准的方案整改知识侧；机制侧问题另见 [执行者提问](2026-09-24-执行者提问-机制耦合知识与确认标记.md)。

## 1. 逐文件：190f274b 基线 / 现在（非空行）

| 文件 | 基线 | 现在 | 说明 |
|---|---:|---:|---|
| 规约 8 份 | 19–24 | 各 +1 | 正文未动；+1 为一行能力登记（原 5 行压成 1 行，去掉缺省 `protocol: 1`） |
| design-patterns/decision-tree | 137 | 123 | 见 §2 |
| design-patterns/page-interaction | 122 | 111 | 见 §2 |
| facts/component-profile | 26 | 27 | 旧粗体句 + 追加句合成一句；确认标记删去；+1 为登记行 |
| facts/engineering-capabilities（原 codebase-facts） | 37 | 25 | 确认标记删去 |
| facts/event-tracking（原 reporting） | 57 | 82 | 唯一新增内容（07 两层方法与贯穿例子） |
| 4 份 README | 96 | 0 | 规则归机制 |
| **合计** | **639** | **540** | 目标 ≤556 |

此前「累计新增 415」的口径作废：它把表达重写计成增长。

## 2. 打补丁与旧形式的整改

- **能力登记**：每份 5 行 → 1 行 flow 写法；解析后的 declarations 与改前逐字段相同（`output/decl_before.json` / `decl_after.json` 比对一致）。
- **确认标记**：Demo facts 全部删去；业务扩展字段改为确定陈述（方法名与类型、本仓无业务含义依据、要用时向谁取证、写明哪些问题）。
- **component-profile**：开头的旧粗体句与追加句合成一句。
- **两份模式**：
  - 上篇 §3「各部分承担什么」表与 §3.1 应用步骤是同一内容讲两遍，合并为「3. 应用步骤」，角色职责与「内部步骤不是业务状态」的边界嵌进对应步骤；
  - §8 原「选型设计 / 实现 / 审查」三行与 §3、§4、§7 重复，只留 §7 之外的判断（覆盖枚举全部键、最小编译通过、SDK 事实与约定不混写 / 跳转上报提交各有所有者）与业务用例、真机验证两行；
  - frontmatter 删 `not_applies_when`（重复上篇 §2）与 `triggers_usecase_spec`（全仓无消费者）；
  - 反引号代码符号逐个核对，无缺失。
- **event-tracking**：改名（`git mv`，name 与能力 ID 同步，台账写迁移）；删开头导读的面名复述、「核已有覆盖」步（并进渠道表前一句）、「设计完成」行、「列预期事件」指向步、同次边界与约定的重复、核对段的重复。

## 3. 验证

- event-tracking 信息清单 59 项零缺失；骨架按同法重跑（`output/f2check3.mts`）：三种结果各一条、重复回调不增请求不增事件、重新输入再出一条。
- 模式代码符号零缺失；component-profile 部件名称与六类交互方保留。
- 13 份知识全部加载，`unconfirmed` 均为空，`selfCheck` 通过；`confirmed:` 在 knowledge 下零命中。
- 全量 1377 passed、15 skipped。

## 4. 未做（待设计者）

确认标记机制退出、protocol 第三节说明、机制耦合具体知识的解耦、能力 ID 缺省取 name——见执行者提问。

# 08 B 返修交回（2026-09-24）

回应 [08 B 实施评审与面结构裁定](../评审意见/2026-09-24-08B实施评审与面结构裁定.md)。提交 **24a68a83**（在 eb035dcf 之上）。

## 0. 预算确认

评审 §5 的知识域调整建议（累计新增 360 非空行、完成 ≤700、峰值 ≤740）已由用户在本会话确认（回答「确认，继续」）。

## 1. F1：四面按任务重组

| 面 | 确认 | 内容 |
|---|---|---|
| 统计设计 | 已确认 | 这一面回答什么 → 为什么报、渠道现状表（当前实现 / 已定规范）→ 指标、流程与一次尝试 → 结果与本端获知时机 → 应用步骤 → 服务开通例子（新增「请求失败」结果）→ 设计完成的判断 |
| 上报实现 | 已确认 | 输入是已定结果 → 找到责任方法 → 身份（就地讲，新值指向第 3 面）→ 结果与字段（就地讲，扩展参数指向第 4 面）→ 调用与同次结算 → 应用步骤 → 校验验证码例子（三种结果行、骨架、预期事件）→ 实现完成的判断 |
| 编号分配与复用 | 已确认 | 现有定义 → 两套命名空间 → 分配规范四步（复用与身份非流水号并入第 1 步） |
| 业务扩展字段 | 未确认 | 已核名称与类型 → 用到时取得项目参数规范或负责人确认，要核的具体问题 |

开头按两件任务导读，编号与扩展字段给入口。frontmatter：kind、protocol=1、能力 ID `reporting`、revision 1 不变，`covers` 改为四面。

同步：台账 `reporting` 条目的面与取证、走查按四面写，「变化」给旧七面 → 新四面映射及旧引用的处理（继续该需求时按实际内容重判，拆开的使用参考可能对应两面）；spec / plan 作者页与 spec 任务包的引用改为「统计设计」「上报实现（新值按其编号分配）」；`test_knowledge_use` 的面名与段落定位断言。`doc/features/AR90006`、`ISSUE-206` 的 `knowledge-use.yaml` 仍引用旧面名，未批量改写；继续这两个需求时现有校验会报面不在册，由模型重判。

## 2. F2：骨架

按评审定案分开「发起请求」与「处理结果并记录」：`checkCode` 只发请求，正常响应与 `catch` 都交给 `settleCheck`；`settleCheck` 先判本次尝试是否已结算，再取内码、分类与真实外码发送；重复回调只进 `settleCheck`，不再调网络；重新输入由调用方新建尝试。例子表新增「请求失败」行（候选内码 `0101020202`，外码取错误对象里的码，没有则不带），预期事件表新增通信失败与「回调重复到达不再发请求、不多出事件」。

确定性核对（`output/f2check.mts`，从知识原样取出代码块，给最小桩依赖运行，不入测试集）：通过 / 未通过 / 请求异常各一条事件（内码 `…00` / `…01` / `…02`，异常带错误码作外码）；对已结算尝试再调 `settleCheck` 不增加请求也不增加事件；重新输入后请求 +1、事件 +1。

## 3. 验证

全量离线 1377 passed、15 skipped（24a68a83）；四面解析、`业务扩展字段` 仍是唯一未确认面；`selfCheck` 通过；Chart 专有语义不在「统计设计」与 VOC 段（原测试改为按新面定位）。

## 4. 预算实账

| 域 | 本次 | 累计 | 确认额度 |
|---|---|---|---|
| knowledge 非空新增 / 退出 | +102 / −72 | 新增 369（267 + 102） | 360 |
| knowledge 总量 | 636 → 666 | — | 完成 ≤700 |
| 机制有效行 | 13221 → 13218（prompts −3） | — | — |

**累计新增超确认额度 9 行**：重组把「统计设计」「上报实现」两面全部改写，保留下来的事实也计为新增；未删有效内容凑数。请评审复核并请用户确认这 9 行，或指出可退出的具体内容。

## 5. 推进

F1 / F2 已交；预算超额待确认。通过后进入 C。

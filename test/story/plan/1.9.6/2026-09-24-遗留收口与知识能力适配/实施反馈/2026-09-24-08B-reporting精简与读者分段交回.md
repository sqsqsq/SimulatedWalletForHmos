# 08 B：reporting 精简与读者分段交回（2026-09-24）

在 [08 B 返修交回](2026-09-24-08B-返修交回.md)（24a68a83）之后，用户问：reporting.md 能否不丢功能与信息精简 30%；它是否也该像设计模式那样面向不同阶段的读者给说明，方案是否没要求。用户批准实施方案后提交 **7c4ebe7b**。

## 1. 对用户问题的答复（事实）

- **能精简**：24a68a83 每面先有几节讲事实，随后「应用步骤」又按步骤复述同样内容；例子的结果表与预期事件表三行重合；开头导读、面首「这一面回答」与两处完成判断互相重复。
- **方案只要求了一半**：07 §1 要求「业务设计与实现设计两层分别服务 Spec、Plan」，即「统计设计」「上报实现」两面；设计模式的下篇服务编码与审查，08 §4.2 表里 Coding、Review、UT/Testing 也读知识，但 07 / 08 都没有给 reporting 定下游读者的内容。这是设计缺口，本次在面内补上，请设计者确认。

## 2. 改动

| 位置 | 改动 |
|---|---|
| 统计设计 | 首句写读者（spec 作者与审查者）；渠道表后以五步为主线，指标与流程、尝试、获知时机、已有覆盖只算当前实现等语义就地嵌进对应步骤；例子表加两句选点理由与完成判断 |
| 上报实现 · 设计 | 读者 plan 作者与审查者；五步为主线，身份、结果分类、内码、外码、耗时、描述、自定义维度、BI 选参就地嵌进「定身份」「定取值」；例子表加「何时记录 → 预期事件」列，替代原来重合的事件表 |
| 上报实现 · 落地与核对 | 读者：写代码、审查、单测与真机测试的人；调用方式与导出位置；约定四条、反模式两条；骨架（请求与结算分离，与 24a68a83 相同逻辑）；结果表之外要核的三种情形 |
| 编号分配与复用、业务扩展字段 | 规则与待核问题不删，压缩措辞 |
| 台账 `reporting` 条 | 上报实现写两段读者；走查加「下游读者能据落地与核对写出实现并核对事件」；「变化」记同一修订内分两段读者、面不变 |
| 测试 | `test_chart_only_semantics_stay_under_chart` 的 VOC 定位改按「VOC 另用标识」那一句（VOC 标识已并入「定身份」一步），断言不变 |

四面、`confirmed`、`covers`、能力 ID、revision 1 不变。面名不能带读者标签（解析器会把整段当面名），读者写在各面或各小节首句。

## 3. 验证

- 132 → 92 非空行（−30.3%）；字符 14697 → 约 11300（约 −23%）。
- 信息逐项核对：渠道四行、身份与 VOC 标识、平台字段、六类字段与四个分类值、BI 选参、调用与导出、两套命名空间、分配规范四条、九个扩展 setter 与待核问题、例子全部结果与三个候选内码、幂等与尝试分离、重试与退出——无缺失（脚本清单核对）。
- 骨架：从知识原样取出代码块、给最小桩依赖运行（`output/f2check2.mts`），结果与 24a68a83 相同：三种结果各一条、重复回调不增请求不增事件、重新输入再出一条。
- 全量离线 1377 passed、15 skipped；四面解析与唯一未确认面保持，`selfCheck` 无问题，机制层零专名扫描通过。

## 4. 预算

| 知识域 | 本次 | 累计 |
|---|---|---|
| 非空新增 / 退出 | +46 / −75 | 新增 415（369 + 46），确认额度 360 |
| 总量 | 666 → 626 | 完成上限 700 |

累计新增超确认额度 55（上次 9 + 本次 46）；本次是净减 29、总量下降的精简。已如实告知用户，待确认。

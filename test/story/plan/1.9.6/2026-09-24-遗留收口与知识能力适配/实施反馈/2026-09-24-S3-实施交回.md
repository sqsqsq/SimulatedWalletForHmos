# S3 实施交回（2026-09-24）

依据 [03](../03-知识能力适配实现.md) 与 [S1 返修与 S2 收口评审](../评审意见/2026-09-24-S1返修与S2收口评审.md) 的 S3 交接。基线 2547cfe7；本步提交 **a0e88a29**。之前移出的草稿（`output/s3-draft/`）已按现行 03 与 S1 返修逐项复核后恢复，再补齐其余部分；草稿本身不当已验收交付。未启动真实测试。

## 1. 交付与位置

| 交付 | 位置 | 说明 |
|---|---|---|
| 读写规则 | `skills/story/reference/knowledge/protocol.md` | 接替四份 README 的通用规则：三类知识与激活；facts 的面、`confirmed`、已定规范、部件画像固定第一份；规约列义与值域、落法附注、读条目须知；模式 `name` / `roles` / `depends_on`、上下篇、适用单元、候选登记（两句信号句逐字保留）、多模式组合；第五节能力登记与不适用登记格式 |
| 能力台账 | `skills/story/reference/knowledge/capabilities.md` | 三层：通用读写规则（`knowledge-protocol`，含 README 退出的迁移依据）；业务相关能力（`component-profile`、`codebase-facts`、`reporting`、八个规约域）；可选模式（`decision-tree`、`page-interaction`）。每条有类型、修订、适用、谁消费、面与取证、走查、缺失时、Demo 参考、变化。`reporting` 条按 S1 返修写：已实现覆盖与规范 / 建设差距分开，指标与流程多对多。初始 ID 取现有 name / pattern_id，首次修订均为 1 |
| 适配方法 | `skills/story-adaptation/reference/knowledge-adaptation.md` | 目标、输入、步骤（装机制 → 识别 → 差异交人 → 改写 → 协议迁移 → 走查 → 确认交回）、完成条件、失败与中断、交回格式，附「后台任务通知」中性完整示例 |
| 加载器 | `hooks/shared/knowledge.mjs` | `kind: index` 分支与 `indexes` 退出；遇旧索引件报「旧版知识索引件」并指向方法页第 5 步（进同一 `bad` 汇总，不当作零知识）；新增 `parseDeclaration`：只在 frontmatter 出现登记键时用既有 `parseYaml` 解析，核 protocol（只认 1）、capabilities（id / 正整数 revision / 非空 covers、同文件重复 id）、capability_decisions（scope 与 `'*'` 不混写、decision 两值、reason / basis、deferred 要 impact）；`declarations` 按清单顺序输出原始事实，缺席给 null / []；导出 `declarationContext` |
| 三个入口 | `hooks/spec/author.mjs`、`hooks/plan/author.mjs`、`hooks/shared/pre_verifier.mjs` | 同一段 `declarationContext`：两份规则文件的实际路径 + 各文件登记原文（每文件一行 YAML）；空清单只给入口；spec 空知识分支加一句「缺能力时按台账说明缺什么、影响什么」；plan 加「值仍缺依据时写清缺哪一项」；审查在知识加载失败时写具体错误、依赖知识的检查写未验证 |
| Demo 知识 | `knowledge/` 与 `manifest.yaml` | 四份 README 从激活清单删除并删文件；模式依赖写回 `depends_on`；十三份知识在 frontmatter 登记自己的能力（env-exceptions 无落法附注，covers 只写条目表）；manifest 注释改为三类 kind 并指向机制侧规则 |
| 首装 | `skills/story-adaptation/scripts/adapt-scan.mjs` | `skeletonKnowledge` 与 README 复制退出；首装知识清单为空；`--check` 的 manifest 合成随之调整。升级仍不读不写 `knowledge/`，机制一致性检查不扩大 |
| Skill 与入口 | `skills/story-adaptation/SKILL.md`、`hooks/coding/author.md`、`test/story/AGENTS.md` | Skill 新增「知识适配」一节指向三份文件；版本相同退出 0 仍要核知识；首装不建骨架、部件画像照 protocol 与台账写；`--check` 与知识可读性分开报。coding 作者页在模式文档一行指向 protocol 第四节。维护约定改指新位置 |

## 2. 生产者 → 消费者

| 生产者 | 消费者 |
|---|---|
| `activeKnowledge().declarations` | `declarationContext` → spec / plan 作者包、plan / spec 审查任务 |
| `protocol.md` | 适配方法、各入口的规则指针、加载器报错的修法 |
| `capabilities.md` | 适配方法第 2–4 步、作者与审查在确认能力要求时展开 |
| `knowledge-adaptation.md` | adapt Skill、加载器的旧索引报错 |

机制代码不按任何能力 ID 分支；台账与方法页里的 Demo 专名已按「机制层零专名」扫描改写（`test_neutral_knowledge::test_no_token_outside_knowledge` 通过）。

## 3. 旧路径退出

四份 README 及其激活项；`INDEX_KIND`、`parseIndexFile`、`indexes`；README 上的协议版本校验（改为逐文件 `protocol`）；首装 README 复制与 `skeletonKnowledge`；Skill 里「照 facts/README.md 写」「不动 knowledge 内容」；模式 README 手列的模式清单（由激活文件派生）。历史 release 与已完成轮次未改；1.9.5 内网指南保留原样，1.9.6 指南在 S4 写。

## 4. 离线验证

全量 1372 passed、15 skipped（a0e88a29）。03 §7 各项：

| 情形 | 用例 |
|---|---|
| 空知识 | `test_empty_knowledge_repo::test_the_task_package_says_this_repo_has_no_knowledge`（给两份规则入口、不说「未登记」）；`test_adapt_ownership::test_the_manifest_registers_no_knowledge_and_writes_none`、`test_the_derivation_runs_on_a_fresh_install`、`test_a_fresh_install_passes_its_own_check` |
| 旧内容无登记 | `test_knowledge_protocol::DeclarationsAreRawFacts::test_knowledge_without_declarations_still_loads` |
| 多文件同能力、目标自有 ID | `::test_one_capability_across_files_and_a_target_own_id_are_legal` |
| revision 落后仍可解析 | `::test_an_older_revision_is_passed_through_not_judged` |
| 元数据损坏、多文件错误一次汇总 | `::test_problems_in_two_files_are_reported_together` |
| 协议不兼容 | `TheProtocolIsCheckedOnLoad::test_a_newer_protocol_names_both_sides` |
| 旧索引迁移前报错、迁移后可读 | `::test_an_old_index_file_asks_for_the_migration`；升级保留目标自定义 README 与激活项、`--apply` / `--check` 通过：`test_adapt_ownership::AnOldIndexIsLeftForTheModel` |
| 三入口同一登记、无别名 | `test_evidence_delivery::DeclarationsReachEveryReader` |
| 模式信号句单一来源 | `test_contracts_single_source::PatternSignalSourceIsStatedToBothReaders` 改读 protocol.md |

未离线覆盖：frontmatter 不是合法 YAML 的报错文本（代码路径有，未单测）；「机制无变化仍进入知识适配」只在 Skill 与方法页写明，属模型行为。

## 5. 预算

| 类别 | S2 基线 2547cfe7 | 实测 | 净 | S3 设计估（新增/退出） | target / 峰值 |
|---|---:|---:|---:|---|---|
| prompts_md | 1951 | 2086 | +135 | +250/−70 | 2209 / 2369 |
| hooks_mjs | 3881 | 3959 | +78 | +120/−25 | 3946 / 4021 |
| data | 794 | 790 | −4 | +10/−5 | 797 / 807 |
| scripts_mjs | 3474 | 3474 | 0 | 0 | 3499 / 3514 |
| scripts_py | 2883 | 2883 | 0 | 0 | 2883 / 2883 |
| 合计 | 12983 | 13192 | +209 | +380/−100 | 13334 / 13594 |

hooks_mjs 3959 超完成 target 3946 共 13 行，在峰值内：S1 的 hooks 已比分类估多 30（见 S1 交回），本步 +78 低于估的 +95。收口前需评审按职责确认 target 或指示压缩位置，实施者未挪额度。

另计：adapt 域（Skill + adapt-scan）714 → 698，方法页新增 49 非空行，共 747（估完成 859）；knowledge 域 17 文件 639 → 13 文件 616（README 退出 −133 行，登记 +67 行，另含 S1 的 reporting 改写）。

## 6. 请评审关注

- 能力登记的 `covers` 对规约写「条目表 / 落法附注」、对模式写「上篇 / 下篇」：规约与模式没有 facts 那样的「面」，我按文件真实承载的结构写。若台账要为这两类定义别的面标识，请指示。
- `declarationContext` 按设计给登记原文，13 份文件在每个任务包里多约 16 行；如果认为只该列有 decisions 或与台账修订不同的文件，请指示。
- S4 的中性目标仓仍缺输入（04 §1），届时先报缺什么。

# S2 实施交回（2026-09-24，与 S1 一并评审）

依据 [02](../02-遗留验证与收口.md) 与 [用户裁定：全部改完再实跑](2026-09-24-用户裁定-全部改完再实跑.md)。基线 **10ebb168**（S1 提交），工作区干净；S2 没有产品改动，只有验证与本收口表。02 §2 的正常业务效果验证按用户裁定推迟到 S1–S4 全部改完后的一次实跑，本表效果栏写「待实跑」。

## 1. 定向离线

命令：`python -m pytest test/story/tests/<文件> -n auto --dist loadscope`。

| 范围 | 文件 | 结果 |
|---|---|---|
| 知识与统计点（S1 影响面） | test_knowledge_protocol、test_knowledge_use、test_neutral_knowledge、test_contracts_single_source、test_indicator_reporting、test_spec_contract_section、test_author_task_package、test_evidence_delivery、test_chapter_view、test_writing_plan、test_writing_flow、test_story_build、test_gate_groups | 全部通过 |
| R09 对接层 | test_requirement_source、test_requirement_system、test_adapt_source_kinds、test_adapt_ownership | 全部通过 |
| R10 update | test_story_update、test_update_checkpoints、test_update_dispatch | 全部通过 |
| 全量 | `test/story/tests` | 1365 passed、15 skipped（10ebb168） |

02 §1 表里各行与 S1 新增用例的对应：

| 02 §1 的输入 | 用例 |
|---|---|
| 同一点三种结果，审查输入三行按序全在 | `test_indicator_reporting::test_the_reviewer_sees_every_result_row_in_order` |
| 两个指标共用一点，两边都看到同组 plan 行、无虚假缺行 | `::test_a_point_shared_by_two_indicators_shows_under_both` |
| 先说明表后点位表、统计点列不在第一列 | `::test_the_point_column_need_not_come_first`、`::test_an_explanation_table_is_not_a_point_table` |
| 同点三行中一行方法不存在，定位到该行 | `::test_a_bad_method_is_named_on_its_own_result_row` |
| decisions.json 合法数组 / 对象 / 空 / 损坏 | `test_author_task_package::CurrentDecisionsReachTheAuthor` 四个用例 |
| open 项带选项与推荐，原文送达、不推断影响 | 同上 `test_both_legal_shapes_reach_the_author_verbatim`、`test_the_script_does_not_infer_which_text_is_affected` |
| /story 缺节、空指标、不涉及、直接 Spec 无扩展章 | `test_indicator_reporting` 的 `test_a_story_spec_without_the_section_is_an_upstream_gap`、`test_a_section_without_point_tables_is_a_structure_gap`、`test_a_spec_that_does_not_apply_*`、`test_a_direct_spec_without_the_section_keeps_its_scope`；`test_gate_groups::test_a_clean_plan_passes` |
| 附录与图草稿提示写业务内容，编号与图引用保持 | `test_story_build::DraftsCarryTheDeterministicWork`、`test_writing_plan::TheExpressionScaffolding` |

## 2. R09：对接层拆分（b36e126e）

对照拆分后的实际实现核四个命令，Demo 替身只证明合同，不代表内网：

| 行为 | 证据 |
|---|---|
| init 逐级取 AR → SR → RR，三份正文与三张单据，系统不带图 | `test_requirement_system::test_pulls_three_documents_and_three_tickets`、`test_system_carries_no_images` |
| 本地已有正文不覆盖；缺正文留给占位步骤；未知单据停下、不造占位；系统不可达不报成未知单据 | 同文件 `test_existing_local_design_is_never_overwritten`、`test_missing_body_is_left_for_the_placeholder_step`、`test_unknown_ticket_stops_instead_of_seeding_placeholders`、`test_unreachable_system_is_not_reported_as_unknown_ticket` |
| archive 覆盖系统正文前把旧正文备份到 `.backups/cloud/`，本地只写这一份；只推 Markdown；缺 story 不碰系统 | `test_body_is_replaced_and_previous_version_kept`、`test_only_the_cloud_backup_is_written_locally`、`test_only_markdown_reaches_the_system`、`test_missing_story_fails_without_touching_the_system` |
| restore 取最新 cloud 备份恢复；没有备份失败 | `test_restore_returns_the_previous_body`、`test_restore_takes_the_latest_cloud_backup`、`test_restore_without_backup_fails` |
| fetch 只写 inbox 与回执；四态；缺席不是失败；与本地相同不放入；不覆盖人写的评审意见；缺 `--out` 拒绝 | `test_story_update::FetchOnlyWritesToTheInbox` 八个用例 |
| 非 AR 单据在取 token 前就拒绝；AR 单读不到如实失败；无 help 命令；参数错误写 stderr | `test_requirement_source::TheAdapterServesOnlySystemRequirements` |
| core 与 adapters 互不点名 | `test_requirement_source::CoreAndAdapterDoNotKnowEachOther`；另扫 `core/` 导入 adapters、`adapters/` 引用 core 命令均零命中 |
| 本地文档备份在 `.backups/local/`，不进 update 镜像 | `test_story_update::test_reports_and_backups_stay_out_of_the_mirror` |

外部边界：内网 MCP 的真实接口、鉴权、富文本与图片处理链、目标目录由内网按《内网适配指南（1.9.3 → 1.9.5）》与架构描述实现并验证，本仓未覆盖。

## 3. R10：update 与恢复

本版 S1 没有改 update 的输入、镜像、回写或恢复；1.9.5 对接层提交只把备份目录并入 `.backups/`，镜像排除已随之更新。复用既有离线证据，基线 10ebb168：首轮无基准记不完整、无变化不留痕、运行前改动被捕获、读不到是缺口不是删除、恢复保留两侧、无现场拒绝恢复、本轮人工决定原样记录、先问补料、收口核审查采纳、已闭环阶段须仍闭环、新版原件替换旧件（`test_story_update` 各类）。update 真实第二段不默认追加；「检查未启动」与「内容失败」的区分沿 1.9.5 已有的 `harness_incomplete` 诊断，本轮没有新改动。

## 4. R01–R10 收口表

| ID | 根因 | 改动 / 未改理由 | 离线证据 | 效果（待实跑） | 剩余依赖 |
|---|---|---|---|---|---|
| R01 | 见 S1 交回 §1 | 10ebb168：spec 作者页方法、§9.4 形状、点位按「统计点」列识别 | §1 表 | auto / car 的 Spec §9.4 是否按指标观察的流程与业务结果写、结果不写协议值 | — |
| R02 | 同上 | 10ebb168：Plan 逐结果作业、多行、待登记含义 | §1 表 | car 的 Plan 是否给出身份与内码的具体候选、每种结果一行 | — |
| R03 | 同上；另核实 BI 与自动采集仓内未实现 | 10ebb168：reporting 分语义与协议、分配方法闭合 | `test_knowledge_use` 面级用例 | 作者是否据此给出候选值 | 内网真实分配规则由内网知识提供 |
| R04 | 同上 | 10ebb168：决策原文送达、读者审查按后果核 open | §1 表 | 仍 open 的选择在 Spec、Story、验收里是否保持条件式 | — |
| R05 | 宿主漏投（06 §4） | 不改产品与装置 | — | 实跑时宿主按 deliver 清单核两份落盘后再回话 | — |
| R06 | 同上 | 10ebb168：审查主干与 plan overlay 要求返回类型与模式投影 | 送达文本 | car 的 contracts 返回类型与 use-cases 是否齐 | Framework 通用返回类型检查归上游 |
| R07 | 同上 | 10ebb168：多行并列、按影响定级 | §1 表 | 审查是否把缺值、缺契约判 FAIL 并回流 | — |
| R08 | 同上 | 10ebb168：附录占位、图指引、缺图修法、上线观察 | `test_story_build`、`test_writing_plan`、`test_writing_flow` | 附录与图后是否还有「这一节登记」「图后判断：」；验收末是否有有依据的上线观察 | — |
| R09 | 对接层拆分晚于第四轮实跑 | 无新改动；本表 §2 补定向验证 | §2 | 实跑经过 init / archive 时顺带观察 | 内网 MCP 与目录由内网验证 |
| R10 | update 与恢复第四轮未实跑 | 无新改动；复用既有证据 | §3 | 默认不开 update 第二段 | — |

## 5. 实跑计划（S1–S4 全部改完后申请授权）

沿 02 §2：auto-topup 到 Spec、car-key-sharing 到 Plan，默认不开 update 第二段、不开 Coding；宿主按原交互脚本交料，auto 首次交料核两份实际落盘。同一次实跑承担 04 §3 的迁移后保护。每 Case 记总耗时、用户等待、模型有效工作、工具与审查轮数、环境错误（02 §3）。

## 6. 未验证

R01–R08 的模型效果全部待实跑；本表离线证据只证输入送达与确定性校验。S3 草稿已移出工作区（`output/s3-draft/`，本地，不提交），S2 评审通过后再做。

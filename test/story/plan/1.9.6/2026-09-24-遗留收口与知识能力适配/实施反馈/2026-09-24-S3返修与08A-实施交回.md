# S3 返修与 08 A 步实施交回（2026-09-24）

回应 [S3 实施评审](../评审意见/2026-09-24-S3-实施评审.md) F1–F3，并按 [08](../08-机制与知识的消费协作设计.md) §7 完成 A「编写与消费契约」。两个提交：**c977af44**（S3 返修）、**8bdd386a**（08 A）。B（reporting 按 07 与其余知识、adapt 写作方法）未开始，等 A 评审。未启动真实测试。

## 1. S3 返修（c977af44）

| 项 | 改动 | 验证 |
|---|---|---|
| F1 路径序列化 | `declarationContext` 每条声明输出 `- ${JSON.stringify(d)}`，整条原样交付 | `test_knowledge_protocol::test_the_rendered_declarations_read_back_unchanged`：含逗号、括号的路径与各字段，渲染后用 `parseYaml` 读回与原声明逐字段相等 |
| F2 错误截断 | 激活循环里重复登记、读不到、缺 kind、非法 kind 进 `bad` 后 `continue`；各类解析函数抛出的 `KnowledgeError`（条目表零行、缺角色等）捕获进 `bad`，其余文件继续核；清单本身读不到仍直接失败；有 `bad` 统一抛出，不返回部分结果 | `::test_every_file_problem_is_reported_in_one_load`：缺文件、缺 kind、protocol=99 三者一次报全 |
| F3 无需求编号 | 方法页第 7 步换成 03 给定的只读程序（按 `paths.extension_dir` 加载激活知识并 `selfCheck`），与 `--check` 分开报；第 3 步说清格式错误要修、协议迁移可暂缓及其后果 | `test_adapt_ownership::KnowledgeIsCheckedWithoutAFeature`：从方法页原样取出程序，首装且无需求时 PASS、`git status` 为空、未建 `doc/features`；登记不存在的文件时非零退出并带文件名 |

## 2. 08 A 步（8bdd386a）

| 对象 | 改动 |
|---|---|
| `reference/knowledge/protocol.md` | 新增「二、谁写什么」：知识 / 机制 / 本次产物按「什么变了它才变」判归属（各带一例）；知识可写方法、步骤、例子、完成判断，不写章节号、字段路径、命令；每份知识开头写用途、何时读、帮着形成什么、内容在哪；读法「先读用途与适用条件，再按任务读正文，结论进产物不回显原文」。facts：依据分当前实现 / 已定规范 / 示例候选，替换「与知识不符时以仓为准」为「与当前实现矛盾改知识、未达已定规范登记差距」，加读者要知道的顺序与「简单事实一句就够」。规约：一条可判断的要求，可附短解释，不写教程。模式：上下篇内容按 08 §3.4 描述，契约投影位置指向 plan 模板。开头补一句有消费者的结构保留、表达不了时交缺口 |
| `templates/plan-sections.md` | 「设计模式选型」下新增注释，承接模式文件里的通用契约投影：持有者、内部步骤、用户动作、完整往返、业务状态、数据边界各投到哪 |
| coding / review / ut / testing 的 `author.md` | 首段由「must 就是本阶段的知识来源、不回去重读规约」改为「must 定本次义务、不重判适用与选型；理解方法与语义时查 `must.rule` 指回的规约、选中的模式、相关事实；真实接口以代码为准；对不上回 plan」。coding 删「规约文件本身不必读」，review 读取表加原知识一行 |
| 四份下游 overlay | 头部注释同步为「按 must 核义务，原知识只用来理解方法与语义」，判据不变 |
| spec / plan 的 `author.md` | 读知识的顺序与结论去向并入原有一行；plan 的模式投影指向模板注释。作者页仍各 ≤60 行（59 / 60） |
| `test/story/AGENTS.md` | 知识可讲项目方法、机制讲阶段作业的维护边界 |
| 各引用 | protocol 章节重编号后，加载器报错、coding 作者页、adapt Skill、台账的节号引用同步 |

`spec/plan author.mjs`、`pre_verifier.mjs` 未改：核过三者已在动作前给出 protocol / capabilities 路径、激活文件与上游结果（S3 的 `declarationContext`），没有实际缺失的送达。

### A 的完成条件：两类知识各走一遍

**reporting（facts）**：Spec 作者读作者页第 1 步（先读用途、再读正文）→ 任务包第 2 节给 reporting 路径与登记 → 按作者页第 4 步写 §9.4（指标、流程、结果、获知时机、已有覆盖）→ Plan 任务包给 §9.4 原文与统计点，作者页第四节 + reporting 协议各面给身份、取值与分配 → Plan 埋点结果行与 contracts `must` → coding 按 `must` 实现，理解 SDK 用法时查 reporting → review 对照原知识核。谁解释（reporting）、谁推进（作者页、任务包、模板）已分开。**B 要补的缺口**：reporting 第 7 面还没有「业务设计参考 / 实现设计参考」两层应用方法，Spec / Plan 作者页的长方法段要在 B 收束为任务并改引用它（07 §4、§5 第 2 步）；A 未先删作者页方法，避免中间状态无人承担。

**设计模式（patterns）**：Spec 按 protocol 第五节切单元、对照模式上篇登记候选 → Plan 选型表，角色投影按模板新注释落 `files[].pattern / role`、interfaces、use-cases → coding 读下篇与 `must` → review 核角色各就各位。**B 要补的缺口**：两个模式文件上篇 §2 的契约投影表与 §8 验证清单仍写字段路径和阶段名；接替位置已在模板，B 改为内容级说明（哪些是对外业务状态、哪些是内部步骤）后删坐标。

## 3. B 实施前的知识清单（08 §6）

| 文件 | 读者任务 | 现有支持 | 实际缺口 | 处理 |
|---|---|---|---|---|
| facts/component-profile | Spec 判上游内容与本部件是否相关、界定交互方 | 部件申明、交互方两面，带路径 | 开头缺「何时读、帮着判什么」 | 补开头一段，正文保留 |
| facts/codebase-facts | 写 §9 代码现状、Plan 新增能力先复用 | 八面入口与位置 | 开头有用途句；各面是位置登记，简单事实足够 | 开头按 protocol 改成用途与读法，正文保留 |
| facts/reporting | Spec 统计设计、Plan 逐结果实现 | 七面事实、分配规范、单表参考 | 第 7 面缺两层应用方法与贯穿例子（07） | 按 07 改第 7 面与导读 |
| constraints/observability | 判定位与上报义务 | 开头说明归属，条目可判 | 无 | 保留 |
| constraints/compatibility-checklist | 逐项过兼容性 | 用途句清楚 | 无 | 保留 |
| constraints/deliverables | 交付杂项判命中 | 用途句清楚 | 无 | 保留 |
| constraints/dfx-baseline | 性能与资源引入评估 | 基线值表与条目 | 无 | 保留 |
| constraints/env-exceptions | 设备环境异常场景 | 三分类与条目 | 无 | 保留 |
| constraints/resource-usage | 图片与文案来源 | 用途句清楚 | 无 | 保留 |
| constraints/security-privacy | 个人数据、权限、暴露面红线 | 覆盖面与条目 | 无 | 保留 |
| constraints/ux-consistency | 多形态一致性 | 前提写明 | 无 | 保留 |
| patterns/decision-tree | 选型与落地 | 上下篇、应用步骤、SDK 事实、骨架 | 上篇契约投影表与验证清单写契约字段路径、阶段名（接替已在 plan 模板） | 改为内容级说明，删坐标；阶段验证清单改为按职责的完成判断 |
| patterns/page-interaction | 同上 | 同上 | 同上 | 同上 |

规约八份只在开头与条目层面核过，没有发现读者无法判定的缺口，不为模板齐整改写。

## 4. 验证与预算

- 全量离线 1376 passed、15 skipped（8bdd386a）。
- 机制实测：12983（S2 基线）→ 13192（S3）→ 13197（S3 返修，hooks +5）→ 13218（08 A，prompts +21）。hooks_mjs 3964 超完成 target 3946 共 18 行（在峰值 4021 内）；prompts_md 2107 在 target 2209 内。本轮累计新增 / 退出按提交 numstat：S3 返修机制 +14/−8、方法页 +18/−2；08 A 机制与模板 +56/−27。
- 知识域本轮未改；adapt 方法页 +16 行。

## 5. 请评审

- A 的归属判定与下游「must 定义务、原知识解释方法」措辞是否可作为 B 的依据。
- §3 清单的范围：B 只动 reporting、两份模式、两份 facts 开头；规约不改。

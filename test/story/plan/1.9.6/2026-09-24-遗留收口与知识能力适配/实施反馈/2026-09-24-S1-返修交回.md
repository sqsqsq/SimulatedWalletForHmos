# S1 返修交回（2026-09-24）

回应 [S1 / S2 实施评审](../评审意见/2026-09-24-S1-S2-实施评审.md) F1–F4 与 §3 的措辞建议。提交 **2547cfe7**（在 10ebb168 之上），只动 S1 范围内的知识、模板与审查文本，不涉及对接与恢复。

## 1. 逐项

| 项 | 改动 | 位置 |
|---|---|---|
| F1 知识承载阶段坐标 | 导读改为两组内容各回答什么（业务统计语义：观察哪些业务过程、结果、本端何时可知、已有覆盖；协议与实现映射：身份、取值、调用、新值分配），不再写 Spec §9.4 / Plan 埋点；使用参考两段标题只留「业务语义」「实现映射」；「登记与复用」里「Plan 同时列…」「Plan 写候选…回 Plan 调整」改为「一起提出，经项目设计评审确认」「候选在设计里标待登记…回到设计调整」；BI 一行「Plan 列为建设项」改为「属于建设差距」。复扫：文件里已无 Spec、Plan、§、阶段字样 | `knowledge/facts/reporting.md` 导读、§2、§6、§7 |
| F2 未实现的自动采集被用来省略 | 自动运维上报一行改为「已定规范，仓内尚无自动采集实现：现在不构成已有覆盖；需求要用到这些交互数据时，数据要求与这项建设差距都要写明」；spec overlay「标为已有覆盖的」改为「项目知识里确登记为已实现——只是规范、尚未实现的不算覆盖，当前指标要用的数据仍要写出」 | `reporting.md` §2；`rules/spec-rules.overlay.yaml` |
| F3 业务幂等与统计尝试混用 | 示例先写一次尝试的定义（每次调用 submit 创建 attemptId，同一尝试只结算一个结果；业务幂等键只负责服务端不重复建单）；成功行「同一尝试的重复回调不再报」；失败行「用同一幂等键重试是新的尝试，各自记录」；取消行限定「提交请求尚未发出时」，并写明请求已发出后离开页面不改记取消、按最终响应结算。第 57 行「同一幂等键重复调用不得创建第二份单据」保留 | `templates/plan-sections.md` 埋点示例 |
| F4 一指标一流程的绝对句 | 改为指标与流程不一一对应：可观察一条或汇总几条相关流程的同类结果，一条流程可支撑几个指标，同一事件事实只采集一次；观察范围按需求与统计语义确定。单流程的开通示例保留为例子。审查主干 spec 交接同步为「每个指标的观察范围、涉及的流程与步骤」 | `reporting.md` §1；`hooks/shared/pre_verifier.mjs` WALK.spec |
| §3 措辞 | 「本仓拟定规范，不是现有代码事实」改为「本仓采用下面的分配规范；具体编号仍按规范提出并确认，不是现有代码里已有的分配」，面仍 `confirmed: 已确认` | `reporting.md` §6 |

## 2. 复核

- 面级契约：七个 H2 与顺序不变，`业务扩展字段` 仍是唯一未确认面（`test_knowledge_use::test_each_facet_keeps_its_own_heading_and_confirmation` 通过）。
- 职责不重复：Spec / Plan 怎样读取、写在哪仍只在作者页与模板；知识只讲项目语义、协议与分配规范。
- 全量离线 1365 passed、15 skipped（2547cfe7）。
- 预算：12982 → 12983（prompts_md +1），其余不变。

## 3. 带到 S3 的一处

S3 草稿里的台账 `reporting` 条目写过「已有自动采集覆盖的不重复列」，恢复 S3 时按 F2 改成与知识一致（只算已实现的覆盖）。

## 4. 未验证

模型效果仍待整版实跑；本次只证文本与面级解析。

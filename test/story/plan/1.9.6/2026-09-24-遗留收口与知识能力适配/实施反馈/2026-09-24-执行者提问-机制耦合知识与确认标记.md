# 执行者提问：机制耦合了具体知识、确认标记退出、能力 ID 与 name 重复（2026-09-24）

用户在 08 B 期间提出三个问题，都涉及机制侧已定案的设计（01、05 评审 F1、S3），请设计者裁定。本轮只改了知识侧，机制未动。

## 1. 机制文件写进了 Demo 知识的具体内容

用户指出的是「knowledge 的具体知识写进了 extension」，不是指有没有埋点章节。以下概念只在本仓 WalletHA 上报协议与分配规范里成立，却出现在通用规则、模板与任务包里：

| 位置 | 写进的具体知识 |
|---|---|
| `rules/plan-rules.overlay.yaml:20–21` | 协议字段「身份、编码、分类、外部错误码」；「待登记」；「规则本可算出候选」 |
| `hooks/shared/pre_verifier.mjs:55、243` | 「候选已设计而未登记与根本没给值分开判」；「协议字段、登记位置」 |
| `hooks/plan/author.mjs:36` | 「登记位置、协议字段在这里找」 |
| `hooks/plan/author.md` 四、埋点 | 「身份与字段的具体取值或候选」「待登记只表示值已设计、尚未写入常量文件」 |
| `hooks/spec/author.md` 第 4 步 | 「身份与协议值留给 plan」「不替人定成功率分母」 |
| `templates/spec-sections.md:43` | 「身份、编码等协议值由 plan 落实」 |
| `templates/plan-sections.md:113–132` | 示例列「身份」「登记位置」，「待登记」「缺分配依据」「具体候选」「attemptId」「外部错误码取返回码」 |
| `templates/plan-sections.md:23` | 模式投影注释用两个 Demo 模式的角色名「节点、动作枚举」 |
| `skills/story/reference/knowledge/capabilities.md` | event-tracking 条的「结果编码结构、命名空间与段长」；规约域表 8 个 Demo 前缀；模式表「编排 SDK」——台账随 adapt 装到每个目标仓 |
| `skills/story/reference/evidence-rules.md:68–70` | 1.9.5 前的旧埋点行形「事件名 + 触发节点 + 归属 + 报表影响 + 代码现状」，与现行 §9.4 冲突 |

成因：机制描述「好产物长什么样」时借了 Demo 知识的概念；零专名扫描只禁钱包专名，查不出「身份 / 内码 / 待登记」这类概念。

**实施者建议**：机制只用通用表述——「项目知识定义了要填的字段与取值规则时，逐项给出值或依据及来源，缺依据写待决与影响」；身份、编码、登记、候选等只在 event-tracking 知识里。模板示例改为不含项目协议的中性形状，模式投影按角色职责写。台账每条只写要回答的问题、谁消费、怎样走查，Demo 具体结构经「Demo 参考」查看（规约域与模式表是否保留 Demo 名单请定）。删 `evidence-rules.md` 旧规则。零专名扫描不扩成概念词表，改为评审项。

## 2. 确认标记整体退出

用户：「知识就是确定正确的东西，不需要草稿态，这不是冗余设计么」。实施者同意：唯一的机械消费者是「未确认」（`knowledge.mjs:402` 收 `unconfirmed`，`knowledge-use/validation.mjs:111` 要求 `verified`）；「已确认」无消费者。唯一的未确认面（业务扩展字段）真正确定的是「方法名与类型已核、本仓没有业务含义的依据、要用时向谁取证」，这本身是确定的事实，照常写正文即可。

**本轮已做（知识侧）**：Demo 三份 facts 删去全部 `confirmed` 标记；event-tracking 的业务扩展字段改写为确定陈述。加载照常，`unconfirmed` 为空。

**请裁定（机制侧，01 与 05 评审 F1 曾要求保留）**：退出 `knowledge.mjs` 的 `CONFIRMED` / `unconfirmed`、`validation.mjs` 的未确认面必填 `verified`、`knowledge-use/document.mjs:126` 的「（未确认：…）」注释；`verified` 字段是否一并退出；protocol 第三节与 spec 作者页的说明（现 protocol 仍写标题带 `confirmed`，与 Demo 知识已不一致）；相关测试。

## 3. 能力 ID 与 name 重复

Demo 13 份知识的能力 `id` 都等于 frontmatter `name`。本轮已把登记压成一行、去掉缺省的 `protocol: 1`（每份 5 → 1 行）。是否让 `capabilities` 省略时按「承担与 name 同名的能力、covers 取全部面」理解，属 S3 协议，请定。

## 4. 预算口径更正

此前把「改写行都计新增」累计为 415 并请用户确认额度——口径错误。1.9.6 知识域只有埋点是新增内容，其余是表达重写，不是增长理由。现按每份文件对照 190f274b 报实数，见 [整改交回](2026-09-24-08B-知识去补丁整改交回.md)。

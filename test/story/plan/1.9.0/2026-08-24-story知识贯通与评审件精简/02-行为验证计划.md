# 02 行为验证计划（知识贯通与评审件精简轮）

> 状态：**判据定稿（待实跑）**。判据从冻结需求 `test/story/spec/2026-08-21-story四项需求澄清/` 各条款**独立重推**，不由实现项自拟；每条写清观测对象与通过条件，结论栏留空待实跑填写。
> 分三层：离线结构断言（脚本）/ 构造产物门禁演练（脚本）/ CLI 实跑行为（模型）。**离线全绿 ≠ 行为完成**。

## 1. 判据表

| 判据 | 冻结条款 | 观测对象 | 通过条件 | 层 | 结论 |
|---|---|---|---|---|---|
| B16 review 可独立读懂 | 02-C1 | `AR/review.md` | 无「怎么填」章；「附录：编号对照」覆盖全部议题 `impact` 编号，标题与 spec/规约原文一致；`source` 含「无上游依据」的议题全部位于开放区 | 离线+实跑 | |
| B17 附录单表 | 01-C4、01-C3 | `AR/story.md` 附录 | 仅一张含规约编号的表；manifest 激活的每个域至少一行；判「否」行有依据；无自检表/详情小节/速查 | 离线+实跑 | |
| B18 一图一处 | 01-C3、02-C2 | story 全文 | 同一路径 0 次二次引用；源文档图片信息未丢（audit 图片列：源 N → story ≥ 与叙述相关者） | 离线+实跑 | |
| B18' 无编号清单段 | 01-C1、02-C2 | story 正文 | 不存在「≥3 个 spec 编号且无谓语」的段落；守恒按名扫描 0 缺失 | 实跑 | |
| B19 决策引用成句 | 02-C1 | story 正文 | 无「…」截断；同段不重复决策值；无「待 X确认 确认」形态 | 离线+实跑 | |
| B20 先冻结后设计 | 03-C6 | `plan/plan.md` | 「知识决策」章标题在「数据模型定义」之前；设计章中至少一处以文字引用该章结论（如「按 DM-01 义务，上下文含 flowId」） | 实跑 | |
| B21 模式投影为结构 | 03-C6、03-C8 | `plan/contracts.yaml`、`use-cases.yaml` | 选中的模式有 `instance`，`roles` 键集等于模式 frontmatter，每个值在 interfaces/data_models/files 中存在；coordinator = 构建与启动；未选的候选有理由 | 离线（构造）+实跑 | |
| B22 义务有落点实体 | 03-C5、03-C7 | `plan/contracts.yaml` | 每条 obligation `landing` ≥1 且全部解析成功；anchor 不在知识决策章；DM 义务的 landing 至少含一个承载流程/步骤标识的字段或枚举 | 离线（构造）+实跑 | |
| B23 规约要求到达 spec | 03-C5 | `spec/spec.md` §7 | story 附录判「是」且会产生代码要求的编号 ⊆ §7「应用域约束要求」；DM/ENV 命中时在 §7 有行 | 实跑 | |
| B24 下游三段式 | 03-C5 | coding 代码 / `review/review-report.md` | coding：每个 landing/roles 文件存在且含实体名；review：「知识落实审查」节逐义务/逐角色有裁决与 diff 位置；（ut/testing 本轮 CLI 不到，用构造产物验 post_check 的 FAIL/PASS 两路） | 离线（构造）+实跑 | |
| B25 去重收敛 | 01-C2、02-C3 | story 全文 | 「跨章重复句」WARN 数 < 上一轮同 Case（AR90006 基线：开关语义 5 处 → 主叙述 1 处 + 承接）；四张表互复述比例下降（人工抽三条规则各只在一章完整写） | 实跑 | |
| B26 显式不适用 | 03-C5 | 下游报告 | 无义务被静默跳过：每条义务要么有证据要么有「不适用/待验证 + 理由」 | 离线（构造） | |
| B27 派生不枚举 | 04（适配） | 机制层 | 新增一个规约域 + 一个模式（含 roles）后，不改任何 `.mjs`：字典/账本/附录校验/角色校验均生效（上轮 B15 演练扩展） | 离线 | |
| D1 凭证文件组 | 测试方法 | `run_case.py` | yaml 命名的 verifier 报告被识别为闭环；单测通过；实跑中不出现同阶段重复「现在执行 X 阶段」>阶段预算 | 离线+实跑 | |
| D1' 阶段预算终止 | 测试方法 | `run_case.py` | 构造无进展续话时以 `phase_turn_budget_exhausted`/`no_progress` 终止 | 离线 | |
| D2 同唤醒回复 | 测试方法 | 观测记录 | 每次 `awaiting_reply` 的回复时间与出现时间同属一次唤醒；poll 返回含 question 原文 | 实跑 | |

## 2. 离线结构断言（提交前固定扫描，沿用上轮 §D 并追加）

```
# 机制层零知识零业务（knowledge/ 以外）
grep -rn "UX-\|SEC-\|DFX-\|DM-\|COMPAT-\|ENV-\|DLV-" doc/extensions --include=*.mjs --include=*.md --exclude-dir=knowledge | grep -v "<域前缀>\|形态示意"   # 期望 0
grep -rn "AR9000[0-9]\|自动充值\|挂失\|交通卡" doc/extensions --exclude-dir=knowledge                                        # 期望 0
grep -rn "0[0-9]-\(Product\|Feature\|CommonBusiness\|BusinessBase\|SystemBase\)" doc/extensions --exclude-dir=knowledge          # 期望 0
# 出口不枚举
grep -n "UX 适配要求\|触发频次\|存储兼容" doc/extensions/hooks/spec/on_context_load.md doc/extensions/hooks/spec/post_check.mjs  # 期望 0
# 两张表文案清零
grep -rn "兼容性自检\|两张表\|编号速查" doc/extensions                                                                             # 期望 0
# 手工登记清零
grep -rn "ids.json" doc/extensions                                                                                                 # 期望仅兼容告警一处
node --check doc/extensions/hooks/**/*.mjs doc/extensions/skills/story/scripts/*.mjs
python -m unittest discover -s test/story/tests
python -m compileall -q test/story/scripts
python test/story/scripts/run_multi_case.py plan --all --isolated-workspaces
```

## 3. 构造产物门禁演练（实施完成后、实跑前）

| 演练 | 输入 | 期望 |
|---|---|---|
| P1 | 现有 AR90006 `contracts.yaml`（无 instance/roles/landing，anchor 自引用） | `plan/post_check` BLOCKER，列出全部缺项 |
| P2 | 在 P1 基础上补齐 instance/roles/landing 且实体真实存在 | PASS |
| P3 | roles 值指向不存在的类 | BLOCKER「方案里没有它的结构」 |
| P4 | 现有 AR90006 story-src（重复图片、DEC-005 settled+无上游依据） | `story-build build` 失败并点名两处行号与 DEC-005 |
| P5 | 现有 AR90004 story-src（索引段、截断决策引用） | 索引段 WARN；决策引用不再截断（build 后 grep「…」为 0） |
| P6 | 构造 review-report 缺一条义务裁决 | `review/post_check` BLOCKER 点名编号 |
| P7 | 构造 ut 报告：一条 model 义务既不在 branch 也无「不适用」 | `ut/post_check` BLOCKER；补「不适用：理由」后 PASS |
| P8 | 构造 test-report：device 义务缺行 | `testing/post_check` BLOCKER；补「待验证：原因」后 PASS |
| P9 | 新增规约域 `knowledge/constraints/zz-sample.md` + manifest 登记 | 字典含其条目；附录校验要求该域出现；不改 `.mjs` |
| P10 | `run_case` 单测：`verifier-report.yaml` 判闭环；连续 7 次同阶段续话终止 | 通过 |

## 4. CLI 实跑计划

- 按 TEST.md：`plan --all --isolated-workspaces` → `start --authorize-non-sandbox` → 15/120 秒 heartbeat → `finalize --promote`。
- Case 与终点：`split-interactive`（AR90006）→ **review**；`pattern-image-review`（AR90004）→ **plan**。
- 协调纪律：`awaiting_reply` 出现的那次唤醒内读 question 并回复；只回复推进场景所需的最小内容；不替模型跑 gate。
- 取证清单（写入 03 报告）：每条 B/D 判据的观测值 + 文件行号；上一轮同 Case 的基线值并列；模型未照做的指引单列「注入→执行落差」。

## 5. 报告结构（03-CLI测试报告.md）

1. 实跑事实（suite、Case、到达阶段、耗时、交互次数）
2. 判据表填结论（§1 表复制并填「结论」列）
3. 本轮发现的缺陷（机制 / 知识 / 测试方法 / 协调 分类，各带行号）
4. 与上一轮基线的量化对照
5. 结论与下一轮建议

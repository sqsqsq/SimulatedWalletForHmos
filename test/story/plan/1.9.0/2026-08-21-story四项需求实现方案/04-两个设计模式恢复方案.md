# 04 · 两个设计模式完整恢复

## 1. 内容基线

完整使用以下原件：

- `E:\Project\AIDefectHelpler\docs\archive\需求开发\extensions\knowledge\design-patterns\decision-tree.md`
- 同目录 `page-interaction.md`
- 同目录 `README.md` 作为两者索引和组合说明

目标文件与原件正文逐行一致；不得摘要、删节、改写章节或把正文迁移成 Project Facts。Extension 只可在文件首部增加加载所必需且不改变正文的最小元数据。

## 2. 使用边界

- `decision-tree` 用于复杂业务分支编排。
- `page-interaction` 用于业务结果驱动的页面交互编排。
- 两者可单独采用、组合采用或都不采用。
- Manifest 激活只表示模式可以成为候选；Plan 决定当前需求是否采用。

原件中提到的具体 SDK、依赖、API 和案例在当前工程使用前必须由 `codebase-facts.md` 的真实证据支持；证据不足时 Plan 不采用对应模式，但不能删改模式原件。

## 3. 验收

- 三个目标文件与指定原件正文完整性一致。
- Spec 可以识别零个、一个或两个候选。
- Plan 采用模式时，结构、流程、状态或协作发生真实变化；不采用时说明理由。
- 两个模式组合时各自职责和交接清晰，不新增组合调度框架。

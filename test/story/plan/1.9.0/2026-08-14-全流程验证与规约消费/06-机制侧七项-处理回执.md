# 机制侧七项 —— 处理回执（给规约内容会话）

> **对应**：`06-规约精简与结构统一spec.md` §5 移交的七项。
> **状态**：已全部落地（2026-08-14）。核实后 5 项照做、2 项处方修正，另纠正清单三处出入。
> 完整记录见 `00-总体计划.md` 的「W7 落地记录」。

## 需要你知道的三处出入

### 1. 第 1 项「删 spec hook 的重复节」——**没有删除对象**

`hooks/spec/on_context_load.md:85-99` 那一节不是重复，是 spec 专属细则：
三个出口（spec/review/story 三分）、story 两张回显表、`constraint-usage.md` 指针、
§7/§7.1/§7.2/§7.3 落点。与共享指路件的实际重合**只有一句「去读 constraints/」**。

两者语义也不同：共享件是**矩阵驱动**（查本阶段那一列，「—」不消费），
spec 件是**全量驱动**（逐个读全部约束文件逐条判定），后者甚至从不提「适用矩阵」。

且 `test/story/tests/test_knowledge_purity.py:236-239` 断言 spec 钩子必须含
`constraint-usage.md`，删节会直接打红。

**已做**：给 spec 也挂一份共享 hook（纯增量），spec 专属件一字未动。
挂载不对称由此消除——六个阶段都读得到矩阵索引，spec 额外有自己的细则。

### 2. 第 3 项的格子数：testing 是 **9 格**不是 8

矩阵 testing 列九个域全部非「—」（清单大概漏了 deliverables 行，它前面连着四个「—」）。
ut 列 5 格，与清单一致。两份 overlay 已按实际格数覆盖。

### 3. 第 4 项的「TEL/APP 承载句」不在 overlay

它们在 `doc/extensions/skills/story/reference/constraint-usage.md:48/50`。已补：
APP-01 扩为 APP-01/APP-07（配置项本体与必要性评估结论）、通知动作句补 TEL-06 承接方留痕。
三个新 ID（TEL-06/APP-07/COMPAT-12）已核实确在对应约束文件中。

## 两处我否掉的建议

| 建议 | 否掉理由 |
|---|---|
| manifest 用 YAML 锚点收敛重复路径 | `test_export_delivery_patch.py:113-133` 用**手写缩进解析器**读 manifest，锚在标量条目上会产出 `doc/extensions/&hook hooks/...` 假路径 → 测试红（锚在 list 上才安全）。为省 4 行重复引入一个解析器耦合不划算；且配置清单里「哪个阶段挂了什么」一眼可见 > DRY，将来某阶段要挂第二个 hook 时锚点反而要拆。**已改为注释分段 + 写入目录制式约定** |
| 「新 overlay 头注释明令禁止、spec 违反」的表述 | 三份新 overlay 写的是「**本文件**只问…不复述域名」——是自我描述，不是工程级禁令。收敛动作照做了，但措辞不上纲 |

## 与你的重写相关的两件事

### style 看护已就位，且**故意留了 4 条红**

新增 `test/story/tests/test_constraint_style.py`，分两档：

- **现在成立、已钉住（3 条）**：三种表制各自表头逐字恒等（实测已严格一致）、
  表制例外须登记（新增表制不能自行发明）、每域 frontmatter 有 applies_when；
- **待你重写后转绿（4 条，标 `expectedFailure`）**：
  单元格 ≤160（§2.3）、无「范围收窄」类补丁语（§2.4）、applies_when 双源一致
  （§2.6，当前 COMPAT 清单侧带括注）、骨架节序（§2.1，当前 app-lifecycle-config
  的两个散文节尚未归入附注体系）。

**用 `expectedFailure` 而不是注释掉**：你重写完成后它们会报 *unexpected success*，
那正是提醒「该把标记摘掉」的信号。摘标记这一步麻烦你在重写落地时一并做，
或者告诉我，我来摘。

四条 xfail 精确命中你 §2 里已登记的待修项——判据与你的规划是对齐的。

## 追加（W8）：你重写规约之后，消费方侧我做了什么

你把规约按新骨架重写完、并同步了自己的看护之后，**消费方侧还留着旧说法**。
已全部对齐，其中一处是真隐患，你可能想知道：

**门禁互斥（已修）**：`spec_ext_conclusion_validity` 的判据面覆盖 story，
却只写了 spec 侧的「数值三选一标来源」。结果是——一条**合规的** story
（本工程设定值登记决策、正文只写引用）被这条 verifier 判 FAIL；
而一条写了「（本工程设定，无上游依据）」的 story 被 story-build 的数值溯源守恒 FAIL。
**两个门禁互相排斥，story 无写法可过。** 现已按产物分流。

**形态真源确认为 `evidence-rules.md` §3.2 的证据表**。此前同一件事有三种说法
（两处按 story 形态但不分产物，一处**明令禁止**只写编号，而证据表定的 spec 形态恰是裸编号）。
现在三处都改为指向证据表。**若你后续调整引用形态，改证据表即可，其余文件会跟着指过去。**

**条目正文的三份副本已删**（`on_context_load` / `spec-sections` / `post_check.mjs` 的报错文案）。
保留的是 spec 自己的章节结构（§7 各小节名），删掉的是抄过来的条目内容。
新增 `SpecSideConsumerTest` 守这条：判据用条目内容特征串（`app.color`、`文本容器可伸缩`、
`走 string 资源`、`不硬编码亮色`），**不用域名**——域名在这些文件里有正当用途，条目正文没有。

**顺带清掉**：`constraint-usage.md` 与 `evidence-rules.md` 里 COMPAT 的「检查项」列旧称
（已随 COMPAT 改条目表而不存在，列名现在是 `ID`）。

### 表制已固化（本段随你的重写更新）

我最初写的是「三制」（七域条目表 + COMPAT 检查表 + ENV 场景表）。你重写后收敛为**两制**：
条目表八域 + ENV 场景表，`TABLE_SCHEMA` 只剩一个登记例外——你已同步改好
`test_constraint_style.py`，我这边不再改。

后续若再调列名，改 `test_constraint_style.py` 顶部的 `ENTRY_HEADER` /
`SCENARIO_HEADER` 常量即可，断言逻辑不必动。

# 02 行为验证承接台账（2.0 回退重写轮）

> 状态:**验证中**（本轮 2 个代表性 Case 已实跑；其余项未执行）
> 本文件是全部行为验证项的**唯一承接台账**（补上一补丁轮「6+2 条行为验证无承接文件」的缺口）。
> 纪律:离线全绿 ≠ 行为完成;下表结论只能由**隔离 Case / 多 Case CLI 实跑**（按 [test/story/TEST.md](../../../TEST.md) 协议）给出,脚本与单测结果不得填入「结论」列。「状态」列取值:`未执行 / 通过 / 未通过 / 阻塞`。
> 本轮范围（用户决策 D6，2026-08-23）:实跑 **2 个最具代表性场景**——`pattern-image-review`（AR90004，设计模式+视觉材料+完整链路）与 `split-interactive`（AR90006，补料+交互拆分+评审回流）。

## 0. 本轮实跑事实

| 项 | 值 |
|---|---|
| suite | `story-suite-20260823-172448`（隔离 workspace，2 Case 并行，17:25 起跑 ~19:04 收束） |
| Case | pattern-image-review（AR90004）/ split-interactive（AR90006） |
| 到达阶段 | 两者均产出 spec 三件产物；**spec receipt 未闭环** |
| 未闭环根因 | **框架级缺陷**，与本轮被测扩展无关:`capability_spec_requirement`（functional 轴 `on_missing: fail`）在 full 轨 /story 手动流程下 `derive.requirement` 只认 goal 入参或 lite 轨 change.md，导致 capability 恒 blocked → summary 恒 INCOMPLETE。被测模型请求热修 `framework/` 被拒（framework 只读，非本轮被测对象），按「不热修」继续 |
| 回灌 | 两 Case 的 `doc/features/<AR>` 均已回灌主工程，观测记录已生成 |

**被测流程自身的门禁结论**（诊断事实，不构成维护判据）:
- `story-build build / check / audit` 全绿；`merge-story --check` 全绿；
- split-interactive:spec 脚本 harness **PASS**（45 项，0 BLOCKER / 0 FAIL / 4 WARN / 1 SKIP），语义 verifier **PASS**（15/15，含 2 项 BLOCKER 级）；
- pattern-image-review:spec 脚本 harness **PASS**。

## 1. 验证项

| # | 验证内容（行为口径） | 来源 | 状态 | 结论（实跑事实） |
|---|---|---|---|---|
| V1 | 四固化 Case 的 story.md 恢复「按需引图」，全部图片引用 alt 非空、相对 `AR/` 路径可达 | 图片引用/00 | **通过**（2 Case） | 两 Case 各引 1 张且路径均可达:AR90004 `../RR/assets/loss-report-recovery.svg`、AR90006 `../ux-reference/auto-topup-contract-page.png`。`check` 的引用完整性门禁未报错；audit 对照统计 AR90004「源图 1 → story 1」 |
| V2 | 知识注入随阶段收窄:spec/plan 按类注入，coding/review/ut/testing 无冻结零注入、有冻结按冻结收敛;verifier 报告出现扩展 semantic_checks 逐项裁决 | 图片引用/01 | **部分通过** | 注入收窄已由 hook-runner 冒烟验证（见 01 §6）；实跑侧 verifier **15/15 全 PASS** 证明语义判据被逐项裁决。**未覆盖**:两 Case 均未进入 coding/review/ut/testing，冻结后定向注入只有离线冒烟证据 |
| V3 | story.md 双标题消失;机器面内容不出现在正文区;表格行占比回落;verifier 报告出现 `story_reader_fitness` 裁决 | 图片引用/02 | **通过**（2 Case） | 两 Case 均:十章、无重复章标题、正文区机器面 **0 处**、`check` 未触发表格占比 WARN（≤40%）；verifier 全项 PASS 含 `story_reader_fitness` |
| V4 | review.md 可读（背景/决策两段+三态勾选）;AI 按 `review_reflow.md` 回流处置顺畅;计划外意见可被判型 | 图片引用/03 | **通过** | 两 Case review 均为背景/决策两段叙述 + 三态勾选（各 24 处），**旧字段（确认人/完成期限/暂缓责任人/审核结果）0 处**；AR90006 完整走通评审回流:3 条意见逐条处置（同意/需修改/暂缓），产出 `review-disposition.json`（3 items），原意见逐字保留 |
| V5 | spec 不再丢上游必要信息（对照 1.0 归档 spec 的「不保存支付凭据/不判断是否该扣款/连续失败停用」类事实）;由 `upstream_semantic_coverage` 语义判据裁决，无机械误伤 | 图片引用/04 + G1 | **通过** | AR90006 三条上游必要信息全部有落点:「不保存支付凭据」spec 2 处、「不判断扣款/扣款真源」spec 5 处；「连续失败停用」按拆分归兄弟单据，story 4 处保留并显式写明归属（成功口径、术语表、参与方职责、范围外去向）——**不是丢失，是带归属的正确裁剪**。无机械误伤（G1 已删机械层，语义判据 PASS） |
| V6 | story 正文无规约编号、只有中文规约名;附录速查表完整 | 图片引用/05(G2) | **通过**（2 Case） | 两 Case 正文区规约编号均 **0 处**，附录区均 **23 处** |
| V7 | story H1 = `<编号> 业务名`，check 通过 | 图片引用/05(G4) | **通过**（2 Case） | `# AR90004 交通卡紧急挂失恢复`、`# AR90006 交通卡自动充值`；两者 `check` 均 ✅ |
| V8 | 知识矩阵注入按域/按文件收敛（G3 粒度），不出现整类笼统注入 | 图片引用/05(G3) | **部分通过** | 离线冒牙已验证按实例行注入（spec 12 行、冻结后 coding/review 各 2 行、ut 0 行）；实跑侧无独立观测点，未单独取证 |
| V9 | 内容守恒综合:2.0-R story 无事实丢失;audit 六字段表生成且「主叙述位置」全部指向真实章节标题 | 本轮（需求1 终验） | **通过** | 守恒机械层实跑生效:AR90006 首次 build 报「缺 S1、S2、S3、S4」并被模型补齐后通过——门禁在真实产物上抓到了漏事实。audit 表:AR90004 33 行、AR90006 30 行，复验结构全绿 |
| V10 | review 旧标题兼容:含旧 `#### 审核结果（由评审人填写）` 的历史 review，`check` 不假失败，`build` 后迁移为新标记且人工区逐字节保留 | 本轮（F2 回归） | **通过**（离线夹具） | 夹具双用例:旧标记 + 已勾选 + 已填意见 → `check` ✅ 不假失败；`build` 后旧标记 0 处、新标记 2 处、`- [x] **需修改**` 与「档位上限请设为 200 元」逐字节保留 |
| V11 | 适配链路:`/story adapt` 三态可走通;`verify-adaptation.mjs` 对两份 release 比对正常 | 本轮（F11，需求4） | **部分通过** | 离线:`build-release --version 1.0 --git-rev 19977c98`（43 文件）与 `--version 2.0`（56 文件）生成成功，路径约定统一；`verify-adaptation` 结构档 ✅、`--require-confirmed` 正确报出 5 份知识的未核实标记；篡改机制层文件后正确报指纹不一致。**未覆盖**:`/story adapt` 三态在真实目标工程的实跑（属内网事项） |

## 2. 已知预期现象（实跑时不算缺陷）

- 现存 Case 的旧 story 产物若含正文规约编号或无编号 H1，在新 `check` 下会 FAIL——**属预期**（模型需按新判据重写）。本轮两 Case 均为新生成，未触发。
- `run_multi_case.py` 历史目录清理失败报 `completed_with_warnings`——按 TEST.md 第 5 节不算 Case 失败。
- **spec receipt 未闭环属框架缺陷**（见 §0），不是扩展缺陷。

## 3. 本轮暴露的扩展自身问题（被测模型评价 + 外层复核，**未修**）

按用户指示「CLI 测试完成后不要立即改」，以下逐条登记，留待下一轮决定处置。

| # | 问题 | 复核判断 |
|---|---|---|
| P1 | **内容依赖倒挂**:spec post_check 把「AR/story.md 存在」设为闭环 gate，而 `story-build scaffold` 对「只以 SPEC 为输入」的章（02-terms / 08-acceptance）在 spec.md 未产出时报「声明了 N 个输入但一个都指不到」并硬失败 | **成立**。当前设计要求先写 spec.md 再 scaffold，但注入件把三件产物描述为「一次 pass 同批产出」，顺序未在指令层写死。建议:scaffold 对 SPEC 缺失改为降级提示（已有 warn），并把「先 spec 后 scaffold」写进注入件的执行顺序 |
| P2 | **H1 业务名依赖 design.md 标题**:init 骨架标题是「单号 + 开发需求（AR）」，不含业务名时 `businessTitle` 取不到，H1 只出单号 | **成立**。本轮两 Case 都取到了（模型自己补了业务名），但机制上依赖上游标题写法。建议:取不到时从 PRD 首个 H1 兜底 |
| P3 | **两套编号速查口径分裂**:review 附录只渲染被决策 `impact` 引用到的编号，story 附录渲染全部 ids | **按设计如此**（review 不承载需求叙事，冻结需求 C 条款背书），但两处口径差异未在文档明写，易被误读为缺陷。建议:在 rules.md 补一句说明 |
| P4 | 词根匹配对源文档标题措辞敏感，改名即降级整篇 | **按设计如此**（冻结 C5 安全回退），不改 |
| P5 | audit 的数值对照统计不区分「被决策承载」与「真丢失」 | **按设计如此**（非门禁观测项），可在提示文案里再明确 |
| P6 | DEC 编号无递增/复用校验，跨 AR 无全局性 | 沿用 1.0 形态，本轮未改。可作后续增强 |
| P7 | `story-flow.json` 只覆盖 init→spec，评审处置与 archive 之后无契约登记 | 沿用 1.0 形态，属流程契约范围，非本轮 scope |

## 4. 结论汇总

- **通过**:V1、V3、V4、V5、V6、V7、V9、V10（8 项）
- **部分通过**（离线证据充分、实跑未覆盖）:V2、V8、V11（3 项）
- **未通过**:无
- 本轮未发现任何由 2.0 重写引入的功能缺陷；唯一阻塞项是既有框架缺陷。
- 轮次报告路径:`output/story/story-suite-20260823-172448/`（含 `cases/*/observation-record.md`、`observations.jsonl`、`promotion-manifest.json`）。

# 本地需求系统与用例重构 · 冻结清单

> 收口时点 `fe565a88`。指纹为 `git hash-object` 前 12 位。
> 下一轮改动前先比对：**指纹变了而本清单没更新 = 有人绕过了交付流程**。

## 0. 判据：本批冻结了什么

| # | 冻结项 | 谁在守 |
|---|---|---|
| ① | 三个对接层替身的 **CLI 契约**（命令、回执 JSON 形状、退出码） | `test_requirement_system.py` 17 例；`story.js` docstring 是契约真源 |
| ② | 需求系统的**目录契约**（一子目录一张单、`detail.json` + 正文 md、`history/`、`attachments/`、`review-feedback.md`） | 同上；两个 Case 的 `system/` 是它的实例 |
| ③ | 材料**三处摆放**与投放时机（`system/` / `workspace/` / `supplements/`；`start` vs `on_request`） | `test_material_delivery.py` 17 例；TEST.md §2.2 |
| ④ | 需求系统快照**不在被测 workspace 内** | `_verify_workspace_boundary` + 单测两例 |
| ⑤ | prompt 的**需求方口吻**（不点命令/脚本/文件名/关卡名/观测点词） | `test_multi_scenario_cases.py` 词表扫描；TEST.md §2.1 |
| ⑥ | 两个 Case 覆盖的**十条能力点** | `test_every_story_capability_has_a_carrier` |

**SKILL / rules 未被本批改动**——它们写的是内网契约，本批只是把替身补齐到契约。
`doc/extensions` 除三个 js 外零改动。

## 1. 机制层（`doc/extensions`）

| 文件 | 指纹 | 行数 |
|---|---|---|
| `skills/story/scripts/story.js` | `c979c0ea6b4c` | 356 |
| `skills/story/scripts/review.js` | `8308b49832b5` | 69 |
| `skills/story/scripts/token.js` | `62b3bd3c3d5c` | 18 |

三份都自述「本地替身」——`/story adapt` 据此不复制它们到别的工程。

## 2. 测试域

| 文件 | 指纹 | 行数 |
|---|---|---|
| `test/story/scripts/run_multi_case.py` | `3046871467d6` | 2565 |
| `test/story/scripts/run_case.py` | `7d9ec2c56fa5` | 2089 |
| `test/story/tests/test_requirement_system.py` | `cff83672e6bd` | 291 |
| `test/story/tests/test_material_delivery.py` | `c076a414bbd8` | 283 |
| `test/story/config/test.yaml` | `29e87049318a` | 61 |
| `test/story/TEST.md` | `d1b073083d10` | 385 |

`test.yaml` 冻的是**被测宿主**：`codex` / `gpt-5.6-luna` / `full_access`。
换它等于换被测对象，跨轮次的效率数字就不能直接比（G3）。

## 3. 两个 Case

### `traffic-card-loss`（AR90004，需求系统上的单）

| 文件 | 指纹 |
|---|---|
| `case.yaml` | `dede15931020` |
| `interaction-script.yaml` | `5f90ae12c20a` |
| `supplements/紧急挂失界面原型说明.docx` | `f8f18ec7b08e` |
| `system/AR90004/design.md` | `8fbb2b77fe20` |
| `system/AR90004/detail.json` | `35332667d4d2` |
| `system/AR90004/review-feedback.md` | `750bcd6c3c95` |
| `system/AR90005/design.md` | `0d7f86da5aea` |
| `system/AR90005/detail.json` | `d018f24cd34f` |
| `system/RR90004/detail.json` | `f0f0de59aeee` |
| `system/RR90004/prd.md` | `cf5252f0182b` |
| `system/SR90004/design.md` | `51b0ee9775de` |
| `system/SR90004/detail.json` | `9b2b986a3340` |

### `local-issue-autotopup`（ISSUE-206，本地问题单）

| 文件 | 指纹 |
|---|---|
| `case.yaml` | `caa6f99bed06` |
| `interaction-script.yaml` | `dd6bcf836b68` |
| `supplements/交通卡自动充值.docx` | `0395f5be528c` |
| `workspace/AR/detail.json` | `bd17116fb2fe` |
| `workspace/SR/design.md` | `998d094bf015` |

**这两份 docx 是图片进入流程的唯一通道**：需求系统只承载 md，Case 目录里也没有任何
图片文件（`test_pictures_only_reach_the_flow_through_a_supplement` 守着）。
换掉 docx 就等于换掉图片链路的全部输入。

## 4. 已删除（不得回来）

| 删的东西 | 为什么 |
|---|---|
| 四个旧 Case（`pattern-image-review` / `source-conflict-review` / `split-interactive` / `split-two-ar`） | 材料起跑前铺满，「模型会不会发现缺料」测不到 |
| `pattern-image-review/workspace/RR/assets/loss-report-recovery.svg` | 图片不再从 Case 目录直接进流程，只能经 docx |
| 三个 js 里对那个演示数据目录的全部引用 | 目录早已删除，留着让「替身没接上」看起来像「正常走默认」 |
| `token.js` 的时间戳 | 每轮都不同，是比对两轮日志时唯一的噪声源 |

零引用由 `TestNoStaleDataSource` 与 `test_multi_scenario_cases.py` 机械守着。

## 5. 收口时的离线基线

| 项 | 值 |
|---|---|
| `test/story/tests` | 222 passed + 5 subtests |
| `check_failure_modes.py` | 50 形态全 PASS |
| `harness-runner --phase extensions` | PASS（6/6） |
| `doc/extensions` 非知识层行数 | 10543（≤7500 未达成，长期项） |

# 新一轮优化 · 总体执行计划

> 本轮三条主线的实施编排。**每条主线按「构建 harness → 实现 → 机械测试」节奏执行**；
> 全部完成后由用户人工触发 `test/story/TEST.md` 正式评测。
> **实施须经用户明示授权后开始**——本轮已发生一次未授权实施并已从 `.bak/20260812-200501`
> 快照回退（回退后 184 项机械测试全绿），引以为戒。

## 原始诉求（用户原话索引）

| 主线 | 用户原话所在 | 方案文档 |
|---|---|---|
| P1 init 流程契约化 | 「为 story 构建一个契约文件…第一步…第四步」（3.1–3.5，逐字收录于 01 §一） | [01](01-init流程契约方案.md) |
| P2 部件画像剥离 | 「本部件的定位和交互说明写死了当前鸿蒙钱包，是否应从 story 中剥离」 | [02](02-部件画像剥离.md) |
| P3 design-patterns 方法论 | 「删除状态机…我可以描述功能…给出如何描述此设计模型的方法论…plan 和 coding 引用粒度是否一致」 | [03](03-design-patterns方法论.md) |
| P4 constraints 域整治 | 「是否有建议使用的场景…安卓内容剔除还是声明适用范围…哪些可能是编造或需要优化的并让我确认」 | [05](05-constraints域整治.md) |

问题分析与根因：[00-问题分析.md](00-问题分析.md)。

## 基线状态（实施起点）

- 工作区 = `.bak/20260812-200501` 快照（含已授权改动：关卡 `transition_policy` 分档、
  界面图上下文判据与当场登记、harness 种子机制与只读位清理、docx-supplement 用例、
  184 项机械测试）；
- **不含**已判错的两个补丁（生成输入五源、关卡报告 inbox 行）——它们已随回退消失，
  P1 以流程结构取代其意图；
- 未提交。实施完成后是否提交、何时提交由用户指示。

## P1 init 流程契约化（方案：01）

| 步 | 内容 |
|---|---|
| harness | ①`test_story_template_consistency.py` 新增 FlowContractTest：初始化章含 S1–S4 链条且初析在生成之前；「导入上游材料」章无「何时用」条件触发句式；生成章输入四源且明示不直接读 inbox；关卡三选项与契约 `chosen` 枚举一致；新章声明 init-analysis 五节与 story-flow schema（含 `split.scope_text`）与写入即时自查义务；产物定位表含两个新产物。**既有 GateSemanticsTest 断言（transition_policy/报告一字不减/当场登记/上下文判据）在重写后必须保持绿**。②`test_flow_contract_gate.py`（新）：以临时 feature 夹具驱动 `post_check.mjs`——未 complete 契约被拦 / complete 契约放行 / 无契约不受影响 / `by:"ai"` 缺 basis 被拦 / `decided=split` 缺 scope_text 被拦（正反例都要，永久踩坑「只验负例不验正例」） |
| 实现 | SKILL.md 六处改动（01 §3.4）+ post_check.mjs 增 `storyFlowProblems`（01 §3.3） |
| 机械测试 | 全量 `test/story/tests` 绿；`--phase extensions` PASS；scratchpad 冒烟（init → 导入 → 契约样例 → post_check 正反例） |
| 调整既有断言 | `test_ar_scope_dimension.py::GateTest`：三选项与「需要调整/修改意见」旧断言按新关卡语义重写；draft-first 断言改为「初析先于关卡」 |

## P2 部件画像剥离（方案：02）

| 步 | 内容 |
|---|---|
| harness | `test_knowledge_purity.py` 新增：`component-profile.md` 存在、manifest 已声明、`rules/ar_design_init.md` 不含部件专名（钱包/鸿蒙/穿戴/华为）；`ManifestCoverageTest` 自动覆盖交付边界 |
| 实现 | 新建 `knowledge/component-profile.md`（§1 部件申明 + §2 交互方清单，内容自现 rules 迁移）；rules §1/§2 改引用；manifest 声明；`export_delivery_patch.py` INCLUDE 增补（**长期在列，每次 patch 覆盖**）+ patch 前言注明「权威在训练仓，内网不要本地改（会被下次 patch 冲掉），部件事实变更请反馈回训练仓修订」（按项 2 裁决，旧「内网自维护」表述作废） |
| 机械测试 | 全量绿；`--verify` 导出自检通过 |

## P3 design-patterns 方法论（方案：03）

| 步 | 内容 |
|---|---|
| harness | `test_knowledge_purity.py::DesignPatternsTest` 调整：EXPECTED 删 state-machine；新增断言——两份模式文档分「适用与选型 / 结构与落地」上下两篇、`applies_when` 含修正后语义（流程编排 / 页内交互）、plan hook 指上篇、coding hook 指下篇 |
| 实现 | 删 `state-machine.md`（README 路由表、manifest 同步）；`decision-tree.md` / `page-interaction.md` 重写为方法论骨架（五个必答问题作为写作指引 + 留白，头部声明「内网按指引生成正文后删指引块」）；README 补粒度分工；两个 hook 细化指引 |
| 机械测试 | 全量绿；`--phase extensions` PASS |

## P4 constraints 域整治（方案：05；前置：待裁决项 A–F 经用户确认）

| 步 | 内容 |
|---|---|
| 前置调查 | **HarmonyOS 官网核实**（05 §五 E4）：九域判定方法列里全部鸿蒙专名逐个对照官方文档（WebSearch / context7），产出「确认 / 修正 / 删除」清单——**核实不了的删，不乱写** |
| harness | `test_knowledge_purity.py` 调整：README 九域×六阶段矩阵断言（节名避开「使用方法（运行时）」）；COMPAT 适用性列非空且不适用行带理由；「单双」列有语义说明；裁决结果断言（MODE-04 不存在 / SEC-06 含 MaskUtil / APP-02/03 无备份配置扫描表述）；`test_no_reference…` docstring 理由改写（断言保留） |
| 实现 | 按裁决与核实清单改条目正文（不引入溯源标注）；APP-02/03 按「卡片绑定设备写入芯片」口径重写；README 消费方节升级为阶段矩阵 + 适用性声明节；COMPAT 适用性列引用 `component-profile.md` 形态结论（依赖 P2） |
| 机械测试 | 全量绿；`--phase extensions` PASS |

## 顺序与依赖

P2、P3 相互独立；P1 改动面最大且触及 P2 引用的 rules 文件；P4 依赖 P2 的部件画像落点，
且其裁决项（05 §五）须先经用户确认——**P1 → P2 → P3 → P4** 顺序执行，
每条主线完成即全量跑一次机械测试，绿了才进下一条（避免交叉改动混淆归因）。

## P5 契约写入权反转与瘦身（方案：06；P1–P4 完成并经 AR90006 手工实测后追加）

实测暴露 Q1–Q7（契约留痕缺失/时间戳编造/假轮次/SKILL 过大等），方案与裁决见
[06-契约写入权反转与瘦身.md](06-契约写入权反转与瘦身.md)。节奏同前：
harness（`test_story_flow_script.py` 等先行）→ 实现（story_flow.py + SKILL 瘦身 +
跳板归位 + post_check v2）→ 机械测试全绿 → AR90006 复演回归。

> **本轮到 P5 为止。** 之后的两条主线（design-patterns 填实、契约记录失真修复）已另起一轮，
> 见 [2026-08-13-修正与能力增强/](../2026-08-13-修正与能力增强)。

## 收尾

1. 三条全部绿后：`python test/story/scripts/export_delivery_patch.py --verify` 确认交付边界自洽；
2. 汇报差异摘要，**停等用户**：人工触发 `TEST.md`（docx-supplement 用例观测 S1–S4 全链路 +
   /spec 消费导入件与 ux-reference 的效果）；
3. 评测结论按 AGENTS.md §5 分析规程处理；验证有效的教训回流 EVOLUTION.md（多轮确认后才回流永久踩坑表）。

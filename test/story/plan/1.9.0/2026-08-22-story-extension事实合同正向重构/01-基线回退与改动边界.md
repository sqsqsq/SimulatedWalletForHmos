# 基线回退与改动边界

## 回退前备份

把上一轮未提交生产实现保存到 `E:\Project\bak\Story-Extension-Rejected-<时间戳>\`。备份包含 tracked patch、untracked 文件副本、清单和 SHA-256。备份只供追溯，新实现不得从中复制代码。

## 直接回退

恢复到当前 HEAD 的范围：

- `doc/extensions/` 的未提交改动和新增资产；
- `.codex/.claude/.opencode/.cac` 的 Story 入口改动；
- 上一轮为 Knowledge 演示加入的 CommFunc HA 源码、导出、Logger 变更和 Catalog 描述。

必须保留：已提交的 `test/story` 与 `tools/cli`、现有 `doc/features`、WalletMain `Loss*` 回灌产物、`framework.config.json`、已授权的 Framework 本地热修、根 `AGENTS.md` 测试入口和其它无关现场。

## 实施边界

生产实现允许修改 `doc/extensions/` 与四宿主 Story 入口。直接验证允许修改 `test/story/scripts/` 和 `test/story/tests/`。`framework/`、产品源码、Case 输入、历史设计正文和冻结方案目录均禁止修改。

回退完成条件：备份摘要可复核；回退路径与 HEAD 一致；保留路径摘要不变；上一轮内容稿、自审、Knowledge 2.0、适配和 HA 演示入口不存在。HEAD 中遗留的 Story Review 自动回流属于新实现需清除的基线问题。


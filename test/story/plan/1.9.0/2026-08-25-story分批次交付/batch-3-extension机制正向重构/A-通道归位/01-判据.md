# 子批 A · 通道归位 · 验收判据（先于实现）

> G3：判据在实现前从需求与冻结条款**独立重推**，每条带出处、口径（计什么不计什么）、责任层；
> 机械判据不得表述为语义结论。G5：任一机械 BLOCKER 或语义 FAIL → 本子批失败。
> 责任层：**机械**＝脚本/grep/wc 可判 · **语义**＝verifier 裁 · **行为**＝实跑或用户验。

## 1. 判据表

| id | 判什么 | 口径（计什么 / 不计什么） | 层 | checker | 出处 |
|---|---|---|---|---|---|
| **KA-1** | 作者向文本不再放在 hooks 里 | `hooks/**/*.md` 只剩 `author.md` 六份；每份 ≤60 行；内容只有四段（读哪些文件 / 产出形态 / 跑哪条命令 / 门禁拦什么）。**不计**：`.mjs` 里的注释、`rules/` 下的 overlay。**判 FAIL 的形态**：出现第七份 `.md`、任一份 >60 行、出现判据推理段（如「判据是…」「三问」整段展开） | 机械 | 台账新形态 | E1；00 §1.2（`.md` hook 只进 ai-prompt） |
| **KA-2** | verifier 注入体积受控 | 对 AR90004 **副本**跑 spec/plan/coding 三阶段 harness，`ai-prompt.md` 的 `## Lifecycle hooks` 段 ≤ **15KB**。计该标题到文件尾的字节数；**不计**产物全文、phase-rules、harness JSON | 机械 | 脚本 | E4、E6；00 §3（spec 扩展占 44.3%、核对表 66KB） |
| **KA-3** | overlay 判据少而实 | **本子批核「每条含三要素」**：14 条 `semantic_checks` 每条须同时含「裁什么对象」「引文要求」「FAIL 条件」，缺任一即 FAIL。**「每阶段 ≤5 条」移交子批 C**——基线 spec 有 7 条，其中 `story_reader_fitness`、`story_decision_substance` 两条随 story 移到 S5 后自然降到 5（01 §5.3 给 spec 排的正是 2 条知识判据 + story 的 2 条另计）。本子批删任何一条都是为凑数字砍判据 | 机械 | 脚本 | E4；01 §3.4 / §5.3 |
| **KA-4** | 通过也留痕 | 六个 `hooks/<phase>/post_check.mjs` **全部** import `shared/evidence.mjs`，且 `ok:true` 路径也写 `<phase>/reports/ext-post-check.json`。基线：仅 spec、plan 两个 import；AR90004 的 `coding/reports/` 无该文件 | 机械 | 台账 | 04 #2；00 §4.3（通过与没跑同形） |
| **KA-5** | 报错一次报全、首行指向 author.md | 构造**缺三项**的 spec 副本跑 spec post_check：单次 `message` 同时列出三项（不是报一项、修完再报下一项），且首行固定指向 `doc/extensions/hooks/spec/author.md`。**不计**：不同 check id 各报各的（那本就分开）；计的是**同一 check id 内**是否洋葱式逐层暴露 | 机械 | 单测 | 01 §2 原则 5；00 §2.3（同一 check id FAIL 5 次） |
| ~~KA-6~~ | **整条移交子批 C** | `knowledge.json` 是 story 附录「规约符合性」表的数据源，表里含**判「否」的条目及其依据**；spec §10 只收判「命中」的行，接不住这部分。01 §9 给它的替代物是「story writer 子 agent + S5」——**附录表改由 writer 直接写**，那是子批 C 的交付物。本子批先删就会在 A→C 之间丢掉「逐条目判定（含判否）」的能力。实测引用面也跨三批：`adjudication.mjs` 7 处（B）、`story-build.mjs` 5 处（C）、`spec/post_check.mjs` 4 处（本可在 A 删，但删了上面那条能力就没落点） | — | — | 01 §3.3 / §9 |
| **KA-7** | 行数净减 | `doc/extensions` 非知识层 < **10358**（`find . -type f -not -path "./knowledge/*" -not -path "./adapt/*" -not -name "*.png" -not -name "*.svg" \| xargs wc -l`）。含 adapt 交付件，只排除 `adapt/` 运行时目录 | 机械 | wc | G11；03 §0 ① |
| **KA-8** | 入口文件含扩展段 | 本仓 `CLAUDE.md` 与 `AGENTS.md` 的「实例扩展」节均含 `doc/extensions/AGENTS.section.md` 全文，两份**逐字相同**；且该段含「先读 `hooks/<phase>/author.md`」一句。**一次性核**，不设常驻检查（framework 重渲染覆盖时由 extension 升级 + adapt 重跑恢复） | 机械 | 脚本（与 adapt `--check ⑤` 同口径） | E1；用户 2026-08-28 裁定 |

## 2. 本子批独立重推的补充判据

上表来自 03 §3 的雏形。下面四条是从需求与本子批实际改动面重推出来的，雏形里没有：

| id | 判什么 | 口径 | 层 | checker | 为什么必须有 |
|---|---|---|---|---|---|
| **KA-9** | 删除项的内容确实有去处，不是丢弃 | 六份 `on_context_load.md` + `story_on_context_load.md` + `constraint-usage.md` 里的每条**作者向要求**，在 `author.md` / `phases/*.md` / 模板 HTML 注释 / overlay 四者之一有对应落点；逐条列表核对，无落点的须写明「为什么不再需要」 | 人核 | 交付报告一张表 | G11「不留新旧并存」的反面——删掉 645 行若无落点清单，等于**静默丢要求**；这正是批次 2 story 去重「删了就丢」的同一形态（02 §2.1） |
| **KA-10** | story-flow 关卡判据无损 | `spec/post_check.mjs` 重写**前**把 `storyFlowProblems`（`:490-887`）现有判据逐条列成清单；重写后逐条对照，每条要么在新实现里有对应，要么在交付报告写明删除理由 | 人核 + 单测 | 对照清单 | 它是 `/story` 三级关卡的唯一门禁（近 400 行）；重写时最容易「看起来跑通了、判据悄悄少了一半」 |
| **KA-11** | 机制层零知识硬编码不回退 | 新增与重写的文件里无域前缀、条目编号（`[A-Z]{2,6}-\d{2}`）、模式标识、角色名、Case 特征字面 | 机械 | 台账 M01（既有） | G7；`author.md` 写「门禁会拦什么」时最容易顺手写出规约编号当例子 |
| **KA-12** | 派生为空出声 | `spec/post_check.mjs` 从 §10 表解析三方集合时，**表不存在或零行**须显式 BLOCKER 报错并指路，不得静默当空集通过 | 机械 | 单测 | G7；基线 `pre_verifier.mjs:50` 已有同款注释（「让人看见判据清单没生效，而不是悄悄注入一份空要求」），重写不得丢 |

## 3. 不在本子批判的（避免越权判 FAIL）

| 项 | 归属 |
|---|---|
| 退场 grep 全量零命中（②）、无新旧并存全量（⑤）、判据③ 全量 | 子批 C 收口首次成立，D 再核一次（03 §0「核的时机」） |
| `plan`/`coding` post_check 的判据内容 | 子批 B 从 0 重写；本子批只加留痕与 try/catch |
| `story-build` / `story-chapters.json` / `merge-story` / 来源单元枚举 | 子批 C |
| 效率七项（回环占比、读文件次数、上下文增量） | 子批 D 实跑度量 |
| author.md 内容是否**真的**让作者做对了 | 行为层，子批 D 实跑 + 内网复测；本子批只判送达形态 |

## 4. 总判定（G5）

本子批 PASS ⟺ KA-1…KA-12 全部满足 **且** §5 的固定验证全绿 **且** §0 ①（行数净减）+ ③（限本子批已处置文件）+ 本子批删除项退场 grep 零命中。

任一机械项 FAIL → 本子批失败，不得以「后续子批会修」为由放行。

# `hooks/spec/post_check.mjs` 判据清单（重写前的对照基准 · KA-10）

> **2026-08-28 修订**：本清单原按「子批 A 从 0 重写此文件」编。实施中核出 895 行里
> 被替换的只有约 60 行（`knowledge.json` 那条链），其余 810 行在 01 设计里原样保留；
> 且 `knowledge.json` 的退场依赖子批 C 的 story 附录新产出方式（KA-6 已整条移交 C）。
> 所以子批 A 对本文件只做**新增通用能力**：顶层 try/catch、PASS 也留痕、报错一次列全
> 并显式声明未执行的判据。下表 A9 / B1–B3 / C1 / C2 的「重写后」列标的是**子批 C** 的目标态。

> 基线 `e96e90fc`，895 行。重写后逐条对照：每条要么在新实现里有对应，要么在交付报告写明删除理由。

## A. 知识出口 §10 / §11（`knowledgeExitProblems` `:231-347`）

| # | 判什么 | 行 | 重写后 |
|---|---|---|---|
| A1 | 派生自检（`selfCheck`）——知识派生为空时出声，不静默通过 | `:242` | 保留（G7） |
| A2 | 域级判定（`domainProblems`）：条件域先判域 | `:247` | 保留 |
| A3 | §10 / §11 并进了 §9——三章须独立成节 | `:263` | 保留 |
| A4 | §10 缺表格（表头须含「编号」「本需求的要求」） | `:271` | 保留 |
| A5 | 编号未到条目级（只写域前缀） | `:282` | 保留 |
| A6 | 编号不在激活清单里 | `:290` | 保留 |
| A7 | 处置标（评审动作）的条目写进了 §10 | `:294` | 保留 |
| A8 | 要求是规约原文的复制或子串 | `:301` | 保留 |
| **A9** | **要求与登记源 `AR/story-src/knowledge.json` 不同文** | `:312` | **删除**（KA-6：§10 表本身就是登记源，不再有第二份） |
| A10 | 缺 §11「设计模式候选登记」章 | `:320` | 保留 |
| A11 | §11 没登记任何适用单元 | `:326` | 保留 |
| A12 | 判无候选但没写理由 | `:332` | 保留 |
| A13 | 候选不在册 | `:337` | 保留 |

## B. 三方 ID 集合（`idSetProblems` `:354-408`）

| # | 判什么 | 行 | 重写后 |
|---|---|---|---|
| **B1** | `knowledge.json` 解析失败 | `:372` | **删除**（该文件退场） |
| **B2** | 归档件判「命中」但 spec §10 无对应行 | `:379` | **改口径**：命中集改从 §10 表按表头名解析，与 acceptance 双向比对 |
| **B3** | spec §10 有要求但归档件没判「命中」 | `:383` | 同上 |
| B4 | 有代码要求但 `acceptance.yaml` 无对应验收条目 | `:396` | 保留 |
| B5 | `acceptance.yaml` 的 `knowledge_rule` 指向 spec 没有的条目 | `:402` | 保留 |
| **新** | §10 表不存在或零行时**显式 BLOCKER 并指路**，不静默当空集通过 | — | **新增**（KA-12） |

## C. story 产物（`STORY_CHECKS` / `storyArtifactProblems` `:445-468`）

| # | 判什么 | 行 | 重写后 |
|---|---|---|---|
| C1 | story 校验脚本跑不起来 | `:458` | **移交子批 C**（01 §6.1：spec post_check 删全部 story 判据） |
| C2 | story 校验未通过 | `:463` | 移交子批 C |

## D. story-flow 三级关卡（`storyFlowProblems` `:490-887`，近 400 行）

**全部保留**——它是 `/story` 三级关卡的唯一门禁，01 §6.1 明写 spec post_check「只留 §9/§10/§11 与 story-flow 收口判据」。

| # | 判什么 | 行 |
|---|---|---|
| D1 | 契约在但没有任何轮次记录 | `:514` |
| D2 | 缺 `analysis.sha256`——无从证明这轮重新初析过 | `:521` |
| D3 | `analysis.sha256` 与上一轮相同——初析没重做，不构成新一轮 | `:523` |
| D4 | 缺 `positioning.scope_text`——本 AR 范围没定就收口 | `:535` |
| D5 | `positioning.sr_related_ars` 不是数组 | `:537` |
| D6 | `sr_related_ars` 含本 AR 自己 | `:539` |
| D7 | 缺 `scope_options`——选项集没落契约，第二级只能现编 | `:544` |
| D8 | `scope_options` 缺固定首项 `carry_all` | `:546` |
| D9 | 缺 `gates` 数组 | `:552` |
| D10 | `gate` 值非法（三种之一） | `:559` |
| D11 | 缺 `options`——摆了哪些选项没留痕 | `:564` |
| D12 | `chosen` 不在 `options` 里——选的必须是摆出来的 | `:566` |
| D13 | `material_scope` 的 `chosen` 非法 | `:569` |
| D14 | 承载份选择的合法性 | `:578` |
| D15 | `outcome` 非法（`accepted` / `rejected`） | `:585` |
| D16 | 被拒却没写 `reason` | `:588` |
| D17 | 缺时间戳 `at` | `:590` |
| D18 | AI 代选但缺 `basis`（用户授权原话） | `:593` |
| D19-D21 | 关卡顺序与前置（`:612` / `:619` / `:627`） | |
| D22 | 已定案切分但缺该级关卡记录 | `:637` |
| D23-D24 | 收口前置（`:649` / `:658` / `:668`） | |
| D25 | 标 `complete` 但 `design_generated_at` 为空 | `:674` |
| D26 | `design.md` 与契约范围一致性 | `:694` |

## E. 阶段产物与章节结构（`:737-766`）

| # | 判什么 | 行 | 重写后 |
|---|---|---|---|
| E1 | 缺本阶段产物 | `:737` | 保留（story 那两项随 C1/C2 移交 C） |
| E2 | 缺宿主扩展章节 §9 / §10 / §11 | `:753` | 保留 |
| E3 | §9 缺小节（小节不得删） | `:759` | 保留 |
| E4 | 小节未填写 | `:764` | 保留 |
| E5 | 残留模板占位「{ … }」 | `:766` | 保留 |

## F. 独立审计（`:780-861`）

| # | 判什么 | 行 | 重写后 |
|---|---|---|---|
| F1 | 术语映射表里 in_scope 业务模块缺说明 | `:789-824` | 保留 |
| F2 | 客户端禁用词 | `:818` | 保留 |
| F3 | 文档坐标（`spec §x` / `见 A5`） | `:834` | 保留 |
| F4 | 数值缺来源类型标注；标「上游约束」时回查 SR/RR 原文 | `:843-861` | 保留 |

## G. 逐行裁决核对（`:867-893`）

| # | 判什么 | 重写后 |
|---|---|---|
| G1 | verifier 报告里有逐行裁决表（`adjudicationLanding`） | 保留（`evidenceVerified` 替代它是**子批 B** 的事） |
| G2 | 知识派生失败时不静默通过——那会让本判据恒真 | 保留（G7） |

## 全局新增（本子批）

| 项 | 判据 |
|---|---|
| 顶层 try/catch → `severityOverride:'BLOCKER'` | 绕过 framework 的崩栈降级（`hooks-dispatcher.ts:190-200` 退回 MAJOR） |
| PASS 也写 `ext-post-check.json` | KA-4 |
| 报错一次列全、首行指向 `hooks/spec/author.md` | KA-5 |

## 合计

**53 条判据**：删 3（A9、B1，C1/C2 移交）、改口径 2（B2/B3）、新增 4（KA-12 + 三条全局）、保留 46。

# 子批 A · 通道归位 · 交付报告

> 四栏分立（AGENTS.md §4「文档状态 ≠ 代码真值」）：**实施事实** / **离线验证** / **历史回归** / **行为验证**。
> 行为栏由子批 D 实跑与内网复测后填写；在此之前本子批**不构成验收结论**（G5）。

## 一、实施事实

七步，每步单独提交，基线 `af205cee`（`ext.b3-step0` 后）。

| 步 | 提交 | 内容 |
|---|---|---|
| Step 0 | `af205cee` | 取回旧批次 2 的三处测试域 hunk（批次 3 共用前置） |
| Step 1 | `b7dd4c99` | `AGENTS.section.md`（5 行）+ 写进本仓 CLAUDE.md / AGENTS.md 的「实例扩展」节 |
| Step 2 | `7b7cedc1` | 六份 `author.md` + `phases/spec.md` + `templates/plan-sections.md` |
| Step 3 | `bc45379d` | 删六份 `on_context_load.md` + `story_on_context_load.{md,mjs}` + manifest 登记 + 四处引用改指 |
| Step 4 | `5d922b87` | `SKILL.md` 加 `phases/spec.md` 指路；三份 reference/rules 移出重写面（偏差二、三） |
| Step 5 | `5f4d7747` | `shared/gate.mjs`：崩栈自报 BLOCKER、通过也留痕、报错一次列全；六个 post_check 接入 |
| Step 6 | `2d19790e` | `pre_verifier` 不再注入被检文本；14 条 overlay 判据补齐三要素 |
| Step 7 | `b40a5178` | 台账三条新形态 A03 / A04 / A05 + 夹具 |

**交付形态**：一条从入口文件到阶段须知的**机械送达链**——
入口文件（会话开始自动加载）→ `hooks/<phase>/author.md`（动笔前读）→ 模板与作业包（写的时候照着）
→ post_check 报错（写错了一次报全，首行指回 author.md）。基线态这条链**一段都没有**。

## 二、KA-9 · 删除项的落点核对（583 行，逐条）

| 原文件 | 内容 | 落到哪 |
|---|---|---|
| `spec/on_context_load.md` | 「spec 是什么」「内容三分」 | `templates/spec-sections.md` 卷首注释（原已有，未重复搬） |
| | §10 / §11 两个出口的写法 | `spec/author.md` §二 + `spec-sections.md` 各节注释 |
| | 三章边界（§9 有什么 / §10 满足什么 / §11 可选什么） | `spec/author.md` §二 |
| | 「写每一行前的三个自问」 | `reference/constraint-usage.md`（未删，已进 author.md 必读表）+ overlay 语义判据 |
| | 独立审计：不写文档坐标 / 结论可按名回查 | `spec/author.md` §四（门禁会拦什么） |
| | 数值必须标来源类型 | `spec/author.md` §四 |
| `plan/on_context_load.md` | 「知识决策章必须在设计章之前」+ 位置即语义 | `plan/author.md` §二 + `plan-sections.md` 章骨架注释 |
| | `knowledge_freeze` 的 YAML 形态 | `plan-sections.md` 冻结块（含中性示例） |
| | 实体引用语法 | `plan/author.md` §二 + `plan-sections.md` |
| | 四阶段验证要求靠 `criterion`/`ut_layer` | `plan/author.md` §二 + `plan-sections.md` 末段 |
| | 「什么叫真的应用了」三类知识对照表 | `plan-sections.md` 卷首注释 |
| | 有落点 vs 没落点的正反例 | `plan-sections.md` 末段 |
| | 门禁 14 条 | `plan/author.md` §四（合并同类后 12 条） |
| | 与 spec 的衔接（无候选时不凭空冻结） | `plan/author.md` §四末句 |
| `coding/on_context_load.md` | 「读到的是冻结结果」 | `coding/author.md` 卷首（四份重复段压成一句） |
| | 「显式不适用优于静默跳过」 | 同上 |
| | 本阶段要落实什么（landing / roles） | `coding/author.md` §二 |
| | 门禁两条 | `coding/author.md` §四 |
| `review/on_context_load.md` | 复核表形态 | `review/author.md` §二 |
| | 怎么判（`review_focus`、指向 diff 位置、不适用要具体） | `review/author.md` §二 + §四 |
| `ut/on_context_load.md` | `ut_layer` 分派三态表 | `ut/author.md` §二 |
| | 怎么算覆盖（断言 vs 用例名） | `ut/author.md` §四 |
| `testing/on_context_load.md` | `ut_layer` 反向分派 | `testing/author.md` §二 |
| | 「已验证」不构成结论、待验证要诚实 | `testing/author.md` §二 |
| `story_on_context_load.md` | 上游输入必读清单（AR/RR/SR + 范围拆分说明） | `phases/spec.md` §一 |
| | 本阶段产出三份文档 | `phases/spec.md` §二 |
| | 逐章成文四条命令 | `phases/spec.md` §二（**修正**：原写「由 `story_flow.py status` 打印」，实测 `next_step` 到 `complete` 只返回「直接进 /spec」，不打印这些命令） |
| | §9 技术契约的形式与取证两源 | `phases/spec.md` §三 |
| | 防重复（哪些内容归哪章） | `phases/spec.md` §三末段 |
| | 知识判定另两个出口 | `phases/spec.md` §二 + `constraint-usage.md`（未删） |
| | 写判定结论前的三个自问 | `constraint-usage.md` + overlay `knowledge_spec_exit_substance` |
| | 数值在 story 侧形态不同 | `reference/evidence-rules.md` §3.2（原本就是真源，此处只是复述，不再搬） |
| | spec 闭环后的下一步提示 | `phases/spec.md` §四 |
| `story_on_context_load.mjs` | 场景探针（按 `story-flow.json` 存在与否决定注入） | **不再需要**：内容已按阶段拆到 author.md 与 `phases/`，作者自己判断走没走 `/story`；`spec/post_check.mjs` 里的 `isStory` 探针保留 |

**一条都没丢**；两条明确改写：命令真源那条是修正错误事实，数值形态那条是去掉复述、指回真源。

## 三、离线验证

| 项 | 结果 |
|---|---|
| 台账全量 | **41 / 41**（FAIL 0、SKIP 0；新增 A03、A04、A05） |
| Story 单测 | 140 passed + 7 subtests（基线 137，Step 0 新增 3） |
| 复述分支 | 9 / 9 |
| `node --check` | 全部改动的 `.mjs` 通过 |
| `python -m compileall` | 通过 |
| `--phase extensions` | PASS 6/6，Blockers 0 |
| KA-11 零硬编码 | 新增件 grep `[A-Z]{2,6}-\d{2}` 零命中 |

**实跑验证**（不是只看语法）：

| 验的什么 | 怎么验 | 结果 |
|---|---|---|
| 六个 post_check 能跑、都留痕 | 临时工程根实跑六个阶段 | 六个全部 `ok=true` + 生成 `ext-post-check.json`，且各自报出「哪条判据没跑成、为什么」 |
| 崩栈自报 BLOCKER | `guard()` 包一个抛异常的主体 | `ok=false severity=BLOCKER`，文案说明「判据一条都没跑完，结论不可用」 |
| 报错一次列全 | 造 3 问题 + 1 未执行判据 | 单条 message 列全 3 项 + 1 条未执行，首行指向 `hooks/spec/author.md` |
| 不插手别的阶段 | 用 `phase: plan` 调 spec 的 hook | `ok=true` |
| pre_verifier 注入体积 | 对 AR90004 实跑 | spec 5882 → **3801** B、plan 4332 → **2944** B（01 §3.4 要求 ≤5KB） |
| 入口段送达 | 规范化空白后子串比对 | CLAUDE.md 与 AGENTS.md 均含全文、逐字相同 |

## 四、历史回归

| 项 | 结果 |
|---|---|
| 既有 38 形态 | 全部仍 PASS（本子批未使任何既有形态失效） |
| A03 反夹具 | 抓到「作者向文本放在 hooks 里」 |
| A04 反夹具 | 抓到「通过时不留痕」 |
| A05 反夹具 | 抓到「入口文件没带扩展段」（两份都报） |
| 三条真实目标 | 均 PASS（A04 认出六个阶段经 `evidence.mjs` / `gate.mjs` 间接留痕） |

## 五、行为验证

**未做**。KA-2（verifier 注入体积对真实 harness 运行的影响）、效率七项、作者是否**真的**因此做对了——
全部归子批 D 实跑与内网复测。本子批只交付并验证**送达形态**。

## 六、与方案的差异

| 项 | 方案 | 实际 | 理由 |
|---|---|---|---|
| 行数 | 净减（估 −1028） | **+29**（10358 → 10387） | 见下「行数账」。子批 A 是从无到有建通道，删的是送错对象的文本、加的是通道本身 |
| `SKILL.md` | 从 0 重写 | 只加一句指路 | 偏差三：01 §9 给它的替代物是「写作/命令段 → phases/」，基线上这个文件没有写作段 |
| `rules.md` / `evidence-rules.md` | 重写、装配器措辞清零 | 不动 | 偏差二：装配器此刻真的还在，清零会造成文档与实现相斥 |
| `constraint-usage.md` | 删除，内容进 author.md | 保留，挂进必读表 | 偏差一：62 行内容不重复，author.md 已 51 行，塞进去必破上限或丢内容 |
| `spec/post_check.mjs` | 从 0 重写（895 → 400） | 只加通用能力 | 895 行里被替换的只有约 60 行，其余 810 行 01 设计里原样保留；为凑「相同行占比 <30%」重写它们是纯措辞变动加回归风险 |
| `phases/story-write.md` | 本子批交付 | 移交子批 C | 消费者（S5 writer）子批 C 才建，现在交付即零消费者导出（G11） |
| KA-6 `knowledge.json` 退场 | 本子批 | 移交子批 C | 它是 story 附录表的数据源，表里含判「否」的条目及依据；spec §10 只收命中行接不住。先删会在 A→C 之间丢掉「逐条目判定」的能力 |
| KA-3 overlay ≤5 条 | 本子批 | 三要素在本批（14/14），≤5 条移交子批 C | 基线 spec 7 条，其中两条 story 判据随 story 移到 S5 后自然降到 5；本批删任何一条都是为凑数字砍判据 |
| `pre_verifier` 行数 | 251 → 120 | 253 | 注入**内容**压了 35%，脚本本身没有死代码可删。判据是注入体积（≤5KB），不是脚本行数 |

### 行数账（G11）

| 项 | 行 |
|---|---|
| 删除（送错对象的文本） | **−583** |
| `author.md` ×6 | +283（原 400 行压缩后送对对象） |
| `gate.mjs` | +120（一处定义替代六处样板，并补崩栈/留痕/一次报全三件基线没有的能力） |
| `phases/spec.md` + `plan-sections.md` | +185（183 行 `story_on_context_load` 拆成有归属的两份） |
| overlay 补三要素 | +40（判据从「说不清怎么裁」补成可核实契约） |
| `AGENTS.section.md` + 引用改指 | +5 ± |
| **净** | **+29** |

§0 ① 要求「任一子批上升即停下重看是不是又在结构外加围栏」——**已重看**：新增的每一项要么有明确替代对象（六处样板 → 一处、400 行 → 283 行），要么补上基线**缺失**的能力（崩栈不降级、留痕 2/6 → 6/6、报错一次列全、引文契约）。没有一项是在既有结构外加围栏。真正的大幅删减在子批 B（`adjudication` / `freeze` / `downstream` 账本链）与 C（`story-build` 1337 行装配器）。

用户 2026-08-28 裁定：「字数限制是要求正向实现、不要打补丁；实现本身合理可以突破数量限制。」

## 七、实施中坐实的方案缺陷（回写方案）

| # | 缺陷 | 怎么暴露的 | 处置 |
|---|---|---|---|
| N1 | 判据③ 的 13 项清单里有 3 项没有替代物（`SKILL.md` / `rules.md` / `evidence-rules.md`）——把「文件大、文本旧」当成了「承载被替换的机制」 | 逐项对照 01 §9 替代关系表 | 三项移交子批 C；03 §0 判据③ 加「本清单只收 01 §9 有明确替代物的文件」 |
| N2 | 02 的行号与结论基于 `67cdc068`（旧批次 2 已删装配器之后），被 03 当成 `e96e90fc` 的现状 | 要清「装配器措辞」时发现装配器还在（`story-build.mjs:1329-1330`） | 与 Q2 同形态；三份文件的措辞随子批 C 同步 |
| N3 | `story_flow.py status` 不打印成文命令 | 写 `phases/spec.md` 时核 `next_step` 实现 | 四条命令直接写进作业包（当前形态，子批 C 换形态时重写） |
| N4 | 我删 `downstream.blocker()` 时漏了 `downstream.mjs` 自己在用它 | `node --check` 查不出（只查语法）；grep 全域引用时发现 | `loadLedger` 契约改为返回 `problem` 文本，报错统一走 `gate` |
| N5 | A03 初版把「判据是**X**」当推理段标记 | 台账真实目标当场 FAIL 在 `ut/author.md` 一句正确的门禁说明上 | checker 的词太宽，不是文件写错；改为只认带标题的推理块 + 补结构判据（二级段落 ≤6） |
| N6 | 00 §3 记的「核对记录表 66KB」是旧批次 2 的 272 行 story 表 | 实测基线 pre_verifier 只注入 18/13 行、5.9KB | KA-2 的 15KB 在删掉六份 `on_context_load` 后已自然满足；仍做了被检文本列的压缩（−35%），因为回声问题与体积无关 |

## 八、遗留与下一步

1. **子批 B**（义务上实体 + 探针）：contracts 实体 `must`、探针列、`knowledge.mjs` 派生索引、`coding/post_check` 重写、删 `knowledge_freeze` / `freeze` 账本读取 / `downstream`。`plan`/`coding` 的 `author.md` 随其 post_check 一起重写。
2. **子批 C**（story 机制）：删 1.0 逐章装配器、S5 writer、整篇 token 守恒；本子批移交给它的四项——`SKILL.md` 从 0 重写、三份 reference/rules 的装配器措辞、KA-6 `knowledge.json` 退场、KA-3 的 overlay ≤5 条。
3. **子批 D**：效率七项实跑度量 + 内网复测；本子批的行为栏在那时填。
4. 收口后新验证机制入 `../../02-已验证机制参考.md`、新坑入 `../../03-踩坑记录.md`。

# 批次 3 · F 修复 · 判据（先于实现）

| id | 判据 | 口径 | checker |
|---|---|---|---|
| **KF-1a** | story 是 spec 交付件 | `isStory` 且 flow 未 `story_written` → spec post_check BLOCKER，文案含登记命令；`story_written` / `archived` → 放行；无 `story-flow.json` 不受影响 | 单测 + 台账 F01 |
| **KF-1b** | 行为 | ISSUE-410 CLI 重跑到 plan：spec 闭环时 `AR/story.md` 已在、`story_written` 已登记、check 通过 | CLI 实跑 |
| KF-2 | 材料按模板约定 | 两 Case 副本枚举：SPEC 的 `>` 单元全部 `machine_facing`（origin: template-notes）；PRD / SE / design 的 blockquote 不受影响；合同无 `---` / 正则类形状规则 | 脚本对照样本 |
| KF-3a | 审稿任务良定 | `story-verify.md` 只有逐对裁决任务，无整篇维度；每条输出二值 + 引文或缺项 | 台账（作业书结构） |
| **KF-3b** | 行为：区分力 | 两 Case 各删 5 个事实（含 ≥2 纯中文单元）用子 agent 重跑 verifier → 各 ≥5 条「未讲清」并点名；不删 → 与现产物一致；写入 TEST.md 常驻项 | 副本实跑（输出贴交付报告） |
| KF-4 | 契约一份 | 扩展只读 `<feature>/contracts.yaml`；只有 `plan/contracts.yaml` 时不回退、报「缺契约」；`plan/contracts` 字样全仓零命中（知识层除外） | 单测 + grep |
| KF-7a | 分配即恰好一处 | 分配完成后 `audit`：三态皆空 = 0、`covered_by` 全有效；任一单元只有一条记录 | 单测 |
| KF-7b | 逐章渲染可续写 | 渲染一章后 `audit` 只改该章命中单元的 `by`；未渲染章清单正确；续写不重写已有章 | 单测 |
| **KF-7c** | 行为 | ISSUE-410 重跑到 plan，记录每章落盘与是否用了 Task；无子 agent 路径（主 agent 渲染）至少在一个 Case 上跑通 | CLI 实跑 |
| KF-0 | 机制层干净 | 非知识层 Case 字面（含注释）零命中；零消费者 `export` = 0 | grep 脚本 |
| KF-5 | 行数 | 收口重算，与 10358 / 9613 并列写进报告 | wc |
| KF-6 | 纪律 | 03 / 01 / 05 / 06 / TEST.md 目标未被执行者改动 | 维护者 |

夹具：`test/story/fixtures/failure-modes/F01-spec-without-story/{bad,good}`。

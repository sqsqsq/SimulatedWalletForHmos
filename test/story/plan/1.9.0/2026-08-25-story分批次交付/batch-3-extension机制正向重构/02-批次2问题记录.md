# 02 批次 2（重排前编号；现批次 4）· story 去重第三版问题记录（只记录，不作基线）

> **行号说明**：本文行号指 `67cdc068`（旧批次 2 的产物），只作问题记录。批次 3 的代码基线是 `e96e90fc`，文中若干对象（`source-units.mjs`、`audit --verify`、自由文本 `reason`、`covered_by` 等）在基线上**不存在**——它们不是本批的删除对象，替代关系以 01 §9（按 e96e90fc 列）为准。

> 对象：`story` 分支上批次 2 的 14 个提交（`36c55c62` … `67cdc068`，未 push）。用户裁定 2026-08-28：方向不再修补，新设计基于批次 1 归档态 `e96e90fc`。本文只记录，供 [01-正向设计.md](01-正向设计.md) 避坑与 [03-实施切分.md](03-实施切分.md) 决定保留项。

## 1. 方向正确、新设计保留的

| 项 | 去向 |
|---|---|
| 删逐章生产线（scaffold/assemble/逐章守恒/编号守恒/占位符/附录） | 保留 |
| story 由作者读完全部材料作为一份文档写成 | 保留，改为 writer 子 agent（01 §6.2） |
| review.md 自解释语法（问题/建议或结论/为什么/影响/请谁确认/三勾选） | 保留 |
| 知识两表移出 story | 保留；且 `knowledge.json` 本身也不再手写（01 §3.3） |
| 归档态改判流程契约（不绑「源摘要」特征行） | 保留 |
| **测试域**：阶段推进归驱动器（Case prompt 不写阶段链）、回灌排除 `oh_modules/build`、交互脚本 `expected_phase`/`requires_archived` 前提 | 保留（03 §1 cherry-pick） |
| 夹具中性题材、机制层零 Case 特征 | 保留纪律 |

## 2. 缺陷（行号以 `67cdc068` 为准）

### 2.1 内容守恒
- `story-build.mjs:816` `reason` 只判非空——自由文本逃生口；实跑 161/272 单元「不进」，理由去重后 2 种。
- `story-build.mjs:659-681` `audit --verify` 不做任何校验、永远 exit 0；`hooks/spec/post_check.mjs:445` 把它挂成 BLOCKER「核对记录齐备」。
- `source-units.mjs:245-250` `table_row` 只从首列取 ASCII 标识符 → 中文表格全部掉进 reason；`:100-108` 表头永不成为单元 → 142 条「表头」理由结构上恒假且机器判得了。
- `story-build.mjs:643` 机器落点要求全部 token 同章 → 分开讲即判无落点；`:649` reason 无条件继承，与 `at` 并存不报。
- `story-build.mjs:686-758` 无图片文件名/链接 URL 整篇校验；`merge-story.mjs:173` 守恒只取契约表首列，字段名与 `freezeTicketId` 不在集合。
- 交付报告把 59% 不进写成「272 全部有交代」、把 142 行说成「不是略写」——指标方向错。

### 2.2 语义裁决
- `verifier-report.mjs:30,72` 只查同行含键 + 裁决词；证据列零校验；一行列全键 + 一个「成立」即全过；`成立|错` 单双字误命中。
- 实跑 `spec/reports/verifier.report.md:55-57` verifier 自述恒真规则；272 行证据 14 种字符串，全为 `audit.json` 回声；0 条「落点错/漏了」。
- `pre_verifier.mjs:253-290` 注入 272 行表，spec prompt +66KB（+56.6%）。
- `rules/spec-rules.overlay.yaml:77-101` 漏加方案 §8 / K2-E2 要求的「出现编号、类名…即不达标」句。

### 2.3 写作指令
- `hooks/spec/story_on_context_load.md:41-85` 负向 13 条 / 正向 5 条 / 「每件事都要有落点」0 条；`:83` 把「填 at」与「填 reason」写成平权；`:84` 把合法理由清单交给被判方；`:63-64` 承诺的机械回查不存在。
- 指令只到 verifier（见 00 §1）；作者读到靠 `SKILL.md:299` 一句。

### 2.4 测试域
- `check_failure_modes.py:1481-1483` P19 记录缺失判 PASS、未实现键集比对；`:1509-1535` P20 做成另一件事；`:1153-1183` p11 不覆盖 audit 键；`:1726` 裁决词过宽。
- `measure_dedup.py:78-83` 不输出理由种类数与不进比例；不读 `also_in`。
- `test/story/scripts/run-hook.mjs:4` 用法注释仍写 `scratch/`。

### 2.5 正向实现纪律
- `skills/story/reference/evidence-rules.md:130` 仍写「装配器渲染成归档件的附录单表」（新旧并存，spec 必读件）；`constraint-usage.md:32,35`、`import_sources.py:5,325`、`SKILL.md:139`、`story_flow.py:903,936`、`post_check.mjs:740` 仍提「装配器」。
- `story-build.mjs` 10 个 `export` 零 importer；`source-units.mjs` 的 `id` 与 `also_in` 派生后零消费者。
- 处置台账 schema 抄三份且已漂移（`story-build.mjs:832-858` / `hooks/plan/post_check.mjs:331-366` / 台账 p23）。
- 退场 grep 5 命中（`状态\*\*` 误命中）、行数 9660 未达方案 <9000，交付报告未登记偏差；冻结清单重出 6 次。
- `post_check.mjs:877` `knowledge_exit_structure` 留痕 id 取全部 problems，与其声称内容不符。

### 2.6 其它实跑发现
- decisions 六类漏扫两类（约束规约命中项、上线与协同）；4 议题 3 个是上游已定宣读；`review_gate_acknowledged` 12 个勾选全空却无 WARN。
- 术语表 L18/L22 逐字重复（P27 事后加，对本轮产物 FAIL）。
- story 4 处无源事实（「管理台」「DIcon / 网络层打开时点」「加密…按项目知识」「叙事件」）。
- 第一轮 `b2v3c` `command_failed` 根因未坐实。

## 3. 批次 2 交付文档的处置
`../batch-4-内容去重与review/STATUS.md` 状态改「用户评审未通过（2026-08-28），方向作废」；00-方案/01-判据/02-交付报告原样留档不改。

# 06 全批次评审（维护者 · 2026-08-29 · `b7dd4c99`→`bfedb22e`）

> 范围：子批 A/B/C/D/R 全部 33 个提交 + `story-suite-20260828-191953` 两 Case 实跑。基线 `e96e90fc`。
> 全部只读实证：对回灌件 `doc/features/AR90004` 跑 `story-build check`、对四样本只读枚举、读两 Case 的 `events.jsonl` / `ext-post-check.json` / `gate_diagnostics.json` / `ai-prompt.md`、跑单测与台账、逐文件算相同行占比。
> 按 `test/story/AGENTS.md` §4：实施事实 / 离线 / 行为 分栏；离线全绿 ≠ 行为完成。
> **结论一句话：四项机制在真实产物上成立，交付闭环未完成——三处必修（F3 致命）、两处待裁定。**

## 一、对照批次目标（结果，不是过程）

| 目标 | 结论 | 证据 |
|---|---|---|
| 知识应用在 plan / coding 生效（批次 1 内网失败的那条链） | **成立** | AR90004：plan `knowledge_obligation_on_entity` PASS（15 条 must 挂在契约实体上）；coding `knowledge_landing_in_code` PASS（义务 15、角色 4）；契约里 UX-01 / OBS-01 标 `verify: probe`，探针在契约点名文件上执行。ISSUE-410 同链 10 条 PASS。verifier 引文核实在真实运行里拦下一次回声（R/04 §2.2） |
| story 不丢（语义覆盖） | **机制成立，判据面一半是橡皮章** | 304 单元三态皆空 0；`check` 九项通过（机器核实 264、S5 裁决 40）；无 token 表格行 96 条里 82 条至少一格原文逐字在 story；图 1/1、mermaid 3；token 区分力仍点名 `freezeTicketId`/`eligible`/AC-R。**但** 40 条作者落点的裁决 43/43 全「讲清」、0「未讲清」，引文与单元共享 ≥2 个中文二元组的只有 28/43——verifier 抄的是同章任意一句（见 §三 F1） |
| **story 必须成文** | **致命 bug** | ISSUE-410 四阶段全绿、`AR/story.md` 不存在：SKILL 成文节写「spec 闭环之后、归档之前」，本地单没有归档，「归档之前」这个时点不存在，S5 被跳过。没有任何阶段边界要求成文（§三 F3） |
| 效率七项 | **未达成 5 项、未取 2 项** | 读 checker 源码 **19 / 22**（目标 0）；读机制文本 96 / 115（≤20/阶段）；全程上下文 +803K / +607K（spec 单取未做）；扩展注入 ≤3.1KB/阶段（✓，从 44% 降下来）；同 id FAIL ≤1（✓）；行数 9613（✗） |
| 扩展文字只减不增 | **减了 7%，目标未达** | 10358 → **9613**（−745；不含 adapt 的裸基线 10022 算也 −409）。A +29 / B +268 / C −1066 / R +24。≤7500 差 2113；剩余三大块 story_flow 1045 / 六 post_check 1312 / IO 层 799 都不是被替换的机制 |
| 三面分离 | 作者面 ✓ 传递面 ✓ 判据面 ✓/✗ | 入口段逐字进两份入口文件、六份 author.md、gate 统一出口；实跑里被测模型第一步就读了 `AGENTS.section.md` → `SKILL.md` → `phases/spec.md`（runlog:121-139）。判据面缺的是 S5 verifier 的区分力 |
| 不打补丁（AGENTS §2 四判据） | ① ✓ ② ✓ ③ ✗ ④ ✓ | ① 旧概念 grep 零命中；② 未见静默降级新增；③ **15 个零消费者导出**（`tokensOf`/`methodBody`/`parseProbe`/`UNIT_KINDS`/`MUST_HOSTS`/`KNOWLEDGE_KINDS`/`INDEX_KIND`/`splitFrontmatter`/`frontmatterPairs`/`PROBE_KINDS`/`ENTITY_KINDS`/`authorDoc`/`maxSimilarity`/`ownTermHits`/`BANNED_TERMS`）；④ 无 TODO/临时 |
| 机制层零测试特征 | **2 处漏网** | `probes.mjs:73` 注释写 `LossReportSheet`、`story-build.mjs:132` 注释写 `WalletMain`——台账 M01/M02 没抓到注释 |

## 二、分栏

**实施事实**：新增 22 文件、删除 14；`e96e90fc→HEAD` 非知识层 +3827/−4237。R 轮没有回写 03/01/05/TEST.md（KR-6 ✓）；C 报告三处失实已就地更正；冻结后两处修（D1 flow 取值域、D3 measure_run 取错层）如实登记新指纹。
**离线**：单测 164+7、台账 48/48、`--phase extensions` PASS、退场 grep 零、node --check / compileall 过。
**行为**（两 Case）：AR90004 全链 story→review 走通，S5 writer + verifier 两个子 agent 真实发生（Task 38 次；story-write.md 读 4 次、story-verify.md 读 2 次），终检因 D1 翻红（已修）；ISSUE-410 四阶段全绿但**没做 S5**，`story_build_check` 如实拦下——门禁对、作业书错。

## 三、必修（方案 [F-修复/00-方案.md](F-修复/00-方案.md)：从六条原则重推，机制只改四处 D1–D4，其余保留；判据 [F-修复/01-判据.md](F-修复/01-判据.md)）

| # | 问题 | 根因 | 落点 |
|---|---|---|---|
| **F3（致命）** | 本地单没写 story | 批次 3 把 story 从 spec 三份产物挪到闭环之后当 S5，没有阶段守着；「归档之前」对本地单不存在 | **D1**：story 回到 spec 交付件，writer/verifier 在阶段内起，门禁恢复核三份齐备 + spec sha 同版，删 S5 |
| F1 | S5 verifier 无区分力 | 模板生成的文字被当材料（按来源判，不按形状）；verifier 任务不良定；验证一次性 | **D2** 材料按来源定义；**D3** verifier 逐对裁决 + 删事实对抗测试设为常驻回归 |
| F2 | 契约双源 | 扩展读 `plan/contracts.yaml`，framework 读根目录 | **D4** 单路径 |

顺带：两处注释 Case 字面改中性；15 个零消费者 `export` 去掉。

## 四、已裁定（2026-08-29）

1. 行数：本批按实数 **9613** 收口，≤7500 保留为长期项（03 §0 ① 已注）。
2. ③ 判法：`spec/post_check.mjs`、`pre_verifier.mjs`、`contracts.mjs` 从清单去掉（03 §0 ③ 已注）。
3. 两项实跑（ISSUE-410 CLI 重跑到 plan；两 Case 删事实测试）已批准。

## 五、不属批次 3 但由实跑暴露（登记，不处置）

- 效率第 3 项 19/22 次读 checker 源码：被测模型自述七条「写完才被门禁告知」的形态全是 **framework** 判据（P0 AC `flows`、facts `key_inputs_read`、plan 目录树、review 表头…）——作者面通道只覆盖扩展要求，framework 自己的没有 author.md。要压这一项得走 04-framework诉求。
- coding `ai-prompt.md` 210KB、review 175KB（`{script_report}` 双注入，04 #1）。
- 测试域：D4 Case 名经 workspace 路径泄漏给被测模型；D5 到达 end_phase 不转终态；D7 verifier 报告解析把散文行当表格行。
- 工作树里 21 项被测模型回灌的源码改动（两 Case 产出）未提交，入库与否由用户定。

## 六、结论

批次 3 **机制层通过**（A 通道、B 义务与探针、C+R story 守恒在真实产物上成立，且实跑抓出的缺陷都由机制自己报出）；**交付闭环未完成**：F3/F1/F2 修完并出区分力证据后，再以行数与 ③ 的裁定收口。批次 4（去重）在此之后。

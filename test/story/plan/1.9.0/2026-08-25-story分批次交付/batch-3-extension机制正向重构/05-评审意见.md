# 05 评审意见（维护者 · 2026-08-28 · 对 `b7dd4c99`→`53ea22e1`）

> 全部只读实证；行数口径同 03 §0 ①；「相同行占比」= HEAD 非空行中逐字存在于 `e96e90fc` 同名文件的比例。
> **验收结论：未通过。** 成立的部分与未通过的部分分开写；修复只给目标形态、替代对象与结果判据，**不给「在哪个文件加什么判断」**——那是补丁。
> adapt 交付件（367 行）是用户批准的净增，基线 10358 与 HEAD 9589 都含它，对行数判定中性。

## 一、成立的部分（不必重做）

| 项 | 证据 |
|---|---|
| 作者面通道 | `AGENTS.section.md` 逐字进两份入口文件；六份 `author.md` 43–60 行；`gate.mjs` 一处定义替代六处样板，崩栈自报 BLOCKER、通过留痕、一次列全均实跑验证 |
| 义务上实体 + 探针 | `obligations.mjs` 五类宿主封闭；`probes.mjs` 四形态封闭、剥注释、放宽范围出声；对 AR90004 真实源码报出 `LossReportSheet.ets:365`，角色零调用判出 |
| 来源单元枚举 | 对 AR90004 真实材料只读实跑：306 单元，173 条有 token，其中 151 条在旧 story 缺 token，精确点名 `freezeTicketId` / `eligible` / `applicationId` / `AC-R1…R10` / `AC-G1…G5`——KC-2 的区分力成立 |
| 新增执行器不是堆砌 | `probes` 198 / `obligations` 119 / `gate` 108 / `source-units` 233 / `review-render` 128 逐个读过：职责不重、无围栏式判据 |
| 离线 | 台账 44/44、单测 141+7、退场 grep（固定词表）零命中 |

## 二、未通过的部分（按机制，不按文件）

### U1 story 守恒只守了 58%

306 单元里 **130 条（42%）无 token**——纯中文段落、列表项、表格行（PRD 用户旅程 7 条、页面状态触发条件这类**正是 00 诊断点名的丢失形态**）。子串兜底只自动落点 14 条；其余 116 条靠作者手填 `at`，而 `check` **从不读 `at` 的值**（`story-build.mjs:303-330` 只看键在不在）。C 方案 §2.7 自己写的「作者填的须该节含该单元 ≥1 token；标题在册」没有实现。三态之外 FAIL 成立，三态之内没有守恒——旧的自由文本 `reason` 换成了不核的 `at`。

另两个逃生口：haystack 含 `decisions.json`（`:292-293`），任何 token 塞进决策件即算落点；IDENT_RE 把 `WalletMain`/`AccountManager`/`CommUI`/`RR90004`/`Story` 取成 token 要求进 story，与 writer 红线 1「不写模块目录名」相斥，writer 只能违反其一或走决策件逃生口。

### U2 规约逐条判定失去落点（批次 1 知识应用在 story 侧退化）

A 报告 KA-6 已预警「先删会丢掉逐条目判定能力」；C 删 `knowledge.json`、写「附录表改由 writer 直接写」，但 `phases/story-write.md` 与 `story-template.md` 一字未提规约判定，枚举器也不为规约条目生成单元。spec-sections §10 注释第 27 行说「判定结论 → 叙事件的影响面与合规章」——现在既无作业指引也无门禁。

### U3 新旧并存（文本层 45 处，且在作者必读链上）

`spec/author.md:11` 把作者引向 `constraint-usage.md`，而它 `:19/:32` 写「附录由知识判定登记表渲染 / 要求列由装配器渲染」；`evidence-rules.md:130`、`rules.md:44/59/89` 同类；`spec-rules.overlay.yaml:9-11` 让 verifier 按「登记源（归档件的判定登记件）」裁——那个文件已删；`story_reader_fitness` 写「story 由章节合同逐章装配而成」；`verdict-set.mjs:9-10`「附录由登记源渲染（各有机械门禁保证）」是它「裁一次就够」的**前提**，前提已不存在；四份 overlay 卷首「知识账本消费」。⑤ 只按固定词表 grep 过关。

### U4 KA-3 未达成且报告失实

C 报告 §六「overlay story 两条删除，spec 7→5，KA-3 达成」——`spec-rules.overlay.yaml` 仍 7 条（`:65` `:87`）。story 已移到 spec 闭环之后的 S5，spec verifier 却被要求通读尚不存在的 `AR/story.md`（SKIP 只看 story-flow.json）。

### U5 判据③ 未达成且报告失实

C 报告 §四「③ 成立，清单内全部重写」。实测：`spec/post_check.mjs` **94%**（895→795，C-step6 −108/+11）、`pre_verifier.mjs` **92%**、`merge-story.mjs` **95%**、`freeze→contracts.mjs` **98%（纯改名）**、`adjudication→verdict-set.mjs` 47%。退场 grep 词表里正有 `freeze\.mjs` / `adjudication\.mjs`——改名过 grep。真正重写的：story-build 13%、coding post_check 18%、SKILL 19%、story-chapters 25%、plan post_check 40%。

### U6 用户裁定项被移交后遗漏

Q7（rules.md / evidence-rules 重写、constraint-usage 删）：子批 A 以「装配器此刻还在」改写 03 §0 ③ 把三项移出、移交 C；C 只改 rules.md 8 行，evidence-rules **100% 未动**、constraint-usage **仍在**。

### U7 执行者回写判据

改了 03 §0 ③ 清单文本、STATUS、`test/story/TEST.md:309`（把「≤7500」改成「较基线净减」）——一边请用户裁定①，一边把目标改掉。①实际：10358 → A 10387(+29) → B 10655(+268) → C/D 9589；A/B 上升时方案要求停下，执行者写理由继续。超出 7500 的部分全来自批次 3 自身。

### U8 KC-7 未对对照样本跑

C 报告用合成夹具 C01 代替 AR90004 真实副本。

### 提交序列

24 个提交 21 个是正向步进。`5d922b87`（A-step4：改判据、不改产物）与 `3ddf1b9f`（C-step6：两处改名 + spec post_check 删 108 行，为 grep 过关收尾）是按判据文本倒推的补丁；`68415ecb` 5 行遗漏可接受。

## 三、修复

修复方案与判据单独成文，**以它们为准**：[R-修复/00-方案.md](R-修复/00-方案.md)（R1 三态每态有判据面 / R2 规约判定成来源单元 / R3 三份 reference 与 overlay 从 0 重写 / R4 门禁一处、post_check 按职责拆 / R5 行数目标不改）、[R-修复/01-判据.md](R-修复/01-判据.md)（KR-1…KR-6）。顺序 R3 → R1 → R2 → R4 → R5。本文只记评审实证，不再重复方案。

## 四、验证（处置后）

按 R-修复/01-判据.md 逐条核；维护者另用 `node --input-type=module` 对 AR90004 只读跑枚举 + 落点核实复验 KR-1d/1e。

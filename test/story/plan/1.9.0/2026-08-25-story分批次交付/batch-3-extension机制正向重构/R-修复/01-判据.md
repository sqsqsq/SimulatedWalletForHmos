# 批次 3 · R 修复 · 判据（先于实现）

| id | 判据 | 口径 | checker |
|---|---|---|---|
| KR-1a | 机器落点两条路径 | `audit` 对 AR90004 副本：`by: machine` 的每条，其章含 ≥1 token 或 ≥8 字片段；不存在「沿用旧 at」记录 | 单测 + 脚本对照样本 |
| KR-1b | 作者落点形态 | `by: author` 的 at 标题不在合同 / 三态皆空 / 作者写 machine_facing → 各自点名 FAIL；`audit` 重跑后 `by: machine` 记录被重算、`by: author` 记录保留 | 单测 |
| KR-1c | 裁决核实 | verdicts 缺行 / 裁决取值非法 / 引文非该章子串或 <12 字 / 引文是单元原文回声 / 「未讲清」→ FAIL 点名；无 author 记录时不要求文件 | 单测 + 台账 R01（回声反例） |
| KR-1d | 逃生口关闭、排除表不过拟合 | token 塞进 decisions.json 不算落点。对四个 Case（AR90004 / AR90005 / AR90006 / ISSUE-410，材料在 `test/story/cases/*/workspace`）的材料都跑枚举：token 里不含模块名、单号形态、`drop` 形态；排除表在四份材料上都成立；枚举不崩、派生非空。AR90004 另核缺失清单不再含 `WalletMain`/`AccountManager`/`CommUI`/`RR90004`/`Story` | 脚本 × 4 Case |
| KR-1e | 区分力保持 | AR90004（唯一有旧 story 的样本）仍点名 `freezeTicketId`/`eligible`/`applicationId`/`AC-R1…R10`；另三个 Case 报无 token 占比与 token 样例 | 脚本 × 4 Case（输出贴进交付报告） |
| KR-2a | 规约单元 | `init` 对本仓激活清单派生 15 个 `knowledge` 单元，key/域名/编号齐；对 `test/story/fixtures/knowledge` 的夹具清单同样派生成功（条目数随夹具） | 单测 |
| KR-2b | 判定表核实 | 缺条目 / 判定取值非法 / 依据空 → 点名；域行「整域不适用」覆盖该域全部条目 | 单测 + 台账 R02（P15 新形态样本） |
| KR-3a | 旧概念清零 | `grep -rn "装配器\|登记件\|登记源\|知识账本\|附录单表\|冻结块\|逐章装配\|逐章成文\|constraint-usage" doc/extensions`（非知识层）零命中 | grep |
| KR-3b | 作者必读链不指向已删文件 | `hooks/*/author.md`、`phases/*.md`、`SKILL.md` 引用的路径全部存在 | 台账 R03 |
| KR-3c | overlay | spec `semantic_checks` = 5；六份卷首无「知识账本」 | 脚本 |
| KR-4a | 门禁一处 | `merge-story.mjs` 消失；`story.js`/`story_flow.py`/`SKILL.md`/测试脚本零引用；术语守恒与四红线在 `story-build check` 有单测 | grep + 单测 |
| KR-4b | 拆分 | `flow-check.mjs` 存在且 `spec/post_check.mjs` 不再含 `FLOW_` 常量；`--phase extensions` PASS；对 AR90004 副本跑 spec harness 结果与拆前一致 | 脚本 |
| KR-5 | 行数 | 收口重算并与 10358 / 7500 并列写进报告；子批净减 | wc |
| KR-6 | 纪律 | 03/01/05/TEST.md 目标未被执行者改动（`git diff` 只读设计目录不可用——由维护者在收口时比对指纹） | 维护者 |

夹具：`test/story/fixtures/failure-modes/R01-verdict-echo/{bad,good}`、`R02-knowledge-row-missing/{bad,good}`、`R03-author-chain-dangling/{bad,good}`；C01 的 good 样本补 `by` 字段、verdicts 表与合规章判定表。

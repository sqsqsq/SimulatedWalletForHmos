# 子批 R · 评审修复 · 冻结清单（G2 · 一次冻死，不重出）

> 冻结时间：2026-08-28，冻结点 = `cd286385`（R5 收口后）。指纹取 SHA-256 前 16 位。
> 行为证据（CLI 实跑、内网复测）不在本清单内——它冻的是**离线态**。

## 1. 交付件

| 文件 | 行数 | 指纹 | 替代了什么（G11） |
|---|---|---|---|
| `skills/story/phases/story-verify.md` | 63 | `2bb0de01ce2def21` | **新建**。S5 裁决者子 agent 的作业书。替代「机器定不了落点就没人管」——三态里作者填的那一态此前无判据面 |
| `skills/story/scripts/flow-check.mjs` | 253 | `f41a5113e839fce3` | **搬出**。承载原 `spec/post_check.mjs:399-625` 的流程契约判据。它校验 /story 链自己的产物，与 spec 章节结构无关 |
| `skills/story/scripts/story-build.mjs` | 692 | `395b404414f2feb0` | `audit` 三态分流（`by: machine`/`author`）、`check` ⑥ 裁决核实、⑦ 判定表核实、⑧ 术语守恒、⑨ 四红线；token 排除表。⑧⑨ 替代已删的 `merge-story.mjs` |
| `skills/story/scripts/source-units.mjs` | 264 | `93cc70cc15ade07e` | 第九类单元 `knowledge`。替代已删的 `knowledge.json` + 附录渲染在 story 侧承担的逐条回显 |
| `skills/story/contracts/story-chapters.json` | 117 | `4959edeefef5b476` | 新增 `token_exclusions`（仓内单号 / 工程代号 / spec Scope 模块名 / `id_shapes.drop`）。替代「守恒要求出现、红线禁止出现」的自相矛盾 |
| `skills/story/reference/evidence-rules.md` | 106 | `a394c811009aa244` | 194 行的旧版（含知识判定登记件、装配器、附录三处已无对象的说明） |
| `skills/story/rules/rules.md` | 116 | `d87146ea174ad066` | 148 行的旧版；删 `[结构]` 标记（其定义「由装配器保证」已无对象）、删「逐章成文」（与 S5 相反） |
| `rules/spec-rules.overlay.yaml` | 94 | `9b64387b4f57cc63` | 7 → **5** 条。两条 story 判据搬进 `story-write.md` 自检与 `story-verify.md` 裁决维度——S5 没有 framework verifier 可挂 |
| `hooks/spec/post_check.mjs` | 558 | `f3267e842257d31a` | 拆出 `flow-check.mjs`（795 → 558），清三个失效 import |
| `skills/story/SKILL.md` | 187 | `a35ada6620276886` | S5 三步（writer / verifier / 登记）；归档节 ① 改 `story-build check` |
| `skills/story/templates/story-template.md` | 91 | `8a8c787e861f121c` | 第 12 章注释给出规约判定表表头与三种取值 |
| `skills/story/phases/story-write.md` | 93 | `b5251936db438876` | 输入表加激活清单；完备清单加「规约条目一条一行」；末尾加两段交回前自检（承接 overlay 删掉的两条） |
| `skills/story/scripts/lint-rules.mjs` | 271 | `1b2a84f524c6b973` | 仅卷首消费者说明改名（词表 SSOT 不动） |

**已删除**：`skills/story/scripts/merge-story.mjs`（222 行，两件事并入 `story-build check`）、
`skills/story/reference/constraint-usage.md`（62 行，四点去处见方案 §1.1，**已存在的不重复写**）。

## 2. 阈值与封闭取值（本子批新增或改动的）

| 项 | 值 |
|---|---|
| 落点来源 | `by: machine`（每次重算）/ `by: author`（保留，交 S5 裁决），**二值封闭** |
| 落点核对域 | **只有 `AR/story.md`**（`decisions.json` 不再参与拼接） |
| 裁决取值 | `讲清` / `未讲清`，**二值封闭** |
| 引文下限 | **12 字**；须是落点那一章的逐字子串；且**不得**是来源单元原文的子串（回声） |
| 规约判定取值 | `命中` / `不命中` / `整域不适用`，**三值封闭**；域级一行覆盖该域全部条目 |
| token 排除 | 合同 `token_exclusions.patterns` + `id_shapes.drop` + spec Scope 模块名（`in_scope` 与 `out_of_scope` 都取） |
| 交给枚举器的编号形态 | **只有 `id_shapes.keep`** |
| `check` 判据数 | **九项**（①标题 ②落点守恒 ③编号形态 ④图与 diagram ⑤决策六类 ⑥裁决核实 ⑦判定表 ⑧术语守恒 ⑨四红线） |
| spec overlay `semantic_checks` | **5** |
| 台账形态数 | **47**（新增 R01 / R02 / R03） |

## 3. 夹具与台账

| 项 | 值 |
|---|---|
| `fixtures/failure-modes/R01-verdict-echo/{bad,good}` | 树指纹 `98b914676670ccc9` |
| `fixtures/failure-modes/R02-knowledge-row-missing/{bad,good}` | 树指纹 `55da66f4fca8b582` |
| `fixtures/failure-modes/R03-author-chain-dangling/{bad,good}` | 树指纹 `5f17a2b257834bbe` |
| `fixtures/failure-modes/R04-flow-status-after-s5/{bad,good}`（冻结后新增） | 见 §7 |
| `test/story/regression/failure-modes.yaml` | 732 行，`3bd64f2f64978ab6` |
| `test/story/scripts/check_failure_modes.py` | 2265 行，`7434576be28b0f3d` |
| `test/story/tests/test_story_build.py` | 360 行，`b7cda4ab886b9cf3`（**新建**，23 例） |
| `test/story/config/test.yaml` | 57 行，`3d0af95ac6f7b900`（`merge_story` 门禁位取消） |
| `test/story/scripts/run_case.py` | 2043 行，`e072d5247b4232e8`（story 门禁三 → 二） |

## 4. 冻结时的离线基线

台账 **47/47**；单测 **164 passed + 7 subtests**；复述分支 **9/9**；
`node --check` / `compileall` 通过；`--phase extensions` **PASS 6/6**；
退场 grep（§0 ② 全量词表 + 本子批词表）零命中；
`doc/extensions` 非知识层 **9603**（基线 10358，净减 755）。

## 5. §0 收口判据

| # | 结果 |
|---|---|
| ① 总量 ≤7500 | **未达成：9603**。剩余最大三块与理由见 02-交付报告 §四 ① |
| ② 退场 grep 零命中 | **成立** |
| ③ 承载被替换机制的文件重写或消失 | **部分成立：7 / 11**。四项占比 >30%，逐项与不重写的理由见 02-交付报告 §四 ③ |
| ④ 替代说明 | **成立**（本清单 §1 末栏） |
| ⑤ 无新旧并存 | **成立** |

## 6. 已知偏差

| 项 | 说明 |
|---|---|
| ① ≤7500 | 差 2103 行。要成立得先裁判据条数或换 `story_flow.py` 实现，两件都超本子批范围 |
| ③ 判法 | 「相同行 <30%」对「大幅删减但保留正确判据」的文件不成立——正确的重写在保留正确部分时必然与旧文件相像。如实报占比，不制造改动去迎合判法 |
| 四 Case 的 token 排除 | 那四份 workspace 没有 `spec/spec.md`，模块名仍成为 token。`init` 在真实链路上只在 S5 跑（spec 已闭环），AR90004 那一行才是真实口径 |
| 无 token 单元 25%–63% | 靠语义判据守恒（三态 + S5 裁决），机器管不到。`check` 通过时显式打印机器核实与 S5 裁决各多少条 |
| `EVOLUTION.md` | 测试域维护件，§1/§2 相对当前实现已大面积过时；本子批只改了直接指向已删文件的一句 |

## 7. 冻结后修订（CLI 实跑抓出，2026-08-28）

G2 说「一次冻死，不重出」——**修缺陷不是重出**：判据没变、形态没变，变的是一处写错的取值域。
如实登记新指纹，不改上面的冻结记录。

| 文件 | 冻结时 | 修订后 | 提交 | 原因 |
|---|---|---|---|---|
| `skills/story/scripts/flow-check.mjs` | 253 行 `f41a5113e839fce3` | **263 行 `61a96f7287b4ff97`** | `b589302e` | 「已收口」由单值改为区间 `{complete, story_written, archived}` |
| `test/story/regression/failure-modes.yaml` | 732 行 `3bd64f2f64978ab6` | **752 行 `6fc7a54ba99d32e2`** | `b589302e` | 新增形态 R04 |
| `test/story/scripts/check_failure_modes.py` | 2265 行 `7434576be28b0f3d` | **2296 行 `72d0f61025979254`** | `b589302e` | 新增 checker `r04_flow_status_after_s5` |
| `test/story/scripts/measure_run.py` | 未冻结（子批 D） | **193 行 `374e20607be851d9`** | `bfedb22e` | 只量到一轮，七项指标失真 |

台账形态数：**47 → 48**。离线基线随之更新：台账 48/48、单测 164 passed + 7 subtests、
`--phase extensions` PASS 6/6。

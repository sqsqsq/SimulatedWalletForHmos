# 子批 B · 义务上实体 + 探针 · 冻结清单（G2 · 一次冻死，不重出）

> 冻结时间：2026-08-28，冻结点 = Step 7 收口后。此后行为验证不改本清单。指纹取 SHA-256 前 16 位。

## 1. 交付件

| 文件 | 行数 | 指纹 | 替代了什么（G11） |
|---|---|---|---|
| `hooks/shared/obligations.mjs` | 119 | `6666184647ded145` | `contracts.knowledge_freeze` 账本 + `downstream.loadLedger`——义务改从契约实体运行期派生 |
| `hooks/shared/probes.mjs` | 198 | `11c62dc18bf0aae7` | `coding/post_check` 里那一行 `\b名\b` 存在性探针 |
| `hooks/shared/verifier-report.mjs` | 166 | `74e45543c3d08a3e` | `keysAdjudicated` 的「同行含键 + 有裁决词」判据 |
| `hooks/shared/freeze.mjs` | 172 | `6a389a1744903f58` | 删 `readFreeze` / `FREEZE_KEY` / `landingRefs`；余下改称契约访问器 |
| `hooks/shared/adjudication.mjs` | 201 | `210a6154b1660a0b` | 必答集改从实体派生（不再读 freeze 块） |
| `hooks/shared/knowledge.mjs` | 441 | `36c3fe70f0020d50` | 加探针派生与 `\|` 转义感知的表格切分 |
| `hooks/plan/post_check.mjs` | 219 | `4dd9202fdbd21354` | 25+ 条账本形态判据 → 两条集合判据 + must 挂位/写法 |
| `hooks/coding/post_check.mjs` | 152 | `0dad59717cec47f9` | 存在性探针 → 实体存在 + 探针执行 + 角色引用可达 |
| `hooks/review/post_check.mjs` | 124 | `3a7cfbced079f69d` | 账本消费 → 派生索引 |
| `hooks/ut/post_check.mjs` | 101 | `e63ff6eb110ce313` | `criterion` → `ut_layer` 二次查表 → `must.verify` 单源 |
| `hooks/testing/post_check.mjs` | 104 | `c96d26321375f8dd` | 同上 |
| `skills/story/templates/plan-sections.md` | 128 | `0774b64ebbb1deae` | 冻结块写法 → 实体 `must` 写法 |
| **已删除** | — | — | `hooks/shared/downstream.mjs`（78 行，整个文件） |

六份 `author.md` 中 `plan` / `coding` / `review` / `ut` / `testing` 五份随各自 post_check 同步重写。

## 2. 知识层（只加列，不动路径与内容）

`knowledge/constraints/*.md` ×8 加「探针」列，`knowledge/design-patterns/*.md` ×2 加 frontmatter `sections`。
知识目录整体指纹：`3718c5f2d27b8a5f`。

**15 个条目里只有 2 条有探针**：

| 条目 | 探针 |
|---|---|
| UX-01 | `absent_regex:\b(left\|right)\s*:` |
| OBS-01 | `present_in_method:Logger\.` |

其余 13 条写「无」——「核对字段 diff」「判断四问结论」这类是语义判断，硬造正则只会制造误报。

## 3. 阈值与封闭取值

| 项 | 值 |
|---|---|
| `must` 允许挂载处 | `data_models[].fields[]` / `interfaces[].methods[]` / `components[]` 及 `state[]` / `resource_keys[]` / `files[]`，**五处** |
| `verify` 取值 | `ut` / `device` / `both` / `review` / `probe`，**五个**（`both` 不可省，见 KB-9） |
| 探针形态 | `absent_regex` / `present_in_method` / `referenced_outside_definition` / `count_eq`，**四种**，未知形态 BLOCKER |
| 探针列缺省 | 按「无探针」派生，**不 throw**（E9） |
| 引文最短长度 | **12 字**，且须是目标产物的子串 |
| 一行裁决对象数 | **1**（一行列多个对象 → 全部未裁） |
| 台账形态数 | **43**（新增 B01、B02） |

## 4. 夹具与台账

| 夹具 | 用途 |
|---|---|
| `fixtures/failure-modes/B01-probe-no-discrimination/{bad,good}` | 探针对已知违规的判别力；含「注释里提及不算引用」一面 |
| `fixtures/failure-modes/B02-evidence-echo/{bad,good}` | 回声证据能否被判「未裁」 |

夹具树指纹：`83691c188c4297af`

| 文件 | 行数 | 指纹 |
|---|---|---|
| `test/story/regression/failure-modes.yaml` | 663 | `2f6f65a25a69cfa6` |
| `test/story/scripts/check_failure_modes.py` | 2080 | `c09d4c5c432dc257` |

## 5. 冻结时的离线基线

台账 **43/43**；单测 **140 passed + 7 subtests**；复述分支 9/9；`node --check` / `compileall` 通过；
`--phase extensions` PASS 6/6；`doc/extensions` 非知识层 **10655**（基线 10358，+268）。

## 6. 已知偏差

| 项 | 方案 | 实际 | 说明 |
|---|---|---|---|
| 行数 | 净减 | **+268** | 执行器（`probes.mjs` 198 + `obligations.mjs` 119）是新增能力——基线只有一行 `\b名\b`。删的是 `downstream.mjs` 78 + freeze 账本约 50 |
| `adjudication.mjs` | 删除 | 保留 | 它的必答集派生仍有四个消费者；被替代的是判据逻辑不是模块 |
| 探针覆盖 | 全部条目补齐 | 15 条中 2 条 | 不臆造：写不出机器判据的条目写「无」 |

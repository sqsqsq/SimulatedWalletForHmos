# 子批 B · 义务上实体 + 探针 · 验收判据（先于实现）

> G3：判据在实现前独立重推，每条带出处、口径、责任层；机械判据不得表述为语义结论。
> G5：任一机械 BLOCKER 或语义 FAIL → 本子批失败。

## 1. 判据表

| id | 判什么 | 口径（计什么 / 不计什么） | 层 | checker | 出处 |
|---|---|---|---|---|---|
| **KB-1** | 义务集合一致 | spec §10 的命中条目集 = contracts 里 `must.rule` 的集合，**双向差集为空**。**不计**处置标「（评审动作）」的条目（它们不产生代码要求，不进 contracts）。派生为空（§10 有命中而零 `must`）→ BLOCKER 指路 plan，不静默通过 | 机械 | plan post_check | E2；冻结 03-C5/C6 |
| **KB-2** | `must` 挂在实体上且含专名 | `must` 只出现在五类实体（`data_models[].fields[]` / `interfaces[].methods[]` / `components[]` 及 `state` / `resource_keys[]` / `files[]`）；出现在别处即 BLOCKER。`text` 过 `paraphrase.mjs` 判非纯复制。`verify` 取值封闭：`ut`/`device`/`both`/`review`/`probe` | 机械 | plan post_check | 01 §4.1；03-C6 |
| **KB-3** | 探针语法封闭 | 规约表「探针」列只接受四形态（`absent_regex:` / `present_in_method:` / `referenced_outside_definition` / `count_eq::`）或「无」；未知形态 BLOCKER。**扫描 0 命中告警**（形态可能不匹配本工程），不静默 | 机械 | 单测 | 01 §4.5 |
| **KB-4** | **区分力**（本子批的核心判据） | 对 AR90004 的**真实源码**：① `absent_regex` 形态的探针必须命中 `LossReportSheet.ets:365` 的 `padding({ left, right })`；② `referenced_outside_definition` 必须判出五个角色类零调用。**不计**「对 contracts 写法的判别力」——验的是探针对源码的判别力 | 机械 | 脚本 + 台账 | E3；00 §4.2 |
| **KB-5** | 引文核实 | 构造回声报告（证据 = contracts 字段复制）→ 全部判「未裁」；构造真引文（产物原文子串 ≥12 字）→ PASS。引文为空 / 一行多对象 → 未裁 | 机械 | 单测 | E4；01 §5.3 |
| **KB-6** | 机制层零规则编号 | `hooks/**/*.mjs` 无 `[A-Z]{2,6}-\d{2}` 字面、无域前缀、无正则字面来自规约 | 机械 | 台账 M01 | G7 |
| **KB-7** | `must.text` 是本需求设计 | verifier `obligation_substance` 逐条附引文裁决 | 语义 | verifier | 03-C6 |
| **KB-8** | 内网复测 | plan→coding：探针 PASS 且 verifier 引文核实通过 | 行为 | 用户 | G10 |

## 2. 本子批独立重推的补充判据

| id | 判什么 | 口径 | 层 | 为什么必须有 |
|---|---|---|---|---|
| **KB-9** | **`ut_layer` 退场不丢「显式不适用」** | `must.verify` 必须支持 `both`——旧 `ut_layer` 有 `unit`/`device`/`both` 三态，只留 `ut`/`device` 会让原来标 `both` 的条目在其中一边掉线 | 机械 | 单测 | 能力守恒：删账本不等于删能力（G11「不留静默降级」） |
| **KB-10** | **删导出前 grep 含自身文件** | `freeze.mjs` / `downstream.mjs` / `adjudication.mjs` 的每个导出，删前 grep 全域**含定义它的那个文件**；删后实跑一次六个 post_check | 机械 | 实跑 | 坑 #59：子批 A 删 `downstream.blocker()` 时漏了模块自己在用，`node --check` 查不出 |
| **KB-11** | **探针 0 命中出声** | 探针在目标工程扫描 0 命中时告警「形态可能不匹配本工程」，不当作通过 | 机械 | 单测 | G7；否则「探针写错了」与「代码没问题」同形——这正是基线 `\b名\b` 恒真的翻版 |
| **KB-12** | **知识层只加列，不动路径与内容** | 规约文件只新增「探针」列；条目编号、约束列、处置列、文件路径、`knowledge/README.md` 索引**逐字不变** | 机械 | diff 核对 | E10；B3-8「知识只动结构」；adapt 的知识守恒判据靠「旧事实序列仍按序在」 |

## 3. 不在本子批判的

| 项 | 归属 |
|---|---|
| 退场 grep 全量、无新旧并存全量、判据③ 全量 | 子批 C 收口首次成立 |
| `story-build` / `story-chapters.json` / `merge-story` / `knowledge.json` 退场 / overlay ≤5 条 / `SKILL.md` 重写 | 子批 C（含子批 A 移交的四项） |
| 效率七项 | 子批 D |
| 探针是否**真的**让编码者做对了 | 行为层，KB-8 内网复测 |

## 4. 总判定（G5）

本子批 PASS ⟺ KB-1…KB-7、KB-9…KB-12 全部满足 **且** 固定验证全绿 **且** §0 ①（行数净减）+ ③（限本子批已处置文件）+ 本子批删除项退场 grep 零命中。

KB-4 是**本子批不可让步的一条**：探针对 AR90004 已知违规没有区分力，等于把基线的恒真探针换了个写法——那是打补丁，不是正向实现。

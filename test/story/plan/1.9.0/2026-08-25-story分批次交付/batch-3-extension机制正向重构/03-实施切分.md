# 03 实施切分

> 状态：**待用户批准**。每批独立会话执行；每批「判据先行 → 实现 → 离线全绿 → 台账全量 → 冻结一次不重出 → 用户指定验收」（交付协议同 `../00-总方案.md` §4，G1–G10 不变）。
> 本文只切分与定判据雏形；每批开工时在本目录新建 `batch-N-*/00-方案.md`（自足、从 0 立契约）与 `01-判据.md`。

> **2026-08-28 重排后**：本文的「批次 2」指 story 去重第三版（现批次 4）。本批（批次 3）在 **adapt（现批次 2）方案批准并完成后**启动。子批 C（story 机制：删 1.0 逐章装配器、writer 子 agent、整篇 token 守恒）**属本批**（E5/E7）；现批次 4 只做 B2-* 的去重/review 验收与补项。

## 0. 不打补丁的判据（用户 2026-08-28 裁定：只约束结果，不约束实现形态）

本批是**重写**，不是在现有文件上加判据。下面五条是收口判据，全部落在结果上；**新文件叫什么、拆几个、多长，由执行者按 01 设计自定**，本文不规定。

| # | 判据 | 口径 |
|---|---|---|
| ① 总量下降 | `doc/extensions` 非知识层行数：HEAD 基线 **10358**（`find . -type f -not -path "./knowledge/*" -not -path "./adapt/*" -not -name "*.png" -not -name "*.svg" \| xargs wc -l`；不含 adapt 工作目录）→ 收口 **≤ 7500**（**2026-08-29 用户裁定：批次 3 按实数 9613 收口，≤7500 保留为长期项**）；子批 A / B / D 各自**净减**，任一子批上升即停下重看这一步是不是又在结构外加围栏 | 机械 |
| ② 旧机制退场 | `grep -rn "scaffold\|assemble(\|findMissingFacts\|knowledge_freeze\|freeze\.mjs\|downstream\.mjs\|adjudication\.mjs\|on_context_load\|knowledge\.json\|knowledge-review\|renderKnowledgeAppendix\|must_answer\|ids\.json" doc/extensions`（`knowledge/` 除外）零命中 | 机械 |
| ③ 不得增量编辑 | 以下文件承载被替换的机制，只能**按 01 从 0 重写或删除**，不得 diff 编辑：`skills/story/scripts/story-build.mjs`、`skills/story/scripts/merge-story.mjs`、`hooks/spec/post_check.mjs`、`hooks/plan/post_check.mjs`、`hooks/coding/post_check.mjs`、`hooks/shared/{adjudication,downstream}.mjs`（**2026-08-29 裁定**：`pre_verifier.mjs`、`freeze→contracts.mjs`、`hooks/spec/post_check.mjs` 从本清单去掉——前者注入已达标、中者是访问器非被替换机制、后者是删一半留正确判据；三件按替代关系表算「接受」）、`skills/story/contracts/story-chapters.json`、六份 `hooks/<phase>/on_context_load.md`、`hooks/spec/story_on_context_load.{md,mjs}`。判法：这些路径要么消失，要么其新内容与 HEAD 版本的相同行占比 < 30%（`git diff --stat e96e90fc -- <path>` 与 `wc -l` 对照）。**本清单只收「01 §9 有明确替代物」的文件**——`SKILL.md` / `rules/rules.md` / `reference/evidence-rules.md` 三项经实施会话复核**不满足这个前提**（把「文件大、文本旧」当成了「承载被替换的机制」），已移交子批 C：`SKILL.md` 在 S5 挂点重组链条章时重写，另两份随删装配器同步措辞 | 机械 |
| ④ 替代说明 | 每个新增文件在交付报告里一句「替代了什么」；零替代对象的新增须写明为什么它不是围栏（G11） | 人核 |
| ⑤ 无新旧并存 | 旧形态指令文本（装配器 / 附录 / 账本 / 逐单元裁决表）全仓清零；`--phase extensions` PASS；台账全量 PASS | 机械 |

**核的时机（2026-08-28 裁定）**：②③⑤ 都是**全批口径**——旧机制分散在 A/B/C 三个子批里处置，A 做完时 B/C 该删该重写的还在，此时核必然 FAIL。所以各子批收口核的是：**① 行数净减 + ③ 限本子批已处置的文件 + 本子批删除项的退场 grep**（同 §4 第 3 条）。②⑤ 与 ③ 的全量口径在 **C 收口后首次成立**，D 收口再核一次。子批间的处置分工见 §3 各节「做」；同一文件只由一个子批重写，其余子批不得对它做增量编辑（新增留痕与 try/catch 不算——那是新增通用能力，不是在旧机制上打补丁）。①的基线**含 adapt 交付件**（`skills/story-adaptation/` 371 行计入 10358），只排除 `adapt/` 运行时工作目录；全文各处判行数一律用这一个基线，不再引 F11。

**参考估算（非判据，只说明 ≤ 7500 是自然结果）**：删除 `on_context_load.md` ×6 + `story_on_context_load.*` + `adjudication/freeze/downstream` ≈ −1060；被替换机制的承载文件（story-build 1337、spec post_check 895、plan post_check 416、pre_verifier 251、knowledge.mjs 377、merge-story 222、SKILL 370、story-chapters 399、rules/evidence-rules/constraint-usage 404、六份 overlay 254 等 ≈ 5380 行）按 01 的职责重写后估 ≈ 2700；新增（六份主 agent 须知 ≈360、`AGENTS.section.md` ≈15、两份作业包 ≈140、两份模板 ≈100、来源单元枚举 ≈200——它是新建，替代基线 story-build 里的取材与逐章守恒函数）估 ≈ 800；原样不动（story_flow.py、import_sources.py、三个 js、lint/yaml-lite/paths/evidence/paraphrase、五份 rules、adapt 三件、manifest）≈ 3700。合计 ≈ 7000。执行者按自己的划分做，只要 ①②③⑤ 成立即可。

## 1. 代码基线与保留项

- **2026-08-28 用户已 `reset --hard e96e90fc`**：`story` 分支 HEAD = `e96e90fc`，工作区与之零差异（已核对）。批次 3 直接在此基线上开工。
- 旧批次 2 的 14 个提交仍可达（`36c55c62 … 67cdc068`），不合入；**Step 0 只取三处测试域 hunk**（三个提交都混有 `doc/extensions` 与旧台账改动，不能整提交 cherry-pick）：
  - `36c55c62`：`test/story/scripts/run_multi_case.py` 的 `expected_phase` / `requires_archived` 关卡前提 + `test/story/tests/test_multi_case_cli.py::GatePreconditionTest` + `test/story/TEST.md` §3 四类 reason 表——命令 `git diff 36c55c62^ 36c55c62 -- test/story/scripts/run_multi_case.py test/story/tests/test_multi_case_cli.py test/story/TEST.md` 后按 hunk 应用；
  - `20686a30`：四个 `test/story/cases/*/case.yaml` 去掉阶段链 + `test/story/tests/test_multi_scenario_cases.py::test_prompts_do_not_drive_phase_advancement` + `TEST.md` §2——**不取** `source-units.mjs`；
  - `a7615791`：只取 `run_multi_case.py` 中 `snapshot_workspace_sources` 排除 `oh_modules/build` 与超长路径处理——**不取** `story-build.mjs`、P27 夹具、`failure-modes.yaml`、`check_failure_modes.py`。
  应用后 `python -m pytest test/story/tests -q` 全绿、`check_failure_modes.py` 与 F17 基线一致，单独提交 `ext.b3-step0`。
- 对照样本位置（doc/features 与 output 均被 .gitignore，回退未删）：`doc/features/AR90004/**`（含 `AR/story-src/*.json`、三阶段 reports）；源码 `output/story/story-suite-b2v3d/cases/pattern-image-review/source-diff/files/02-Feature/WalletMain/src/main/ets/presentation/components/LossReportSheet.ets`（`:365` `padding({ left, right })`）与 `LossFlowOperates.ets`/`LossReportFlowLauncher.ets`（零调用角色）。
- `doc/features/AR90004`（批次 2 实跑回灌件）**保留为对照样本**，不入回归目标集；本轮新判据对它必须有区分力（见各批判据）。

## 2. 事实核对清单（每批开工前执行；命令在仓库根、基线分支上）

| # | 事实 | 命令 | 预期 |
|---|---|---|---|
| F1 | hook 事件全在 harness 进程内、verifier 前 | `sed -n '765,768p' framework/harness/harness-runner.ts` | `pre_verifier / on_context_load / post_verifier / post_phase` 四行相邻 |
| F2 | fragment 唯一去处是 ai-prompt 尾部 | `sed -n '311,318p' framework/harness/scripts/utils/report-generator.ts` | 追加 `lifecycleHookFragments` |
| F3 | `{script_report}` 全局替换 + coding 模板两次占位 | `grep -n "script_report" framework/harness/scripts/utils/report-generator.ts framework/harness/prompts/verify-coding.md` | `report-generator.ts:257` 用 `/g`；`verify-coding.md` 两处 |
| F4 | asset 协议不认扩展覆盖；解析器认 | `sed -n '40,66p' framework/skills/README.md; sed -n '166,171p' framework/harness/scripts/utils/profile-skill-assets.ts` | README 只读 profile 清单；解析器先查 `extensionSkillAssetAbsPaths` |
| F5 | coding SKILL 输入不含账本 | `grep -n "knowledge_freeze\|contracts.yaml" framework/skills/feature/coding/SKILL.md` | 无 `knowledge_freeze`；`:38-53` 列 7 集合 |
| F6 | hook 失败默认 MAJOR、崩栈降级、成功不留痕 | `sed -n '48,50p;177,200p' framework/harness/hooks-dispatcher.ts` | 如 00 §1.2 |
| F7 | overlay 解析失败静默 | `sed -n '176,193p' framework/harness/profile-loader.ts` | 两处 `return current`（`:184`、`:191`），解析失败不报错 |
| F8 | manifest 路径错静默清空 | `sed -n '55,73p' framework/harness/extension-loader.ts` | `finalize()` 清空 provides |
| F9 | `knowledgePaths` 零消费方 | `grep -rn "knowledgePaths" framework/harness --include=*.ts` | 仅 `extension-loader.ts:31/61/168`、`types.ts:726` |
| F10 | contracts 实体级未知键是否被剔除 | `grep -n "normalizeArrayField\|delete \|allowed\|unknown" framework/harness/scripts/utils/spec-loader.ts | head -30`；再用含 `must` 的最小 contracts 跑 `harness-runner.ts --phase plan` 看 `feature_spec_shape` | 决定 01 §4.1 的落点（contracts 或 plan.md） |
| F11 | 基线态 `doc/extensions` 行数 | 同 §0 ① 的命令，在 HEAD 上跑（含 adapt 交付件，只排除 `adapt/` 工作目录） | **10358**（= §0 ① 的唯一基线） |
| F12 | 基线态 hooks 文件清单 | `git ls-tree -r --name-only e96e90fc -- doc/extensions/hooks` | 含 `on_context_load.md` ×6、`story_on_context_load.md/.mjs`、`shared/{adjudication,freeze,downstream,evidence,knowledge,paths,paraphrase,pre_verifier,verifier-report,yaml-lite}.mjs` |
| F13 | 模式文档已按读者分篇 | `grep -n "^# \|^## 8" doc/extensions/knowledge/design-patterns/decision-tree.md` | `:19` 上篇（plan）、`:63` 下篇（coding、review）、`:162` 验证清单 |
| F14 | 规约条目表列 | `sed -n '12,14p' doc/extensions/knowledge/constraints/ux-consistency.md` | 六列；UX-01 验证列含「检索 `left:`/`right:`」 |
| F15 | story_flow 现有 mode | `grep -n "choices=\[" doc/extensions/skills/story/scripts/story_flow.py` | `init, round, decide, status, complete, archived` |
| F16 | 对照样本可用 | `ls doc/features/AR90004/AR/story-src doc/features/AR90004/coding/reports; grep -n "padding({ left" 02-Feature/WalletMain/src/main/ets/presentation/components/LossReportSheet.ets` | `audit.json` 在；`coding/reports` 无 `ext-post-check.json`；`:365` 命中 |
| F17 | 测试域基线 | `python -m pytest test/story/tests -q \| tail -1; python test/story/scripts/check_failure_modes.py \| tail -1` | 基线分支的数字（记入 batch STATUS） |

核对不符先回报用户再动手（`test/story/AGENTS.md` §3）。

## 3. 批次

### 批次 A · 通道归位（01 §3）

**做**：`hooks/<phase>/author.md` ×6；`AGENTS.section.md`（≤15 行，正文三句见 01 §3.2）并把其内容写入本仓 CLAUDE.md 与 AGENTS.md 的「实例扩展」节（替换该节现有一句；两份逐字相同）；`skills/story/phases/{spec,story-write}.md`；`skills/story/SKILL.md` 只加一句「进入 spec 前先读 `phases/spec.md`」（**不从 0 重写**：01 §9 给它的替代物是「写作/命令段 → phases/」，而基线上写作段在已删的 `story_on_context_load.md` 里、命令段是 `/story` 自己的——`phases/` 替代不了。重写移交子批 C 的 S5 挂点）；模板增补件 `plan-sections.md`；`rules/rules.md`、`reference/{evidence-rules,constraint-usage}.md` **不重写**（实施会话实证：它们的「装配器」措辞在 `e96e90fc` 上是准确的——装配器还在；02 §2.5 那条基于 `67cdc068`。三份改为**挂上作者通道**，措辞随子批 C 删装配器时同步，判据③ 里 `evidence-rules` 移交 C、`constraint-usage` 移除）；删 `on_context_load.md` ×6 与 `story_on_context_load.*`；`pre_verifier.mjs` 重写（只注入清单，≤5KB）；overlay 每阶段 ≤5 条；六阶段 post_check 留痕 + 顶层 try/catch；post_check 报错一次报全、首行指向 author.md；`spec/post_check.mjs` 重写（从 spec §10 解析判定，删 `knowledge.json`；story 判据由子批 C 移出）。
**不做**：contracts `must`（批次 B）、story writer（批次 C）。
**判据雏形**：

| id | 判据 | 口径 | 层 | checker |
|---|---|---|---|---|
| KA-1 | 作者向文本不在 hooks 里 | `hooks/**/*.md` 只剩 `author.md`；其内容无「必须写 / 三问 / 示例长文」以外的判据推理（行数 ≤60） | 机械 | 台账新形态 |
| KA-2 | verifier 注入体积 | 对 AR90004 副本跑各阶段 harness，`ai-prompt.md` `Lifecycle hooks` 段 ≤ 15KB | 机械 | 脚本 |
| KA-3 | overlay 判据数 | 每阶段 `semantic_checks` ≤5，每条含对象/引文要求/FAIL 条件 | 机械 | 脚本 |
| KA-4 | 六阶段留痕 | 六个 post_check 均 import `evidence.mjs` 且 PASS 也写文件 | 机械 | 台账 |
| KA-5 | 报错一次报全 | 构造缺三项的 spec 副本，首跑 `message` 列出三项 | 机械 | 单测 |
| KA-6 | `knowledge.json` 退场 | 全仓零引用；spec post_check 从 §10 表解析三方集合 | 机械 | grep + 单测 |
| KA-7 | 行数下降 | `doc/extensions` 非知识层 < **10358**（§0 ① 基线，含 adapt 交付件） | 机械 | wc |
| KA-8 | 入口文件含扩展段 | 本仓 CLAUDE.md 与 AGENTS.md 的「实例扩展」节均含 `AGENTS.section.md` 全文且逐字相同；含「先读 `hooks/<phase>/author.md`」一句 | 机械 | 脚本（adapt `--check ⑤` 同口径） |

本子批收口时复核 §0 **①③ + 本子批删除项的退场 grep**（②⑤ 的全量口径见 §0「核的时机」）。

### 批次 B · 义务上实体 + 探针（01 §4–§5）

**做**：contracts 实体 `must` / `files[].pattern`（按 F10 定落点）；模式文档 frontmatter `sections`；规约表「探针」列（全部条目补齐或写「无」）；`knowledge.mjs` 派生索引；`coding/post_check.mjs` 重写（实体存在 + 探针 + 角色引用可达 + 结构探针 + 留痕）；review/ut/testing 改消费派生索引；`plan/post_check.mjs` 改两条集合判据；`verifier-report.mjs` `evidenceVerified`；删 `knowledge_freeze`、`freeze.mjs` 账本读取、`downstream.mjs`。
**判据雏形**：

| id | 判据 | 口径 | 层 | checker |
|---|---|---|---|---|
| KB-1 | 义务集合一致 | spec §10 命中集 = contracts `must.rule` 集（双向差集空） | 机械 | plan post_check |
| KB-2 | `must` 挂在实体上且含专名 | 允许的五类实体；`text` 过 `paraphrase.mjs` 非纯复制 | 机械 | plan post_check |
| KB-3 | 探针语法封闭 | 四种形态；未知形态 BLOCKER；0 命中告警 | 机械 | 单测 |
| KB-4 | **区分力** | 对 AR90004 副本：五角色 `referenced_outside_definition` FAIL；`absent_regex:\b(left\|right)\s*:` 命中 `LossReportSheet.ets:365` | 机械 | 脚本 |
| KB-5 | 引文核实 | 构造回声报告（证据 = 字段复制）→ 全部「未裁」；构造真引文 → PASS | 机械 | 单测 |
| KB-6 | 机制层零规则编号 | `hooks/**/*.mjs` 无 `[A-Z]{2,6}-\d{2}` 字面（台账 M01 扩展） | 机械 | 台账 |
| KB-7 | `must.text` 是本需求设计 | verifier `obligation_substance` 逐条附引文 | 语义 | verifier |
| KB-8 | 内网复测 | plan→coding：探针 PASS 且 verifier 引文核实通过 | 行为 | 用户 |

本子批收口时复核 §0 **①③ + 本子批删除项的退场 grep**。

### 批次 C · story 机制：删 1.0 逐章装配器 + 独立成文 + 事实级守恒（01 §6；E5/E7）——**属批次 3**

> 基线 `e96e90fc` 上 `skills/story/scripts/story-build.mjs`（1337 行）就是 1.0 逐章装配器（`scaffold`/`assemble`/`findMissingFacts`/`findMissingIdentifiers`/`renderIdRefs`/`renderStoryAppendix`），`story-chapters.json`（399 行）是它的取材合同。本子批把它们**整体替换**为 01 §6 的形态；批次 4 只做 B2-* 的去重/review 验收与补项（KC-5 完整率人核归批次 4）。

**做**：`story_flow.py` 加 `story` 态与前置（`archived` 前置改为成文态）；`/story` S5 + `phases/story-write.md` + `templates/story-template.md`；**新建**来源单元枚举（模块名由执行者定：单元切分、全列 token、机器面按合同数据打标、diagram 单元、跨文档去重）；`story-build.mjs` **从 0 重写**（`init` 登记来源单元与 decisions 骨架 / `audit` 三态核对 / `check` 14 标题、整篇 token 守恒、编号形态、图单元、review 语法、`scanned_categories` / `build` 渲染 review.md）；`story-chapters.json` **从 0 重写**（id/title/question + 机器面 + 编号形态）；`merge-story.mjs` 重写（归档件四红线 + 术语首列；删编号与契约首列守恒）；`skills/story/SKILL.md` **从 0 重写**（链条 + 关卡语义 + 产物定位 + S5 挂点）；`rules/rules.md`、`reference/{evidence-rules,constraint-usage}.md` 的「装配器 / 附录」措辞随之同步（三项均自子批 A 移交，见其方案偏差二、三）；spec post_check 不再含 story 判据；overlay story 语义两条（`story_reader_fitness`、`story_decision_substance`，引文核实）；测量脚本输出三态分布。
**判据雏形**：

| id | 判据 | 口径 | 层 | checker |
|---|---|---|---|---|
| KC-1 | 三态之外 FAIL | `audit.json` 无自由文本；`covered_by` 目标有效 | 机械 | check + 台账 |
| KC-2 | 整篇 token 守恒 | 非机器面单元每个 token 有落点；对 AR90004 副本必点名 `freezeTicketId`、`eligible`、`AC-R1…R10`、`loss-report-recovery.svg` 之外的缺失 | 机械 | check |
| KC-3 | 编号形态 | `drop` 出现 FAIL；`keep` 在验收章 | 机械 | check |
| KC-4 | writer 独立 | events 里 story 写入发生在 Task 子 agent；主会话 spec 阶段上下文增量 ≤150K | 行为 | measure_run |
| KC-5 | 完整率（**归批次 4**） | 对新实跑产物按本轮 221 单元人工对照口径复核：完整 ≥ 90%，缺失 = 0（机器面除外） | 语义（人工） | 用户/评审 |
| KC-7 | 区分力 | 新 `check` 对 AR90004 副本（`doc/features/AR90004/AR/story.md` + 材料）必点名：`freezeTicketId`、接口字段名（如 `eligible`/`applicationId`）、AC-R 编号、SE 时序图与 spec 流程图两个 diagram 单元的落点缺失；旧 `audit.json` 的自由文本理由在三态下无法表达 | 机械 | 脚本对照样本 |
| KC-6 | prompt 无逐单元表 | spec/verifier 注入 ≤15KB | 机械 | KA-2 |

本子批收口时复核 §0 **①③ + ②⑤ 的全量口径**——A/B/C 三批该删的到此全部删完，②⑤ 在此首次成立。

### 批次 D · 效率度量与实跑

**做**：`test/story/scripts/measure_run.py`（01 §7 口径；**本批交付**，01 §7 原写「批次 4」是旧编号残留）；把七项目标写进 TEST.md 验收；用户指定 Case 实跑到 coding；内网复测。
**判据**：01 §7 七项；任一不达标 → 回批次 A–C 定位（不加新围栏）。本子批收口时复核 §0 **全部五条**（②⑤ 第二次全量核），并把最终行数与基线 10358 的对比写进交付报告。

### 批次 R · 修复（2026-08-28 评审后新增）

评审结论与实证见 [05-评审意见.md](05-评审意见.md)；自足方案与判据见 [R-修复/00-方案.md](R-修复/00-方案.md)、[R-修复/01-判据.md](R-修复/01-判据.md)。顺序 R3 → R1 → R2 → R4 → R5，前缀 `ext.b3R-step<N>`。**执行者不回写本文 / 01 / 05 的判据与 TEST.md 目标；偏差只进 R-修复/02-交付报告「差异」栏。**

## 4. 每批固定动作

1. 开工：F 系列核对 → 写本批 `00-方案.md`（自足）+ `01-判据.md`（先于实现）；
2. 每步单独提交，前缀 `ext.<批>-step<N>`；
3. 每步末：`node --check` 全部 `.mjs`、`python -m pytest test/story/tests -q`、`check_failure_modes.py`、`check_paraphrase_branches.mjs`、`--phase extensions`、`--clear-state`；退场 grep 零命中（本批删除项）；行数复核；
4. 收口：冻结清单**一次**；报告分栏（实施事实 / 离线 / 回归 / 行为）；新坑入 `../03-踩坑记录.md`，新验证机制入 `../02-已验证机制参考.md`。

# 子批 C · story 机制 · 验收判据（先于实现）

> G3：实现前独立重推，带出处、口径、责任层。G5：任一机械 BLOCKER 或语义 FAIL → 本子批失败。

## 1. 判据表

| id | 判什么 | 口径（计什么 / 不计什么） | 层 | checker | 出处 |
|---|---|---|---|---|---|
| **KC-1** | 三态之外 FAIL | `audit.json` 每条记录只能是 `at` / `covered_by` / `machine_facing` 三态之一；出现自由文本理由即 FAIL。`covered_by` 的目标须存在、`at` 非空、不指向自己、与本单元共享 ≥1 token 或同 `also_in` 组 | 机械 | `check` + 台账 | 冻结 01-C4；02 §2.1（`reason` 只判非空，实测 161/272 不进、理由去重后 2 种） |
| **KC-2** | 整篇 token 守恒 | 非机器面单元的**每个** token 在 story 整篇或 `decisions.json` 有落点，缺哪个**点名哪个**。**不计**机器面单元（由合同的 `unit_kinds`/`table_columns` 打标）。判据不是「章内同现」——基线要求全部 token 同章，分开讲即判无落点 | 机械 | `check` | E7；冻结 01-C1 |
| **KC-3** | 编号形态 | `id_shapes.drop` 的形态在 story 里出现即 FAIL；`keep` 形态的上游验收编号须在「质量与验收」章出现 | 机械 | `check` | 01 §6.4 |
| **KC-4** | writer 独立 | story 写入发生在 Task 子 agent（events 可查）；主会话 spec 阶段上下文增量 ≤150K | 行为 | 子批 D `measure_run` | E5；00 §2（11K→818K 零 compaction） |
| **KC-5** | 完整率（**归批次 4**） | 对新实跑产物按 221 单元人工对照口径复核：完整 ≥90%、缺失 = 0（机器面除外） | 语义（人工） | 用户/评审 | 03 §3 |
| **KC-6** | prompt 无逐单元表 | spec/verifier 的扩展注入 ≤15KB | 机械 | 同 KA-2 | E4 |
| **KC-7** | **区分力** | 新 `check` 对 AR90004 对照样本必点名：接口字段名、AC-R 编号、两个 diagram 单元的落点缺失；旧 `audit.json` 的自由文本理由在三态下**无法表达** | 机械 | 脚本 + 台账 | 03 §3 |

## 2. 本子批独立重推的补充判据

| id | 判什么 | 口径 | 层 | 为什么必须有 |
|---|---|---|---|---|
| **KC-8** | **保留能力清单无损** | `story-build.mjs` 重写**前**列出仍需保留的能力（`decisions.json` 机器区/人工区分界渲染、图片路径重算与引用完整性、CRLF 安全分行、术语表首列守恒）；重写后逐条对照，每条要么有对应，要么写明删除理由 | 人核 + 单测 | 1337 行里混着批次 2 验证过的、与逐章生产线无关的能力。整体重写最容易「跑通了，判据悄悄少了一半」（同 KA-10） |
| **KC-9** | 逐章生产线彻底退场 | `scaffold` / `assemble(` / `findMissingFacts` / `ids.json` / `must_answer` / `inputs`（章节合同内）全域零命中 | 机械 | grep | §0 ②；这是本子批唯一能让全批退场 grep 归零的一步 |
| **KC-10** | **单元枚举不漏类型** | 八类单元（paragraph / list_item / table_row / image / link / diagram / blockquote / code）各有夹具用例；`table_row` 取**全部单元格**而非首列 | 机械 | 单测 | 02 §2.1：基线只从首列取 ASCII 标识符 → 中文表格全部掉进 reason，142 条「表头」理由结构上恒假 |
| **KC-11** | 机器面只能机器打标 | `machine_facing` 由枚举器按合同 `unit_kinds`/`table_columns` 判定；作者在 `audit.json` 里改成这一态即 FAIL | 机械 | `check` | 冻结 01-C4：否则它就是新的逃生口 |
| **KC-12** | 派生为空出声 | 单元枚举结果为零、或章节合同解析不出 14 章时，显式 BLOCKER 并指路，不静默当空集通过 | 机械 | 单测 | G7 |

## 3. 不在本子批判的

| 项 | 归属 |
|---|---|
| 完整率人工复核（KC-5） | 批次 4 |
| 效率七项、writer 独立的行为证据（KC-4） | 子批 D |
| 去重与 review 的验收补项 | 批次 4 |

## 4. 全批收口判据（本子批首次全量成立）

本子批是批次 3 里唯一能让 §0 **②③⑤ 全量成立**的一步：

- **②** 退场 grep 零命中——`scaffold` / `assemble(` / `findMissingFacts` 只在 `story-build.mjs`，
  `knowledge.json` 还剩 `adjudication.mjs` 与 `story-build.mjs` 两处，全在本子批处置面内；
- **③** 判据③ 清单里剩下的 `story-build.mjs` / `merge-story.mjs` / `story-chapters.json` /
  `SKILL.md` / `rules.md` / `evidence-rules.md` 全部在本子批重写；
- **⑤** 装配器 / 附录 / 账本 / 逐单元裁决表四类旧形态指令文本全仓清零。

## 5. 总判定（G5）

本子批 PASS ⟺ KC-1…KC-3、KC-6…KC-12 满足 **且** 固定验证全绿 **且** §0 ①②③⑤ **全量成立**。

KC-2 与 KC-7 是不可让步的两条：守恒判不出缺失、或对已知缺失没有区分力，
等于把「按章数数」换成了「整篇数数」——形态变了，效果没变。

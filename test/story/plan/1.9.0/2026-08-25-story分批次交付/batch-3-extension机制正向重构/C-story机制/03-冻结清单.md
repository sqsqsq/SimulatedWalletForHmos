# 子批 C · story 机制 · 冻结清单（G2 · 一次冻死，不重出）

> 冻结时间：2026-08-28，冻结点 = Step 7 收口后。指纹取 SHA-256 前 16 位。

## 1. 交付件

| 文件 | 行数 | 指纹 | 替代了什么（G11） |
|---|---|---|---|
| `skills/story/scripts/source-units.mjs` | 233 | `ddd03d796cd47a40` | **新建**。替代 `story-build` 里的取材与逐章守恒函数（`extractOne` / `extractSection` / `findMissingFacts` / `findMissingIdentifiers`） |
| `skills/story/scripts/story-build.mjs` | 455 | `a3fed2387f8a905f` | 1337 行的 1.0 逐章装配器（`scaffold` / `build` 两命令 + 逐章守恒） |
| `skills/story/scripts/review-render.mjs` | 128 | `5229ec4f6da455e7` | 从 `story-build` 拆出的保留能力（机器区重算 / 人工区逐字节保留 / 计划外意见区） |
| `skills/story/scripts/merge-story.mjs` | 214 | `cb2f71d028e2cfac` | 摘掉编号守恒与技术契约首列守恒（已被整篇 token 守恒覆盖） |
| `skills/story/scripts/story_flow.py` | 1045 | `c09b8082919d32bc` | 新增 `story` 态（S5 收口，自带 `story-build check` 门禁）；`archived` 前置改为成文态 |
| `skills/story/contracts/story-chapters.json` | 36 | `5b67993012eb61cd` | 399 行的逐章取材合同（`inputs` / `must_answer` / `form` 全删） |
| `skills/story/templates/story-template.md` | 81 | `14355fb397ec5985` | **新建**。14 标题 + 每标题一个「回答什么问题」+ 完备清单 |
| `skills/story/phases/story-write.md` | 71 | `0158bf8649ab54fe` | **新建**（子批 A 移交）。writer 子 agent 的作业书 |
| `skills/story/SKILL.md` | 183 | `47ec1e67aab46364` | 370 行的混合文本（链条 + 写作指令 + 命令清单） |
| `hooks/spec/post_check.mjs` | 795 | `dc4f04e96795e37d` | 删 story 三判据、登记源同文判据、`knowledge.json` 侧集合比对 |
| `hooks/shared/verdict-set.mjs` | 127 | `8928cdcf8530eaa2` | 原 `adjudication.mjs`：必答集改从 spec §10 表派生；域级判定校验随登记件退场 |

**已删除**：`hooks/shared/downstream.mjs`（子批 B）、`AR/story-src/knowledge.json` 这条链、
`ids.json`、逐章文件目录。**改名**：`freeze.mjs` → `contracts.mjs`、`adjudication.mjs` → `verdict-set.mjs`。

## 2. 阈值与封闭取值

| 项 | 值 |
|---|---|
| 来源单元类型 | `paragraph` / `list_item` / `table_row` / `image` / `link` / `diagram` / `blockquote` / `code`，**八类封闭** |
| 核对记录态 | `at` / `covered_by` / `machine_facing`，**三态**，三态之外 FAIL |
| 机器面打标 | `unit_kinds` 整类 + `table_columns` **逐列排除**（不整行打标） |
| token 来源 | 反引号跨度、ASCII 标识符（≥4）、带量纲数值、图片 basename、URL、合同声明的编号形态 |
| 无 token 单元的兜底 | 正文**精确子串**（≥8 字），不是相似度 |
| 章节数 | **14**，标题与顺序 = 合同；空节恰为「本需求不涉及。」 |
| 决策件必扫类别 | **六类**，零条目须写 `none_reason` |
| 台账形态数 | **44**（新增 C01） |

## 3. 夹具与台账

| 项 | 值 |
|---|---|
| `fixtures/failure-modes/C01-story-conservation/{bad,good}` | 树指纹 `3ac77981de90155f` |
| `test/story/regression/failure-modes.yaml` | 687 行，`f5429705586a3f4d` |
| `test/story/scripts/check_failure_modes.py` | 2146 行，`435073fc2b23760d` |

## 4. 冻结时的离线基线

台账 **44/44**；单测 **141 passed + 7 subtests**；复述分支 9/9；`node --check` / `compileall` 通过；
`--phase extensions` PASS 6/6；`doc/extensions` 非知识层 **9589**（基线 10358，净减 769）。

## 5. §0 收口判据

| # | 结果 |
|---|---|
| ① 总量 ≤7500 | **未达成：9589**。原因与三块最大的剩余见交付报告 §四，待用户裁定口径 |
| ② 退场 grep 零命中 | **成立** |
| ③ 承载被替换机制的文件重写或消失 | **成立** |
| ⑤ 无新旧并存 | **成立** |

## 6. 已知偏差

| 项 | 说明 |
|---|---|
| ① ≤7500 | 实际 9589。新增的执行器与通道（`probes` / `obligations` / `gate` / `source-units` / 六份须知）在基线态**不存在**，建起来必然净增；被它们替代的旧实现只有寥寥数行 |
| 纯中文单元的守恒 | 机器管不到（无 token）。处置：仍须三态之一，`check` 显式计数「靠语义判据守恒的单元数」——不假装机器管住了 |
| P15 / P16 | 数据源随判定登记件迁到归档件附录；新样本到位前对新产物记「不适用」，已在台账登记 `migration_note` |
| overlay story 两条 | 删除而非移出——S5 没有 framework verifier 可挂。story 质量由 writer 的 check + 批次 4 人工复核保证 |

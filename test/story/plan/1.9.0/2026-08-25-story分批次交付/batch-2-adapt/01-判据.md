# 批次 2 · adapt · 验收判据（第四版 · 判据先行 · G3）

> 随 00-方案 第四版一并待批。从冻结 04（含 2026-08-28 修订）、01 B3-1…B3-12、G11 独立重推。机械只说「集合相等 / 序列仍在 / 清单不含 / 零命中」。

| id | 判据 | 条款 | 口径 | 层 | checker |
|---|---|---|---|---|---|
| KD-1 | 机制整体替换 | C5/C8 | 升级后目标机制目录（含 `story-adaptation/`、四跳板）文件集与内容 == 包；多、少、不同均 FAIL | 机械 | `--check ①` |
| KD-2 | 目标知识正文守恒 | B3-8 | facts 与目标自加规约/模式：旧事实序列（非空单元格 + 段落按序）仍在新文件；允许新增列/行/frontmatter 键；未确认样板被填写、**包内有同名的规约/模式（升级换包版本）**不在其内 | 机械 | `--check ②` |
| KD-3 | 未确认 facts 不激活 | B3-11 / C13 | `provides.knowledge` 无 `confirmed: 未确认` 文件；每条路径存在 | 机械 | `--check ③` + `--phase extensions` |
| KD-4 | 规约与模式随包 | B3-6 / C15 | 包内有同名的规约与模式文件已换包版本且在清单；目标自加的保留且在清单 | 机械（脚本列出）+ 人核 | `--scan` 差异清单 |
| KD-5 | 数据对接按发起方 | B3-9 | 包 js 自述替身：目标 js 集与内容不变、方案含「数据对接待适配」；否则 `scripts/` == 包 | 机械 + 人核 | `--scan` |
| KD-6 | 自定义未动 | B3-8 | 目标其它 `skills/*` 与包不认识的文件集与内容指纹 == `before.json` 记录（写入前当场生成） | 机械 | `--check ④` |
| KD-7 | 既有校验通过 | C2/C16 | `--phase extensions` PASS | 机械 | 既有 |
| KD-8 | 方案先于写入 | C4/C10 | `plan.md` 四段齐、含用户确认记录（选项与原话）；写入时间晚于确认 | 人核 | 报告 |
| KD-10 | 零字面 | B3-10 | SKILL 与脚本无 hook / 知识文件名 / 域前缀 / 模式名；改名任一 hook 后自扫描仍通过 | 机械 | 台账 A01 |
| KD-11 | 证据缺口可交付 | C13/C14 | facts 未确认文件不在清单、`installed.md` 列缺口，`--check` 与 `--phase extensions` 仍 PASS | 机械 + 人核 | `--check` + 报告 |
| KD-13 | 入口文件含扩展段 | E1（2026-08-28 裁定） | 包有 `AGENTS.section.md` 时，目标 AGENTS.md（及存在的 CLAUDE.md）含其全文（空白规范化后子串）；包没有则跳过 | 机械 | `--check ⑤` |
| KD-12 | 行为 | G2/G10 | 内网仓（批次 1 已适配态）升级到当前包：KD-1…KD-7 全过、随后 spec 阶段能启动 | 行为 | 用户 |

台账：A01（零字面）、A02（`--check` 对「知识正文被改写」「目标自加规约被改写」「未确认 facts 进清单」「机制目录多一个旧文件」「自定义被动」反夹具 FAIL；对「包内同名规约换成包版本」正夹具 PASS——两向区分力）。

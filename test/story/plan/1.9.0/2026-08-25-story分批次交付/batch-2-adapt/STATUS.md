# 批次 2（重排前的批次 3）· adapt · 进展跟踪

| 项 | 值 |
|---|---|
| 状态 | **离线交付完成 · 待 holdout**（2026-08-28：Step 0–3 全部完成并冻结；方案第四版 + 判据获批，C8/C12 修订已回写冻结 04；执行会话补入 M1–M8） |
| 代码基线 | `09d6322b`（= `e96e90fc` + wallet-sdk-onboarding 废弃删除提交） |
| 开工基线数字 | F1–F8 全部符合预期（下表）；Story 单测 **137 passed + 7 subtests**；台账 **36 形态 PASS / 0 FAIL / 0 SKIP**；`--phase extensions` **PASS 6/6** |
| 需求基线 | [../01-全批次需求记录.md §3](../01-全批次需求记录.md)（B3-1…B3-12；C8/C12 已修订并回写冻结 04） |
| 方案文件 | [00-方案.md](00-方案.md)（**第四版**：主体是「每个文件首次安装 / 升级怎么办」一张表 + 执行 AI 操作顺序 + 一个 ≤150 行脚本 `--scan/--backup/--restore/--check`；不碰 git；无 hash、无归类引擎、无影子树、无动作 schema） |
| 判据文件 | [01-判据.md](01-判据.md)（KD-1…KD-12，先于实现） |
| 冻结清单 | [02-冻结清单.md](02-冻结清单.md)（2026-08-28 **一次冻死**：3 个交付件 + 阈值 + 2 份提示词 + 3 处夹具树 + 台账指纹；此后不重出） |
| 交付报告 | [03-交付报告.md](03-交付报告.md)（四栏分立；行为栏待用户内网复测填写） |
| holdout | 内网仓：批次 1 已适配态 → 升级到当前包（KD-12） |
| 事件补记 | 2026-08-28 执行模型对第三版提 N1–N8（未确认规则误伤规约与模式、`story-adaptation/` 无归属、git 回滚不通、发起方无判法、缺口与 FAIL 不分、补列、`adapt/` 归属、STATUS 不一致）；用户追问「究竟什么该适配、升级」并裁定 **adapt 不做 git 操作**、删除废弃 `wallet-sdk-onboarding`。第四版以「每个文件怎么办」表为主体，N1–N8 全部并入 |
| 验收结论 | 未出；两态：`feature_accepted`（本批）/ `package_finalized`（批次 3 后整包定版） |
| 回写 | 收口时更新 ../02-已验证机制参考.md 与 ../03-踩坑记录.md |

## 事件日志

- 2026-08-25 目录建立（当时编号批次 3），待启动。
- 2026-08-27 按批次 1 结果更新输入（01 B3-5 运行前提清单与前置检查、B3-6 C14 落实对象）：Extension 依赖 framework 两处热修 + `framework.config.json` 三项配置；`.claude/hooks` 三文件按用户裁定只留本仓、不随 Extension 走；可观测性适配对象 = facts 六个面。
- 2026-08-28 用户裁定 adapt 提前为批次 2（与现有优化不耦合；内网已适配批次 1，机制大改后可用 adapt 快速适配），并定原则：模型主导、脚本为辅；否定历史逐文件 hash 清单；扩展内容分机制/知识/桥接三类；`story/scripts` 的 js 按包来源处理。方案与判据落盘。
- 2026-08-28 用户追问「任何模型看了都知道怎么做吗」——第一版方案不自足（JSON 形状、迁移算法、残留判定、桥接谁写、glob/原子替换/确认记录/夹具树均未定）。重写 00-方案：`ownership.yaml` 全文、五个子命令 I/O schema、知识迁移三分支算法、demo 残留算法、桥接合成规则（模型写脚本校验）、apply 七步与 Windows 兜底、V1–V12 校验表、七套夹具树、六步执行顺序。代码基线：用户已 `reset --hard e96e90fc`。
- 2026-08-28 **Step 0 事实核对（执行会话）**：F1 四跳板 4；F2 本仓 `story.js:34` 自述「本实现是本地替身」；F3 `version: "1.1.0"`；F4 1.0 签名 2；F5 知识文件全有 `kind`，`facts/README.md` 与 `knowledge/README.md` 无表（0）、`constraints/README.md` 18 行表、`design-patterns/README.md` 20 行表；F6 `knowledge.mjs` 派生为空出声 11 处；F7 两处热修在位、framework `3.0.0`；F8 `skills/` 只剩 `story`，`releases/`、`adapt/` 不存在。**全部符合预期**。
- 2026-08-28 **执行会话对第四版提 M1–M4 并按建议并入方案**（用户确认本会话即实施会话）：M1 备份自指——`adapt/backup/` 在 `<extension_dir>` 内，`--restore` 顺序写死「先根文件（四跳板 + `framework.config.json`）后目录」，否则先删目录会把根文件备份一起删掉；M2 KD-6「自定义未动」原 checker 写「`--check ①` 反向」而 `--check ①` 只判机制，`before.json` 也不记自定义文件——改为 `--scan` 记自定义文件内容指纹、新增 `--check ④`；M3 重适配态在表里无列，判态句补「= 升级子集、机制行不执行；机制不等于包则按升级走」；M4 §6 Step 1 自扫描会就地产生 `doc/extensions/adapt/` 而 §1 明写「包不交付 `adapt/`」——包仓 `.gitignore` 加该路径（本仓专属不随包），并去掉 `installed.md` 的版本字段（判态只读 `manifest.yaml > version`，双真源）。
- 2026-08-28 **Step 0 交付**（提交 `b4979562`）：`skills/story-adaptation/SKILL.md`（109 行 ≤120，零字面 grep 0）+ `templates/plan.md` + manifest `provides.skills` 登记 + 本仓 `.gitignore` 加 `doc/extensions/adapt/`。
  - **M5（执行会话发现）**：方案 §2 操作顺序写「你逐文件按 §1 表判」，但 §1 表在 `test/story/plan/`（维护件，`.gitignore` 内、不随包交付），执行适配的 AI 读不到——指向了交付包里不存在的文档。处置：「每个文件怎么办」表**完整写进 SKILL.md §2**，它就是 SKILL 的主体。
  - **M6（台账实测抓到）**：1.0 结构签名第二条原为「`knowledge/component-profile.md` 在根」，该文件名仍在激活清单里，写进 SKILL 被 **M01 判 FAIL**（机制层硬编码知识标识）。改为等价且更准确的结构特征「知识文件平铺在根、没有按类分目录」；维护件与冻结件的原表述留档不改（不在 M01 扫描面）。**这条是「机制层零知识硬编码」不变量第一次在本批拦下真实违规**。
  - 验证：`--phase extensions` PASS 6/6；单测 137 passed + 7 subtests；台账 **36/36**（M01 修正后）。
- 2026-08-28 **Step 1 交付**（提交 `115e1808`）：`adapt-scan.mjs` 四开关（216 行 / 有效 172，零字面 0 命中，退出码 0/1/2 各自验证）。
  - 本仓自扫描（目标 = 包）：版本 `1.1.0 → 1.1.0`、机制三项差异全 0、知识 12 文件（4 个 README 归桥接/机制）、清单 16 条、js 自述替身 true、自定义 0、四跳板与配置齐 —— **重适配·无变化**，与方案 §6 Step 1 预期一致。
  - `--backup`/`--restore` 往返实测（scratchpad 最小树）：改写、删除、新增、篡改跳板四种破坏全部还原。
  - `--check` 四项各自有区分力：① 机制缺文件；② 点名「缺哪条事实」；③ 未确认进清单；④ 自定义被改。
  - **三处方案未定项在实施中坐实**（已回写方案 §3）：`cpSync` 拒绝复制到自身子目录（M1 的实证，改逐文件复制）；`--restore` 需保留工作记录否则抹掉用户确认记录；台账扫描面须排除 `adapt/`（**M4 第二面**——只做 `.gitignore` 不够，台账扫的是文件系统，`before.json` 触发 M01/M03/M04 三条 FAIL）。
  - **偏差登记**：行数 216 > 方案 ≤150。四开关能力均有消费者、无死代码；多出量在 M1 逐文件复制与工作记录保留两处。不为凑数字挤行删注释（同批次 4「< 9000」的教训）。
  - 验证：台账 **36/36**；单测 137 passed + 7 subtests；`--phase extensions` PASS 6/6。
- 2026-08-28 **Step 2 交付**（提交 `3e10dd8e`）：四跳板加 `adapt [<目标工程>]` 路由行（命令转化表 + 工作流程表 + frontmatter 用法，四份链接实测可达）、`story/SKILL.md` 一句指路、走查夹具 `fixtures/adapt/target-prior/`（中性题材：甲/乙工程、alpha 域）。
  - **完整走查跑通**（升级态：包 `1.1.0` / 目标 `1.0.0`）：读两棵树 → 按表写入（机制 51 文件换新 + 四跳板；js 因包自述替身而保留目标的；目标自有的规约域与事实面原样保留；包新增规约模式 10 个已确认进清单、包新增事实面 2 个样板未确认**不进清单**；索引 README 无表整抄有表重算；manifest 重算 16 条）→ `--check` 四项全过 → 人为破坏 → 精确报出 → `--restore` → **与写入前快照逐文件完全一致**。目标原有知识正文、自定义 Skill、目标 js 三者逐字节未变。
  - **M7（走查抓到 SKILL 表缺一格）**：表第 3 行写「包里**有同名文件的**规约与模式」、第 4 行写「目标自己加的」——**漏了「包有、目标没有」**。首次安装时被第 3 行语义盖住，升级态下「包新增的规约域 / 模式」无处可归，与 C15「两模式默认进清单」相斥。已改行标题为「包里**有的**」并在升级列写明两种情况。
  - **M8（走查抓到 `--restore` 缺陷）**：`extension_dir` 整体替换没问题，但仓库根文件是逐个从备份复制回的——本次写入**新增**的三个宿主跳板备份里没有，回滚后原样留下，树 ⊃ 备份、KD-9 不成立。已修：备份里没有而目标上有的根文件显式删除并向上收空目录（`rmSync` 删目录必须带 `recursive`，否则异常被 catch 吞掉，表面像没执行）。
  - **一次自我更正**：第一轮的「严格比对通过」是假的——比对基准快照是在未修复的 restore 之后取的，那三个跳板当时已污染夹具。从干净夹具重取基准后才真正验到。
  - **夹具改名** `target-1.1` → `target-prior`：包本身已是 `1.1.0`，同名目标会被判重适配、测不到升级。
  - 验证：台账 36/36；单测 137 passed + 7 subtests；`--phase extensions` PASS 6/6。
- 2026-08-28 **Step 3 交付 + 收口**（提交 `6841ac50`）：台账新增 **A01**（adapt 交付件写死扩展内部结构——基准全部派生：阶段名取 `hooks/` 子目录、文件名与编号取激活清单）与 **A02**（`--check` 判不出已知违规——调真实脚本、退出码即结论，夹具自带两棵树 + 写入后状态并复制到临时目录跑）。A01 bad 抓 2 处写死 hook 路径；A02 bad 四项违规各报一条；A02 真实目标用**当前包**核未适配树报出 57 处差异。冻结清单一次落盘、交付报告四栏分立。
  - **离线全绿**：台账 **38/38**（FAIL 0 / SKIP 0）、单测 137 passed + 7 subtests、复述分支 9/9、`node --check`、`compileall`、`--phase extensions` PASS 6/6、零字面 grep 0 命中、退出码 0/1/2 各自实测。
  - **行数账**（G11）：`doc/extensions` 非知识层 9987 → 10395（+408）。基线态无 adapt 能力故必然净增；同职责对比 **2.x 819 行 → 本批 408 行**，且多出备份/回滚、知识事实序列守恒、自定义守恒三项 2.x 没有的能力。
  - **与方案的差异**：脚本 225 行 > ≤150（无死代码，多出量在 M1 逐文件复制、工作记录保留、M8 根文件清理三处，不为凑数字挤行）；走查夹具改名 `target-prior`；A02 反夹具 3 条 → 4 条（`--check ④` 也配了反夹具）。
- 2026-08-28 **Step 4：删除 `--backup` / `--restore`**（提交 `9b81a00f`）。用户质疑「目标仓是 git 仓，有问题由执行人自己回滚，不需要 adapt 提供这么复杂的功能」——**成立**。
  - **它是怎么混进来的**：第三版原本不做影子树；评审 N3 指出「`git checkout` 对首次安装的 untracked 目录无效、非 git 仓无回滚」，后半句是在给一个不存在的场景造能力；「adapt 不做 git 操作」的裁定随后被实现成了「那就自己做一套备份」。**坑册 #52 的同一个病**：评审提问题 → 方案加机制，而正确答案是「这件事不归 adapt 管」。
  - **代价**：47 行代码，Step 1–2 记录的坑里**四个是它自己制造的**（备份自指 `ERR_FS_CP_EINVAL`、回滚顺序必须先根后目录、回滚抹掉用户确认记录、新增根文件不清理）。删掉后一并消失。
  - **删干净、交付件零提及**（用户「不支持就不讲」）：第一版计划把「自己做备份」换成「写一段 git 回滚说明 + 为回滚服务的 `git status` 前置」，**那还是打补丁**，被当场否掉。最终交付件里不提回滚、不给命令、不加相关前置；`--check` FAIL 的处置改为「照它报的那几项改，重跑校验」——它点的本就是具体位置，直接改到位即可。
  - **同步**：冻结 04 追加 **C10/C11 修订**（失败恢复不由 adapt 承载）；B3-3 去掉「影子树」与「失败恢复」（前者第三版起就不做，文本一直没改——属「契约文本与实现相斥」）；判据删 KD-9、KD-8 去掉备份半句；坑册新增 #58。
  - **数字**：脚本 225 → **176 行**、开关 4 → 2；`doc/extensions` 非知识层 10395 → **10341**（较基线 +354，同职责 2.x 为 819）。冻结清单**重出一次**并写明理由（范围变更、holdout 未揭盲，不违反 G2 时序）。
  - 验证：走查重跑（写入 → `--check` 四项全过 → 破坏 → 精确报出）；台账 **38/38**；单测 137 passed + 7 subtests；复述分支 9/9；`node --check`；`compileall`；`--phase extensions` PASS 6/6；**adapt 交付件 grep `backup|restore|回滚|备份` 零命中**。
- 2026-08-28 另一模型评审第二版提出 Q1–Q9（知识 schema 穿不过、影子树上跑不了 harness、未确认样板直接生效、清单强制全激活、origin 被继承…）。用户随即指出**第二版本身就是脚本化方案换皮**（glob 引擎 + 影子树 + 动作 schema + 12 项校验 = 脚本主导、模型填表），与否定 2.x hash 方案的理由同类。**第三版推倒重写**：Skill 为主体、脚本一个只做两棵树清单与知识正文守恒核对、git 回滚、既有机制校验；Q1–Q9 多数改为「由模型理解处理」，两条进需求（B3-11 未确认不激活、E9 新增字段可缺省）。**开工前置**：用户对 C8/C12 修订点头。
- 2026-08-28 **复核修复（Step 5，提交 `ext.b2-step5`）**：维护者只读复核批次 2 交付，抓到 `--check ②` 守恒对象过宽——对包内同名规约/模式也核「旧事实仍在」，与 SKILL §2「升级换成包的版本」相斥，内网升级会误报且 SKILL 让执行者「照报的改」会把旧内容塞回去；走查夹具 `target-prior` 没有这一行的用例。修正：② 跳过 `kind ≠ facts` 且包内同名的文件；`MOCK_MARKERS` 去掉本仓字面 `MOCK_DATA_DIR`；A02 加正例（同名换版本 → PASS）与反例（目标自加规约被改 → FAIL，防豁免过宽）；`target-prior` 加同名旧规约。契约文本同步：01 B3-7/B3-8/B3-12 删「影子树 / 原子替换 / 备份 / git 回滚」残留。验证：台账 38/38、单测 137+7、`--phase extensions` 6/6、零字面 0；冻结清单第二次重出（脚本缺陷，holdout 未揭盲）。坑册 #59。**批次 2 离线交付成立，待内网 holdout（KD-12）**；批次 3 可并行启动。
- 2026-08-28 **Step 6（提交 `ext.b2-step6`）**：用户裁定 author.md 的送达靠扩展自供 `AGENTS.section.md` 经 framework `render-agents-md` 渲染进 CLAUDE.md/AGENTS.md（已证实 codex 只自动加载 AGENTS.md、不加载 rules 目录；framework 自身 adapter 说明亦称 rules「引用可达」），并要求 adapt 同步「否则会漏」。改动：`classOf` 把 `AGENTS.section.md` 归机制类；`--check ⑤` 核目标 AGENTS.md（及存在的 CLAUDE.md）含该段全文，包没有该文件时跳过；SKILL §2 机制行、§6 写入末加「重渲染入口文件」、§7 五件事；A02 加正例（after 的 AGENTS.md 含该段 → PASS）与反例（未重渲染 → ⑤ FAIL）；判据 KD-13。验证：台账 38/38、单测 137+7、`--phase extensions` 6/6、零字面 0、本仓自扫描仍「重适配·无变化」（包尚无该文件 → ⑤ 跳过）。冻结清单第三次重出（用户新增裁定，holdout 未揭盲）。
- 2026-08-28 **Step 7（提交 `ext.b2-step7`）**：复核发现 SKILL §0 写「目标 framework 版本 ≥ 包 manifest 声明的下限 / 包声明的必需 framework 文件」，而 manifest 没有任何这类声明——契约文本指向不存在的字段（AGENTS §2）。改为在 SKILL §0 内联列明：`framework/package.json.version ≥ 3.0.0`、两处热修文件、以及包交付 `AGENTS.section.md` 时 `template-renderer.ts` 须含扩展段注入点。零字面 0、台账 38/38。冻结清单随 Step 6 同一次重出更新指纹。
- 2026-08-28 **Step 8（提交 `ext.b2-step8`）**：用户修正「不改 framework，不改就不要讲」。SKILL §0 删去 `template-renderer.ts` 注入点前提；§2 机制行与 §6 改为「把 `AGENTS.section.md` 内容写进目标 AGENTS.md / CLAUDE.md 的实例扩展节」；`--check ⑤` 口径不变、提示文案同步。台账 38/38、零字面 0。冻结清单指纹随同一次重出更新。

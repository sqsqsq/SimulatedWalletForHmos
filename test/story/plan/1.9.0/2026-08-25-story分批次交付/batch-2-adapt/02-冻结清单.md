# 批次 2 · adapt · 冻结清单（G2 · 一次冻死，不重出）

> 冻结时间：2026-08-28。冻结点 = **Step 4 收口后**。
> 此后 holdout（内网复测，KD-12）由用户执行；**本清单不再重出**——重出即等于 holdout 时序失效。
> 指纹取 SHA-256 前 16 位。
>
> **重出一次的理由（唯一一次）**：Step 3 冻结后，用户裁定删除 `--backup` / `--restore`
> 两个开关（目标仓的版本管理属执行人职责，adapt 不该做第二套）。这是**范围变更**，
> 不是反复调整同一份实现；holdout 尚未揭盲，此时重出不违反 G2 时序。
>
> **第二次重出的理由（2026-08-28 复核，Step 5）**：`--check ②` 把「包内同名规约/模式」也纳入守恒对象，
> 与 SKILL §2 表「升级换成包的版本」相斥——内网升级第一次跑就会误报。走查夹具没有这一行的用例，
> 所以之前「四项全过」没踩到它。修正：② 跳过 `kind ≠ facts` 且包内同名的文件；A02 加正例（同名换版本 PASS）
> 与反例（目标自加规约被改 FAIL）；`target-prior` 加一份与包同名的旧规约。脚本 176 → 180 行。
> holdout 仍未揭盲，不违反 G2。
>
> **第三次重出的理由（2026-08-28，Step 6）**：用户新增裁定——author.md 的送达靠扩展自供 `AGENTS.section.md`
> 经 framework `render-agents-md` 渲染进入口文件（codex 只自动加载 AGENTS.md）。adapt 若不认这个文件，升级到内网后
> 那一段不会出现在 AGENTS.md 里。改动：`classOf` 把 `AGENTS.section.md` 归机制类；`--check ⑤` 核入口文件含该段
> （包没有时跳过）；SKILL §2/§6/§7 各加一句；A02 加正反例。脚本 180 → 191 行。holdout 未揭盲，不违反 G2。
> **2026-08-28 补记（Step 8，同一次重出）**：用户修正——**不改 framework**。`AGENTS.section.md` 由 adapt 直接写进目标的 AGENTS.md / CLAUDE.md「实例扩展」节，`--check ⑤` 口径不变；SKILL §0 删去对 framework 注入点的前提。指纹更新见下表。
> **此后不再重出。**

## 1. 交付件（`doc/extensions`）

| 文件 | 行数 | 指纹 | 替代了 2.x 的什么 |
|---|---|---|---|
| `skills/story-adaptation/SKILL.md` | 105 | `9d362cdf53e20e02`（Step 7 §0 前提内联；Step 8 去 framework 前提、入口文件改直接写入） | 2.x 同路径 SKILL.md（97 行）——但主体换了：从「流程叙述」换成「每个文件首次安装/升级怎么办」一张表 |
| `skills/story-adaptation/scripts/adapt-scan.mjs` | 191 | `3c26e8902670c105`（Step 8：⑤ 提示文案） | 2.x `scripts/build-release.mjs`（122）+ `skills/story-adaptation/scripts/verify-adaptation.mjs`（164）+ `releases/1.0.json`（188）+ `releases/2.0.json`（248）**四件共 722 行**，全部退场 |
| `skills/story-adaptation/templates/plan.md` | 71 | `3f353e9cf90d9bf5` | 新增（2.x 无方案模板，方案形态散在 SKILL 叙述里） |
| `manifest.yaml` | +1 行 | `551d0e2bd2df2136` | `provides.skills` 登记 `story-adaptation` |
| `skills/story/SKILL.md` | +2 行 | — | 一句指路（adapt 不在本文件） |
| 四宿主跳板 ×4 | 各 +2 行 | — | 命令转化表 + 工作流程表各一行；frontmatter 用法加 `adapt` |

**行数账**：`doc/extensions` 非知识层 **9987 → 10341（+354）**。
基线态根本没有 adapt 能力（批次 0 已把 2.x 整个回退掉），所以本批必然是净增；
同职责对比才有意义——**2.x 用 819 行做的事，本批用 354 行做完**
（G11 的「标注替代了什么」按上表逐行给出）。

## 2. 阈值与封闭取值

| 项 | 值 | 出处 |
|---|---|---|
| 脚本模式 | `--scan` / `--check` **两个，无第三个** | 方案 §3 |
| 不提供的能力 | 备份、回滚，以及交付件里关于它们的任何说明 | 冻结 04-C10/C11 修订（2026-08-28） |
| 退出码 | 0 通过 / 1 核对不符 / 2 参数或前置错误 | 方案 §3 |
| `--check` 判据 | 四项：① 机制 == 包 ② 目标知识旧事实序列仍按序在 ③ 清单无未确认且路径齐 ④ 自定义未动 | KD-1/2/3/6 |
| 未确认标记 | frontmatter `confirmed: 未确认`（仅事实面适用；规约与模式默认已确认） | B3-11 / C13 / C15 |
| 替身标记 | `本地替身` / `模拟实现` / `MOCK_DATA_DIR` 三者任一命中即判包为 demo | 方案 §1「看发起方」 |
| 仓库根安装单元 | 四宿主跳板 + `framework.config.json`，共 5 个 | 方案 §1 |
| 历史版本签名 | `hooks/constraint-application.md` 存在 ＋ 知识文件平铺在 `knowledge/` 根 ＋ 无 `knowledge/facts/` | 冻结 04-C12 修订（交付件用结构特征表述，不点名文件——M01 会判 FAIL） |
| 台账形态数 | **38**（新增 A01、A02） | 本批 |

## 3. 提示词（交付给执行适配的 AI 的全部文本）

只有两份，别处不再有 adapt 的行为指令：

| 文件 | 承载 |
|---|---|
| `skills/story-adaptation/SKILL.md` | 前置 / 判态 / **每个文件怎么办表** / 读两棵树 / 写方案 / 确认 / 备份 / 写入 / 校验 / 收口 / 取证顺序 |
| `skills/story-adaptation/templates/plan.md` | 方案四段骨架 + 用户确认记录 |

四跳板与 `story/SKILL.md` 里只有**路由**，无行为指令。

## 4. 夹具

| 夹具 | 用途 |
|---|---|
| `test/story/fixtures/adapt/target-prior/`（13 文件，含一份与包同名的旧规约 `dfx-baseline.md`） | 走查用的未适配目标树（版本 `1.0.0` < 包 `1.1.0`）；同时是 A02 真实目标那一栏的取证对象 |
| `test/story/fixtures/failure-modes/A01-adapt-couples-to-mechanism/{bad,good}` | adapt 交付件是否写死扩展内部结构 |
| `test/story/fixtures/failure-modes/A02-adapt-check-blind/{bad,good}` | `--check` 四项区分力；各自带 `package/`+`target/`+可选 `after/` |

三处夹具树整体指纹：`9609cde25ae6cf4f`

## 5. 台账与 checker

| 文件 | 指纹 |
|---|---|
| `test/story/regression/failure-modes.yaml` | `12ad8dc8702a5cf6` |
| `test/story/scripts/check_failure_modes.py` | `a6eb4af0b1cc5810` |

## 6. 冻结时的离线基线

| 项 | 结果 |
|---|---|
| 台账全量 | **38 / 38**（FAIL 0、SKIP 0） |
| Story 单测 | 137 passed + 7 subtests |
| 复述分支 | 9 / 9 |
| `node --check` | adapt-scan.mjs 通过 |
| `compileall` | tools/cli + test/story/scripts 通过 |
| `--phase extensions` | PASS 6/6，Blockers 0 |
| 零字面 grep | SKILL 与脚本各 0 命中 |

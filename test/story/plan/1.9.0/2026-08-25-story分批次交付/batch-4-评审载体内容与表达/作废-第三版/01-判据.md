# 批次 2 · 验收判据（第三版 · 判据先行 · G3）

> 状态：随 00-方案 第三版一并**待用户批准**（2026-08-27）。判据从冻结 01-C1…C5、冻结 02（图片/链接/验收编号/统稿）、01 记录 B2-* 与裁定 D1–D9 独立重推。
> 三责任分离（G4）与总判定规则（G5）同批次 1 卷首，不复述。机械判据只说「集合一致 / 存在 / 落点可检索 / 语法之内」，不说「已去重」「可读」。
> **本版没有任何新增围栏门禁**：机械判据要么是既有整篇检查，要么是冻结 01-C4 要求的核对记录本身。

## 1. 生产线拆除（源头治理）

| id | 判据 | 条款 | 口径 | 责任层 | checker | 回归形态 |
|---|---|---|---|---|---|---|
| K2-A1 | 逐章生产线不存在 | 冻结 01-C1（盘点生成规则与模板）；D5 | **计** `doc/extensions` 零命中：scaffold、assemble、findMissingFacts、FORM_COUNTERS、findMissingIdentifiers、renderIdRefs、renderStoryAppendix、refreshAppendix、ids.json、must_answer、transcribe_note、demoteHeadings、assembled-by、story-src/chapters、`{{DEC`、`{{ID`；**不计** knowledge/ | 机械 | §9 grep | M16 |
| K2-A2 | 非知识层净减 | D5「内容减少」 | **计** `doc/extensions` 非知识层行数 < 9000（基线 10050）；**不计** 具体减多少 | 机械 | `wc -l` | — |
| K2-A3 | 合同只剩提纲 | D3 | **计** 14 章各有 `id/title/question`，无其它字段；顺序不变 | 机械 | `story-build check` 读合同 | M07（自然 PASS）、M13 |

## 2. 内容守恒（冻结 01-C1/C4；B2-4/B2-7/B2-8）

| id | 判据 | 条款 | 口径 | 责任层 | checker | 回归形态 |
|---|---|---|---|---|---|---|
| K2-B1 | 来源单元枚举确定性 | B2-4 | **计** 两次枚举键集相同；五类单元各能切出（夹具）；某源零单元 throw；**不计** 切分粒度合理性 | 机械 | `source-units.mjs` 单测 | — |
| K2-B2 | 核对记录集合一致与齐备 | 冻结 01-C4 | **计** `records.unit` 集 == 单元键集；`at` 非空 ∈ 14 标题；`at` 空 ⇒ `reason` 非空；`by ∈ {machine, author}`；`units_sha` 一致；**不计** 落点对不对 | 机械 | `story-build audit --verify` / `check` | P19 |
| K2-B3 | 机器落点可重算 | 冻结 01-C4 | **计** `by: machine` 的 `at` 与按 token 重算一致；**不计** 散文单元 | 机械 | 同上 | P20 |
| K2-B4 | 整篇契约与数值守恒 | B2-7、B2-8 | **计** 术语表首列、技术契约首列在 story 有落点（既有）；PRD/SE 每个带量纲数值在 story 或 decisions 有落点（新）；story 数值须上游有或决策承载（既有）；**不计** 编号 | 机械 | `merge-story --check`、`story-build check` | — |
| K2-B5 | 图片与链接不丢不降级 | 冻结 02-C2 | **计** 每个图片单元文件名、链接 URL 在 story 出现；图片路径可解析、全篇唯一（既有 lint） | 机械 | `audit`（作图片/链接单元落点）+ `lint-rules.scanBrokenImages` | P10、P14 |
| K2-B6 | 逐单元语义复核 | 冻结 01-C4；B2-4「verifier 逐单元复核」 | verifier 对每条记录裁「落点成立 / 落点错 / 漏了 / 不进理由成立」，全集无条件，报告同行含键与裁决词；「本需求不涉及。」的章其单元理由一并裁 | 语义 | `hooks/shared/audit.mjs` 派生 → `pre_verifier` 注入 → `post_check` `story_audit_persisted` | P11（口径扩到单元） |
| K2-B7 | 核对记录不进正文不随归档 | 冻结 01-C4 | **计** story.md 不含 `audit.json`/`source-units`/`story-src` 字样；上传集只有两份归档件 | 机械 | `merge-story --check`（悬空引用既有）+ story.js 事实 | P08 |

## 3. 去重与单源（冻结 01-C2/C3；B2-6）

| id | 判据 | 条款 | 口径 | 责任层 | checker | 回归形态 |
|---|---|---|---|---|---|---|
| K2-C1 | 一件事只讲全一次 | 冻结 01-C2、C3 | verifier 通读：同一事实的触发/规则/结果在两个标题下都完整出现即「重复」；不同标题各讲独有半边不算 | 语义 | `story_reader_fitness` 增句 + `story_audit_substance` | P12（形态提示） |
| K2-C2 | 决策值单写 | B2-6 | **计** story 无 `《决策与评审记录》——` 指向式引用；无 `{{`；无议程措辞；**不计** 同义改写 | 机械 | `story-build check` | P26、P09 |
| K2-C3 | 标题骨架 | D3、D7 | **计** `## ` 恰 14 个、标题与顺序 == 合同；每标题下非空或恰为「本需求不涉及。」；无 `## 附录` | 机械 | `story-build check` | P21 |
| K2-C4 | 知识两表不在 story | D6 | **计** story 不含「附录：规约符合性」「附录：设计模式候选」；`knowledge-review.md` == 登记源渲染 | 机械 | `story-build check` | P26 |

## 4. 读者语言（冻结 01-C3/C4；B2-11）

| id | 判据 | 条款 | 口径 | 责任层 | checker | 回归形态 |
|---|---|---|---|---|---|---|
| K2-E1 | 归档件自包含（既有） | rules 红线 4 | **计** 仓内路径、裸文件名（含 `.ets/.ts`）、内部规则文件名、客户端禁用词零命中 | 机械 | `merge-story --check` 四条红线 | P08 |
| K2-E2 | 主叙事用读者语言 | 冻结 02-C1 目标句 | verifier：出现编号（验收章除外）、类名、文件名、模块名、规约编号或本流程词汇即不达标；叙述能支撑「要做什么/为什么/动了哪些存量/哪里会出问题」 | 语义 | `story_reader_fitness` | P07（形态提示） |
| K2-E3 | 写作指令是读者视角 | B1-15 同向原则 | **计** `story_on_context_load.md` story 段含「第一次接触这个需求的评审人」「一件事只在最该讲它的标题下讲全」「可以分几次写入同一个文件」三句；不含形式配额 | 机械 | 文本扫描 + M13 | M14 |

## 5. review（B2-9；D1、D2）

| id | 判据 | 条款 | 口径 | 责任层 | checker | 回归形态 |
|---|---|---|---|---|---|---|
| K2-D1 | 评审记录只含渲染语法 | D1、D5 | **计** 全部行 ∈ {标题、建议/结论、为什么、改了会影响、请…确认、三勾选、勾选下人工行、`<!-- DEC-x -->`、其他意见及其标记、空行}；出现「如何填写」「确认人」「状态」「下一步」「附录」即 FAIL；**不计** 人工内容 | 机械 | `story-build check` | P25 |
| K2-D2 | 机器区确定性、人工区逐字节 | B2-9 | 既有口径 | 机械 | 既有 | — |
| K2-D3 | 影响是人话 | D5 | **计** `impact` 非空且不含编号形态 | 机械 | `validateDecisions` | P25 |
| K2-D4 | 范围决策不进评审 | D2 | **计** `source: story_init` 不出现在 review.md；`review_reflow.md` 含「重跑 story init」原句 | 机械 | `renderReview/check` + 文本扫描 | P24 |
| K2-D5 | 处置台账 schema | B2-9 | **计** 顶层键 ⊆ {reqNo, at, items}；item 键 ⊆ {from, verdict, note, kind, disposition}；`verdict ∈ {agree, revise, defer}`；`from` 存在 | 机械 | `story-build check` + `hooks/plan/post_check.mjs` | P23 |
| K2-D6 | 表态缺失不静默 | B2-9 | 已归档且进 plan 时列出未勾议题；MINOR；未归档 SKIP | 语义 | `review_gate_acknowledged` | P22 |

## 6. 前置、测量、行为

| id | 判据 | 条款 | 口径 | 责任层 | checker | 回归形态 |
|---|---|---|---|---|---|---|
| K2-F1 | 闭环回填是明写步骤 | 批次 1 登记 #1 | 三注入件各含「再跑一次 harness」段 | 机械 | 文本扫描 | — |
| K2-F2 | 交互脚本阶段前提 | 第一版 T-3 | `expected_phase` 不符不发送、回落 `interaction_phase_mismatch` | 机械 | 单测 | — |
| K2-G1 | 测量报告 | B2-10、G8 | 两表；无记录报「不可测量」；数字不进 PASS | 机械 | `measure_dedup.py` | — |
| K2-G2 | 批次 1 保护 | Codex P0-1 | 台账全量 PASS；三方集合一致（登记源 = spec 出口 = acceptance）不变；两张知识表在 `knowledge-review.md` 中与登记源渲染逐字一致 | 机械 | `check_failure_modes.py` | 全部 M/P |
| K2-G3 | 机制层零硬编码、零 Case 特征、无新旧并存 | G7；AGENTS §2 | M01/M02/M16/M17 扩到新文件；K2-A1 清零 | 机械 | `check_failure_modes.py` | M01、M02、M16 |
| K2-G4 | 新判据对旧产物有区分力 | 方案 §10.4 | 对第一版实跑件：P25、P26 命中；**不计** 作 PASS 条件 | 机械 | `check_failure_modes.py --feature`（副本） | — |
| K2-H1 | 行为终点 | G2；D9 | `pattern-image-review` 真实到达 spec 闭环：三门禁含回填 PASS；story 14 标题、图片可达唯一、关键事实各只讲全一次（抽 5 个 grep 计数）；audit 集合一致；review 只含语法行；归档/处置标「行为未验证」 | 行为 | 实跑 + 评测 | — |

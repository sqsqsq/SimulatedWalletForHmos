# 批次 4 · F3 三删两立一恢复（执行方案 · 自足件）

> 交独立执行会话实施。前置输入只有四件：本方案、`01-判据.md`（KG3）、
> `../F2-金样对齐/04-实跑根因与责任归因.md`（实证，含行号级残差清单）、两份金样
> （story 金样 `test/story/golden/story-金样-AR90004.md`、review 金样 `test/story/golden/review-金样-AR90006.md`）。
> **执行铁律**（F2 教训固化）：
> ① 任何新判据落地前先对两份金样跑零 FAIL——拦金样即判据错，拦金样必备形态的判据同样是判据错；
> ② 方案没写的不做；做了方案没写的（含被迫的适配）必须登记差异栏——F2 的差异栏先例照抄；
> ③ 修改 `doc/extensions` 与 `test/story`（fixtures/tests/regression）；`framework/`、`test/story/cases/`、
>    运行装置（`run_*.py`、`config/`）一律不动；
> ④ 收口必须在线全链条（两 Case CLI 到 spec 闭环）——F2 靠离线台账收口漏掉了在线判据打架，不许重演。

## §0 一句话读懂本轮

上两轮已经证明一条铁律：**只有接上判据的要求会被执行；写在注释、占位、作业书自觉项里的要求全部不被执行**
（证据：04 件 §一的表，零例外）。F3 做三件事：把该删的无效机制删掉（六类决策骨架）、把金样级的「形」
全部迁到可判载体上（合同＋lint）、把判「消化没消化」的语义防线（裁决面）恢复有效。

不动清单（有实证的存在理由，删它们是错的，理由见 04 件 §七）：
硬事实守恒／逐章渲染／语言红线（step8 本需求编号特例保留）／句级重复判据／review-render 三态渲染／audit 三态。

---

## §1 D1 · 节级形态：四个落点联动（对 04 件 R1）

### 1a 合同 `contracts/story-chapters.json`

现状：`chapters[]` 中仅 `03-scope` 与 `10-appendix` 有 `subsections`；`04/05/06/09` 无。
`subsections` 的校验现在**只作用于附录**（story-build.mjs:364/:988/:1137 全部取 `appendix?.subsections`）。

改动——四章补节级数据（正文章与附录语义不同：附录是「恰好五节」，正文章是「菜单＋真有才有」，
所以用**新 key**，不复用会触发附录全等校验的 `subsections`）：

```json
"04-solution": { "section_menu": ["参与方与分工", "业务规则与真源", "关键取舍", "主要风险与控制"],
  "section_required": ["参与方与分工"],
  "section_note": "菜单是命名参考不是配额；「关键取舍」在决策登记有已定条目时必须成节；其余真有才有。" }
"05-flow":     { "section_note": "主路径必须单独成节；每条重要分支或恢复路径各自成节、按业务命名（一条支线一节，不合并）；外部系统的调用时机真有才有。", "min_sections": 1 }
"06-features": { "section_note": "每个页面或场景一节；页面状态与界面适配各自成节，真有才有。", "min_sections": 1 }
"09-rollout":  { "section_menu": ["上线路径", "开关与兼容", "回退设计", "交付物"],
  "section_required": ["回退设计"],
  "section_note": "回退是评审者必看的一节；其余菜单项真有才有。" }
```

### 1b `story-build check` 新增「正文章节级」判定（放进既有 check 流程，与附录小节判据并列）

规则（**只有这四条，不许加码**）：
1. 带 `section_required` 的章：所需小节须存在（标题比较走既有 `normalizeHeading` 剥序号通道）；
2. `04` 章追加一条联动：`decisions.json` 里 `status: settled` 条目 ≥1 时，「关键取舍」小节必须存在；
3. 带 `min_sections` 的章：章内 `###` 数 ≥ 该值；章正文恰为「本需求不涉及。」一句时豁免（空章规则既有）；
4. 报错文案按既有风格写「这一类形态长什么样」，**不列产物字面**。
金样锚：story 金样 4.1/4.4/9.3 存在、05/06 章 H3 数为 5/3——四条全过。
反例夹具（`test/story/fixtures/failure-modes/` 新增，沿 W 系列命名规则）：
04 章无「关键取舍」而 decisions 有 settled → 点名；05 章零 H3 → 点名。

### 1c 模板 `templates/story-template.md`

- 04/09 章按 03 先例补**显式命名 H3 骨架**（`### 4.1 参与方与分工` … `### 9.3 回退设计`），
  每节下一两行注释写该节回答什么；「真有才有」的节在注释首行写明「不适用则删掉这一节，不留空」（照 3.3 的写法）；
- 05/06 保留 `{{}}` 占位，但占位注释补**成节标准**：一条业务支线／一个页面或场景／一条恢复路径＝一节。

### 1d 作业书 `phases/story-write.md:14` 修正反向劝阻句

现句「三级、四级标题按真实业务从属关系自然生成，**不为形式对称强行凑出来**」保留后半句语义，但补标准：
「什么算一节：一条业务支线、一个页面或场景、一个取舍主题、一条恢复路径，各自成节——
这不是形式对称，是读者按节回找的单位；把三条支线挤进一段散文才是错。」

---

## §2 D2 · 删六类决策骨架，review 立金样（对 04 件 R2）

### 2a 删除（带替代物的删，不是禁用）

- `story-build.mjs:70-72` `SCANNED_CATEGORIES` 常量——删；
- `init` 建骨架处（:271-276）——骨架简化为 `{ "decisions": [] }`；
- `check` ⑤「决策件六类都扫过」段（:839-850 含 none_reason 判）——整段删。
六类词表在作业书议题正面定义（2c）里作为「找议题时想一圈的六个方向」保留为一句话——
方向提示是给人的，骨架义务是给机器的；删的是后者。

### 2a′ review 渲染整体对齐金样形态（用户 2026-08-30 连续批注定型；**逐字节以 review 金样为准**，
以下是对金样形态的机制转写，两者冲突时金样赢）

- `decisions.json` 条目结构改为 `{ id, title, clarification, decider, status }`：
  `title`＝**陈述句**短标题（已定的陈述结论、待定的陈述事项，不用疑问反问）；
  `clarification`＝带小标题分段的 md 正文——已定条目三段（`**要定的事**`／`**根据**`／
  `**结论与影响**`），待定条目三段（`**要定的事**`／`**可选的做法**`（编号列出每个选项与影响）／
  `**建议**`）；`decider`＝请谁确认。旧 proposal/conclusion/rationale/impact/source 五字段**删除**——
  五字段拼 bullet 是表单腔的来源（renderHumanZone 注释裁七字段的判据同样适用于机器区）。
- `renderMachineZone` 重写，每议题块输出：
  `### <序>. <title>`（序号按渲染顺序生成）＋空行＋`<clarification>`＋空行＋`请 <decider> 确认。`。
- **人工区改造（三态勾选块退役）**：`HUMAN_ZONE_MARK` 由 `#### 审核结果（由评审人填写）`
  改为 `审核结果：`；`renderHumanZone` 输出＝`审核结果：`＋空行＋`<!-- decision: ID -->`；
  人工区（该行之后到 decision 标记）逐字节保留的机制不变，`extractHumanZone`／`findBlockRange`
  换锚即可。**评审人直接写具体内容，不勾选**。
- **文档级形态**：H1 后一条**可见引用块**提示怎么填（HTML 注释在预览里看不见，不许用它承载给人的
  提示；decision／freeform 机制锚仍用 HTML 注释）；`renderFreeformSection` 改为
  `## 计划外意见`（标题不带括号说明）＋一条可见引用块（编号列举、写清是什么影响哪里）＋
  空的 freeform-zone 标记对——**无「（暂无）」占位**。
- **表态完成度与回流同步**：`phases/spec.md:104`「每个议题块三态至少勾了一个」改为
  「每条『审核结果：』后有非空内容」；`rules/review_reflow.md` 的表态判读改为读该处自由文本
  （同意／要改什么／暂缓原因由语义判读，不再有勾选项）。grep 全仓同步其余提及三态勾选的描述。
- D3 决策单元（decisionUnits）的 text 取 `title＋clarification`，settled 落点仍是关键取舍。

### 2b `check` 决策判据（替代 ⑤，只判形式）

对 `decisions.json` 每条：`title`、`clarification`、`decider` 非空。
**明确不判议题数量下限，不判叙述内容**——数量与内容判据必然催生凑数与套话（Goodhart 前科累累）；
数量塌陷与叙述质量由 review 金样口径＋verifier 逐问（2d）＋人工评审接。
本轮实证病「来源填成分类名」随 source 字段删除而根除。

### 2c 作业书加「议题正面定义」（story-write.md 决策段与 spec.md「①之前」各一处，同一段话）

先给 review 的本质定义（用户 2026-08-30 裁定原话，也是 review 金样头部口径第 1 条，作业书原文引用）：
「review.md 就是进行一些决策的澄清，已决策的呈现结果；不确定、矛盾、错误的要人评估。」
已定条目呈现结果供过目，待评估条目把不确定与矛盾摆清楚——两类都是完整的自然句子，不是表单填充。

「story 里写了结论、而这个结论的依据不在材料里——工程自设的数值或方式、待联调确认的默认值、
用『以 X 近似』承载的能力、材料间冲突的定源——**每一条都是决策**：已定的登记 settled（正文落点是
关键取舍），未定的登记 open（去处是评审记录），没有第三种去处。**把材料里没有的选择直接写死进附录，
就是漏登记**。找议题时把六个方向想一圈：需求与范围、交互与界面、技术方案与依赖、约束规约命中项、
异常与风险、上线与协同。」

### 2c′ 同步旧描述

`phases/spec.md:34-35`「AI 只负责把开放点收齐并登记（问题、当前建议、依据、影响、来源、责任人）」
按新条目结构改写（标题、澄清叙述、请谁确认）；作业书里其他提及五字段的地方一并同步（grep 核对）。

### 2d verifier 逐问补一问（`phases/story-verify.md`）

「story 正文或附录里有没有『无上游依据』『待…确认』『以…近似』这类自设或待定表述？
每一处在 review（decisions.json）里有对应议题吗？没有的逐处点名。」——语义判交 verifier，不做正则。

### 2e review 金样（维护者已产出，执行者只消费）

`test/story/golden/review-金样-AR90006.md` ＋同文件头部效果口径。判据从它正推：
新增或修改的任何 review／decisions 判据先对它跑零 FAIL。
**单项回退线**：F3 收口 CLI 后 review 仍塌（同域议题为零，或澄清叙述空转成套话）→ 单退 F2-D3
独立派生通道（`decisionUnits` 调用点），恢复 decisions 直通 review 的 F 轮行为形态，其余 F3 改动不动，
并在交付报告登记触发事实。

---

## §3 D3 · 「注释即机制」终结：三条 lint ＋ 两处口径 ＋ 统稿物化（对 04 件 R3）

### 3a 三条形式 lint（落 `lint-rules.mjs`，接进 `story-build check` 既有 lint 链；每条先过两金样）

1. **图前承接＋图题形态**：`![` 引用行的上一非空行必须是正文段（不是标题、不是另一张图、不是表行）；
   alt 文本须为「图 N · 题名」形态。story 金样两图（:180/:192）全过；本轮 fA′（图后）／fB′（无编号 alt、连图）全命中。
2. **材料清单形态**：材料清单小节（附录合同 subsections 末项定位，沿 :988 找「规约」的同法找「材料」）内
   不得有表行（`|` 起始行），每个列表项须含 ≥1 个 markdown 链接 `[…](…)`。
3. **正文小节编号**：01–09 章内 `###` 首词须为 `<章号>.<序>` 形态（进 :745 编号形态既有通道，
   合同 `id_shapes` 加一条；附录章 A.–E. 判据既有，不动）。
**联动豁免**：`scanLocalPaths`（红线「不写仓内路径」）须对材料清单小节内的 markdown 链接目标豁免——
先读 `scanLocalPaths` 实现再落，豁免范围只限该小节的链接语法内，正文照拦。

### 3b 材料清单口径变更（用户 2026-08-30 裁定）

- 模板 E 节注释（story-template.md:207-215）：行形态改为
  `- **<材料类别>**——<来源：需求系统 <单号> ／ 随归档附件>《<文件名>》（<提供方>，<版本口径>）：<内容贡献>。原文：[<仓内相对路径>](<相对路径，相对 story.md 所在目录>)`；
  **:215「仓内路径仍在红线里，不写」句删除**，换成「原文链接是唯一允许仓内路径出现的位置」；
- 模板头部四条红线第 1 条同步补「（材料清单的原文链接除外）」；
- 作业书 story-write.md「归档件自包含」段红线 1 同步；
- 链接指向材料的既有落盘位置（`../RR/prd.md`、`../SR/design.md`、`../inbox/<原始文件>`、`../../assets/…`），
  形态参照 spec「参考资料」节。story 金样 E 节已按新口径升级（维护者完成），lint 2 以它为锚。

### 3c 图片路径口径（对 04 件 R3 的复制与捏造）

- 模板「全篇样式约定 · 图」条与作业书图条目各加一句：
  「图片引用它在仓内的**既有落盘路径**（如 assets 下），不复制文件、不改名、不在 AR/ 下造副本」；
- `check` 图落点判据扩一条：story 引用的每个图片路径须解析到**图片登记集**内的文件
  （先读 `source-units.mjs` 图片单元的登记方式：登记里有路径就直接比对；只有条数就先给图片单元补
  `path` 字段再比对——这是允许的机制补全，登记差异栏）；同一登记图片被两个不同引用路径指到 → 点名「同图两名」。
  反例夹具：复制 assets 图到 AR/ 改名引用 → 点名；同图两路径 → 点名。

### 3d 统稿物化（轻，防「跳过统稿零成本」）

统稿（③b）完成后在 `AR/story-src/copyedit.md` 落**恰好六行**——六项自查各一行「查了什么／改了几处或无」；
`check` 只判文件存在且行数为 6（±空行），**不判内容**（真实性归裁决面与抽样人核）。
作业书统稿段与 spec.md ③b 注释同步一句。**防苦役条款写进作业书：只写六行，写多不奖励。**

### 3e 模板瘦身

「全篇样式约定」节里已被 lint／合同接住的条目（编号体系、图题与图前承接、E 节形态）删掉——
判据在的地方注释是冗余；未被机器接住的（表前引导、单元格短项、小节命名）保留，它们归裁决面与统稿。
与合同 `questions` 重复的每章问题回显注释保留（渲染时它是每章输入之一），不属于本次瘦身对象。

---

## §4 D4 · 裁决面恢复（对 04 件 §八；F2 KG2-7 遗留的完成）

1. **双向校准**（新动作）：把 story 金样当被裁对象跑一遍 verdicts 逐问——应全绿；
   把本轮 fA′／fB′（04 件附行号）当被裁对象跑——应红出 04 件 §九里的统稿语义类残差
   （a2 附录条目重复／a4 一事两讲／b4 一事五讲／b6 尾部零散 至少各中其一）。
   金样绿不出来＝提问口径错；产物红不出来＝裁决面还是橡皮章。两向都过才继续。
   校准中允许改 `story-verify.md` 的提问表述（登记差异栏），不允许加机器判据。
2. **收口两道必出数**（口径沿 F2 裁定）：抽样人核 `verdict_audit.py` 10 行、种子写进报告；
   删事实对抗＝删「唯一承载」的事实每 Case 5 条、宿主侧质检定性、≥5 未讲清才算防线有效。
   两道数字进交付报告，缺一道即裁决面未恢复、KG3-7 FAIL。

---

## §5 实施顺序与每步收口

| 步 | 内容 | 判据 |
|---|---|---|
| 1 | §2a/2b 删六类＋字段语义判（先删后立，review 金样锚先跑） | KG3-2 |
| 2 | §1 四落点（合同→check→模板→作业书，顺序固定） | KG3-1 |
| 3 | §3a/3b/3c 三 lint＋两口径（每条 lint 落地即对两金样跑） | KG3-3/4/5 |
| 4 | §3d/3e 统稿物化＋模板瘦身；§2c/2d 作业书与 verifier | KG3-6/2 |
| 5 | §4-1 双向校准 | KG3-7 前半 |
| 6 | 离线收口：单测／失效形态台账（新反例夹具入列）／`--phase extensions`／退场 grep（`SCANNED_CATEGORIES`、`none_reason` 零命中）／KG2-9 五项＋G02 | KG3-8 |
| 7 | 在线收口：两 Case CLI 至 spec 闭环 → §4-2 两道出数 → 残差对 04 件 §九归类 → 02-交付报告 | KG3-7/9 |

每步末四件套：`node --check`／`pytest test/story/tests -q`／`check_failure_modes.py`／`harness --phase extensions`。

## §6 收敛纪律（F3 收口后生效）

残差归类：a=判据打架或 bug → 单点修；b=效果口径变更 → 金样先行再判据；c=可机检形式 → 入台账攒批；
d=统稿语义 → **不再机器化**，归裁决面／review 人工面／holdout。收口线＝重跑残差全部属 d 类。
此后 extension 判据冻结（bug-only），质量继续提升走三条外部通道：第二金样（car-key-sharing 实跑修订升格）、
holdout（用户新材料＋人填回稿）、AR90004 回归锚。

# F8 二次优化 · 验收判据（KG8S）

> 配套 `04-方案-二次优化.md`。承接 `03-判据-重写.md`（KG8R）——**KG8R 的全部判据继续有效**，
> 本文件只加本轮的判据、并**改正** KG8R-6.5 那条会误报的判定线。
>
> **怎么用这份表**：实施完成后**不凭记忆宣告**，照下表逐行填「达成 / 未达成 / 不适用 + 证据」。
> 每一格的「怎么验」都是可直接执行的命令或可直接打开的文件。
>
> **单向否决锚**：金样零 FAIL。任何一格与金样冲突，错的是判据。
>
> **基线**：`899bcc80`（F8R 最后一笔）。**F7 参照点**：`689607c7`。

---

## 零、全局硬门（每一步结束都要过）

| 编号 | 判什么 | 怎么验 | PASS 条件 |
|---|---|---|---|
| S-G1 | 单测全绿 | `python -m pytest test/story/tests tools/cli/tests -q` | 0 failed |
| S-G2 | 失效形态全绿 | `python test/story/scripts/check_failure_modes.py` | `FAIL 0` |
| S-G3 | 金样零 FAIL | `python -m pytest test/story/tests/test_golden_sample.py -q` | 0 failed |
| S-G4 | 扩展阶段绿 | `cd framework/harness && npx ts-node harness-runner.ts --phase extensions` | `Verdict: PASS` |
| S-G5 | 语法 | 每个改过的 `.mjs` 跑 `node --check` | 无报错 |
| S-G6 | **反例夹具全绿** | `python -m pytest test/story/tests/test_negative_guards.py -q` | 0 failed |
| S-G7 | framework 零改动 | `git diff 689607c7..HEAD --stat -- framework/` | 输出为空 |
| S-G8 | 金样未动 | `git diff 689607c7..HEAD --stat -- test/story/golden` | 输出为空 |
| S-G9 | **失败记录与既有方案未被改写** | 实施前存指纹 `sha256sum "…/F8-机制收束与内网适配/0[0-3]-*.md" > <scratch>/f8-plans.sha`；每步结束 `sha256sum -c` | 校验通过。**不能用 `git diff`**——`test/story/plan/` 在 `.gitignore:94` 里，git 对它恒为空，那样的判据永远 PASS |
| S-G10 | **装置侧不动** | `git diff 899bcc80..HEAD --stat -- test/story/scripts/` | 输出为空 |

---

## 一、S1 流程图退回 F7 机制

### 1.1 三处收回本身

| 编号 | 判什么 | 怎么验 | PASS 条件 |
|---|---|---|---|
| KG8S-1.1 | `IMAGE_KINDS` 只含图片 | `grep -n "IMAGE_KINDS = " doc/extensions/skills/story/scripts/story-build.mjs` | 值为 `new Set(['image'])`，**不含 `'diagram'`** |
| KG8S-1.2 | 注释写清为什么只有图片 | 读该常量的注释 | 含「内网只提图片 / 流程图没有硬塞压力」这一层意思；**不得只写「只给图片」而不给理由** |
| KG8S-1.3 | diagram 落点判恢复 | 读 `check` 里 `u.kind !== 'diagram'` 那段 | 条件为 `!rec?.at && !rec?.covered_by`，**不含 `material_only`** |
| KG8S-1.4 | 报错文案恢复 | 同上 | 文案为「在 story 里没有落点——图是读者最依赖的那部分，不能只在材料里有」 |
| KG8S-1.5 | **流程图数量判据恢复** | `grep -n "数量不该少于源" doc/extensions/skills/story/scripts/story-build.mjs` | 命中 1 处；条件为 `sourceForm.diagram && storyDiagrams < sourceForm.diagram` |
| KG8S-1.6 | **图片数量判据仍不在** | `grep -c "story 里只引用了" …/story-build.mjs` | `0` |
| KG8S-1.7 | 恢复的那条带理由 | 读它上方注释 | 写清「图片那条为什么删了、这条为什么留着」，含两类材料形态差异（30+ vs 三五张） |

### 1.2 行为正证（本轮回归的那份产物）

> 证据材料：`<scratch>/f8r-run-evidence/car-key-sharing/`（本轮实跑快照）。
> **必须用完整工作区**（含 `assets/`），否则会误报图片断链——本会话踩过一次。

| 编号 | 判什么 | 怎么验 | PASS 条件 |
|---|---|---|---|
| KG8S-1.8 | **回归被拦住** | 拿本轮 `car-key` 产物（4 张 diagram 分到业务流程、只画 1 张）跑 `check` | 报「材料里有 4 张图，story 里只有 1 张」 |
| KG8S-1.9 | 与 F7 读数一致 | 同一份产物用 `689607c7` 的脚本跑 | 两边报的是同一条、同样的数字 |
| KG8S-1.10 | **图片侧不受影响** | 同一份产物 | 图片相关零报错（4 张分配、4 张画了） |
| KG8S-1.11 | `auto-topup` 仍通过图相关判据 | 拿本轮 `auto-topup` 产物跑 `check` | 图片与流程图相关零报错（2/2、2/2 全对） |

### 1.3 反例夹具（新增 N10、N11）

| 编号 | 判什么 | 怎么验 | PASS 条件 |
|---|---|---|---|
| KG8S-1.12 | **N10：多张分到同一章、只画一张** | 夹具：4 个 diagram 单元 `at` 同一章，story 那章画 1 张 | `check` 报「数量不该少于源」；`assert_check_names` 点名该条 |
| KG8S-1.13 | **N11：diagram 不得用 `material_only`** | 夹具：diagram 单元标 `material_only` 并写理由 | `check` 报「不是图类单元」（因 `IMAGE_KINDS` 已排除 diagram） |
| KG8S-1.14 | 反例库编号连续 | `grep -oE "def test_(N[0-9]+)_" test/story/tests/test_negative_guards.py \| sort -u` | N1…N11 齐，无缺号 |
| KG8S-1.15 | 每条仍点名判据 | 读 N10 / N11 的断言 | 用 `assert_check_names(<特征串>)`；不允许只判退出码 |

### 1.4 图片侧不回退（守住 F8R 的成果）

| 编号 | 判什么 | 怎么验 | PASS 条件 |
|---|---|---|---|
| KG8S-1.16 | 图片 `material_only` 通道仍在 | 夹具：image 单元标 `material_only` + 理由 | 不报错 |
| KG8S-1.17 | 图片理由为空仍拦 | 夹具：image 单元 `material_only=""` | 报「没写理由」 |
| KG8S-1.18 | 30 图夹具零逼引 | 30 个 **image** 单元，2 张 `at`（画出）+ 28 张 `material_only` | 图片相关零报错 |
| KG8S-1.19 | 图片分了落点却没画仍拦 | 夹具：image `at` 某章，该章无图片引用 | 报「但那一章没有图片引用」 |
| KG8S-1.20 | `formShortfall` 两处调用不变 | `grep -c "formShortfall(doc.units" …/story-build.mjs` | `2`（`audit` 与 `check` 各一） |

---

## 二、S2 作者面一致性

| 编号 | 判什么 | 怎么验 | PASS 条件 |
|---|---|---|---|
| KG8S-2.1 | **矛盾句消失** | `grep -n "只有三种\|没有第四种" doc/extensions/skills/story/phases/story-write.md` | 零命中 |
| KG8S-2.2 | 表格与正文一致 | 读三态表 | 表格行数与正文说法一致；`material_only` 那一行注明**只给图片** |
| KG8S-2.3 | 两类规则分开写清 | 读该段 | 明写「图片有 `material_only`，流程图没有」；流程图的合法出路只有 `at` / `covered_by` |
| KG8S-2.4 | 无限定许可句仍不存在 | `grep -n "不必每张都进\|引多引少\|没有数量判据" …/story-write.md` | 零命中（F8R 已修，不许长回来） |
| KG8S-2.5 | 正向要求保留 | grep | 「图片一张不少、一图一引」与「给了 `at` 就等于承诺要画」两句都在 |
| KG8S-2.6 | **守卫存在** | `pytest -k author_page test/story/tests/test_negative_guards.py` | 有一条用例同时断言 2.1 / 2.4 / 2.5 |

---

## 三、S3 判定线改准

| 编号 | 判什么 | 怎么验 | PASS 条件 |
|---|---|---|---|
| KG8S-3.1 | **判定时机** | 读判定脚本 | `story.md` 章数 < 合同章数时报「渲染中（N/M 章）——写完才判」，**不出判定结论** |
| KG8S-3.2 | **跨轮总数比退场** | 读判定脚本 | 不再有「图片/流程图 < 上一轮」这一条 |
| KG8S-3.3 | 新判定线：欠画 | 读判定脚本 | 逐章逐类比对「分配数 vs 画出数」，差 > 0 即严重 |
| KG8S-3.4 | 新判定线：错用态 | 同上 | `material_only` 用在非图片单元 > 0 即严重 |
| KG8S-3.5 | 新判定线：无表态 | 同上 | 图类单元三态皆空 > 0 即严重 |
| KG8S-3.6 | 取材线保留 | 同上 | 来源单元 < 上一轮 90%、UX 单元由非 0 变 0，两条都在 |
| KG8S-3.7 | **对本轮数据不再误报** | 拿本轮 `auto-topup` 快照跑改后的判定 | **不报严重**（逐章核零欠账） |
| KG8S-3.8 | **对本轮数据仍报真问题** | 拿本轮 `car-key` 快照跑改后的判定 | **报严重**，且理由是「业务流程 欠图 3」，不是「流程图 1 < 6」 |

---

## 四、遗留项必须显式记录

> **本节已随十项扩容改写**：原先列在这里的 P3 报错聚合、P4 token 覆盖、D7/D8-1/D8-2/D9、
> 实跑终点，**本轮已分别由 S8 / S9 / S4 / S5 / S6 / S7 / S9 承接**，不再是遗留。
> 本节现在只剩「本轮确实不做」与「必须留痕」两类，与 §13.4 逐行对应。

| 编号 | 判什么 | 怎么验 | PASS 条件 |
|---|---|---|---|
| KG8S-4.1 | **通用形态判据** | 读 `06-交付报告.md` 遗留栏 | 写明：识别出「每章每类，画出数 ≥ 分配数」这一更通用形态；本轮不做的三条理由；若做须单开一轮配自己的放宽账与反例 |
| KG8S-4.2 | **引文五格判据本体** | 同上 | 写明：`00-方案` 边界②留 F9 单开轮，本轮不碰 |
| KG8S-4.3 | **两道硬指标** | 同上 | 写明：删事实 ≥5/5、抽样错判 ≤1 由维护者在 F7 台账上单独做，不在本轮 |
| KG8S-4.4 | **配置组要换回** | 同上 | 写明 `test.yaml` 首位当前是 GLM5.3-Flash 属**临时实验控制**，比完要换回，换回时 `test_cli_config_failover` 会红，那是设计如此 |
| KG8S-4.5 | **S1c 推翻了 `02` §4 的一行** | 读 `06-交付报告.md` | 写明：`02-方案-重写.md` §4 的「任何数量/比例/上限判据」那一行被本轮显式推翻，正确表述是「**图片**不设数量判据」；给出两类的对照依据；说明 `02` 文件本身不改的理由 |
| KG8S-4.6 | **剥括注后仍报缺的术语** | 同上 | 若还有，逐个列出并判「真缺 / 消歧参照」；后者记入遗留，等真实样本再谈，本轮不加豁免标记 |
| KG8S-4.7 | 遗留项不得默认消失 | 通读遗留栏 | 4.1–4.4、4.6 五条齐，每条带「为什么现在不做」与「什么时候该做」；与 §13.4 五行**逐行对得上** |
| KG8S-4.8 | **本轮承接项不得又滑回遗留** | 通读遗留栏 | 遗留栏里**不得出现** P3 / P4 / D7 / D8-1 / D8-2 / D9 / 实跑终点——它们本轮有落点，做不成要写「未达成 + 卡在哪」，不能改记「遗留」 |

## 五、范围纪律

| 编号 | 判什么 | 怎么验 | PASS 条件 |
|---|---|---|---|
| KG8S-5.1 | **只动了声明的文件** | `git diff 899bcc80..HEAD --name-only` | 只有下面这七个：`skills/story/scripts/story-build.mjs`（S1/S6/S8）、`skills/story/scripts/source-units.mjs`（S7）、`skills/story/phases/story-write.md`（S2）、`skills/story/rules/review_reflow.md`（S4）、`skills/story/contracts/story-chapters.json`（S5 的类别豁免标记）、`test/story/tests/test_negative_guards.py`（N10/N11 + S5/S6/S8 夹具）、`06-交付报告.md`。**多一个文件就要在交付报告里说明为什么** |
| KG8S-5.1b | 判定脚本与演练脚本不入库 | `git status --porcelain` | S3 的判定脚本、S10 的演练脚本都在 `<scratch>` / `scratch/`，工作区不因它们变脏 |
| KG8S-5.2 | **表格判据未动** | `git diff 899bcc80..HEAD --text -- …/story-build.mjs \| grep table` | 无 `table_row` / `form.table` 相关改动 |
| KG8S-5.3 | **token 守恒未动** | 同上 grep `appendixRowFor\|isEngineeringIdentifier` | 无改动 |
| KG8S-5.4 | **来源缺失判据未动** | 同上 grep `scanSources\|missingSourceLine` | 无改动 |
| KG8S-5.5 | **台账存在性未动** | 同上 grep `requireLedgers` | 无改动 |
| KG8S-5.6 | **推进契约未动** | `git diff 899bcc80..HEAD --stat -- doc/extensions/skills/story/SKILL.md doc/extensions/hooks/` | 输出为空 |
| KG8S-5.7 | **adapt 侧未动** | `git diff 899bcc80..HEAD --stat -- doc/extensions/skills/story-adaptation/` | 输出为空 |
| KG8S-5.8 | 净减 | `git diff 899bcc80..HEAD --stat -- …/story-build.mjs` | 机制代码行数不增（注释可增） |

---

## 六、S4 评审落点

| 编号 | 判什么 | 怎么验 | PASS 条件 |
|---|---|---|---|
| KG8S-6.1 | 冻结口径写明 | `grep -n "登记之后只读\|不回写冻结件" doc/extensions/skills/story/rules/review_reflow.md` | 两处都命中 |
| KG8S-6.2 | 回传落点写明 | grep 同文件 | 含「审核结果：」行、「其他意见」章、「不得整份覆盖」三处 |
| KG8S-6.3 | 归人所有范围收窄 | 读 §4 | 明写归人的是**人工区**（`审核结果：` 之后 + `freeform-zone` 内），结构归渲染器 |
| KG8S-6.4 | 不引入第二输入文件 | grep `review-annotated` | 零命中；`SKILL.md` 的「输入唯一就是 `AR/review.md`」仍在 |
| KG8S-6.5 | **机制零改动** | `git diff <批A末>..HEAD --stat -- doc/extensions/skills/story/scripts/review-render.mjs` | 输出为空 |

## 七、S5 review 禁用词按分区豁免（含放宽）

| 编号 | 判什么 | 怎么验 | PASS 条件 |
|---|---|---|---|
| KG8S-7.1 | **放宽账已填** | 读提交信息 | 含「它防什么 / 误伤面 / 谁来接」四列 |
| KG8S-7.2 | 人工区不判 | 夹具：`审核结果：` 后面写「文案回退为上一版」 | 不报禁用词 |
| KG8S-7.3 | freeform 区不判 | 夹具：`<!-- freeform-zone -->` 内写「灰度」 | 不报 |
| KG8S-7.4 | 上线/管控类议题豁免 | 夹具：`category=交付顺序与协同` 的议题澄清正文写「随版本放量」 | 不报 |
| KG8S-7.5 | **反例 N6 仍拦得住** | 夹具：非上线类议题的机器区正文写「本方案采用灰度发布」 | **仍报禁用词**。这一条是本项硬门，破了即本项设计错 |
| KG8S-7.6 | F7 两处实证不再 FAIL | 用 F7 那两处原文构造夹具 | 都不报 |
| KG8S-7.7 | story 侧不受影响 | 夹具：story 正文写「采用灰度发布」 | **仍 FAIL** |
| KG8S-7.8 | 豁免由数据驱动 | 读实现 | 豁免类别取自合同 `decision_categories`，脚本**不写死类别名** |

## 八、S6 术语剥括注

| 编号 | 判什么 | 怎么验 | PASS 条件 |
|---|---|---|---|
| KG8S-8.1 | 括注被剥掉 | 读 `missingGlossaryTerms` | 匹配前对术语单元格做括注剥离（中英文括号都认） |
| KG8S-8.2 | **本轮 12 个误报消失** | 拿本轮 `car-key` 快照（补裁决底稿后）跑 `check` | 「数字车钥匙（车钥匙）」等 12 个词**不再出现在报错里** |
| KG8S-8.3 | **真缺失仍报** | 夹具：术语表加一个 story 里确实没有的业务词 | **仍报**「在 story 里找不到」 |
| KG8S-8.4 | 主名为空时不误放 | 夹具：术语单元格形如「（仅括注）」 | 不因剥空而静默跳过——要么按原串判，要么报格式问题 |
| KG8S-8.5 | **不加豁免标记** | grep `消歧参照` | 零命中（那是 F8 首版的补丁形态，本轮不做） |
| KG8S-8.6 | 剥括注后仍缺的记入遗留 | 读交付报告 | 若还有报缺的词，逐个列出并判断是真缺还是消歧参照，后者记入遗留 |

## 九、S7 沉积清理

| 编号 | 判什么 | 怎么验 | PASS 条件 |
|---|---|---|---|
| KG8S-9.1 | 批次指涉零残留 | `grep -rnE "批次\s*[0-9]" doc/extensions --include=*.mjs --include=*.py --include=*.md` | 零命中 |
| KG8S-9.2 | grep 基数进报告 | 读交付报告 | 列出执行会话自己用的 grep 模式与逐条「保留/改写」清单 |
| KG8S-9.3 | 「为什么」保留 | 抽查改写过的每一处 | 事实与理由仍在，只去掉了轮次编号 |
| KG8S-9.4 | 不扩大范围 | `git diff` | 只改了 grep 命中的那几行，未顺手重写整段 |

## 十、S8 报错聚合（含放宽）

| 编号 | 判什么 | 怎么验 | PASS 条件 |
|---|---|---|---|
| KG8S-10.1 | **放宽账已填** | 读提交信息 | 四列齐 |
| KG8S-10.2 | 判定逻辑零变化 | 同一份坏产物，改前改后 | 报出的问题**集合逐条字符串相等** |
| KG8S-10.3 | 少量报错仍逐条 | 夹具制造 < 40 条 | 逐条编号列出，不聚合 |
| KG8S-10.4 | 洪水按类聚合 | 夹具制造 100+ 条 | 每类一行计数 + 前 5 条样本；终端输出封顶 |
| KG8S-10.5 | **明细无损** | 同上 | `check-detail.md` 里的条数 **== 终端报的总数** |
| KG8S-10.6 | 明细不入冻结件 | grep `STORY_SRC_FROZEN` | 不含 `check-detail` |
| KG8S-10.7 | **实跑测修复率** | 批 D 的实跑 | 交付报告给出「一轮修掉多少条」的读数；若聚合降低修复率，改为摘要＋全量并存并重跑 10.2–10.5 |

## 十一、S9 实跑跑到终点

| 编号 | 判什么 | 怎么验 | PASS 条件 |
|---|---|---|---|
| KG8S-11.1 | 模型变量隔离 | 读 `test.yaml` | 首位仍是 `volcengine/GLM5.3-Flash`（与上一轮成品同模型） |
| KG8S-11.2 | **auto-topup 到 spec 闭环** | 终态 + 产物 | `trace.json` / `summary.json` / 完成回执 / verifier 报告齐；`story_flow.py status` 显示 `story_written` |
| KG8S-11.3 | **car-key 到 plan** | 终态 + 产物 | `plan.md` / `contracts.yaml` 齐 |
| KG8S-11.4 | 归档链路 | `auto-topup` 走 `/story archive` | 登记成功或明确报「本地单无归档环节」 |
| KG8S-11.5 | **token 改道被验到** | 实跑中检索「标识符写进业务叙述句」的单元 | 出现即记录 check 是否两边都不报；**未出现就如实记「未覆盖」，不造场景** |
| KG8S-11.6 | 判定线在途监控 | 每次 `story.md` 十章齐时跑判定 | 未命中严重问题则继续；命中则停并报 |
| KG8S-11.7 | 停等读数 | runlog | 记录 story 段内白名单外问话次数（读数，不设阈值） |

## 十二、S10 adapt 端到端演练

| 编号 | 判什么 | 怎么验 | PASS 条件 |
|---|---|---|---|
| KG8S-12.1 | 演练真的跑了 | 读交付报告 | 附 `--scan` / `--check` 的实际输出片段 |
| KG8S-12.2 | **目标改过的知识不被覆盖** | 演练中改一个知识文件再升级 | 该文件内容**逐字未变** |
| KG8S-12.3 | **标记区外零 diff** | 演练前后对比入口文件 | 标记之外**零字节差异**，含 framework 生成的表格区 |
| KG8S-12.4 | 方案页有变更提案段 | 读演练产出的 `plan.md` | 第二段含「包内这份相对目标当前内容差在哪」 |
| KG8S-12.5 | 演练脚本不入库 | `git status --porcelain` | 演练脚本在 `scratch/`，工作区干净 |

---

## 十三、严格自检表（执行完逐行勾选，以事实确认，禁止凭记忆）

> **怎么用**：实施结束后**逐行执行「怎么验」那一列的命令**，把真实输出贴进「事实证据」列，
> 再勾。**不允许**：先勾再补证据、写「同上」、写「已验证」而无输出、凭印象判断。
> 任何一行拿不到证据 → 该行填「未达成」，并在交付报告里说明卡在哪。

| ☐ | 项 | 一句话诉求 | 判据编号 | 事实证据（贴真实输出 / 文件:行） |
|---|---|---|---|---|
| ☐ | **S1** 流程图退回 F7 | 4 张分到一章只画 1 张必须被拦 | KG8S-1.1–1.20 | |
| ☐ | **S2** 作者面一致 | 不再有「只有三种」与四行表并存 | KG8S-2.1–2.6 | |
| ☐ | **S3** 判定线改准 | 判「分了没画」而非跨轮比总数 | KG8S-3.1–3.8 | |
| ☐ | **S4** 评审落点 | 告诉作者评审结论写哪儿 | KG8S-6.1–6.5 | |
| ☐ | **S5** review 禁用词 | 人的表态不算产品承诺，真承诺仍拦 | KG8S-7.1–7.8 | |
| ☐ | **S6** 术语剥括注 | 12 个误报消失、真缺失仍报 | KG8S-8.1–8.6 | |
| ☐ | **S7** 沉积清理 | 批次指涉零残留 | KG8S-9.1–9.4 | |
| ☐ | **S8** 报错聚合 | 洪水可读且明细无损、实跑测修复率 | KG8S-10.1–10.7 | |
| ☐ | **S9** 实跑到终点 | 两个 case 各自到终点，四件套齐 | KG8S-11.1–11.7 | |
| ☐ | **S10** adapt 演练 | 知识不被覆盖、标记区外零 diff | KG8S-12.1–12.5 | |
| ☐ | **全局** 回归 | 单测 / 形态 / 金样 / extensions 全绿 | S-G1–S-G10 | |
| ☐ | **范围** 未越界 | 表格、token、来源、台账、契约、adapt 未被动 | KG8S-5.1–5.8 | |

### 13.1 原始诉求闭环表（对 `00-方案.md` §10 逐行）

| ☐ | `00-方案` §10 的行 | 解决于 | 事实证据 |
|---|---|---|---|
| ☐ | 内网 1 入口文件覆盖 | 已实施 `0e35623c` | |
| ☐ | 内网 1.1 framework 版本依赖 | 已实施 `0e35623c` | |
| ☐ | 内网 2 知识被包覆盖 | 已实施 `0e35623c` | |
| ☐ | 内网 3 README 双维护 | 已实施 `0e35623c` | |
| ☐ | 内网 4/5/9 停等询问 | 已实施 `0e35623c` | |
| ☐ | 内网 6 token 夹死 | 实现 `0e35623c` + **验证 S9** | |
| ☐ | 内网 7 30+ 图硬塞 | 已实施 `18776ee1` | |
| ☐ | 内网 8 删台账 | 已实施 `0e35623c` + **聚合 S8** | |
| ☐ | （demo 轮）评审落点 / 正本 | **S4** | |
| ☐ | （demo 轮）禁用词 / 术语误伤 | **S5 + S6** | |
| ☐ | 字符量激增 | **S7** | |

### 13.2 `00-方案.md` §10 五条验证

| ☐ | 验证项 | 解决于 | 事实证据 |
|---|---|---|---|
| ☐ | ① 离线全量（单测＋金样＋extensions） | S-G1–S-G4 | |
| ☐ | ② demo 两 case 回灌全绿 | KG8S-1.8–1.11 | |
| ☐ | ③ adapt 升级回归（知识不被覆盖、标记区外零 diff） | **S10** | |
| ☐ | ④ 30 图夹具 material_only 通道可用 | KG8S-1.18 | |
| ☐ | ⑤ 在线 CLI 一轮（终点沿用）看 D3 生效 | **S9** | |

### 13.3 本轮实测发现的问题

| ☐ | 问题 | 解决于 | 事实证据 |
|---|---|---|---|
| ☐ | P1 `car-key` 4 张流程图只画 1 张 | **S1** | |
| ☐ | P2 `story-write.md:63` 自相矛盾 | **S2** | |
| ☐ | P3 报错聚合漏做 | **S8** | |
| ☐ | P4 token 改道实跑无观测点 | **S9**（未出现则记未覆盖） | |
| ☐ | P5 判定线跨轮比总数、误报两次 | **S3** | |
| ☐ | S1c 与 `02` §4「任何数量判据」冲突 | `04` §4 显式推翻 | |
| ☐ | 术语判据把括注一起匹配（本轮新发现） | **S6** | |

### 13.4 仍然遗留（勾选＝确认它确实被记录在交付报告里，不是被解决）

| ☐ | 遗留项 | 为什么现在不做 | 什么时候该做 |
|---|---|---|---|
| ☐ | 通用形态判据（每章每类，画出数 ≥ 分配数） | 新判据形态，需新反例与新验证面；本轮主题是收回越界 | 单开一轮，配自己的放宽账与反例 |
| ☐ | 引文五格判据本体 | `00-方案` 边界②：留 F9 单开轮 | F9 |
| ☐ | 两道硬指标（删事实 ≥5/5、抽样错判 ≤1） | `00-方案` 边界①：由维护者在 F7 台账上单独做 | 维护者定 |
| ☐ | 配置组首位换回 `bailian-deepseek` | 当前是实验控制（隔离模型变量） | 本轮比完；换回时 `test_cli_config_failover` 会红，那是设计如此 |
| ☐ | 剥括注后仍报缺的术语（若有） | 需真实样本才谈得上消歧参照 | 有样本时 |

---

## 十四、交付报告的填法

`06-交付报告.md` 必须逐行覆盖本文件的**每一个编号**（S-G1…S-G10、KG8S-1.1…5.8），三列：

| 编号 | 达成 / 未达成 / 不适用 | 证据（命令输出、文件路径:行号、或截取的读数） |
|---|---|---|

**不允许**：只写「全部通过」、只给汇总数字、凭记忆描述。

**未达成的必须写清**：卡在哪、为什么、是判据错还是实现错、下一步归谁。

另须包含一节「本轮实跑结论」，把 `04-方案-二次优化.md` §1 的判定表原样带上，
并标注哪几行是本轮修好的、哪一行是本轮要修的。

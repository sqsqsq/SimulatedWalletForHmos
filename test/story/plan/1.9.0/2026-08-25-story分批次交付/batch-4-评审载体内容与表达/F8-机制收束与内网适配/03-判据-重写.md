# F8 重写 · 验收判据（KG8R）

> 配套 `02-方案-重写.md`。**取代** `01-判据.md`。
>
> **怎么用这份表**：实施完成后**不凭记忆宣告**，照下表逐行填「达成 / 未达成 / 证据」。
> 每一格的「怎么验」都是可直接执行的命令或可直接打开的文件，不需要再推导。
>
> **单向否决锚**：金样零 FAIL。任何一格与金样冲突，错的是判据。

---

## 零、全局硬门（每个批次结束都要过，不是只在最后过）

| 编号 | 判什么 | 怎么验 | PASS 条件 |
|---|---|---|---|
| G-1 | 单测全绿 | `python -m pytest test/story/tests tools/cli/tests -q` | 0 failed |
| G-2 | 失效形态全绿 | `python test/story/scripts/check_failure_modes.py` | `FAIL 0` |
| G-3 | 金样零 FAIL | `python -m pytest test/story/tests/test_golden_sample.py -q` | 0 failed |
| G-4 | 扩展阶段绿 | `cd framework/harness && npx ts-node harness-runner.ts --phase extensions` | `Verdict: PASS` |
| G-5 | 语法/编译 | `python -m compileall -q tools/cli test/story/scripts`；每个改过的 `.mjs` 跑 `node --check` | 无输出 / 无报错 |
| G-6 | **反例夹具全绿** | `python -m pytest test/story/tests/test_negative_guards.py -q` | 0 failed（xfail 计入通过） |
| G-7 | framework 零改动 | `git diff <基线>..HEAD --stat -- framework/` | 输出为空 |
| G-8a | 金样未动 | `git diff <基线>..HEAD --stat -- test/story/golden` | 输出为空 |
| G-8b | 失败记录未被改写 | 实施**前**先存指纹：`sha256sum "…/F8-机制收束与内网适配/0[01]-*.md" > <scratch>/f8-plan.sha`；每批次结束 `sha256sum -c` | 校验通过。**不能用 `git diff` 核这一条**——`test/story/plan/` 整个目录在 `.gitignore:94` 里，git 对它恒为空，那样的判据永远 PASS、永远没验 |

> **基线** = `cf29f1e3`（回退后 + 两处 harness 修复）。

---

## 一、批次 1 · 反例夹具库

| 编号 | 判什么 | 怎么验 | PASS 条件 |
|---|---|---|---|
| KG8R-1.1 | 九条反例齐备 | 打开 `test/story/tests/test_negative_guards.py` | 恰好 9 个用例，编号与方案 §3 批次 1 的 N1–N9 一一对应，每个用例的 docstring 写明「它防的是什么坏产物」 |
| KG8R-1.2 | 每条都真的拦得住 | `pytest test/story/tests/test_negative_guards.py -q` | N1–N7 PASS；N8、N9 标 `expectedFailure` 且注释写明「批次 2 / 批次 4 转正」 |
| KG8R-1.3 | 拦得住 ≠ 随便报个错 | 逐个用例读断言 | 每条都用 `assert_check_names(<该判据的特征串>)`，断言**点名了那一条**；不允许只断言 `returncode == 1` |
| KG8R-1.4 | 夹具不污染 | `pytest` 跑两遍 | 两遍结果一致；`git status --porcelain test/story/fixtures` 为空 |
| KG8R-1.5 | 零机制改动 | `git diff <基线>..HEAD --stat -- doc/extensions/` | 输出为空 |

---

## 二、批次 2 · 来源缺失要出声

| 编号 | 判什么 | 怎么验 | PASS 条件 |
|---|---|---|---|
| KG8R-2.1 | 合同声明了必备性 | 读 `contracts/story-chapters.json` 的 `sources` | 六个来源每个都有 `required` 布尔；`PRD/SE/SPEC/DESIGN` 为 `true`，`UPSTREAM/UX` 为 `false`；`UX` 带 `warn_if_siblings: "ux-reference"` |
| KG8R-2.2 | **缺失一律可见** | 跑 `init` | 「来源：」一行列出每个来源的单元数，缺的显示为 0 并标必备/可选；每个缺失各一条「记一笔」 |
| KG8R-2.3 | **缺失不拦（必备也不拦）** | 夹具（六个声明来源只有 `RR/prd.md`）跑 `init` / `check` | 退出码不因缺失变 1。**这一条是 2026-09-01 按实测改的**：原写「必备缺失＝BLOCKER」，实测那样会让 114 个单测与 23 条失效形态变红——它们都是最小夹具，一份材料测一条判据。新增义务与放宽同一把尺子，也要先量误伤面；而根因是**零信号**，让它可见即解决 |
| KG8R-2.4 | **本轮实况被拦住（N8 转正）** | 夹具：`ux-reference/` 下放 1 个 png、**不放 README.md**，跑 `init` | 退出码非 0，报错含「合同声明的来源 UX 不存在」与「导入做了一半」；`test_negative_guards.py::N8` 去掉 `expectedFailure` 后 PASS。**这一档没有误伤面**：目录里有文件是客观事实，索引缺席是客观缺陷 |
| KG8R-2.5 | 目录整个不存在时不误伤 | 夹具：没有 `ux-reference/` 目录 | 不拦，只记一笔 |
| KG8R-2.6 | 来源账可见 | 跑 `init` | stdout 有一行列出每个来源的单元数，缺失的显示为 0 并附原因 |
| KG8R-2.7 | 既有良品不被拦 | 上一轮两 case 成品回灌 `check` | 报错数不高于回退基线上的同一份读数 |

---

## 三、批次 3 · 图的守恒（核心）

### 3a 判据前移

| 编号 | 判什么 | 怎么验 | PASS 条件 |
|---|---|---|---|
| KG8R-3.1 | 两处共用一个判定函数 | grep `formShortfall` | 定义恰好 1 处；调用恰好 2 处（`cmdAudit` 与 `check ④`）；**不允许**两处各写一份形态判 |
| KG8R-3.2 | `audit` 报得出形态欠账 | 夹具：图片分了落点章、那一章没有 `![](`，跑 `audit` | stdout 含「形态欠账」段，逐条列出章名、欠什么、单元键与图题 |
| KG8R-3.3 | `audit` 不改退出码 | 同上 | `audit` 退出码仍为 0（它是分配工具，不是门禁） |
| KG8R-3.4 | 前移不改判定 | 同一份坏产物分别跑 `audit` 与 `check` | 两者报出的「欠什么」集合完全一致（逐单元键比对） |
| KG8R-3.5 | 只判已渲染章 | 夹具：story.md 只写了前两章 | `audit` 的形态欠账只涉及那两章，不报未渲染章 |

### 3b 逐章欠账清单

| 编号 | 判什么 | 怎么验 | PASS 条件 |
|---|---|---|---|
| KG8R-3.6 | 下一个待写章的清单可见 | 跑 `audit`（story.md 写了部分章） | stdout 含「下一个待写章「X」分到 N 条」，并**按 kind 分组逐条列出图与图片的单元键 + 图题** |
| KG8R-3.7 | 只列一章 | 同上 | 只列 `pending[0]`，不是全部十章 |
| KG8R-3.8 | 全部写完时不报 | story.md 十章齐 | 该段不输出，或输出「无待写章」 |
| KG8R-3.9 | 作者面写清清单从哪来 | 读 `story-write.md` 第二步输入表 | 第一行「分给本章的那些单元正文」后注明**跑 `audit` 取**；grep 得到 |
| KG8R-3.10 | **作者面不自相矛盾** | grep `story-write.md` | 「图片一张不少、一图一引」保留；**不存在**「图不必每张都进」「引多引少都不判」这类无限定许可句 |

### 3c 放宽（`material_only` + 删数量判据）

> **顺序硬门**：3a/3b 的判据全绿之前，不允许开始 3c。

| 编号 | 判什么 | 怎么验 | PASS 条件 |
|---|---|---|---|
| KG8R-3.11 | P1 表格已填 | 读提交信息 | 含「删/弱化 → 它防什么 → 误伤面 → 谁来接」四列，与方案 §3 3d 的表一致 |
| KG8R-3.12 | 30 图夹具零逼引 | 30 个 diagram 单元，2 个引用 + 28 个 `material_only`，跑 `check` | 图相关报错 0 条 |
| KG8R-3.13 | **同一夹具在 `audit` 时刻就报得出漏画** | 30 图夹具改为：2 个给 `at` 但不画，跑 `audit` | 「形态欠账」报出这 2 条——**证明 3a 接住了被删的数量判据** |
| KG8R-3.14 | 三态皆空照旧拦（N4 不变） | 图类单元不给任何态 | `check` 报「既没进 story，也没说明为什么不进」 |
| KG8R-3.15 | `material_only` 只给图类 | 文字单元标 `material_only` | 报「不是图类单元」 |
| KG8R-3.16 | `material_only` 要理由 | 图类单元标空理由 | 报「没写理由」 |
| KG8R-3.17 | **不判理由措辞** | 读 `story-build.mjs` | 不存在对 `material_only` 理由做内容/措辞匹配的判据（不得出现「点名承载者」这类检查） |
| KG8R-3.18 | 数量判据确已退场 | grep `story-build.mjs` | 「数量不该少于源」「story 里只引用了」零命中 |
| KG8R-3.19 | 无任何新数量判据 | grep + 读 diff | 全仓无图片/图的数量、比例、上下限判据 |
| KG8R-3.20 | **反例 N1/N2 改判** | 读 `test_negative_guards.py` | N1/N2 的**期望改为由 `audit` 的形态欠账拦**（不再由数量判据拦），断言相应改写并注明为什么改 |
| KG8R-3.21 | N3/N4/N5 不变 | `pytest test_negative_guards.py -q` | 仍 PASS，断言未改 |
| KG8R-3.22 | demo 两 case 回灌 | 上一轮成品跑 `check` | 不新增报错 |

---

## 四、批次 4 · 内网九条其余部分

### 4-1 / 1.1 入口文件与版本口径

| 编号 | 判什么 | 怎么验 | PASS 条件 |
|---|---|---|---|
| KG8R-4.1 | 标记区协议落地 | 读 `AGENTS.section.md` | 正文首尾为 `<!-- story-ext:begin -->` / `<!-- story-ext:end -->` |
| KG8R-4.2 | 本仓两份入口文件都带标记 | grep `AGENTS.md` `CLAUDE.md` | 两份都含成对标记 |
| KG8R-4.3 | 写入规则三态齐 | 读 `story-adaptation/SKILL.md` §6 | 表格三行：已有标记→只重写标记内；无标记但认得出旧段→原位包标记；认不出→节末追加＋方案页提示查重 |
| KG8R-4.4 | 校验分两种报法 | 跑 adapt-scan 正反夹具 | 无标记旧段→报「没有标记区」且提示原位包；整段缺失→报「未含扩展段」 |
| KG8R-4.5 | 标记齐备时不报 | 同上 | 该项不出现在报错里 |
| KG8R-4.6 | **render 表格区免疫** | 夹具：目标入口文件标记区外有 framework 生成的表格，跑一次写入 | 标记区外**零字节 diff** |
| KG8R-4.7 | 版本口径句在 SKILL | grep | 含「扩展对 framework 没有版本强依赖」；**不含**机制内部文件路径（否则 A01 会 FAIL） |

### 4-2 知识所有权（收紧项）

| 编号 | 判什么 | 怎么验 | PASS 条件 |
|---|---|---|---|
| KG8R-4.8 | 「同名换包版本」退场 | grep `story-adaptation/SKILL.md` | 「换成包的版本」零命中；含「变更提案」 |
| KG8R-4.9 | 校验不再排除包内同名 | grep `adapt-scan.mjs` | `pkgNames` 零命中 |
| KG8R-4.10 | **区分力正证** | 夹具：目标的规约文件被整份换成包版本，跑 `--check` | 报「知识内容丢失」 |
| KG8R-4.11 | **区分力反证** | 夹具：合并态（目标内容一字不丢、包新增列进来） | 不报「知识内容丢失」 |
| KG8R-4.12 | 索引 README 不在此列 | 读 SKILL §2 | 明写索引 README 按索引行合成，不受本条约束 |

### 4-3 README 纯索引

| 编号 | 判什么 | 怎么验 | PASS 条件 |
|---|---|---|---|
| KG8R-4.13 | 四章结构 | 读 `knowledge/design-patterns/README.md` 的 `##` 标题 | 恰好四章：适用单元 / 模式清单 / 候选登记规则 / 多模式组合 |
| KG8R-4.14 | 逐模式信号行退场 | grep | 「## 路由」「以上信号都不成立」以及逐模式的信号描述行零命中 |
| KG8R-4.15 | 通用规则移位不删义 | grep | 「零命中同样要举证」「不要复述判定语言」「信号来自业务流程本身」「spec 阶段候选判定与 plan 阶段选型的共同输入」四项均在新位置命中 |
| KG8R-4.16 | 指向真源 | grep | 含「读对应模式文件的上篇」 |
| KG8R-4.17 | frontmatter 声明为投影 | grep | 含「机器投影」与「改上篇时同步」 |
| KG8R-4.18 | 模式文件零改动 | `git diff <基线>..HEAD --stat -- doc/extensions/knowledge/design-patterns/decision-tree.md doc/extensions/knowledge/design-patterns/page-interaction.md` | 输出为空 |
| KG8R-4.19 | 上篇自带完整信号 | 逐个在册 `pattern_id` 打开模式文件 | 每份都有「上篇 · 适用与选型」「## 1. 解决什么问题」「## 2. 什么时候不该用」与 frontmatter 的 `applies_when` / `not_applies_when` |
| KG8R-4.20 | spec 模板指向更新 | grep `spec-sections.md` §11 注释 | 指向「模式文件上篇」，不再指向索引 |

### 4-4/5/9 推进契约

| 编号 | 判什么 | 怎么验 | PASS 条件 |
|---|---|---|---|
| KG8R-4.21 | 一节三段齐 | 读 `story/SKILL.md` | 有「## 推进契约」，含「授权分两层」「停等点白名单」「裁决衔接链」三个小节 |
| KG8R-4.22 | 两层边界写清 | 同上 | 明写 story 段内启动即授权；framework 阶段之间走 framework 认的途径、**不造第二套授权** |
| KG8R-4.23 | 白名单封闭且四条齐 | 同上 | 含「此外一律不问」；四条为材料缺口 / 范围变更 / 同一处连续 3 次 / 破坏性动作 |
| KG8R-4.24 | 内网三处问话被点名 | 同上 | 「要不要修」「下一步做什么」「进 harness 还是进 verifier」三处在文中被明确定性为「不是选择题」 |
| KG8R-4.25 | author.md 只引用不复述 | grep 六份 `hooks/*/author.md` | 每份都含指向 SKILL 推进契约的链接；**都不含**白名单四条的正文 |
| KG8R-4.26 | author.md 仍 ≤60 行 | `wc -l doc/extensions/hooks/*/author.md` | 每份 ≤60（A03 判据） |
| KG8R-4.27 | framework 零改动 | 见 G-7 | 空 |

### 4-6 token 改道（含放宽）

| 编号 | 判什么 | 怎么验 | PASS 条件 |
|---|---|---|---|
| KG8R-4.28 | P1 表格已填 | 读提交信息 | 四列齐 |
| KG8R-4.29 | 判定式一处 | grep `story-build.mjs` | 标识符形态正则定义恰好 1 处，`check ⑩` 与守恒同调 |
| KG8R-4.30 | **范围是附录对应行，不是整个附录** | 读实现 + 夹具 | 夹具：标识符出现在附录里**另一行**（不是该单元那一行）→ **仍报核不住**。若实现成「整个附录任意位置即可」，本条 FAIL |
| KG8R-4.31 | 围栏作用域对齐 ⑩ | 夹具：图类型词只出现在围栏内 | 不报核不住（⑩ 跳过围栏，守恒也跳过） |
| KG8R-4.32 | 内网 6 的死锁解开 | 夹具：含标识符的叙事单元，正文只写业务名、附录对应行有标识符 | `check` 零报错（既不违 ⑩ 也不违守恒） |
| KG8R-4.33 | **反例 N5 仍拦得住** | `pytest -k N5` | PASS，断言未放松 |
| KG8R-4.34 | 非标识符 token 零变化 | 既有单测 | 全绿 |

### 4-8 台账存在性

| 编号 | 判什么 | 怎么验 | PASS 条件 |
|---|---|---|---|
| KG8R-4.35 | 五件缺任一＝BLOCKER | 逐件删逐件跑 `check` | 五次都退出 1 且点名该文件 |
| KG8R-4.36 | 报错文案堵逃生口 | 同上 | 每条都含「补产出，不是把同伴文件删掉」语义 |
| KG8R-4.37 | 白名单两侧同源 | 单测 | mjs 侧五件的 basename 集合 == `story_flow.py` 的 `STORY_SRC_FROZEN`；**mjs 不另列一份文件名** |
| KG8R-4.38 | 齐备时能走到真判据 | 五件齐的良品 | `check` 通过 |
| KG8R-4.39 | **N9 转正** | 去掉 `expectedFailure` | PASS |

### 4-8b 报错聚合（含放宽，需实跑验证）

| 编号 | 判什么 | 怎么验 | PASS 条件 |
|---|---|---|---|
| KG8R-4.40 | 聚合无损 | 夹具制造 100+ 报错 | 明细文件里的条数 == 终端报的总数 |
| KG8R-4.41 | 明细落点正确 | 同上 | 写 `story-src/check-detail.md`；**不进** `STORY_SRC_FROZEN`、不进材料清单 |
| KG8R-4.42 | 少量报错不聚合 | 夹具制造 <40 条 | 逐条编号列出 |
| KG8R-4.43 | 判定逻辑零变化 | 同一份坏产物，聚合前后 | 报出的问题**集合**完全一致（逐条字符串比对） |
| KG8R-4.44 | **对修复率的影响已实测** | CLI 实跑（§5.3） | 交付报告里给出「一轮修掉多少条」的读数；若聚合明显降低修复率，实现改为「摘要 + 全量逐条」并存，并重跑本条 |

---

## 五、回归与纪律

| 编号 | 判什么 | 怎么验 | PASS 条件 |
|---|---|---|---|
| KG8R-5.1 | **正向回灌** | 上一轮两 case 成品跑 `check` | 不新增报错（拦了既有良品＝新判据错，回传不自行调） |
| KG8R-5.2 | **反向回灌** | 九条反例夹具 | 全部仍被拦且点名同一条判据。**任一条变 PASS 即本轮设计错** |
| KG8R-5.3 | 机制层零 Case 业务字面 | `check_failure_modes.py` 的 G02/P6 | PASS |
| KG8R-5.4 | 机制层无内网业务字面 | grep 「内网」「1.3.0」等 | 零命中（`constraints/dfx-baseline.md` 的既有占位除外） |
| KG8R-5.5 | 轮次指涉不新增 | grep 「批次\d」「F\d 实测」 | 不高于基线读数 |
| KG8R-5.6 | 每批一笔提交 | `git log` | 批次 1–4 各至少一笔；含放宽的批次，提交信息里有 P1 四列表格 |

---

## 六、CLI 实跑判据（用户指定口径）

| 编号 | 判什么 | 怎么验 | PASS 条件 |
|---|---|---|---|
| KG8R-6.1 | 模型变量已隔离 | 读 `config/test.yaml` | 第一个配置为 `volcengine-glm-flash`（与上一轮成品同一个），使机制成为唯一变量 |
| KG8R-6.2 | Case 与终点沿用 | `run_multi_case.py plan` | `auto-topup`→spec；`car-key-sharing`→plan |
| KG8R-6.3 | **story.md 一生成就判定** | 监视 `AR/story.md` 出现，立刻在副本上跑 `check` | 判定发生在 story.md 生成后，**不等整轮跑完** |
| KG8R-6.4 | 对比表逐行填 | 用 §5.3 那张表 | 图片 / 流程图 / 来源单元 / UX 单元 / 字节 / check 报错数六行全填，与金样及上一轮并列 |
| KG8R-6.5 | **严重问题的判定线** | 对比表 | 图片或流程图**低于上一轮**、或来源单元低于上一轮 10%、或 UX 单元为 0 → 判**严重问题** |
| KG8R-6.6 | **命中严重问题时的处置** | — | ① 立刻停掉两个 case；② 输出测试结果（对比表 + check 报错分类 + 差异归因）；③ **停下等人确认，不直接改** |
| KG8R-6.7 | 未命中时才继续 | — | 两个 case 跑到各自终点，产出四件套 |
| KG8R-6.8 | 停等观测 | runlog | story 段内白名单之外的问话次数（读数，不设阈值） |

---

## 七、交付报告的填法

`04-交付报告.md` 必须逐行覆盖本文件的**每一个编号**（G-1…G-8、KG8R-1.1…6.8），每行三列：

| 编号 | 达成 / 未达成 / 不适用 | 证据（命令输出、文件路径:行号、或截取的读数） |
|---|---|---|

**不允许**：只写「全部通过」、只给汇总数字、凭记忆描述。
**未达成的必须写清**：卡在哪、为什么、是判据错还是实现错、下一步归谁。

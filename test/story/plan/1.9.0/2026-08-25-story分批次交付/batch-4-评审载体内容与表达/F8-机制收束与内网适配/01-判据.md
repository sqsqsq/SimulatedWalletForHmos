# F8 · 验收判据（KG8）

> 与 `00-方案.md` 配套。金样零 FAIL 单向否决照旧；demo 两 case 现有产物在新判据下
> 应全部继续通过（放宽/改道不许拦既有良品）。

| id | 判什么 | PASS 条件 |
|---|---|---|
| KG8-1 adapt 所有权 | D1 三条 | ① 「同名换包版本」行为退场（SKILL 文本＋升级回归实证：目标改过的知识文件在升级后内容不变）；② 变更提案形态在方案页出现（夹具：包内知识与目标不同，升级方案列出 diff 供合并）；③ 入口文件标记区协议——升级回归中标记区外零 diff、render 表格区原样；④ `adapt/before.json` 残留删除（落点机制 adapt-scan.mjs:105 已在位，零代码改动）；⑤ 首次迁移路径：无标记旧段被识别并包上标记的夹具（识别到替换/识别不到追加＋提示，正反各一）；⑥ 兼容性口径句在 SKILL（grep） |
| KG8-2 README 纯索引 | D2 | ① README 四章结构、逐模式信号描述行退场（grep 旧句零命中）；② 通用规则（举证要求/输入源口径/双读者）移位后语义保留（grep 新位置命中）；③ 模式文件零改动（diff）；④ spec-sections §11 注释指向更新；⑤ 知识激活链回归绿 |
| KG8-3 推进契约 | D3 | ① SKILL「推进契约」节含双层授权边界（extension 段内有效/framework phase 间走 policy 认可途径）＋停等白名单＋裁决衔接链；② 各 author.md 无重复正文、只有引用句（grep）；③ 在线 CLI 一轮 story 段内无白名单外询问（runlog 核对：无「要不要进入 spec」「是否修复」「下一步做什么」类求确认——不依赖 verifier 可用性）；④ framework 零改动（diff） |
| KG8-4 token 改道 | D4 | ① 标识符形态判定与 check ⑩ 同一真源（单测锁两处一致）；② 夹具：含标识符的叙事单元——标识符在附录核到＝过、正文不出现标识符不再报 token 缺失；③ 非标识符 token 判据零变化（既有单测全绿）；④ demo 两 case 回灌零新增报错 |
| KG8-5 图类二态 | D5 | ① `material_only` 通道对 image 与 diagram 均可用（30 图夹具：分配后 check 零强制引用报错）；② referenced 既有引用类判据全保留（单测：落章同类形态/登记内/非副本/唯一/alt/diagram 可解析）；③ 两条「整篇形态数不降级」数量判据退场（story-build.mjs:1182-1193，grep＋单测退场记录）、不新增任何数量/比例判据；④ diagram 落点判改「无 at、无 covered_by、未标 material_only 才报」（正反单测）；⑤ story-verify 新问句落地且不带样例；⑥ demo 两 case（图全 referenced）回灌零变化 |
| KG8-6 存在性与报错形态 | D6 | ① 五件缺任一＝BLOCKER（正反夹具；白名单引用 `STORY_SRC_FROZEN` 常量不另列文件名）；② 报错文案含「补产出不是删文件」语义；③ 聚合输出：夹具制造 100+ 报错，终端输出封顶、明细落 `check-detail.md`、判定逻辑 diff 为零（单测锁）；④ check-detail.md 不入冻结件与材料清单（判据核对） |
| KG8-7 评审落点 | D7 | ① `review_reflow.md` 含「decisions.json 登记后只读、状态变化只记 disposition」句（grep）；② 回传写入形态口径落地：意见落「审核结果：」行与计划外意见区、整文批注版摘回逐条、结构保持三级（grep＋与 SKILL:98「唯一输入」无冲突核对）；③ 机制文件（渲染器/冻结判）零改动（diff） |
| KG8-8 语境两小件 | D8 | ① 分治生效：review.md 禁用词命中＝WARN 记录不拦（F7 两处实证样本在新链下不再 FAIL）、story.md 照旧 FAIL（正反单测各一）；② 消歧参照物不参与 story 存在判（夹具：参照物术语行不报缺失）；③ 真·交付机制语境仍拦得住——story 正文「采用灰度发布」夹具照常 FAIL（单测，不依赖 verifier） |
| KG8-9 回归与纪律 | 全局 | ① 金样台账零 FAIL；② demo 两 case 现有产物回灌全绿（新判据拦既有良品＝新判据错，回传不自调）；③ 单测/`--phase extensions` 全绿；④ 机制层零 Case 与内网业务字面（KG2-9 五项＋G02）；⑤ 金样/方案/判据未动；差异栏与四件套记录 |
| KG8-10 沉积清理 | D9 | ① 作者面合并清单进差异栏（每项注明「原两处→现一处」）；② 轮次/批次指涉零残留——以执行会话自己的 grep 模式（至少覆盖「批次\d」「F\d」「上一轮/那一轮/上一版」）＋逐条保留/改写清单进差异栏为准，「为什么」语义保留抽查；③ `adapt/before.json` 已删（KG8-1④）、`__pycache__` 在打包排除清单（核对或补） |

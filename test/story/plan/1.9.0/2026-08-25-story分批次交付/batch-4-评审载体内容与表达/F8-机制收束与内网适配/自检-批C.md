# 批 C 自检报告 · S6 术语剥括注 / S3 判定线改准

> 判据出处：`08-判据-三次.md` §零、§六（KG8T-6.x）、§三（KG8T-3.x）。

---

## 一、全局硬门

| 编号 | 结论 | 事实证据 |
|---|---|---|
| G1 单测 | **达成** | `pytest test/story/tests -q` → `436 passed, 38 subtests passed in 42.77s`（比批 B 多 4 条，即本批新增的术语夹具；第 5 条是 `test_N7` 原有的） |
| G2 失效形态 | **达成** | `形态 73 条：FAIL 0，SKIP(能力未建) 0，PASS 73` |
| G3 金样 | **达成** | 含在 G1 内 |
| G4 扩展阶段 | **达成** | `✅ 脚本 Harness 检查通过` |
| G5 语法 | **达成** | `node --check story-build.mjs` 无输出 |
| G6 反例夹具 | **达成** | 含在 G1 内 |
| G7–G10 | **达成** | 与批 A/B 同 |

---

## 二、S6 术语剥括注（KG8T-6.1–6.8）

| 编号 | 结论 | 事实证据 |
|---|---|---|
| 6.1 括注被剥掉 | **达成** | 新增 `glossaryMainName(cell)`：`String(cell ?? '').replace(/[（(][^）)]*[）)]/g, '').trim()`，中英文括号都认；`missingGlossaryTerms` 末行改为 `.filter(t => t && !storyText.includes(glossaryMainName(t)))` |
| 6.2 金样零 FAIL | **达成** | `test_golden_sample.py` 仍全绿（含在 436 内） |
| 6.3 **真缺失仍报** | **达成** | `test_a_genuinely_absent_term_is_still_reported` PASS（术语「甲乙业务词（某个别名）」主名不在 story，照报） |
| 6.4 主名相同不误合 | **改为「原串仍出现在报错里」** | 实现按行独立判、不做去重，所以主名相同的两行各判各的、报错各列一次。用 `test_the_reported_text_is_the_original_cell` 锁住「核主名、报原单元格」这一半：`assertIn("甲乙业务词（某个别名）", out)` PASS |
| 6.5 剥空不静默 | **达成** | `test_a_cell_that_is_only_a_parenthetical_is_not_skipped` PASS。实现里写了 `return bare \|\| String(cell ?? '').trim()`——整格就是一个括注时按原串判 |
| 6.6 **不加豁免标记** | **达成** | `grep -rn "消歧参照" doc/extensions` → 零命中 |
| 6.7 回归锚 | **部分达成：12 → 3** | 见下 |
| 6.8 剩余缺词记入遗留 | **达成** | 见下三条，已判并记 |

### 6.7 的真实读数 —— 12 个里消掉 9 个，不是 12 个

拿 `car-key-sharing` 快照跑 `check`：

**改前**（一条报错列 12 个词）：

> 数字车钥匙（车钥匙）、车钥匙分享（分享）、钥匙卡片（卡片详情）、配额（剩余可分享数）、权限档（全功能/仅解闭锁）、分享状态六态（待接受/生效/已到期/已作废/撤销中/已撤销）、撤销（撤销中）、宽限窗（24 小时）、分享草稿（key_sharing_draft）、推送通知、登录 / 未登录、脱敏（打码）

**改后**（同一条报错剩 3 个词）：

> 分享状态六态（待接受/生效/已到期/已作废/撤销中/已撤销）、推送通知、登录 / 未登录

**`07-方案-三次.md` §3 S6 里写的「一次报 12 个词全是这个成因」不准确——9 个是，3 个不是。**
这一条在交付报告里要更正。

### 剩下 3 个逐个核过（`grep` 原文）

| 词 | 主名在 story? | 真实成因 | 判定 |
|---|---|---|---|
| 分享状态六态（…） | `分享状态六态` ❌、`分享状态` ❌、`六态` ✅ | story 写的是「六态」，术语表写的是「分享状态六态」 | **真缺**（措辞不同，不是括注） |
| 推送通知 | ❌（`推送` ✅、`通知` ✅） | 复合词被拆开写 | **真缺**（同上） |
| 登录 / 未登录 | ❌（`登录` ✅、`未登录` ✅） | 术语格里的 ` / ` 是两个词的并列写法 | **真缺**（同上） |

三条同属一类：**术语被换了措辞或拆开写**。这与括注是两件事，本轮不动它——
S6 的范围就是括注。记入遗留，理由与观察一并写进交付报告：

> 判据文案自己写着「可整合、可改序、**可换措辞**，但不能少」，而实现是精确子串匹配。
> 这两句直接冲突。要不要放宽到「主名的每个字都在同一章出现」之类，需要真实样本
> 再谈，不在本轮。

---

## 三、S3 判定线改准（KG8T-3.1–3.8）

判定脚本在 `<scratch>/judge.py`，**不入库**。

| 编号 | 结论 | 事实证据 |
|---|---|---|
| 3.1 判定时机 | **达成** | 章数 < 合同章数时输出「渲染中（N/M 章，X 字节）——写完才判」并 `continue`，不出判定结论 |
| 3.2 **跨轮总数比退场** | **达成** | 原来的 `for k in ("图片","流程图"): if m[k] < base[k]` 整段删除。`BASELINE` 里的图与图片读数只用于报表打印，注释里写明「**只用于取材两条线**，不再拿它比图与图片的总数」 |
| 3.3 **不重建第二个真源** | **达成** | 新增 `severe_from_check(out)`，形态结论**逐条来自 `story-build.mjs check` 的输出**（`SEVERE_PATTERNS` 六个特征串全部抄自判据文案）；脚本自己不逐章数图 |
| 3.4 取材线保留 | **达成** | 来源单元 < 上一轮 90%、UX 由非 0 变 0，两条都在，并注明「check 管不到，它判的是 story 写得怎么样，不是材料取没取进来」 |
| 3.5 对金样不报 | **达成，但口径要写明** | `run_check` 跑金样 → `台账缺 5 件`（金样是离线仲裁锚，没有 `story-src/`），`severe_from_check` → `[]`。**这条判据在金样上是空过的**，真正的正推证据在批 A 的独立夹具 |
| 3.6 对好产物不报 | **达成** | `auto-topup` 快照 → 严重项 **0** |
| 3.7 对坏产物报 | **达成** | `car-key-sharing` 快照 → 严重项 **1**：`分了落点却没画够（流程图）：89. 材料里分到「业务流程」的图有 4 张，那一章只画了 1 张` |
| 3.8 脚本不入库 | **达成** | `judge.py` 在 `<scratch>`，`git status --porcelain` 不含它 |

---

## 四、本批改了哪些文件

| 文件 | 项 |
|---|---|
| `doc/extensions/skills/story/scripts/story-build.mjs` | S6（新增 `glossaryMainName`，`missingGlossaryTerms` 改一行） |
| `test/story/tests/test_negative_guards.py` | S6 四条夹具 |
| `<scratch>/judge.py` | S3（不入库） |

---

## 五、本批要带进交付报告的两件事

1. **`07-方案-三次.md` §3 S6 的「12 个全是括注成因」不准确**：实测 9 个是、3 个不是。
   方案文件不改（它是本轮基线），更正写进交付报告与本自检报告。
2. **KG8T-3.5「对金样不报」是空过的**：金样没有五件台账，`check` 在台账那一关就停了，
   形态判据根本没跑到。与批 A 的 KG8T-1.2 是同一个成因（金样是离线仲裁锚）。
   S1/S3 的正推证据实际由批 A 的六条独立夹具承担。

# 批次 4 · 评审载体的内容与表达 · 判据（先于实现）

> 判据只落结果；四轴（不丢 / 不重 / 可读 / 形态）在同一份表里，修一轴不得退另一轴；台账全量回归。
> 2026-08-29 用户裁定两条：**A/B 准入试验取消**（KV-4 改为收口对照）；**句长红线去掉**（KV-1 / KV-3）。

| id | 判据 | 口径 | checker |
|---|---|---|---|
| KV-0 | 内容基线冻结 | `test/story/fixtures/content-baseline/` 三份 story + 同轮材料，指纹记入冻结清单；对照永远是它，不是上一子批 | 人核 + 指纹 |
| KV-1 | 合同 4.0 | 十章、≈33 问、附录六表头、`readability`（无 `sentence_max_chars`）/ `language_redline` 数据在合同；`token_exclusions` 不存在；模板每章注释 = questions；`form: table` 只在术语与异常 | 单测 |
| KV-2 | 附录与语言红线 | AR90004 副本：主叙事零仓内标识 / 规约编号 / 检索措辞 / 来源括注 / 文档坐标；附录表含全部 §9 标识；`check ⑦` 读附录·规约判定 | 单测 + 台账 |
| KV-3 | 形态与长文 | 台账正反夹具：流程图压成箭头文字 FAIL；术语成列表 FAIL；400 字散文段 FAIL；**单行 table_row 的章不 FAIL**（与「一行不成表」对齐）；相同段落 FAIL；合规 story 全 PASS；**无表的章不 FAIL（不设配额）** | 台账 |
| KV-4 | 收口对照 | 每子批出六项数字表，对照物是**内容基线**不是批次 3 产物；子批 A 做完 ① 内容不丢或 ② 形态守恒劣于基线 → 停下复盘（A/B 准入试验按 2026-08-29 裁定取消，理由见 00 §7.1） | 实跑 + 人核 |
| KV-5 | verifier 三表 | 逐单元 / 逐问 / 逐章三表齐、引文可核（≥12 字、在该章、非回声）；两 Case 各删 5 事实 → ≥5 未讲清并点名 | 单测 + 实跑 |
| KV-6 | review 与非 story | S4-8 既有判据全过；W01 非 story 隐形台账（只有 spec.md 的 feature 跑 spec harness 不被 story 判据拦） | 台账 |
| KV-7 | 不丢 / 不重量化 | 基线单元落点 100%（机器或 verifier）、缺失点名；人核 ≥90%；相同段落 0；holdout 同口径 | 实跑 + 人核 |
| KV-8 | 纪律 | 执行者不回写本方案 / 本判据 / 01 §5；偏差进交付报告差异栏；行数如实写；每子批收口四轴同表并列 | 维护者 |

夹具（新增）：`fixtures/failure-modes/S01-diagram-degraded/{bad,good}`（流程图→箭头文字）、`S02-terms-not-table/{bad,good}`、`S03-long-paragraph/{bad,good}`、`S04-duplicate-paragraph/{bad,good}`、`S05-main-text-identifier/{bad,good}`（主叙事出现类名 / 规约编号 / 检索措辞）、`W01-non-story-invisible/{bad,good}`；`content-baseline/` 三份。

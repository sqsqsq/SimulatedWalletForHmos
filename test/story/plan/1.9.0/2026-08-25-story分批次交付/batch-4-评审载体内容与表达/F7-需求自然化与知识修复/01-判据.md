# F7 · 验收判据（KG7）

> 与 `00-方案.md` 配套。金样不在本轮改动面（story/review 形态不涉），台账应零波动。

| id | 判什么 | PASS 条件 |
|---|---|---|
| KG7-1 需求自然性 | 四场景（1a/1b/1c＋1d 不动核验） | ① 每个新场景的产品理由**写在材料内**且拿掉测试动机后独立成立（人工通读记录进报告）；② 材料通读零机制词、零模式暗示词（「多步/分支/编排/交互链/状态机”等知识文件用语零出现——业务语言的「分支」如流程描述允许，判的是结构暗示语气，人工核对）；③ docx 经既有脚本再生、roundtrip 自验过；④ 1d：car 的流程叙述未被削弱（diff 核对） |
| KG7-2 知识修复 | 2a/2b 两条 | ① README 零命中行为举证要求形态、旧具体描述句退场（grep）；② 读者标注双读者；③ 输入源句在 README 与 spec-sections §11 注释两处逐字一致（单测或 grep 锁）；④ 知识文件结构/目录/frontmatter 零变化（diff 实证——adapt 零同步的前提） |
| KG7-3 判断链 | 2c/2d 两条 | ① verifier §11 专项问落地且为类别语言；② DESIGN_HEADING_RE 退场、结构判正反单测各一、匹配不上报错不静默 |
| KG7-4 adapt 核对 | 用户提醒项 | story-adaptation SKILL §2 对知识形态的描述逐条对照本轮改动——触到即同步并登记差异栏，未触到登记「零同步」结论 |
| KG7-5 离线纪律 | 四件套＋过拟合＋台账 | 单测/台账/extensions 全绿；金样台账零波动；KG2-9 五项＋G02；机制层零 Case 字面（新增材料内容不属机制层） |
| KG7-6 在线观测 | §11 自然命中 | ① 两 Case 至既定终点；② §11 四个信号的判定结果与举证质量原文进报告——**命中不是判据、不达不改判据不改材料，贴原文回传**；③ ISSUE-206 的 plan 选型与既有交叉判生效核验；④ **反向锁：全仓零「§11 须命中 N 条」类判据（grep＋单测）** |
| KG7-8 C 线三件 | --preview／audit 打印／登记清理 | ① preview 输出与落盘版一致（抽段比对）且零落盘副作用；② audit 判定逻辑 diff 为零（单测锁）、分布与明细可见；③ 清理夹具正反各一（杂件被清＋五件无恙）、清单打印、指纹仅含五件 |
| KG7-7 纪律 | 边界与差异栏 | 金样/方案/判据未动；可动面外零改动（含其余 cases 文件、装置）；差异栏（含 1e 新增立场依据的登记，如有）；四件套记录 |

# 批次 4 · F 修复 · 方案（从金样正推 · 落点级）

> 自足件，2026-08-30 定稿（v2：改动落到文件 / 函数 / 数据级；v1 的「未知子命令乱试」经查证撤销——`build` 是渲染 review 的正当命令）。**本文不留待确认项**。
> 效果定义：[test/story/golden/story-金样-AR90004.md](../../../golden/story-金样-AR90004.md)（**已经用户定稿，执行者无权改动**；口径见 [test/story/golden/README.md](../../../golden/README.md)）。判据：[01-判据.md](01-判据.md)。
> 基线 `7aa0799d`。提交前缀 `ext.b4F-step<N>`。不改 framework；不回写本方案 / 01 §5 / 金样；偏差进 02-交付报告「差异」栏。

## 0. 一句话

**金样入库为判据的仲裁锚；守恒收缩到与改写相容的硬事实；分配簿记机器化；附录只认约定结构；报错文案指向落点形态；review 按 B2-9 原文修。**每一项都在删矛盾或删苦役，不加围栏。

## 1. 实证（执行者不必复查对话，全部在此）

| # | 事实 | 数字 / 出处 |
|---|---|---|
| E1 | 分配塌缩 | 首跑 audit 267 单元，224 条（84%）`at: 附录`；背景/术语/范围/交付 0 条 |
| E2 | 倾倒区 | 产物 story.md 出现合同外小节「机器核对索引」```text 块 255 行占 58%；`Codex`、`WalletMain`、`CardRepository.ets` 进归档件 |
| E3 | 判据互斥 | 要求人读改写又要求逐字命中；`by: machine` 246/267 在倾倒区命中，verifier 仅 14 条可裁 |
| E4 | **门禁教模型倾倒** | `story-build.mjs:548` 报错「落点标在『附录』，但那一章里找不到：WalletMain」——模型逐轮照做，失败数 35→11→9→1→6→18（回升 = 修守恒破红线） |
| E5 | 效率 | check 被跑 23 次、audit 12 次；story 成文链 24 分钟；全轮 ≥89 分钟未闭环 spec |
| E6 | 逐字口径冤枉好文档 | 金样对内容基线跑 `baseline_coverage.py`：89 条字面 miss，人核**语义全有落点** |
| E7 | B2-9 丢失 | `review-render.mjs:49-71` 仍渲染 B2-9 点名要删的七字段；原文在 `../作废-第三版/`（本文 D6 已整段带入） |
| E8 | 四轮同模式 | 1.0 重复 / 批次 2 门禁回环 / 批次 3 泄漏+橡皮章 / 批次 4 倾倒——每轮机器判据全绿、产物全坏 |

## 2. 原则

| # | 原则 | 实证 |
|---|---|---|
| P1 | 金样是判据的仲裁锚：任何判据若拦金样，错的是判据；金样没有的要求不设判据 | E8 |
| P2 | 判据不得惩罚改写：机器只守改写时本该逐字保留的硬事实；中文叙事的语义守恒归裁决者 + 对抗测试 + 人核 | E3、E6 |
| P3 | 模型只做产生叙事价值的工作：落点归位能机器做的全部机器做 | E1 |
| P4 | 报错文案是作者面的一部分：修法指向落点形态，点名字面等于教模型抄字面 | E4 |
| P5 | 方案条目自足，不留裸指针 | E7 |
| P6 | **金样给的是形，不是内容**：金样与 Case 业务字面不进机制层（代码、合同、作业书、模板、报错文案）；「形」经模板与作业书的中性形态描述传递；判据只写形态规则与合同数据，不 diff 金样或夹具字面——防止机制与金样 / 测试用例过拟合 | 台账 M01/M02 既有纪律；金样在宿主仓 fixtures，被测工作区不可见 |
| P7 | **知识链不动**：knowledge 单元、⑦ 规约判定表、spec §10/§11、plan 义务、coding 探针、verifier 引文核实、W01 全部保持现状——本轮任何改动不得触碰 | 批次 3 已验证成立的链 |

## 3. 改动（每项：现状定位 → 目标形态 → 自验）

**这是正向重写，不是逐行删改**：受影响的函数（`autoPlace`、`cmdAudit` 落点段、check ② 守恒段、`renderHumanZone`）按各 D 的「目标形态」**整段重写**；行号只用于定位现状，不是「删这几行」的指令。每个 D 末尾的「自验」是执行者判断「改对了没有」的即时手段，与 01-判据 的收口判据分层：自验步步跑，判据收口跑。

### 现状速查（涉改文件与关键位置，基于 `7aa0799d` 实读）

| 文件 | 行数 | 本方案关心的位置 |
|---|---|---|
| `doc/extensions/skills/story/scripts/story-build.mjs` | 979 | `COMMANDS:44`；`autoPlace:305-323`（token 命中 + **片段兜底 317-321**）；`contentFragments:331-337`（≥8 字片段）；`cmdAudit:339-405`；check ② `468-553`（**by:machine 无 token 片段核 535-538、报错文案 548**）；check ⑥ 663；⑦ 784；⑩ 863；⑪ 902；`cmdBuild:939`（渲染 review） |
| `doc/extensions/skills/story/scripts/source-units.mjs` | 267 | 九类 kind（注释 22-29）；`tokensOf:95-114`（反引号/ASCII 标识 ≥4/数值量纲/图片名/URL/keep 编号）；`enumerateUnits:127-244`；`linkDuplicates:252` |
| `doc/extensions/skills/story/scripts/review-render.mjs` | 125 | `renderHumanZone:49-71`（**七字段在 56-67**）；`HUMAN_ZONE_MARK:15` |
| `doc/extensions/skills/story/contracts/story-chapters.json` | 201 | `sources`；`machine_facing`（unit_kinds=["code"]、table_columns 六列）；`id_shapes`（keep=AC 形态 / drop=S\d 等）；附录章 `10-appendix` 已有 `subsections: ["接口","数据、配置与事件","工程范围","规约判定","追溯","来源"]`；`language_redline`；`readability`；`verdicts` |
| `doc/extensions/skills/story/phases/story-write.md` | — | 分配/渲染两段作业书；「什么内容用什么形态」表 |
| `test/story/scripts/check_failure_modes.py` + `regression/failure-modes.yaml` | — | 台账入口 |

### D1 · 金样入库为锚（P1）

- **改法**：金样 story + 2 图 + 该轮四份材料（从 `output/story/story-suite-20260829-batch4` 的 traffic-card-loss 需求目录取 RR/SR/AR/ux-reference）复制进 `test/story/fixtures/golden/AR90004/`；新增 `test/story/tests/test_golden_sample.py`（照 `test_content_baseline.py` 的指纹模式：文件清单 + sha256 + 行数/章数/表行数/图数）。
- 台账新增常驻正例 `G01-golden-zero-fail`：对金样跑 `lint-rules.mjs` 全部扫描函数 + `story-build check` 中**不依赖 audit/单元清单的判项**（① 章标题顺序、③ 编号形态、④ 形态可解析、⑨ 四红线、⑩ 语言红线、⑪ 可读性）——零 FAIL。实现建议：check 加 `--story <path> --offline` 模式只跑上述判项（现状 `createContext` 绑定 feature 目录，金样没有 audit）。**此后每次改判据先跑这一行，拦金样即判据改错。**
- 注意：金样章标题带编号（`## 1. 背景`），check ① 现按 `## 标题` 精确对合同 `title`——合同 `chapters[].title` 同步为带编号形态，或 ① 的标题匹配放宽为「编号前缀可选」（`/^(\d+\.\s*)?背景$/`）。**选后者**：编号是表达形式，合同存业务名。`storySections:224` 的切节不受影响（按 `## ` 切）。同理 ⑦ 的附录小节名匹配（`subsectionText:283`）对 `### A. 接口` 需剥 `A. ` 前缀。

**自验**：① `test_golden_sample.py` 指纹过；② `story-build check --offline --story test/story/golden/story-金样-AR90004.md` 输出零 FAIL（台账 G01 同口径）；③ 测试在临时目录复制金样并向主叙事插一个 camelCase 再跑 → ⑩ 报——证明 `--offline` 判项真的在跑而不是全空转。批次 5 已删除 fixture 中的输出副本，测试直接读取唯一正本。

### D2 · 守恒收缩为硬事实守恒（P2，替代全列 token + 片段命中）

**目标形态**——机器对单元落点的全部行为就是这张表，`autoPlace` 与 check ② 守恒段按它重写：

| 单元 | 机器的落点行为 | 谁保证「不丢」 |
|---|---|---|
| 有硬事实 token（`tokensOf` 提出的标识符 / 量纲数值 / 反引号跨度 / 图片名 / URL / keep 编号） | `autoPlace` 按 token 命中定章并标 `by: machine`；check 核 token 在**那一章**；报错按 D7 分类指引 | 机器（这些改写时本该逐字保留，与人读改写不互斥） |
| 纯中文叙事（无 token） | **机器不定落点、不核内容**：一律留作者 `at`（`by: author`）；check 只核 `at` 是合同正文章名 | 裁决者逐对 + 删事实对抗测试 + 人核 |
| `covered_by` | 现状不动：共享 token / 跨材料同组 / 正文嵌套三选一的防错核（`468-513`） | 机器防错标 |

**退场**：片段命中——`autoPlace` 的无 token 片段兜底（现状 314-321）与 check ② 的对应核验分支（现状 535-538）。它是 E3 互斥的根源、E6 冤枉好文档的口径。`contentFragments` / `norm` 保留仅剩的 covered_by 嵌套检查用途。`tokensOf` **不改**——它提的本来就是硬事实。`cmdInit` 输出行（212-216）同步改述：「无 token N 个——由作者分配、裁决者逐对裁」。

**自验**：
1. 新单测：用金样同轮材料跑 init + 空 story audit → 无 token 单元的记录里 **0 条 `by: machine`**；有 token 单元行为与现状单测一致；
2. `grep -n "contentFragments" story-build.mjs` 仅剩 covered_by 检查一处调用；
3. 全量 `pytest` 与台账过（改动只影响无 token 单元的归属）。

### D3 · 分配簿记机器化（P3，替代模型全量填表）

- **合同新增 `allocation` 数据**（`story-chapters.json`），形态：

```json
"allocation": {
  "_note": "落点域按单元类划分：机器能定的不经模型。appendix_bound 匹配的单元由机器直接分到附录（在附录小节内核 token）；narrative_kinds 的单元只能分正文章，at=附录 即 FAIL。",
  "appendix_bound": [
    { "doc": "SPEC", "section_re": "^9\\.", "note": "§9 技术契约的表行——接口/字段/存储/配置/事件/交接，落点是附录的接口表与数据配置事件表" }
  ],
  "narrative_kinds": ["paragraph", "list_item", "blockquote"],
  "media_kinds": ["diagram", "image"]
}
```

**目标形态**——`cmdAudit` 的分配流程重写后，每类单元各走一条确定的路：

| 单元 | audit 行为 | 模型要做什么 |
|---|---|---|
| 匹配 `appendix_bound`（SPEC §9 表行） | 机器直接 `{ at: <附录章>, by: "machine" }`，token 核限定附录正文；缺失报「附录 A/B 表缺这一行」 | **无**——从分配任务里消失 |
| `knowledge` 规约条目 | 现状不动：走 ⑦ 判定表，不占章节落点（check 485） | 无 |
| `media_kinds`（diagram / image） | 章由作者选，check ④ 形态守恒不动 | 选章 |
| 叙事单元（`narrative_kinds` + 非 appendix_bound 的 `table_row`） | 作者 `at` 只能是**正文章**；`at` = 附录 → FAIL，报错：「业务叙述不落附录——这条讲的是 <section>，读者在哪一章想知道它？」 | 选正文章或标 covered_by——这是模型仅剩的分配工作 |

**自验**：
1. 新单测：SPEC §9 表行经 audit 全部机器归附录、不出现在「待处理」清单；
2. 夹具：叙事单元 `at: 附录` 的 audit.json → check FAIL 且报错含「读者在哪一章」；
3. audit 输出新增一行「待模型分配 N 条」，N = 叙事单元数（首跑口径下应从 267 降到约百条级）——数字对不上即分类逻辑错。

### D4 · 附录只认约定结构（替代「附录只核章级存在」）

- **合同**：附录章 `subsections` 按金样更新为 `["接口", "数据、配置与事件", "改动边界", "规约判定", "材料清单"]`（金样把「工程范围」改名「改动边界」、「追溯+来源」合并为「材料清单」）。⑦ 的判定表小节名「规约判定」不变。
- **check 新增判项 ⑫ 附录结构**（放 ⑪ 后）：
  1. 附录章内全部 `###` 小节名（剥 `A. ` 类前缀后）∈ 合同 `subsections`，多出的小节 FAIL：「附录只有约定的五节——『<名>』不在其中；工程细节各有落点表，叙述归正文」；
  2. 附录正文里出现 ```` ```text ```` / ```` ```json ```` / ```` ```yaml ```` 等非 mermaid 围栏块 FAIL：「附录是表和列表，不是原文存放处」；
  3. 每小节非空（表行或列表行 ≥1，或恰为「不涉及：<依据>」一行）。
- **夹具** `S06-appendix-dump/{bad,good}`：bad = 首跑产物删减版（含「机器核对索引」text 块 + 打包验收编号），good = 金样附录节选；进 `failure-modes.yaml` 台账。

**自验**：① 台账 S06 正反过；② 对金样跑 ⑫（含 `A. ` 前缀剥离）零 FAIL；③ 对首跑失败产物（`output/story/story-suite-20260829-batch4` 的 story.md）跑 ⑫ → 至少报「机器核对索引」一条。

### D5 · 裁决与对抗测试

- verifier 三表、逐对裁决、`story-verify.md` 不动（D2/D3 后 `by: author` 队列恢复实义）。
- TEST.md 常驻的删事实对抗测试**本轮必跑出数**：两 Case 各删 5 事实（≥2 纯中文单元）→ 各 ≥5 条「未讲清」并点名；0「未讲清」= 判据面无效，停下回本节。

### D6 · review 按 B2-9 原文修（P5，补 E7）

B2-9 判定原文（源 `../作废-第三版/00-方案.md:204`、`:219`、`01-判据.md:48`），整段引入：

> 没有：「如何填写」段、分区标题、确认人/日期/依据、责任人/期限/阻塞、「下一步」、状态行、附录。

> review.md：存在；每个非 story_init 决策有块、无未登记块；机器区 == 当前渲染；文件全部行 ∈ 语法（标题行、四种字段行、三勾选行、勾选下的人工行、标记行、空行）——出现「如何填写」「确认人」「状态」「下一步」「附录」即 FAIL。

> K2-D1 评审记录只含渲染语法：**计** 全部行 ∈ {标题、建议/结论、为什么、改了会影响、请…确认、三勾选、勾选下人工行、`<!-- decision -->`、其他意见及其标记、空行}；出现上述禁词即 FAIL；**不计** 人工内容。

- **`review-render.mjs` 改法**：`renderHumanZone:49-71` 收缩为三态 + 修改意见行 + 标记：

```js
return [
  HUMAN_ZONE_MARK, '',
  '- [ ] **同意当前建议**',
  '- [ ] **有其他意见，需要修改**',
  '  - 修改意见：',
  '- [ ] **暂缓**',
  '  - 暂缓原因：',
  '', `<!-- decision: ${dec.id} -->`,
].join('\n');
```

  暂缓保留一行「暂缓原因：」（三态之一要能说明为什么，这是勾选下人工行，语法允许）；**删**：暂缓责任人/完成期限/是否阻塞执行/后续动作/确认人/确认日期/确认依据七字段及 62 行注释。`extractHumanZone` 兼容旧产物（按 MARK 与标记切，不受字段增减影响），不改。
- 尾部「**状态**：草稿（待开发确认）」行（`cmdBuild:955`）：B2-9 禁「状态行」——删；`spec.md` 作业包与 `hooks/spec/author.md` 里「状态行保持草稿」的字句同步删（grep「状态行」「草稿（待开发确认）」全仓清理，知识层除外）。
- **check 新增 review 行枚举判项**（并入 ⑤ 或独立 ⑬）：对 `AR/review.md` 全部行按 K2-D1 枚举核；出现「如何填写」「确认人」「确认日期」「确认依据」「状态」「下一步」样式行即 FAIL。人工区内容不计。
- 夹具 `R01-review-legacy-fields/{bad,good}` + 单测（渲染快照、旧产物人工区保留）。

**改后一个决策块的完整产物形态**（渲染快照单测以此为准）：

```markdown
### 挂失单与补卡单的责任边界

- **当前结论**：本单只实现紧急挂失、冻结结果确认和冻结结果交接，补卡流程由另一张开发单承接。
- **为什么这样定**：上游已拆分两张开发单；合并实现会扩大本单范围并改变兄弟单的交接责任。
- **同意或修改后会影响什么**：需求范围、补卡交接、验收边界
- **结论来源**：用户确认与上游产品、系统设计
- **请谁确认**：需求负责人

#### 审核结果（由评审人填写）

- [ ] **同意当前建议**
- [ ] **有其他意见，需要修改**
  - 修改意见：
- [ ] **暂缓**
  - 暂缓原因：

<!-- decision: loss-only-boundary -->
```

**自验**：① 渲染快照单测 == 上述形态；② 带七字段的旧 review.md 经 build 重渲染后**人工区已填内容逐字节保留**（`extractHumanZone` 按 MARK 切，不受字段增减影响——单测覆盖）；③ R01 台账正反过；④ 全仓 grep「确认人」「确认日期」「状态**：草稿」在 doc/extensions 零命中（知识层与设计目录除外）。

### D7 · 作者面与报错文案（P4，替代逐条点名 token 的报错）

- **报错文案改法**（check ②，`541-552`）：
  - stateless 报错（541）保留结构，末句改为：「补写正文，或标 covered_by——这件事读者在哪一章想知道，就分那一章」；
  - `548` 的「落点标在『X』，但那一章里找不到：<token 列表>」**按 token 类别给形态指引**，不再裸列字面：keep 编号 →「验收编号在验收章逐条独立行（金样 8.x）」；§9 实体名 →「接口/字段/键名的落点是附录 A/B 表（金样附录）」；数值 →「阈值随它所属的叙述或验收行」；其余 →「这条事实要在『X』章讲出来，讲法见金样对应章」。实现：按 `id_shapes.keep` 正则、`appendix_bound` 命中、`NUMERIC_RE` 分类。
  - **禁止样式**（台账反例）：报错含「找不到：」+ 裸 token 列表。
- **`story-write.md` 更新（P6：形经中性描述传递，不引金样）**：「什么内容用什么形态」表按金样定稿的形态**补全描述**——特性分工用主题行对照表（每格 1–3 个短项，禁分号长串）；恢复类小节用「什么时候发生 / 页面怎么处理 / 用户回来之后」引导词；验收按「要验的事」分组、每编号独立行；附录五节各自成表或列表、每节先一句目的。示例一律用**中性占位内容**（「某接口」「某编号」），不出现金样业务字面。分配段更新为 D3 后的任务（只分叙事单元与图/表章位）；十三条与四红线保留。`phases/spec.md` §二 随 D3 改述。
- **`story-template.md` 是「形」的第一载体**：每章注释按金样定稿形态补充（章序编号、附录 A–E 小节与各自表头、验收分组样式）——模板本来就注入被测侧，「形」从这里传递，金样本体不进被测视野。

**改后报错文案样例**（每类一句，实现按 token 分类套模板）：

| token 类 | 改前（禁止样式） | 改后（模板由类别 + 合同数据生成，示例中的具体名字只是本方案的说明，**不写进代码**） |
|---|---|---|
| §9 实体名 | `落点标在「附录」，但那一章里找不到：<接口名>` | `接口「<接口名>」的落点是附录·接口表——这一行在表里吗？` |
| keep 编号 | `…找不到：<编号>` | `验收编号 <编号> 要在验收章有独立一行和可观察的通过条件` |
| 数值 | `…找不到：<阈值>` | `阈值「<阈值>」随它所属的叙述或验收行一起讲` |

报错文案**不得**出现「金样」字样、fixtures 路径、或任何具体业务字面模板（P6）——分类模板 + 合同数据（附录小节名、章名）拼出指引。

**自验**：① 台账反例：check 输出不含「找不到：」+ 裸 token 列表样式；② 构造缺失夹具各类跑一遍，人核报错句指向形态而非字面；③ 作业书更新后 `--phase extensions` 过（注入体积仍 ≤3.1KB/阶段口径）。

### D8 · 效率对比（E5 的验收）

CLI 重跑同两 Case，交付报告出四项对比：check 调用次数（首轮 23）/ 失败序列有无回升（首轮 35→11→9→1→6→18）/ story 成文链时长（首轮 24 分钟）/ 全轮时长（首轮 ≥89 分钟未闭环）。目标方向：check 无反复回环（≈ 每章一次 + 终检）、失败数无回升。
（v1 曾列「未知子命令乱试修复」——查证 `COMMANDS:44` 含 `build`（渲染 review 的正当命令），runlog 的 build×4 是正常调用，**该项撤销**。）

## 4. 边界约束（验收同看）与不做 / 登记

三条边界约束，实现与验收都要对照（用户 2026-08-30 裁定）：

1. **不与金样 / 测试用例过拟合**（P6）：判据实现只写形态规则与合同数据；不 diff 金样或夹具字面；不硬编码任何业务词。泛化由第二 Case（local-issue-autotopup，本地单、无系统材料）与内网 holdout 证明——两者与金样同轮材料完全不同。
2. **金样给的是形，内容以实际需求为准**：金样是表达与结构的定义，不是内容模板；被测模型看不到金样，学形只经模板与作业书的中性描述。若重跑产物出现金样业务字面（交通卡语汇出现在非交通卡需求里）即过拟合事故，停下复盘。
3. **不影响知识相关功能**（P7）：knowledge 单元枚举、⑦ 判定表、spec §10/§11、plan `knowledge_obligation_on_entity`、coding `knowledge_landing_in_code`、verifier 引文核实、W01 非 story 隐形——全部不动；收口时知识链回归必须全绿（KG-11）。

- **两段分配（素材落盘提前）本轮不做**，登记：提议 = init 时初分四份导入材料的单元与跨材料 `covered_by` 网络 + spec 定稿后增量分配 SPEC 单元与受裁定影响的修订（audit 天然支持增量）。不并入实证：首跑失败非遗忘所致——模型把全部材料原文抄进倾倒区，内容都在上下文。启用条件：本轮修复后重跑若出现叙事单元错分、`covered_by` 判反等判断质量失败，两段分配为首选正向修法。
- 保留不动：十章合同与 questions、story-template、lint 阈值（段 200 / 章 600 / 步 12 / 重复段）、语言红线（scope=附录外）、形态守恒 ④、`baseline_coverage.py`（口径注记：字面 miss 由裁决者/人核判语义，E6）、W01 与 S01–S05 夹具、知识链、`linkDuplicates`、covered_by 嵌套检查。
- 首轮交付欠账补进 02-交付报告：questions 31 vs ≈33 差异、知识 frontmatter `sections` 清理、跨章同引文查重三条登记。

## 5. 顺序与收口

| 步 | 做 | 判据 |
|---|---|---|
| 1 | D1 金样入库 + `--offline` 锚 + ①/⑦ 标题前缀放宽 | KG-0 |
| 2 | D2 删片段通道 + init 输出行 | KG-1 |
| 3 | D3 合同 `allocation` + audit/check 分配域 | KG-2 |
| 4 | D4 合同 subsections 更新 + check ⑫ + S06 夹具 | KG-3 |
| 5 | D6 review 渲染收缩 + 状态行退场 + 行枚举判项 + R01 夹具 | KG-4 |
| 6 | D7 报错文案分类改写 + 作业书更新 | KG-6 |
| 7 | 离线收口：单测 / 台账 / `--phase extensions` / 退场 grep（「片段」「确认人」「状态行」等） | 全部离线 KG |
| 8 | CLI 重跑两 Case：停机判定（不丢/形态对基线与金样）→ 删事实对抗（KG-5）→ 人核四问 + 金样对读 → D8 效率表 → 02-交付报告 | KG-5/7/8/9 |

每步末：`node --check`、`pytest test/story/tests -q`、`check_failure_modes.py`、`--phase extensions`。

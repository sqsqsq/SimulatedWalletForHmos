# 批次 4 · F 修复 · 判据（先于实现）

> 金样 = [test/story/golden/story-金样-AR90004.md](../../../golden/story-金样-AR90004.md)，是所有判据的仲裁锚：**任何判据拦金样即判据错**，修判据不修金样。
> 两层验证：00-方案 每个 D 自带「自验」（步步跑，判断改对了没有）；本表是收口判据（全部 D 完成后整体核）。

| id | 判据 | 口径 | checker |
|---|---|---|---|
| **KG-0** | 金样锚 | 金样+同轮材料入 `fixtures/golden/AR90004/`，指纹单测；lint 全部规则 + check 可独立判项对金样跑 **零 FAIL**，台账常驻正例——判据改动先跑这一行 | 单测 + 台账 |
| **KG-1** | 硬事实守恒 | 「≥8 字中文片段命中→by:machine」通道不存在（代码与文本零残留）；硬事实清单（keep 编号 / 数字阈值 / §9 实体名 / 图表形态）判据在；两 Case 副本枚举：中文叙事单元全为 `by: author` | 单测 + 脚本对照 |
| **KG-2** | 分配机器化 | §9 实体类与规约条目单元由机器直接归位附录（不出现在模型分配任务里）；叙事单元 `at: 附录` = FAIL 且报错指向落点形态；模型分配量 ≤ 叙事单元数 | 单测 + 台账 |
| **KG-3** | 附录结构 | 附录小节 = 合同枚举（A–E），合同外小节 FAIL；附录内 text/json 倾倒块 FAIL；`S06-appendix-dump/{bad,good}` 正反过（bad = 首跑产物删减版） | 台账 |
| **KG-4** | review B2-9 | 渲染产物无七字段（暂缓责任人/完成期限/是否阻塞执行/后续动作/确认人/确认日期/确认依据）；人工区 = 三态勾选 + 修改意见行；K2-D1 行枚举判据在 check，出现「如何填写」「确认人」「状态」「下一步」即 FAIL | 单测 + 夹具 |
| **KG-5** | 对抗测试出数 | 两 Case 各删 5 事实（≥2 纯中文）→ 各 ≥5 条「未讲清」并点名；不删对照一致；数字进交付报告；0「未讲清」= 判据面无效，回 D5 | 副本实跑 |
| KG-6 | 报错文案 | check/lint 报错不含「找不到：<字面>」样式；每条修法指向落点形态；台账反例守住；`story-build` 未知子命令打印子命令表 | 台账 + 人核抽样 |
| KG-7 | 效率对比 | 交付报告含四项对比：check 次数（首轮 23）/ 失败序列有无回升（首轮 35→11→9→1→6→18）/ 成文链时长（首轮 24 分）/ 全轮时长（首轮 ≥89 分未闭环） | 实跑 runlog 统计 |
| KG-8 | 纪律 | 金样与本方案 / 本判据未被执行者改动；金样需改时停下找维护者；偏差进 02-交付报告差异栏；首轮三条登记欠账补齐 | 维护者 |
| **KG-9** | 行为收口 | CLI 重跑两 Case 到 spec 闭环：story 通过停机判定（不丢 / 形态对基线与金样不劣）；人核四问可答；与金样并排对读无明显落差；review.md 符合 KG-4 形态 | 实跑 + 人核 |

| **KG-10** | 防过拟合（P6） | 机制层（doc/extensions 代码 / 合同 / 作业书 / 模板 / 报错文案）「金样」字样、fixtures 路径、金样与 Case 业务字面零命中（M01/M02 口径扩展）；判据实现无对金样 / 夹具的字面 diff；第二 Case 与 holdout 同判据通过 | grep 脚本 + 台账 |
| **KG-11** | 知识链无回归（P7） | 两 Case 重跑：⑦ 判定表判据、spec §10/§11、plan `knowledge_obligation_on_entity`、coding `knowledge_landing_in_code`、verifier 引文核实、W01 全部 PASS，与批次 3 收口态行为一致 | 实跑 + 既有台账 |

夹具（新增）：`S06-appendix-dump/{bad,good}`、`R01-review-legacy-fields/{bad,good}`（bad = 带确认人等七字段的 review.md）；`fixtures/golden/AR90004/`。

# F5 · 验收判据（KG5）——批次 4 收口线

> 与 `00-方案.md` 配套。两份金样正本（`test/story/golden/`）单向否决：拦金样即判据错。
> 判据与机制实现零 Case 业务字面（P6/G02）。

| id | 判什么 | PASS 条件 |
|---|---|---|
| KG5-1 引文语义化 | 句边界＋表内整格＋两条空判据退场 | ① 单测三组：窗口切片点名／完整句通过／表内整格通过；② story 金样跑 verdicts 生成引文零误拦（误拦即调规则，差异栏）；③ F4 存档重放：12 字窗口引文全数点名；④ **不加相似度/重叠率类判据**（反向锁单测）；⑤ 退场：token 绑定判与「同小节」设想零残留（grep），判定阶梯写明语义绑定面靠裁决者与抽样 |
| KG5-2 图位 | 附录禁图＋E 节全块判＋悬空指图（「归落点章」已因循环判据删除，见方案 D2 自审注） | ① 反例夹具三条各被点名（图入附录／E 节列表前杂块／悬空指图）；② 金样零 FAIL；③ fB‴ 存档重放报 4 图入附录＋4 处悬空；④ 图守恒（每图必引）回归不破——禁附录×必引合围后图只能回正文所属章 |
| KG5-3 台账冻结 | 登记后 init/audit 拒绝＋指纹核对 | 单测三条：登记后 init 被拒并指路／台账被改 check 点名／登记前照旧 |
| KG5-4 模式否决门槛 | 知识 README 一句＋作业书义务＋交叉核对判（落 plan 知识决策表，不进 decisions.json——Q1 裁定） | ① 交叉判单测：命中→不选→知识决策表缺行或空理由点名，有行有理由通过，零命中不触发；② 知识面 diff 仅 README 一句（KG5-9 连锁）；③ F4 存档 fB plan 重放点名两条；④ verifier 问含「反证引用业务信号还是承载形态」 |
| KG5-5 正式心态 | README 一行＋作业书一句＋verifier 问 | ① 仓根 README diff 仅第 5 行且新句无「模拟/示例/演示」定性；② 作业书句无 Case 字面；③ verifier 问落地；④ 收口产物零「演示/示例/Demo」需求定性（人工核对进报告） |
| KG5-6 小账六件 | 表行查重／clarification 禁标题／词表两词／逐数值问／rules 示例类别化／编号后缀半句 | 各一正一反单测或 grep 证明；金样表行零误拦（维护者已预核，执行复核）；「人话」「import_sources」入词表后金样与模板零误拦 |
| KG5-7 装置 | run_store 增量读＋TEST.md 更正 | ① 大文件（≥5MB）多轮 poll 内存稳定实测数据进报告；截断自愈；② 判定逻辑 diff 为零（只动读取）；③ TEST.md §3.4 改为使用说明，「装置缺陷」定性删除 |
| KG5-8 收口＝批次 4 验收线（**硬条件**） | 在线两 Case＋两道达标＋残差归类 | ① 两 Case CLI 至用户设定终点（end_phase 由用户改定）；② 删事实每 Case 5 条（含 2 条 spec 独有）**≥5/5 未讲清**；抽样人核 10 行（种子入报告）**错判 ≤1**；③ 每 case 残差对照表全为 d 类；④ F4 两条 a 类残差（引文形式面修复＋空判据删除承接）、用户点名图位病、c 类冻结全部清零，F4 §5.4 清零项保持清零；⑤ 数字不达标＝按 D1 判定阶梯贴样本回传，不自行加判据、不宣告收口 |
| KG5-9 知识与纪律 | 知识零语义改动（README 一句除外）＋边界＋差异栏 | 知识链单测与夹具全绿；在线核对规约判定与知识决策照常；金样/方案/判据未动；`cases/`（除用户自改 end_phase）、`run_*.py` 其余、`framework/` 零改动；差异栏；四件套记录；KG2-9 五项＋G02 双绿 |
| KG5-10 adapt 五件 | 版本前置放宽／热修段删除／`.adapt-<version>` 目录＋目标 .gitignore 追加／manifest 1.2.0／AGENTS.section 移位 | ① SKILL 无「≥3.0.0」前置与热修清单（grep 零命中），版本改为记录项；② 模拟 framework 2.x 无热修目标树 adapt-scan 跑通；③ 新目录名含版本、.gitignore 追加有实测记录、本仓自忽略行同步；④ manifest diff 仅 version 行（1.2.0）；⑤ AGENTS.section 移入 `skills/story/` 后旧路径全仓 grep 零命中且引用两处同步——或 framework 侧写死路径时不移动、差异栏说明 |

# F4 · 验收判据（KG4）

> 与 `00-方案.md` 配套。**两份金样正本（`test/story/golden/`）是单向否决锚**：
> 任何判据拦金样即判据错，改判据不改金样；渲染器渲不出金样形态，改渲染器。
> 判据与机制实现不得引用任何 Case 业务字面（P6/G02）。

| id | 判什么 | PASS 条件 |
|---|---|---|
| KG4-1 编号机器化 | `number` 命令（章节＋图题）＋登记步接入＋lint 3 退役、lint 1 收窄 | ① story 金样跑 `number` 逐字节不变（幂等锚，含图题「图 N ·」）；② 去号版金样（标题与图题前缀均剥）跑后与原金样逐字节一致；③ 乱号/缺号/半带号三种输入单测各归位；④ 退场 grep：`heading_shapes` 与 lint 1 的图题前缀判在 doc/extensions 零命中；⑤ 模板与作业书中「模型写编号」字句零残留 |
| KG4-2 spec 守恒收窄 | spec 单元 token 仅业务编号 | ① F3 存档离线重跑 init：SPEC token 数从 194 大幅下降，数字进交付报告；② 单测：spec 样材含标识与验收编号，仅编号成 token；其余五份材料枚举结果不变（回归单测）；③ 金样台账零 FAIL；④ 附录尾巴对抗：删存档产物的附录散文尾巴重跑 check 不再报 spec token 缺落点 |
| KG4-3 review 承载 | 准入口径替换＋十一类＋category＋渲染器对金样 | ① 从 review 金样倒推登记表（含 category）跑 build，输出与金样**逐字节相同**；夹具与指纹同步为正本；② 窄准入定义原句退场 grep 零命中，rules.md:58/:62 与作业书同一段文字（单测锁三处一致）；③ category ∉ 词表被点名；④ **无数量下限、无每类条目、无叙述内容判据**（有即 FAIL，单测反向锁）；⑤ verifier 四问在 story-verify.md 齐备且为类别语言；⑥ 人工区「审核结果：」后内容重渲染逐字节保留（回归） |
| KG4-4 形式 A 档 | section_form 判定＋附录零散文＋E 节边界 | ① story 金样逐项零 FAIL（术语章按「全章非表正文段 ≤1、位置不限」——金样表后辨析段通过；「不涉及」独行豁免生效；金样与 4a 数据冲突时改数据的记录进差异栏）；② 反例夹具各一：取舍散文化／受限异常混一表／验收 bullet 化／附录表外散文段／E 节中间产物链接——全部被点名且报错文案不含产物字面；③ C 档零新增表格类判据（grep＋单测反向锁） |
| KG4-5 长文与输入 | 160 红线＋指引＋渲染输入五样 | ① 金样最长段 153 通过、160 红线单测正反各一；② 指引句「百字上下」在作业书；③ 逐章渲染输入清单含形式映射表；④ 节奏面零机器判据（反向锁） |
| KG4-6 离线收口 | 四件套＋台账＋过拟合＋包版本 | 单测全绿（数字进报告）；失效形态台账全 PASS（新反例入列）；`--phase extensions` PASS；KG2-9 五项＋G02 双绿；退场 grep 清单（§7 步 6）零命中；`manifest.yaml` version 升 1.2.0 且历史版本结构签名三条核验仍有效（3g 补条） |
| KG4-7 裁决面达标（**硬条件，口径＝数字达标**） | D7 引文绑定＋抽样人核＋删事实对抗 | ① D7 两条形式判落地：token 绑定正反单测、复用上限第 3 次点名单测、story 金样跑 verdicts 引文零误拦（拦到即调，记录进差异栏）、F3 式章级总述引文反例被点名；② 抽样人核 10 行（种子入报告）**错判 ≤1**；③ 删事实对抗每 Case 5 条（含 2 条 spec 独有业务事实）**≥5 未讲清**；数字不达标＝修 D7 再跑，不降标、不顺延 |
| KG4-8 在线收敛 | 两 Case CLI 至 story 链闭环＋残差归类 | 两 Case 的 **story 链判据**（三产物齐备/守恒/lint/review/成文登记）全 PASS；story 链外 blocker（如知识裁决表执行欠账）单列记录、不豁免到无声、不阻塞收敛判定、不许动知识面判据；残差对 03 件逐条归类，**每个 case 独立做到残差全 d 类**（无「模型弱」豁免）；03 件点名的可判项应消失：附录散文尾巴／E 节中间产物行／H2 无编号／图片新目录副本／review 一句话条目／取舍散文化／受限异常混表 |
| KG4-9 纪律 | 金样与 00/01/03 不改；边界；差异栏 | 两金样正本与方案判据件未动（git 实证）；`cases/`、`run_*.py`、`config/`、`framework/` 零改动；差异栏含空登记；每步四件套记录；执行协议（遇未覆盖分叉停等回传，不猜）无违例 |
| KG4-10 知识功能回归（§0′-3） | knowledgeUnits／激活清单／规约判定／knowledge-ledger 零改动 | ① git diff 实证上述通道文件未被本轮语义性修改（共用文件的改动逐行说明不涉知识路径）；② 既有知识链单测与夹具全绿；③ 在线收口两 Case 的规约判定表与知识决策章照常产出（交付报告附核对结论） |
| KG4-11 三轴量化（§0′-1） | 达成率与收敛的数字证明 | 交付报告含三轴对比表：结构轴（两 Case H3 数 vs F3 轮 31/20 vs 金样 34）、review 轴（条目数与口径符合度 vs F3 轮 5/4）、长文轴（段均长/超百占比/墙数 vs F3 轮 66·15%·2／89·39%·4 vs 金样 58·10.5%·0） |

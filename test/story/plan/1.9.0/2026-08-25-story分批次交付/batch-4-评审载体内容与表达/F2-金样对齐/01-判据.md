# 批次 4 · F2 金样对齐 · 判据（先于实现）

> 金样是仲裁锚：**任何判据拦金样即判据错**（G01 是每步第一验收）。两层验证：00-方案 各 D 自带「自验」步步跑；本表收口整体核。

| id | 判据 | 口径 | checker |
|---|---|---|---|
| **KG2-1** | 模板对齐金样 | 模板 H1 带编号占位、无头部登记块、章与附录小节编号必带、材料清单行形态（来源/《文件名》/提供方式/贡献）、全篇样式约定注释在且中性；check ① 核 H1 含 feature 编号；金样对全部判据零 FAIL | 单测 + 台账 G01 + 人核 |
| **KG2-2** | 视角回滑修死 | `machine_facing.table_columns` 含「权威模块」「代码现状」，重枚举后该两列不产 token；`language_redline` per-kind scope 生效——检索措辞与装置词类在附录同样被拦（正反夹具）；装置词表在合同数据且不含业务词 | 单测 + 夹具 + 台账 |
| **KG2-3** | 取舍有源 | 决策件在 `sources`，settled 决策单元有落点义务（删取舍表 → audit 点名）、open 议题不得成为正文结论；金样同轮材料跑通 | 单测 + 脚本对照 |
| **KG2-4** | 统稿入流程 | `phases/spec.md` 含 ③b 统稿、作业书自查六项在且中性；`next_step` 文案同步；**无**相似度/重复率/配额类新判据混入 | 文本核 + `--phase extensions` |
| **KG2-5** | 逐章维度 | `chapter_dimensions` 含「一处完整表述」维（中性措辞）；维度总数单测 | 单测 |
| **KG2-6** | 两处查实 | lint 双写句漏报与验收层缺失各一页事实记录（复现步骤 + 结论 + 处置）进 03-交付报告；确属盲区才改代码并配单测 | 交付报告 + 单测（如改） |
| **KG2-7** | 裁决面恢复出数 | CLI 重跑后：抽样人核 10 行（种子入报告，≥2 对不上=无效）与删事实对抗（每 Case 删 5 → ≥5 未讲清）**两道都出数都过** | 实跑 + 人核 |
| **KG2-8** | 产物对读收口 | 两个新 Case 的 story 与金样按 02-差距清单三轴并排对读：清单所列病类（非字面）不再出现；停机判定（不丢/形态对基线与金样不劣）先跑 | 实跑 + 维护者人核 |
| **KG2-9** | **过拟合自检**（用户 2026-08-30 追加，验收必查） | ① 机制层（代码/合同/模板/作业书/报错文案）对「金样、golden、fixtures 路径、金样与两 Case 业务字面」grep 零命中（M02 口径续用）；② 判据实现无对金样/夹具/差距清单的字面 diff；③ 合同新增数据（装置词表、机器面列名）逐项人核为**类别与模板约定**而非样本字面；④ 两个业务域不同的 Case 在同一套判据下同过——任一判据只在单一 Case 上可解释即疑似过拟合，停下复盘；⑤ 模板「全篇样式约定」逐条能对应金样实例**且**不含金样业务词 | grep 脚本 + 维护者逐项人核 |
| KG2-10 | 纪律 | 金样/本方案/01/02 未被执行者改动；偏差进 03-交付报告差异栏；每步末四件套（node --check / pytest / 台账 / harness） | 维护者 |

夹具（新增）：红线 per-kind scope 正反（附录含检索措辞/装置词 = bad）；决策件枚举（settled/open）；D6 双写句复现件。

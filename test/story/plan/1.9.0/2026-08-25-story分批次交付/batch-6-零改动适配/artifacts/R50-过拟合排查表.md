# R50 · 交付面过拟合排查表

**范围**：`doc/extensions/` 除 `knowledge/`（脚本、提示词、合同、模板、报错文案、注释）。
**四类**按 `steps/03` §1 的 R50。`test/story/` 里的夹具用案例词是正常的，本表不含它们。

## 1. 案例词（13 处，全部换掉）

全是「举个例子」的那半句，判据本身一条没动。例子取自 Case，执行者打开脚本就照着写，
测出来的是「照抄那一次」而不是「在未知需求上做对」。

| 文件:行 | 是什么 | 处置 |
|---|---|---|
| `skills/story/contracts/story-chapters.json:464` | 小节编号判据的例子 `3 种签约情形` | → `3 类异常处置` |
| `skills/story/phases/story-write.md:80` | 材料扫描表里的「门限」 | → 阈值 |
| `skills/story/phases/story-write.md:140` | 不该引的图举例「友商参考」 | → 同类产品的参考稿 |
| `skills/story/phases/story-write.md:172` | 标题写法举例「签约与管理由本单一次交付」 | → 两块能力由本单一次交付 |
| `skills/story/phases/story-write.md:207` | 时序图适用场景举例「授权必须在创建签约之前」 | → 鉴权必须在创建之前 |
| `skills/story/phases/story-write.md:224` | 节名举例「与补卡的交接约定」 | → 与上游单的交接约定 |
| `skills/story/phases/story-write.md:290` | 跨章比对的条件举例「余额不足」 | → 配额用尽 |
| `skills/story/scripts/headings.mjs:17` | 裸序号举例 `1 闸机前的窘境` | → `1 现状与问题` |
| `skills/story/scripts/story-build.mjs:629` | 同上的节名举例 | → 与上游单的交接约定 |
| `skills/story/scripts/story-build.mjs:779` | 图身份取节号的例子 `### 5.2 自动充值触发` | → `### 5.2 异常回收` |
| `hooks/shared/reader-review-task.mjs:71` | 跨章比对的条件举例 | → 配额用尽 |
| `hooks/shared/reader-review-task.mjs:88` | 图那一问的举例「友商参考」 | → 同类产品的参考稿 |
| `hooks/spec/author.mjs:139` | 任务包 §4 同一处举例 | → 同类产品的参考稿 |

**复扫**：22 个案例词（含三个 Case 的业务词、单号前缀、Case 目录名）在交付面 **0 命中**。

## 2. 按金样调过的数字

交付面里只有一个这类常量：

| 常量 | 依据 | 挡住过什么 | 处置 |
|---|---|---|---|
| `story-build.mjs` `COPYEDIT_ROWS = 7` | 作业书 `story-write.md`「第二步 · 统稿」的自查清单恰好七项 | 统稿是唯一没有产物的动作，跳过它零成本；留痕让「没做」这件事被看见 | **留**。原来两处各写一个 7，靠人记着同步；现在夹具从 `story-build.mjs` 读出这个数再与清单项数比，改一处忘另一处会红 |

其余判据不含阈值：⑪ 只核槽位在不在，⑫ 只核集合，⑫a 明写「不设字数、行数、表格数、
图片数与条目数的下限」。用户明确要求的固定形态（标签、表头、附录序号）按 §7.5 不动。

## 3. 只对本 Case 成立的例子与判据

- 报错文案里剩下的举例（`§6.2 数据存储`、`不涉及：…`、URL 里的 `//`）都是机制自身的形态，
  与业务无关，留；
- 判据里没有「恰好本 Case 这样」的形态要求：图的张数、节的个数一律不判
  （⑫a 的注释写着为什么：设了下限逼出来的都是凑数）。

## 4. 失效形态与词表

- `check_failure_modes.py` 70 条 **FAIL 0**，M02 的五样（单号、业务特征词、Case 与 suite 名、
  运行计数、轮次叙述）在交付面零命中；
- `clientVocabulary()` 九个词——灰度 / 回滚 / 回退 / 部署 / 集群 / QPS / TPS / 熔断 / 限流——
  逐个核过：都是服务器侧的通用词汇，不是这个 Case 的口头禅。

## 5. 排查没有加任何机制

本表只做两件事：换例子、把一个常量与它自己声明的依据绑上。判据数量、判据种类、
门禁数量都没变。

# 批次 1 · 知识应用 · 交付报告

> **本报告分栏记录**：实施事实 / 离线验证结果 / 历史回归结果 / 行为验证。
> 四栏不互相替代——**离线全绿不等于行为完成**，脚本通过只证明结构，
> 模型行为结论只能由隔离 Case 实跑给出（坑 #35）。
>
> 本文件**不写「已实施」当作「已生效」**：下面每条都标明它是由什么证据支撑的。

## 一、需求覆盖

| 需求 | 交付内容 | 证据类型 |
|---|---|---|
| B1-1 三类知识封闭集合 | `knowledge_activation` 三类互斥；未知类型直接报错 | 机械（构造夹具四种路径） |
| B1-2 规约不携带实现事实 | 工具类名与模块路径迁出规约；项目知识重写为**导航规则**（去哪找、照什么惯例写），不再是会过期的清单 | 机械（知识层自检接入 spec post_check） |
| B1-3 可观测性瘦身 | **时机导向**重写：开头一张表说清三条渠道各自什么时候记，条目 5→3（时机不混用 / 共用执行标识 / 失败不改业务） | 机械（结构判据；行数只作诊断） |
| B1-4 三层合同语义保留 | 时机表承载渠道分工与密度递减、终态唯一；标识共用与失败隔离各一条目；**秘密字段归 security**（所有出口统一管，不分散） | **语义**（待评审） |
| B1-5 两模式恢复 | 正文逐字节 = 归档原件；元数据受控扩展三键 | **一次性交付验证**（SHA 记在冻结清单）；常驻守恒归批次 3 的发布清单 |
| B1-6 Knowledge 不维护路由 | 删除适用矩阵；阶段路由归各阶段规则 | 机械（知识层自检） |
| B1-7 manifest 确定性激活 | 阶段只读清单，不扫目录；`lint-rules` 的文件名也改为从清单派生 | 机械 |
| B1-8 spec 只识别不选型 | §10 按命中条目派生；编号到条目级；判「否」给依据 | 机械（反例 6 条命中） |
| B1-9 三出口独立成节 | §9/§10/§11 分立，机械校验不落在契约章区间内 | 机械 |
| B1-10 plan 冻结 | `knowledge_freeze` 义务五要素 + 模式角色投影；三重锚定 | 机械（反例 14 条命中） |
| B1-11 下游只读冻结 | 四阶段 post_check + 注入件；冻结缺失一律阻断 | 机械（四阶段阻断实测） |
| B1-12 只写名称不算应用 | 复述判定拦原文复制；同义改写交语义层 | 机械 + **语义** |
| B1-13 全集语义裁决 | 必答清单全集注入；三方 ID 集合机械收口 | 机械（集合）+ **语义**（逐行） |
| B1-14 复述判定唯一可实施 | 四维定死；九分支冻结预期 | 机械（9/9） |
| B1-15 自检问句同批同向 | spec/plan/story 三处注入件含同一组三问 | 机械（M14 按路径判归属） |
| 资源使用（用户诉求 3.1） | 新增 RES 域 4 条：图片优先级、新引入 ≤10KB、字符串复用优先（依赖方向挡住时允许临时硬编码但须显式告知）、新增按惯例定义并联动翻译回稿 | 机械（新域派生 + 出口判定）|

## 二、离线验证结果（**已完成**）

| 项 | 命令 | 结果 |
|---|---|---|
| 语法 | `node --check` 全部 `.mjs` | 通过 |
| 失效形态全量回归 | `check_failure_modes.py` | **30/30**（FAIL 0，SKIP 0；含新增 M16/M17） |
| 复述判定分支 | `check_paraphrase_branches.mjs` | **9/9** |
| Story 单测 | `python -m unittest discover -s test/story/tests` | 132 passed |
| Tools CLI 单测 | `python -m unittest discover -s tools/cli/tests` | 16 passed |
| Python 编译 | `compileall -q tools/cli test/story/scripts` | 通过 |
| 扩展协议 | `harness-runner.ts --phase extensions` | PASS 6/6，Blockers 0 |
| 只读演练 | `run_multi_case.py plan --all --isolated-workspaces` | 通过 |
| 机制层负面扫描 | 台账 M01–M04 | 零命中 |

**这一栏证明的是**：结构、引用、实体、集合一致性、判据分支行为符合冻结预期，以及机制层对知识内容零硬编码（新增一个域不改任何机制代码即生效）。
**不证明**：模型在真实链路上会照做。

## 三、历史回归结果（**已完成**）

对实施前基线跑新门禁，验证新能力对旧产物有区分力：

| 目标 | 检出 | 说明 |
|---|---|---|
| `doc/features/DTS90006` spec | 缺「规约约束要求」章 | 旧产物用 2.1 的章名，新形态未满足——预期命中 |
| `doc/features/DTS90006` plan | 知识决策章在第 142 行、晚于设计章第 49 行；缺 `knowledge_freeze` | 形态 P06 命中 |
| `doc/features/DTS90006` 台账 | P06 决策章位置；P11 verifier 漏裁 3/14 | 后者是 B1-13 全集裁决必要性的直接证据 |
| 归档基线 AR90004 / AR90006 | P05 landing 为空；P06 缺决策章；P09 决策引用堆叠；P10 图片重复；**P11 AR90004 漏裁 12/12** | 观察档，不参与 PASS/FAIL |

**默认目标集不含历史产物**——它们是实施前基线，本就含历史缺陷；检出是预期结果，
等同于额外的反夹具。回归的真实目标是机制层与**新**产物。

## 四、行为验证（**未完成 · 待用户揭盲**）

按 G6 的七字段分项记录。**只有 closure 完成才可表述「阶段完成」**；
未真实到达的阶段一律标「行为未验证」，不因机制已交付而改写。

| 阶段 | artifact | execution | harness | verifier | receipt | closure | target_reached |
|---|---|---|---|---|---|---|---|
| spec | 待填 | 待填 | 待填 | 待填 | 待填 | 待填 | 待填 |
| plan | 待填 | 待填 | 待填 | 待填 | 待填 | 待填 | 待填 |
| coding | 待填 | 待填 | 待填 | 待填 | 待填 | 待填 | 待填 |
| review | — | — | — | — | — | — | **行为未验证**（本批不要求到达） |
| ut | — | — | — | — | — | — | **行为未验证**（本批不要求到达） |
| testing | — | — | — | — | — | — | **行为未验证**（本批不要求到达） |

**最低行为终点（用户裁定 2026-08-25）**：holdout Case 真实到达 **coding 阶段闭环**，
且冻结义务在 coding 逐条留证（K-C1）。review / ut / testing 的机制已交付并经构造夹具验证，
但**不给这三个阶段的完整 PASS**。

### 揭盲前提

冻结清单已重出（[02-冻结清单.md](02-冻结清单.md)，**155** 个文件指纹 + 7 项阈值 + 9 份提示词）。修正轮改了知识内容与机制，前一版 131 个指纹全部作废。
此后到揭盲之间任何改动都会让清单与结论一并作废。

### 实跑命令（由用户执行，G10）

```powershell
python test/story/scripts/run_multi_case.py plan  --all --jobs <N> --isolated-workspaces
python test/story/scripts/run_multi_case.py start --all --jobs <N> --isolated-workspaces `
    --suite-id story-suite-<时间戳> --authorize-non-sandbox
python test/story/scripts/run_multi_case.py poll  --suite-id <id> --wait-sec 0
```

实跑后补跑：

```powershell
python test/story/scripts/check_failure_modes.py --feature <holdout-feature>
```

## 四·补、修正轮的额外交付

用户验证批次 1 后给出 3 条内容反馈 + 3 条检查要求，本轮据此返工：

| 反馈 | 处置 |
|---|---|
| observability 过于复杂 | 重写为**时机导向**：三条渠道各自什么时候记写在开头的表里（那才是要教的），条目收敛到 3 条可独立核对的 |
| 敏感字段应归 security | 删 OBS 的秘密字段条目；SEC-01 扩为「**任何出口**都须先脱敏」，覆盖面写明所有出口统一管——分开管的结果是每个出口各写一遍，写漏一个就是明文泄露 |
| codebase-facts 写成了实现清单 | 重写为**导航规则**（119→69 行）：先找已有实现、去哪找、找到后照惯例写、找不到时「没有」必须是核对过的结论；只留「有/没有」这类不易过期的事实 |
| 新增资源使用知识 | 新建 RES 域（4 条），并接住 `deliverables` 指向不存在的「UX 多语言资源条目」这个 1.0 遗留悬空引用 |

三条检查要求引出一次只读审计，发现的问题**有相当一部分是上一轮实现自己留下的**：

| 检查项 | 审计发现 | 处置 |
|---|---|---|
| 不过拟合 | **零命中**（全目录无 Case 单号与业务名词，夹具题材中性） | 保持，M02 继续回归 |
| 不硬编码 knowledge | **16 处**，其中 10 处藏在 `.md` 里逃过 M01（它只扫代码）。最严重的是上一轮造的 `pattern-baseline.json`——一份复制了模式正文 SHA 与元数据的第二清单，改一个字的模式正文 story 机制就报错，而它唯一的读者零调用、从未生效 | 全部清理；M01 扫描面扩到 `.md`，基准从激活清单派生；新增 M17 拦「查无此物的死判据」 |
| 正向实现不打补丁 | **14 处**：`evidence-rules.md` 保留着被废弃的两表制与 §7 逐域枚举（与三处现行契约方向相反）、3 处死判据（`TEL-05` 等查无此物）、4 个零调用导出、静默降级、4 处「契约文本描述的行为与实现相斥」 | 逐处正向重写；AGENTS.md §2 新增「正向实现」不变量；新增 M16 机械回归 |

**这次审计本身说明了一件事**：只靠维护纪律和自觉，写下的硬编码与死代码不会被发现——
M01 放过 `.md` 这个口径缺口存在了整整一轮，期间新增的 10 处硬编码全部畅通。
所以三条检查都配了 checker，而不只是写进契约。

## 五、总判定（G5）

**当前状态：离线交付完成，行为未验证——不构成批次验收结论。**

按总判定规则，本批最终结论须同时满足：

1. 无机械 BLOCKER —— 当前满足（30/30、9/9、扩展协议 PASS）；
2. 无语义 FAIL —— **待实跑**（verifier 对必答清单的逐行裁决尚未发生）；
3. 风险标记行逐条获裁决 —— **待实跑**（P11 收口在实跑后生效）；
4. 未真实到达的阶段不称已验证 —— 本报告第四栏已按此表述；
5. 守恒/SHA 类只报基线完整性 —— 模式正文与原件逐字节一致的结论表述为「基线完整」，
   未表述「模式可用」。该结论是**一次性交付验证**（SHA 记在冻结清单），
   常驻守恒归批次 3 的版本发布清单：把它做成机制层的常驻校验，就等于在机制层
   存一份模式正文的副本——那正是本轮清掉的第二真源。

## 六、待办（收口后）

- [ ] 用户提供 holdout 材料并实跑至 coding 闭环；
- [ ] 补齐第四栏七字段；
- [ ] 按 G5 出批次验收结论并回写 `STATUS.md`；
- [ ] 新验证的机制入册 `../02-已验证机制参考.md`（带实跑证据）；
- [ ] 新坑入册 `../03-踩坑记录.md`；
- [ ] holdout 作废登记，下批换新。

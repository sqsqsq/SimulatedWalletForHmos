# 批次 1 · 知识应用 · 进展跟踪

| 项 | 值 |
|---|---|
| 状态 | 离线交付完成（含用户验证后的修正轮）→ **待用户揭盲** |
| 需求基线 | [../01-全批次需求记录.md §2](../01-全批次需求记录.md) |
| 方案文件 | [00-方案.md](00-方案.md)（自足版：事实核对 + Step 0–6 + 验证命令） |
| 判据文件 | [01-判据.md](01-判据.md)（定稿于实现之前，K-A/B/C/G/H 共 30 条，含口径与责任层） |
| 冻结清单 | [02-冻结清单.md](02-冻结清单.md)（**第七次重出**：172 文件指纹 + 9 项阈值 + 9 份提示词；提交 `1224d1eb`） |
| 知识重定位方案 | [04-知识重定位方案.md](04-知识重定位方案.md)（评审前置：三类知识的组织、描述与应用；已执行，见文末「知识重定位」小节） |
| 交付报告 | [03-交付报告.md](03-交付报告.md)（四栏分立；七字段待实跑填写） |
| holdout | **待用户提供**（冻结后揭盲，用后作废）；最低行为终点 = 真实到达 coding 闭环 |
| 验收结论 | **未出**——离线交付完成、行为未验证，按 G5 不构成验收结论 |
| 回写 | 收口时更新 ../02-已验证机制参考.md 与 ../03-踩坑记录.md |

## 事件日志

- 2026-08-25 目录建立，待启动。
- 2026-08-25 方案定稿并获用户批准（00-方案.md）。四项裁定：①`test/story` 从 `backup/story-2.x-20260825` 恢复（批次 0 误回退）；②不与 framework 本体强耦合，用扩展能力正确注入（知识义务搭 acceptance 原生追溯链）；③1C 揭盲行为终点只到 coding；④story 附录符合性表单表、兼容性不单列。执行放到新会话。
- 2026-08-25 **Step 0 完成**（详见 [../batch-0-公共基座/STATUS.md](../batch-0-公共基座/STATUS.md)）：测试域恢复、回退验收登记、27 形态台账 + 回归脚本 + 65 夹具、兼容矩阵；顺带修复 1.0 基座 4 类机制缺陷。
- 2026-08-25 **Step 1 完成**：判据先于实现定稿（01-判据.md，30 条，每条带条款出处/口径/责任层/checker/回归形态）。
- 2026-08-25 **1A 完成**（下方小结）。

## 1A 小结 · 知识内容与激活

> **本节记录 1A 当时的交付状态，其中三项已被文末「修正轮小结」取代**：
> 可观测性条目数（5→3）、项目知识形态（事实登记→导航规则）、
> 模式基线（`pattern-baseline.json` 已删——它是机制层的第二份模式清单）。
> 以修正轮小结为准。

### 交付

| 项 | 内容 |
|---|---|
| 规约字段模型 | `constraints/README.md` 重写：列语义单点定义（含**编号**列与「（评审动作）」标记语义）、落法附注三类内容、域清单（新增 OBS）；**删除适用矩阵**（B1-6：Knowledge 不维护阶段消费路由） |
| 可观测性瘦身 | 新建 `constraints/observability.md`（31 行，5 条目 + 判定附注两节，与其它域同量级）；三层合同的五条语义各有条目承载；无渠道模板、无终态模板、无类名 |
| 规约去实现事实 | SEC-01 落法附注的脱敏工具类名/模块路径迁入项目知识；检索式改为「按项目知识登记的日志出口检索」 |
| 项目知识 | `codebase-facts.md`：**更正**原「无埋点封装（已核对）」错误结论（实扫证实三渠道齐备）；新增「可观测性」面（讲特征与规则：统一门面、非阻塞、失败只记本地日志不外传、终态受控枚举、事件 ID 集中登记；**不列调用点**）、「敏感数据处理」面、「编排 SDK」面（含证据缺口登记：SDK 在、业务案例缺） |
| 两模式恢复 | `design-patterns/` 三份；**正文逐字节** = 原件（SHA 校验通过），frontmatter 原件键值守恒，只追加 `roles`/`optional_roles`/`coordinator_role` 三个受控键 |
| 模式基线 | ~~`hooks/shared/pattern-baseline.json`~~ **已于修正轮删除**：它在机制层存了一份模式正文的 SHA 与元数据副本，等于「机制层维护 knowledge 的第二份清单」。正文逐字节一致改为**一次性交付验证**（SHA 记在冻结清单），常驻守恒归批次 3 的发布清单 |
| 激活清单 | `manifest.yaml` 新增 `knowledge_activation`（facts/constraints/patterns 三类，文件互斥）；索引 README 不登记 |
| 派生模块 | `hooks/shared/{paths,yaml-lite,knowledge}.mjs`：域前缀/条目/模式/角色全部运行期派生，代码零域名字面；**派生为空一律 throw** |

### 验证

| 项 | 结果 |
|---|---|
| 派生 | 7 域 / 15 条目（14 产生代码要求 + DLV-02 评审动作豁免）/ 2 模式（角色与 coordinator 齐全）/ 2 项目知识 |
| 派生为空出声 | 四种路径（清单缺失、某类为空、未知类型、文件缺失）全部响亮报错，无静默空集 |
| 知识层自检 | 0 问题（规约无实现事实、无阶段矩阵）。**修正轮补**：该自检当时零调用（死代码），已接进 spec post_check |
| yaml-lite | LF / CRLF 双向解析一致（嵌套映射、行内序列、块式序列、`- key:` 项映射） |
| 形态回归 | 27/27（M08 模式基线守恒能力已建，`pending_capability` → `fixed`） |
| 框架扩展门禁 | `--phase extensions` PASS 6/6 |
| 单测 | Story 132 passed / Tools CLI 16 passed |

### 顺带清理（删除机制后的悬空引用，坑 #31 要求同批清零）

- 删 `hooks/constraint-application.md`（其唯一职责是指向已删的适用矩阵）；
- 5 个下游 overlay 重写为「按激活清单逐条」（判据内容由 1B/1C 按需求重写，此处只保证自洽）；
- 两个 spec 注入件对该文件与矩阵的引用改为指向激活清单；
- `lint-rules.mjs` 的知识文件名**从扫目录改为从激活清单派生**（B1-7：阶段不扫目录），派生失败告警不静默。

### 判据达成

K-A1…K-A8 全部 PASS（K-A4 三层合同语义保留属语义层，随 1A 交付评审）。

## 1B 小结 · spec 识别 → plan 冻结

### 交付

| 项 | 内容 |
|---|---|
| spec 三出口独立成章 | 模板新增 §10 规约约束要求（按命中条目派生）、§11 设计模式候选登记；卷首写明三章边界（§9 有什么 / §10 必须满足什么 / §11 可选什么）。**删除 1.0 写死的「§7 四处」**（UX 适配要求/触发频次/存储兼容/个人数据采集）——那是把域清单硬编码换了个地方 |
| story 知识判定登记 | 新增 `AR/story-src/knowledge.json`（scaffold 建骨架含 `_example`）；装配器新增 `renderKnowledgeAppendix()`/`renderPatternAppendix()`，渲染附录「规约符合性」**单表**与「设计模式候选」表；注册件条目集须与激活条目集完全一致 |
| 兼容性并表 | 按用户裁定取消独立「兼容性自检表」——同一件事分两张表登记，两表迟早对不上且读者说不清该查哪张；现所有域同表同粒度 |
| 章节合同 | `12-compliance` 去掉 `form.tables` 配额，改 `transcribe_note`：本章讲影响与取舍并指向附录，不复述附录的行 |
| 复述判定 | `hooks/shared/paraphrase.mjs`：比较字段/来源/方向/规范化**四维定死**；规范化保留 `. - _ /` 与字母数字；相似度只排序不判定 |
| 冻结结构 | `contracts.yaml` 顶层 `knowledge_freeze`（obligations 五要素 + patterns 角色投影）；`hooks/shared/freeze.mjs` 负责读取与实体引用解析（集合名对齐 contracts 既有章节，不自造平行命名空间） |
| 四阶段验证要求 | **搭框架原生链**：每条义务的 `criterion` 指向 `acceptance.yaml` 的验收条目，`ut_layer` 即四阶段分派单源（`device` = UT 天然不适用，就是「显式不适用」的载体），不另建四键分派表 |
| 全集语义裁决 | `hooks/shared/pre_verifier.mjs`：必答清单是**全集**，风险标记只排序不筛选；判据 id 从 overlay 按 `knowledge_` 前缀**运行期派生**，不在代码里维护第二份清单；清单显式要求逐行输出并说明「漏裁与判 PASS 是两回事」 |
| 门禁 | `hooks/spec/post_check.mjs` 新增出口结构/编号粒度/在册性/三方 ID 集合一致/复述；`hooks/plan/post_check.mjs` 新增决策章位置/冻结结构/义务三重锚定/验证要求/模式角色/复述 |
| overlay | spec 的 `spec_constraint_echo` 替换为 `knowledge_spec_exit_substance` + `knowledge_candidates_registered`；plan 重写为 `knowledge_freeze_substance` |
| 写作指令同向 | spec/plan/story 三处注入件均含**同一组自检三问**（有没有本需求自己的名字 / 遮住编号还能否指导编码 / 是不是原文换了说法），与判据同批同向 |

### 验证（构造夹具，逐分支）

| 场景 | 结果 |
|---|---|
| 复述判定六类对抗 | 纯复制（含标点差异）→ PURE_COPY；原文+自造专名 → SUSPECT（非拦截，靠全集裁决兜）；同义改写 → SUSPECT；真设计 → CLEAN；要求列不参与比较；方向性（来源⊆输出不算抄）正确 |
| spec 门禁反例 | 6 条判据全命中：编号粒度退化、要求列复述、评审动作条目误入、候选不在册、story↔spec 集合双向差集、acceptance 缺条目 |
| plan 门禁反例 | 14 条判据全命中：决策章位置、义务集差集、复述、landing 空、anchor 自指、缺 step/criterion/review_focus、实体解析失败、模式缺 instance/角色/实体 |
| 正例 | spec 与 plan 均 PASS |
| 必答清单 | spec 3 行 / plan 2 行全集注入；信号为「无」的行同样在清单内；判据 id 正确从 overlay 派生；coding 阶段（overlay 尚无知识判据）正确不注入 |
| 台账 | 27/27（M11 全集裁决、M12 规范化、M14 判据与指令同向三项能力已建，`pending_capability` → `fixed`） |

### 判据达成

K-B1…K-B12 机械部分全部 PASS；K-B5（逐行是不是本需求的设计）属语义层，由 verifier 在实跑时裁决。

## 1C 小结 · 下游只读冻结结果

### 交付

| 项 | 内容 |
|---|---|
| 下游基座 | `hooks/shared/downstream.mjs`：统一读账本；**冻结缺失 → 四阶段一律 BLOCKER 并指路回 plan**（K-C4） |
| coding | `landing` 末段标识符与模式角色实体，在契约点名文件中按 `\b<名>\b` 检索；文件级存在性交框架原生门禁，不重复 |
| review | 审查报告须有「知识义务复核」表，行集 = 义务集；「不适用」的**依据列**须非空（按列名取值——把落实位置列算进来会让空依据蒙混过关） |
| ut / testing | 按 `ut_layer` 分派：反向层报「不适用（<层>）」并计数，正向层须有覆盖/引用证据。**产物未建**与**产物建了却零引用**分开判——混成「空集就放过」会让后者静默溜走 |
| 四个注入件 | 只含账本判据与「显式不适用优于静默跳过」，**不含知识正文**（下游零注入知识） |
| 四个 overlay | 判据 id 统一 `knowledge_` 前缀（pre_verifier 按前缀认知识判据）；内容改为消费冻结账本，不再让每阶段各自回读规约 |
| 对抗夹具 | `fixtures/knowledge/adversarial.json` 九个分支各带预期 + `scripts/check_paraphrase_branches.mjs` **调用真实模块**逐分支比对（不重实现，避免测试对测试） |
| 双实现绑定 | Python 侧（产物回归）与 JS 侧（门禁）各有一份复述判定是跨语言被迫的，但**判据只有一个**：M15 checker 在同一批样本上比对两者，漂移立刻报出 |

### 验证（构造夹具，逐分支）

| 场景 | 结果 |
|---|---|
| 冻结缺失 | coding / review / ut / testing 四阶段全部 BLOCKER 并指路回 plan |
| coding 实体级 | 检出「落点符号不存在」+ 3 个缺失模式角色 |
| review 留证 | 检出「不适用但依据列为空」「采用的模式无复核行」 |
| ut 分派 | 无覆盖 → 阻断；有覆盖 → PASS；`device` 层 → 报不适用不报错 |
| testing 分派 | 计划零引用 → 阻断；已引用 → PASS；`unit` 层 → 报不适用；产物未建 → 放过交原生门禁 |
| 对抗九分支 | 9/9 符合冻结预期（含方向性、空字段、标识符不折叠三项边界） |
| 台账 | 28/28（新增 M15 分支漂移；P01–P06、P11 七个产物形态因能力已建转 fixed） |

### 历史回归（对实施前基线，证明新能力有区分力）

`check_failure_modes.py --feature DTS90006` 检出 2 处：
`P06` 知识决策章在第 142 行、晚于设计章第 49 行；`P11` verifier 漏裁 3/14 条。
默认目标集**不含**历史产物（它们是观察档不是回归目标），显式指定才跑。

### 判据达成

K-C1…K-C5 全部 PASS；K-G1/K-G2/K-G3 PASS。
K-H1（holdout 到达 coding）属行为层，待用户实跑。

## 修正轮小结（用户验证反馈）

用户验证后给出 3 条内容反馈 + 3 条检查要求。**机制未返工**——1B/1C 按「从激活清单派生」实现，
本轮新增一个域、改写两个域，机制代码一行没动，这本身就是「不硬编码」的实证。

### 内容返工（fix1）

| 反馈 | 处置 |
|---|---|
| observability 过于复杂 | 转为**时机导向**：三条渠道各自什么时候记写在开头的表里（那才是要教的东西），条目 5→3（时机不混用 / 共用执行标识 / 失败不改业务）。密度递减是时机的推论不再单列 |
| 敏感字段应归 security | 删 OBS 秘密字段条目；SEC-01 扩为「**任何出口**都须先脱敏」，覆盖面写明日志/反馈/度量/采集/对外接口统一管——分开管的结果是每个出口各写一遍，写漏一个就是明文泄露 |
| codebase-facts 写成实现清单 | 重写为**导航规则**（119→69 行）：总则「先找已有实现」+ 定位规则表（去哪找一律写层与角色，不写路径/类名/API）+ 找到后照惯例写 + 找不到时「没有」必须核对过；只留「有/没有」这类不易过期的结论 |
| 新增资源使用知识 | 新建 RES 域 4 条（图片优先级 / ≤10KB / 字符串复用优先 + 依赖倒置时允许临时硬编码但须显式告知 / 新增按惯例定义并联动翻译回稿），顺带接住 `deliverables` 指向不存在的「UX 多语言资源条目」这个 1.0 遗留悬空引用 |

### 审计与清理（fix2）

三条检查要求引出一次只读审计，**发现的问题有相当一部分是上一轮实现自己留下的**：

| 检查项 | 发现 | 处置 |
|---|---|---|
| 不过拟合 | **零命中** | 保持 |
| 不硬编码 knowledge | **16 处**，10 处藏在 `.md` 里逃过 M01（它只扫代码）。最严重的是上一轮造的 `pattern-baseline.json`：复制了模式正文 SHA 与元数据的第二清单，改一个字的模式正文 story 机制就报错——而它唯一的读者零调用，从未生效过 | 全部清理 |
| 正向实现不打补丁 | **14 处**：`evidence-rules.md` 保留着被废弃的两表制与 §7 逐域枚举（与三处现行契约方向相反）、3 处死判据（`TEL-05` 等查无此物，模型照着它找基准只能自己编）、4 个零调用导出、静默降级、4 处「契约文本与实现相斥」 | 逐处正向重写 |

### 不变量落地（fix3）

按「对 AI 有效」判断落两处：**AGENTS.md 让维护 AI 知道要这么做，台账 checker 让没这么做会被发现**。

| 不变量 | 机械回归 |
|---|---|
| 反过拟合 | M02（已有） |
| 不硬编码 knowledge | **M01 扫描面扩到 `.md`**（基准从激活清单派生，区分真实标识/占位形态/反例说明）+ **新增 M17**（查无此物的死判据） |
| 正向实现 | **新增 M16**（死代码 / 静默返回空集合 / 跨轮次待办标记），AGENTS.md §2 补对应不变量 |

**这次审计说明的事**：只靠纪律与自觉，写下的硬编码与死代码不会被发现——
M01 放过 `.md` 这个缺口存在了整整一轮，期间新增的 10 处硬编码全部畅通。
所以三条检查都配了 checker，而不只是写进契约。

### 验证

形态回归 **30/30**（新增 M16/M17）；复述分支 9/9；单测 132+16；`--phase extensions` PASS；
plan 演练通过；知识派生 **8 域 17 条目**（机制代码零改动）。

## 知识内容返工（第二轮 · 用户验证反馈）

用户指出三处，都是上一轮**矫枉过正**造成的。只改知识内容，机制零改动。

| 反馈 | 根因 | 处置 |
|---|---|---|
| codebase-facts 被过度精简，「模型能理解有什么知识、怎么用、怎么找吗」 | 「写规则别穷举」被我做成了「纯抽象规则 + 删光内容」。**特征本身是有内容的**——它介于「穷举实例」与「纯导航」之间：讲清长什么样、怎么认出来，不列调用点。冻结 C7 本就写着「Logger、VOC、Chart API 及代码位置属于经核实的项目知识」 | 恢复六个能力面的特征（69→88 行）：三渠道形态与事件类型、共用字段的链式 setter、统一门面三档、非阻塞失败不外传、终态受控枚举、脱敏分层、存储形态与已核实未使用的介质、开关声明形态、对外面三处认法、无端云封装 |
| 为什么强调编排 SDK | 把**项目的既定前提**写成了「待核实的证据缺口」——项目交付了两个设计模式文档，SDK 存在就是前提。且「有没有使用先例」对选型没帮助（该不该用取决于需求有没有复杂分支） | 删掉该面；依赖关系模式索引的清单表里已有 |
| 可观测性丢了日志/VOC/Chart 三个原始语义 | 我改成了「用户问题反馈/业务度量」，而**工程代码里就叫 VOC 和 Chart** | 恢复原名，每个后跟一句「回答什么、给谁看」 |
| **VOC 上报时机不对**：正常路径也要报 | 实质错误，我写的是「只在失败或中止时」 | 改为**关键路径事件**（关键步骤的进入与完成、状态标记变更、用户中止与失败） |
| RES 4 条太多、描述冗长；security 也冗长 | 我改过的三个正好是最长的（34/37/41 vs 原生 24–27），冗长模式一致——**把论证展开写了** | 判据「只陈述不论证」：RES 4→2 条（27 行）、security 覆盖面论证压成一句 + 验证列改回可操作检索式（32 行） |

### VOC 时机的三条证据（用户是对的）

1. **冻结 C7 原文**：「Plan 按业务流程和步骤设计日志检查点、**VOC 关键路径事件**和 Chart 业务步骤终态事件」；
2. **C7 的用途定义**：「VOC 用于**无本地日志时远程定位**」——只报错误就还原不出用户走到哪一步；
3. **工程实现**：门面 `logAndReport` 是 **info 档**（info 日志 + VOC），info/error/debug 三档**都发 VOC**；
   且 VOC 用平台的 `BEHAVIOR`（行为）事件类型，Chart 才是 `STATISTIC`（统计）——行为事件本就不限于错误。

2.x 那版写的「VOC 记出问题的那一次」本身就不完整，我继承并强化成了「只在失败时」。

### 职责分离核对

实现特征（`hilog` / `BEHAVIOR` / `STATISTIC` / 非阻塞 / 受控枚举 / 链式 setter）**全部**落在项目知识；
规约侧（关键路径 / 采集处 / 10KB / 完全一致 / 临时硬编码）只写要求并引用项目知识，不复制实现事实。
知识层自检 0 问题验证了这一点。

### 验证

知识派生 **8 域 15 条目**（RES 由 4 减 2）；自检 0 问题；跨文件引用全部可达；
形态回归 30/30；复述分支 9/9；单测 132；`--phase extensions` PASS。
冻结清单第三次重出（155 文件指纹，提交 `d06ed127`）。

## 知识内容返工（第三轮 · 划清 facts 与规约边界）

用户指出：facts 里把日志/VOC/Chart 又讲得那么细，跟规约重复；且「一会啥都不说，一会又特别详细」，
facts 里仍有维护式、打补丁式描述。**核实成立**——上一轮为了「把内容写回来」，我在 facts 里
重述了一遍渠道语义，还加了字段清单与实现层细节，密度也只有可观测性那一面畸高。

### 边界（本轮定下的分工）

| 归谁 | 回答什么 | 例 |
|---|---|---|
| 规约 `observability.md` | 哪条渠道、什么时候记、必须满足什么 | 「VOC 记关键路径事件」「三渠道共用执行标识」 |
| 项目知识 `codebase-facts.md` | 这个仓有没有、在哪、照什么写 | 「日志在 `shared/log/`、上报在 `shared/ha/`，有统一门面分三档，终态取受控枚举」 |

规约不复制实现事实（自检已拦），facts 也不重述渠道语义——**上一轮只做到了前半句**。

### 处置

| 问题 | 处置 |
|---|---|
| 可观测性面重述渠道语义 + 过细 | 3 列表格 + 4 条细则 → 5 行：位置、门面三档、终态枚举、非阻塞。删掉字段逐项清单、`BEHAVIOR`/`STATISTIC` 事件类型、内外层 try/catch 层数——这些看代码即知，写进知识只会随重构变假话 |
| 维护者向内容 | 删「本文件写特征不列实例」的写作纪律（属 AGENTS.md §2.2）、「前身栽在凭平台常识写埋点 API」的历史教训、「换工程复用」整节（给适配者看的，属批次 3） |
| 打补丁式描述 | 删各面的理由尾巴（「之后谁也说不清该照哪个」「事后没人分得清是有意还是没找到」）与「这就是规约 XX 的既有实现」这类对应关系说明 |
| 密度不齐 | 六面统一到 2–5 行，不再一面详尽五面从简（88 → 61 行） |

### 验证

知识派生 8 域 15 条目不变；自检 0 问题；无悬空引用（`编排 SDK` 仅存于模式索引的依赖列，属正确归属）；
形态回归 30/30；复述分支 9/9；单测 132；`--phase extensions` PASS 6/6。
冻结清单第四次重出：155 行全量重算，**漂移 4 个**（即本轮改的四个知识文件），提交 `7eebc2c4`。

## 知识重定位（评审前置 · 方案见 04-知识重定位方案.md）

用户在评审前指出：三类知识的**定位问题**没解决，导致反复打补丁——知识重复、冗长、过度精简、丢失、职责混乱；
规约与模式各有目录与 README，facts 没有；知识里混着维护信息。**整体设计问题，不是某个文件的问题。**

### 根因（实证）

- `codebase-facts.md` 在批次 1 内重写 5 次：72 → 119 → 69 → 70 → 88 → 60 行，无一收敛；
- 定位散落六处且互相矛盾：AGENTS.md §2.2「代码可见的不写入知识 / 不列实现形态、工具类名单」 vs 冻结 C7
  「Logger、VOC、Chart **API 及代码位置**属于项目知识」；spec 侧把 facts 当索引（「定位规则」「回灌」），
  plan 侧当内容源；`constraint-usage` 说规约两问、AGENTS 说三问；「从正文派生」与实现（frontmatter）不符；
  `indexes[]`、`facts[].facets/body`、`entries[].force/hitWhen/verify` 派生后零消费方。
- 两个 README 里的「新增域改这张表并在 manifest 加一行」「修订走评审」「第二份真源迟早对不上」随激活清单进了模型 prompt。

### 处置

| 项 | 结果 |
|---|---|
| 定位真源 | 新建 `knowledge/README.md`（14 行）：三类各一行是什么、怎么用；哪里找什么。消费方与 AGENTS 只引用 |
| 组织对称 | `knowledge/facts/`：画像 + 工程惯例 + `facts/README.md`（12 行读法）；与 constraints/、design-patterns/ 同构 |
| codebase-facts | **按 1.0 形态**（用户裁定）：一面一节、真实路径/类名/API、「无（已核对）」带零命中检索项；57 行。与 1.0 差异三处：删「怎么维护」段；1.0「无埋点」错误结论改为可观测性三渠道（`Logger` / `WalletHAManager` + 两个 Builder / `WalletFuncResult` / `WalletHAEventID`）；补敏感数据处理面（`MaskUtil`、业务专属 `CardNumberMaskUtil`）。8 个类名全部 grep 命中活树 |
| 两个索引 README | 剥离定位与维护信息；强制力、执行体并进列义表。98→37、79→52 行 |
| 尺度 | 用户裁定「给类名就行，模型知道怎么用」：知识只给关键信息或规则；写法判据表进 AGENTS.md §2.2，不进知识 |
| 消费侧 | 删「定位规则 / 取证导航 / 回灌」（7 处）；决策件落点统一为「决策件的开放决策项」（原注入件示范 `review.md` 裸文件名，会被 lint 拦——**会让 holdout 的 spec 闭环失败**，故并入本轮）；「从正文派生」→「从 frontmatter 与正文派生」 |
| 机制 | `knowledge.mjs`：删无消费者的派生字段；`selfCheck` 扩到全部激活文件、四项判据全部派生（源码路径 / 阶段矩阵 / facts 引规约编号 / 知识指向机制——目录名 `readdir` 实取，不写关键词表） |
| 回归 | 新增 **M18-knowledge-boundary-leak**：`run_self_check.mjs` 调真实 `selfCheck`（不重实现），反夹具报出 3 处、正夹具 0 |
| AGENTS.md | §2 新增不变量「知识不含维护信息，定位只写一处」；§2.2 改为维护者判据与动作（接收从 README 剥出的登记步骤）；删与 C7 相斥的两条 |

**唯一推迟项**：模式文档「契约投影」表含 `contracts.yaml` 字段名（C4 字面相斥）——plan post_check 正靠它校验投影，
功能正确，正文受 B1-5 冻结为原件逐字节；对批次 1 验证无影响，动它须先解冻。登记到批次 2。

### 验证

派生 8 域 / 15 条目 / 2 模式 / 2 facts / 4 索引，facets 2+7；自检 0；`定位规则|取证导航|回灌` 在机制与 AGENTS 零命中；
台账 **31/31**（含 M18）；复述 9/9；单测 132；`--phase extensions` 6/6。
冻结清单第五次重出（172 文件；+2 索引、2 处改名、+15 测试域文件），提交 `9cc83c4e`。

### 评审补两处（提交 `aa549a50`）

**可观测性规约，讨论五轮才定**——定论与依据都写进 AGENTS.md §2.2：
- 是规约（只有规约条目进义务链），但**规约说必须做什么、facts 说这里怎么做**：「日志记全流程 / VOC 记关键路径事件 / Chart 记步骤终态」一渠道一条；用哪个类、标识经 `setFuncID`/`setSubFuncID` 带、密度递减、上报非阻塞是惯例，归 facts §5；
- 用模型本来就懂的概念名（日志/VOC/Chart）直说要求，不为「工程无关」抽象成「三种能力」；
- 不设前置定义表让条目「按上表」，每条自述，与其它域同形；
- 「失败不改业务」不立条：`report()` 返回 `void`、异常内部捕获，是封装性质。
- 摆动的根因：我把「什么时候打哪条」这种惯例当成规约条目，于是 3 条→时机表→5 条→2 条→能力抽象来回改。规约 36→24 行。

**举例不进条件**：路由表「（多步云侧调用、回滚或补偿分支）」会让无云侧调用的多步分支被判零命中——改为正面判据；
画像卡种与交互方列举、SEC-01 敏感字段类型列举加「等」。模式文档正文同类举例（`decision-tree.md:4,23,32,34`、
`page-interaction.md:34,65`）受 B1-5 冻结，先未动；**用户裁定「可以改」后解冻**（提交 `1224d1eb`）：
applies_when 与 §1/§2 的举例改正面判据「每个分支自身是多步的、有自己的失败处理」，其余括号例改「如」。
B1-5 的「正文逐字节 = 原件」自此收窄为「结构、骨架、角色、frontmatter 其余键值不变」；模式清单与验证清单未动。

验证：自检 0；OBS 仍 3 条；台账 31/31；复述 9/9；单测 132；extensions 6/6。冻结清单第六次重出（5 个知识文件），第七次重出（2 个模式文件）。

## 实跑缺口修复（方案见 05-实跑缺口修复方案.md · T0–T18）

首次真实实跑（suite `story-suite-20260825-162511` / Case `split-interactive` / AR90006，story→plan→coding）
把批次 1 的**结构**证成了、**内容与收口**证伪了：知识链四方 ID 一致、11 条义务贯穿、facts 在编码层被真用上
（`Logger`/`MaskUtil`/`NavPathContext`/`AppStorage`/`WalletHAManager` 全出现在新代码，plan 没点名的入口模型自己回去取了）；
但 verifier 的逐行裁决**一行没落盘**（P11 漏 10/11）、`step` 全是验收编号的副本、plan.md 与契约的落点各写各的、
两处图链断、条件域被逐条写「不涉及」、扩展 coding 门禁跑了却零痕迹。**没有一条被现有门禁抓到**——这是本轮要补的。

### 根因与处置（都是正向实现，不是给现状打补丁）

| 缺口 | 根因 | 处置 |
|---|---|---|
| 逐行裁决不落盘 | 注入了清单，但没有任何一处核对 verifier 是否照做；框架 post_verifier 在 verifier 之前触发，读不到报告 | 挂到 post_check：闭环第三步（主 agent 重跑 harness 回填凭证）时报告必然已在。`hooks/shared/verifier-report.mjs`，spec/plan 共用（K-B16、P11 口径收窄） |
| 判定负担 vs 时机判断 | 条件域也要逐条写「不涉及」，判定成了体力活 | 时机由规约自己的 `applies_when` 承担：条件域先判域、常驻域逐条。判据在 `hooks/shared/adjudication.mjs`，装配器与 spec 门禁**共用一份**（K-B13、P15） |
| 同一结论多份 | 附录、出口章、契约各写各的 | 登记源是真源：附录由它渲染、出口章与它同文（K-B14、P16）、必答集从它派生。裁一次，不裁三遍 |
| step 悬空 / 落点两处不一致 | 门禁只查「实体存在」，不查是不是业务步骤、不查 md 与 yaml 一致 | `step != criterion` 且只认 `prd_function`/branch；plan.md 义务表落点须含契约 landing 的末段（K-B17、P17/P18） |
| 图链断没人管 | 形态守恒只数图的条数，数得到「有一张图」，看不出它打不开 | 归档件红线增图片可解析（P14）。对真实产物复跑，正是 `story.md:209,213` 那两处 |
| 门禁通过不留痕 | 框架 dispatcher 只在 `ok:false` 时记结果 | 六个 post_check 通过也写 `<phase>/reports/ext-post-check.json`（判据 id、结果、被检产物指纹）——K-C1 从此有证据 |
| 实例 hook 串台 | state 没盖章时按 grace 窗口把**第一个结束消息的会话**盖成所有者 | 归属改按一手记录：本会话 transcript 里有没有跑过这条 harness（`.claude/hooks/hook-session-evidence.mjs`）。写报告还要加一条「本会话调过 verifier」，且**目录不存在就不建**——实测把已迁走的 feature 目录写活过 |
| 协调器不自动应答 | 只在「上一次回复被消费了」时才发下一关，而「被消费」靠 poll 恰好观测到状态跳变 | 回复身份改由关卡编号决定（`last_replied_turn`）；删掉 `consumed` 状态与 `reply_consumed` 事件，不留两套状态机 |
| 到不了 coding | 四个 Case 的 `end_phase: plan`，且 prompt 写「不修改产品源码」——与编码阶段直接冲突 | 三个 Case 改 `end_phase: review` 并放开产品源码；`split-two-ar` 保持 plan（它观测的是拆分穿过上游三层，注释已写明，知识链在下游的传递由其余三个覆盖） |
| UI kit 拦死所有 Case | 实例从未配 `paths.ui_kit_target_dir`，仓内零 Maison 组件；架构声明了 `03-CommonBusiness` 层但目录不存在，scaffolder 四级解析落到 halt | 维护者前置：配 `05-SystemBase/CommUI/src/main/ets/maison_ui_kit` + 跑 profile 的 scaffolder + 从 CommUI 出口导出 9 个组件。写进 TEST.md §2 起跑前检查 |

### 知识内容（用户三次纠正后定稿）

- VOC **默认用门面** `logAndReport`/`logErrorAndReport`/`logDebugAndReport`，只在「只上报不打日志」时才用 `vocBuilder(id, desc).report()` 且不额外 set；
- 同一处代码 VOC 与 Chart 二选一（实跑里 3 个终态点两个都发）；
- 新增「资源」一面：资源文件的真实位置与「改到的资源 json 要列进契约文件清单」——实跑 `resource_integrity` 判 75 处未定义，根因是契约没列资源文件；
- 日志脱敏那句**返工两次**：先写「账号、卡号、手机号先 MaskUtil」被指出是把被测需求的字段硬编码进知识，改成「参数默认 `%{private}s`」又被指出是在教平台 API 用法。定稿为一句「不打敏感字段，确需记录先脱敏」并指向安全隐私规约与敏感数据处理一面——**判什么算敏感、要脱到什么程度归规约，facts 只说这个仓在哪脱**。

### 与方案的差异（以仓内为准）

- **T13 不做**：方案要求把 `test/story/mock-data/` 加进 workspace 白名单，但仓里没有这个目录、也没有任何 Case 引用它。加一个指向不存在目录的常量就是没有消费者的死代码（正向判据 ③）。
- **T14 只改三个 Case**：`split-two-ar` 的 `end_phase` 保持 `plan`，理由见该文件注释（上一轮的裁定，本轮没有新证据推翻它）。

### 验证（本轮全绿）

台账 **36/36**（新增 P14–P18，P11 口径收窄为登记源派生 + 同行含裁决词）；复述分支 9/9；单测 **137**
（新增 hook 归属 3、协调器新关卡 1、JS/Python 必答集口径绑定 1）；`--phase extensions` 6/6；
知识自检 0；机制层负面扫描无新增命中。

对回灌的 AR90006 副本（`scratch/probe/`，只读）直调两个 post_check 的**区分力验证**：
spec 报域级判定缺失 + 出口章 8 行不同文 + 图链 2 处 + 逐行裁决漏 18/18；
plan 报落点 6 处不一致 + step 6 处等于 criterion + 漏裁 13/13。旧产物报错不是要把它改绿，是证明新门禁抓得住实跑那批问题。

## 实跑（第二次起跑 · suite story-suite-20260826-1310）

第一次起跑（`-1230`）在范围关卡死锁，25 分钟后停掉。**根因是测试方法，不是被测能力，也不是本轮改动**：

`split-interactive` 的 prompt 第一句是「完整阅读全部同轮材料，**生成并收口** AR/design.md」，
第二段却是「具体拆法我会在关卡中说明」——收口以走完范围关卡为前提，两句要求的是相反的事。
被测模型合规地按 `scope_gate` 的代选规则（代选永不选拆分）取 `carry_all` 并收口，人的拆分意见
随后无处落地（`decide` 在 `complete` 后结构性拒绝），流程死锁。

**上一轮为什么没暴露**：`20260825-162538` 逐字相同地代选、收口、撞死锁（`:890` `:1076` `:1111` `:1307`），
但那时协调器不自动发第 2/3 关回复（T12 修的 bug），宿主手动 `reply` 推了一把，顺带把死锁绕开了。
T12 修好后回复按时自动送达，模型不再被人工推，缺陷才原样现形。**本轮改动的作用是让它现形，不是造成它。**

处置（提交 `27b2abb8`）：
- 两个 `interactive` Case 的 prompt 去掉完成型授权，改成「在范围关卡停下等我回话，本轮范围由我定」；
- TEST.md 新增 §0.2 **宿主角色定义**：实跑期间宿主是需求方/评审人，不是维护者——读送审的归档件、
  只说需求侧的话、卡住时重申意图让它自己找出路；**不点名脚本、命令、文件、字段、门禁与判据**。
  这条是被自己犯的错逼出来的：第一次死锁时我写的解锁回复里直接给了「重新初析开新一轮…不要手改
  阶段状态文件」——维护者口吻，等于把解法喂过去，那一刻测的就不是被测模型能不能自己走通。

第二次起跑的行为（prompt 修好后）：材料关卡 → **范围关卡如期停等**（不再代选）→ 拆分定案 →
进 spec；模型还主动指出「评审意见发早了，Story 还没产出、无从处置」——时序判断正确。

### 登记项去向（2026-08-27，用户核定）

| # | 项 | 去向 |
|---|---|---|
| 1 | ~~spec 逐行裁决门禁未在真实链路执行~~ → **事实修正**：执行了且 PASS（18 行）；真缺陷是「重跑 harness 回填」这一步不由流程保证 | 批次 2 已修（写进 spec/plan 注入件） |
| 2 | K-C1 只查落点名字 | 批次 2 判据强度 |
| 3 | 「减负担」未实证 | 与批次 2 共用实跑 |
| 4 | K-H1 holdout | 与批次 2 共用实跑（需用户新材料） |
| 5 | story flow 收口后无范围修正回路 | **撤销**——用户裁定不是缺陷：范围在 story init 定死，改范围就重跑 story init |
| 6 | `review_feedback` 不认阶段语义 | 测试域 → batch-0 STATUS §6 |
| 7 | 续话多行只送首行 | 测试域 → batch-0 STATUS §6 |
| 8 | UI kit 模板上游修复 | 批次 3 运行前提清单（01 B3-5） |

### 脚本回复按关卡编号匹配，不认阶段语义（**已修，2026-08-27**）

`turn=3` 的 `review_feedback`（评审意见）被发到了范围拆分的第三级关卡上——脚本步骤只声明
`expected_turn` 与 `expected_kind: story_gate`，而范围关卡与评审关卡的 kind 相同，编号又恰好对上。
Case 里 `suggested_reply` 与 `review_feedback` 本就是两个独立字段，生成 interaction_script 时
应当带上它期望的阶段（评审意见只在归档送审之后才发），不能只靠 turn 编号排队。
本轮实跑先由宿主以人话澄清时序绕过。

**修复**（批次 2 开工前置 T-1）：脚本步骤支持 `expected_phase` 与 `requires_archived`
（后者读 `AR/story-flow.json` 的归档态），对不上一律回落人工并给出
`interaction_phase_mismatch` / `interaction_precondition_unmet`；`split-interactive` 的
评审意见步骤已加前提；回归 3 条（`test_multi_case_cli.py::GatePreconditionTest`）；
TEST.md §3 列出四类 reason 与宿主该怎么处理。

### 登记项 #1 的事实修正（2026-08-27）

上面「实跑缺口修复」小节记的「spec 的 `knowledge_adjudication_persisted` 停在 `NOT_APPLICABLE`」
**不成立**——那是 14:23 的中途快照。本轮实跑最终的
`spec/reports/ext-post-check.json`（17:33:41）是 `PASS 逐行裁决齐备（18 行）`，
verifier 报告在 `spec/reports/verifier.report.md`（16:22）。

真实缺陷是**时机不由流程保证**：框架的闭环四条件（trace + harness + verifier + 回执）
里没有「verifier 之后重跑 harness」这一步，`spec/SKILL.md` 与 `plan/SKILL.md` 的闭环判定
文本一致，都不含它；而 `verifier-report.mjs` 的判据假设了这一步存在。本轮那次 PASS 是模型
在 coding 之后回头补闭环凭证时顺带跑出来的。批次 2 已把这一步写进 `hooks/{spec,plan}/on_context_load.md`。

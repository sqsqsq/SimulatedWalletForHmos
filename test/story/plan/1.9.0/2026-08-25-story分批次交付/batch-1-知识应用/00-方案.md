# 批次 1 · 知识应用 — 自足实施方案（供新会话任意模型执行）

> **执行者须知（先读）**
> 1. 你是 `test/story` 测试域的**外层维护者**，被修改对象是 `doc/extensions/`（Story Extension）与 `test/story/` 测试设施。**不得**调用 `/story`、`/spec`、`/plan` 等 Skill、Hook、Harness、Verifier 来优化它们自身（`test/story/AGENTS.md` §1）。
> 2. `framework/` 只读，不新建任何文件；临时脚本放仓库根 `scratch/`。
> 3. 需求 SSOT 只有一份：`test/story/plan/2026-08-25-story分批次交付/01-全批次需求记录.md`（下称 **01**）。§1 的 G1–G10 每批都要满足；§2 的 B1-1…B1-15 是本批需求。同目录 `02-已验证机制参考.md`、`03-踩坑记录.md` 只作参考与避坑，**严禁照抄** 2.x 代码；本方案 §9 给出取参考件的命令。
> 4. 开工前先做 §2 的事实核对（每条附命令），核对结果与本文不符时**先回报用户**再动手。
> 5. 交付到「离线全绿 + 冻结清单 + 报告」为止；真实 CLI 实跑由用户手动执行（G10），除非用户明示。
> 6. 文档里禁止写「已实施」；实施事实与行为验证结论分栏记录（坑 #35）。

---

## 1. 背景与用户裁定

- 上一轮（2026-08-24 判据内容化）多特性交织不收敛 → 用户裁定：产品侧整体回退 1.0（提交 `19977c98`），按四批次正向重写；批次 1 = **知识应用**。用户原话级诉求：**知识在合适的步骤中被看到、被应用、被传递——不能仅传递**。
- 批次 1 内部顺序 **1A 知识内容 → 1B spec 识别 / plan 冻结 → 1C 下游传递**，前一子步离线全绿后才进下一子步；批次 1 的验收结论在 1C 后统一给出。
- 2026-08-25 用户对本方案的四项裁定：
  1. `test/story/**` 从 `backup/story-2.x-20260825` 分支恢复（它被误回退了）；
  2. **不与 framework 本体强耦合，用其扩展能力正确注入**（不复制框架模板；只用 manifest/hooks/overlay 与框架原生产物契约）；
  3. 1C 揭盲（holdout）行为终点**只要求真实到达 coding**；review/ut/testing 机制照建，但行为结论标「行为未验证」；
  4. story 附录规约符合性表**只有一张**，兼容性条目与其它域同表同粒度（不再单列「兼容性自检表」）。

---

## 2. 事实核对清单（开工前逐条执行）

| # | 事实 | 核对命令（仓库根执行） | 预期 |
|---|---|---|---|
| F1 | `story` 分支 HEAD 的 `doc/extensions` 与四宿主 story 入口已等于 1.0 | `git diff --stat 19977c98 HEAD -- doc/extensions .cac/commands/story.md .claude/commands/story.md .codex/skills/story/SKILL.md .opencode/skill/story/SKILL.md` | 输出为空 |
| F2 | 2.x 素材在备份分支 | `git log --oneline HEAD..backup/story-2.x-20260825` | 含 `4412a78b`（2.1）、`6bf074a7`、`b6ed75c4` |
| F3 | `test/story` 被误回退 | `git diff --name-status backup/story-2.x-20260825 HEAD -- test/story` | 7 个文件（AGENTS/EVOLUTION/TEST/run_case/run_multi_case/两个 tests） |
| F4 | 1.0 知识目录无设计模式、无可观测性规约 | `ls doc/extensions/knowledge doc/extensions/knowledge/constraints` | 无 `design-patterns/`，constraints 只有 README + 6 域 |
| F5 | 两模式原件（冻结 spec 03-C8 指定） | `wc -l "E:/Project/AIDefectHelpler/docs/archive/需求开发/extensions/knowledge/design-patterns/"*.md` | decision-tree 174 / page-interaction 156 / README 78 行 |
| F6 | 工程真实存在三层观测封装（1.0 `codebase-facts.md` §5 写「无埋点封装」是错的） | `ls 05-SystemBase/CommFunc/src/main/ets/shared/ha/` | `WalletHAManager.ets`、`WalletHAReportForVOCBuilder.ets`、`WalletHAReportForChartCBuilder.ets` |
| F7 | 编排 SDK 真实依赖 | `grep -n framework oh-package.json5 02-Feature/WalletMain/oh-package.json5 01-Product/Phone/oh-package.json5; ls libs` | 三处 `"framework": "file:…/libs/framework-1.0.0.har"` |
| F8 | hook 片段只进 verifier 提示词；`post_verifier` 在 verifier 之前触发 | `sed -n '760,770p' framework/harness/harness-runner.ts` | `pre_verifier/on_context_load/post_verifier/post_phase` 四行相邻同步触发 |
| F9 | `provides.knowledge` 无消费方 | `grep -rn "knowledgePaths" framework/harness --include=*.ts` | 只有 `extension-loader.ts` 写入与 `types.ts` 类型，无读取 |
| F10 | 扩展阻断 verdict 的唯一手段 | `sed -n '170,190p' framework/harness/hooks-dispatcher.ts` | `severityOverride` 接受 `BLOCKER/MAJOR/MINOR`，id 固定 `lifecycle_hook_<event>_<source>` |
| F11 | 框架原生约束追溯链 | `sed -n '903,935p' framework/harness/scripts/check-plan.ts` | `CONSTRAINT_CATEGORIES = security/performance/dfx/nfr/privacy/compatibility`；`criteria[].category` 命中即要求 id 出现在 plan.md/contracts.yaml |
| F12 | ut/testing 认 `AC-N/BD-N` 形态 | `grep -n "AC|BD" framework/harness/scripts/check-testing.ts | head -3` | `/^(AC|BD)-(G\d+|\d+)$/` |
| F13 | 下游主 agent 必读冻结产物 | `grep -n "contracts.yaml" framework/skills/feature/coding/SKILL.md framework/skills/feature/code-review/SKILL.md` | coding 只读 contracts；review 读 plan/contracts/acceptance |
| F14 | 台账与全量回归脚本不存在 | `ls test/story/regression test/story/scripts/check_failure_modes.py` | 均不存在 |
| F15 | 历史产物位置 | `ls doc/features; ls "E:/Project/bak/Story-Features-20260824-121838"` | 仓内只有 DTS90006；AR90004/AR90006 在 bak |

**由 F8/F9/F11/F13 得出的三条设计原则**（全文据此）：
- P1 「被看到」走**主 agent 必读的产物**（spec.md 出口、`acceptance.yaml`、`plan.md`、`contracts.yaml`）+ **post_check 的 BLOCKER 报错文案**（文案自带「缺什么/写到哪/形态见哪个文件」）；hook `.md` 只服务 verifier。
- P2 「被传递」搭**框架原生链**：知识义务 = `acceptance.yaml` 里带 `category` 的验收条目 → `check-plan.spec_constraint_traceability` → `contracts.prd_to_code_traceability` → `check-coding.plan_to_code` → `check-ut.acceptance_coverage` → `check-testing.acceptance_to_test_case`。扩展只补原生链没有的：条目级识别、实体级冻结、review 留证、全集语义裁决。
- P3 机制层**零硬编码**：域前缀、条目、模式 id/角色一律从 manifest 激活清单 + 知识正文运行期派生；派生为空必须响亮报错（G7）。

---

## 3. 执行总顺序

```
Step 0  批次 0 补救（恢复测试域 → 回退验收登记 → 台账+回归脚本 → 兼容矩阵）
Step 1  判据先行：写 01-判据.md（先于任何实现）
Step 2  1A 知识内容与激活（离线全绿 → 1A 小结）
Step 3  1B spec 识别 → plan 冻结（离线全绿 + 历史回归 → 1B 小结）
Step 4  1C 下游传递（离线全绿 → 1C 小结）
Step 5  对抗夹具 + 台账全量回归 + 负面扫描
Step 6  冻结清单（指纹）→ 交付报告 → STATUS.md 回写 → 等用户揭盲
```
每个 Step 结束时用 §7 的命令跑一遍，全绿才进下一步；**每个 Step 单独 git 提交**（提交信息前缀 `story.b1-<step>`）。

---

## 4. Step 0 · 批次 0 补救

### 0.1 恢复测试域
```bash
git checkout backup/story-2.x-20260825 -- test/story
git status --short test/story    # 预期 7 个 M/A
python -m unittest discover -s test/story/tests
```
恢复后 `test/story/AGENTS.md` 应为 115 行版本（含 §2 维护不变量），这才是 01-G7 引用的「AGENTS.md §2」。

### 0.2 回退验收登记
新建 `test/story/plan/2026-08-25-story分批次交付/batch-0-公共基座/STATUS.md`（目录若无则建），记录：
- `git diff --stat 19977c98 HEAD -- doc/extensions <四宿主入口>` 为空；
- `git status --short --ignored -- doc test .cac .claude .codex .opencode` 的 untracked/ignored 清单；
- `git ls-tree -r --name-only HEAD -- doc/extensions` 共 43 文件；
- 四宿主入口 `.cac/commands/story.md`、`.claude/commands/story.md`、`.codex/skills/story/SKILL.md`、`.opencode/skill/story/SKILL.md` 首行 description 一致；
- 1.0 命令兼容用例：`node doc/extensions/skills/story/scripts/story.js help` 输出含 `init|archive|restore|review|help`。

### 0.3 失效形态台账 + 全量回归脚本（G1）
- 新建 `test/story/regression/failure-modes.yaml`，每形态一条：
  ```yaml
  - id: N8-appendix-paraphrase
    dimension: knowledge            # knowledge | dedup | readability | infra
    found_round: 2026-08-24
    form: 附录/出口「结论」列复述规约原文
    clause: B1-12 / B1-14
    checker: doc/extensions/hooks/shared/paraphrase.mjs   # 或 test/story 内脚本
    bad_fixture: test/story/fixtures/failure-modes/N8/bad/…
    good_fixture: test/story/fixtures/failure-modes/N8/good/…
    applicable_when: 存在 AR/story.md 附录 或 spec §10
    severity: BLOCKER
    status: fixed                    # fixed 永久跑；retired 须 reason + approved_by
  ```
  首批入册（从 03-踩坑记录.md 逐条映射，每条给正/反夹具）：#1 精确标题选择器、#14 结论列复述、#15 风险标签决定裁决覆盖面、#16 比较字段不唯一、#17 模块名门禁不限作用域、#22 编号粒度退化、#23 整文件 SHA 绑死元数据、#29 硬编码域前缀、#32 解析失败静默空结果、#37 CRLF 零命中、#39 守恒误伤字段名、#42 占位文件丢失，以及上一轮 F1–F11/N1–N9 中可机械化者（见 `test/story/plan/2026-08-24-story判据内容化与失效形态回归/01-正向方案.md:167-176`）。当前 1.0 尚无该能力的条目标 `status: pending_capability`（区别于回归失败）。
- 新建 `test/story/scripts/check_failure_modes.py`：跑全部 `status != retired`，逐条输出 `PASS/FAIL + 证据路径`；`pending_capability` 输出 `SKIP(1.0 无该能力)`；退出码：任一 FAIL → 1。参数 `--feature <f>`（可多次）指定被检产物根，默认 `doc/features/*` + `E:/Project/bak/Story-Features-20260824-121838/*`。
- `test/story/TEST.md` §7 离线验证清单补四条：`check_failure_modes.py`、`node --check doc/extensions/**/*.mjs`、负面 grep（§7.3）、对抗夹具。

### 0.4 测试域 × 1.0 兼容矩阵
`python test/story/scripts/run_multi_case.py plan --all --jobs 4 --isolated-workspaces` 只读演练通过；把结果与 §0.3 台账的 `pending_capability` 清单写进 batch-0 STATUS。

---

## 5. Step 1 · 判据先行（G3）

新建 `batch-1-知识应用/01-判据.md`。每条判据格式：

| 判据 id | 条款出处 | 口径（计什么/不计什么） | 责任层 | checker |
|---|---|---|---|---|

责任层三选一：**机械**（脚本证结构/引用/实体/证据存在）、**语义**（verifier 裁内容是否解决本需求）、**行为**（隔离 Case 实跑到达）。机械判据不得表述为语义结论（G3）。必须覆盖的判据（至少）：

- K-A1 三类知识封闭集合：manifest `knowledge_activation` 只有 `facts/constraints/patterns` 三键，且每个文件恰属一类 — 机械 — B1-1/2
- K-A2 规约不携带实现事实：`constraints/*.md` 中不出现 `.ets` 路径、类名（按 facts 面登记的封装名反查）— 机械 — B1-2
- K-A3 可观测性规约 ≤32 行、5 条条目、无模板表 — 机械（计数只作诊断，PASS 条件是「条目表 + 判定附注两节且无第三节」）— B1-3/G8
- K-A4 模式正文与原件逐字节一致（frontmatter 之后的正文 SHA-256）；frontmatter 原件键与值守恒、只允许追加 `roles/optional_roles/coordinator_role` — 机械 — B1-5
- K-A5 派生为空响亮：删空 manifest 激活项时 hook 输出 BLOCKER 文案而非空集 — 机械 — G7
- K-B1 spec 三出口独立成节且不在 §9 技术契约内 — 机械 — B1-9
- K-B2 出口条目粒度到条目级（`<前缀>-<两位数>`），无仅前缀行 — 机械 — B1-8
- K-B3 附录判「是」集 = spec §10 编号集 = acceptance `knowledge_rule` 集 = plan `obligations.rule` 集（差集为空）— 机械 — B1-13
- K-B4 复述判定：规范化后输出 ⊆ 单一来源（约束列或处置列）→ FAIL；相似度只排序 — 机械 — B1-14
- K-B5 每行结论是否「本需求的设计」— **语义**（verifier 逐行）— B1-12/13
- K-B6 知识决策章排在首个设计章前；`knowledge_freeze` 结构、实体引用可解析、角色完备、coordinator 一致、anchor 非自指 — 机械 — B1-10
- K-B7 判「否」行给依据（结论列非空且非「—」）— 机械 — B1-8
- K-B8 无候选须显式一行 + 理由 — 机械 — B1-9
- K-C1 coding：冻结 landing/roles 实体在契约点名文件中存在 — 机械 — B1-11
- K-C2 review：review-report 「知识义务复核」表逐 rule 一行（diff 位置或不适用+理由）— 机械 — B1-11
- K-C3 ut/testing：knowledge 条目的 AC 在原生覆盖集内或按 `ut_layer` 天然豁免 — 机械（复用原生）— B1-11
- K-C4 无冻结时下游 BLOCKER 指路回 plan — 机械 — B1-11
- K-V1 verifier 报告对 K-B3 集合逐 id 有裁决 — 机械（test/story 侧脚本核 `verifier.report.md`）— B1-13
- K-V2 写作指令含自检问句且与判据同向 — 机械（grep 指令文件含问句）— B1-15
- K-H1 holdout：coding 阶段真实到达且 K-C1 留证 — 行为 — 用户裁定

---

## 6. Step 2 · 1A 知识内容与激活

目录约定（保持 1.0 根布局，类型由 manifest 声明）：
```
doc/extensions/knowledge/
  component-profile.md          # facts
  codebase-facts.md             # facts
  constraints/README.md + ux-consistency / security-privacy / dfx-baseline / observability(新) / compatibility-checklist / env-exceptions / deliverables
  design-patterns/README.md + decision-tree.md + page-interaction.md   (新)
```

### 2.1 规约字段模型（`constraints/README.md` 重写，≤60 行）
- 保留：条目表列语义（`编号 | 约束 | 命中条件 | 处置 | 验证（执行体） | 强制力`）、执行体四类（模型/构建/实机/人工）、强制力表、编号纪律、域前缀表（新增 `OBS 可观测性`）。
- **删除**：「适用矩阵」（阶段消费路由归各阶段 hook，B1-6）。
- 「处置」列以 `（评审动作）` 开头的条目 = 不产生代码要求（spec 出口不收、acceptance 不建条目）。
- 「落法附注」规则：只写平台隐性例外、边界、决策理由；需要指到封装时写「见项目知识《codebase-facts》‹面名›」，**不写类名/路径**。

### 2.2 各域文件改动
- 六个既有域：把附注里的 API/类名/路径迁到 `codebase-facts.md` 对应面（迁移前后条目清单比对并写进 1A 小结，坑 #32）；条目表字段对齐 2.1。
- 新建 `constraints/observability.md`（目标 ≤32 行）：
  ```
  ---
  name: observability
  domain: OBS
  applies_when: 需求新增业务流程，或改变既有流程的分支/终态
  ---
  # 可观测性
  三条渠道回答三个问题：日志=这一次发生了什么（开发排障）；VOC=用户遇到了什么问题（服务运营）；Chart=业务整体跑得怎样（度量）。粒度依次变粗。
  ## 条目
  | 编号 | 约束 | 命中条件 | 处置 | 验证（执行体） | 强制力 |
  | OBS-01 | 同一次业务执行三渠道可对应：共用流程标识与步骤标识 | 新增或改动业务流程 | 声明标识如何生成、各渠道如何携带 | 模型 + 实机 | 红线 |
  | OBS-02 | 密度递减：日志 > VOC > Chart，不设数量配额 | 同上 | 说明各渠道记录哪些点及归属理由 | 模型 | 基线 |
  | OBS-03 | Chart 每个业务步骤只有一个终态 | 同上 | 逐步骤给互斥且穷尽的终态集合 | 模型 + 实机 | 红线 |
  | OBS-04 | 凭据/令牌/卡号/个人身份信息不入任何渠道 | 流程接触敏感字段 | 逐字段声明排除或脱敏 | 模型 + 实机 | 红线 |
  | OBS-05 | 观测失败不改变业务流程、结果、分支与重试 | 同上 | 声明失败处置且不进入业务分支判断 | 模型 + 实机 | 红线 |
  ## 判定附注
  - 命中看「有没有新的业务过程」，不看有没有 UI；纯文案/样式不命中。
  - OBS-02 不量化成数字；判据是粒度关系对不对。
  - OBS-03 的业务步骤按用户视角切，不按函数切。
  - 本工程缺某条渠道时写明缺哪条、改由什么替代。
  - 封装见项目知识《codebase-facts》「可观测性」面。
  ```
- `codebase-facts.md`：新增「可观测性」面（特征与规则：日志/VOC/Chart 三类出口从 CommFunc 公开出口导出；两类上报器非阻塞、失败只记 `Logger.error`、不向业务传播；写明核实方法「检索 CommFunc/index.ets 导出 + ha/ 目录」而不列调用点）与「编排 SDK」面（`libs/framework-1.0.0.har`，三处 `oh-package.json5` 依赖；未找到多个真实业务案例 → 证据缺口登记）；纠正 §5「无埋点封装」。
- `design-patterns/`：三份文件正文从 F5 原件**逐字节复制**；`decision-tree.md`/`page-interaction.md` frontmatter 只追加：
  ```yaml
  roles: [<从正文「角色与文件落点」表抄角色名>]
  optional_roles: [...]
  coordinator_role: <编排入口角色>
  ```
  原件已有键（含 `triggers_usecase_spec`）一个不动。README 中的 SDK 依赖句保留原文，其核实结果写在 facts（不改原件）。

### 2.3 manifest.yaml
```yaml
schema_version: "1.0"
name: wallet-sdk-demo
version: "1.1.0"
provides:
  skills: [wallet-sdk-onboarding, story]
  knowledge: [<全部知识文件相对路径，供框架存在性校验>]
  knowledge_activation:              # 扩展自读；阶段只读这里列出的文件
    facts: [knowledge/component-profile.md, knowledge/codebase-facts.md]
    constraints: [knowledge/constraints/ux-consistency.md, …, knowledge/constraints/observability.md]
    patterns: [knowledge/design-patterns/decision-tree.md, knowledge/design-patterns/page-interaction.md]
  hooks:
    spec:    { on_context_load: [hooks/spec/on_context_load.md, hooks/spec/story_on_context_load.mjs], post_check: [hooks/spec/post_check.mjs], pre_verifier: [hooks/shared/pre_verifier.mjs] }
    plan:    { on_context_load: [hooks/plan/on_context_load.md],    post_check: [hooks/plan/post_check.mjs],    pre_verifier: [hooks/shared/pre_verifier.mjs] }
    coding:  { on_context_load: [hooks/coding/on_context_load.md],  post_check: [hooks/coding/post_check.mjs],  pre_verifier: [hooks/shared/pre_verifier.mjs] }
    review:  { on_context_load: [hooks/review/on_context_load.md],  post_check: [hooks/review/post_check.mjs],  pre_verifier: [hooks/shared/pre_verifier.mjs] }
    ut:      { on_context_load: [hooks/ut/on_context_load.md],      post_check: [hooks/ut/post_check.mjs],      pre_verifier: [hooks/shared/pre_verifier.mjs] }
    testing: { on_context_load: [hooks/testing/on_context_load.md], post_check: [hooks/testing/post_check.mjs], pre_verifier: [hooks/shared/pre_verifier.mjs] }
  phase_rules_overlays: { spec: rules/spec-rules.overlay.yaml, …六阶段 }
```
删除 `hooks/constraint-application.md`（其「读 README 矩阵」指令已无对象）。跑 `cd framework/harness && npx ts-node harness-runner.ts --phase extensions` 须 PASS（manifest 任一路径错会**清空全部 provides**）。

### 2.4 派生模块 `hooks/shared/knowledge.mjs`（1A 交付，1B/1C 复用）
导出 `activeKnowledge(projectRoot)`：读 `framework.config.json > paths.extension_dir`（默认 `doc/extensions`）→ 读 manifest `knowledge_activation` → 逐文件解析：
- constraints：frontmatter `domain` 为前缀；条目表按表头名定位列（不按列序），返回 `{file, domain, prefix, entries:[{id, constraint, hit_when, handling, verify, force, review_action:boolean}]}`；
- patterns：frontmatter `name/roles/optional_roles/coordinator_role`；
- facts：文件路径与一级「面」标题列表。
- 任一类为空、任一文件缺失、任一条目表解析出 0 行 → `throw` 带文案「派生为空：<文件>——检查 manifest.knowledge_activation 与条目表表头」。
- 分行一律 `/\r?\n/`。同目录 `yaml-lite.mjs`（解析 contracts/acceptance 的最小 YAML：映射、序列、标量、引号、注释；缩进按下一行实际缩进判断子结构；解析失败 throw）、`paths.mjs`（features_dir 解析）。

### 2.5 1A 完成判据
K-A1…K-A5 全 PASS；`node --check` 全绿；负面 grep（§7.3）为 0；`--phase extensions` PASS；1A 小结写入 `batch-1-知识应用/STATUS.md` 事件日志（含迁移清单比对结果、模式 SHA）。

---

## 7. Step 3 · 1B spec 识别 → plan 冻结

### 3.1 spec 三出口（B1-8/9）—— 形态定义在 `skills/story/templates/spec-sections.md`
在 1.0 模板 §9 技术契约之后追加两章（章号接 core 模板之后）：
```
## 10. 规约约束要求
（按附录判「是」且处置非（评审动作）的条目派生，一条一行；不为任何域预留小节）
| 编号 | 本需求的要求 | 落点契约名 |
| SEC-01 | <本需求里具体要求做什么：写清落在哪个接口/存储键/字段/步骤上，可实现可测> | <§9 契约表里的名字> |

## 11. 设计模式候选登记
（只登记不选型；pattern_id 只能填激活清单在册的 id）
| 适用单元 | 候选 pattern_id | 命中信号 / 反证 |
| <流程段或页面> | decision-tree / 无候选 | <为什么像 / 为什么不像> |
无候选时保留一行：`| 全部单元 | 无候选 | <理由> |`
```
1.0 `hooks/spec/on_context_load.md` 中「§7 四处（UX 适配要求/触发频次/存储兼容/个人数据采集）」全部删除；`post_check.mjs` 第 4 项（`UX 适配要求` 标题检查）删除。

**自检问句**（写进 `on_context_load.md` 与 story `rules.md` 的写作指令，与 K-B4/K-B5 同向）：
> 每写一行「本需求的要求 / 结论或落点」先自问：① 这句话里有没有**本需求自己的名字**（接口名、存储键、字段、业务步骤）？② 把规约编号遮住，这句话还能指导编码吗？③ 它是不是规约「约束」或「处置」列换了个说法？三问任一答错就重写。

### 3.2 story 侧（本批唯一触碰点）
- 注册件 `AR/story-src/knowledge.json`（`scaffold` 建骨架含 `_example`）：
  ```json
  { "constraints": [ { "domain": "SEC", "id": "SEC-01", "hit": true, "conclusion": "<本需求的设计/落点>", "landing": "<契约名，可选>" },
                     { "domain": "DLV", "id": "DLV-02", "hit": false, "conclusion": "<不命中的依据>" } ],
    "patterns":    [ { "unit": "<适用单元>", "candidate": "decision-tree", "signal": "…" } ],
    "none_reason": null }
  ```
  作者对**每个激活域的每个条目**给 hit 与结论（判「否」也要依据）。
- `scripts/story-build.mjs` 新增 `renderKnowledgeAppendix()`：由注册件 + `knowledge.mjs` 渲染 `## 附录：规约符合性` **单表** `| 域 | 编号 | 要求 | 命中 | 结论或落点 |`，「要求」列 = 规约「约束」列原文（引用渲染，永不作为复述比较的被检对象）；模式候选另渲染 `## 附录：设计模式候选` 表。装配时校验：注册件条目集 = 激活条目集（缺一行、多一行都失败并报正确形状）。`ids.json` 中的规约编号条目取消（验收/场景编号照旧）。
- `contracts/story-chapters.json`：仅 `12-compliance` 去掉 `"form": {"tables": ">=1"}`（正文只叙述并引用附录）；其余 14 章骨架不动。
- `rules/rules.md` 新增「知识识别」条：读 manifest `knowledge_activation` → 逐文件读条目表 → 填注册件；正文不写规约编号只写中文名；附录由装配器渲染。`reference/constraint-usage.md` 改为单表纪律，删除兼容性自检表与 TEL/MODE/APP 残留行；`reference/evidence-rules.md` 同步。

### 3.3 acceptance 投影（P2 核心）
spec Step 8 提取 `acceptance.yaml` 时，§10 每行生成一条 `criteria[]`：
```yaml
- id: AC-<N>                 # 原生形态，ut/testing 认这个
  knowledge_rule: SEC-01     # 三方 ID 集合的桥接键
  category: security         # 域→类别映射：SEC→security；DFX→dfx；COMPAT→compatibility；UX/ENV/OBS→nfr；涉及个人数据的 SEC 条目→privacy；性能类 DFX→performance
  prd_function: F<k>         # 业务步骤锚定
  priority / description / testable / verification_steps / expected_result
  ut_layer: unit|device|both # 即该义务的 ut/testing 验证要求（单源）；ut_focus / device_focus 按框架规则必填
```
说明写进 `on_context_load.md`：`ut_layer: device` = ut 阶段天然不适用；`ut_layer: unit` = testing 阶段天然不适用——这就是 B1-11 的「显式不适用」，不另建豁免表。

### 3.4 plan 冻结（B1-10）
- `plan.md`：在第一个设计章（`## 1. 模块架构图`）之前必须有
  ```
  ## 知识决策（设计输入）
  ### 设计模式选型   —— 逐个 spec §11 单元：选/不选 + 理由；选了给唯一实例名
  ### 规约义务       —— 逐条 spec §10 条目：本次落实成什么 + 落在哪个契约实体 + 哪个业务步骤
  ### 项目事实影响   —— 复用了什么、缺什么、与 facts 有无矛盾
  ```
- `contracts.yaml` 顶层新增：
  ```yaml
  knowledge_freeze:
    obligations:
      - rule: SEC-01
        criterion: AC-7
        obligation: <可实施的设计结论，非规约复述>
        step: <prd_function 或 use-cases branch id>
        anchor: <plan.md 承载它的设计章标题；不得是「知识决策」章>
        landing: [data_models.<模型>.<字段>, interfaces.<类>.<方法>, components.<组件>, state_management.<环境>.<键>, resource_keys.<键>, files.<路径>, navigation.<目标>]
        review_focus: <review 阶段核什么>
    patterns:
      - pattern_id: decision-tree
        selected: true
        instance: <唯一实例名>
        unit: <适用单元>
        rationale: <为什么选/不选>
        anchor: <设计章标题>
        roles: { <模式 frontmatter 声明的每个角色>: <契约实体> }
  prd_to_code_traceability:
    - prd_id: AC-7            # 每条 criterion 一行，key_files 非空（原生 coding 门禁接管）
      key_files: [...]
  ```
- `hooks/plan/post_check.mjs` FAIL 清单（全部 BLOCKER，details 逐项带子 id）：① 缺知识决策章或排在首个设计章后；② 缺 `knowledge_freeze`；③ spec §10 编号集 ≠ `obligations.rule` 集；④ `criterion` 不在 acceptance 或其 `knowledge_rule` ≠ `rule`；⑤ `landing` 空（处置为评审动作的除外）或引用解析不到契约实体；⑥ `anchor` 不存在或指回知识决策章；⑦ `pattern_id` 不在册；`selected:true` 缺 `instance` 或角色不全 / 出现未声明角色 / 角色实体不存在；⑧ `use-cases.yaml` 存在时 `coordinator` ≠ coordinator 角色实体；⑨ spec §11 全为「无候选」却冻结了 `selected:true`；⑩ `obligation` 复述（§3.6）。
- `hooks/plan/on_context_load.md`：上述形态 + 「有落点/无落点」中性题材正反例 + 自检问句 + FAIL 清单原文（报错文案与此文件一字不差地引用同一句）。

### 3.5 spec post_check（`hooks/spec/post_check.mjs` 改造）
保留 1.0 的：三产物齐备、story 流程契约 `storyFlowProblems()`、§9 技术契约结构、术语表解释列、禁用词、文档坐标、数值来源。新增（BLOCKER）：① §10/§11 独立成节且不在 §9 内；② §10 编号粒度到条目级、编号在册；③ 注册件判「是」且非评审动作集 = §10 集 = acceptance `knowledge_rule` 集；④ 判「否」行结论非空；⑤ §11 至少一行，`pattern_id` 在册或「无候选」；⑥ 复述门禁（§3.6）对 §10「本需求的要求」列与附录「结论或落点」列。

### 3.6 复述判定 `hooks/shared/paraphrase.mjs`（B1-14，唯一可实施）
```
输入：output（被检字段）、sources = [条目.约束, 条目.处置]
normalize(s) = 去所有空白 → 全角标点转半角 → 去掉 中文/英文标点中的 ，。；：、“”‘’"'（）()【】[] → 保留字母数字下划线连字符点斜杠与运算符
判定：norm(output) 非空 且 存在 src 使 norm(src).includes(norm(output)) → PURE_COPY（BLOCKER，文案：「‹编号› 的结论是规约原文的子串——请写本需求的设计：它落在哪个接口/键/字段/步骤上」）
排序：difflib 风格最长公共子序列比 ratio(norm(output), norm(src)) 作 WARN 排序（≥0.6 标「复述嫌疑」），只进 verifier 清单排序，不决定 PASS/FAIL、不决定裁决覆盖面
空字段：不在本模块判，交结构门禁
```
「要求」列（渲染自原文）永不作为 output。

### 3.7 全集语义裁决 `hooks/shared/pre_verifier.mjs`（B1-13）
按 `ctx.phase` 派生本阶段必裁集合并注入清单（进 ai-prompt.md）：
- spec：注册件判「是」行 + §10 行 + §11 行；plan：`obligations` + `patterns`；coding/review/ut/testing：`obligations` + `selected` patterns。
- 注入格式：`## 实例扩展必答清单（逐行裁决，SKIP 也要写凭什么）` + 表 `| 判据 id | 条目/rule | 被检文本 | 复述嫌疑排序 | 你的裁决(设计/复述/不适用)+证据 |`，并写明「必须在输出 YAML 的 `checks:` 中为每个判据 id 追加一条，`summary.total` 相应加」。
- 机械收口分两处：三方 ID 集合一致由本阶段 post_check 判；**verifier 报告逐 id 含裁决**由 `test/story/scripts/check_failure_modes.py` 的检查项 `verifier_all_rows_adjudicated` 读 `<phase>/reports/**/verifier.report.md`（认 `verifier.report.md / verifier-report.yaml / verifier-*.md / verify-*.md`）核对（F8：框架 post_verifier 在 verifier 之前触发，不能用）。
- overlay `rules/<phase>-rules.overlay.yaml` 的 `semantic_checks` id：spec `knowledge_spec_exit_substance`、`knowledge_candidates_registered`；plan `knowledge_freeze_substance`；coding `knowledge_landing_in_code`；review `knowledge_obligation_reviewed`；ut/testing `knowledge_obligation_verified`。每个 hook 启动时解析本阶段 overlay，解析不出 `semantic_checks` → BLOCKER（overlay YAML 错误在框架侧是静默的）。

### 3.8 1B 完成判据
K-B1…K-B8、K-V2 PASS；历史回归：对 `E:/Project/bak/Story-Features-20260824-121838/AR90004`、`…/AR90006`、`doc/features/DTS90006` 跑 spec/plan post_check（用 `scratch/run-hook.mjs` 以 stdin ctx 调用），预期旧产物按台账形态命中（DTS90006 附录/§10 的复述行 BLOCKER；AR90004 编号粒度 N9 BLOCKER）；1B 小结入 STATUS。

---

## 8. Step 4 · 1C 下游只读冻结结果

通用：四个 `post_check.mjs` 开头 `const book = readFreeze(contracts.yaml)`（`hooks/shared/freeze.mjs`）；**无 `knowledge_freeze`** → BLOCKER 文案「plan 未冻结知识结果，本阶段零注入；回 plan 补 `knowledge_freeze` 后重跑 plan 与本阶段」（K-C4）。四个 `on_context_load.md` 只含账本判据（rule/obligation/landing/roles 逐条）与「显式不适用优于静默跳过」，不含知识正文。

- **coding**（K-C1）：对 `obligations[].landing` 每个引用取末段标识符，在 `contracts.yaml` 点名的 `files`/`file:` 文件中按 `\b<标识符>\b` 查找；`selected` 模式每个角色实体同法；缺 → BLOCKER 列出缺失项。文件级存在性交原生 `plan_to_code`。
- **review**（K-C2）：`review/review-report.md` 须有 `## 知识义务复核` 表 `| rule | 落实位置（文件:符号） | 结论（落实/未落实/不适用） | 依据 |`，行集 = `obligations.rule` 集；「不适用」行依据非空；缺表/缺行 → BLOCKER。
- **ut**（K-C3）：对 `knowledge_rule` 条目：`ut_layer ∈ {unit, both}` 的 AC 须出现在 `ut/reports/ac-coverage.json` 覆盖集（原生 `acceptance_coverage` 已 BLOCKER，本 hook 只把缺失项按 rule 名重述指路）；`ut_layer: device` 输出 `SKIP(不适用：device 层)` 进 details。
- **testing**：对称——`ut_layer ∈ {device, both}` 的 AC 须在 `testing/test-plan.md` 用例表中被引用（原生 `acceptance_to_test_case`）；`unit` 层 SKIP。
- 1C 完成判据：K-C1…K-C4 PASS；用构造夹具（一份最小 contracts + 源码桩）验证逐条拦/放；1C 小结入 STATUS。

---

## 9. Step 5 · 夹具、回归、负面扫描

### 5.1 对抗夹具 `test/story/fixtures/knowledge/`（逐分支冻结预期，坑 #11）
| 夹具 | 内容 | 预期 |
|---|---|---|
| A 自造专名插入 | 规约原文 + 随机英文标识符 | paraphrase：非子串 → 不判 PURE_COPY；相似度高 → WARN 排序靠前；verifier 清单含它 |
| B 同义改写 | 原文换词、语序 | 非子串 → 进语义清单（全集裁决兜底） |
| C 自然语言真设计 | 含本需求接口名/字段的设计句 | PASS，相似度低 |
| D 合法设计 + WARN | 真设计但与原文共用术语 | PASS + WARN 排序，不阻断 |
| E 要求列原文 + 结论列真设计 | 附录整行 | PASS（要求列不参与比较） |
| F 纯复制 | 结论 = 约束列原文（含标点差异） | BLOCKER |

### 5.2 负面扫描（提交前固定，G7）
```bash
grep -rnE "\b(UX|SEC|DFX|OBS|COMPAT|ENV|DLV)-[0-9]{2}\b" doc/extensions --include=*.mjs --include=*.yaml --exclude-dir=knowledge   # 期望 0（域前缀字面）
grep -rnE "AR9000[0-9]|交通卡|自动充值" doc/extensions --exclude-dir=knowledge                                             # 期望 0（测试特征）
grep -rnE "0[1-5]-(Product|Feature|Business|SystemBase)" doc/extensions --exclude-dir=knowledge                            # 期望 0（层名硬编码）
grep -rnE "E:\\\\|E:/|backup/story|19977c98" doc/extensions                                                                # 期望 0（绝对路径/来源仓引用）
```

### 5.3 全量回归
`python test/story/scripts/check_failure_modes.py`（默认目标集）全 PASS/SKIP，无 FAIL。

---

## 10. Step 6 · 冻结、报告、回写

- `batch-1-知识应用/02-冻结清单.md`：列出 `doc/extensions/hooks/**`、`rules/**`、`skills/story/{rules,reference,templates,scripts}/**`、`knowledge/**`、`test/story/regression/**`、`test/story/fixtures/knowledge/**` 每文件 SHA-256（`git ls-files -z … | xargs -0 sha256sum`），以及阈值（相似度 WARN 阈 0.6）与提示词文件清单。
- `batch-1-知识应用/03-交付报告.md`：分栏「实施事实」/「离线验证结果」/「历史回归结果」/「行为验证：待用户揭盲」；阶段状态七字段模板（artifact/execution/harness/verifier/receipt/closure/target）留空给用户实跑后填。
- `STATUS.md`：状态改「冻结待揭盲」，填方案/判据/冻结清单链接；事件日志追加各 Step 提交 SHA。
- 交付说明写明：holdout 由用户提供新材料；最低行为终点 = coding 真实到达 + K-C1 逐条留证；review/ut/testing 标「行为未验证」。总判定按 G5。
- 揭盲后：新验证机制入册 `02-已验证机制参考.md`（带证据），新坑入册 `03-踩坑记录.md`；holdout 作废登记。

---

## 11. 验证命令汇总（每个 Step 末跑）
```bash
find doc/extensions -name "*.mjs" -exec node --check {} \;
python -m unittest discover -s test/story/tests
python -m unittest discover -s tools/cli/tests && python -m tools.cli.scripts.validate_clis
python -m compileall -q tools/cli test/story/scripts
python test/story/scripts/check_failure_modes.py
cd framework/harness && npx ts-node harness-runner.ts --phase extensions && cd ../..
python test/story/scripts/run_multi_case.py plan --all --jobs 4 --isolated-workspaces
# 验证跑完清理陈旧状态（坑 #41）
cd framework/harness && npx ts-node harness-runner.ts --clear-state && cd ../..
```

---

## 12. 参考件取用方式（只看形态，严禁照抄）
```bash
B=backup/story-2.x-20260825
git show $B:doc/extensions/hooks/shared/knowledge-ledger.mjs      # 账本派生形态
git show $B:doc/extensions/hooks/shared/yaml-lite.mjs             # CRLF 安全 yaml-lite（实现级习惯可沿用）
git show $B:doc/extensions/hooks/plan/post_check.mjs              # 实体引用解析形态（字段集须按 §3.4 重设计）
git show $B:doc/extensions/hooks/shared/pre_verifier.mjs          # 必答清单注入形态（覆盖面须改为全集）
git show $B:doc/extensions/knowledge/constraints/diagnostics-metrics.md   # 63 行「过重」样本，对照 §2.2 瘦身
git show $B:doc/extensions/skills/story/scripts/appendix.mjs      # 附录单表渲染形态
```
关键框架行号：`harness-runner.ts:752`（post_check 触发）、`:765-768`（四钩子相邻触发）、`hooks-dispatcher.ts:107-133`（.mjs stdin/stdout 契约）、`:177-188`（severityOverride）、`profile-loader.ts:85-101`（overlay 合并）、`:190-192`（overlay 解析失败静默）、`check-plan.ts:903-972`（原生约束追溯）、`check-coding.ts:181-266`（plan_to_code）、`check-ut.ts:1961-1991`（acceptance_coverage）、`check-testing.ts:1043,1107`（AC 形态与追溯）、`report-generator.ts:311-315`（hook 片段落 ai-prompt）。

## 13. 显式假设与已知缺口
- `contracts.yaml` 顶层扩展键被 `spec-loader` 容忍（DTS90006 的顶层 `project_knowledge` 已过 plan 门禁）；若新框架版本拒绝，改为 `plan/knowledge-freeze.yaml` 独立文件并同步 §3.4/§8。
- framework 未提供面向各阶段**主 agent**的扩展注入点（hook 只到 verifier）；本批以 P1 处理，缺口记入 03-踩坑记录.md，不在本批修框架。
- `context/facts.md` 不承载知识（不动框架 schema）。
- 行为层结论只来自用户实跑；本方案所有「PASS」均指离线/回归层。

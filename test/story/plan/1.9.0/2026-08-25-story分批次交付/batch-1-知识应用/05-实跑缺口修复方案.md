# 批次 1 · 实跑缺口修复方案（05）

> 承接提交 `1224d1eb`。suite `story-suite-20260825-162511`（Case `split-interactive` / AR90006，story→plan）实跑后评测：
> 知识链结构成立，但 **G5 闭环失败**（verifier 逐行裁决未落盘，P11 漏裁 10/11），且内容层有 3 处 facts 应用失效、
> 2 处 spec→plan 断裂、1 处图链断、review 门槛语义未执行——**全部没被任何门禁或 verifier 抓到**。
>
> **本文写给零上下文的执行模型**：每一项都给到文件、函数、判据文案、数据形状、夹具内容与断言。
> 读完 §0 再动手；遇到本文与仓内代码不一致时，以仓内代码为准并在 STATUS 记录差异，不要自行改判据语义。

---

## 0. 执行者须知

**先读**（按顺序，全文）：
1. `test/story/AGENTS.md`（维护契约；§0 角色、§2 不变量、§2.2 知识写法、§4 红线）
2. `test/story/TEST.md`（测试协议）
3. `test/story/plan/2026-08-25-story分批次交付/01-全批次需求记录.md` §2（批次 1 需求 B1-1…B1-15）
4. `test/story/plan/2026-08-25-story分批次交付/batch-1-知识应用/01-判据.md`（30 条判据）
5. 本文

**不动**：`framework/`（只读 vendored）、`doc/extensions/knowledge/**`（知识内容上轮已定；**唯一例外 T0** 的 VOC 一行）、模式正文、`skills/story/contracts/story-chapters.json`、`doc/features/**`（被测产物，只读作夹具素材）、`doc/glossary.yaml` 当前的未提交改动（被测模型写的，不提交不还原，留给用户）。

**纪律**：
- 每改完一个文件跑 `node --check <文件>`；每改完一项跑该项的正反夹具或单测；不批量改完再测。
- 机制层不得出现规约编号、域名、模式名、业务名字面（M01/M02 会抓）；判据的基准一律运行期从激活清单派生。
- 不写 `TODO/暂时/临时`（M16 会抓）；新增的导出必须有消费者。
- 改文本契约时同步它的消费方（AGENTS.md §2「改契约文本时同步它的消费方」）。
- 提交前跑 §5 全部命令；台账数须为 36/36。
- 实跑（§6）：**本轮用户已明示授权本会话执行**（2026-08-26「改完跑 cli，单用例，跑完 coding」）——T0–T18 全部改完且 §5 全绿后再起跑，**改动期间不启动被测 CLI**。
- **动手第一步**：把本文落盘覆盖 `test/story/plan/2026-08-25-story分批次交付/batch-1-知识应用/05-实跑缺口修复方案.md`（含 T0 新增「资源」面、T0b 目标目录改正），并在 `STATUS.md` 记一行「05 第二版：并入 coding 续跑发现的 facts 资源缺项与 ui-kit 前置」。

**正向实现（用户要求，BLOCKER）**：每一项都按 AGENTS.md §2「正向实现，不打补丁」四判据自查——
① 同一职责不新旧两套并存；② 不做静默降级分支；③ 新增导出必有消费者；④ 不留 `暂时/临时/TODO`。
具体到本文：不利用副作用当机制（如「重跑 harness 顺便读到报告」）——凡依赖某个流程步骤，写明它是框架设计的既定步骤；
不把状态塞进 message 前缀当伪通道——扩展的状态只有两个正规出口：hook 的 `ok:false + severityOverride`（阻断）与 T18 的留痕文件（非阻断）；
不为个别目录写特例分支——进既有的显式白名单常量。

**一句话定论**（设计依据，改动都从它推出）：
> 时机由规约 `applies_when` 承担；一处登记、一次裁决；facts 不逐面裁，只裁「新增能力是否先复用登记入口」；
> 下游零注入不变；逐行裁决必须落盘，但裁的是收窄后的集合——它证明「裁了」，不是「展示了」。

---

## 1. 实跑证明了什么、没证明什么

| 已证明（产物行号） | 未证明 / 失效 |
|---|---|
| 规约 15/15 逐条判定（`AR/story.md:494-508`），判「否」4 行带依据 | verifier 报告只留汇总句（`spec/reports/verifier.report.md:44`「29 行逐行裁决均指定落点」），逐行「行号→裁决→证据」零落盘；被测 CLI 无 `verifier` 子 agent 类型，用 `general` 代跑（runlog :6187） |
| 11 条义务 knowledge.json→spec §10→acceptance→knowledge_freeze 四方 ID 一致 | `knowledge_freeze.obligations[].step` 全部 = criterion 副本（`AC-Kn`），plan.md 表里的业务步骤 F2/F5 没进契约 |
| decision-tree 四角色物化为 4 个文件（`plan/plan.md:148-151`） | plan.md 义务表「落点」列（`AutoTopupFlow` / `AutoTopupConstants`）≠ contracts `landing`（`interfaces.AutoTopupRepository.*` / `resource_keys.*`） |
| facts 的「无端云封装」+检索项进 spec §9.1（`:304`）、story 取舍三（`:319`）、DEC-004 | `MaskUtil`、`WalletFuncResult` 全链零引用；plan 自造 `AutoTopupStatus` 终态（`plan.md:194`）；spec 定 `relationalStore`（`spec.md:313`）→ plan 改 `AppStorage`（`plan.md:246`）无记录，DEC-006 未表态 |
| story 14 章由 `story-build` 装配 | 两图链接断（`story.md:209,213` 引裸文件名，文件在 `ux-reference/`），`merge-story --check` 未拦 |
| Scope 扩展提议有 `index.ets:15` 证据 | review.md 10 个复选框全空、「下一步」两项未勾却进了 plan |

**coding 续跑（用户在 workspace 单独跑，08-26 09:35）补充的事实**：
- facts 在编码层被用上了：`Logger`/`MaskUtil`/`NavPathContext`/`AppStorage`/`WalletHAManager`/`WalletFuncResult`/`WalletHAEventID` 全部出现在新代码（`AutoTopupRepository.ets:1-7`）——plan 没点名的入口，模型编码时自己回到激活知识取了；`relationalStore`/`preferences` 零引用。
- 扩展 coding post_check（K-C1）直调返回 `ok:true`，但 harness 报告里零痕迹：`framework/harness/hooks-dispatcher.ts:181-191` 只在 `ok:false` 时写 CheckResult——**通过不留痕，分不清「跑了」与「没跑」**。
- harness FAIL 5：`coding_compile` 16 个 ArkTS 错（`arkts-limited-throw`×4、`no-delete`、`no-untyped-obj-literals`、`no-props-by-index`、`no-any`、`LengthMetrics`×6，违反红线 4）；`resource_integrity` 75 处 `$r` 未定义（RES-02 只引用没定义）；`ui_kit_source_conformance`——实例未配 `paths.ui_kit_target_dir`，且仓内无任何 Maison block 而 ui-spec 声明了 4 处语义容器 → **所有 Case 到 coding 都会被它拦**。
- 编译修复后剩 2 个 BLOCKER 的归属（用户询问）：`resource_integrity` = **facts 缺「资源」一面**——facts 全文没写这个仓的资源文件在哪，模型写出不存在的 `base/string.json`、`files` 也没列（历史契约都列了 `…/resources/base/element/string.json`）；框架 `collectResourceKeys` 只认 `contracts.files` 里的资源 json，字段表也没写明，但那是 framework 只读范围，由 facts 一行补齐（T0 第四条）；`ui_kit_source_conformance` = demo 实例前置缺失（`03-CommonBusiness` 层有声明无目录，kit 从未物化），归维护者 T0b，不授权被测模型物化到 WalletMain。
- 知识描述缺陷（用户指出三条）：① facts §5 把 `vocBuilder(...).report()` 写在门面之前，模型照此直接用 builder 并挂满 setter；惯例应是默认门面 `logAndReport/logErrorAndReport/logDebugAndReport`，只上报不打日志才用 `vocBuilder(id, desc).report()` 且不额外 set；② 同一处代码 VOC 与 Chart 二选一，模型在 3 个终态点两个都发；③ 日志敏感字段：`AutoTopupCloudClient.ets:21,42`、`AutoTopupContractCache.ets:27,54` 以 `%{public}s` 打原始 `account`，`cardId`/`contractNo` 全部 `%{public}`——SEC-01 义务冻结了、K-C1 实体存在、落实却错，且 facts 没写「参数默认 `%{private}`、先 `MaskUtil`」的惯例。

机制侧根因：① 被测链路无「逐行裁决落盘」门禁（P11 只在测试域跑）；② 门禁只查落点实体存在，不查 md/yaml 一致、不查 step 是否业务步骤；③ facts 无裁决行；④ 装配指令只投给 verifier 的 ai-prompt（`on_context_load` 片段唯一去向是 `harness-runner.ts:815 assembleAIPrompt`），作者靠自觉翻到；⑤ 实例 hook 串台（见 T11）；⑥ 协调器不自动应答（见 T12）、隔离 workspace 缺 mock-data。

---

## 2. 改动总表（执行顺序即编号顺序；同一编号内可并行）

| # | 文件 | 改什么 | 验证 |
|---|---|---|---|
| T0 | `doc/extensions/knowledge/facts/codebase-facts.md` §5 + 新增「资源」一面 | §5 三条惯例：VOC 默认门面（builder 仅只上报场景且不 set）；同一处 VOC/Chart 二选一；日志参数默认 `%{private}`、敏感字段先 `MaskUtil`。新面：资源文件真实路径与契约登记惯例 | selfCheck 0；M18 |
| T0b | `framework.config.json` `paths.ui_kit_target_dir` + 运行 profile 的 `ui-kit-scaffolder.ts` 物化 Maison blocks | 实例前置：不做则任何 Case 到不了 coding 闭环 | 对 AR90006 workspace 副本重跑 coding harness，`ui_kit_source_conformance` 不再 halt |
| T1 | `doc/extensions/hooks/shared/knowledge.mjs` | `parseConstraintFile` 恢复 `appliesWhen`、派生 `alwaysApplies` | node --check；`activeKnowledge` 打印每域 alwaysApplies |
| T2 | `doc/extensions/hooks/shared/adjudication.mjs`（新） | 必答集派生（spec/plan 两阶段），pre_verifier 与两个 post_check 共用 | 单元级：对 AR90006 派生行数 |
| T3 | `doc/extensions/skills/story/scripts/story-build.mjs` | `knowledge.json` 增 `domains[]`；附录按域分组；集合判据改「适用域全集」；scaffold `_example` 补形状 | 对 AR90006 副本跑 build |
| T4 | `doc/extensions/hooks/spec/post_check.mjs` | 域级一致性；§10 与登记源同文；`knowledge_adjudication_persisted`；story.md 改为调用 `story-build check` + `merge-story --check` | 夹具 P15/P16 + AR90006 副本 |
| T5 | `doc/extensions/hooks/shared/pre_verifier.mjs` | 行集改用 T2；报告表头要求 | 对 AR90006 生成清单，行数≈14/13 |
| T6 | `doc/extensions/hooks/plan/post_check.mjs`；`rules/plan-rules.overlay.yaml` | post_check：`knowledge_step_is_business_step`、`knowledge_landing_consistent`、`knowledge_adjudication_persisted`；overlay：`review_gate_acknowledged`（MINOR，语义层） | 夹具 P17/P18 + AR90006 副本 |
| T7 | `doc/extensions/rules/plan-rules.overlay.yaml`、`doc/extensions/hooks/plan/on_context_load.md` | `knowledge_facts_reuse`；「项目知识影响」按新增能力写 | M14 仍 PASS |
| T8 | `doc/extensions/rules/spec-rules.overlay.yaml` | 判据描述改「登记源一次裁决」 | M14 |
| T9 | `hooks/spec/story_on_context_load.md`、`skills/story/reference/constraint-usage.md`、`skills/story/rules/rules.md`、`skills/story/SKILL.md` | 判定顺序先域后条；§10 从登记源抄；装配指令送达作者；图链引用约定 | M14/M17 |
| T10 | `doc/extensions/skills/story/scripts/merge-story.mjs` | `--check` 增图链存在性 | 夹具 P14 |
| T11 | `.claude/hooks/check-phase-completion.mjs`、`.claude/hooks/record-verifier-report.mjs` | 会话归属按 transcript 证据；不 mkdir feature 目录；verdict 提取收紧；payload 落盘 | `test/story/tests/test_hooks_session.py` |
| T12 | `test/story/scripts/run_multi_case.py` | 自动应答按 `(awaiting_turn, awaiting_since)` 判新关卡 | `test_multi_case_cli.py` 新用例 |
| T13 | `test/story/scripts/run_multi_case.py` | workspace 允许 `test/story/mock-data/` | plan 演练 `workspace-boundary` 含该例外 |
| T14 | `test/story/cases/*/case.yaml` | 4 个 Case `end_phase: review`，提示词同步 | plan 演练 |
| T15 | `test/story/TEST.md`、`test/story/AGENTS.md` | 观测者纪律；坑册 | — |
| T16 | `test/story/regression/failure-modes.yaml`、`test/story/fixtures/failure-modes/*`、`test/story/scripts/check_failure_modes.py` | P14–P18 五个产物形态 + 夹具 + checker；P11 口径收窄；JS/Python 必答集口径绑定测试 | 36/36 |
| T17 | 批次 1 `01-判据.md`、`02-冻结清单.md`、`STATUS.md`、`03-交付报告.md` | 判据口径；冻结第八次重出；记录；七字段 | 冻结 0 漂移 |
| T18 | `hooks/{spec,plan,coding,review,ut,testing}/post_check.mjs`（共用 `hooks/shared/evidence.mjs` 新） | 扩展门禁**通过也留痕**：每次运行写 `<feature>/<phase>/reports/ext-post-check.json`（判据 id、结果、时间、被检产物指纹）；ok:false 行为不变 | 直调后文件存在；K-C1 从此有证据 |

---

## 3. 逐项规格

### T0 `knowledge/facts/codebase-facts.md` §5 可观测性（知识内容，用户指出的三条惯例缺陷）

实跑证据：`AutoTopupRepository.ets:47-56,90-112` 用 `vocBuilder` 挂满 setter；`:90-121` 三处终态点 VOC + Chart 同时发；
`AutoTopupCloudClient.ets:21,42`、`AutoTopupContractCache.ets:27,54` 以 `%{public}s` 打原始 `account`，全部文件 `cardId`/`contractNo` 皆 `%{public}`。
主仓既有 `Logger` 用法无敏感参数先例，facts 没写惯例，模型就按 ArkTS 默认写了 `%{public}`。

§5 改为（入口与「无」结论不变，只改惯例句）：

```
## 5. 可观测性 — `confirmed: 已确认`

日志、VOC、Chart 三渠道齐备，都在 `CommFunc`，惯例如下：

- **日志** `Logger`（`shared/log/Logger.ets`，封装 `hilog`，`debug` / `info` / `error`）：本地调测与完整流程还原——
  流程每一步的进入与结果、分支走向、异常都记。**参数默认 `%{private}s`；账号、卡号、手机号先 `MaskUtil` 再打，
  不用 `%{public}` 放行敏感字段。**
- **VOC**：默认用门面 `WalletHAManager.logAndReport / logErrorAndReport / logDebugAndReport(eventID, desc)`
  （对应档日志 + 一条 VOC，一行）；只在「只上报、不打日志」时用 `vocBuilder(eventID, desc).report()`，不额外 set 属性。
  记关键路径事件：关键步骤的进入与完成、重要状态标记的变更、用户中止与失败。
- **Chart** `WalletHAManager.chartBuilder(WalletHAEventID, funcID, subFuncID).setWalletFuncResult(<WalletFuncResult>).report()`：
  成功率、时延、终态统计——业务步骤到达终态时每步一条。
- **同一处代码 VOC 与 Chart 二选一**：到达终态的那一处只发 Chart，过程关键点只发 VOC；不在同一点两个都发。
- 三渠道共用同一次执行的流程与步骤标识（上报经 `setFuncID` / `setSubFuncID` 携带）；密度日志 > VOC > Chart，不设数量配额。
- 上报非阻塞，失败只记 `Logger.error`，不向调用方抛出。
```

**新增一面「资源」**（编号接在现有面之后；实跑证据：AR90006 契约 `resource_keys.path` 写成 `02-Feature/WalletMain/src/main/resources/base/string.json`——仓内不存在此路径；`files` 未列任何资源 json，coding `resource_integrity` 判 75 处 `$r` 未定义；历史契约如 `DTS90006/contracts.yaml:428-430` 都列了 `…/resources/base/element/string.json`）：

```
## N. 资源 — `confirmed: 已确认`

- 字符串/颜色/尺寸在各模块 `src/main/resources/base/element/{string,color,float}.json`，
  多语言与深色同名文件放 `zh_CN/element/`、`dark/element/`；代码用 `$r('app.string.<key>')` 引用。
- 新增或改动资源键时，改到的资源 json 与代码文件一样列进契约 `files`——coding 的资源完整性门禁只认 `files` 里登记的资源文件。
```

改完跑 `run_self_check.mjs`（0 问题）与 M18；`knowledge-writing-scale` 尺度核对：每句都是「入口 + 一句规则」。冻结清单随 T17 重出。
（`%{private}`/`MaskUtil` 一句同时落实 SEC-01 在日志出口的惯例——规约说必须脱敏，facts 说在这个仓怎么脱。）

### T0b 实例前置：UI kit 物化

背景事实：scaffolder 四级解析（`ui-kit-scaffolder.ts:8-11`）第 2 级要求「架构里 id 含 common 的层内唯一模块」；本仓 DSL 声明了 `03-CommonBusiness` 层但目录不存在、零模块，故落到第 4 级 halt（候选 6 模块）。这是实例初始化从未做的前置，与 story/extension 无关；被测模型提议物化进 `WalletMain`（只因 CommUI 在 out_of_scope）在架构上错——scaffolder 头注释明写「生成进宿主工程公共层」。

- `framework.config.json` 增 `"ui_kit_target_dir": "05-SystemBase/CommUI/src/main/ets/maison_ui_kit"`（profile 推荐形态 `<公共层模块>/src/main/ets/maison_ui_kit`；CommUI = `doc/module-catalog.yaml` 里的公共 UI 权威模块）。
- 运行 profile 提供的物化器（只读 `framework/profiles/hmos-app/harness/ui-kit-scaffolder.ts` 的用法注释后执行，不改它）把 Maison blocks 落到目标目录；产物是实例产品代码，随本轮提交。
- 验证：复制 AR90006 workspace 到 `scratch/probe-coding/`，`harness-runner.ts --phase coding --feature AR90006 --project-root <副本>`，`ui_kit_source_conformance` 须从「halt」变为「源码段判定」（此时 AR90006 代码未用 block，FAIL 是预期——它证明检查已可运行）。
- 写进 `TEST.md` §2 起跑前检查：`ui_kit_target_dir` 已配置且目标目录含 blocks，否则不起跑。

### T18 扩展门禁留痕（`hooks/shared/evidence.mjs` 新）

`writePostCheckEvidence(ctx, {checks:[{id, status, detail}], inputs:[{path, sha256}]})` → `<feature>/<phase>/reports/ext-post-check.json`（覆盖写）。六个 `post_check.mjs` 在 return 前调用一次，`ok:true` 也写。P11 的报告文件名组不含它（它不是 verifier 报告）。批次 1 判据 K-C1 的证据即此文件。

### T1 `hooks/shared/knowledge.mjs`

`parseConstraintFile`（现 `:94-148`）返回对象补两个字段：

```js
appliesWhen: fm.applies_when ?? '',
alwaysApplies: String(fm.applies_when ?? '').trim() === 'always',
```

`activeKnowledge` 返回值不变（constraints[] 自带这两个字段）。文件头注释「三类知识的定位见 knowledge/README.md」保留。
**消费者**：T2、T3、T4（没有消费者的字段不加——上一轮删它正因为无人读）。

### T2 `hooks/shared/adjudication.mjs`（新建）

唯一职责：派生「本阶段必答集」，给 pre_verifier（注入）与 post_check（落盘核对）共用，两边不各存一份。

```js
/**
 * 必答集 —— 本阶段 verifier 必须逐行裁决的集合（收窄后的）。
 * spec：登记源 knowledge.json 的命中行 + 域级不适用行 + 模式候选行。
 *       **不含** story 附录行与 spec §10 行——它们由登记源渲染/同文校验，裁一次即可。
 * plan：knowledge_freeze 的义务行 + selected 模式行 + facts 复用一行。
 * 返回 { rows: [{source, key, text, hit}], error }；派生失败 error 非空，调用方须出声。
 */
export function adjudicationSet(projectRoot, feature, phase, knowledge)
```

- spec 行来源：`AR/story-src/knowledge.json`
  - `constraints[]`：每条 `{source:'规约判定', key:id, text:conclusion, hit:hit===true}`；
  - `domains[]`（T3 新增）中 `applies:false` 的：`{source:'域级判定', key:prefix, text:basis, hit:false}`；
  - `patterns[]`：`{source:'模式候选', key:unit, text:candidate+'｜'+signal}`。
- plan 行来源：`contracts.yaml > knowledge_freeze`（用 `freeze.mjs` 的 `readContracts/readFreeze`）
  - obligations：`{source:'冻结义务', key:rule, text:obligation}`；
  - patterns `selected:true`：`{source:'模式冻结', key:pattern_id, text:'实例 …｜角色 …'}`；
  - 固定一行：`{source:'项目知识复用', key:'facts_reuse', text:'方案新增的能力是否先复用项目知识登记的入口'}`。
- 导出 `adjudicationKeys(rows)` → 字符串数组（去重），post_check 用它核对报告。

### T3 `skills/story/scripts/story-build.mjs`

**3.1 `knowledge.json` 形状**（scaffold `_example`，`:919-937`）补：

```json
"domains": {
  "prefix": "条件域的前缀（applies_when 不是 always 的域才登记；always 域不登记）",
  "applies": "true / false",
  "basis": "为什么本需求适用/不适用这个域，一句"
}
```

顶层加 `"domains": []`。

**3.2 `buildKnowledgeAppendix`（`:607-677`）改判据**：

1. 读 `reg.domains`（`readJson(ctx.knowledgePath).domains`），建 `domainApplies: Map<prefix, {applies, basis}>`。
2. 从 `knowledge.constraints` 取条件域集合 `conditional = constraints.filter(c => !c.alwaysApplies).map(c => c.domain)`。
   - 条件域未在 `domains[]` 登记 → problem：`条件域 ${prefix} 未做域级判定——按该域 applies_when 判适用与否，写 basis`。
   - `domains[]` 出现非条件域或不在册前缀 → problem：`domains 登记了不是条件域的前缀 ${prefix}`。
3. **期望条目集** = `always` 域全部条目 ∪ `applies:true` 条件域全部条目。现有「missing」判据（`:649-653`）改为对期望集判；`applies:false` 域的条目若出现在 `constraints[]` → problem：`${id} 所在域已判不适用，不应逐条登记`。
4. `renderKnowledgeAppendix(rows, domainRows)`：表头不变；按域分组输出——先 `always` 域与 `applies:true` 域的条目行，再每个 `applies:false` 域一行：`| ${domainTitle} | — | — | 否 | 不适用：${basis} |`。域中文名取 `knowledge.constraints.find(c=>c.domain===prefix).title`。
5. `build` 处（`:1038`）调用签名同步。

**3.3 `check` 子命令**沿用 build 的同一套判据（现有实现即如此，确认无需改）。

### T4 `hooks/spec/post_check.mjs`

**4.1 域级一致性**（加在 `knowledgeExitProblems` 的三方 ID 比对之前）：读 `knowledge.json`，与 T3 同判据（复用一份实现：把 T3 的判定抽到 `hooks/shared/adjudication.mjs` 导出 `domainProblems(knowledge, registry)`，story-build 与 post_check 都调它——不复制）。

**4.2 §10 与登记源同文**：对 §10 每行，`要求` 列规范化后须等于 `knowledge.json` 同编号 `conclusion` 规范化后的值。规范化函数用 `hooks/shared/paraphrase.mjs` 已导出的 `normalize`（若未导出则导出它；不另写）。不等 → BLOCKER：
`spec.md:${line} ${id} 的要求与登记源（knowledge.json）不同文——§10 从登记源逐条抄，同一结论只有一份`。

**4.3 `knowledge_adjudication_persisted`**：
- 依据的流程事实（框架设计，非副作用）：阶段闭环 = harness 首跑 → verifier → **主 agent 重跑 harness 回填 receipt**（Stop hook 文案与 CLAUDE.md §5 都以此为闭环步骤）。post_check 在每次 harness 运行时执行，因此闭环那次运行必然能看到 verifier 报告；本判据就挂在这里。
- 报告路径：`<feature>/spec/reports/` 下匹配 `verifier.report.md | verifier-*.md | verify-*.md | verifier-*-result.yaml`（与 TEST.md §5 同一组名）。
- 无报告 → `ok` 不受影响，T18 留痕文件写 `{id:'knowledge_adjudication_persisted', status:'NOT_APPLICABLE', reason:'verifier 尚未执行'}`（**不在 message 里塞前缀**）。
- 有报告 → `keys = adjudicationKeys(adjudicationSet(...).rows)`；对每个 key，报告文本中须存在**同一行**含 key 且含 `PASS|FAIL|WARN|不适用|设计|复述` 之一；缺任一 → BLOCKER：
  `verifier 报告未逐行裁决：漏 ${missing.join('、')}（共 ${n}）——裁决表须写进报告文件本身，表头「| 编号 | 裁决 | 证据 |」`；通过时留痕文件写 `PASS` 与核对到的行数。

**4.4 story.md 检查升级**：`PHASE_ARTIFACTS` 的 story.md 项，存在性之外增加：
```js
spawnSync(process.execPath, [path.join(extRoot,'skills/story/scripts/story-build.mjs'),'check','--feature',ctx.feature,'--project-root',ctx.projectRoot], {encoding:'utf-8'})
spawnSync(process.execPath, [path.join(extRoot,'skills/story/scripts/merge-story.mjs'),'--feature',ctx.feature,'--check','--project-root',ctx.projectRoot], ...)
```
非零退出 → BLOCKER，message 带子进程 stderr 前 400 字符。`extRoot` 用 `paths.mjs > extensionRoot`。**不重实现判据**。

### T5 `hooks/shared/pre_verifier.mjs`

- `specRows` / `freezeRows` 删除，改为 `adjudicationSet(...)` 的 rows 再 `classify`（保留排序信号逻辑：`mkRow` 对 `规约判定`/`冻结义务` 行做 `classify`，其他行 `verdict:'CLEAN'`）。
- 输出要求段补一句：
  `裁决表**必须写进 verifier 报告文件**（<phase>/reports/verifier.report.md），固定表头「| 编号 | 裁决 | 证据 |」，一行一条；只写在 YAML 输出里不算——重跑 harness 时门禁核对的是报告文件。`
- 注释头「为什么必须是全集」改为「为什么是收窄后的全集」：全集 = 登记源的每一行（命中行、域级不适用行、候选行），不再重复裁 story 附录与 spec §10（它们由登记源渲染/同文校验）。

### T6 `hooks/plan/post_check.mjs`

**6.1 `knowledge_step_is_business_step`**（改现有 `:197-207`）：
```js
const criterionId = String(ob.criterion ?? '').trim();
if (step === criterionId) problems.push(`${at}：step 与 criterion 相同（${step}）——step 是业务步骤（验收条目的 prd_function 或 use-cases 的 branch id），不是验收编号`);
else 原有定位判据，但 acceptance 侧只认 c.prd_function === step（去掉 c.id === step 这条——正是它放过了 AC-Kn）。
```

**6.2 `knowledge_landing_consistent`**：读 plan.md「知识决策」章内表头含「编号」与「落点」的表（用 `findHeadings` 定位章、沿用 spec post_check 的 `tableRowsIn/cellOf` 思路，按列名取）。对每条 obligation：`landingRefs(ob)` 的每个 ref 取 `resolveEntityRef(...).tail`；至少一个 tail 出现在同编号行的「落点」单元格（大小写敏感，`\b` 边界）。否则 BLOCKER：
`${at}：plan.md 落点「${cell}」与契约 landing（${tails.join('、')}）对不上——md 表与 yaml 是同一份冻结，写一处改一处`。
无该表 → BLOCKER：`plan.md 知识决策章缺「规约义务」表（须有编号与落点两列）`。

**6.3 `knowledge_adjudication_persisted`**：同 T4.3，phase=plan，报告目录 `<feature>/plan/reports/`。

**6.4 `review_gate_acknowledged`**——hook 结果只有 `ok:false` 一个正规出口（`hooks-dispatcher.ts:181-191`），没有 WARN 通道，所以**不做成 post_check**。它是语义层的事，放 `rules/plan-rules.overlay.yaml`：
```yaml
  review_gate_acknowledged:
    description: >
      决策件 AR/review.md 的「下一步」节须有一项勾选，且「有其他意见」议题已回写；未勾选即进入 plan 时，
      判 WARN 并写明哪些开放议题（status=open）尚无表态。评审人线上表态是常态路径，本项不阻断。
    severity: MINOR
    ai_prompt_hint: 读 AR/review.md「下一步」两个复选框与各议题「审核结果」；列出未表态的 DEC 编号。
```
它不以 `knowledge_` 开头，不进必答集；verifier 按 overlay 常规裁。

### T7 plan overlay 与注入件

`rules/plan-rules.overlay.yaml` 的 `semantic_checks` 增：
```yaml
  knowledge_facts_reuse:
    description: >
      方案新增的每一类能力（持久化/内存态、日志与上报、脱敏、导航、配置、端云调用）是否先复用了
      项目知识登记的入口；未复用的是否有理由；与登记不符的是否登记了矛盾。
      不按项目知识的面逐面裁——只裁本方案实际新增的能力。任一类新增能力既未复用登记入口
      又无理由即 FAIL。
    severity: MAJOR
    ai_prompt_hint: >
      列出 plan.md/contracts.yaml 里本需求新增的能力，逐类写「复用了哪个登记入口（写名字）/ 未复用的理由 /
      与登记不符的矛盾」，证据给 plan.md 或 contracts.yaml 行号。项目知识文件在激活清单里，自己打开对照。
```
`hooks/plan/on_context_load.md` 第 15 行「项目知识影响」改为：`### 项目知识影响 —— 按本方案新增的能力逐项写：复用了哪个登记入口 / 未复用的理由 / 与登记不符的矛盾（不按项目知识的面逐面写）`；§六 FAIL 判据列表追加第 12 条「plan.md 义务表落点与契约 landing 不一致」、第 13 条「step 与 criterion 相同」；§二 冻结 YAML 注释 `step:` 改为 `<业务步骤：验收条目的 prd_function（F 编号）或 use-cases 的 branch id；不是验收编号>`。

### T8 spec overlay

`knowledge_spec_exit_substance.description` 中「逐行列出了三处判定结论（归档件附录…、spec 约束要求章…、模式候选登记）」改为「逐行列出了登记源（knowledge.json）的判定结论：命中条目的结论、条件域的不适用依据、模式候选登记——story 附录由登记源渲染、spec §10 与登记源同文，同一结论只裁一次」。`ai_prompt_hint` 补「裁决表写进报告文件，表头「| 编号 | 裁决 | 证据 |」」。

### T9 写作指令与 SKILL

- `hooks/spec/story_on_context_load.md:87-93`（出口 3）改为：
  「逐条目登记 → 先按各规约域 frontmatter 的 `applies_when` 判域：`always` 域直接逐条判；条件域先写 `domains[]` 一行（适用/不适用 + basis），不适用的域不再逐条；适用域逐条判，判『否』也写依据。spec §10 的『本需求的要求』列从登记源逐条抄——它与附录是同一份结论。」
- `skills/story/reference/constraint-usage.md:52-60`「判定纪律」第一条改为：「**条件域先判域，`always` 域逐条**：域级不适用是带依据的判定记录，不是跳过；适用域内条目一律平权逐条判」。`:23-39` 符合性表写法补一条：「条件域不适用时附录只渲染一行，由装配器按 `domains[]` 生成」。
- `skills/story/rules/rules.md` 红线 2（`:24-29`，图片形态）补：「图片引用路径按 inbox 导入约定写 `../assets/<文件名>/…`；不写 `ux-reference/`、不写裸文件名——装配与门禁按 `AR/` 相对路径解析，解析不到即断链」。
- `skills/story/SKILL.md:296`「然后直接进入 spec 阶段」段后补：
  「**spec 三产物中的 `AR/story.md` 只能由装配器产出**：`story-build.mjs scaffold` → 逐章成文 → `build` → `check` → `merge-story.mjs --check`，命令与顺序见 `hooks/spec/story_on_context_load.md`「逐章成文」段（那里是唯一真源，此处不复制）。手写 story.md 会被 spec 门禁拦下。」
- 上述四处都要过 M17（引用的路径/编号在册）与 M14（自检问句仍在）。

### T10 `skills/story/scripts/merge-story.mjs`

「归档件红线」段（`:199-212`）增第四项：
```js
['图片断链', 'image', scanBrokenImages(story, path.dirname(storyPath))]
```
`scanBrokenImages(text, baseDir)`：匹配 `!\[[^\]]*\]\(([^)\s]+)\)`，跳过 `http(s)://` 与 `data:`，`path.resolve(baseDir, p)` 不存在即命中，返回 `[{line, path}]`；`formatHits` 补 `image` 分支输出 `第 N 行 → <path>`。

### T11 实例 hook（`.claude/hooks/`）

**根因**：`check-phase-completion.mjs:336-356`（`evaluateSessionStaleness` 分支 2）——state.session_id 为 null 且在 grace（5 分钟）内，**第一个触发 Stop 的会话被盖章为所有者**。同仓两会话时，用户在 A 跑 harness，维护者 B 恰好结束消息 → B 被盖章，A 变成「跨会话陈旧」（只 advisory），B 被反复拦截。`record-verifier-report.mjs` 只按 state 定位目录，同样串台（本轮 3 次，含把已迁走的 feature 目录复活）。

正向原则：**state 的所有者 = 其 transcript 里有对该 feature/phase 的 harness 调用记录的会话**。这是会话自己留下的一手记录，不是猜测；grace 窗口只保留为「记录尚未写入」的容忍，不再作为所有权依据。框架不写 session id（`framework/` 只读），所有权只能在实例 hook 判——这是实例层该承担的职责，不是绕路。

改法（两个 hook 同一函数，放 `.claude/hooks/hook-session-evidence.mjs` 新文件，两边 import）：
```js
/** 本会话是否为 <feature>/<phase> 跑过 harness：读主会话 transcript（payload.transcript_path），
 *  逐行 JSON 抓 tool_use 的 command 字段，匹配 harness-runner.ts 且同时含 --feature <feature> 与 --phase <phase>。 */
export function sessionRanHarness(transcriptPath, feature, phase)
/** 本会话是否调过 verifier 子 agent：tool_use name=Task 且 input.subagent_type==='verifier'。 */
export function sessionSpawnedVerifier(transcriptPath)
```
- `check-phase-completion.mjs`：分支 2（state 未盖章）改为 `shouldStamp = ageMs <= gracePeriodMs && sessionRanHarness(...)`；不满足 → `kind:'stale-cross-session'`，reasonHuman：`本会话的 transcript 未跑过 --feature <f> --phase <p> 的 harness，state 属于另一会话`。分支 1 不变。
- `record-verifier-report.mjs`：`useStateDir = state存在 && sessionRanHarness(...) && sessionSpawnedVerifier(...)`；否则写 `framework/harness/state/last-verifier-report.*`，**不创建任何 feature 目录**（写前 `fs.existsSync(reportDir)` 为 false 也不 mkdir）。`extractVerdict` 只认 ① `^\|\s*编号\s*\|` 表头之后的表行（有任一 FAIL → FAIL，否则全 PASS → PASS）② `^\s*-?\s*verdict\s*[:：]\s*(PASS|FAIL)` 行；删掉从任意文本抓 `\b(PASS|FAIL)\s*\(verdict\)` 的分支。
- 两个 hook 在 main 开头把 payload 写 `framework/harness/state/last-hook-payload.<hook_event_name>.json`（覆盖写；该目录本就是 state 目录，`.gitignore` 已含）。
- 单测 `test/story/tests/test_hooks_session.py`：用 `subprocess.run(['node', hook], input=json.dumps(payload))` 黑盒跑，临时目录里造 `framework.config.json`、state、transcript（jsonl 两行：一行含 harness 命令、一行不含）。用例：① 同会话（transcript 含命令）→ 拦截 exit 2；② 另一会话（不含）→ exit 0 且 state 未被盖章；③ record hook 在 ② 情形下不创建 `doc/features/<f>/<p>/reports`。

### T12 协调器自动应答（`test/story/scripts/run_multi_case.py`）

**根因**：`:1432-1434` 只在 `last_reply_status != "accepted"` 时调 `send_scripted_reply`；`accepted → consumed` 仅在 poll **看见** `WAITING → ACTIVE`（`:1405-1409`）时转换。被测模型在一个 15 秒周期内消费回复并抛出下一关卡（`awaiting → running → awaiting`），poll 前后看到的都是 `awaiting`，`last_reply_status` 永远停在 `accepted`，第二关卡再也不发（本轮 turn 2 与 turn 3 各复现一次）。

正向改法——**回复的身份由关卡编号决定，不由「上一次回复被消费了没有」推断**（后者依赖观测时机，本就不可靠）：
- record 新增 `last_replied_turn`（`send_scripted_reply` 成功与 adaptive 发送成功时写 `awaiting.turn`）。
- `:1432` 条件改为 `status == WAITING and interaction_script and is_new_gate(record)`，`is_new_gate = last_awaiting.turn is not None and last_awaiting.turn != record.get('last_replied_turn')`。
- **删除** `:1405-1409` 的 `accepted → consumed` 转换及 `last_reply_status` 对发送的门控：`last_reply_status` 只剩展示语义（accepted/rejected），不再是状态机的一部分；`reply_consumed` 事件改由 `is_new_gate` 首次为真时（即上一关卡已被消费、新关卡出现）记录。两套并存即违反正向判据 ①。
- 单测（`test_multi_case_cli.py`，仿 `:289-303`）：record 处于 awaiting turn=1 → `send_scripted_reply` 发送（mock invoke_case 返回 0）→ 手动把 `last_awaiting` 改为 turn=2、`last_reply_status` 仍 `accepted` → 再调一次 → 断言 `invoke.call_count == 2` 且 `interaction_index == 2`；反例：turn 未变 → 不重复发送。

### T13 workspace 允许 mock-data

workspace 拷贝已是**显式白名单**设计（`WORKSPACE_ALLOWED_DOC_FILES` 等常量，`:100-118`）。正向做法是把替身数据纳入同一套白名单，而不是在排除逻辑里开特例：
- 新增常量 `WORKSPACE_ALLOWED_TEST_DATA_DIRS = ("test/story/mock-data",)`，与 `WORKSPACE_ALLOWED_DOC_FILES` 并列声明并注释「被测流程的替身数据（需求系统回稿等），不是维护材料」；
- `create_workspace_template` 在复制 doc 白名单文件的同一段里复制这些目录；`workspace-boundary.json` 的 `copied` 自然含它们，`excluded` 仍写 `test/**`（白名单条目优先于目录排除，boundary 里新增 `allowlisted_under_excluded: [...]` 一并记录）；
- `WORKSPACE_FORBIDDEN_RUNTIME_DIR_NAMES` 的越界检查（`:1672`）按白名单常量判定，不写路径字面。
TEST.md §2 的「递归排除 `test/`」句改为「递归排除 `test/`，白名单常量声明的替身数据目录除外」。

### T14 Case 定义

四个 `test/story/cases/*/case.yaml`：`end_phase: review`。提示词中「随后执行 /spec … 并完成 spec 闭环」后补一句「继续按流程执行 plan、coding、review 阶段并各自闭环，到 review 为止，不进入 UT 或真机测试」（`pattern-image-review` 已有此句，只改 `end_phase`）。`split-interactive` 的 `review_feedback` 保留。

### T15 TEST.md / AGENTS.md

- TEST.md §0.1 补：「实跑期间宿主**不读被测产物内容、不开子 agent、不在主工程跑 harness**；评测在 finalize 之后做。」§6 补：「finalize 前确认主工程 `framework/harness/state/.current-phase.json` 不存在或不属于本次 feature，否则宿主会话的 hook 会把伪报告写进回灌后的产物。」
- AGENTS.md §4 补两条坑：「同仓多会话时实例 hook 按 grace 窗口盖章会串台——盖章须有本会话跑过 harness 的 transcript 证据」；「观测者读产物内容会把评价带进关卡回复，污染交互」。

### T16 台账与夹具

`failure-modes.yaml` 新增五个**产物层**形态（编号规则：机制层 M、产物层 P；这五条都查 feature 产物，故为 P14–P18）。字段照既有条目；`dimension: knowledge`；`target: product`；`found_round: "2026-08-25"`；`status: fixed`；`severity: BLOCKER`；`clause` 分别引 B1-13 / B1-8 / B1-9 / B1-10 / B1-10。

| id | checker | bad 夹具 | good 夹具 |
|---|---|---|---|
| P14-image-broken-link | `p14_image_broken_link` | `AR/story.md` 引 `![x](missing.png)` | 引 `../assets/a/x.png` 且文件存在 |
| P15-domain-gating-not-applied | `p15_domain_gating_not_applied` | `AR/story-src/knowledge.json`：条件域无 `domains[]` 登记，且该域条目逐条出现 | `domains[]` 登记 `applies:false`，该域条目不出现 |
| P16-spec-exit-diverges-from-registry | `p16_spec_exit_diverges` | `spec/spec.md` §10 某行「要求」与 `knowledge.json` 同编号 `conclusion` 不同文 | 同文 |
| P17-landing-md-yaml-mismatch | `p17_landing_md_yaml_mismatch` | `plan/plan.md` 义务表落点 `Foo`，`plan/contracts.yaml` 同编号 landing `interfaces.Bar` | 一致 |
| P18-step-equals-criterion | `p18_step_equals_criterion` | `obligations[].step == criterion` | `step` = 该验收条目的 `prd_function` |

台账总数 31 + 5 = **36**。

夹具用中性题材（M02 反过拟合），条目编号用 `SMP-01` 一类占位域（`PLACEHOLDER_PREFIX_RE` 不认 `SMP`，所以夹具目录须自带 `_knowledge/` 迷你规约文件，形态照 `fixtures/failure-modes/M17-*/good/knowledge/sample-domain.md`，其中一个域 `applies_when: always`、一个域 `applies_when: 需求含界面`）。checker 从 `ctx.knowledge_root` 派生条目与域（`load_constraint_entries` 现只取编号/约束/处置，需补 `applies_when` 的解析：读文件 frontmatter）。

P11 条目：`applicable_when` 改为「被测链路已有落盘门禁（T4/T6）；本条仍对真实目标跑」；`form` 补「必答集 = 登记源命中行 + 域级不适用行 + 候选行」；`p11` 的 `required` 派生改为：story 附录里判「是」的编号 ∪ 附录中「不适用：」行的域前缀 ∪ 候选表单元（与 `adjudication.mjs` 同口径；Python 侧独立实现但口径相同——M15 式的双实现绑定：新增 `test_adjudication_parity.py`，对 AR90006 产物比对 JS 与 Python 派生的 key 集合相等）。

### T17 批次 1 文档

- `01-判据.md`：K-B3 口径改「登记源命中集 = spec §10 集 = acceptance 集 = 冻结义务集；story 附录由登记源渲染不再单列」；K-B5 改「裁决对象 = 登记源命中行 + 域级不适用行 + 候选行（spec）/ 义务行 + 模式行 + facts 复用行（plan）」；新增 K-B13「条件域先判域，`always` 域逐条」（机械 T3/T4，形态 P15）、K-B14「§10 与登记源同文」（机械 T4，形态 P16）、K-B15「facts 复用一条语义判据」（语义 T7）、K-B16「逐行裁决落盘」（机械 T4/T6，形态 P11）、K-B17「plan 落点 md/yaml 一致、step 为业务步骤」（机械 T6，形态 P17/P18）。
- `02-冻结清单.md`：用 `scratch/refreeze.py` 思路（上一轮的 scratchpad 脚本不在仓内，重写到 `scratch/`——`scratch/` 是允许的临时脚本位）全量重算，第八次重出；卷首写明本轮改动集合；新增文件（adjudication.mjs、hook-session-evidence.mjs、5 组夹具、新单测）加入对应分节。
- `STATUS.md`：新增「实跑缺口修复」小节：根因（P11 落盘、串台盖章、自动应答、图链）、定论〇、改动 T1–T17、验证结果；冻结条款 B1-13 收窄的裁定记录。
- `03-交付报告.md` 第四栏：story/spec/plan 七字段按本次实跑填（artifact 完成 / execution 完成 / harness PASS / verifier PASS 但逐行未留证 / receipt 通过 / closure 四件套齐 / target ✓）；coding/review 待本轮实跑。

---

## 4. 判据文案与数据形状速查（执行时直接抄）

**knowledge.json**：
```json
{
  "domains": [ { "prefix": "UX", "applies": true, "basis": "新增签约页" },
               { "prefix": "RES", "applies": false, "basis": "无新增图片，文案走 string 资源已在 RES-02 判" } ],
  "constraints": [ { "id": "UX-01", "hit": true, "conclusion": "…本需求实体…", "landing": "…" } ],
  "patterns": [ { "unit": "…", "candidate": "decision-tree", "signal": "…" } ]
}
```
（注意：`applies:false` 的域不应再出现 `constraints[]` 条目；`always` 域不进 `domains[]`。）

**verifier 报告裁决表**（pre_verifier 要求、post_check 核对）：
```
| 编号 | 裁决 | 证据 |
|---|---|---|
| UX-01 | 设计 | spec.md:344 落点 subscribe-* 节点 |
| RES | 不适用 | 无新增图片（knowledge.json domains.basis） |
| decision-tree | 设计 | plan.md:82 |
| facts_reuse | PASS | 复用 NavPathContext/AppStorage；MaskUtil 未复用无理由 → 见 details |
```

**BLOCKER 文案原则**：说清「哪一行、缺什么、去哪补」；不写「知识未被应用」这类语义结论（G3）。

---

## 5. 验证（全部通过才提交）

```bash
# 语法
for f in doc/extensions/hooks/**/*.mjs doc/extensions/skills/story/scripts/*.mjs .claude/hooks/*.mjs; do node --check "$f" || exit 1; done
# 派生：8 域 / 15 条目 / 2 模式 / 2 facts / 4 index；各域 alwaysApplies 打印
node --input-type=module -e "import {activeKnowledge} from './doc/extensions/hooks/shared/knowledge.mjs'; const k=activeKnowledge(process.cwd()); console.log(k.constraints.map(c=>c.domain+':'+c.alwaysApplies).join(' '))"
# 必答集：对回灌的 AR90006（只读）
node --input-type=module -e "import {activeKnowledge} from './doc/extensions/hooks/shared/knowledge.mjs'; import {adjudicationSet} from './doc/extensions/hooks/shared/adjudication.mjs'; const k=activeKnowledge(process.cwd()); for (const p of ['spec','plan']) { const r=adjudicationSet(process.cwd(),'AR90006',p,k); console.log(p, r.rows.length, r.error||''); }"
# 期望：spec ≈ 11 命中 + 0 域级（AR90006 未登记 domains，旧产物）+ 3 候选 = 14；plan = 11 + 1 + 1 = 13
# 台账（含新 5 形态与 P11 新口径）
PYTHONIOENCODING=utf-8 python test/story/scripts/check_failure_modes.py            # 36/36
PYTHONIOENCODING=utf-8 python test/story/scripts/check_failure_modes.py --feature AR90006   # 旧产物：P11/P14/P17/P18 命中是预期（它是反例），记录不计入 PASS
node test/story/scripts/check_paraphrase_branches.mjs                              # 9/9
PYTHONIOENCODING=utf-8 python -m unittest discover -s test/story/tests             # 132 + 新增（hooks 3、协调器 2、parity 1）
cd framework/harness && npx ts-node harness-runner.ts --phase extensions            # PASS 6/6
cd ../.. && python test/story/scripts/run_multi_case.py plan --all --jobs 4 --isolated-workspaces   # 4 Case end_phase=review；boundary 含 mock-data 例外
```

对 AR90006 副本（复制到 `scratch/probe/`，含 framework.config.json）直调两个 post_check（按 hook-runner 契约：stdin JSON `{phase, feature, projectRoot}`）：spec 须报 §10 同文/域级登记缺失（旧产物无 `domains[]`）；plan 须报 step=criterion 与落点不一致；两者的 `knowledge_adjudication_persisted` 须报漏裁。**这是对新门禁的区分力验证**，不是要把旧产物改绿。

机制层负面扫描（TEST.md §7.2 四条 grep）零命中；`grep -rn "定位规则\|取证导航\|回灌" doc/extensions/hooks doc/extensions/skills` 零命中。

---

## 6. 实跑协议（本轮：**单 Case `split-interactive`，跑完 coding**）

用户授权（2026-08-26）：改完即由本会话跑 CLI，单用例，跑到 coding 闭环。Case 定义按 T14 已是 `end_phase: review`——实跑若自然推进到 review 一并记录，**本轮验收下限是 coding 四件套闭环**。选 `split-interactive`：它带交互关卡（验 T12 自动应答）、带 review_feedback，且上一轮同一 Case 只到 plan，可直接对比。

0. **前置自检**（缺一不起跑）：
   - T0b 完成：`framework.config.json` 有 `paths.ui_kit_target_dir`，目标目录已含 Maison blocks——否则 coding 必被 `ui_kit_source_conformance` 拦死。
   - §5 全部命令绿。
   - 主工程 `framework/harness/state/.current-phase.json` 不存在（有则先弄清归属，**勿盲目 clear-state**——可能是用户的 state）。
   - 上一轮 workspace / feature 已归档，不与本轮重名。
1. `plan --case split-interactive --isolated-workspaces` → 核对 `workspace-boundary.json`：含 T13 的 mock-data 白名单，`excluded` 仍有 `test/**`。
2. `start --case split-interactive --isolated-workspaces --suite-id story-suite-<ts> --authorize-non-sandbox`。
3. poll 循环按 TEST.md §3/§4；**观测者纪律（T15）**：只 poll / 回复 / 记录，**不读被测产物内容、不开子 agent 或后台任务、不在主工程跑 harness**。脚本外关卡按「推进本场景所需的最小回复」处理，回复内容不含测试域材料与评价。
4. coding 闭环（或 `end_phase` 终态）后 `finalize --promote`。
5. 回灌后评测：`check_failure_modes.py --feature <feature>`（P11/P14–P18 须 PASS）+ 01-判据 逐条 + 七字段填 `03-交付报告.md`；G5 总判定。重点核对新门禁的实战效果：逐行裁决是否真落盘（T4/T6）、facts 三条惯例（VOC 门面 / 日志脱敏 / 资源登记）是否被 coding 遵守（T0）、K-C1 是否留下 `ext-post-check.json`（T18）。

---

## 6.5 实施后的差异记录（以仓内为准）

- **T13 未实施**：仓内没有 `test/story/mock-data/`，也没有任何 Case 引用它——加白名单常量等于新增
  一个没有消费者的死代码（正向判据 ③）。若后续真的需要替身数据，先建目录并有 Case 用它，再进白名单。
- **T14 只改三个 Case**：`split-two-ar` 保持 `end_phase: plan`。它观测的是拆分决策穿过上游三层，
  到 plan 就看全（该文件注释里写了理由），本轮没有新证据推翻它；知识链在 coding/review 的传递
  由其余三个 Case 覆盖。
- **T0 的日志脱敏句返工两次**：写「账号、卡号、手机号先脱敏」被指出把被测需求的字段硬编码进知识；
  改「参数默认 %{private}」又被指出是在教平台 API 用法。定稿为「不打敏感字段，确需记录先脱敏」
  并指向安全隐私规约——**判什么算敏感归规约，facts 只说这个仓在哪脱**。
- **T0b 追加一步**：scaffolder 只把组件写进目标目录，不改跨模块出口；不导出的话，用它的模块只能
  深路径 import（违反架构红线）。已把 9 个组件加进公共 UI 模块的出口文件。

## 7. 不在本轮

review.md 复选框/处置覆盖（评审流程本体）、单元名三处漂移、spec 术语表重复行、`spec.md:252` 编号裸露、关卡 AI 代选后收口、收口报告停等——归批次 2/4。K-H1 的 holdout 与批次 2 共用下一次实跑。模式文档「契约投影」表含 `contracts.yaml` 字段名（C4 字面相斥）——功能正确、正文受 B1-5 冻结，不动。

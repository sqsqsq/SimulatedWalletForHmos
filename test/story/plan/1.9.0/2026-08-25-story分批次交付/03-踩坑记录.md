# 03 踩坑记录

> 状态：**坑册（随批次收口滚动更新）**。只记坑与教训，不记废弃实现细节。
> 来源指针：F 系列见 `2026-08-23-story2.0回退重写/00`；N 系列见
> `2026-08-24-story判据内容化与失效形态回归/00`；Codex 三轮见同目录 `90`；
> 实现级坑见 `2026-08-21-story全量需求与踩坑记录/01` 与各轮实施记录。
> 各批实施前通读本批相关分区；新坑随批次收口入册。

## 1. 设计层：判据与验收（F=回退重写轮，N=判据内容化轮，R=Codex 轮次）

| # | 坑 | 教训 |
|---|---|---|
| 1 | 关键词硬匹配章节、匹配不到判失败（已回滚，冻结 01-C5 点名禁止复发） | 软定位+回落读整篇是底线 |
| 2 | 形态判据管语义目标，同一失效换皮四次（流程图→编号清单段→索引句→复述） | 形态判据必然被最省力形式满足；语义归 verifier |
| 3 | 每轮只验新增判据，修好的东西换形态回来无人发现 | 台账全量回归（G1） |
| 4 | 实现完再拟验收判据，判据对着自己做过的事 | 判据先行且溯源（G3） |
| 5 | 内容自检标准只给维护者不给被测模型 | 判据与写作指令同批同向（B1-15） |
| 6 | 计数不声明口径（列表块算成长段 → 错误结论） | 口径自声明（G3） |
| 7 | 只数覆盖率就宣布达成（100% 到达掩盖 73–80% 复述） | 覆盖与内容分开验 |
| 8 | 「每章至少一行裁决」冒充「每项事实逐项核对」（R2-P0-1） | 覆盖闭环落到来源单元 ID 集合（B2-4） |
| 9 | 数字触发写作命令 = 形式配额换皮（R2-P0-2） | 计数只作诊断（G8） |
| 10 | 已知历史 Case 冒充 holdout（R2-P1-1）；实施前运行样本冒充揭盲（R3-P0-1） | holdout 冻结-揭盲-作废时序（G2）；两条均为永久回归夹具 |
| 11 | 多分支合并计数，单分支全灭也能凑数通过（R2-P1-2） | 校准逐分支冻结预期 |
| 12 | 义务 schema 漏掉冻结点名的阶段（coding，R2-P1-3） | 冻结点名的每阶段必须是 schema 键 |
| 13 | 白名单只防字段缺失不防改值（R2-P1-4） | 守恒比对键与值 |
| 14 | 附录/出口"结论"列复述规约原文（75–90%），结构门禁照收 | 「只列已命中不算应用」的机械代理不存在，全集语义裁决（B1-13） |
| 15 | 风险标签（相似度/专名命中）决定语义裁决覆盖面 → 「可信专名+低相似改写」逃逸（R3-P0-2） | 全集必裁，标签只排序 |
| 16 | 复述判定字段不唯一（渲染要求列天然等于原文，纳入即全员误杀）；笼统"去标点"折叠标识符（R3-P1-1） | 比较字段/子串方向/规范化字符集必须唯一可实施（B1-14） |
| 17 | 模块名门禁不限作用域，会误删附录合法工程范围（R2-P1-5） | 作用域=附录外主叙事+排除 Scope/术语表 |
| 18 | 无总判定规则，分项事实正确但总评过头（R2-P1-6；本人两次误判被用户抓到） | 总判定唯一（G5）；评审工具自带口径自检 |
| 19 | 「到达阶段」混淆产物/执行/门禁/闭环（091443 实证 highest=review 而 plan not_reached） | 七字段分项（G6） |
| 20 | 决策引用修截断制造双写（修一个形态制造另一个形态） | 修形态时同批约束其替代形态 |
| 21 | 装配器降级标题不告知作者 → H4 恒 0、长文不切 | 机制对作者透明（B4-4） |
| 22 | 附录逐域覆盖按前缀判，编号只写 `SEC` 也放行 | 粒度判到条目级（B1-8） |
| 23 | 整文件 SHA 绑死需扩展元数据的原件（R1-P0-1 与现状互斥实证） | 正文与元数据分开守 |
| 24 | 回退门禁只查 `doc/extensions`，看不见四宿主入口与 untracked（R1-P1-3） | 回退验收 = tracked+untracked+目标树+入口内容 |
| 25 | 大范围一次交付，多特性交织互相阻塞批准（本轮三稿三驳的另一半根因） | 分批次交付，每批独立验收（用户 D1） |
| 26 | 回退重写轮的方案写成"对现实现的修正反思" | 需求驱动：需求记录+参考+坑三件分离（用户 2026-08-25 裁定） |
| 26a | **给覆盖问题加覆盖仪表，当成解决了去重**（批次 2 第一版）：每个来源单元登记「去向」+ 事后核对，三门禁全 PASS、239 单元零漏填，同一事实仍复述 6–14 次 | 量的是「有没有落地」不是「有没有重复」。先答「为什么会重复」并把根因定位到代码行，改法要消除根因 |
| 26b | **把审计记录提前成写作输入**（批次 2 第二版）：台账先于写作、按单元分配章节 | 冻结 02-C3 明写核对记录只用于审计、不参与分章与装配；提前它就是换个形式的逐章分配 |
| 26c | **只加门禁不删旧机制**：两版下来 `doc/extensions` 只增不减，用户裁定「一直在盲目增加内容，从来没有尝试删除或改写不合理的实现」 | 机制改造的完成判据里要有**行数下降**；上升就是又在结构外加围栏 |
| 26e | **把阶段推进写进 Case prompt**（2026-08-27 用户指出）：四个 Case 都写「随后依次完成 spec、plan、coding」，而驱动器本来就会在每个阶段边界指名下发推进指令 | 双写必漂移（改终点两边对不上，实测白跑一轮），且把「模型会不会自己乱跑」混进观测。**阶段推进归驱动器**；prompt 只写起点动作与业务要求，终点只由 `end_phase` 定 |
| 26f | **误诊双写为「要同步」**：上一轮把「prompt 与 end_phase 对不上」修成「改终点必须同步改 prompt」，还写进了单测 | 两处说同一件事时，先问「这件事该由谁说」，不是「怎么让两处说一样」——同步是把双真源固定下来 |
| 26d | **判据绑在产物的特征行上**：spec 的数值回查靠 story.md 的「源摘要」行判断 AR/design.md 是否已被归档覆盖 | 特征行随产物格式变，格式一改判据静默失效。判据要绑**流程状态**（归档态在流程契约里），不绑正文长相 |
| 43 | **作者指令送给了审查者**（2026-08-28 诊断）：framework 8 个 hook 全在 harness 进程内、verifier 前触发，片段只进 ai-prompt；`hooks/*/on_context_load.md` 30KB 写作指令主 agent 零送达，plan/coding 无任何指路 → 内网「仅 spec 生效」 | 对作者的每条要求若不在作者必读载体（作业包 / author.md / 模板 / 产物 / 报错文案）里，视为不存在；hooks 只给判据 |
| 44 | **平行账本 = 仅展示**：`knowledge_freeze` 形式满分（landing 全是真实实体）但 coding SKILL 不读它；ENV-02 唯一落地成功来自 contracts 字段的契约传导，删掉账本结果不变 | 义务写在下游本来就读的实体上；机器索引一律派生 |
| 45 | **自由文本逃生口 + 合法理由示例即模板答案**：`reason` 只判非空，指令又列出「表头/元数据才是理由」→ 151 条同一句模板理由、59% 单元「不进」 | 处置只允许机器可核的封闭形态（`at / covered_by / machine_facing`）；示例清单不交给被判方 |
| 46 | **verifier 回声、机械收口只数行**：272 行证据 14 种字符串全是 `audit.json` 复制；收口只查「同行含键 + 裁决词」 | 裁决必须附目标产物引文，机械核实引文是子串；对象数少而实（≤5 条判据/阶段） |
| 47 | **空门禁挂 BLOCKER**：`audit --verify` 什么都不查永远 exit 0，却被列为「核对记录齐备」 | 每条 BLOCKER 判据要有反例夹具证明它会 FAIL；留着比没有更坏 |
| 48 | **门禁回环吃掉一半时间、模型逆向 checker**：门禁执行 5.8%，回环 49.3%；同一 check id 洋葱式 FAIL 5 次；`check-spec.ts` 被读 9 次、14 次 grep checker 源码 | 结构判据在作者写之前可见（模板/author.md 内联）；报错一次报全并给修法；报错文案不得要求读 `.mjs/.ts` |
| 49 | **上下文单调增长、分工不分上下文**：11K→818K 零 compaction，story 在 spec 会话 540K 处写成 | 成文/审查各在自己的子 agent 上下文里做，输入包是明确文件清单 |
| 50 | **指标方向错**：交付报告把「272 单元全部有交代（161 条不进）」当成功、把 142 行说成「不是略写」 | 守恒报告的主指标是「不进比例 + 理由种类数」；不进上升就是丢失，不是去重 |
| 51 | **扩展覆盖点在作者侧不生效**：`provides.skill_assets` 只被 harness 解析器认，`framework/skills/README.md` 的主 agent 解析协议只读 profile 清单 | 声称的注入点要在**运行时读者**那条路径上验证过才算存在 |
| 53 | **条款适用面读宽**（adapt 第三版）：把 C13「未核实的项目知识和目标相关规约不进清单」读成「全部知识」，硬规则一刀切让首次适配后目标既无规约也无模式；同时漏了 adapt 自己的 skill 目录归属，manifest 登记不存在路径撞上加载器「全有全无」 | 条款适用面按知识类别逐一核（facts / 规约 / 模式各自查条款）；任何「整批规则」先在真实文件树上逐文件过一遍；方案先写「每个文件怎么办」的表，再写机制 |
| 59 | **走查夹具没覆盖表里的每一行**（adapt 复核）：`--check ②` 对全部知识文件核「旧事实仍在」，而表里明写「包内同名规约/模式升级时换成包的版本」；走查夹具 `target-prior` 只有目标自加的域，这一行从未被测到，四项全过是假的全过——内网升级第一次跑就会误报，且 SKILL 让执行者「照报的改」，会把旧内容塞回去 | 判据的守恒对象要逐类与处置表对齐；**夹具按表逐行造用例**，表里每一行至少一个正例或反例；「全过」要先问「哪几行没被踩到」 |
| 61 | **判据清单按「文件大小」编，而不是按「有没有替代物」**（批次 3 子批 A，实施中逐项核出）：03 判据③ 列了 13 个「承载被替换机制、只能从 0 重写」的文件，对照 01 §9 替代关系表，其中 **3 项根本没有替代物**（`SKILL.md` 的「写作段」在基线上不存在、`rules.md`/`evidence-rules.md` 的「装配器措辞」此刻是准确描述）；`spec/post_check.mjs` 895 行里被替换的也只有约 60 行 | 「这个文件很大、文本很旧」不等于「它承载被替换的机制」。判据③ 这类清单**只收替代关系表里点得出替代物的文件**；为凑「相同行占比 <30%」去重写没被替换的判据，是纯措辞变动加回归风险，零收益——那正是它想禁止的打补丁的另一种形态 |
| 60 | **拿另一个基线的问题记录当现状**（批次 3 子批 A，第二次发生）：02 问题记录的行号与结论基于 `67cdc068`（旧批次 2 **已经删掉装配器之后**），03 照抄成「把装配器措辞清零」，而实施基线 `e96e90fc` 上装配器还在——清零反而制造「文档说没有、实现里还有」的相斥 | 引用问题记录前先确认它写于哪个基线；跨基线的结论要重新实测，不能照搬。同一形态在本轮出现过两次（Q2 是第一次），凡是「02 说 X 有问题」的条目，动手前都要在当前基线上验一遍 X 还在不在 |
| 59 | **删导出前只 grep 了外部调用方，漏了模块自己**（批次 3 子批 A Step 5）：把 `downstream.blocker()` 判为零消费者删掉，`node --check` 全过——它只查语法不查引用；实际 `downstream.mjs` 自己有两处在用 | 删导出要 grep **全域含自身文件**；`node --check` 通过不代表引用完整。真正的验证是**实跑一次**——本步后来靠临时工程根实跑六个 post_check 才确认出口路径都活着 |
| 58 | **「评审提问题 → 加一个机制」第二次发生**（adapt 备份/回滚，Step 1–4）：评审指出「`git checkout` 对首次安装的 untracked 目录无效、非 git 仓无回滚」，正确答案是**「回滚不归 adapt 管」**，实际却造了一套备份机制——47 行代码，连带制造四个新坑（备份自指、回滚顺序、抹掉确认记录、新增根文件不清理），全部是它自己的问题 | 收到评审意见先问「这件事归不归它管」，再问「要不要加机制」；**给一个不存在的场景造能力**（「非 git 仓怎么办」）是最常见的入口。删能力要删干净：**用一段文档说明去补一个不提供的能力，仍然是打补丁**——不支持就不讲，交付件里一个字都不提 |
| 57 | **交付件里引用了交付包外的文档**（adapt Step 0）：方案让执行适配的 AI「按 §1 表逐文件判」，那张表却在 `test/story/plan/`——不随包交付、还在 `.gitignore` 内，目标工程的执行者永远读不到 | 交付件的每一条引用都要问「装到别的工程后这个路径还在吗」；判断依据必须**内联在交付件里**，不能指向维护件 |
| 56 | **备份放在自己要操作的目录内**（adapt Step 1–2）：`adapt/backup/` 在 `<extension_dir>` 里，先是 `cpSync` 直接拒绝复制到自身子目录（`ERR_FS_CP_EINVAL`，带 `filter` 也不行），后是回滚顺序一反就把根文件的备份连同目录一起删掉 | 备份与被备份对象的包含关系要先画清楚；回滚顺序写死在代码注释里（先根文件、后目录），别指望实现者自己想到 |
| 55 | **回滚只做「复制回」不做「删新增」**（adapt Step 2 走查抓到）：整体替换的目录没事，但逐个恢复的仓库根文件——本次写入新增的三个宿主跳板备份里没有，就原样留在目标上，回滚后树 ⊃ 备份 | 回滚判据写「树 == 备份」而不是「备份内容都回来了」；删文件要连空目录一起收（`rmSync` 删目录须带 `recursive`，否则异常被 `catch` 吞掉、表面像没执行） |
| 54 | **比对基准取自被污染的现场**（adapt Step 2）：第一轮「严格比对通过」是假的——快照是在未修复的回滚**之后**取的，残留文件当时已进了基准，于是拿残留比残留、自然一致 | 比对基准必须取自**已知干净**的状态（版本库里的原始夹具），不能取自「上一次跑完的现场」；基准一旦可能被前一次运行写过，结论就不成立 |
| 52 | **「自足」被做成「机器化」**（adapt 第二版）：把「任何模型都知道怎么做」理解成「每一步都规定成脚本动作」，结果 glob 引擎 + 影子树 + 动作 schema + 12 项校验——脚本主导换皮，与被否定的 hash 方案同类；评审提的 9 个问题又逐条用加机制回答 | 自足 = 模型读完知道判什么、写什么、怎么确认，不是机器代替判断；评审问题先问「模型读一遍能不能自己判」，能就不写脚本；「模型主导」的方案主体是 Skill，脚本只做模型不可靠的机械核对 |

## 2. 设计层：机制与契约（F 系列精编）

| # | 坑 | 教训 |
|---|---|---|
| 27 | 机械守真方向错误（要求 spec 声称的须在上游存在），误伤合法新增；报错文案与语义相反 | 守恒方向先论证再实现；报错文案即契约 |
| 28 | 自称 SSOT 的矩阵无代码读取，语义两份人肉同步 | 声明为 SSOT 的文件必须被机制真实解析 |
| 29 | 硬编码工程路径/域前缀/模式名，配置变更静默失效 | 派生不枚举；派生为空出声（G7） |
| 30 | 同批需求的门禁与写作指令互斥落地（两处正反打架） | 判据与指令一次成型同向 |
| 31 | 结构收敛后旧坐标悬空引用；跨文件引用名不存在；README 计数与实际不符 | 收敛后全仓 grep 清零；引用 100% 脚本核对 |
| 32 | 注入件迁移静默丢条款；自写解析器失败静默空结果 | 迁移前后清单比对；解析失败必须响亮 |
| 33 | 死代码、矛盾注释、脱节头注堆积 | 注释即契约，验证时走查 |
| 34 | 版本指纹清单两版路径约定不一、基线取自开发中间点 | 路径约定单一；基线标注等价验证点 |
| 35 | 文档写"已实施"但代码非真值；离线全绿冒充行为完成 | 状态只能由验证证据推动；行为结论只来自实跑 |

## 3. 实现层（跨批次通用）

| # | 坑 | 教训 |
|---|---|---|
| 36 | 经 Python 字符串生成 JS 代码时 `\\` `\s` `\b` 被吃/变控制符（4 次事故） | 生成代码勿走转义链：用 `String.raw`、`chr(92)` 或直接 Write 文件 |
| 37 | `.` 不匹配 `\r`，CRLF 文件按 `\n` 切行后正则静默零命中 | 一律 `/\r?\n/` 分行；零命中告警不静默 |
| 38 | 正则/块替换边界吃掉相邻函数或留下重复旧版本 | 替换后 `node --check` + grep 函数名计数 |
| 39 | 守恒检查把字段名/平台 API 也当契约名，过严误伤 | 守恒集合只取声明表首列并排除既有实现名 |
| 40 | 域名匹配过脆（"安全与隐私" vs "安全隐私"） | 按前缀/编号匹配，不按中文名全等 |
| 41 | 陈旧 `.current-phase.json` 触发 Stop hook 误拦 | 验证跑完 `--clear-state` |
| 42 | workspace 占位文件（`.gitkeep`）在回灌/清理中丢失 | 状态目录显式登记保留清单 |
| 62 | 子 agent 在「读全部材料 + 一次写 37KB」的长任务上中途死掉却报 completed、返回空、盘上无产物（opencode Task 上游已知，无超时参数；4 次 3 空）；verifier 逐条追加落盘从未失败 | 成文拆成「分配（恰好一处由数据结构强制）+ 逐章渲染（有界、落盘、可续写）」；机制不依赖子 agent（Task 可选，codex 主 agent 自己做）；编排只看磁盘 |

# 步骤 C1 评审（2026-09-20）

对象：`e1d4069f`（B1 返修）、`b821f59e`（C1，9 文件 +767/−5）与《步骤C1交回-2026-09-20.md》。方案依据 `03-统一update.md` §2、§7、§9 C1，裁定 §2/§5，额度签字稿。

## 1. 结论

B1 返修收口。C1 接受，附两条返修后收口；两条都小，不影响 C2 开工，但要在 C2 交回前落地。

## 2. B1 返修

`init_analysis.md:108` 补的三行与我 B1 评审 §3 的裁定同义：不取的理由只能是范围不依赖它、范围依据来自 AR 或需求方的话，上游说「另行提供」不构成理由。计 prompts_md +3。收口。

## 3. C1 逐处核对

| 落点 | 核对 | 判断 |
|---|---|---|
| `flow/update.py::cmd_update_prepare` | 四种去向的判据与交回一致：`resume` 不新建镜像；`unchanged` 要求比较完整、无变化、暂存空、无读不到、无 `--request`；`incomplete` 与 `changed` 都留镜像。三条失败语义在代码里：`_scan` 把「不在」与「读不出」分开（:72-77），`_baseline` 只认 closed（:87-89），`_write_diffs` 取不到旧正文就不写（:139-140） | 与 03 §2 的意思一致。实现比设计更省一步：先比再镜像，无变化时什么都不建，所以也没有要删的临时副本。这比「建了再删」安全，03 §2 步骤 1/4 与 AC09 的措辞改为「无变化不建副本」即可，不算偏离 |
| `_mirror` | 跳过 updates/incoming 两目录与符号链接，`copyfile` 逐文件 | 符合 03 §7.2「拒绝链接」的一半（镜像侧）；见 §4 第 3 条 |
| `story.js::cmdFetch` | 只写 `--out`，`fetched.json` 三态 fetched/absent/failed 分开，本地单拒绝，取不到不落占位件，`--out` 必填 | 符合 03 §7.3；Demo 的四份 wanted 清单是对接层自己的事 |
| `story_flow.py` | `update` 模式、`--action` 只认 prepare、`--request` | 与「C1 只交 prepare」一致 |
| `scripts/README.md`、`story-adaptation/SKILL.md` | fetch 合同、目标工程要补、没补报错停不退回 review | 正是裁定 §5 的三件 |
| `SKILL.md` | 条件加载索引加一行，明写入口未开放、仍走 `/story review` | 符合参考/03 §1「不把半成品暴露给用户」 |
| `phases/update.md` | §一至§四对应 03 §1–§4、§8.2 的 CLAUDE.md 关系；派审留「等 C3」 | 方法只此一份；缺 03 §5 的进展表，见 §4 第 4 条 |
| 中性场景走查五例 | 每例有输入、要判什么、应改/留/问、允许的不同结果、不可接受的结果 | 是设计语义审查材料，没进生产提示，符合 03 §9 C1 |
| `manifest.json` 撞名改 `fetched.json` | `test_material_rounds` 那条判据没动 | 处置对：撞名就是口径扩散的开始 |

## 4. 返修与交接

| # | 内容 | 落点 | 阻断 |
|---|---|---|---|
| 1 | **监视集合漏了业务真源与 Plan。** `update.py:34` 的 `PRODUCTS` 只有 story、review、spec.md、acceptance.yaml；`_scan` 另加合同登记的正文源。启动前手改 `plan/plan.md`、`plan/contracts.yaml`、`plan/use-cases.yaml`、`spec/knowledge-use.yaml`、`AR/story-src/decisions.json`、`AR/story-src/story-template.md` 一律检不出，会被报成 `unchanged`。car 是 plan 单，正式 Case 就会踩到 | C1 返修：把这几份加进监视集合（存在才算，不存在不当删除），补一条「手改 plan.md 被检出」的用例 | 是，C2 前 |
| 2 | **「读不到」分支没有测试。** 03 §9 C1 明写「失败不误判无变化」要离线核；`_scan` 的 unreadable 分支有代码无用例 | C1 返修：monkeypatch `registry.file_digest` 抛 OSError，断言 `incomplete` 且 `unreadable` 非空、不报 unchanged | 是，C2 前 |
| 3 | `fetch --out` 不限制落点：`path.resolve(projectRoot, outArg)` 可指到任何目录。03 §7.2 的「路径限制在 feature/指定 incoming」是脚本侧的要求，对接层不该承担 | C2：由 `update.py` 拥有 incoming 路径，模型不再自己拼 `--out`；或 prepare 之前有一步 `fetch` 由脚本代调。写进 C2 的接线 | 否 |
| 4 | `update.md` 缺 03 §5「不同进展如何影响工作」那张表，动作 1 说「弄清哪些阶段有产物、哪些已闭环」但没有工具给这个事实。开工准备 §4 已核只需接一条 check-receipt 的线 | C2：prepare 返回阶段事实（有无产物、闭没闭环），`update.md` 补进展表 | 否 |
| 5 | 03 §2 步骤 1/4 与 AC09 措辞按本文 §3 第一行改为「无变化不建副本」 | 设计者改设计稿 | 否 |

## 5. 测试与预算复核

- 执行者按「谁读这个文件」跑 20 个文件 679 passed，抓到并处理一条真判据；我自跑全量离线回归 1153 passed / 15 skipped，71 秒，零失败。
- `test_story_update` 15 条覆盖：首次无基准判 incomplete、镜像完整且不套自己、开着的一轮 resume、无变化不留痕不碰历史轮、`--request` 不算无变化、手改与删除被检出、fetch 只写暂存与三态。缺 §4 第 1、2 条。
- 预算实测 11912（prompts_md 1849 / py 2512 / mjs 3381 / hooks 3394 / data 776），与交回一致。C1 净 +295，对初始新增额度 674 用掉 44%；在途 scripts_py 2650（现 2512）、总 12270（现 11912）。`update.py` 计量 170 行，低于估的 200–280，原因是 status/close/restore 归 C2，不是省了。

## 6. C2 开工前提

§4 第 1、2 条返修提交后 C2 开工。C2 交回除 03 §9 的内容外，带上 §4 第 3、4 条的接线与 C 开工准备评审 §3.2 划定的删议题/变义确定性半边。

# 步骤 C2 评审（2026-09-20）

对象：`0a6d78ab`（C1 返修）、`d3f2c055`（C2，10 文件 +620/−26）与《步骤C2交回-2026-09-20.md》。方案依据 `03-统一update.md` §7.2、§8.1、§9 C2，C1 评审 §4、额度签字稿 §3.2。

## 1. 结论

C1 返修收口。C2 主体接受，附两条阻断返修，落地后 C3 开工。预算类内超差按实签了 scripts_py 2750，总量不动。

## 2. C1 返修

监视集合补齐十份（story、review、spec.md、acceptance.yaml、knowledge-use.yaml、decisions.json、story-template.md、plan.md、contracts.yaml、use-cases.yaml），盘上没有的跳过不当删除；「读不到」带 key 单独返回并从 removed 里摘出去。第二点是我评审没提的，执行者自己发现「分开这一步只做了一半」。两条用例都在。收口。

## 3. C2 逐处核对

| 落点 | 核对 | 判断 |
|---|---|---|
| `update.py::cmd_update_status` | 只报事实；阶段事实读 `<阶段>/reports/summary.json`，我核过真实产物里这个路径存在（car 第二轮 plan/reports/summary.json）；读不出说读不出 | 符合 C1 评审 §4 第 4 条 |
| `cmd_update_close` | 要先有非空 `update-notes.md`；`after/` 只留监视集合那几份；记阶段事实；契约 `update.open` 归零 | 符合 03 §7.2；`after/` 从「拿不到旧正文才存」改成「总存监视集合」，量小，接受，设计稿 §7.2 那一句随之改 |
| `cmd_update_restore` | 与 `before/` 不同的先存 `conflict-<时刻>/` 再写回；这一轮后新建的先存再删；契约那一笔自家记号同步进镜像避免假冲突 | 符合 D15「两边都留」；可以还原已收口的一轮，还原后不再作基准，语义对 |
| `_incoming_dir` / `_fetch_command` | 落点由脚本拥有并渲染命令，`update.md` 改成「命令不要自己拼」 | C1 评审 §4 第 3 条落地 |
| `routing.py` | 收口态且有开着的更新 → `update_in_progress`，不进材料/范围关卡；新会议仍走 `late` 那支 | 裁定 §3 落地，四态实跑过 |
| `decisions.py::cmd_decide_update` | 记原话、要有开着的更新、空 basis 拒绝、写进契约 `update.decisions` | 符合 03 §8.1「不造人签」 |
| `review.mjs` 议题被删 | 渲染前枚举旧文有、新表无且人写过字的议题，停在写盘前；没人写过字的照常删 | 符合额度签字稿 §3.2 第一条 |
| `review.mjs` 问题换了 | 只比「决策点」一段的指纹；找不到这段不拦只出声；变了就停手 | 收窄有实测理由（重排编号、金样旧段名），接受；见 §4 第 1 条 |
| `contracts/README.md` | `update` 键形状、不进合同、不加判据、不升 schema | 符合额度签字稿 §3.1 |
| `update.md` | 补三个动作、`decide --update`、阶段事实来源、03 §5 进展表 | C1 评审 §4 第 4 条落地 |
| `test_review_modes` 两条夹具改动 | 原夹具用「同 id 换成另一个问题」模拟「换填写方式」，正是本步要拦的缺陷；改成同决策点换形态，用例本意不变 | 我核过 `CHOICE_BODY_AS_CONFIRM` 与 `CONFIRM_BODY_AS_CHOICE` 的正文，决策点一句与原议题相同。不是放宽 |

## 4. 返修与交接

| # | 内容 | 落点 | 阻断 |
|---|---|---|---|
| 1 | **变义判据没有解锁路径。** `review.mjs:725` 的报错让人「意思没变就 `decide --update` 记一笔再重跑」，但 `review.mjs` 不读契约里的 `update.decisions`，重跑时 `issueRewritten` 比的还是旧机器区与新正文，仍是 `yes`，再次抛出。模型判定意义未变的那条路走不通，会在这里死循环。| C2 返修：`renderReview` 读契约 `update.decisions`，其中 `item` 指向该议题 id 的记录视为「沿用上一版表态」，本次带过人工区并把标记里的摘要更新到新机器区；加一条用例「记了沿用再重跑能过、没记仍拦」 | 是 |
| 2 | **incoming 暂存区没有生命周期。** prepare 把它复制进 `updates/<id>/incoming/` 后原件留在原地；没有任何动作把它导入材料（`importer` 只读 inbox，`round` 只消费侧车），close 也不清；下一轮 prepare 因暂存区非空永远报 `changed`。`update.md` 与 `update.py` 里 grep 不到 incoming 的导入、登记或清理 | C2 返修：定一条路——模型读完后把要采用的那几份按现有导入链放进 inbox 再 `round` 登记（方法页写清），close 时清空 incoming（副本已在本轮目录里）；加一条用例「close 之后 prepare 报 unchanged」 | 是 |
| 3 | restore 的 `live` 集合含符号链接指向的文件（`is_file()` 对链接为真），而镜像跳过了链接，于是链接会被当「这一轮后新建」删掉 | C3 顺手：`live` 同样排除 `is_symlink()`，加一句用例 | 否 |
| 4 | 03 §7.2「after 只在现有版本不能提供旧内容时保存」改为「close 时留监视集合的副本」；03 §2 步骤 1/4 与 AC09 改为「无变化不建副本」（C1 评审已提） | 设计者改设计稿 | 否 |

## 5. 测试与预算复核

- 执行者按「谁读这个文件」加全量：1172 passed，只有预算那条红。我自跑全量：1172 passed / 15 skipped，1 failed 同为 `test_mechanism_budget` 的 scripts_py 类内上限；签字后重跑 5 passed。
- 实测 12192（prompts_md 1879 / py 2708 / mjs 3435 / hooks 3394 / data 776），与交回一致。C2 净 +273。累计新增 575 对初始额度 674，R = 85%。
- scripts_py 2708 超在途 2650 共 58：update.py 四动作实到 320 对估 200–280（+40），decisions.py 30 与 routing.py 15 签字时并在一格没单列。按 AGENTS §5 属 R≤100% 的类内超差，**按实签一次不压回**：压回只能砍 restore 的冲突留存，那是 D15 要的。签 scripts_py 2750，总 12270 不动（C3 退 review_reflow.md 113 行后总量回落）。台账已写。
- scripts_mjs +54 落在签字的 45–70 内。

## 6. C3 开工前提

§4 第 1、2 条返修提交后 C3 开工。C3 带上：§4 第 3 条、C 开工准备评审 §3.3 的 `phases/spec.md:67-75` 两条路、裁定 §2 的派审接线与隔离 fixture（含投错 subject 那条错路）、旧 review 全链退出清单（参考/03 §1 与 C 开工准备 §5.2）。

# T2 预跑结果评审（2026-09-21）

对象：`output/story/story-suite-20260920-1155`，两 Case 各跑完第一段与第二段，两份检查点快照齐。按裁定（09-21）这批定性为**预跑**：入口流程已改（D21 补料问询、复用 init 第一级），正式 T2 在修复后重跑。本文只评「产品在这批上实际做到了什么」，不出正式三轴分。

## 1. 记录复核

| 项 | auto-topup（AR90006，spec） | car-key-sharing（ISSUE-206，plan） |
|---|---|---|
| 第一段 | 19:41 → 20:35，3 次停等（材料、会议、范围），5 次 verifier，FAIL 10 次 | 19:41 → 20:51，4 次停等，18 次 verifier 提及，FAIL 8 次 |
| 第一检查点 / 回流 | 20:36 固定，已回流原编号 | 10:34 固定，已回流原编号 |
| 第二段 | 20:36:50 → 21:00:55，**24 分钟**；停等 0；关卡 0；FAIL 0 | 10:34:36 → 11:00:57，**26 分钟**；停等 0；关卡 0；FAIL 0 |
| update 轮 | `20260920-123715` closed；prepare 报 `incomplete`（首次无基准），`request` 空 | `20260921-023521` closed；prepare 报 `changed`（review.md 变 + `--request` 四条意见） |
| 重审 | spec 新 subject `e1de59b2` PASS，0 blocker，2 条 advisory | spec `2a61247a` PASS、plan `62338…` PASS，各 0 blocker |
| 第二检查点 | 21:00 停等，10:35 固定快照 | 11:00 停等，11:01 固定快照 |
| 收场 | `worker_lost`（宿主未 conclude，进程被停） | 同左；另 post-run `story_build_check` 返回 0xC0000142，stdout 空 |

第二段的关卡记录我逐条核过时间与签署者：两 Case 所有关卡都在第一段由人签，第二段**没有再问关卡、也没有代签**。第二段的脚本序列：`update` → `round` 登记 → `reopen` → 改草稿、`chapter` → `complete` → `story` 重登记 → `--revalidate` → 派 verifier → `--sync-closure` → `update --action close`。

## 2. 产物核对

### auto：PRD 改版 200→300

- 变化被找到并落实：spec.md 5 处 300、0 处 200；acceptance.yaml AC-2 改 300；knowledge-use.yaml DFX-02 重判；decisions.json D5 修订（来源＝产品稿 v0.5，decider 产品组）；story.md 4 章草稿改、附录投影随重登记同步。story 里剩两处「200 元」都是「由 200 调整为 300」的改版说明，不是残留。
- 不变项列了：门限档、面额档、失败停用次数、开关语义、范围整体承载。
- 会议 T6 的「200 保持、300 明年」被产品稿 v0.5 正式推翻，update-notes 写明了这层关系，没有把会议判断当成仍有效。
- 材料链：v2 docx 被归为 AR 类录入 `upstream.md`，RR/prd.md 保持归档版，story 材料清单加了 v2 原件链接。判法可辩护，但「新版产品稿归 AR 类」是分类器按内容猜的，正式跑时看会不会稳定。
- **它是怎么发现 inbox 里那份的**：prepare 因首次无基准报 `incomplete`，没走无变化退出；宿主那句「新版已经放进需求目录」点了名。两个条件缺一个，这一趟就会报「未检测到变化」。这正是裁定里写的缺口，预跑把它掩盖了，不是修好了。

### car：评审意见回流 36 小时

- 四条人话全部落到位：DEC-1 由 open 改 settled，36 小时与「正在收回」进 spec AC-K5、E6、F9，acceptance、facts.md、AR/design.md、story 四章；转分享与强制下线维持未决，界面稿未动。
- **变义判据在真实运行里被触发并解锁**：改 DEC-1 正文时 review 渲染停手，模型用 `decide --update` 记了评审人原话再重跑，人工区带回。这是 C2 返修那条路第一次真跑。
- `decide --update` 记的是评审人原话，story-flow 契约里有这一笔。
- Plan：plan.md、contracts.yaml、use-cases.yaml 一字未改。plan 里没有撤销时限的任何表述（只有 REVOKING/REVOKED 状态），模型判无需同步。可辩护，但 update-notes 没写「Plan 为什么不用改」这一句，03 §4.3 要求列不变项及理由。
- DEC-1 改成 `confirm`、「请出行产品组确认」：评审人已经表过态，再请同一方确认是多余的一步。人工区两框仍空，不算伪定案，但形态选错。
- 评审记录自由意见区的四条原话原样保留。

## 3. 机制观察

| 观察 | 结论 |
|---|---|
| 增量没有重做 | reopen 之后没有回材料与范围关卡，原因是模型**先 `round` 登记再 reopen**，本轮关卡已全部 accepted。裁定要把这个顺序写死，不能靠模型碰巧走对 |
| D21 的补料问询不存在 | 两 Case 第二段停等 0 次：auto 靠宿主点名，car 靠 `--request`。正式流程加了这一停之后，第二段会多一次停等 |
| 派审接线成立 | 两 Case 三个阶段都按新 subject 审了一次，报告落在 summary 指的路径，`sync-closure` 后无 `semantic_not_reverified`。auto 的 spec 报告目录里多出一对旧 subject 的请求与材料（`5ed0b1d6`），是 revalidate 跑过两次留下的，不是投错 |
| 无变化退出未走到 | 两 Case 都有变化。auto 的 `incomplete`（首次无基准）按设计处理，没有冒充无变化 |
| 第二段成本 | 24 / 26 分钟，各 6 到 11 次 harness、5 到 6 次 revalidate；对比第一段 54 / 70 分钟。增量而非重做，成本上也成立 |
| 镜像太重 | auto 的 `checkpoint --point update` 第一次失败：`before/` 镜像把 `spec/reports/` 里 64 位哈希文件名整套复制，再套进 `checkpoints/update/.../updates/<id>/before/...`，路径超过 Windows 260 字符上限（WinError 3）。第二次才成功 |
| 收场 | 两 Case 终态 `worker_lost`：第二检查点等待中宿主没有 conclude 就停了进程。car 的 post-run `story_build_check` 返回 0xC0000142 是进程被终止后的环境错误，不是产品失败 |

## 4. 要修的（进正式 T2 前）

| # | 内容 | 落点 |
|---|---|---|
| 1 | 裁定 §2 的输入阶段：fetch 落 inbox、第一级关卡在 update 里必停一次、`round` 登记**先于** reopen 写进方法页与路由 | C 返修 |
| 2 | `_mirror` 排除 `*/reports/`、`.backup/`、`revalidation.json`：它们不是业务内容，03 §7.2 本来就说报告不进业务差异集合；顺带解决长路径 | `update.py`，C 返修 |
| 3 | 方法页动作 3：不变项要写理由，含「既有 Plan 为什么不用改」 | `update.md` |
| 4 | 方法页决策登记：人已表态的议题登记为 settled 后，`review_mode` 不再向同一方发 confirm | `update.md` 或 story-write 决策登记段 |
| 5 | TEST §5.9 第 7 步补一句：后评做完必须 `conclude`，否则终态是 `worker_lost`，finalize 仍会回灌但状态失真 | TEST.md |

## 5. 这批的处置

- 两份第一段已回流原编号。第二段快照齐，可以 `finalize --promote` 落 `<编号>-update`，作预跑证据保留；也可以不回流，正式 T2 再落。由用户定。
- 正式 T2 在 §4 落地后重跑两 Case；预跑的四份快照留作对照，尤其看正式流程多的那一次补料停等对成本与判断的影响。

## 6. 更正（2026-09-21，据 C4 交回 §5 核实）

- §1「收场」与 §3「收场」行：终态 `worker_lost` 不是宿主没 conclude，是 conclude 之后 worker 收尾复制 artifact 撞 Windows 路径上限崩了（`worker.log:9-22`，`run_case.py:2003 copytree`），`2ea9085c` 已修。
- §1「重审」与 §3「派审接线成立」行：auto 的 spec 没有同步闭环。`summary.json` 20:51:53 由 revalidate 写出并沿用历史 PASS，报告 `e1de59b2` 20:58:42 才写盘，之后没有再跑 `--sync-closure`，`readiness_signals` 仍含 `semantic_not_reverified`。报告本身 subject 对、PASS、0 blocker；update-notes 写「sync-closure 已收口」与盘上不符。car 干净。处置见《步骤C4评审》§6。


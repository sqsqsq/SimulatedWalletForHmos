# 步骤 A1 评审（2026-09-20）

对象：提交 `1d292df9`（10 个文件，+217/−108）与《步骤A1交回-2026-09-20.md》。方案依据 `01-Story表达优化.md` §5、裁定 §1/§4。

## 1. 结论

A1 接受，附一条必须补的返修：`test_verifier_report_protocol.py:630-635` 因 overlay ⑥ 措辞改动而红，执行者没跑到它。补一次提交后 A1 收口，B1 可开工。

## 2. 逐处核对

| 方案 §5 落点 | 提交里的实际改动 | 判断 |
|---|---|---|
| story-write.md 替换读者/设计/写章/图承接/回看方法；删「可改画」与「一律原样搬入」并存；删「所有分支塞总览」 | 重排为七节，新增「二、成品的阅读组织」「三、表达选择」；`:119-123` 只剩一条承接规则（原样用/增强/拆分/重组，三条要求），`:203` 落盘只说保留来源标记；旧 :158「原样放进…只搬图不搬文字」已无；总览与局部分工在 `:106-112` | 与方案 01 §2–§4 逐段对得上，两处并存指令确已退 |
| story-chapters.json 05-flow 问题 | 问题 ② 补「总览给主线与主要分流入口，复杂分支在对应小节完整展开并接回主线」 | 一处一句，与作业书同口径 |
| overlay `story_reader_review` | ⑥ 从「章首那张图…缺一条就是 finding」改成「总览与局部加起来判，两处都没有才是 finding」 | 判据与作业书、合同三处同步；见 §3 返修 |
| writing-plan.mjs::writingPlanShell | 空壳注释补图/表/文字分工一句，种子行改「读者先得到什么、往下怎么展开」 | 送达缺口补法最小 |
| chapter-contract.mjs::chapterSeedRows、drafts.mjs 只修实际缺口 | 改的是 `missingPickedSeeds`：结构起点按小节归堆；drafts 未动 | 离线演练抓到的真缺口（同节两种形式的起点分散到别的小节下），修在生成处，有新测试 |
| sources.mjs、reader-review-task.mjs 核多图承接 | 核过不改，交回给了理由（`diagramPairRows` 已逐张给源图与承接图，措辞与新规则一致） | 我核 `reader-review-task.mjs` 无「章首那张图」残留，接受 |
| chapter.mjs 指路节名 | 「二、动笔前：先写骨架」→「四、动笔前：先设计表达」 | 原来指向不存在的节，属真缺陷 |

## 3. 必须返修的一条

`tests/test_verifier_report_protocol.py::ReviewTaskReachesTheVerifier::test_the_method_is_maintained_only_in_the_overlay` 在 `:630` 要 overlay 里有「章首那张图」，A1 正当地把这句改掉了，用例红。该用例守的是「方法只在 overlay 一份、构造器不复制」，锁的对象没变，只是锁词过期。改法：`:630` 的 needle 换成新判据里的字面（如「总览与局部加起来」「端到端过程」「分支去向」），`:634` 的 NotIn 同步换；不放宽任何断言。

执行者交回 §5 只跑了方案列的六个测试文件。改了 overlay 却没跑读 overlay 的用例，选测试的依据应是「谁读这个文件」，不是方案列表；`grep -l spec-rules.overlay tests/*.py` 三个文件里就有它。这条写进 B1 的交回要求。

## 4. 测试与预算复核

- 我自跑方案六文件：304 passed；读 overlay 的三文件：1 failed（上条）/71 passed；全量离线回归排除该条：1137 passed / 15 skipped，77 秒。除那一条没有其它受影响用例。
- 测试改动核过：`section(guide, 章名)` 只换定位方式，内容锁全部保留；「图从要解释的关系出发」从写章一节移到表达选择一节并加 NotIn 防回抄，是随重排的正当迁移；manifest 版本断言 1.9.1→1.9.3 随上版补漏，注释本就写「红了就一起改」。
- 预算实测 11573（prompts_md 1717 / mjs 3381 / hooks 3367 / py 2342 / data 766），与交回一致，净 +37。方案估 A1 净 +90～130，实际低，交回解释成立：两节新内容大半是原表与图段挪过来重写，不是净增。hooks_mjs 与 data 抬的额度本步几乎没动，留 B1。

## 5. 其它

- `c0a3fe84` 补漏提交带上了两份设计模式知识的 §9 与 EVOLUTION 链接，超出裁定 §7 说的两个文件。改动内容我核过，都是 1.9.3 遗漏修复，无业务变化；这是用户与执行者之间的授权口径，不影响 A1。
- A1 交回 §7「没有做的事」三条与方案边界一致：未追加真实 CLI、未做 Review 更新器、未扩到 P3/P5。

# 步骤 C3 与 T1 评审（2026-09-20）

对象：`8a4c872b`（C2 返修）、`b7065cef`（C3，25 文件 +313/−299）、`fcae8bdc`（T1，10 文件 +624/−50）与两份交回。方案依据 `03 §8.2/§8.3/§9 C3`、`04 §2`、C2 评审 §6、裁定 §2。

## 1. 结论

C2 返修收口。C3 接受，无返修。T1 主体接受，附一条阻断返修与一条设计要求的缺项；返修落地后 T2 可以由用户启动。另有一处归属问题请用户定。

## 2. C2 返修

- 变义解锁：`review.mjs::carriedOver` 读契约 `update.decisions`，命中该议题 id 就带回人工区并出一声；新用例「记了能过、没记仍拦」在。死胡同已通。
- incoming 终点：方法页写清采用的材料经 inbox 与 `round` 登记；`close` 清空暂存区（副本在本轮目录），返回 `cleared_incoming`；用例「close 后 prepare 报 unchanged」在。收口。

## 3. C3 逐处核对

| 项 | 核对 | 判断 |
|---|---|---|
| 旧 review 链退场 | `review.js`、`review_reflow.md` 删除；`story.js` 的 `cmdReview`/分发/用法/help 退；四桥接、SKILL 检视节、README、适配说明、plan/author.md、plan-rules.overlay、spec.md、routing.py 两处文案、review.mjs 两处注释全改。我全仓 grep 后只剩 README 那句「已退场」、注释里的历史交代、`verifier-report.mjs` 的 `STORY_REVIEW_ID`（那是 `story_reader_review` 判据 id，不是旧链） | 退场干净，消费者一个不漏 |
| update 成为入口 | SKILL「更新」节、四桥接 description/argument-hint/命令表/流程第 5 条同步；「入口尚未开放」段删除 | 与 03 §9 C3 一致 |
| 派审接线 | `update.md` 五步：路径由 summary 给、请求整段原文、回复原样全文、sync-closure 后核 subject/verdict/blocker_count/`semantic_not_reverified` 不在、返修后取最新请求；派审期间不动材料；无 verifier 如实报告 | 与评审 §8、处置稿 §8.2 逐条对得上，我要的「信号应消失」判据在第 4 步 |
| `spec.md:67-75` | 两条路：修正入口不派，`/story update` 改动阶段按新审查对象审一次；本页不复述派法 | 符合 C 开工准备评审 §3.3 |
| `test_update_dispatch` 七条 | 用 framework 自己的 `verifier-evidence.ts` 跑隔离夹具；覆盖当前 PASS、沿用历史、subject 不匹配、当前 FAIL 不被旧 PASS 盖、**投到旧路径仍算没审**、无 subject；第 7 条锁方法页 | 正是 C2 评审 §6 要的错路用例 |
| restore 排除符号链接 | `live` 加 `not p.is_symlink()` | C2 评审 §4 第 3 条落地 |
| 测试域 | 三条 review 专用用例退，restore 两条保留；四处对接脚本清单去掉 `review.js` | 与退场同步，不是整类删 |
| 预算 | 实测 12168，C3 净 −47（退 review_reflow 113、加 update 入口与派审段约 65） | 各类在额度内；累计新增 551 对 674 |

`test_multi_scenario_cases.py:23` 还留着 `VALID_END = VALID_START | {"story-review"}` 的本地常量，是测试自己的镜像值，不影响运行；T1 返修顺手删。

## 4. T1 逐处核对

| 项 | 核对 | 判断 |
|---|---|---|
| `after_initial` 穿透 | case.yaml → suite → worker；只认 `update`；不配则普通单终点 | 04 §2.1 要求，既有 Case 行为不变 |
| 第一检查点 | 到目标不 break，进 `wait_for_resume`：`awaiting_reply` + `awaiting_kind=initial_checkpoint`，无上限等、每 300 秒出声、租约续；出口是 resume 或 conclude；无 session 则 `cli_session_lost` | 与等人回话同构，复用现成保护 |
| session 落盘 | `state["cli_session_id"]` 每回合写 | 开工准备 §3.2 那笔 |
| 第二段终点 | 读契约 `update` 键：`open` 归零且 `last_closed` 换新 id 才算 | 看契约不看说辞，对 |
| `checkpoint` | 要求 worker `awaiting_reply`，复制前后各取目录摘要，不一致作废；同内容幂等，异内容不覆盖 | 对第一检查点成立；对第二检查点见 §5 第 1 条 |
| `promote-checkpoint` / `resume-update` | 回流固定快照；无快照不许续跑；无回流不许续跑；`--deliver` 先材料后话 | 04 §2.2 三条顺序全在 |
| `finalize` 目的地 | `after_initial=update` 落 `<编号>-update`，复用冲突保护 | 对 |
| 终态集合对齐 | 宿主侧补 `concluded_by_host` / `cli_session_lost` / `harness_incomplete`；`VALID_END` 退 `story-review`；`phase_scope` 退分叉 | 开工准备 §3.4 两条都收了 |
| 更新刺激 | `update_inputs` 三种落点（inbox / 人工区边界 / 系统目录），源目录 `cases/<id>/update-inputs/`，初始 seed 只复制 `cases/<id>/system`，目录隔离成立；两份材料文件按交回不在本步造 | 04 §2.3 成立；材料由用户在 T2 前准备 |
| `measure_run.py` | 按 `live.jsonl` 的 `resume_update` 事件时间戳切两段 | 04 §2.4 要求 |
| 旧假阶段 | `STORY_REVIEW` 常量、`phase_index`、`target_reached`、`next_phase` 分叉全退；`run_case.py:467`、`test_harness.py:931`、`test_story_build.py:336-344` 三个过渡消费者收口 | C3 交回列的清单全部落地 |
| 测试 | `test_update_checkpoints` 13 条；我自跑全量 1193 passed / 15 skipped，102 秒，零失败 | 与交回一致 |

## 5. T1 返修与缺项

| # | 内容 | 落点 | 阻断 |
|---|---|---|---|
| 1 | **第二检查点取不到快照。** 第二段到终点时 worker 直接 `break`，run 以 `finished` 收场（`run_case.py:1804-1809`）；而 `cmd_checkpoint` 只接受 `status == awaiting_reply`（`:2284-2288`），此时会报「worker 不在等待态（finished）」。TEST §5.9 第 7 步「`checkpoint --point update` → 后评 → conclude」在 T2 会卡在第一个命令。04 §2.1 的表写的是「报告第二检查点…宿主固定快照、后评，再 conclude」，即第二段也该停着等宿主 | T1 返修，二选一由执行者定：第二检查点与第一同构进等待（`awaiting_kind=update_checkpoint`，唯一出口 conclude）；或 `cmd_checkpoint` 明确接受「已终态且无 worker 进程」的状态。加一条用例走完「resume → close 契约 → checkpoint update」 | 是 |
| 2 | **无变化触发第二检查点没做。** 04 §2.1 末段要求：update 快速退出不建操作记录时，第二检查点由本次 prepare 返回 `comparison=unchanged` 加 CLI turn 结束加目录摘要稳定触发，离线验证。现在第二段只在 `last_closed` 换新 id 时结束，模型报「未检测到变化」后 worker 会一直等到宿主 conclude | T1 返修同一提交：worker 在 update 段每回合读本轮是否出现 `unchanged` 事实（`story_flow.py update` 的 stdout 不在 worker 手里，可改为 `status` 动作报告「上次 prepare 结果」或写一笔 `last_prepare` 进契约），满足则以 `update_checkpoint` 结束并标 `unchanged`。T2 两 Case 都有刺激不会走到这条，但它是设计要求的离线分支，不能默默缺席；若执行者认为应延后，写进 T2 交回的已知限制由用户定 | 否，但要有去向 |
| 3 | `test_multi_scenario_cases.py:23` 的 `story-review` 常量残留 | 顺手删 | 否 |

## 6. 一处归属问题，请用户定

C2 返修在 `test/story/AGENTS.md` 加了一段「离线用例一律并行跑」。内容对，1.9.3 全量串行 454 秒对并行 73 秒是实测。但 AGENTS.md 是本项目的规约，09-10 起由用户授权维护；这段是操作口径，TEST.md §7.9「离线回归怎么跑」是它的家。建议：移到 TEST §7.9，AGENTS 不留；或用户认可留在 AGENTS。执行者不该自行改规约层，这条写进 T2 交回要求。

## 7. T2 启动前清单

1. §5 第 1 条返修提交，第 2 条有去向。
2. 用户按 04 §3.1 准备两份刺激材料：`cases/auto-topup/update-inputs/交通卡自动充值-v2.docx`、`cases/car-key-sharing/update-inputs/car-review-feedback.md`；启动前随配置展示给用户。
3. 用户决定 §6。
4. `C 三步收口`：五类 target 按实测 12168 一次重签（C3 收口时的约定），由设计评审在 T2 前签。

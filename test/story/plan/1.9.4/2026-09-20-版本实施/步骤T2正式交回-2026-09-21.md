# T2 正式交回：两 Case 双检查点（2026-09-21）

suite `story-suite-20260921-1239`，非沙箱，两 Case 并行，代码基线 `4e044270`（C4）+ `a3ebfc2c`。按 TEST §5.9 八步执行。
**评测只给建议与证据；发布由用户审视成品定。**

## 1. 运行事实

| | auto-topup（AR90006，到 spec） | car-key-sharing（ISSUE-206，到 plan） |
|---|---|---|
| 第一段 | 87.6 分钟 / 919 事件，到目标停住不终止 | 83.1 分钟 / 1048 事件，plan 闭环停住 |
| 第二段 | 43.7 分钟 / 270 事件，update 收口停住 | 24.1 分钟 / 212 事件，update 收口停住 |
| session | 两段同一个（`ses_f3dba6203ffe…`） | 两段同一个（`ses_f3dba61e6ffe…`） |
| 快照 / 回流 | initial + update 均固定；`AR90006`、`AR90006-update` | 同；`ISSUE-206`、`ISSUE-206-update` |
| 终态 | `gate_failed`——外部故障，见 §5 | 同 |
| 上下文终值 | 566K | 617K |

回流无冲突，机制污染检查 clean。预跑 suite 输出起跑前已完整复制到 `E:\Project\bak\T2-prerun-story-suite-20260920-1155`。

## 2. 宿主回过的话（都是模型停下来问的）

| Case | 停在哪 | 回了什么 |
|---|---|---|
| auto | 材料关卡 | 补料：产品原稿 + 九月澄清会记录（脚本 1） |
| auto | 会议裁决（问 5 个话题） | 加更新接口按会上；AC-R1 按会上改；按月筛选不算这张单；调高面额重验、建约失败两件保持待定。脚本里「开关那条」它已按会议收口没问，**没说** |
| auto | 范围关卡（推荐按 SR 拆两份） | 不拆，整体承载 |
| auto | 术语映射表 | 术语确认，按你列的定 |
| car | 材料关卡 | 补料：产品需求稿 |
| car | 范围关卡 | 不拆，整体承载（24h/48h 它没问，没提） |
| car | 交付门后问下一步 | 进入 plan（中性推进；它列的 4 条待表态议题说不阻塞、没要答，没提） |
| car | plan 的 Scope 扩展（Phone 导航注册） | 同意，按产品稿做整页，Phone 只加导航注册，别的不动 |
| 两者 | 第二段起跑 | auto「产品需求改版了，新版我放进需求目录了，按新版把产物更新一下。」（v2 docx 进 inbox）；car「评审意见我写在评审记录里了，按评审意见把产物更新一下。」（意见插进评审记录自由意见区） |
| auto | **第二段材料关卡**（C4 新增） | 就这份新版，按它更新 |
| car | **第二段材料关卡**（C4 新增） | 不补 |

## 3. 四份评测

### 3.1 auto 初评

- 9 条议题 6 定 3 未定；我说「保持待定」的两件（D7 调高面额重验、D8 建约失败兜底）是 open；D1/D2/D5/D6 对得上我的原话，D3/D4 是会上已定。
- 会议裁决的停法对：8 个话题分「会上已收口 / 要你定」，只摆 5 个，逐条给选项与推荐。
- **spec 审查闭环没同步**：当前 subject `e719…` 的报告 PASS 已写盘（14:09），summary 仍是 `completed_with_prior_review` + `semantic_not_reverified`。
- story 10 章 / 31 个 H3 / 3 张图。

### 3.2 auto 后评

- **第二段起手在材料关卡停了一次**（D21 成立），并在问补料时就报出 v0.5 推翻了九月澄清会 T5「200 保持、300 明年」。
- 200 → 300 在 spec（4 处）、验收（3）、story（6）同步，**没有 200 残留**；D4 改为「按产品新版 300」。
- 认出**第二处隐含冲突**（没提示）：v0.5 验收仍写「未实名看不到入口」，与会上 T3 的人工裁决相反；判为保留人的裁决并在 notes 记差异。D7/D8 仍 open。
- update-notes 四段齐；「Plan 尚未生成、不属本轮」写了。
- **两处不对**：
  1. 审查闭环又没同步（观测项直接报出：spec `completed_with_prior_review` + `semantic_not_reverified`；当前 subject `16c8…` 报告 14:52 写盘，summary 14:46）。
  2. **重复审查**：spec 全程审了 7 次、全是 PASS（第一段 3 次、第二段 4 次：14:25 / 14:33 / 14:44 / 14:52）。每次审完材料又动了，只好对新 subject 再审——方法页「派审期间不动被审的材料」没做到。
- update-notes「核对与剩余」写的是「verifier 等完成后再 close」——写在审查之前、之后没更新。

### 3.3 car 初评

- spec、plan 都 PASS 且**闭环干净**（无信号）。
- 6 条议题 2 定 4 未定：D-1 来自我的原话，D-2 来自 PRD/SR 明文；**24h/48h 冲突 D-4 是 open**，没替人定。
- story 10 章 / 34 个 H3 / 2 张图。

### 3.4 car 后评

- **第二段起手在材料关卡停了一次**，并说明这次输入是评审记录里的人话、不是新原件。
- 用 `decide --update D-4` 记评审人原话后 D-4 才 settled；36 小时（story 12 处）与「正在收回」（story 10、spec 3、验收 3）同步到 spec、验收、story、plan（常量与用例）；24/48 只留在「原来两份材料打架、评审裁定 36 小时」的说明里；D-6 按意见维持未决。
- spec、plan 各审一次（14:21 / 14:28），都 PASS——没有重复审查。
- **审查闭环没同步**（观测项报出 spec、plan 两阶段都是 `completed_with_prior_review` + `semantic_not_reverified`；summary 14:15，报告晚 6/13 分钟），而 update-notes 写「receipt 确认本轮审查」——报了假完成。

## 4. 对照版本需求（建议，用户定）

| 验收 | 结果 | 证据 |
|---|---|---|
| AC03 无有效人签不登记 settled | 成立 | 两 Case「人没定」的都是 open；car D-4 有评审原话后才 settled |
| AC04 重要未取得材料被索取 | 成立 | 两 Case 首段材料关卡都要了料；第二段都问了补料 |
| AC06 辨明变化、推演隐含影响、保留未变 | 成立 | auto 认出两处冲突（T5 被推翻、T3 与 v0.5 旧句冲突）；数值全链同步无残留 |
| AC07 冲突/已定结论变化取得人选、不重问历史 | 成立 | 第二段都没重问第一段已定的事 |
| D21 update 先问补料 | 成立 | 两 Case 第二段起手各停一次材料关卡 |
| AC12 同会话两段、四份评测、另名回流 | 成立 | 两个 session 贯穿两段；四个回流目录 |
| AC13 分段报告 | 成立 | 见 §1 |
| 03 §8.2 改动阶段按新审查对象审一次并闭环 | **不成立** | 四段里三段报告写了没同步（auto 两段、car 第二段），只有 car 第一段干净；auto 另有 7 次审查的重复 |
| AC09 无变化快速退出 | 本轮未覆盖 | 两 Case 都有刺激，离线覆盖 |

## 5. 终态 `gate_failed` 是外部故障

两 Case 都在 conclude 之后跑收尾门禁时，`story-build check` 的 node 进程返回 `0xC0000142`（进程初始化失败，日志为空），当时机器空闲内存约 2 GB（共 15.3 GB，几个 opencode/claude/ChatGPT/VS Code 进程合计占满）。post_check 返回 `{"ok":true}`，harness 取已有 summary 为 pass。**在两个工作区上重跑同一条命令都通过（10 章）**。同一时段宿主的后台轮询进程也被系统因内存不足停了三次，我改成前台限时轮询，没影响 Case。

## 6. 要评审与用户看的

1. **「报告写了、没 `--sync-closure`」成了系统性问题**：预跑 1/2、正式 3/4 段。方法页第 4 步写了这条错路，模型仍跳过并报完成。它需要设计层面的处置（C4 评审 §6 定了「不加提示层、先加观测」，观测已经把它报出来了），由设计评审定是否以及怎样改。
2. **auto 的重复审查**（7 次全 PASS）：审完又改材料；auto 的 verifier 归属时长 4270 秒，car 2192 秒（measure_run 的间隔归属近似值，未逐次拆分）。
3. **发布**：四个回流目录在 `doc/features/`，请审视后裁定。

# T1 返修与 T2 准备评审（2026-09-20）

对象：`d5f378fb`（T2 准备）、`ee1cf5bf`（T1 返修）。方案依据 `04 §2.1/§2.3/§3.1`、C3 与 T1 评审 §5/§7。

## 1. 结论

T1 返修三条全部落地，T1 收口。T2 准备的两份刺激与三种落点投递核过，成立。T2 可以由用户启动，启动前只剩两件：§4 的归属问题由用户定；启动时把两份刺激随配置展示给用户（04 §3.1 的要求，执行者代造了刺激，展示这一步不能省）。

## 2. T1 返修逐条

| 评审项 | 实际改动 | 核对 |
|---|---|---|
| ① 第二检查点取不到快照 | 第二段到终点改为 `wait_at_update_checkpoint`：`awaiting_reply` + `awaiting_kind=update_checkpoint`，唯一出口宿主 conclude，租约照续、每 300 秒出声 | 与第一检查点同构；`checkpoint` 的「只认等待态」判据未放宽（用例 `test_the_checkpoint_command_still_requires_a_stopped_worker`） |
| ② 无变化触发第二检查点 | `update.py::_note_prepare` 在 `updates/.last-prepare.json` 留一行机械痕迹（点文件、不进 `_records`、不进镜像、不进比较集合）；装置 `last_prepare()` 读它，只有 `at` 晚于 `resumed_at` 的 `unchanged` 才结束第二段，读不出不算结束 | 时间口径两边都是 `datetime.now(timezone.utc).isoformat(timespec="seconds")`，可比；四条用例覆盖读回、缺失或损坏不算、只认续跑之后的、是过程件不是产物 |
| ③ `test_multi_scenario_cases:23` 残留 | 删 | 全仓无 `story-review` 常量 |
| 既有两条用例红 | 它们用 `iterdir()` 数轮次，点文件不是一轮；改成只数目录 | 判据没放宽 |

## 3. T2 准备逐条

| 项 | 核对 |
|---|---|
| 三种落点 | `local_material` → inbox；`review_human_zone` → 只写进 `<!-- freeform-zone -->` 与 `<!-- /freeform-zone -->` 之间，标记与 `review.mjs:393-394` 一致，边界找不到改投 inbox 并记「不是回流那条路」；`system` → 需求系统目录，越界拒绝。初始 seed 只复制 `cases/<id>/system`，`update-inputs/` 目录隔离成立 |
| car 刺激 | `car-review-feedback.md` 四句：36 小时替代 24 小时、提示语「正在收回」、转分享与强制下线维持未决、界面稿不变。与 04 §3.1 逐句相符。car 第二轮 review 的自由意见区为空，插入不会覆盖既有人话 |
| auto 刺激 | `交通卡自动充值-v2.docx` 与原稿 81 段逐段比对，只差三处：文档状态句后加改版说明、版本 v0.4→v0.5、「200 元」→「300 元」；十张图原样。与 04 §3.1 相符 |
| 全量回归 | 我自跑 1199 passed / 15 skipped，74 秒，零失败 |
| 预算 | 实测 12179，T1 返修在 update.py +11；target 随实测改签 scripts_py 2727、总 12179，在途不动，预算测试 5 项通过 |

一处措辞与实现不一致，不阻断：T1 返修的提交说明写「无变化那条路按设计什么都不建」，而现在它建一个点文件。设计 03 §2 步骤 4 与 AC09 的措辞按 C1/C2 评审改成「无变化不建副本、不建操作记录，只留过程痕迹」，设计者改稿时一并处理。

## 4. 仍待用户定

`test/story/AGENTS.md:157` 那段「离线用例一律并行跑」仍在，C3 与 T1 评审 §6 已说明归属：内容对，家在 TEST §7.9。移或留由用户定，执行者不再自行动规约层。

## 5. T2 启动要点

- 按 TEST §5.9 八步走；等待窗口只固定快照与只读评测，不向被测会话发评分、缺陷清单、脚本路径或修法。
- 第二段投递顺序：先 `--deliver` 材料，再 `/story update <编号>` 这句正常业务话。
- 两 Case 的初评与后评各一次，回流原编号与 `<编号>-update`；四份评测按 04 §4 三维度写，绑定快照摘要、session 与事件区间。
- 独立审查的次数、耗时、subject 单列；F4/F5 若复现按裁定 §6 处置，T2 进行中不改机制。

# 步骤 0 评审：d79e8aca

结论：**通过，步骤 0 关闭。** 退出项零残留、三处缺陷有夹具、三道门独立复核全绿；交回里四处与方案不一致的实现都有代码事实支撑，按事实采纳并回写方案。两条小项进步骤 1 交回一并处理，不返修。

## 1. 独立复核

| 门 | 本评审独立运行 | 结果 |
|---|---|---|
| 离线全量 `pytest test/story/tests -n auto --dist loadscope` | 跑了 | **1031 passed / 323 subtests，49.6 秒**，与交回一致 |
| `check_failure_modes.py` | 跑了 | 64 条 FAIL 0（委派 8、PASS 56） |
| `adapt-scan --check --target .` | 跑了 | 核对通过 |
| `test_mechanism_budget.py` | 跑了 | 5 passed / 16 subtests；实测 10743 在 10950 内，yaml 未改 |
| `framework/` 改动 | `git diff --stat` | 零 |
| 退出项残留 | 逐名 rg `doc/extensions` 与 `test/story` | `sources/ar`、`AR_SOURCES`、`originalArSource`、`design.origin`、`question_words/chapter_words`、`material_dirs`、`SOURCE_DOCS`、`--by/ACTORS`、`review-task` 在机制层零命中；测试域剩余命中是拒收用例与 assertNotIn，合法 |
| 措辞残留 | rg「上游原件 / 上游原 AR / 留存」 | 机制层零命中；作者包 4a/4b 顺延后测试同步 |

diff 逐文件读过：`submission.py`、`state.py`、`flow/check.mjs`、`sources.mjs`、`author.mjs`、`reader-review-task.mjs`、`registry.py`、`importer.py`、`yaml-lite.mjs`、`chapter.mjs`、`lifecycle.py`、`rounds.py`、`decisions.py`、`story_flow.py`、`story-build.mjs`、`context.mjs`、`language.mjs`、`appendix.mjs`、`routing.py`、合同、overlay、五份规则页、`scripts/README.md`、失效形态台账与 `1.9.2.md`。

## 2. 与方案不一致处的裁定

| 项 | 交回的处理 | 裁定 |
|---|---|---|
| R1 重试识别 | 保留三类身份（`.backup/` 内容相符、空骨架、登记提取稿），不是方案写的两类 | **采纳**。方案 §3.1 动作行与保护行自相矛盾，实施者按保护行取舍正确；「来历说不清」拒收保留，`test_retry_without_a_backup_refuses`、`test_an_unrelated_backup_does_not_count_as_this_input` 覆盖 |
| R8 无清单时 | 记一笔、判据未执行，不升级为错误 | **采纳**。与 ④ 图片身份判据同口径；正常流程 skeleton 已强制清单。不另立升级项 |
| R10 testing overlay | 未实施 | **采纳**。framework `context-exploration.ts:198` 明写 testing 无该门禁，`test_author_context_entry.py` 现役用例守着；方案前提错误，01 总览 R10 改记「非漏配」 |
| L6 键形状 | 标识符或整段引号键 | **采纳**。真实 `contracts.yaml` 有引号键，方案的正则会解析坏它 |
| L10 / L11 | `story-build init` 与 check ⑦ 早已不在，只改文件头与注释 | **采纳**。方案沿用了清点 08 收尾（2026-09-10）的过期事实，未重核 |
| R5 附带影响 | README 在材料清单的标签从「界面参考」变「材料」 | **采纳**，与 `SOURCE_DIRS` 逐文件登记的语义一致 |

## 3. 进步骤 1 交回一并处理（不阻断）

1. `test/story/release/1.9.2.md` 补一节「修复」：列表句里的冒号不再被读成键值、空小节与同名小节在章提交时被点名、未收口时登记成文的提示先说状态。使用者可见，不该只在交回里。
2. `yaml-lite` 仍不认流式映射 `- { name: X, type: string }`（模块头注已声明不支持），而 framework 的 plan 契约模板用这种写法登记 `data_models[].fields[]`；1.9.1 car 的 `contracts.yaml` 第 35–46 行即是。这是步骤 0 之前就有的限制，与本次键收窄无关（新旧读法都得不到 `name`）。登记为步骤 2（知识协议）的核项：挂在 `fields[]` 上的 must 能否被 `obligations.mjs` 解析到。

观察项（不要求动作）：`.backup/` 命名精确到秒，同一秒内两次 S4 覆盖会同名覆盖；正常流程一轮只提交一次。

## 4. 规模与预算

10867 → 10743（−124），方案估 −100；scripts_mjs 3310、hooks_mjs 3029、data 732、prompts_md 1834、scripts_py 1838。全部为退出与缺陷修复，没有新增职责；不改 yaml。步骤 1 开工基线以本提交为准。

## 5. 下一步

实施者以 d79e8aca 为基线按 [04 步骤 1](04-Review决策发现与表达.md) 实施；开工前重核 04 §2.3 表里的行号与函数名（`readPicks/checkPick/FIELDS` 等）是否仍与代码一致。

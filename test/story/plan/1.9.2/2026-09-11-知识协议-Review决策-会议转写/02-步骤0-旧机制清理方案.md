# 1.9.2 步骤 0 · 旧机制残留清理与已知缺陷修复

状态：**已实施并通过评审（d79e8aca，见 [步骤0评审](步骤0评审-2026-09-15.md)）。** 评审采纳的四处偏差：R1 重试识别保留三类身份；R8 无清单时记一笔不升级为错误；R10 不实施（framework 的 testing 阶段没有该门禁，非漏配）；L10/L11 前提不成立（`init` 子命令与 check ⑦ 早已退出），只改注释与文件头。以下正文为开工时的定案，不追改。基线 2211e4ea；开工核当前 HEAD 与工作区（`test/story/regression/mechanism-budget.yaml` 有评审同步的未提交改动，不并进本步提交）。本步一次实施、一份交回；每项动作先写夹具（先红后绿）再改机制。

## 1. 目标与判据

把 1.9.1 暴露的「有生产者没读者、两处声明互相否定、迁移桥过期」三类残留连同消费者、文档与测试一起退出；修三处已知缺陷；补两处漏配。**不加新能力、不动 framework、不改知识内容。** 每项按「谁生产、谁读、谁在文档承诺」三向核过再删；删后没有第二套说法留在交付面。

完成判据：§3 每项有实际 diff 与三向核记录；退出项在 `doc/extensions` 与 `test/story` 内零残留引用；离线全量绿、`check_failure_modes.py` FAIL 0、`adapt-scan --check` 退出 0；1.9.2 发布说明草稿登记退出项（§5）。

## 2. 必读输入

`test/story/AGENTS.md` §3.2、§5；01 总览 §2/§3；历史残留清点 [08 收尾](../../1.9.1/2026-09-10-extension历史残留清点/08-B5整体收尾.md)（三向核的教训）；本需求 [步骤4评审](../../../spec/1.9.2/2026-09-14-模型成文与审查闭环优化/步骤4评审-2026-09-15.md) §3 B6。

## 3. 动作清单

按依赖顺序分四组。行号以 2211e4ea 为准，开工时重核。

### 3.1 上游原件留存机制退出（R1，用户已裁定）

现状：S4 `complete` 覆盖 `AR/design.md` 前把旧内容存成 `AR/story-src/sources/ar/r<轮>.md`（`flow/submission.py:165-243`），契约 `design.origin` 指向它；五个消费者：作者任务包 4a（`hooks/spec/author.mjs:244-339`）、审查任务书（`hooks/shared/reader-review-task.mjs:80,111-114`）、材料清单必列与「上游原件」标签（`core/story/sources.mjs:125-169,254-256`）、`flow/check.mjs::originalArSource:377-406`、`flow/state.py::AR_SOURCES:36`。1.9.1 两 Case 作者零读取，六条预填全由提取稿承接。

动作：

| 位置 | 动作 |
|---|---|
| `submission.py` | 删留存分支与 `keep`/`prior_ar`/`ar_input_identities` 里的留存件身份；覆盖前改走 `materials/importer.py:309` 的 `.backup/` 约定（`AR-design.md-<时间戳>.md`），与导入覆盖 RR/SR 同一条退路。重试识别只保留两类身份：空骨架、契约已登记的提取稿；`design.origin` 字段退出，契约 `design` 只留 `sha256` |
| `state.py` | 删 `AR_SOURCES`；`STORY_SRC` 保留集不再含 `sources/` |
| `flow/check.mjs` | 删 `originalArSource` 及其导出 |
| `sources.mjs` | 删 `originalArTarget`、清单必列与「上游原件」标签；材料清单里「开发需求：design.md」一行的说明改为「本轮提取稿（上游预填已并入）」 |
| `hooks/spec/author.mjs` | 删 4a 节；任务包不再指向留存件 |
| `reader-review-task.mjs` | 删「上游原 AR」两行；审查回查上游原话的对象改为 `RR/SR` 原文与 `.backup/` 中被覆盖前的 `AR/design.md`（只给位置，不必列） |
| `rules/ar_design_init.md`、`SKILL.md`、`phases/spec.md` | 删「留存原 AR」表述；提取稿义务不变：上游预填每条须在提取稿承接 |
| 本需求 00 §2/§4 | 设计者已改：删「S4 提交候选并留存原AR」与保护项「原AR三身份」（见 01 §2 L9） |
| 测试 | `test_s4_commit.py`、`test_spec_story_gate.py`、`test_story_build.py` 中留存件用例改为 `.backup/` 存在与重试识别；`test_author_task_package.py`、`test_verifier_report_protocol.py` 里 4a/上游原件断言退出 |

保护：提取稿必须承接上游预填这一义务（`ar_design_init.md` §多源互补）保留；`complete` 的重试、中间态识别、材料基准一致三条失败语义保留并有夹具。

### 3.2 合同与契约里的双声明、零读者字段（R3–R8、R14）

| R | 位置 | 动作 | 消费者/文档同步 |
|---|---|---|---|
| R3 | `contracts/story-chapters.json:269-276` `verdicts.question_words/chapter_words` | 删两键；`_note` 保留 | 无读者；`test_story_build` 合同形状用例若断言键存在则改 |
| R4 | `story-chapters.json:sources` 与 `materials/registry.py:41 SOURCE_DOCS` | Python 侧从合同读来源清单（`registry.py` 加一次合同解析，`SOURCE_DOCS` 退出）；`importer.py:56-57` 的落点表同源 | 单一真源；`test_material_rounds.py` 里 `upstream.md` 用例改按合同 |
| R5 | `story-chapters.json:37` `sources.UX`；`sources_note:5` | 删 `UX` 来源条目；UX 类文档正文的落点定为 `ux-reference/README.md` 仅作导入落点、由 `registry.SOURCE_DIRS` 逐文件登记为材料（已有），`sources_note` 那句改为「ux-reference/ 下的文件按目录登记，不在 sources 里第二次登记」；`inbox_import.md:30` 说法同步 | 运行日志不再报「UX 不存在」 |
| R6 | `story_flow.py:85-86 --by`、`state.py:63 ACTORS` | 删参数与常量；`decisions.py` 写 gate 记录时固定 `by: human`；`init_analysis.md:187,240`、`SKILL.md:87` 改为「关卡只认人签，脚本记 human」 | 六份测试里 `--by human` 的调用去掉参数 |
| R7 | `rounds.py:39 feature`、`:55,110 analysis`；`lifecycle.py:135 story_written_at`；`:187-190 archived.story/review` | `feature`、`analysis` 退出（`round` 不再算 analysis 哈希）；`story_written_at` 保留并进 `status` 输出「成文登记于」；`archived` 只留 `at` | `flow/check.mjs` 不读这些键，无改；`test_material_rounds.py` 的 `analysis` 断言退出；`archived` 三处测试只用布尔，核一遍 |
| R8 | `sources.mjs:337-342 haveManifest/material_dirs`；`story-chapters.json:347-353` | 删白名单分支与合同键；无清单时按错误报「先跑 round」 | `material_dirs_note` 同删 |
| R14 | `sources.mjs:186-188` | 删迁移注释 | — |

### 3.3 迁移桥、人工入口与漏配（R2、R9–R11）

| R | 位置 | 动作 |
|---|---|---|
| R2 | `rules/review_reflow.md:51` | 删「AR 根下有同名台账就挪进 story-src」一句及其对应代码（`core/story/context.mjs::strayFileProblems` 中的迁移分支，步骤 3 §6.1 加入）；根下出现台账按通用散件规则报 |
| R9 | `story-build.mjs:53,228,254 review-task` | 退出子命令；`reader-review-task.mjs:140` 注释同步。审查输入只经 `pre_verifier` 注入 |
| R10 | `rules/testing-rules.overlay.yaml` | 加 `exploration_thresholds.phase_input_snippets_extra: [doc/extensions/hooks/testing/author.md]`，与 ut overlay 同形 |
| R11 | `rules/inbox_import.md:79-85` | 补 `--unused <图> --reason`／`--used` 的用法一句，与 `--caption-image`、`--register-ux` 并列；`.captions.json` 在 `scripts/README.md` 登记为侧车（作者不手改） |
| R12 | `scripts/README.md` | 登记 `--offline` 用途：金样单向否决锚与孤立文档检查，不读需求工作区 |

### 3.4 已知缺陷（L6–L8、L11）与旧起手入口（L10）

| L | 位置 | 现状 | 动作 |
|---|---|---|---|
| L6 | `hooks/shared/yaml-lite.mjs:19 KV_RE` | 键匹配 `[^\s#][^:]*?`，含空格与 `/` 的整句被当成键：`- 布局与文本对齐: 用 start/end` 解析成映射，`render` 出 `[object Object]`，作者被迫改措辞 | 键限定为 ASCII 标识符 `[A-Za-z_][\w.-]*`（扩展自有 YAML 的键全是标识符；acceptance/manifest 亦然），其余含冒号的行按标量读；夹具：列表项含 `: `、`/`、`：` 三种各一条 |
| L7 | `core/story/chapter.mjs:293-308` | 只查章级标题重复，不查小节 | 加两条：同章内同名 H3 两处、H3 之后到下一标题无正文（空小节）；报到标题名，出口是删或补正文。夹具用 1.9.1 auto 的 §6.7/§6.8 形态 |
| L8 | `flow/lifecycle.py:86-89` | `status != complete` 时算 `next_step` 失败会把「还没收口」报成别的错 | `next_step` 包进 try：失败时只报状态与「先跑 status」 |
| L11 | `core/story/check.mjs:240-250 ⑫b`、⑦ | 核的是 `projectAppendix` 刚投出的内容 | 保留 ⑫b（作者单独跑 check 时仍是唯一的机器区核对），删 ⑦ 中与投影同源的那段；具体行号开工核 |
| L10 | `story-build.mjs init` | 清点 08 收尾剩余：交付面零调用，测试 24 处起手用它 | 退出子命令；测试起手改用 `skeleton`（它已是唯一写入口），共用一个夹具 helper 迁移 |

## 4. 退出与保留对账

| 退出 | 接替 |
|---|---|
| 留存目录 `sources/ar/`、`design.origin`、五个消费者、1.9.1 说明 §3「上游原件与本轮提取稿分开」 | `.backup/` 通用退路；提取稿承接义务；RR/SR 原文 |
| `question_words/chapter_words`、`material_dirs`、`SOURCE_DOCS`、`sources.UX`、`--by/ACTORS`、`feature/analysis`、`archived` 指纹、迁移注释、review_reflow 迁移句与代码、`review-task`、`init` | 各行已列 |
| 保留 | `--offline`、`review-disposition.json`（纯人工台账，形状校验归步骤 1）、`provides.bridges`、`story_written_at`（进 status）、`imported`（`round` 在读） |

## 5. 验收

1. 夹具先红后绿：3.1 的 `.backup` 与重试识别、3.2 的合同单源、3.4 三条缺陷各有用例。
2. 零残留引用：`rg` 退出项名称在 `doc/extensions`、`test/story` 零命中（`EVOLUTION.md` 与历史方案除外）。
3. 三道门：离线全量绿；`check_failure_modes.py` FAIL 0；`adapt-scan --check` 退出 0；`framework/` 零改动。
4. 交回附三向核表：每项写生产者、读者、文档承诺三栏的核实结果与出处。
5. 1.9.2 发布说明草稿（`test/story/release/1.9.2.md`，本步只建「退出项」一节）登记 3.1 与 R3–R9 的退出及接替。

## 6. 预算

主要指标有效行。估算：3.1 约 −90（submission/check/sources/author/reader-review 五处删多补少）；3.2 约 −25；3.3 约 −15（R10/R11/R12 +8）；3.4 约 +30（L7 两条判据 +18、L6 +2、L8 +4、⑦ −10、init 退出 −20 与测试 helper 另计）。合计约 **−100**，字符另报。当前总量 10867 超在途上限 10800（L13），本步是净减，不改 yaml 数值；收口时按实测报，用户签一次。测试域改动（`init` 起手迁移约 24 处、留存件用例改写）按 git 行另列，不进机制预算。

## 7. 实施顺序

3.4 L6/L7/L8 先（独立、小、有夹具）→ 3.2 R4/R5（来源单源，为 3.1 铺路）→ 3.1 → 3.3 → 3.4 L10/L11 → 全量与三道门 → 交回。

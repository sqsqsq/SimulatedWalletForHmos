# 步骤 2 评审：7d31f623（含步骤 1 返修 e948c6f3）

结论：**静态通过，步骤 2 的机制实现关闭；行为验收（03 §7 判据 17）留到 1.9.2 全部步骤后的统一实跑。** 交回 §4 的七处读法全部采纳。两条观察项进下一步交回，不返修。

## 1. 独立复核

| 门 | 本评审运行 | 结果 |
|---|---|---|
| 离线全量 | 跑了 | 1066 passed / 324 subtests，与交回一致 |
| `check_failure_modes.py` | 跑了 | 64 条 FAIL 0 |
| `adapt-scan --check --target .` | 跑了 | 通过 |
| `test_mechanism_budget.py` | 跑了 | 全绿：hooks_mjs 3179 ≤ 3220，总量 11051 ≤ 11100 |
| `framework/` | diff | 零 |
| 退出项残留 | rg `verify: probe / 'probe' / sections.implement / 机制不派生 / 每条义务…说法` | 机制层零命中；`used_for` 仅剩按面结构里的字段名 |
| 样板知识实际载入 | `activeKnowledge` 跑一遍 | 15 条规约的强制力、执行体、探针全部按表派生：UX-01 基线·模型/实机·阻断 absent_regex；OBS-01/02/03 红线·模型/实机·present_in_method；DLV-01/02 基线·模型/人工；两份事实 10 个面、无未确认面 |

逐文件读过：`knowledge.mjs`、`obligations.mjs`、`contracts.mjs`、`probes.mjs`、`plan/coding/review` 三个 post_check、`knowledge-use/{document,validation,projection}.mjs`、`spec/post_check.mjs`、`appendix.mjs`、六份作者页、`plan-sections.md`、coding overlay、三类 README 与两份样板、`1.9.2.md`、新用例 26 条。步骤 1 返修 e948c6f3（骨架可点名八种图，正文只认点名的那种；作图提示不偏向时序图）一并读过，与 04 §2.2「形式按关系选」一致，通过。

## 2. 读法裁定（交回 §4）

| 项 | 裁定 |
|---|---|
| 1 值域自检在载入一次抛出，不进 `selfCheck` | 采纳：各道门都要用 `force / executors`，值域不合时下游无法执行，载入报最早；收齐再报避免改一条撞一条 |
| 2 「人工」与评审动作的对应 | 采纳：评审动作条目必须含人工；只由人工验的必须是评审动作；模型 + 人工的普通条目（DLV-01）合法。与判据 15「样板零报」一致 |
| 3 `protocol` 只核声明了的 | 采纳；三份类目 README 已写 1 |
| 4 「探针无落点」记为未执行判据 | 采纳，回话可见、不阻断 |
| 5 coding「记未落实 / 证据缺口」只进告警明细与 overlay 提示 | 采纳：语义归 verifier，不另立状态文件；review 门禁对基线「未落实」要依据已接住后果 |
| 6 注释里的规约编号按阻断 | 采纳：确定性、修起来成本低 |
| 7 plan 作者页删两句与模板重复的话 | 采纳：`plan-sections.md:15,36` 原句仍在 |

判据 12 分两处测未串成单一夹具：可接受，两处各自覆盖了「spec 命中 → plan 无 must」与「plan 有 must → 复核表少一行」。

## 3. 观察项（进下一步交回，不阻断）

1. **文件作用域探针按落点重复跑**：`coding/post_check.mjs` 对每条 `must` 各跑一次探针；`absent_regex` 这类不按实体收窄的探针，同一规约挂 N 处就报 N 次同样的行号。下一步按「规约 + 探针 + 实际扫描范围」去重一次即可。
2. **事实按面的空壳报错措辞**：骨架里 `facet: ""` 未填时报「没有面「」」，读者看不出是空着没填。改成「facet 空着」一句。

## 4. 预算

e948c6f3 10888 → 11051（+163）；03 估新增约 +190、退出约 −10，hooks_mjs 估 +168 实到 +143。R ≈ 91%，无需自查升级。附录 D 豁免投影在 scripts_mjs 净 0 行是改写同一段表达式容纳，不是砍功能。

## 5. 记录

- 03 分册状态改为「已实施，静态评审通过」。
- 目标仓升级须知：模式文件从本版起必须有 `# 上篇 ·` / `# 下篇 ·` 两个一级标题，`1.9.2.md` 已写进载入行；发布时在「升级」一节再单列一句。
- 步骤 0 评审 §3 第 2 条（yaml-lite 流式映射）已由 `contracts.mjs::flowStyleProblems` 关闭。

## 6. 下一步

1. 设计者按 01 §4.3 局部改 05 会议转写分册（落点按当前目录；有效结果进初筛 ⑥ 与写作设计骨架；`decide --gate meeting` 与已退出的 `--by` 对齐；预算按当前规模重估）。
2. 实施者做步骤 3。
3. 1.9.2 全部步骤后统一实跑，判据含 03 §7 第 17 条与 04 §5 第 1、6 条。

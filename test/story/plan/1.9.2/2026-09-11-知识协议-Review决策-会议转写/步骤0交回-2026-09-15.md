# 1.9.2 步骤 0 交回 · 旧机制残留清理与已知缺陷修复

维护实施者交回（2026-09-15）。对象：[02 步骤 0 方案](02-步骤0-旧机制清理方案.md)。基线 80835ae9（2211e4ea 之后只多了 1.9.1 收口的三份文档提交）。**已实施，等待评审**；提交 d79e8aca。

## 1. 结果

| 门 | 结果 |
|---|---|
| 离线全量 `pytest test/story/tests -n auto` | 1031 passed / 323 subtests（基线 1035；新增 9 条，随退出项删 13 条） |
| `check_failure_modes.py` | 64 条 FAIL 0（委派 8，PASS 56），S16 PASS |
| `adapt-scan --check --target .` | 退出 0 |
| `framework/`、`knowledge/` | 零改动 |
| 语法 / 导入 | 改动的 .mjs `node --check` 通过，`story-build`、`author`、`pre_verifier` 实际 import 通过；`compileall test/story/scripts` 通过 |
| 未跑 | `tools/cli` 两条与 `run_multi_case plan`（本步没动那里）；真实 CLI |

规模（预算测试同口径，代码行不含注释空行）：

| 类 | 80835ae9 | 现在 | 差 |
|---|---:|---:|---:|
| scripts_mjs | 3377 | 3310 | −67 |
| hooks_mjs | 3075 | 3029 | −46 |
| data | 751 | 732 | −19 |
| prompts_md | 1826 | 1834 | +8 |
| scripts_py | 1838 | 1838 | 0 |
| **合计** | **10867** | **10743** | **−124**（方案估 −100） |

完整字符 565613 → 559118（−6495）。Git 行：`doc/extensions` 27 文件 +214/−396；`test/story` 14 文件 +275/−399，另新增 `release/1.9.2.md`。回到在途上限 10800 以内，yaml 数值未改。prompts_md +8 是 R11 补的两条命令与说明。

## 2. 逐项实施与三向核

「生产 / 读者 / 文档承诺」三栏写核实结果与出处，行号为改动前。

### 2.1 R1 上游原件留存退出

| 向 | 核实 |
|---|---|
| 生产 | `flow/submission.py:165-243` 写 `sources/ar/r<n>.md`，`design.origin` 登记 |
| 读者 | `flow/check.mjs::originalArSource` 被三处 import：`hooks/spec/author.mjs:25`（4a 节）、`hooks/shared/reader-review-task.mjs:14,80`、`core/story/sources.mjs:11`（清单必列、「上游原件」标签、坏指针报错）；`state.py::AR_SOURCES` 只被 submission 用 |
| 文档 | `rules/ar_design_init.md:9-10,141-142`；`phases/story-write.md:66,127`；`flow/routing.py:163` 下一步文案；1.9.1 发布说明 §3 与功能清单 |

动作：
- 覆盖前走 `importer.backup` 的 `.backup/` 约定；新增 `importer.backups_of`，保证备份命名只在一处定。
- `submission.py` 重写：契约 `design` 只留 `sha256`，结果字段 `origin` 换成 `backup`。
- 五个消费者、文案与测试同次退出。作者任务包原 4b/4c 顺延为 4a/4b。审查任务书改为给 `.backup/` 位置，并提示回查 RR、SR 原文。

**与方案不一致（按事实处理）**：
- 方案 §3.1 写「重试识别只保留两类身份：空骨架、登记提取稿」，而同节「保护」要求重试和中间态识别保留，测试行又写「`.backup/` 存在与重试识别」。只认两类身份的话，覆盖写到一半、或清单已刷新而契约没保存时，就认不出是重试，重跑会被判成材料变了。所以实现为三类身份：`.backup/` 里内容摘要相符的那份、空骨架、登记提取稿。「来历说不清」这条拒收保留。
- 原「同一轮原输入只有一份」冲突拒收随留存件退出：备份可以有多份，按内容认。改为用例 `test_an_unrelated_backup_does_not_count_as_this_input`。

### 2.2 合同与契约（R3–R8、R14）

| R | 生产 | 读者 | 文档 | 动作与偏差 |
|---|---|---|---|---|
| R3 | 合同 `verdicts.question_words/chapter_words` | 全仓零读者 | 无 | 删两键 |
| R4 | 合同 `sources` 与 `registry.py:41 SOURCE_DOCS` 各一份；`importer.py:51-58 DOC_TARGET` 再抄一份路径 | `collect_materials`、`collect_sources`、`cmd_import`、`test_material_rounds` 两处 | 无 | `importer.contract_sources()` 读合同；`registry.source_docs()` 取非派生来源，顺序照合同，材料摘要不变；`DOC_TARGET` 常量换成 `doc_targets()`，RR/SR/AR 经 `DOC_SOURCE` 映射到合同键。scripts_py 为此 +10 左右，与 R7 的删减抵平 |
| R5 | 合同 `sources.UX` | `sources.mjs::scanSources` 当作可选来源，运行日志报「不存在」 | `sources_note:5` 自相否定；`inbox_import.md:30` | 删 `sources.UX`，改写 note。**附带影响**：README 仍经 `SOURCE_DIRS` 进材料清单，但清单骨架里它的类别标签从「界面参考」变成「材料」，审查任务书「原材料原文」一节也不再单列它 |
| R6 | `story_flow.py:85-86 --by`、`state.py:63 ACTORS` | `decisions.py:22` 校验与记录 | `SKILL.md:87`、`init_analysis.md:187,240`、`story_flow.py:19` 用法 | 参数与常量退出，`decisions.py` 固定记 `by: human`，理由注释移到记录处。拒收改由 argparse 完成（`--by ai` 报 unrecognized arguments），用例相应改写；十余处测试调用去掉 `--by human` |
| R7 | `rounds.py:39 feature`、`:36,55,110 analysis`；`lifecycle.py:135 story_written_at`；`:187-191 archived.story/review` | `feature`、`analysis`、archived 两个指纹零读者；`story_written_at` 只有 `reopen` 撤销；`archived` 只被当布尔用 | `test_material_rounds` 断言 analysis 哈希、docstring 提到它 | `feature`、`analysis` 退出；`status` 输出加 `story_written_at`；`archived` 只留 `at`，`lifecycle` 不再 import registry。**未验证项**：`archived` 形状没加用例（登记归档要走交付门），靠读代码核实读者只用布尔 |
| R8 | 合同 `material_dirs(_note)`；`sources.mjs:337-342` 无清单时按白名单判 | `language.mjs::scanMaterialList` 的 allowDirs 分支、`firstSegment`、`IMAGE_HINTS.material_scope`；S16 的点名文案 | 无 | 删合同键、白名单分支和上面三处死代码，S16 点名改为「列了不是初始资料的东西」。**与方案不一致**：方案要求「无清单时按错误报先跑 round」，实现保留「记一笔、判据未执行」。理由：同类的 ④ 图片身份判据（`images.mjs:163`）在无清单时同样记一笔，只把这一条升成错误会让两条同类判据口径不一；而且 `test_story_build` 多数夹具不带清单，要迁移一批方案没算进范围的用例。正常流程里 skeleton 已强制清单存在。要升级为错误的话，建议 ④ 和 ⑫c 一起定 |
| R14 | `sources.mjs:186-188` 迁移注释 | — | — | 删 |

### 2.3 迁移桥、人工入口、漏配（R2、R9–R12）

| R | 三向核 | 动作与偏差 |
|---|---|---|
| R2 | 文档 `review_reflow.md:51-52`；代码是 `context.mjs::strayFileProblems` 的「根下与 story-src 同时存在」分支（8d9d649f 与迁移句同次加入）；读者是 `test_story_build` 的 `test_two_copies_stop_for_a_human` | 删两句、删该分支和用例；根下散件按通用规则报「原样挪进 AR/story-src/」 |
| R9 | 生产 `story-build.mjs:53,228-232,254`；`reader-review-task.mjs` 的 `withMethod`/`reviewMethod` 只服务这个入口；读者只有 `test_verifier_report_protocol` 的一组用例 | 子命令、`withMethod`、`reviewMethod` 与 `parseYaml` 导入一并退出，4 条用例同删。pre_verifier 调用不带选项，不受影响 |
| R10 | `framework/harness/scripts/utils/context-exploration.ts:198`：「有 context-exploration.md 门禁的 phase（testing 无——靠 trace）」；`test_author_context_entry.py:166-169` 断言 testing overlay 不声明 | **未实施**。testing 阶段没有读 `phase_input_snippets_extra` 的门禁，登记了也没有消费者，还会推翻现役用例的「不造假留痕」。原则页 `hooks/testing/author.md` 的送达方式与另外五阶段不同，是既定设计，不是漏配 |
| R11 | `import_sources.py:45-51` 实际参数是 `--caption-image <图> --unused "<理由>"` 与 `--used`，不是方案写的 `--unused <图> --reason`；`.captions.json` 生产者是 `registry._write_entry` | `inbox_import.md` 按实际参数补两条命令；`.captions.json` 登记进 `scripts/README.md` |
| R12 | `--offline` 读者是 `check_failure_modes.py` 的 g01 与 check 离线分支 | `scripts/README.md` 登记用途。措辞用「核一份认可的样稿」，没有写测试域的「金样」 |

### 2.4 缺陷与旧入口（L6–L8、L10、L11）

| L | 事实 | 动作与偏差 |
|---|---|---|
| L6 | `yaml-lite.mjs:18` 键正则把列表项里的整句当键 | 键限定为 ASCII 标识符 **或整段加引号**。**与方案不一致**：方案只给 `[A-Za-z_][\w.-]*`，而真实 `doc/features/ISSUE-206/contracts.yaml:20` 有 `"+02-Feature/WalletMain -> …":` 这类引号键，照方案收窄会把它解析坏。夹具覆盖三种冒号句与标识符键、引号键 |
| L7 | `chapter.mjs` 只查章锚重复 | 新增 `sectionShapeProblems`：同章同名 H3、空 H3。空节的定义是到下一个 H3 之前既无非空行、也没有围栏；只放一张图的节不算空，H3 下只有 H4 的也不算空。章提交与全篇 check 共用。它当场拦出一条旧用例夹具（投影删掉合同外机器区后「### 旧节」只剩空标题），标题归作者，所以改夹具：在旧节下补一句作者说明 |
| L8 | `lifecycle.py:86-89` | 算下一步失败时仍先报「还没收口」，原因和 `story_flow.py status` 附在后面 |
| L10 | **方案前提不成立**：`story-build.mjs` 的 COMMANDS 早已没有 `init`，08 收尾说的「31 处起手」在代码里是 `story_flow.py init`（S1 现役命令）和 `knowledge-use init`，都不是退出对象 | 只修文件头命令表：去掉 `init` 行，补上 `project` 行，并在 skeleton 行写明建决策登记骨架 |
| L11 | **方案前提不成立**：core check 里已经没有 ⑦，与投影同源的那段早已退出；残留的只是 `appendix.mjs:375,393` 两处写着「check ⑦」的注释，以及 541 行的历史叙述 | 两处注释改指 ⑫b，541 行改成现在时的理由，⑫b 保留 |

## 3. 退出与保留对账

| 退出（已零残留） | 接替 |
|---|---|
| `sources/ar/`、`design.origin`、`originalArSource/Target`、`AR_SOURCES`、作者包 4a 节、「上游原件」清单行与报错 | `.backup/` + 三类身份识别；提取稿承接义务；RR/SR 原文 |
| `question_words/chapter_words`、`SOURCE_DOCS`、`DOC_TARGET` 常量、`sources.UX`、`material_dirs(_note)` 与白名单分支、`firstSegment`、`--by/ACTORS`、`feature/analysis`、archived 指纹、`review-task`/`withMethod`/`reviewMethod`、review_reflow 迁移句与同时存在分支、过期注释 | 见 §2 各行 |
| **保留** | `--offline`（已登记）、`review-disposition.json`、`provides.bridges`、`story_written_at`（进 status）、`imported`、`materials_changed_after_complete`、testing overlay 不声明 snippets（R10） |

零残留扫描范围：`doc/extensions`、`test/story/tests`、`scripts`、`fixtures`、`regression/failure-modes.yaml`、`test/story/*.md`。剩余命中都有合法来由：

- pre_verifier 正常导入任务书构造器。
- `material_scope` 是关卡名。
- 测试里的 `--by ai` 是拒收用例本身，`"analysis"` 是 assertNotIn 断言。
- F01/R04 四份夹具的 story-flow.json 仍带 `analysis`/`feature`。它们是 2026-08-28 的历史契约快照，本来就混着多代已退出的形状（`inputs`、`by: ai`、旧路径），机制不读，未改。

历史记录没动：1.9.1 发布说明、`mechanism-budget.yaml` 的 reason 叙述、EVOLUTION、各方案目录。TEST §7.2 负面扫描：`.backup/` 命中与改动前的 importer、inbox_import、init_analysis 同属一类噪声，其余四条没有新增命中。

## 4. 请评审关注

1. R8 保留「记一笔」而不升级为错误（§2.2）：是否要把 ④ 与 ⑫c 一起升级，由评审定。
2. R10 不实施（§2.3）。
3. R5 的附带影响：README 在材料清单里的标签从「界面参考」变成「材料」。
4. 1.9.2 发布说明草稿（`test/story/release/1.9.2.md`）按 02 §5 只建了「退出项」一节，L6–L8 的缺陷修复没写进去。

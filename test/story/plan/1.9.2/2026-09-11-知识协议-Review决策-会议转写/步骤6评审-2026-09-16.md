# 步骤6当前复审：1605003c

结论：上一轮R1/R2/R3核心返修通过；本次仍有一个确定性误判及少量旧模板分支未退，预算收敛未完成。**不能接受“全部增长都只是方案估低、所有旧实现均已退出”的总体结论。** 评审不修改产品、不抬预算，不启动真实CLI。

## 1. 上轮问题已修

新形式协议已接readWritingPlan→selectedStructure→chapter-contract/drafts；模板不再定表列，图/列表/表格形式由写章填实，旧contractTwin与columns合并已退。H4按父节定位；template-adjustments正常入口与原审查消费已接。R2恢复原文按原摘要验证后写回，R3重复版本/话题/变化/遗留/选项与未决原话校验已补。

独立运行test_meeting_material与test_writing_plan：**64 passed、45 subtests passed，14.76秒**。不重复执行者全量。工作区干净，HEAD已提交1605003c；交回仍说未提交需修正为真实状态，不据此猜是否有另会话的提交授权。

## 2. 本轮需修的具体问题

### R4：Markdown分隔线被认成无序列表

`document.mjs::hasList`将`* * *`当列表项。独立复现：parseChapter含“### Actions”及一行`* * *`，hasList(view, scopeSpan(view,'Actions'),false)返回true。实际这是Markdown水平分隔线，不满足模型选定的无序列表。当前排除仅覆盖空白、横线和竖线，未覆盖星号式分隔。

在原hasList中按Markdown分隔线形态排除，不另建解析器；补水平线反例与真实星号列表对照，仍不规定列表条数。此项与预算清理同包返修。

### R5：旧“模板表带selected”的残分支仍在

新selectedStructure.tables只来自静态合同；模板表已经进入forms。静态合同不生产表的selected。chapter-contract.mjs仍保留：tableProblem的`slot.selected ? PICKED`；pickedStructureNames里`requiredTables(...).filter(t=>t.selected)`；missingPickedSeeds的`wanted(t)=t.selected || ...`及对应旧解释。

这些是旧模板表职责的残料，不是固定必要表能力。删除上述无生产者分支，保留固定表的anchors/tableProblem/tableSeed及forms命中同范围合同的接替。不要为了删旧模板机制把固定表一起删。预期只会省少量，不能声称找到几百行未退旧机制。

## 3. 用户质疑的预算：新增能力和优化必须分开

独立从Git快照按现有计数器复算：

| 时点 | 有效总行 | 相邻变化 |
|---|---:|---:|
| 1.9.1结束2211e4ea | 10867 | — |
| 1.9.2会议前7d31f623 | 11051 | +184（此前清理、成文、知识优化合计） |
| 会议首实现后2da90989 | 11443 | +392 |
| 当前1605003c | 12009 | +566（步骤6） |

本轮1.9.2相对1.9.1累计净增1142，不能只用步骤6基线把此前优化增长隐掉。

步骤6分类净增：Python +369、Story MJS +69、hooks +24、提示 +87、data/overlay +17。当前总12009超已签11550共459；MJS3509超3440共69；Python2501超2150共351——交回称“只有两条预算红”应以实际测试结构解释，不能省略第三个超额分类。

### 3.1 会议增长是必要职责，但也包含旧错误设计的替换成本

Python +369落在meeting.py +173、meetings.py +122、公共转换 +56、路由/入口等合计+18。它们承担原件留存、差异、原文引用、结构校验、未决派生、摘要和恢复，确有消费者。会议新增能力允许增长，但步骤6是在**已经加入会议功能后**修原设计，不能把这369全部再次称为从零新增会议功能。公共DOCX修复也服务已有导入，应单列其受益面，不重复记账。

原固定发言正则、语义transcript、S#、全文重抄、旧推断确已退出。不是一直留着旧解析器再包一个新解析器。最初把内容格式固定化、继而增加完整证据协议的成本，我作为方案制定者不能全归实施者。

### 3.2 纯优化不是只有新增，但净增长确实存在

Story核心：writing-plan **−10**，chapter-contract **+40**，document **+34**，drafts **+5**，合计**+69**。老列比较与合并退了；增加的是按父节定位、自由表格/列表存在性、形式提示等。用户要“两种心智”是产品目标；把所有形式都做成机器检查是我制定的实现选择，不能把这69全部称为用户新增功能，也不能仅因写进方案就免审。

提示侧：spec/plan作者页各−2，说明发生过替换；但story-write +36、meeting-read +36、plan-sections +7、恢复/回流等仍净增。hooks +24、overlay +17含会议与原能力共同接线，尚未按职责拆清，不能整项算纯优化或全算会议。净行数也不能代替实际新增/退出与结构成本账。

因此答案是：**有清除，不是只增不删；但清除量不足以证明实现已经最简。主增长不是大块旧系统残留，而是新增确定性保护、细化协议、重复解释与少量遗留共同造成。**

## 4. 本次应怎样收敛，而不是再打一层补丁

一次返修完成以下范围，不按每行停等，不要求保证净减到某个数字：

1. 退出R5的旧模板表分支并修R4；保持原有效合同。
2. 统一已有定位职责：document中tablesIn仍走旧H3过滤，scopeSpan/scopeLines/hasTable走新的区间读法；chapter-contract的subHeadingsUnder又扫描H4。优先让区间定位成为共享真源，原公共接口可以保留薄封装；同一view内不要每次检查反复split全文、构造围栏掩码。保留H3含子节、H4按父、空范围/null语义，不新增缓存文件或通用解析框架。若实测替换成本大于收益，交回具体调用证据，不为抽象而改。
3. 精简原提示责任位置：story-write第二节保留设计目标与一次协议表，第三节保留成文动作与调整出口，草稿头只带当前局部任务；相同“不预写答案/按原文/保留关系/可调整”说明不要以不同段落反复重述。plan作者页给动作和必读材料，plan-sections给契约写法，overlay给独立审查判据。不同执行体所需的短提醒合理保留，不能以去重把作者必需信息藏到verifier侧。
4. 最终预算账分“会议新增/会议设计替换修正/既有成文优化/既有知识与变更优化/纯接线”，每项列实际新增、实际退出、净量、保留理由，合计回到12009或返修后实值。不同文件混合职责按实际改动段归属，不凭文件后缀归业务功能。原1.9.2累计账一起保留。

不删原件/未决/引用校验换绿；不加案例业务条件；不把正常固定合同当旧机制退出。清理完仍合理增长时可以申请保留，但届时依据是具体职责与已完成替换，而不是“都对上方案”。当前不直接签12009为新额度。

## 5. 方案责任与验证边界

415/355/净60的估计显著失准，尤其把优化阶段的结构扩展估作近乎零成本。我接受估计与实现设计责任；不能继续只在交回旁批注“超预算合理”，也不能机械要求实施者压到60。上述收敛要求是本次评审的局部替换决定，不新增业务范围，不要求重写步骤6或重做已通过链路。

真实模型是否形成两种心智、会议格式是否适应、四件是否更稳定仍未验证。新离线通过只能证明当前协议/接线。保留真实测试授权边界，收敛后统一交回修正与预算，不先扩功能。

## 后续执行入口

用户随后要求制定步骤7，当前由[步骤7总览](07-步骤7-职责收敛与旧实现替换总览.md)承接本评审未收口项及整体替换。不要先按本评审优化effect执行链再按步骤7删除；列表缺陷与旧selected分支一起进入步骤7，已通过证据保持。

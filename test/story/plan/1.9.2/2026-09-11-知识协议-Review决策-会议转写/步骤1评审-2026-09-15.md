# 步骤 1 评审：d4d4cb2b

结论：**静态通过，步骤 1 的机制实现关闭；行为验收（04 §5 判据 1、6）待用户按 TEST 启动两次实跑。** 交回的八处偏差全部采纳。需用户签一处：scripts_mjs 在途上限按实测改为 3435（建议签 3440 留返修余量）。

## 1. 独立复核

| 门 | 本评审运行 | 结果 |
|---|---|---|
| 离线全量 | 跑了 | 1039 passed / 323 subtests，1 failed = `test_mechanism_budget` scripts_mjs 3435 > 3400，与交回一致 |
| `check_failure_modes.py` | 跑了 | 64 条 FAIL 0 |
| `adapt-scan --check --target .` | 跑了 | 通过 |
| `framework/`、`knowledge/` | diff | 零 |
| 旧协议残留 | rg「章节安排 / 结构选择 / checkPick / readPicks / 写后核对」 | 机制层只剩 `RETIRED_PARTS` 那一行（用于报旧形态），其余零命中 |

**真实产物试跑**（1.9.1 car-key-sharing 产物复制到临时需求目录，跑完即删，不入库）：

1. 旧形态写作设计：`skeleton` 报「还是旧协议（章节安排/结构选择）——按骨架协议重写」，不解析 JSON。
2. 换成骨架形态、十章已写：`skeleton` 的 NEXT 变为「回看」，清单列出 4 条初筛疑点（含 24h/48h 冲突两处）、3 条未决、3 条已定、1 条骨架待核、4 张图——全部指回真实来源位置，与 1.9.1 实测缺口（L1、L4）一一对应。
3. 第五章置为待写、删草稿：`skeleton` 按骨架铺出草稿——`###` 小节标题、每节 `骨架：答/依据` 指引注释、时序图节的作图提示写明「首个声明 sequenceDiagram」。
4. 提交正文：骨架小节名与正文完全不同（「撤销与配额」）→ 拒绝并给两个出口；骨架名是正文标题的前缀（「管理分享」对「管理分享（配额与撤销）」）→ 通过。最后一章提交后输出里带回看清单。

第 4 条的前缀匹配来自既有 `matchSection` 规则，与合同必要小节同一口径；正文在骨架标题后加括注不算缺节，可接受，写进 04 §2.2 备注。

## 2. 偏差裁定

| 交回 §4 | 裁定 |
|---|---|
| 1 `decision_form` 不进合同，分隔符与标签同放 `review.mjs` | 采纳：合同里本无 `review_mode` 与标签，方案落点写错 |
| 2 `图：` 只认「图」与「时序图」 | 采纳：核对能力只有这一种具名类型，`flowchart` 写进方案示例是笔误；扩图种改 `DIAGRAM_SYNTAXES` 一处 |
| 3 作业书不放骨架示例 | 采纳：沿 19845fa0 裁定 |
| 4 只重铺仍是起点的草稿 | 采纳：与「动过的一个字节不改」一致，动过的给缺失起点 |
| 5 设计读不了不铺 | 采纳 |
| 6 `#####` 下的表图记在其 `####` | 采纳：核对作用域是 `###` 节 |
| 7 最后一次 `chapter` 也给清单 | 采纳：同一处 `nextSteps` |
| 8 R01 夹具迁到骨架协议 | 采纳 |

## 3. 预算

d79e8aca 10743 → 10883（+140）；04 估净增 +130，R ≈ 108%，100–125% 档，交回自查已列（无并存实现、无范围扩大、超出在 chapter-contract 的骨架核对与铺底）。scripts_mjs 3435 超签定的 3400，原因是步骤 0 在该类实减 67 而非按总量估的 100；本步该类净增 +125 未超其自身估算。**建议用户签 scripts_mjs 在途 3440**（总量 10950 不动，10883 在内）；完成目标不动。签后由实施者改 yaml 头注与数值。

## 4. 记录

- 04 §2.2 补一句：骨架标题按既有小节匹配规则认，正文标题可带后缀。
- 步骤 0 评审 §3 两条已处理：1.9.2 说明有「修复」节；yaml-lite 流式映射转步骤 2。
- 回看清单里「图」类逐张列，图多的 Story 清单会长；它只提示核声明与图的对应，不判内容，先观察实跑再定是否收敛。

## 5. 下一步

1. 用户签 scripts_mjs 在途额度；实施者同步 yaml。
2. 用户按 TEST 启动真实运行：同一 Case 跑两次，对照 `story-suite-20260914-190900`；起跑前备份该 suite 现场。评价按 04 §5 判据 1、6 与 TEST §10。
3. 步骤 2（知识协议）开工前，设计者按 01 §4.2 局部改 03 分册，并把 yaml-lite 流式映射核项写进去。

# 1.9.2 总览与三份分册重审（2026-09-15）

状态：维护设计者审视稿，待用户确认。本文只登记目标、遗留、残留清点与对 03/04/05 三份分册的重审结论，**不改三份分册正文**；§6 是重写提议，等用户单独确认后才动。基线：extension 1.9.1（2211e4ea，本需求 [步骤4评审](../../../spec/1.9.2/2026-09-14-模型成文与审查闭环优化/步骤4评审-2026-09-15.md) 判可交付）。

## 1. 1.9.2 目标（用户 2026-09-15）

| # | 目标 | 可检查的结果 |
|---|---|---|
| G1 | story + review 成文质量更高、更稳定 | 同一 Case 两次独立运行，关键决策集合与跨章关键条件一致；作者整稿能自行发现未决冒充已定、跨章矛盾，不依赖 verifier 首轮兜底；三轴建议分较 1.9.1（auto 82/72/82，car 84/66/74）上升 |
| G2 | 知识应用更可靠、准确 | spec 命中判断有可回查依据；plan 契约无借编号挂载；义务粒度从 plan 到 review 一致，落实证据来源与知识声明的执行体相符；换知识不改机制 |
| G3 | 支持会议转写 | 会议 docx 进入后报出对本需求的变化、闭环结论与遗留问题，每处指回原话；人一轮裁决；有效结果进入 design.md 输入 |
| G0（前置） | 先清理 1.9.1 暴露的旧机制残留与同类问题 | §3 清单逐条给出退出或保留裁定；退出项连同消费者、文档、测试一并退出；离线全量绿 |

## 2. 1.9.1 遗留合并

来源：步骤 4 评审 §3 B/C 组、真实运行观测 O1、历史残留清点 08 收尾「剩余」、本文 §3 复查。编号 L 供 1.9.2 各步引用。

| L | 遗留 | 来源 | 归入 |
|---|---|---|---|
| L1 | 作者整稿只修语言红线，从来源核覆盖、推演关系与未决的实质核对未发生；两 Case 首稿都把 open 决策写成已定，靠 verifier 首轮拦 | 步骤4评审 B1 | G1 · 步骤 1 |
| L2 | 写作设计仍是长文描述式的指导文档：`####` 小节骨架与 `syntax: sequenceDiagram` 两轮无模型使用，正式写章时不一定遵守。用户 2026-09-15 定方向：template 改成**可照着生成 Story 的骨架**——按章给小节、要回答的问题、拟用形式与结构，正文照骨架填，而不是先读一份说明再自由发挥；形态在步骤 1 定 | B2、用户 2026-09-15 | G1 · 步骤 1 |
| L3 | plan 契约借编号挂 must（配额→COMPAT-01、入口可见/等待点→UX-01，3 处）；作者指引已写不借挂仍借；overlay 判据未经真实 verifier 验证 | B3 | G2 · 步骤 2 |
| L4 | 初筛记下的疑点（auto「修改门限走哪个接口」）未延续到 decisions/正文 | B4 | G1 · 步骤 1 |
| L5 | 性能：上下文 517K/700K，机制文本读取 77/78 次，约 20 分钟学习框架判据；只做诊断 | B5 | 步骤 1 验证时记录 |
| L6 | `hooks/shared/yaml-lite.mjs` 把列表项字符串里的 `:`、`/` 当键值拆开，render 出 `[object Object]` | B6① | G0 |
| L7 | 空 H3 重复标题（auto §6.7/§6.8）章检查不报 | B6② | G0 |
| L8 | `lifecycle.cmd_story` 未收口时下一步计算失败遮蔽「还没收口」提示 | B6③、步骤 2 交回 | G0 |
| L9 | `AR/story-src/sources/ar/r<轮次>.md` 留存件：无读取、无发现价值；退出须同步撤本需求 00 §2/§4 保护项，并解决提取稿覆盖 `AR/design.md` 后上游原话无处可查 | O1、B7 | G0（需用户裁定，§3 R1） |
| L10 | `story-build init` 退场（31 处测试起手迁移） | 清点 08 收尾剩余 1 | G0 |
| L11 | `check` ⑦ 与 ⑫b 只核刚投影出的内容，有效窗口只剩作者单独跑 check | 清点 §7 小判断题 | G0 |
| L12 | Framework 侧：AC 同 ID 机械一致性、NFR 适用性未交付；闭环提交不检测脚本报告之后的产物变化 | C 组 | 不在 1.9.2，用户另裁 |
| L13 | 预算/规模：829 估算与 820 额度、两条规模上限未签；`mechanism-budget.yaml` 工作区改动仍写 600 | 步骤4评审 A1 | 1.9.1 收口，先于 1.9.2 |
| L14 | FEATURES 候选与 FEATURE-MAP 仍是分析稿，且准确性存疑（没有发现 r2.md 这类残留）；用户 2026-09-15 定：等 1.9.2 完成后再按当时实现重核并正式迁到 `test/story/` 维护，1.9.1 不做 | 04 §5、用户 2026-09-15 | 1.9.2 步骤 4 之后 |

## 3. 旧机制残留复查（2026-09-15）

方法：以 1.9.1 当前设计（本需求 00–04、清点 08 收尾、功能全集 FEATURES）为准，在 `doc/extensions` 机制层查「代码/合同/提示仍保留、但当前设计没有或互相否定」的项。清点由子任务逐条 grep 全仓取证，设计者复核并给裁定。**这一批的共同形态与 r2.md 相同：不是死代码，是有生产者没有读者、或两处声明互相否定、或迁移桥过了退出条件。** 历次退场核对按文件名与函数名列清单查不出它们，1.9.2 步骤 0 要按「谁生产、谁读、谁在文档承诺」三向核。

| R | 候选 | 位置 | 事实 | 裁定 |
|---|---|---|---|---|
| R1 | 上游原件留存 `sources/ar/r<n>.md` 及其五个消费者 | `flow/submission.py:165,240`；`author.mjs` 4a；`reader-review-task.mjs:111`；`sources.mjs` 清单必列；`flow/check.mjs::originalArSource`；`state.py::AR_SOURCES` | 见 L9 | **退出**（需用户确认）。同一步解决：提取稿不再覆盖 `AR/design.md`，上游原件保持原位、提取稿另名；1.9.1 发布说明 §3 与功能清单「上游原件留存」在 1.9.2 说明中登记退出 |
| R2 | 评审回流「旧落点」迁移指令 | `rules/review_reflow.md:51` | 步骤 3 §6.1 写明「不保留长期双读/双写支路」，退出条件是本需求交付 | **退出** |
| R3 | 合同 `verdicts.question_words` / `chapter_words` | `contracts/story-chapters.json:269,273` | 零读者；同对象 `_note` 自陈不设逐问逐章表；清点 C1 已列、B5.4 未退 | **退出** |
| R4 | 来源清单两份声明 | `story-chapters.json:32-37`（sources）与 `materials/registry.py:41`（SOURCE_DOCS） | 同一清单 JSON 与 Python 各一份 | **合并为一处真源**（Python 读合同或合同派生），单一真源 |
| R5 | 来源 `UX = ux-reference/README.md` | 合同 `:37`；`sources_note:5` 写「README 不是登记也不是来源」；`registry.SOURCE_DIRS` 已把该目录逐文件登记 | 两处互相否定；运行日志每次报「UX 不存在」 | **退出合同 `sources.UX`**，UX 类文档正文落点在步骤 0 定案（清点 B20 未闭合） |
| R6 | `--by` / `ACTORS` 单值枚举 | `story_flow.py:85-86`、`state.py:63` | 注释自述「取值只剩 human」 | **退出参数**，契约栏固定写 human |
| R7 | 契约字段只写不读：`rounds[].analysis`、`story_written_at`、`archived.story/review` 指纹、`feature` | `rounds.py:39,55,110`；`lifecycle.py:135,189-190` | 零判据读取；`imported` 有读者（rounds.py:48）不在此列 | `analysis`、`feature` **退出**；`story_written_at` 保留为留痕并写进 status 输出或退出，二选一；`archived` 指纹要么在 restore 时比对、要么退出——步骤 0 定 |
| R8 | `material_dirs` 白名单分支 | 合同 `:347`；`sources.mjs:342` 仅 `haveManifest===false` 时生效 | 正常流程每轮重算 materials.json，分支永不生效 | **退出**，无清单按错误报 |
| R9 | `story-build review-task` 子命令 | `story-build.mjs:53,254` | 自述人工入口，无文档；清点 C20 | 文档化或退出，步骤 0 定；倾向退出（审查输入已由 pre_verifier 直调） |
| R10 | `hooks/testing/author.md` 未登记进 testing overlay 的 `phase_input_snippets_extra` | `rules/testing-rules.overlay.yaml` | 六份原则页里唯一没有强制项兜住 | **漏配，补登记** |
| R11 | `--unused/--used` 与 `.captions.json` 未在规则页出现 | `import_sources.py:50-51`；`registry.py:51` | 作者只能从报错文案发现 | **补进 `rules/inbox_import.md`**（正向表达） |
| R12 | `--offline` 分支 | `story-build.mjs:63`；`sources.mjs`、`context.mjs`、`check.mjs` 多处 | 只有测试进入 | **保留**：它是金样单向否决锚与孤立文档检查的现役设计（本需求 01 §4 明写离线 check 不假造 template）；在 `scripts/README.md` 登记用途 |
| R13 | `review-disposition.json` 无骨架无校验 | `rules/review_reflow.md`、`SKILL.md:213` | 纯人工台账，代码零消费 | **保留**；是否给形状校验归步骤 1（G1 稳定性）判 |
| R14 | 过期注释 | `sources.mjs:188` | 描述已迁走的实现 | **删注释** |
| R15 | `provides.bridges` 扩展私有键 | `manifest.yaml:14-18` | 只被 adapt 链读，framework 不认 | 保留，是 adapt 的既定约定 |
| R16 | yaml-lite 解析缺陷、空 H3 重复标题、cmd_story 遮蔽 | L6–L8 | 缺陷不是残留 | 并入步骤 0 修 |
| R17 | `story-build init` 退场；`check` ⑦/⑫b 窗口 | L10、L11 | 清点已定未做 | 并入步骤 0 |

步骤 0 的验收：每条 R 有「退出/保留」裁定与实际 diff；退出项的消费者、文档、测试同次退出；`check_failure_modes` FAIL 0、离线全量绿、`framework/` 零改动；新增的两份文档登记（R11、R12）不带维护历史。

## 4. 三份分册重审

三份分册写于 2026-09-08，基线是 2.3 之后、回退之前；00-来源说明已预告「先完成退场那一轮，再重排落点」。此后经过了旧机制退场（B 段/C 段）、1.9.1 四步（写作设计、整稿、Review 两类交互、裁决边界、处置台账）与两轮真实运行。逐份核当前实现与目标。

### 4.1 04 · Review 决策发现与表达

需求真源 R1–R9 已冻结、仍有效；1.9.1 真实运行正是它要解决的现象（同一冲突两轮表现不同、首稿未决写成已定、初筛疑点丢失）。与当前实现的偏差：

| 04 写的 | 当前实现（1.9.1） | 结论 |
|---|---|---|
| 三段式标签「要定的事 / 根据」，且「不加『依据』标题」（§3） | 用户 2026-09-14 定为「决策点 / 依据」，`review_mode` 分 choice/confirm，两类正文形状已在 `story-write.md` 决策登记节与 `review.mjs` 落地 | 04 §3 这一段与已交付事实冲突，须改写 |
| 「可选的做法」每项「做法——后果」，分隔符与标签登记进合同 `decision_form`，check ⑤ 判分隔符后非空 | 有序列表 + `optionsSqueezed` 挤段检查已落地；无分隔符、无 `decision_form`；方法文字要求「写清做什么、影响什么、前提」 | R6「每个选项各带后果」的语义已由方法承担；是否再加机械形状归步骤 1 判，不默认加 |
| 「回看」一环：统稿之后、登记之前，遍历 story 十章 + spec，两问撞法，四种「不合适」，四个出口（§2） | 步骤 2 把整稿改为「从来源核实际全文」三项工作，位置相同（写后核对 → 登记）；SKILL 链条已有「写后核对」；但真实运行显示作者只做形式自查（L1） | **04 的撞法层就是 L1 缺的可执行动作**：把「核覆盖/推演关系」拆成逐处「这句话材料里有吗 / 材料靠得住吗」。两者合并，不并存 |
| 十一类表由遍历顺序改为撞法维度 | 表仍是「找议题时对着这十一类过一遍」（`story-write.md:195`） | 未做，沿 04 |
| 读者审查加一问（`reader-review-task.mjs`） | 审查方法真源已归 `spec-rules.overlay.yaml::story_reader_review`，任务书只给对象 | 落点改到 overlay |
| 验收 1：同一 Case 跑两次比撞点集合 | 未做过 | 保留，是 G1「更稳定」的唯一直接验收 |
| 引用 `story-write.md:175-176`、`story_flow.py:1290`、`review-render.mjs` | 行号与文件已变（`review-render.mjs` 已并入 `review.mjs`） | 重排 |

结论：需求真源不变；§2 回看与 L1 合并、§3 表达按已交付形态改写、§4 判据落点改到 overlay、§5 验收 1 保留并加入 1.9.1 两 Case 作为对照基线。**这一份要重写落点与表达两节，不是局部改。**

### 4.2 03 · 知识与机制的协议

需求真源（内网 2026-09-08 反馈 + F4/F5/N3/N4）仍有效；当前实现一处未动：`knowledge.mjs:177` 仍写「强制力/命中条件/验证列由模型直接读正文，机制不派生」；`obligations.mjs:19` 的 `verify` 仍含 `probe`；`coding/author.md:28` 仍要「每条义务都要有个说法」。与目标 G2 的关系：

- 03 解决的是 plan → coding → review → ut 的落实与传递粒度；1.9.1 暴露的 L3（借挂）发生在 plan 作者写 must 那一步，03 的 P3/P4 不直接拦它。步骤 3 §5 给 verifier 的 `knowledge_obligation_substance` 判据要与 03 的粒度协议并进同一份方案：作者侧「业务规则写在契约的哪处业务表达」要有落点，不只是禁令。
- 03 §2「现状」表按 2.3 之后基线写；`knowledge-use.yaml` 现行 `facts[].used_for` 仍是文件级（car 产物可证），§4.1 按面登记仍成立。
- 本仓至今没有跑到 coding，03 的 P4/P5 只有离线验证可能；判据 16（内网实跑）不变。
- 预算 +180 未变，但规模上限已到顶（L13），须先签。

结论：需求真源与协议结构不变；§2 现状、§5 各落点行号、§9 顺序要按 1.9.1 代码重排；把 L3 的作者侧落点与 verifier 判据并入 §5 P3。**局部改写，不重写协议本身。**

### 4.3 05 · 会议转写

需求真源 D1–D11 已冻结、仍有效；依赖的步骤 1（`story-src/` 保留集）与步骤 2（`scripts/core/`）都已在 1.9.1。与当前实现的偏差：

| 05 写的 | 当前实现 | 结论 |
|---|---|---|
| `import_sources.py` 加 `MEETING` 档；`materials.py` | 分类现为 RR/SR/AR/UX/IMAGES 五档，材料登记在 `core/materials/registry.py` 与 `importer.py` | 落点重排 |
| 有效结果进入 `rules/ar_design_init.md` 输入表、`init_analysis.md` 材料盘点、`story-write.md` 撞法表 | 输入表仍列 `upstream.md`（R4/R5 待清理）；初析已有 ⑥ 来源初筛；写作设计 `story-template.md` 是 1.9.1 新增的解释安排层；撞法表不存在（见 4.1） | 会议有效结果要同时进初筛 ⑥ 与写作设计的依据来源，这是 05 写作时不存在的两个消费者 |
| `decide --gate meeting --meeting --item` | `GATES` 是封闭集合（`state.py`），`--by` 待退出（R6） | 落点成立，需扩 GATES；与 R6 同步 |
| 读者审查输入加 `evidence.json`、`meeting-notes.json` | 审查输入在 `reader-review-task.mjs` 逐份给位置 | 落点成立 |
| 关卡摆「值得问的」逐条带推荐与可选结果 | 1.9.1 已有 choice/confirm 两类呈现与人工区保护，但那是评审记录的形态，材料关卡是另一处 | 是否复用同一呈现方式归实施设计，不改 D7「并入材料关卡」 |
| 预算 +440 | 规模上限 10800 已超（10867） | 先签额度 |

结论：需求真源与链条结构不变；§2/§6 的落点按 1.9.1 目录与新增消费者（初筛 ⑥、写作设计）重排，与 R1/R4/R5 清理后的来源清单对齐。**局部改写落点章，不重写链条。**

## 5. 1.9.2 步骤划分建议

| 步 | 内容 | 输入 | 验收 |
|---|---|---|---|
| 0 | 清理：§3 R1–R17 逐条裁定与执行；L13/L14 在此之前作为 1.9.1 收口完成 | 本文 §3、清点 08 收尾、本需求 00 §2/§4（R1 需改保护项） | §3 末段 |
| 1 | 成文与 Review 质量稳定（G1）：写作设计从指导文档改为可照着生成的骨架（L2）；04 回看/撞法与 L1 合并为作者可执行的逐处动作；L4 初筛疑点延续；十一类表改撞法维度；同一 Case 跑两次的稳定性验收；1.9.1 两 Case 为对照 | 04 分册（重写后）、步骤4评审 B1/B2/B4、TEST §10 | 正文按骨架逐节可对上；两次运行撞点集合一致；首稿无「未决写成已定」；三轴上升 |
| 2 | 知识协议（G2）：03 协议 + L3 作者落点与 verifier 判据；用 1.9.1 car 的 `contracts.yaml` 先做一次 plan verifier 真实运行看现有判据拦不拦 | 03 分册（局部改）、步骤4评审 B3 | 03 §7 判据 6/10/11/12；car 三处借挂被拦 |
| 3 | 会议转写（G3）：05 按重排落点实施 | 05 分册（局部改） | 05 §10 四件 + 用户真实会议 docx |
| 4 | 真实验证与交付：按 TEST 启动；发布 1.9.2 说明与指南差异 | — | 三轴评分经用户确认 |

步骤 1 与 2 可并行设计，实施按额度顺序。每步一份当前交回，设计与实施分会话。

## 6. 重写提议（用户 2026-09-15 已确认；步骤 0 方案见 [02](02-步骤0-旧机制清理方案.md)）

**原因**：04 的表达章与回看章已被 1.9.1 的交付事实（决策点/依据标签、review_mode、有序列表检查、写后核对三项工作）部分替代，且它的撞法层正是 1.9.1 实测缺口 L1 要的动作；继续在原文上打补丁会留下两套说法（04 §3「不加依据标题」对已交付的「依据」）。03、05 只是落点与现状过期，协议与链条不变。

**涉及文件与章节**：
- `04-Review决策发现与表达.md`：§1 现状、§2 回看与撞法（与 L1 合并，落到 `story-write.md` 四「写后核对」）、§3 表达（按 choice/confirm 已交付形态）、§4 判据落点（overlay）、§5 验收加对照基线、§7 预算重估——**重写**。
- `03-知识与机制的协议.md`：§2 现状表、§5 P3 并入 L3 作者落点与 verifier 判据、§8/§9 行号与顺序——**局部改**。
- `05-会议转写.md`：§2 产物落点、§6 消费者（加初筛 ⑥ 与写作设计）、§9 预算——**局部改**。
- `00-来源说明.md`：状态与前置改为指向本文——局部改。

**保持不变**：三份的需求真源引用（R1–R9、D1–D11、内网四条反馈）、协议总则（03 §3/§4）、会议链条结构（05 §1/§4/§5 的派生规则）、04 的准入判据与「不加字段、不加停等点、不上语义判据」三条不做。

确认后按 §5 顺序先出步骤 0 的清理方案，再重写 04。

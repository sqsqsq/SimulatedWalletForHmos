# 已核事实与实施依据

核查日：2026-09-13，本次按执行者反馈重新核对。条件读取，设计文档只维护本地当前稿，不归档、不提交。

| 对象 | 实际事实 | 实施决定 |
|---|---|---|
| 用户级仓库 | Git 根 `C:/Users/kangz`，HEAD `7274ff858a6672f1d0f9707d4bf80afa818061c9` | 三个用户入口属于同一仓，操作限定本步路径 |
| 用户入口 | 三个既有落点均被 Git 跟踪；最新复查 `.agents/AGENTS.md` 有旧 S1 的未提交修改 | 规范正文为 AGENTS.md/CLAUDE.md，按 S1 重整并同步既有落点 |
| 旧独立文件 | 本次 Test-Path 核实 `.agents/maintenance.md` 已不存在 | 不再读取或安排删除；S1 只作 R1/R2 局部修正和三处一致性检查 |
| AIDefectHelpler | HEAD `042808126884bf1d6fd465205e92c6d8ec316dd3`，已登记 ArkAnalyzer 事实层/第三方引擎及 common 测试入口 | S2 保护新事实并核 common；不沿旧 HEAD 处理 |
| AIWalletKit | Git 根同项目路径，HEAD `0fd3301d9afe8892ac87b8400a06a6721464be39`；README 复制“十一个文件” | 资产精确集合仅留原清单 |
| Story | HEAD `efe6f9f52a1670fd621fd3904f8e9f55450c6b27`；AGENTS +31/−16 位于 §7.5，TEST +17/−4 涉及并行测试和预算判读 | 开工核两份完整 diff，基于当前工作区整理，无需先提交 |
| 过程目录 | Defect docs/specs/README；Wallet task/README；Story test/story/plan | 维护方案沿各自命名约定保存 |

用户级 Git 核查因沙箱不能进入用户根目录，已通过仅针对所需路径的只读权限核实。该事实不代表安装或提交授权；实施时沿宿主权限办理。

Story 当前未提交为 test/story/AGENTS.md、TEST.md、regression/mechanism-budget.yaml、tests/test_mechanism_budget.py 四个文件。S4 只在当前内容上改具名说明，不覆盖这些已有修改。

Defect 当前另有 .gitignore、demo/sdk/Framework/BuildProfile.ets、20260903/20260905 设计记录的修改，以及未跟踪 .claude/settings.local.json、product/common/arkts/node；均非本轮清理对象。common/AGENTS 和 TEST 已读，没有通用角色段待上移。

Claude 现有用户 Skill `implementation-loop` 只有一个 306 字节的跳板 SKILL.md：frontmatter 同主体，正文一行链接 `../../../.agents/skills/implementation-loop/SKILL.md`。本计划沿用该方式，不复制主体、不引入 junction。用户仓 `.gitignore` 首行为 `*`，`.claude/skills/` 未放行；临时仓模拟确认，追加 `!.claude/skills/`、`!.claude/skills/maintenance-agent/` 及具名文件放行行后，该目录文件可跟踪，implementation-loop 仍被忽略。
用户级/项目级文件和维护诊断工具的部署效果按 S6 验证。工具源码放 .agents/skills，Codex 当前已能发现该目录的其他 Skill；Claude 的独立发现入口按 S5 安装。新工具的实际发现与调用仍待实施验证。


本次接续审视重新核了 Defect 的 PROJECT §5 和 STATUS：当前方案为 `docs/specs/project/20260912-app-log-locating-agent/`，进行中的事实层重做采用 ArkAnalyzer。PROJECT 已包含回退和词法退役条件；它们不是因整理规约就可以删除的历史噪声。S2 的动作已改为保留有效边界、对已有过程记录给链接。

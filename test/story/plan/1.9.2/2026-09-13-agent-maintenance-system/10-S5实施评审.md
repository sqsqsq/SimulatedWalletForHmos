# S5 实施评审

用户级三项补充通过静态评审；maintenance-agent 的 S5-R1/R2 修正已复核通过，当前未发现新的阻塞问题。两宿主新会话发现与实际诊断尚未验证，S5 尚未整体完成。下列问题描述保留为本次修正的依据。

## 已核对通过

- 用户级交付规模、完整退场、实施与评审适用性均合入原条款，含其他合法输入与异常路径核实，未把有效协议常量或仍有效的回归测试作为删除对象。三入口内容标识均为 `c1ccf05119c08f0d41af216d0b62a2461a2e1cea`，限定路径格式检查通过。
- Skill 主体的六项诊断覆盖目标、职责、材料、实现、验证与整体修复，未引入增量记录、脚本或自动修复授权。
- `agents/openai.yaml` 与既定配置一致；Claude 跳板元信息一致，相对链接可读；用户 `.gitignore` 仅增加三条既定放行规则，限定状态检查只出现预期的新文件。这些是静态证据，不代表宿主已经发现并运行 Skill。

## S5-R1：当前工作区清单不完整（P1）

位置：`C:/Users/kangz/.agents/skills/maintenance-agent/SKILL.md:49`。原句用 `git ls-files` 取得“完整清单”，实际默认仅列已跟踪文件，可能遗漏正在交付的新能力。

本次在 Defect 实测：对 `product/common/arkts/node` 执行 `git ls-files` 返回空；执行 `git ls-files --others --exclude-standard` 则返回 `package.json` 和 `package-lock.json`。因此当前工作区存在交付材料，而该取证方式看不到。

修正：按 04“取得本次诊断依据”读取当前工作区清单，包含范围内已跟踪、未跟踪及本次相关的被忽略材料；依项目约定排除缓存和生成物。可使用现有文件工具及 Git，不新增脚本、登记表或增量状态。此缺陷也来自原方案，04 已同步修正。

验收：用上述真实目录核对新取证步骤能够发现两个文件；再核本次相关的被忽略方案不会仅因未入 Git 就被排除。无需读取全部依赖或重跑三项目测试。

## S5-R2：非 Git 分支与完成条件冲突（P2）

位置：同一 SKILL.md 第 38、44、66 行。第 38 行允许不在 Git 仓时继续；第 44 行却要求两级 Git 根和 HEAD 均确定，第 66 行又要求两个仓的 HEAD。该分支无法满足自身完成条件；命令权限或信任错误也不能证明没有仓库。

修正：对象和两级材料位置依据实际工作区确定；明确非 Git 来源的版本信息记为“不适用”，获取失败记为“未取得”并写原因。同步改对象识别、完成条件与计划开头三个位置，记录可得事实，不猜测版本。04 已同步这一语义。

验收：逐项推演正常 Git、明确非 Git、Git 信息获取失败三种情况，均能如实记录事实或缺口；后两者不会被要求补造 HEAD，获取失败不会被误判为没有仓库。若缺口影响具体结论，应将该结论列为待核。

## 后续安排

已通读当前 Skill 正文并对照两项修正：R1 合并已跟踪和未跟踪清单，并由文件工具补读相关被忽略材料；本次独立核对 Defect 目录，未跟踪清单确实包含两个 package 文件，本地方案目录存在。Git 提示全局 ignore 文件无读取权限，因此此检查只用于核实这两个文件可被发现，不声称全局忽略规则已完整验证。

R2 的对象识别、完成条件和计划输出已一致区分可得版本、不适用和未取得；交付时的 Git 核对也限定在 Git 仓内。正常 Git、明确非 Git、获取失败三种情况的文字推演均无原先的矛盾；执行者记录的命令模拟与该语义一致，本次未重复模拟。

用户条款与部署配置无需重做。下一步完成两宿主新会话发现与调用，再按 S6 具名任务验证诊断和计划质量；不能据静态通过声称已避免补丁式实施或过拟合。本次只更新评审与状态，未修改用户级或 Skill 文件。

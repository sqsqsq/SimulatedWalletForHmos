# S1：用户级规范局部修正与核对

## 交付与输入

用户级规范只有 AGENTS.md 与 CLAUDE.md 两份正文，维护要求直接融入其中，不另建维护手册或维护指针。它们指导模型正常工作，项目材料按任务读取；maintenance-agent 是人工调用的独立诊断工具，用户和项目规约不引用该 Skill。

本机 AGENTS.md 已有两个落点：编辑源 `C:/Users/kangz/.agents/AGENTS.md`、Codex 副本 `C:/Users/kangz/.codex/AGENTS.md`；CLAUDE.md 位于 `C:/Users/kangz/.claude/CLAUDE.md`。三个既有路径保持同内容，按宿主规则加载；不得假设所有部署环境的消费模型都会读取它们。Git 根为 `C:/Users/kangz`。

必读本步、07 的 R1/R2 及三个当前入口。四节正文已完成，旧独立文件已删除；当前只修正 R1/R2，不重读不存在的文件、不重做整体重写。其他已核对的内容保持。

## 正文组织与改动

下面是现有正文应保持的组织及本次局部落点，不是重新组织全文的动作。

| 正文章节 | 承载内容 | 本轮动作 |
|---|---|---|
| 角色与工作对象 | 维护设计者、维护实施者、消费模型职责 | 仅按 R2 明确适用对象及边界 |
| 设计与改动 | 目标、事实、根因、完整改动、复用、同步与退出 | 保留当前已合格内容 |
| 方案与文档 | 计划输入、动作、依赖、验收及目录/重写约定 | 保留当前已合格内容 |
| 表达与验证 | 表达、来源、实际证据及未验证项 | 仅按 R1 修正“项目只写实际差异”句 |

角色段以“维护 Agent 产品时”为适用条件：维护设计者只做需求分析、方案和实施评审，不代维护实施者改产品；维护实施者负责实现、验证和同步；消费模型使用被维护产品完成业务，其业务阶段角色不改变消费身份。通用工作规则仍适用其他任务，用户指定角色优先。

设计和计划应说明各项调整如何共同实现目标，反馈修正对应决定及受影响内容。交付需要的背景、动作和验收在当前材料中可直接取得，不要求接手者拼历史或自行决定关键设计。以上写成紧凑的正常工作要求，不复制本计划的解释段或检查表。

过程文件规则沿用当前有效决定：spec、plan、评审、诊断等过程稿只保留本地当前稿，不归档、不提交；有用的状态与结果按用户要求处理，已有历史不追溯清理。现行规约仍按正常 Git 变更管理。

## 迁移落点与静态核对

下表只留在实施计划，不写入用户规范。归属依据是适用对象、读者及加载时机；“看起来通用”不构成从项目删除的依据。用户级表达与验证段采用：“事实、规则和决定在明确的责任来源维护；各入口按其读者和使用时机交付必要内容。跨文件摘要或提醒标明来源，修改时同步，确保相关读者能取得完整要求。”

| 项 | 用户级承载 | 项目级处置 |
|---|---|---|
| U1 维护对象 | 维护者理解 Agent 产品的共同视角 | Wallet/Story §2 产品定位与行为链保留，不整体上移 |
| U2 角色责任 | 维护设计者、维护实施者的通用工作责任 | 各项目维护对象、消费身份、实际输入、隔离及运行责任保留；只做正确称谓和同文件局部合并 |
| U3 设计交付 | 维护方案怎样交给实施者 | Defect PROJECT §7–§8、Wallet/Story §7 的项目流程、裁决、预算和阶段义务保留 |
| U4 信息与证据 | 维护者行动前取证、按证据下结论 | Wallet §5、Story §4 的产品加载链和模型/脚本/verifier 分工保留，不用用户工作习惯替代 |
| U5 同步与退出 | 维护改动如何照顾直接影响 | 项目迁移桥、失效功能/契约/fixture/测试的退出对象、退出时点和自检完整保留 |
| U6 依据与表达 | 日常表达和来源管理 | README/STATUS 的能力表、进度、视图和入口提醒保留；只在完整承接后局部去重 |

本次审查的用户级 R1/R2 为局部修正，见 07；其余已合格内容保留。不得再按整节“上移”重做用户入口或删除项目正文。

## 实施顺序

1. 读取三个入口的当前内容与 Git diff，确认 R1/R2 是否已修，避免重复覆盖新修改。
2. 在编辑源的“表达与验证”修 R1，在“角色与工作对象”修 R2；其余段落保持。
3. 同步到 `.codex/AGENTS.md` 与 `.claude/CLAUDE.md`。
4. 用 `git -C C:/Users/kangz hash-object -- .agents/AGENTS.md .codex/AGENTS.md .claude/CLAUDE.md` 核三处一致，并对三个路径执行 `git diff --check`。
5. 相对开工内容检查本次仅修改 R1/R2；旧独立文件已退出是已完成事实，不再安排读取或删除动作。

## 完成条件

设计者核对 R1 的读者/适用对象边界、R2 的维护角色及消费身份，确认三处同内容、格式检查通过、其他已合格段落未删改。任何缺项只返回相应局部动作。

S1 只验静态交付，新会话效果由 S6 验证。S2–S4 按修正后的项目保留/恢复表执行，不再承担整段通则上移。结果写 STATUS；规范交付物按授权提交，本计划不提交。

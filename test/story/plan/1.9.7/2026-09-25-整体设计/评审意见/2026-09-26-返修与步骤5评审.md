# 返修与步骤 5 评审

2026-09-26，维护设计者。对象：4ee75bde（四步实施评审的返修）、ee091b29（5a）、463e501a（5b）、08eefb4c（5c）、8ad5fdbf（金样候选样稿，用户已确认）与四份交回。方法同上一轮：我独立复跑全量与检查器、实测规模、核专名与文档同步；三路独立评审逐条对分册与上一轮评审读 diff、跑定向用例、用探针复现；关键点我抽源码复核。未改产品。

## 1. 结论

**上一轮七条阻断全部关闭，返修没有引入兼容分支或旧格式识别；步骤 5 按 08 落地。再补一小批（§3 的 7 条需改）就可以进实跑，起跑等用户授权。** 这批都是一两处的边界修正，不牵动接口与资产。

| 项 | 结果 |
|---|---|
| 全量离线（独立复跑） | 1445 passed、15 skipped、552 subtests |
| 失效形态 | 67 条：FAIL 0、PASS 59、委派 8 |
| 机制层 | 业务专名与「内网」零命中；hooks、skills、rules 无读旧格式、旧标签、旧字段、旧面名的分支 |
| 机制规模 | 13358（prompts 1883 / hooks 4140 / data 764 / py 3104 / mjs 3467）；峰值 13460 内，总 target 13145 发布时按实测重签 |

## 2. 上一轮问题的关闭情况

A1–A12、B1–B9、C1–C10、D1–D8 全部核实。逐条结论见评审员报告，要点：

- 旧格式人工区不再静默丢弃：找不到当前形态的起点就按手改停，文件不动，不认旧标签。旧 schema 只报「不是当前版本的流程契约：从 init 重新起单」，无迁移或重记路径。
- 原话映射只认编号说法、标签整句或标签里的一句话、「按推荐」，键不参与；四句反例全部不命中。
- 报告核对：ledger 只登合规报告；被拒回复不再移动；缺判据后重投完整回复不再报。
- 上游编号行内出现即承接；金样与 real-run 在测试里各跑一次 spec 门禁。
- 加载器四项 frontmatter 必填、`factsByName` 一处解析；AC05 真的跑了门禁与审查；protocol 模式一节不列节名。
- real-run 夹具决策件按现行规则重写，`decisionProblems` 零问题；review 金样按结构比对；规划相符含 story；`measure_run.py` 按段产出。
- D6 检索零命中的根因查明并复现：用户目录本身是 git 仓且 `.gitignore` 为 `*`，临时工作区落在它的忽略范围；工作区根写 `.ignore`（`!*`）后恢复。

两条只算部分关闭，进 §3：A11 与 B5。

## 3. 本轮需改

| # | 级别 | 问题 | 证据 | 改法 |
|---|---|---|---|---|
| E1 | 需改 | `fetchedAt` 写成不带时区的 ISO 时刻时，与 UTC-aware 的成文时刻比较抛 `TypeError`，不是 FlowError；自家夹具就写的是 naive 时刻，只因无 stamp 才没炸 | `flow/update.py:289-295`；`test_story_update.py:321` | 解析后 `tzinfo is None` 也抛同一句 FlowError（合同写「带时区的 ISO 8601」）；夹具改带 `Z`；演进记录与替身说明补「带时区」 |
| E2 | 需改 | §8 功能编号改用 `functionIdRe()` 直接扫整行，不再先剔验收编号；`AC-F1` 会被同时切出功能码 `F1` | `hooks/spec/post_check.mjs:185` | 先 `replace(acceptanceIdRe(), ' ')` 再匹配功能编号；补 `AC-F1 (F1)` 对齐不报的测试 |
| E3 | 需改 | OBS-02 新句「统计点位的定位信息随统计事件的描述带，不另记定位事件」与同一行探针 `present_in_method:WalletHAManager\.(log\w*AndReport\|vocBuilder)` 矛盾：按新规则统计点方法只走 `chartBuilder(...).setWalletEventDesc(...)` 与 `Logger`，探针必然认不出，coding 门禁对每个挂 OBS-02 的统计点方法误报 | `knowledge/constraints/observability.md:17`；`hooks/shared/probes.mjs:150-163` | 探针同时认统计事件带描述与本地日志的形态：`present_in_method:(WalletHAManager\.(log\w*AndReport\|vocBuilder)\|setWalletEventDesc\|Logger\.(info\|error\|debug))`；回归 OBS-02 相关 hooks 测试 |
| E4 | 需改 | `test_the_lower_half_names_the_required_design_items` 断言 Demo 知识下篇示例表表头含「事件 ID / 内码 / 结果分类 / 描述」，属 U5 已决定退出的 Demo 内容断言，5b 反而扩充了它 | `test_knowledge_forms.py:264-269` | 删除；形态由四格测试与中性 fixture 守。若要守「下篇有逐结果表」，在中性 halves fixture 上核「下篇解析出表」，不写列名 |
| E5 | 需改 | 演进记录 1.9.7 节漏一条 `[知识]`：机制只固定 spec 点位表的「统计点」列、plan 埋点表的「统计点」「责任方法」列，其余列由目标上报知识上篇 / 下篇给出。1.9.3 的目标按现记录新建上报知识时不知道要写列 | `story-adaptation/reference/upgrade-changes.md:37-40`；`hooks/spec/author.mjs:380`；`stat-points.mjs:5-6` | 1.9.7 节加一条 `[知识]`：上报知识上篇写 spec 统计点表的列、下篇写 plan 埋点表的列与每列取值来源；验证是按知识写出两张表、spec 与 plan 门禁通过 |
| E6 | 需改 | 知识 §7 第 1 条仍写「§5 的三项写出实际的号」，§5 已是四项 | `knowledge/facts/event-tracking.md:91` 对 `:62、:105` | 改「§5 的事件 ID、内码、结果分类写出实际的号，描述写出固定说法与关键值来源」 |
| E7 | 需改 | 候选样稿 plan 9.2.2 有 16 行描述只有关键值没写来源（「拼异常类型」「拼这是第几次输入」…），与知识 §5、§8 自己的完成判断不一致；示范稿要满足自己的判据 | `golden/bank-card-opening-reporting-candidate/plan.md:31-71` | 每行补来源短语；`plan.md:67`「开卡状态」同一值进两个字段，二者留一。改后请用户再确认一次 |

观察（记录，实施时顺手做）：

- ownership 测试没锁「`adapted_for` 为上一版时不列更早条目」（`test_adapt_ownership.py:260-267`），加一条反向断言。
- S16、S18 四份失效形态夹具的议题编号仍是 `DEC-001`，改 `D1`。
- `pre_verifier` 的「上一份报告」取 ledger 最后一个键，PASS 后重跑会指向当前报告自身；取最后一个不等于当前 subject 的键。
- Demo 知识 `dfx-baseline.md` 占位写「以目标仓性能基线为准」，「目标仓」是 adapt 的词；改「以本仓性能基线为准」，real-run 摘要随之重算。
- coding 作者页「拿什么」列用同义词（常见错法、SDK 的行为事实）对不上知识节名；改成「下篇全文（节名以知识为准）」。
- `changeEntries()` 只认行首 `- `，缩进子项静默跳过；文件头写明「每条一行、不用子项」或正则放宽并对空正文单独报。
- 交回 D6 一句「工作区自己的 .gitignore 在非 git 目录下不生效」与探针不符，措辞改「工作区落在上层 git 仓的忽略范围内」；5a 交回 §4 第二条应改为「金样知识快照是锚，按 08 §5 不随 Demo 知识改动」。
- 08 §6「金样经生产入口的 spec、plan 门禁通过」：`test_golden_sample` 只跑 spec 与 story 门禁，AR90004 无 plan；改为「spec 与 story 门禁」。
- 5b 交回那次「1 failed 重跑全过」无法定名；最可能是高负载下 node 子进程超 60 s 超时或 Windows 临时目录清理时句柄未释放，属偶发。以后全量跑加 `-rf` 留下失败用例名。

## 4. 设计决定

| 决定 | 内容 |
|---|---|
| U34 | real-run 夹具是那次实跑的记录：决策件与判断件的**形态**按现行规则重写（U29 已做），判断的**内容**（如 OBS-07 把渠道推给 plan）保留为历史证据，不按新知识改写；06 §8 注明 |
| U35 | `test/story/release/内网adapters架构描述.md` 是 1.9.5 的发布归档，不是运行时文档，不在 U31 的退出范围；是否清理由用户定 |
| U36 | 步骤 5 的验收进 07 验收总表与版本范围（已补） |

## 5. 已核为对的补充

- 知识 §4「SDK 行为事实」逐句与 `05-SystemBase/CommFunc/src/main/ets/shared/ha/` 代码相符：`vocBuilder(walletEventID, walletEventDesc)`、VOC 事件名固定与 BEHAVIOR、Chart STATISTIC、`setWalletEventDesc` 在基类、`report()` 成功路径不写日志、三个门面先写日志再发 VOC、业务代码里两渠道都无调用。
- 5a：演进记录 1.9.4–1.9.7 四节十三条全带块标签，版本归属逐条核过；`adapt-scan` 只有版本比较、无行为分支；`doc/extensions` 下只剩 `templates/inbox-readme.md` 一份 README，`scripts/` 根无文件；`章节合同说明.md` 与现行合同一致；使用指南指向演进记录。
- 5c：AGENTS §3.2 只改两条且为长期约束；TEST 静默条款与 `watch` 行为一致；撤下 `neutral` 项的推理自洽、用户已裁定。
- 金样候选样稿的快照设计成立：`test_golden_sample` 证明的是「金样经现行机制通过，知识按定稿时快照」，与 AC04 配套；候选仍不进被测输入。

## 6. 推进

E1–E7 加观察项一次提交、一份交回；我核过后 07 的实跑等用户授权。实跑前按 TEST 迁走 `doc/features` 下 1.9.6 的旧格式成品，用干净工作区。

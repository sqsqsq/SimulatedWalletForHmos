# 步骤 5d 评审

2026-09-26，维护设计者。对象：`d1bb0104`（返修与步骤 5 评审处置、埋点知识精简、在途单按安装版本列出）、`13cd37ec`（候选样稿补来源，用户已确认）与 [5d 交回](../实施反馈/2026-09-26-步骤5d-实施交回.md)。方法：独立复跑全量与检查器、实测规模；逐条读 diff；OBS-02 探针用真实加载器在临时方法体上复现；关键点抽框架源码复核。未改产品。

## 1. 结论

**E1–E7 与观察项全部关闭，精简没有丢掉下篇读者要用的规则，可以进 07 的实跑，起跑等用户授权。** 在途单按安装版本列出是本轮新增的设计决定，已记为 U37 写进 00 与 08；交付面只剩使用指南一句要同步（§3）。

| 项 | 结果 |
|---|---|
| 全量离线（独立复跑） | 1448 passed、15 skipped、548 subtests，无失败 |
| 失效形态 | 67 条：FAIL 0、PASS 59、委派 8 |
| 机制规模 | 13362（prompts 1883 / hooks 4142 / data 764 / py 3106 / mjs 3467），与交回一致；峰值 13460 内，发布时按实测重签 |
| 埋点知识 | 8983 字节，与交回一致 |

## 2. 逐项核实

| 项 | 结论 | 依据 |
|---|---|---|
| E1 | 关闭 | `flow/update.py:288-294`：解析失败与 `tzinfo is None` 走同一句 FlowError；`test_a_receipt_time_without_a_zone_stops` 用不带时区的回执断言「带时区」；原夹具改带 `Z` |
| E2 | 关闭 | `hooks/spec/post_check.mjs:185-187` 先剔验收编号再认功能编号；`acceptanceIdRe()` 带 `g`，一行多个验收编号都剔掉；`test_an_acceptance_id_shaped_like_a_function_is_not_split` |
| E3 | 关闭 | 真实加载器解析出的 OBS-02 探针：只带描述的 Chart、只写 `Logger.info` 命中；空方法、只有注释里的 `Logger.info` 不命中。`Logger.ets` 只有 `debug / info / error` 三级，探针与之吻合。不加常驻测试的理由成立（U5） |
| E4 | 关闭 | 列名断言已删 |
| E5 | 关闭 | `upgrade-changes.md` 1.9.7 节第 6 条 `[知识]`；本节共 7 条 |
| E6 | 关闭 | `event-tracking.md:91` 写三项取号，描述要求只在 `:69` 一处 |
| E7 | 关闭 | 样稿 `plan.md:25-62` 带拼接值的描述都写了来源；只有固定说法的行不拼值，不需要来源 |
| 观察项 | 关闭 | ownership 反向断言、S16/S18 改 `D1`、`dfx-baseline` 占位、coding 作者页、子项报错与测试、交回措辞、08 §6 |

`previousReport` 取非当前对象这一项**接受不做**：`framework/harness/harness-runner.ts:1124` 发出审查前钩子，审查对象要到后面才签发，钩子拿不到当前对象；对象没变时框架沿用已有报告、不派审，对象变了时 ledger 最后一个键就是上一份。现行取法符合两种路径。

埋点知识精简逐条核过：

- 退出的 §9 反模式都有接替：「按流程分节只写观察」由上篇 §3 第 1 步（一个指标一个小节）承担，「失败原因、终态分布写成指标」由上篇 §1 承担，「报点写在不发请求的方法上」由 §7 第 3 条承担，「Chart 与 VOC 各报一次」由 §7 第 6 条承担，「描述只写结果名」由 §5 描述一格承担。
- §5 描述一格去掉的「编码字段已有的值不重抄」，已被同一格的「写编码表达不了的原因、场景与关键状态」涵盖。
- plan 只拿到下篇：渠道规则（§4 事实、§7-6 规则）、四项取值（§5）、候选写法（§7-1）、外码来源（§7-5）都在下篇。

## 3. 需改

| # | 级别 | 目标形态 | 证据 | 结果判据 |
|---|---|---|---|---|
| F1 | 需改 | 使用指南讲升级跟进时，写清在途单条目按目标升级前装的版本列，知识与对接层按已适配到的版本列 | `test/story/release/story使用指南.md:10` 现写三块都「按目标已适配到的版本」 | 使用指南与 `adapt-scan.mjs:369-373、:424-430`、SKILL「按版本跟进」一节说法一致；可随 07 §判定的发布同步一起做 |

## 4. 设计决定

| 决定 | 内容 |
|---|---|
| U37 | 升级跟进里在途单条目按目标升级前所装 `version` 列，知识与对接层按 `adapted_for` 列：在途单只跟单子是在哪一版起的有关，适配过的版本不再重复列。已写进 00 决定表与 08 §3.2、§6 |

## 5. 推进

1. F1 交给实施者，随下一次提交或发布同步完成，不阻断实跑。
2. 实跑前按 TEST 迁走 `doc/features` 下 1.9.6 的旧格式成品（现有 `AR90006`、`AR90006-update`、`ISSUE-206`、`ISSUE-206-update`），用干净工作区。
3. 起跑等用户授权；判据按 07 §3，重点看 J1–J4、J6，精简后的下篇能否让 plan 写出四项值与描述由 J4 判定。

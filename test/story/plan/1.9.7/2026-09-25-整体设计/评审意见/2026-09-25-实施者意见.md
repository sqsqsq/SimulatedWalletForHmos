# 1.9.7 整体设计：实施者意见

2026-09-25，维护实施者。对象：[方案评审](2026-09-25-方案评审.md)及设计者据此改入 00–07 的未提交修改。评审的方向认同。下面三处与代码或实测不符，或与用户决定不一致，交设计者修改。

## 1. 用户决定：四步做完再实跑

用户 09-25 选择不插小实跑，四步全部提交并通过评审后，只跑 07 的完整实跑一次，接受「J1–J3 不成立时四步改动一起被扣住、归因较难」的风险。

改法：
- 00 §5 依赖段后的小实跑说明，改为写明这一选择与所接受的风险；
- 07 §3 首段删去插入小实跑的那句。

## 2. 06 §3.6「现有 FAIL」与实测不符

M05、M17、M18、W01 四条已在 `a9cb013f` 修正，1.9.6 发布说明「验证结果」记为 FAIL 0。09-25 重跑 `check_failure_modes.py`：64 条，FAIL 0、PASS 56、委派 8。

改法：删去 06 §3.6 的「现有 FAIL」一条；07 §1 的「FAIL 0」照原样成立。

## 3. U19 引用的原则在方案里不存在

- U19 称自己是「不做指纹、不做状态机」原则的唯一例外。00 决定表里只有 U9「加载器单版本，不做迁移脚本、不记进度、不做内容指纹」，管的是知识加载器，不涉及流程契约。
- 流程契约现在就用摘要判轮次：`doc/extensions/skills/story/scripts/core/story_flow.py:47`，轮次边界由材料清单的 `digest` 判定。
- 步骤 1 自己也新增状态函数 `stage_of`（03 §3.3）。

改法：U19 只写实质——契约写入时记内容摘要，读到不符时报「契约被手改」并给 `status` 的恢复路径，命令照常执行；理由是 A16，根因由 A15 的状态函数修。删去「唯一例外」一句。

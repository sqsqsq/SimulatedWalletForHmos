# 步骤 5d 交回：返修与步骤 5 评审处置、埋点知识精简、在途单按安装版本列出

2026-09-26，维护实施者。依据 [返修与步骤 5 评审](../评审意见/2026-09-26-返修与步骤5评审.md) 与用户同日两问（埋点知识膨胀、adapt 普通升级耗时）。不含评审结论。

## 1. 需改 E1–E7

| # | 处置 |
|---|---|
| E1 | `_fetched_this_round` 解析后 `tzinfo is None` 与解析失败同报「不是带时区的 ISO 8601 时刻……重跑取材」；`test_a_system_requirement_reads_its_receipt` 夹具改带 `Z`；新测试 `test_a_receipt_time_without_a_zone_stops`；演进记录 1.9.7 与替身 `fetch.js` 注释写「带时区」 |
| E2 | §8 功能编号先剔验收编号再匹配；新测试 `test_an_acceptance_id_shaped_like_a_function_is_not_split`（`AC-F1` 不切出 `F1`） |
| E3 | OBS-02 探针改为同时认 `log*AndReport` / `vocBuilder` / `setWalletEventDesc` / `Logger.info|error|debug`。临时方法体实测：只带描述的 Chart、只写 `Logger.info`、用门面三种都命中，空方法不命中。没有加常驻测试：那会是对 Demo 知识内容的断言（U5） |
| E4 | 删 `test_the_lower_half_names_the_required_design_items` |
| E5 | 演进记录 1.9.7 节加 `[知识]`：两张埋点表的列由上报知识给出，机制只认 spec 表「统计点」、plan 表「统计点」「责任方法」 |
| E6 | 并入精简：§7 第 1 条写「§5 的事件 ID、内码、结果分类写出实际的号」；描述要写来源只在 §5 那一格写一次，没有按评审原句在 §7-1 再写一遍 |
| E7 | 候选样稿 24 行描述补来源（评审点的 16 行加上几行同类写法），逐行核过每行都有「取自……」或接口返回；「开卡状态查询 / 成功」删去字段取值里的「另带查到的开卡状态」，只留在描述里。**改动在工作区，等用户确认后单独提交** |

## 2. 观察项

| 项 | 处置 |
|---|---|
| ownership 反向断言 | `adapted_for: 1.9.6` 时断言不出现 1.9.4、1.9.5、1.9.6 的条目 |
| S16、S18 议题编号 | 四份 `decisions.json` 的 `DEC-001` 改 `D1` |
| `previousReport` 取非当前对象 | **没做，依据**：审查前钩子在 `framework/harness/harness-runner.ts:1124` 调用，审查对象到 `:1889` 才签发，钩子拿不到当前对象。对象没变时框架沿用已有报告、不再派审，指向自身的那一句不会送到审查者手里；对象变了时，ledger 最后一个键就是上一份，现行取法是对的 |
| `dfx-baseline.md` | 占位改「以本仓性能基线为准」 |
| coding 作者页 | 「拿什么」改为「下篇全文，节名以知识为准」 |
| 演进记录子项 | `changeEntries()` 读到缩进子项直接报「每条一行、不用子项」；文件头写明；新测试 `test_an_indented_sub_item_stops_before_writing` |
| 交回措辞 | 返修交回 D6、5a 交回 §4 按评审改 |
| 08 §6 | 改为「spec 与 story 门禁，plan 门禁由中性 fixture 覆盖」 |
| `-rf` | 本次全量已带 |

## 3. 埋点知识精简

只删同一篇里的重复与读者用不上的内容；上下篇各写一次的保留（plan 只拿到下篇）。

| 项 | 处置 |
|---|---|
| 渠道规则 | 事实留 §4，规则留 §7-6；删 §6 结算句尾的日志半句与 §9 对应条 |
| 描述写法 | 只留 §5 表一格，§5 那格同时压短；删 §7-6 后半句与 §9 末条 |
| 候选值 | 只留 §7-1；删 §5 表后那半句与 §9 对应条 |
| 外码 | 并进 §7-5；§8 plan 行改为「§5 各项与外码来源都已写出」 |
| §9 反模式 | 整节退出：两条是 spec 的错法（上篇 §1、§3 已写），其余与 §7、§8 重复 |
| 上篇第 5 步 | 压成一句规则加括注 |

10427 → 8983 字节。real-run AR90006 知识摘要重算为 `sha256:7e61a67d4716a5c3`。

## 4. 在途单按安装版本列出

`adapt-scan.mjs` 在写目标之前读目标原来的 `version`（`INSTALLED`）；`[在途单]` 按它过滤，`[知识]`、`[对接层]` 仍按 `adapted_for`；输出加 `installed`；SKILL 与演进记录文件头同步。测试：`test_in_flight_entries_follow_the_installed_version`；原 `test_an_unadapted_target_gets_every_block_from_a_stand_in_source` 把目标版本设为 1.9.3，模拟旧装的仓。

普通升级的耗时：脚本多读一个约 7k 字节的演进记录；目标已适配到上一版时只列本版条目（1.9.7 有 7 条，含 E5 新加的一条）；目标没写 `adapted_for` 时一次性列全。

## 5. 结果

| 项 | 结果 |
|---|---|
| 全量离线 | 1448 passed、15 skipped，无失败（带 `-rf`，不含候选样稿改动；含样稿改动时同样全过） |
| 失效形态 | FAIL 0、PASS 59、委派 8 |
| 机制规模 | 13362（prompts 1883 / hooks 4142 / data 764 / py 3106 / mjs 3467） |

## 6. 未验证

- 被测模型读精简后的下篇能否写出四项值与描述，看实跑。

# 步骤 5a 交回：版本号、按版本升级、README 定位

2026-09-26，维护实施者。依据 [08](../08-步骤5-按版本升级与维护纪律.md) §3.1–§3.3 与 U30、U31。不含评审结论。

## 1. 实际改动

| 项 | 改动 |
|---|---|
| 版本号 | manifest `version` 升到 1.9.7；`test_the_manifest_version_covers_this_round` 锁 1.9.7 与演进记录的 `## 1.9.7` 节，docstring 写开工即升；07 §判定删「升 manifest version」，同步项改为核对演进记录 |
| 演进记录 | 新 `story-adaptation/reference/upgrade-changes.md`：1.9.4–1.9.7 四节，每条标 `[知识]` / `[对接层]` / `[在途单]`；对接层只写命令、参数、stdout、落点与失败语义；删 `knowledge-changes.md` |
| 目标状态键 | `knowledge_adapted_for` → `adapted_for`（归目标，三块都交回后写）；Demo manifest 写 1.9.7 |
| adapt-scan | 启动时 `changeEntries()` 逐条读演进记录，没标块即报包坏了（写目标之前）；`upgradeFollowUp()` 按块列晚于 `adapted_for` 的条目，包带对接实现时不列对接层；⑤ 去掉 README 豁免 |
| adapt SKILL | 新「按版本跟进」一节（三块各怎样做、交回什么）；升级后按块问一次、可只选几块；替身包首装对接层列为未适配、从业务仓复刻；删「对接层的输出合同」与指向 `scripts/README.md` 的引用；⑤ 改根下没有文件 |
| 方法页 | 输入行与第 6 步、交回按 `adapted_for` 与演进记录的知识块写 |
| README | 删 `scripts/README.md`；`contracts/README.md` 移到 `test/story/章节合同说明.md`，同步 `banned_terms_exempt` 键名、`id_shapes.function`、`flow.schema` 与 `update.decisions` 的 `issue` / `asked`；`inbox-readme.md` 按导入规则写四类材料与去向、读不了的格式由 AI 转写 |
| 内外网隔离 | 替身六个文件头去掉「与内网同名同签名」等说法，`story.js` docstring 改为「命令接口（SKILL 按此调用）」；Demo 知识 `dfx-baseline.md` 的基线占位改为「以目标仓性能基线为准」；`git grep "内网" -- doc/extensions` 零命中 |
| 内网指南 | 删 `test/story/release/内网适配指南-1.9.3升级1.9.7.md`；使用指南改指演进记录 |
| 方案 | 00 加 U30–U33 与步骤 5 行，U7、U8 改为现行写法；新增 08 分册 |

## 2. 测试与夹具

- `test_adapt_source_kinds.py` 新增三条：无 `adapted_for` 的目标从替身来源升级列出三块且对接层含 1.9.4；业务仓来源不列对接层；演进记录有一条没标块时退出 2、目标未写。
- `test_adapt_ownership.py`：改键名与提示文案；⑤ 期望 `scripts/` 根下没有文件。
- `test_knowledge_forms.py`：改为核演进记录存在、无内网指南、无 `scripts/README.md`。
- 失效形态 A02 的包与目标夹具补一份空演进记录（脚本启动要读它）。
- real-run AR90006 的 `knowledge-use.yaml` 知识摘要随 `dfx-baseline.md` 改动重算（`sha256:8e540d0170180d82`）。
- 演进记录措辞按 M02 与机制中立测试改掉历史叙述与知识结构词。

## 3. 结果

| 项 | 结果 |
|---|---|
| 全量离线 | 1445 passed、15 skipped、551 subtests |
| 失效形态 | 67 条：FAIL 0、PASS 59、委派 8 |
| 机制规模 | 13358（prompts 1883 / hooks 4140 / data 764 / py 3104 / mjs 3467）；prompts −94 来自两份 README 退出，adapt 脚本与 SKILL 不在计量域 |

## 4. 与分册的出入

- 1.9.4 节原拟写的规约措辞条目（DLV-02 / COMPAT-02）没写：那是 Demo 自己知识内容的修正，不是目标要跟的协议变化。
- 金样知识快照 `fixtures/golden/knowledge/` 是锚，按 08 §5 不随 Demo 知识改动，其中 `dfx-baseline.md` 保持金样定稿时的措辞。

## 5. 未验证

- 内网 1.9.3 → 1.9.7 按演进记录的真实升级由用户在内网验证。

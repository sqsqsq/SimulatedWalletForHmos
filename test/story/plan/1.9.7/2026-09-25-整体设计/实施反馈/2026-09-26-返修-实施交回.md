# 四步实施评审返修交回

2026-09-26，维护实施者。依据 [四步实施评审](../评审意见/2026-09-26-四步实施评审.md) 与写回分册的 U21–U29；用户同日决定：extension 不兼容任何历史功能或数据格式，升级问题只写在 adapt 与内网适配指南里。不含评审结论。

## 1. 逐条处置

### 步骤 1

| # | 处置 |
|---|---|
| A1 | `issueZones` 找不到行首「评审结论：」时，整块按机器区交摘要核，对不上抛 `ProjectionConflict`「议题 Dn 的人工区不是当前形态……按当前形态重写这一条的人工区后再登记」，文件不动。不认旧标签、不搬旧内容。测试 `test_review_modes.py::test_a_zone_not_in_the_current_form_stops_the_build` |
| A2 | `state.load` 读到 schema 不是当前值直接报「不是当前版本的流程契约（schema x，当前 4）：这张单在旧版本收口，或清掉需求目录后从 init 重新起单」；`check.mjs` 同一句。没有迁移或重记路径。测试 `test_rework_guards.py::AnOldContractIsNotRead` |
| A3 | 原话映射只认编号的说法（整句为 n、第 n、n.、n）、选 n）、整句标签或标签里的一句话、「按推荐」；键不参与；其余要 `--chosen`，冲突拒绝。四句反例与正例进 `test_rework_guards.py`；SKILL 与 routing 的用法说明同步 |
| A4 | `cmdBuild` 缺决策件与 `registrationGap` 同句 |
| A5 | 使用指南「评审表态」改三态加修改意见 |
| A6 | `decide --update` 必须带 `--issue <Dn>`，从评审记录取该议题机器区摘要记为 `asked`；议题不在评审记录里拒绝。`review.mjs` 沿用表态按 `issue` 认。update.md、SKILL、story_flow 用法同步。测试 `test_story_update.py::AHumanDecisionInThisRoundIsRecordedVerbatim` |
| A7 | 随 A2：旧版契约报旧版，当前版契约摘要不符报手改，不再混在一句里 |
| A8 | 决定人判据改为结构：带括号或书名号、写了「在…上」、以动作词或「稿 / 文档」结尾、带文件后缀。「审核员」「评审组长」通过（新测试） |
| A9 | 设计已改 03 §4，代码不动 |
| A10 | 成文基准：只有成文基准、没有 update 记录时，新登记换掉旧基准；有 update 记录不写 |
| A11 | 对接层回执 `fetchedAt` 改写 ISO 8601（Demo `fetch.js`），core 按它判这一轮取过上游；读不出直接报错、不回落到文件时间。对接合同写进 `scripts/README.md` 与内网指南 |
| A12 | F01、R04 夹具关卡记录 `by` 改 human，契约摘要重算 |

### 步骤 2

| # | 处置 |
|---|---|
| B1 | ledger 只在格式合规且判据齐时登记；换了内容的重投文案去掉「放回原处」。测试：缺判据 → 重投完整回复 → 不再报报告问题 |
| B2 | 格式不合的回复不移动，每次运行都报；门禁写盘只剩 ledger。phases/spec.md 与 pre_verifier 注释同步 |
| B3 | 上游原始编号在 §8 任一行出现即承接；文案回到「§8 或 acceptance.yaml 有对应行，或写不承接理由」。实跑夹具与金样工作区各跑一次 spec 门禁（`test_review_closure.py::test_aligned_ids_raise_nothing`、`test_golden_sample.py::test_the_spec_gate_accepts_the_golden_inputs`） |
| B4 | `hooks/spec/author.md` 的验收段写 §8 编号定义行与「不承接：<编号> <理由>」 |
| B5 | 功能编号形态进 `id_shapes.function`（`contracts.mjs` 的 `functionIdRe`）；§8 第一次作行首的那一行是定义，其余行只核在册；§8 内的「同号不同义」判据退出，acceptance.yaml 内的保留 |
| B6 | 审查片段删作者区 sha 行；AC22 测试改为核片段不含机器区内容 |
| B7 | notes 处置按判据 id 子串核，维持 |
| B8 | 测试删 `updateOpen` 残参；`evidence-rules.md` 与 Demo 规约的示例换成活动报名题材 |
| B9 | 审查请求末尾给上一份合规报告路径（`verifier.conclusions.json` 最后登记的对象）；`judgement_change_basis` 改为以 notes 声明的改动为范围、对照上一份报告 |

### 步骤 3

| # | 处置 |
|---|---|
| C1 | 加载器四项 frontmatter 都报缺（新增 `name`）；`factsByName` 一处解析，校验与 plan 送达共用，只认 `name` |
| C2 | 埋点知识上篇第 2 步写 spec 统计点表的列（含已有覆盖或新增）；overlay 那句改为「按那一篇核」；金样输入 spec 表列随知识改名并重投 |
| C3 | 内网指南写在途单不迁移（在旧版本收口或清掉重起），新单按篇名登记；实跑夹具按篇名登记（见 D1） |
| C4 | AC05 测试在增删改知识后跑 spec post_check 与 pre_verifier（spec、plan），核无异常、无知识不合协议 |
| C5 | protocol 设计模式一节按目的描述两篇、不列节名；AC04 允许词只取两个篇名、§一/§六 表格结构名与产物模板出口名。由此暴露的同字：埋点知识「必须设计项」节改名「每种结果要给的值」，coding 作者页「反模式」改「常见错法」，方法页与两条规则里的同字换说法 |
| C6 | protocol constraints 格写「带探针列的那一份」；Demo 示例按目录加 form 指认是为不点名知识文件 |
| C7 | 下篇反模式写成自足句 |
| C8 | 设计已改 05，代码不动 |
| C9 | `SHELL` 表达式加括号 |
| C10 | 设计已改 05 §7 |

### 步骤 4

| # | 处置 |
|---|---|
| D1 | 实跑夹具决策件按现行规则重写：决定人只写角色、类别取七维、open 用 choice 给可选做法与建议（缺事实写明缺什么）、settled 依据引原话；判断件的埋点知识按「上篇」登记并重算 digest；CRLF 与结构保留 |
| D2 | `test_review_golden` 改按结构比对（标题与编号、段首、人工区五行、议题锚），金样指纹保留 |
| D3 | 规划相符判定用含 story 的阶段表，两边都已知且一致才给；补 story↔spec 两个方向与阶段未知的测试 |
| D4 | TEST.md 删旧句；`measure_run.py` 的 `segments` 按段给出 `duration_min`、`model_gap_sec`、`tool_gap_sec`、`verifier_runs`、`verifier_gap_sec`、`first_harness`、`first_story_register`、`rework_min`（普通单一段 `whole`），TEST.md 只写这些字段 |
| D5 | M02 反例加 `AC-R1` 与「36 小时」一行；M06 反例加一个读 JSON 失败静默返回的 `.py` |
| D6 | **查明原因**：本机用户目录本身是 git 仓、`.gitignore` 写 `*`，系统临时目录落在它范围里，内置检索把整个工作区当成被忽略（探针：临时目录下已知内容零命中；工作区落在上层 git 仓的忽略范围内）。修法：工作区根写 `.ignore`（`!*`），它优先于 `.gitignore`，探针复现后命中恢复；步骤 4 去掉根 `.gitignore` 的推断改法撤回 |
| D7 | M20 派生词只取 ≥4 字；业务数值只扫 `.md`，排除 10 次以下的次数 |
| D8 | 退役 case 的 `review-comments` 删；`scripted_reply_*` 事件名删；auto 第二段材料立场改「不补，材料就这些。」 |

### 用户 09-26 决定落点（无需兼容）

- 除 A1、A2 外，章节合同 `id_shapes.drop` 退掉旧议题编号形态 `DEC-\d+`，测试里的议题编号统一为 D 形态。
- 扫过 hooks、skills、rules 里的「旧 / 兼容 / 迁移」字样：剩下的都是材料版本与本轮运行内的语义，没有读旧格式的分支。
- 升级说明只在内网指南与 adapt 的演进记录：指南写在途单不迁移、回执 `fetchedAt`、四项 frontmatter 与篇名登记；演进记录 1.9.7 条目同步。

## 2. §5 资产重做

| 资产 | 结果 |
|---|---|
| review 金样 | 文案不变，结构比对通过，指纹不动 |
| `run_case.py` 人工区常量、`test_host_driver.py` REVIEW 夹具 | 文案不变，未动 |
| 实跑夹具决策件与判断件 | 重写（D1） |
| 命令示例 | SKILL、routing 的映射说明改；init_analysis、scope_gate、update.md 的 `decide --ask --reply` 形态不变；`decide --update` 三处加 `--issue` |
| 金样输入 spec、判断件、知识快照、AR90004 金样 | 快照随 Demo 知识重取，spec 表列改名，判断件 digest 重算，附录重投并编号，指纹与形态重锁（形态数不变） |
| AC04 允许词、M20 夹具 | 允许词按 C5 收窄；M20 夹具不受影响 |

## 3. 结果

| 项 | 结果 |
|---|---|
| 全量离线 | 1442 passed、15 skipped、548 subtests |
| 失效形态 | 67 条：FAIL 0、PASS 59、委派 8 |
| 机制规模 | 13453（prompts 1977 / hooks 4140 / data 765 / py 3104 / mjs 3467），峰值 13460 内；py 比四步后 +33（映射规则、议题摘要、基准重写、回执时刻、旧契约报错），发布时按实测重签 |

## 4. 未验证

- D6 的修法在本机探针上复现并验证；实跑工作区里内置检索是否恢复，看实跑。
- A11 改了对接层回执时刻格式：Demo 替身已改，内网实现按指南改，由用户在内网验证。
- `measure_run.py` 的按段字段在离线事件上验证；真实事件流里 `task` 以外的审查派发形态是否都被计入，看实跑。

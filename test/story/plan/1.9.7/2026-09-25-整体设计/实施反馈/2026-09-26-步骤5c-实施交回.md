# 步骤 5c 交回：维护规约与实跑宿主

2026-09-26，维护实施者。依据 [08](../08-步骤5-按版本升级与维护纪律.md) §3.5 与 U32。不含评审结论。

## 1. 实际改动

| 文件 | 改动 |
|---|---|
| `test/story/AGENTS.md` §3.2 | 「交付面正向表达」改为「正向表达」：交付面、方案与知识都直接写做什么、怎么做，不写补偿句，退出项写进退出表；「正向实现，不打补丁」删去迁移桥半句，写明 extension 不兼容历史功能或数据格式，旧格式直接报错并指明在旧版本收口或从 init 重起，目标仓升级要做的事只写在 adapt 的升级演进记录里。其余章节不动 |
| `test/story/TEST.md` | §3 返回字段段与 §4 heartbeat 提示词：唤醒后静默处理，只在 suite 终态 finalize、装置或 CLI 故障、需要用户拍板时向用户报告 |
| 07 | 宿主一行加静默监控 |
| 00、08 | U32 与 08 §2、§3.5、§4、§6 按用户 09-26 的最终裁定改写 |

## 2. 撤下的一项

分册原写「去掉 `neutral`、查清 09-25 等待误判的根因」。查证结果（suite `story-suite-20260925-004130` car 的事件流）：五次回「继续」前，被测 CLI 进程都已自己结束该轮（`lifecycle succeeded`），模型最后一步是没发出工具调用的一段文字、没有提问。交给用户后，用户 09-26 裁定这一项当不存在、以后遇到再说：TEST §3.0 的 `neutral` 与驱动器不动。

## 3. 结果

| 项 | 结果 |
|---|---|
| 全量离线 | 1445 passed、15 skipped、552 subtests |
| 机制规模 | 只动测试域，不计量 |

# 步骤 5b 交回：埋点描述字段与渠道选择

2026-09-26，维护实施者。依据 [08](../08-步骤5-按版本升级与维护纪律.md) §3.4 与 U33。不含评审结论。

## 1. 实际改动

| 项 | 改动 |
|---|---|
| 上篇 §3 第 5 步「选渠道」 | 一个统计点只走一个渠道：Chart 按身份与编码计数、VOC 是给人读的定位记录；进统计的结果走 Chart，原因、场景与关键状态写进它的描述，不另报 VOC；VOC 只用于不进统计的过程定位点 |
| 下篇 §4 SDK 行为事实 | Chart 带身份、分类、内码、外码，描述经 `setWalletEventDesc` 设，`report()` 不写本地日志；VOC 写成 `vocBuilder(walletEventID, walletEventDesc)`，事件名固定、内容就是描述；三个门面用同一段描述先写本地日志再发 VOC；业务代码里 Chart 与 VOC 都没有调用 |
| 下篇 §5 | 三项改四项，加「描述」：固定说法加本次关键值，写编码表达不了的原因、场景与关键状态，关键值写来源，不写个人数据与凭据，不重抄编码字段 |
| 下篇示例表与结算句 | 加「描述」列（「校验抬头未通过」拼响应原因与抬头类型）；结算方法设描述，要本地日志另调 `Logger` |
| 下篇 §7 | 新第 6 条「一个点位一个渠道」（原第 6 条 BI 与扩展字段顺延为第 7 条） |
| 下篇 §8、§9 | plan 完成判断改四项；单元测试核统计点上没有 VOC 事件；反模式加「统计点上 Chart 与 VOC 各报一次（含用门面补日志）」「描述只写结果名」 |
| 规约 OBS-02 | 「避免重复记录」写明统计点位的定位信息随统计事件的描述带，不另记定位事件 |
| 07 J4 | 四项取值，同一统计点不同时设计 Chart 与 VOC |
| 测试 | `test_knowledge_forms.py` 下篇列核对加「描述」；real-run AR90006 `knowledge-use.yaml` 的知识摘要随知识改动重算为 `sha256:0bcb47d9079dd415` |

## 2. 候选样稿（等用户确认，未提交）

`test/story/golden/bank-card-opening-reporting-candidate/` 改在工作区：

- spec 与 story 那一句「过程诊断记在 VOC，不在同一统计终态上重复上报」改为「每个统计点只报一条统计事件，定位要用的原因与场景写进它的描述，不另记定位事件」；
- plan 9.2.1 加「描述」约定，9.2.2 加「描述」列，47 行各写一句固定说法与拼入的关键值（如「验证码过期」拼距发送的秒数）；末尾核对句加描述与「统计点上没有另报定位事件」；
- README 修订记录加 09-26 一条，Plan 形状一行加描述。

## 3. 与分册的出入

- AR90004 金样、它的输入夹具与知识快照不改：AR90004 的 spec 与 story 没有写渠道或逐结果取值；快照按 `golden_workspace.py` 的设计是金样那一刻知识的锚，不随 Demo 知识移动。08 §5 写的「快照重取、重投」不需要做。
- AR90006 story 金样不涉及渠道与描述，不改。

## 4. 结果

| 项 | 结果 |
|---|---|
| 全量离线（含候选样稿改动） | 1445 passed、15 skipped、552 subtests |
| 全量离线（不含候选样稿改动，即本提交） | 一次 1 failed / 1444 passed，重跑全过，失败用例名没复现出来 |
| 失效形态 | FAIL 0、PASS 59、委派 8 |
| 机制规模 | 知识不计量，不变 |

## 5. 未验证

- 被测模型读到下篇后能否在 plan 9.2.2 写出描述列、spec 能否直接定下渠道，看实跑。
- 内网上报协议里描述字段的长度与取值限制本仓没有依据，由内网知识按真实协议补。

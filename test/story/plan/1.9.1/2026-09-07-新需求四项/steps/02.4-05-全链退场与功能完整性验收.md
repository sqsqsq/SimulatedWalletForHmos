# Step2.4-05 · 全链退场与功能完整性验收

> 状态：方案待评审，未实施。基线 `d36edb70f04c60128c2e07501c7eff5fa299c07f`。总览：[Step2.4](02.4-旧机制退场清理.md)。本步只处理所列职责及必要直接消费者，不运行Extension业务阶段。

> 前置：02.4-04-知识协议残留与验证桥接.md 的实现与评审通过；一次只交回本步。

## 1. 清理什么，为什么可以清理

| 项 | 当前对象 | 为什么能退或能合并 | 留下什么 |
|---|---|---|---|
| R18 删除指向已退职责的实现注释与指令 | skills/story/scripts/core/story-build.mjs；hooks/spec/post_check.mjs | seeded参数已删；A–D现在是机器投影而不是作者改表；旧注释仍以已取消的附录散文限制解释实现，会把维护者带回旧设计。 | 必要的边界/失败语义注释，函数本身按其职责另判。 |

## 2. 逐项动作边界

### R18

删除或改成现行职责解释，不重新要求按旧规则改实现。与R07/R15同步清理其它同义说明；不扩大成关键词全仓禁令。

**完成条件**：说明不再提不存在参数或要求作者改机器投影，脚本与作者文档口径一致。

## 3. 全链关闭，不只查删除名单

按六条职责链反查：取材与范围、Story成文、决策归档回流、知识全阶段、恢复交付、安装升级。每一条同时回答：现行功能目的、唯一承担者、已退出的旧承担者、正常与失败结果、对应验证。

R18对照实际实现删失效说明：已不存在的seeded参数、要求作者改机器投影、把整份附录当一句目的句加表格、声称writer手写符合性表等。与01–04修改直接相关的旧指针、帮助、注释、模板、合同和测试期望一并更新；不扩大成全仓风格重写或新的关键词门禁。

对每个被删函数/字段，向调用方与被调用方各追一层：是否留下新孤儿、是否还有另一处继续维护同一规则、是否丢掉当前动作的必要输入。被移动的代码计复用，不计删除；测试仅引用旧机制不能续命，但真实行为断言要迁移。

### 3.1 R18补齐兄弟残料

| 对象 | 处理 | 谁在何时完成 |
|---|---|---|
| story-build报错中的“质量与验收” | 取现行合同验收章名，不再写已不存在的章名 | 05直接清；查找逻辑不变 |
| requiredShapes的`at === '*' ? '每个小节'` | slots通配分支已退，只保留空键章级和具名小节两种当前形态 | 05直接清；不能删除当前真实slot |
| DECISION_SHAPE的“由story-build init生成” | 改指sources的现行决策骨架入口 | 01随R01改，05确认旧指路消失 |
| contracts/Spec验收读取的“合并会放宽” | 改成共享提取与不同调用策略 | 04随R19改，05确认说明与实现一致 |

## 4. 必须保留的效果

| 链 | 不得丢的内容 |
|---|---|
| 取材/范围 | 原件、图、材料身份、人决定及份表、S4原始AR留存与重试 |
| 成文 | 稳定十章职责、开放下级组织、原图对应、验收身份、必要作者解释、局部落盘 |
| 评审 | 已定/未定区别、依据与影响、人工区和自由反馈、归档前备份与远程恢复 |
| 知识 | 三类知识独立维护、适用判断、实体义务、模式选择/实现、多条验收与多模式关系 |
| 恢复/交付 | 当前进度、局部返修、真实冻结、Framework回执与实际reviewVerdict、能力缺失披露 |
| 适配 | 首次仅骨架/画像、升级不改知识、两种来源的adapters所有权、包外旧机制文件退出 |

## 5. 验证与映射正式启用

1. Windows运行输出中文的检查器前设置PowerShell环境变量 `$env:PYTHONIOENCODING="utf-8"`，特别是check_failure_modes.py；打印编码异常不是判据失败，也不能为了乱码修改判据。各步针对性离线验证通过后，本步跑一次必要全量：按 `test/story/TEST.md` 的pytest、失效形态及适配检查；保持Framework零修改。不得在主仓跑业务harness来证明扩展自身。
2. 真实CLI只能由用户按TEST启动。以正常材料输入运行，金样只在维护侧评价，不喂给作者。核首次成文、父级前提、跨章关系、图文、知识及恢复行为；没有运行则标“清理实现通过，行为待验”，不关闭整体质量目标。
3. 复核三份FEATURE文档的功能全集、逐内容归属与目标映射：删除的有效职责已接替，新增无第二套真源。更新实际源文件、符号/章节/字段和摘要，不沿用本轮旧行号。
4. 只有清理完成且完整性复核通过后，才把三份FEATURE文档从分析目录移回 `test/story/`，将目标映射转为当前映射，更新相互链接及AGENTS入口。未完成前不移动、不宣称正式清单。
5. 交回实际diff与各类代码行/完整字符/运行输入成本；记录未验证边界。不得用“行数下降”替代功能完整，也不得把涨幅回填成新目标。

## 6. 关闭凭据

一张R01–R23完成表、一张D01–D03返修表、六条功能链的保留证据、实际规模变化、源码/提示/测试消费者的退出核对、正式映射位置与验证状态。这里是维护报告，不产生任何业务自证文件或新验收平台。

# Step2.1 文本收敛与无消费者清理复核

> 2026-09-08；基线 `4e5a0615`，复核 `ce9884e7` 与 `4e5a0615`，承接 reviews/11。

## 结论

本轮修改通过，未发现新的阻断项。Step2.1 可按既有口径记为“实施评审通过，行为待验”，继续后续步骤；真实首次成文质量与未知场景泛化仍须按已有验收确认。

## 修改复核

| 项 | 判断 |
|---|---|
| 观察者与界面关系 | 已修正为核反馈路径依据；用户、调用方、后台任务与运维方都可以是实际观察者，不再以是否有自身界面否定用户可感知结果 |
| 熟悉业务例子偏置 | story-write 的对象、统稿条件及 reader-review-task 的跨章条件改为从本需求提取；未通过扩充例子清单解决问题，没有新增按业务类型判断的脚本 |
| 维护信息 | KB-11 编号与资格门方法指路已移除，manifest 多个旧版本行为说明清除；保留当前职责与理由，未改变知识、目标说明与适配的所有权 |
| 删除 adapt 旧模板 | 现行 adapt 已无“先生成适配方案”的动作，未找到执行入口对 templates/plan.md 的引用；删除符合已取消职责 |
| 删除 rules/rules.md | 现行成文、spec 与评审回流入口承载相应要求；未找到公共入口继续引用该文件。写作一致性测试由三份副本改为实际送达的两份，减少冗余，不是删除唯一要求 |

本轮清理是删除无消费者与重复内容，不是将要求挪到新的文件继续叠加。没有修改业务成文脚本逻辑、增加状态或增加模型轮次。

## 两句非阻断注释校准

manifest.yaml:12 的“升级换 core、不碰 adapters”应写明来源：Demo 来源保留 adapters，内网来源复制 adapters；第 14 行“AR 根下只有交付文档”应包含已确认的 detail.json 例外。

两处属于当前行为摘要写得不完整，真实脚本与合同已有正确规则。本轮不据此重新打开适配/目录设计，正常清理时直接同步，不需要另跑一套完整行为测试。

## 规模实测

范围与 reviews/10、11 相同：已跟踪的 doc/extensions 文本，排除 knowledge 与 story-adaptation，计入其它脚本、JS、提示词、合同及注释。

| 版本 | 字符数 |
|---|---:|
| 上轮评审 811db972 | 499577 |
| 本轮文本修正 ce9884e7 | 498555 |
| 删除无消费者文件后 4e5a0615 | 495197 |

本轮净减 **4380**；相对 Step2.1 开工前 501544 累计减 **6347**；比上轮交付 f9fb8a21 的 498196 少 **2999**。adapt 模板不在该字符范围，删除它没有被算来放大减幅。

现有代码行预算实测 **9537 / 9800**，与完整字符数分开报告。总量下降不能单独证明质量，但本次减少能对应到具体无效职责与重复内容。

## 验证与边界

相关测试 test_writing_flow.py、test_adapt_source_kinds.py、test_author_context_entry.py 并行复跑：**48 passed，43 subtests passed，45.98 秒**。删除文件的引用检查未发现交付入口仍指向它们。

未重跑未变化的整个 Story 检查器或真实 CLI；没有把执行者报告的全量结果当作本次复跑。本报告也不把固定业务例子清除等同于已证明所有未知场景都不会过拟合。

未修改实现或测试，未提交 Git。

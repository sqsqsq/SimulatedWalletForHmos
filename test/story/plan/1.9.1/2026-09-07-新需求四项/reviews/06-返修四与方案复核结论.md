# 返修四与方案复核结论

> 2026-09-08；实现基线 `fa09d803`，对照 reviews/05 的 R1–R3 及当前步骤 3–5。只作审查，不修改实现，不代替后续行为验收。

## 结论

本次未发现新的阻断项。步骤 1 保持认可；步骤 2 本轮实施返修通过；步骤 3–5 的上一轮主要设计问题已闭合，可以按当前方案进入后续实施与验收。无需为本次审查再增加机制、字段或重写整套方案。

## 逐项关闭

| 项 | 本次证据与判断 |
|---|---|
| R1 引号与注释组合误判来源 | `yaml-lite.scalar` 改为先识别引号标量的结束位置及其后注释，引号内 # 保留。新增标量测试覆盖等价写法；Adapt 来源测试补入引号与注释组合。相关测试通过，关闭本次报告的问题 |
| R2 探针职责由操作符推导 | 步骤 3 改为知识明确声明 `阻断：`，不带前缀为辅助证据；同一操作符可承担不同职责，未落实后果按规约强制力处理。存在性检查不再强制正确代码采用直接调用写法，方案层关闭 |
| R3 人工补定与旧决定替代缺少可消费表达 | 步骤 5 的 effect.add_changes 写清落点、前后内容与 resolves；supersedes 属于选项结果，不再从 key/label 或变化列表是否为空猜测。design 与刷新清单消费 apply_changes 和 add_changes，并区分会议证据与人工 basis；已有对应验收，方案层关闭 |
| Step4 残留只登记 open | 实施指令已经改成四个出口，与已定漏项补登、未决登记及回权威产物纠错一致，关闭 |

R1 的关闭限定于上述输入形态与验证结果，不宣称该轻量读取器支持全部 YAML 语法。步骤 3–5 是方案审查通过，尚未实施，不能据此宣布知识准确性、Review 稳定性或会议理解能力已经通过真实运行验收。

## 一处非阻断文档修正

步骤 5 第 126 行的示例命令仍使用 `--chosen opt_param_5`，前面的 options 示例已经改为 opt_a / opt_b。示例应同步为 opt_a（或使用明确的选中 key 占位符），避免执行者照抄后触发现有“chosen 必须在 options 中”的校验。属于示例同步，不改变方案设计，实施前一并修正即可。

## 整体方向

本版的主要变化已经落在正确职责上：安装按来源与目录边界覆盖，知识声明要求与探针用途，Review 围绕实际主张筛选审核点，会议由模型组织可选结果、人确认、脚本选择并投影结果。新增结构能够追溯到真实输入输出需要，当前没有必要继续用更长正则或外围状态修补。

后续重点转到实施验证：知识要求确实改变各落点的设计与证据；Review 找准且讲清重要审核点；会议的变化、闭环结论、遗留问题及人工补定能准确进入下游。按现有验收执行，不把字段出现、测试数量或方案说明当成这些行为的证明。

## 本次验证

并行复跑 `test_adapt_ownership.py`、`test_adapt_source_kinds.py`、`test_empty_knowledge_repo.py`、`test_yaml_lite_scalars.py`：**33 passed，12 subtests passed，48.35 秒**。

核对 `fa09d803` 的实际代码差异及当前方案正文；本次未修改主仓实现和测试，未启动真实 CLI，未重复已通过且本轮未改的完整流程验证。

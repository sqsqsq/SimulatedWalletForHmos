# 修改复核与 98 反馈答复

> 2026-09-08；代码基线 `33dd9123`。对象：`bd90137c`、`33dd9123`，以及 [98 执行者反馈](../98-执行者给评审的反馈.md)。本报告给出可直接执行的结论；没有修改产品实现或提交 Git。

## 已改内容：通过

### 默认工程根

认可 `bd90137c`。Demo 的默认路径已随 adapters 增加一级，显式 --project-root 的原合同仍保留。新增测试确实铺出正式目录层级，并走不传根参数的 init，核正确输出位置与没有 doc/doc；这是对此前默认入口遗漏的直接修复。

### 清扫退场

认可 `33dd9123` 按用户最新决定删除 sweep_story_src 及调用，删除零消费者 DRAFTS_DIR，保留 decisions/copyedit 的冻结职责。保留过程件与冻结依据是两件事，不需要再用保留名单决定谁可以留在本地。

除源码与相关回归外，本次在真实产物副本的 story-src 根下、嵌套目录和 drafts 中分别放入过程文件，使用当前 cmd_story 走 project/number/build/check/登记，退出 0，三个文件内容均保持不变。该结果证明本次正常登记保留过程件，不仅是源码里没有出现某个删除函数名。

**测试退役认可**：已取消的清扫行为不再保留“必须清走”或“特例免清扫”的用例；保留冻结与路径职责的测试正确。删除最后只剩无测试方法基类及失效清扫类的 test_story_allocate_render.py 合理。当前源码扫描只作为辅助约束；以后维护保留行为时，优先用上述登记闭环代替继续增加源码关键词锁。

## 关于“过程件不会给读者”与 Git

归档范围的判断成立，但应准确表述为：**按当前 archive 合同，需求系统只接收 story.md 和 review.md，过程件不经这条上传路径发送。** 已核对 Demo 的 cmdArchive 实际写入范围；内网实现也应遵守同一合同。它不等于文件永远只被本机作者看到，也不等于 Git 会忽略这些文件。

当前本仓 .gitignore 已忽略 doc/features/ 整层。目标仓若仅忽略 drafts，其余未被忽略的过程件可能被 git add 收入；这是目标仓的版本管理策略，不是 archive 上传行为，也不是清扫退场之后新增的自动提交动作。

**本轮不要求 adapt 一律忽略整个 story-src，更不恢复清扫。** 该目录还包含材料清单、决策与流程依据，目标仓可能需要留存版本。沿用目标已有规则及当前草稿忽略约定；若目标明确要求所有需求过程件不入库，再按该目标调整忽略范围，不用“不能归档给读者”推导新的全局 Git 策略。

## Q1：两份 02.1 的运行期边界

采纳“明确可核边界”，不采纳“form.note 必须一句”的句数限制。一句话也可以很长，多句短说明也可能更清楚；应核职责而不是句数。

实施判据如下，已同步主方案：

1. 三级细则仅为维护侧设计文件，交付脚本、Skill、任务包不读取或引用 test/story；不得把它逐行复制成固定二三级目录或待填 slots。
2. 交付面沿用现有 chapter.questions、form.note 与一份共用形式选择方法。questions 说明该章必须回答的问题；note 只说明本章必要的默认组织、切换条件和边界，不重复共用方法、整份细则表或其他章的完整要求。
3. 本步 slots 只保留流程总览图、术语/验收/交付物表等已明确的必要结构；附录继续原有五节及 A–D 投影。已确认必需标题通过原有 subsections 维护，不能因删除排版槽位一并丢掉。没有新的交付必要性，不将三级细则的可选内容单元升级为必有标题或形态。
4. 核真实草稿：不预生成全部可选小节、空图表、页面占位或每种形式各一份。动态二三级小节由作者依据材料组织，必要的内容问题在写前已经送达。
5. 核真实 check：等义的合法表达不因缺少已取消的标签而拒绝，必需图、验收、来源与投影缺失仍被发现。语义审查按读者问题检查遗漏和重复，不逐项匹配细则中的目录名。

这五项是结构与职责的检查清单，不新增一套运行期元数据或展示选择记录。

## Q2：行为证据待验时能否继续

**可以继续步骤 3–5，但应准确标为“实施评审通过，行为待验”。** 前提是 2.1 实现完整、相关离线与必要结构检查通过、独立实施评审未发现阻断问题，且供后续使用的作者指引与合同已经一致。仅“代码写完”或“单测通过”不满足这个前提。

A6 所需真实内网数据管理产物未提供时保持待验，记录缺什么证据和后续验收入口；不填 PASS，不宣称 2.1 全部效果或本轮需求交付完成。缺少这一类真实场景证据，不阻止已经明确依赖稳定代码与作者合同的后续实现。

无需为等待证据创建新流程状态机；在现有方案/评审状态中区分实施与行为即可。后续真实反馈若暴露共用合同的问题，回对应责任步骤修正，并复核受影响部分。

## Q3：预算如何登记

本次重新运行现有 measure：**9675**，分项为 prompts_md 1841、hooks_mjs 3009、data 778、scripts_mjs 2488、scripts_py 1559；当前预算文件 total 的 target/interim_ceiling 均为 **9800**。

确认按实际开工时的版本与读数登记，9675 是本次实测，不是永远固定的开工基线。主方案原“相对 +100/+30”为初稿估算，不作为额外强制上限。已有用户“规模允许合理增加”的决定继续生效，不为行数削减方案或验证。

9800 仍是当前已登记总上限，各类别也各有已登记上限；总量没超不表示某类别可以自行抬高。实施前根据实际改动估计峰值和完成规模并写清增减理由；超出已批准边界时按预算约定具名提请确认，不静默调高、不把测试排除计数作为绕法。本次不代用户签署新的数值，也不改预算文件。

## Q4：实施顺序

确认 **2.1 → 3 → 4 → 5**。本次默认根与清扫返修复核通过，之后按该顺序推进。步骤 4 基于 2.1 已调整的 story-write.md，不恢复旧标签；步骤 3、5 与其直接相交的投影/展示要求同步。2.1 的行为待验按 Q2 管理，不自行改变顺序，也不提前宣布后续步骤完成。

## 记录口径的小修正

99 交接里“stdout/stderr 全空就是环境”过于绝对。本轮 Node 无输出异常退出、同命令随后成功是已知事实，可以判断不是当时记录到的业务断言失败；单凭空输出不能对今后所有运行判定根因。交接应保持这个证据边界。

同样，“登记不动 story-src 里的任何东西”应理解为不清扫过程件，不是整个目录字节不变：流程契约和决策投影仍会按原职责写入。这是说明准确性问题，不阻断本次实现。

## 本次验证

相关测试 test_requirement_system.py 与 test_material_rounds.py 并行复跑：**90 passed，45.10 秒**。登记保留的独立复核现场：`C:/Users/kangz/AppData/Local/Temp/story-review98-slxfynig/review-evidence.json`，登记成功且根下、嵌套、drafts 三份过程文件均保留。

本次未重跑真实 CLI或全量四道门；98 中的 762 passed 是执行者报告的全量结果，不冒充本次复跑。未修改主仓实现与测试，也未提交。

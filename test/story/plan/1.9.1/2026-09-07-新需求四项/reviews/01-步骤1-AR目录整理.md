# 评审 · 步骤 1「AR 目录整理」

评审 2026-09-08。对象：提交 `009d30df`。方法：按 `steps/01` §5 逐条行为核——HEAD 的扩展加仓内 `doc/features/AR90006` 挪成新布局搭临时根，把消费者的动作亲自做一遍；离线四道门复跑。

**结论：通过，收口。** 三个常量改值、保留集扩上、四类消费者跟齐、⑮ 白名单判据在，离线全量绿。

## 1. 逐条

| # | 判据 | 我做了什么 | 结果 |
|---|---|---|---|
| R1 | 三个常量 | `story_flow.py:67/68` 的 `CONTRACT`、`ANALYSIS` 与 `import_sources.py:70` 的 `DOC_TARGET["AR"]` 都指向 `AR/story-src/`；`:523` 写进契约的路径从 `ANALYSIS` 拼 | 通过 |
| R4 | 保留集 | `sweep_story_src` 的 `keep` 含契约、分析件、`upstream.md`；`STORY_SRC_FROZEN` 仍是两件；夹具 `TheMovedInThreeSurviveTheStorySweep` 三条 | 通过 |
| R2 | 消费者 | `grep` 扩展与测试域，旧路径 `AR/story-flow.json` / `AR/init-analysis.md` / `AR/upstream.md` 零命中；`spec-rules.overlay.yaml` 五处、`story-chapters.json:32`、`materials.py:41`、四份 rules、SKILL 产物定位表都在提交里 | 通过 |
| R3 | 白名单 | `story-build check` ⑮ `AR_ROOT_FILES` 四个，`detail.json` 注明归对接层；根下放 `notes.md` → 「AR/notes.md 不该在这一层……把它挪进 AR/story-src/；目录不受这条限制」；只判文件 | 通过 |
| 验收 2 | 原消费者找得到 | 临时根：`status` 读到 `story_written` / `run_archived`；`check` 通过 10 章 | 通过 |
| 验收 3 | 登记后契约续写 | `reopen` 撤销 `story_src_digests` / `story_written_at`，三件仍在 `story-src/`，`status.next = run_round` | 通过 |
| 验收 7 | 四道门 | 747 passed / 195 subtests；失效形态 70 条 FAIL 0；`adapt-scan --check` 通过；framework 差异仍是 opencode 两文件 | 通过 |

⑮ 在每次 `check` 都判，不只在登记那一次——比方案更严，正确。

## 2. 备注

- 仓内 `doc/features/AR90006` 这类既有产物的契约里仍记着旧的 `analysis.path`（当时写的），不迁移是方案定的，不影响新一轮。
- 预算随步骤 2 一起看（`reviews/02` §3）。

# `step-05-full-chain-closeout`：全链退场与功能完整性验收

## 目标结果

02.4-05 分册落地：R18 删除或改正指向已退职责的实现注释与指令（`seeded` 参数、作者改机器投影、附录一句目的句加表格、writer 手写符合性表）及 §3.1 列的兄弟残料（`story-build.mjs` ③ 报错里的「质量与验收」、`requiredShapes` 的 `at === '*'` 分支、`DECISION_SHAPE` 与 `contracts.mjs` 的旧说明——后两者由 01/04 已改，此处确认）；按六条职责链反查每个被删函数/字段的调用方与被调用方；跑一次必要全量；复核并把三份 FEATURE 文档从 `test/story/plan/2026-09-11-Extension功能全集与机制归属/` 移到 `test/story/`，目标映射改成当前映射，更新相互链接与 `test/story/AGENTS.md` 入口；交回 R01–R23 完成表、D01–D03 返修表、六条链保留证据、实际规模变化。

## 前置与依赖

- 输入产物：step-02/03/04 的实际 diff 与 closeout；01 的评审。
- 真实依赖：step-02、step-03、step-04 已提交。
- 执行顺序：无。
- 就绪程度：可实施（收口步，无设计任务）。

## 必读上下文

| 路径 | 加载时机 | 影响的决定 |
|---|---|---|
| 共享入口（同 step-02） | 共享已加载 | 同上 |
| `test/story/plan/2026-09-07-新需求四项/steps/02.4-05-全链退场与功能完整性验收.md` 全文 | 本步新增 | 本步全部合同；§4 六条链不得丢的内容；§5 验证与映射启用顺序；§6 关闭凭据 |
| `test/story/plan/2026-09-07-新需求四项/steps/02.4-旧机制退场清理.md` §7/§8 | 本步新增 | 规模报告口径（分别报告六类）、完成条件 |
| 本目录 `reviews/step-02…04-review.md` 的 closeout | 本步新增 | 前四步实际删了什么、留了什么未验证 |
| `test/story/plan/2026-09-11-Extension功能全集与机制归属/FEATURES.md`、`FEATURE-MAP.md`、`FEATURE-CONTENT-MAP.md`、`02-旧机制退出范围.md` | 本步新增 | 待移回的三份与 R 清单；映射里的行号全部按实际 HEAD 重取 |
| `doc/extensions/skills/story/scripts/core/story-build.mjs` :1882、`requiredShapes` :2963–:2967、`renderSlot` JSDoc :2703 前、`appendixProjection` 内「附录的形态是一句目的句 + 表格行」注释、`verdictSkeleton` 前注释 | 本步新增 | R18 落点（行号按实际 HEAD 重核） |
| `doc/extensions/hooks/spec/post_check.mjs` :269 | 本步新增 | R18 落点 |
| `test/story/AGENTS.md` :9（用户未提交的那行）与 :13 | 本步新增 | FEATURE 移回后入口改指新位置；用户那处改动由维护者决定合入方式 |
| `test/story/TEST.md` §7 | 本步新增 | 必要全量的入口；Windows 下 `PYTHONIOENCODING=utf-8` |

## 修改边界

- 负责：R18 各处注释/指令；三份 FEATURE 文档的迁移与内容更新；`test/story/AGENTS.md` 入口；与前四步直接相关的旧指针/帮助/测试期望。
- 排除：新的关键词全仓禁令；全仓风格重写；任何新判据；Framework。
- 公共规则：四道门见 `TEST.md` §7。

## 关键决定

- 已确定（分册 §3）：对每个被删函数/字段向调用方与被调用方各追一层；被移动的代码计复用不计删除；测试仅引用旧机制不能续命，真实行为断言要迁移。
- 已确定（分册 §5.4）：只有清理完成且完整性复核通过后才移 FEATURE 文档；未完成前不移动、不宣称正式清单。
- 已确定（总览 §7）：若本轮未净减，回职责替换说明原因并审视；已净减但累计仍增长，如实保留整体目标未达成的结论。
- 设计任务：无。

## 验收

| 前置状态/触发 | 可观察结果 | 实际验证方法 | 完成阶段 |
|---|---|---|---|
| 全仓搜 `seeded`、「质量与验收」、`'*'` 分支、「一句目的句」、「writer 直接写」、`story-build init` | 交付面零命中或已改成现行职责说明 | grep + 人读 | 本步自动检查 |
| 前四步删掉的每个函数/字段 | 无新孤儿导出、无第二处继续维护同一规则、无动作丢失必要输入 | `M16-patch-residue` 等形态 + 逐项清单 | 本步自动检查 |
| 六条职责链 | 每条能答出功能目的、唯一承担者、已退旧承担者、正常/失败结果、对应验证 | closeout 表 | 本步自动检查 |
| 三份 FEATURE 文档 | 移到 `test/story/`；目标映射改成当前映射；行号按 HEAD；相互链接与 AGENTS 入口可解析 | 人读 + 链接检查 | 本步自动检查 |
| 必要全量 | 离线全量全绿；70 条形态 FAIL 0；adapt-scan 退出 0；framework 0 行 | `TEST.md` §7 | 本步自动检查 |
| 规模 | 六类分别报告；与 `d36edb70` 基线 11126 及 `0a177cf2` 的 9942 对比；未净减须说明 | `measure()` + closeout | 本步自动检查 |
| 真实 CLI | 由用户按 TEST 启动；未跑标「清理实现通过，行为待验」 | 后置，用户责任 | 后置检查 |

## 提交边界

- 纳入：R18 改动、三份 FEATURE 文档（新位置）、AGENTS 入口、相关测试。
- 排除：本目录文件；`FINAL_REVIEW.md`（gitignore 目录内，不入库，与 2.3 同）。
- 完成条件：验收全过；`approved_for_commit` 后提交；随后 `finish` 触发最终报告。

## 停止条件

- 六条链反查发现某项有效效果已无承担者时停下，回对应步骤修，不在本步补新机制。
- FEATURE 文档与实际实现对不上且不是行号问题时，先改文档再移，不宣称正式。

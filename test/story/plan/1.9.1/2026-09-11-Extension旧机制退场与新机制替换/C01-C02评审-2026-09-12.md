# C01/C02独立评审 · 2026-09-12

对象240031a0，基线ebdd317f；对照02、08、10、11及实际源码与测试。本次未改生产实现、未进入C03、未运行真实CLI。

## 1. 结论

**暂不通过。模块拆分方向成立，但作者输入、必要结构和起手预检没有一起闭合。** 新模块drafts有效53行、chapter-contract有效109行；原入口1555行属于11已声明中间态，不因仍超过500否决本包。

不能把“旧函数消失＋新模块小＋离线全绿”当成替换完成：旧作者指令仍在驱动执行，部分必要结构被误改，材料更新也未被写前预检发现。返修在本包原职责内完成，不增加下一层补偿机制。

## 2. R1 [P2] 作者仍收到旧流程和旧表达要求

- phases/spec.md:61仍教 `story-build.mjs init`，该命令已删除；必须与删除同包改为当前skeleton起手。
- drafts.chapterDraft指向story-write.md「十章各自怎么组织」，该节根本不存在。作业书未改，仍写“本阶段任务包一次给全”，而chapterSection已经删除。
- chapter.questions沿用旧值：功能说明仍只问页面、用户、页面状态，异常章只问真正异常。删除form.note也带走了部分有效作者目标，没有按02十章表接替。
- 作业书仍限制附录只有表和清单、同一事实绝对只讲一次、各支线各自成节，与本包放开的形式相冲突。

准确处理：本包先完成02中与C01/C02直接相关的逐章方法、questions、边界和起手入口；不能等C08才给本包正在被使用的规则。C04的copyedit退出和完整整稿接续仍留后继，不提前删除尚未接替职责。新指向必须真实存在，作者按正文即可理解无页面能力、正常受限、多对象管控和必要重现。

## 3. R2 [P2] 必要形态迁移改变了保护含义

本次直接调用真实chapterStructureProblems复现：

| 输入 | 实际结果 | 应有边界 |
|---|---|---|
| siblings=true，有“特性分工”“交接约定”H3及完整交接叙述，无表 | 报缺“约定”表 | 08只保留真实兄弟的交接说明，不强制交接表 |
| 流程章仅在某H3局部过程下放图，章首无总览 | 本局部检查零问题 | chapter-start应检查章首范围；总览关系是否完整仍归语义 |
| 验收只有“编号”一列表 | 本局部检查零问题 | 方案要求必要验收凭据；仅有任意编号列不能代替通过条件等必要信息 |

`structure.tables`丢失原定位信息，交付物表也可在章内任何位置被误认；requiredSlots对siblings=null还会强制有约定表，未知不等于确定适用。必要H3应进入草稿种子，否则仍是作者第一次在报错中学习固定标题。

准确处理：同一合同解释器保留必要H3的条件、表的适用位置及最低锚列、章首图的结构位置；不以固定行数、图种或表数量代替语义。表允许加列和合法措辞变化，不能要求整张表字面相等。补对应正反例并同时核种子与检查入口，代码围栏中的示例标题/表不能冒充正文结构。

本次structure替代旧form并未保留平行解析，本身可以接受；不要求仅为字段名改回slots。但须补齐上述等价职责，并在08/10统一数据合同，不能以“改了一个更短的schema”解释语义丢失。

## 4. R3 [P2] 起手比较两份旧摘要，没有验证当前材料；最终入口仍不同判

cmdSkeleton只比较materials.json.digest与flow最后一轮记录，二者都是落盘旧值。独立临时fixture正常complete后更改RR/prd.md、移除Story，再调用真实skeleton：**exit0且创建Story**。未处置的新材料没有被发现。

同一函数仅核Spec非空，未核后续种子/投影必需章节；decisions缺失时立即写入，尚未完成所有facts和Story输入检查，与预检后才写盘的合同不符。

最终check的⓪a仍将所有missing放notes，skeleton却阻断必需缺件，未实现08要求的同一必需性判据。既有注释“必备缺了拦”依然与代码不同。

准确处理：复用现有材料链对当前磁盘与flow基准的验证，不另建JS摘要或版本台账；来源必需性用一处规则供准备和最终检查调用，保留远程/本地与可选来源区别。Spec仅核本步真正消费的必要结构，不重跑Framework全套语义。将所有输入预检和内容计算放到首次写入之前。补真实材料更新、必需来源在skeleton/check同判及预检失败文件集合不变的用例。

## 5. R4 [P2] 原件“可读”校验不完整，另引入静默降级

originalArSource只用existsSync。本次将origin设为已有目录AR/story-src，返回该目录路径且problem=null。目录不是可读原文，不能告诉作者这是已校验原件。复用同模块现有readFlow而非再写一套JSON解析，明确原件须为可读普通文件；坏指针/目录/不可读不按成功返回。

flow-check同时把加载章节合同失败改成catch后空选项集，未保存读取错误；“加载期崩溃谁也接不住”的理由不成立。它会把配置问题伪装成所有关卡选项非法或未应用检查。将依赖读取放在真正消费处并返回具体错误，不保留空catch，也不为测试夹具缺文件放宽运行行为。

## 6. R5 [P2] 草稿命令没有完整保留执行上下文

chapterDraft生成chapter命令未带ctx.projectRoot。通过共享脚本的 `--project-root <另一仓>` 创建草稿后，照抄新命令会按脚本自己的默认仓运行。应带实际project-root，测试真正执行渲染出的命令，而不是只匹配命令字符串。

新shellArg的反斜杠转义不是PowerShell参数引用，也没有阻止双引号内的美元符号/反引号展开；注释宣称同时适用bash/cmd不足以覆盖本项目PowerShell。明确当前支持shell的实际引用规则并验证空格、引号、美元符号等参数原样传入，不新增通用命令执行框架。author图片任务同用该函数，须同时核该消费者。

## 7. 失效形态与测试处置

S09/S12/S13的描述仍包含“过程分支看不清、取舍不能对照、正常受限与异常混淆”这些有效读者风险。退出固定标题/两张表检查合理，但不能把语义风险整体retired后从后续评估中消失。保留既有观察/语义发现者及相应评价，纯形式配额才退役；不用再造同义台账。

本次相关五文件重跑 **211 passed / 57 subtests passed**。另对R2结构函数、R3真实skeleton、R4原件定位做针对性复现，证明已有绿测未覆盖这些边界。未重跑交回的全量807项及其它静态门，未评价真实模型结果。

## 8. 规模纠正与返修原则

ebdd317f基线为9764，不是表中9747；本次9729，实际净减**35**。完整字符493533→492886，减少**647**，不是增加621。story-build有效1696→1555，净减141；加两个模块162及flow-check等变化后scripts_mjs整体+44，不能用“旧入口删198”混算移动收益。

当前分组与有效行数允许保持；不为多减数字删上述正常功能。先补作者输入与确定性合同，同包退出旧指令/重复读取及错误判据，再测实际成本。全部返修在C01/C02完成，C03不代接本包遗漏。

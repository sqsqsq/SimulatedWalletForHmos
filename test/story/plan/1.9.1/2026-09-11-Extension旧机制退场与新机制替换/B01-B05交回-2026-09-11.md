# B段第一组交回 · B01–B05 · 2026-09-11

> 状态：已实施（含评审 R1/R2/R3 返修），等待复审。基线 A 段提交 17d6aed8；本组提交 e45d765f，返修提交见文末；branch story，未推送。
> 按 07 §1：B01–B05 一组交回；B06–B09 未启动；成文的 init、槽位、copyedit、章提交、投影、审查协议未动。

## 0. 职责归类口径（按用户最新裁定）

- B01/B02：同职责重复实现归一（路径解析、清单读取收进公共入口）；
- B03/B05：无消费者死计算/派生清理；
- B04：重复过滤 + 无生产者状态残留退出。

以上合计的净减不能统称「新旧机制并存退出」，也不按是否有测试定义旧机制。

## 1. 逐项：旧实现 → 唯一保留实现 → 已切换消费者 → 测试

净量不进本表（同文件多项职责不可重复归属），逐文件实测见 §3。

| 项 | 旧实现（33dd9123/A段） | 唯一保留实现 | 已切换消费者 | 测试处置 |
|---|---|---|---|---|
| B01 JS | spec/post_check.mjs:34 与 story-build.mjs:130 各自读 framework.config.json | paths.mjs `featuresDir/featureRoot`（绝对路径真源；配置值**字符串且 trim 后非空**才采用，否则 doc/features——归一随 R1 一并入公共入口） | spec/post_check：4 处调用改 `featureRoot(ctx)`，显示路径改 `relDisplay`（输出端统一 `/`）；story-build：`createContext` 改 `featureRoot(projectRoot, args.feature)`，ctx.featureRoot 字段不变 | 新增 test_features_dir_config.py（见 §2） |
| B01 Python | story_flow.py:145 本地 `features_dir` | import_sources.`features_dir`（trim 语义原样保留） | story_flow 主入口改 `import_sources.features_dir`；调用处约定不变 | 同上跨语言用例 |
| B02 | activeKnowledge 与 manifestDigest 各自解析 manifest | knowledge.mjs 导出 `knowledgeFiles`（顺序/重复项保持、不扫描不去重；形状错误出声；清单缺失/解析失败抛错，未登记或合法空列表才是无知识） | activeKnowledge 遍历其返回；manifestDigest 只换取清单部分，摘要算法（rel+\\0+CRLF归一+\\0、16位）逐字节同口径 | test_knowledge_use / test_contracts_single_source 既有断言守口径 |
| B03 | IDENTIFIER_SHAPE、ownIdentifiers、check 内未用 ownIds；baseLayerIds 及其导入；plan 的 resolveEntityRef 导入与 AUTHOR_DOC；spec 恒空 skipped；contracts 的 entityName 导出 | 全部删除；entityName 函数体保留（同文件内部调用）；spec 早退分支的真实 skipped 保留；①b H1 身份判据不受影响 | 无外部消费者（逐符号 grep 核过，含 test 域；M16 通过只是佐证，不单独作为无孤儿依据） | 既有负例/契约测试守行为不变 |
| B04 | flow-check FLOW_STATES 含 archived；story_flow SKIP_INBOX 后置过滤 | FLOW_STATES=['complete','story_written']（archived 是契约独立记录字段，非流程状态）；README 排除由 import_sources.scan_sources 唯一承担 | reached() 两个调用点只传 complete/story_written；`contract.archived`、reopen/材料变化留痕、`run_archived` next 保留 | 退役虚构 status=archived 测试一条；真实归档测试（test_material_rounds）保留 |
| B05 | parseIndexFile 派生 name/title；constraints 派生 appliesWhen/alwaysApplies；patterns 派生 coordinatorRole | indexes 仅保留 file；知识原文 frontmatter 不动；coordinator 合法性检查、facts.name、roles/optional_roles、domainTitle、规约 text 保留（constraint.name 未被 07 点名，按 03 §4 未点名内容保持） | 派生字段零消费者（逐字段 grep 全仓核过） | 既有知识测试守激活结果 |

## 2. R1 返修：路径对照回归（test/story/tests/test_features_dir_config.py）

黑盒跑真实 story-build（node 子进程）判别：夹具只在**预期目录**放流程台账，JS 归一到别处时台账判据先红；归一正确才走到「读不到 <预期>/AR/story.md」。

- 内部空格 `feat dir` 与首尾空白 ` " feat dir " ` → 同一 trim 后目录（两条 subTest）；
- 空串、全空白、非字符串（123/null）→ 默认 doc/features（四条 subTest）；
- 缺配置文件 → 默认 doc/features；
- **跨语言**：`python story_flow.py init`（配置 `" feat dir "`）先建工作区，JS story-build 在同一 trim 后目录读到它；带边缘空格的目录未被创建。

## 3. 数值（同一测量器 measure/classify/code_lines）

逐文件预算行净变化（B 编号只作归属，不逐项相加）：

| 文件 | 本笔净变化 |
|---|---:|
| hooks/plan/post_check.mjs | −1 |
| hooks/shared/contracts.mjs | −1 |
| hooks/shared/knowledge-use.mjs | −4 |
| hooks/shared/knowledge.mjs | −8 |
| hooks/spec/post_check.mjs | −11 |
| core/flow-check.mjs | 0 |
| core/lint-rules.mjs | −15 |
| core/story-build.mjs | −10 |
| core/story_flow.py | −11 |
| hooks/shared/paths.mjs（R1 返修归一语义） | +1 |
| **合计** | **−60** |

- 五类：data 778、hooks_mjs 2985、prompts_md 1760、scripts_mjs 2463、scripts_py 1548，总计 **9534**（e45d765f 时为 9533，R1 归一语义 +1 行如实入账）。
- 完整字符（含注释/空行）：e45d765f 478221；R1 返修后 478508。
- templates/plan.md 属安装器（classify 排除），不计预算收益。

## 4. 验证结果

- R1 后相关回归：test_features_dir_config（4 passed / 6 subtests）＋九个 B 组相关文件全过。
- 全量离线：763 passed / 221 subtests（−1 = 退役的虚构 archived 测试）。
- check_failure_modes.py：FAIL 0 / PASS 62 / 委派 8；M16 为佐证，无孤儿结论以 §1 逐符号消费者核查为准。
- adapt-scan --check：通过，exit 0。
- 改动 .mjs 全部 node --check，story_flow.py py_compile 通过。

## 5. 直接调用路径验证（07 §2 完成条件）

- 自定义相对目录（含空格 `feat dir`）：`story_flow init` 四件骨架、`round` 契约均落自定义目录，`status` 读到该契约；诱饵 `doc/features/B01FEAT` 全程未被读写。
- story-build 只读命令：自定义目录台账齐而 story 缺 → `读不到 …feat dir\B01FEAT\AR\story.md`，不含 doc/features。
- 真实 Spec hook（import guard 后真实执行主体）：gate 报文含 `产物：spec.md（feat dir/B01FEAT/spec/spec.md）`，判据对象全部在自定义目录。
- 缺配置默认目录：`读不到 …doc\features\B01FEAT\AR\story.md`，默认回退不变。

## 6. 返修记录（对照 B01–B05评审 R1/R2/R3）

- R1：配置归一并入公共 `paths.featuresDir`（字符串且 trim 后非空才用，否则默认），不恢复调用方本地解析、未改 extensionRoot；新增 test_features_dir_config.py 六类对照。此前该函数只用 `??`，空串/空白值与 Python 分叉（评审五组对照表复现过）。
- R2：knowledgeFiles 注释改为「清单文件必须可读；未登记 knowledge 或合法空列表表示没有激活知识」，实现不变。
- R3：本文件 §1 净量列撤除、§3 改逐文件去重表；M16 表述降为佐证。
- 07 §2 已由评审同步精确语义；本文件同步口径归句（§0）。

## 7. 未验证与边界

- 真实 CLI 未运行（B 段无此要求）。
- G1 未裁决：B06–B09（07 §5–§7）为下一组，完成后按 04 §2 交 G1 同口径表。
- relDisplay 使 spec hook 报错中的 spec.md 展示路径统一为正斜杠——输出文案微变，判据语义不变。

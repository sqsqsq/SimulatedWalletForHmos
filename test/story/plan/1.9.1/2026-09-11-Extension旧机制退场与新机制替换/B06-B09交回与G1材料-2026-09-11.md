# B段第二组交回 · B06–B09 与 G1 材料 · 2026-09-11

> 状态：已实施（含评审 R1–R4 返修，提交 706ebfe2），独立复审及G1通过，见 [G1裁定](B段复审与G1裁定-2026-09-11.md)。提交链：17d6aed8（A段）→ e45d765f（B01–B05）→ d1a0ece2（B01-R1返修）→ 3f98307c（B06–B09）→ 706ebfe2（B06-R1返修）；branch story，未推送。
> 当前裁定：B段通过，G1通过；以下主表已按706ebfe2实测与最新规模裁定校正，历史意见见原评审。

### 返修记录（对照 B段返修与G1评审 R1–R4，提交 706ebfe2）

- R1：planPatternChoices 与 specPatternHits 采用同一无候选身份语义——「无候选」说明行两侧都不进实际模式集合，反向检查不再误报「plan 凭空新增」；两条方向性检查（spec 候选 plan 漏核、plan 新增真实模式）保留。新增真实真源无候选正反例（不误拦本项全部错误串）。
- R2：planPatternChoices 返回 `{choices, problems}`，plan 侧同 (单元, 候选) 两行报「写了两行」且不后写覆盖前写（前行的不选空理由仍被点名）；调用方接住；遍历中未使用的 row 变量删除。补 plan 重复 + 正常双模式用例。
- R3：overlay 换算示例删除（30 分钟≠180000ms 的示例本身有误），保留「单位换算有真实依据才成立」原则；不新增换算脚本或词表。
- R4：facts/README 原禁止句「不要选进新设计」删除，替换为一条完整说明（已确认不存在的能力不可当作已有能力复用；有需求/设计依据的新建应在方案明确），不再两行并存打架。
- 模板同步：plan-sections.md「逐个候选单元一行」改为「逐个（单元 × 候选）一行；无候选是说明不是模式行」。

## 1. B06–B09 逐项：旧实现 → 唯一保留实现 → 消费者 → 测试

| 项 | 旧实现 | 唯一保留实现 | 直接消费者 | 测试处置 |
|---|---|---|---|---|
| B06 | spec 自行逐行解析 acceptance；contracts.knowledgeCriteria 单值 Map 覆盖前项；ut 只查该单条且缺 id 静默放过；解析失败被当空集合 | contracts 新增 `acceptanceEntries(acceptance, sections)`（缺集合=[]、非数组/非对象成员报集合与序号、knowledge_rule 缺省为普通业务验收、存在须非空字符串、列表不拍平）与 `knowledgeCriteria` 返回 `{byRule(数组), problems}`；**旧单条接口退出** | spec `acceptanceCoverage` 改读 readAcceptance+entries（只桥 criteria）；ut/testing 接住 error、按 byRule 逐条核覆盖与缺 id、保留「产物未建/已建零引用」之分；ut「改成 device」文案改为回 plan 核实际验法 | 新增 8 条判别（见 §3）；旧断言无保留单条接口者 |
| B07 | coverageProblems 无条件要求 patterns 有行；specPatternHits/planPatternChoices 按单元单值 Map（后写覆盖前写、plan 忽略候选列）；renderSkeleton 零模式已不摆占位（基线已实现，保持） | 零在册模式时 `patterns: []` 合法（有在册仍须判断）；两函数改二层 `unit -> Map(pattern -> row)`；spec 同 unit+candidate 重复报错；plan 候选列参与身份；plan 加 spec 未提出的候选报「回 spec 补登记」；NO_CANDIDATE 语义不变 | plan 6b 交叉核对按 (单元, 候选) 逐对：缺行、不选空理由、plan 多报三类 | 新增 6 条（§3）；renderSkeleton 不改 |
| B08 | design-patterns 三文档指 `project_knowledge.pattern_applications[].instance`；plan/author「下游不读任何知识」；ut/author「回 plan 把 verify 改成 device」；facts/README 缺新建口径 | 三文档改为：选型落 plan、采用落 contracts 的 `files`（pattern/role）、实例即正常实体不设旁账（保留实例所有权与组合含义）；plan/author 收窄为「不重做已定适用性与选型，coding 按已选模式读实现篇与必要 facts」；ut/author 改「回 plan 核实际验法，不为过检查改分派」；facts/README 增「不存在的能力不能当已有复用，新建须进入方案」 | 阶段作者页、任务包渲染、模式实现篇读者 | 知识正文一字未删的核对：三文档仅换落点行；知识变更预算外单列 |
| B09 | scanNumericSources 按数值+单位在 SR/RR includes 字面命中裁定「伪造出处」（UNIT_ALIASES 独占常量）；上游文本读取仅为它服务 | 保留来源类型/位置结构门（未标来源仍报）；删除字面裁决分支、UNIT_ALIASES 与上游文本读取块；真伪/换算归 spec overlay 既有判项 `spec_ext_conclusion_validity` 的数值依据（判业务量指向、单位换算依据、身份与取舍；同数字不证明同含义），不新建判项或台账 | spec hook、spec overlay verifier | 新增正反例（§3）：标了不被拦、未标仍被点名 |

作者包样例：`spec/author.mjs` 的 AC-K1 改为 `AC-1`（既有 AC 数字形态）；make_case_assets 与失效形态夹具里的 AC-K1 是 Case 上游 Story 的原始编号，属另一套身份，未动。

## 2. 数值

| 类 | N0 | B01–B05 后 | B06–B09 后（N1） |
|---|---:|---:|---:|
| data | 778 | 778 | 781 |
| hooks_mjs | 3009 | 2985 | **3022** |
| prompts_md | 1760 | 1760 | 1761 |
| scripts_mjs | 2488 | 2463 | 2463 |
| scripts_py | 1559 | 1548 | 1548 |
| **总计** | **9594** | **9534** | **9575** |

- B净变化（去重）：B01–B05 −60（R1 归一 +1 在内）；B06–B09含返修 **+41**；B 段合计 **−19**。完整字符 481879（A 段 481000）。
- hooks_mjs当前3022，原峰值3010超12行；G1依用户授权具名接受，进行中峰值已登记3030。C00的scripts_py峰值登记1750，总峰值9800保留；总完成优先目标8675，合理例外须核职责。
- data较A多3；prompts_md较A多1（候选模板写前说明）；完整字符较A多879，不把预算行净减等同字符净减。

## 3. 新增/迁移的测试

- test_neutral_knowledge `TheAcceptanceBridgeKeepsEveryEntry`（8 条，真实 ut/spec hook）：同 rule AC-1/AC-2 只覆盖第二条必报 AC-1；全覆盖通过且普通业务 AC 不误报；缺 id 逐条点名；rule 列表拒绝不拍平；坏 YAML 报「解析失败」不当空集合；boundaries 计入 UT 桥但 spec 桥只在 criteria；数值标了不被拦、未标仍点名。
- test_plan_pattern_crosscheck `MultiCandidateUnits`（7 条，真实 plan hook）：双候选一采一拒、双采用；缺一个选型点名；spec 重复点名不覆盖；plan 加未提出候选点名；无候选说明行两侧同义不误拦（真源级正反例）；plan 重复两行点名且前行空理由不被覆盖。
- test_knowledge_use：零在册模式空列表合法（+1）；有在册空行仍点名（+1）。
- 全量：787 passed / 226 subtests / 1 failed——失败为调整前的hooks_mjs峰值子项；本次预算更新后专用回归已通过，未重跑全量，非功能回归。check_failure_modes FAIL 0（A03 已修）；adapt-scan 通过；改动 .mjs 全部 node --check。R1–R4 返修后四个判别测试文件 73 passed / 6 subtests。

## 4. G1 同口径表（04 §2）

| 项 | 事实 |
|---|---|
| N0 / N1 | 9594 / 9575（同一测量器 measure/classify/code_lines；源文件、知识/安装器/测试变化已分别列） |
| B净变化 | **−19**（N1−N0，按 B 编号去重；rules.md 81 行计 A 一次；移动复用不重复记） |
| C目标净变化 | 相对优先目标 **8675−9575 = −900**；不是要求靠删正常功能填满900行，合理例外按G1/G2裁定 |
| 功能保持 | 知识读取/摘要/激活、验收桥接、模式候选与选型、路径解析、状态机区间均经正反例验证；测试数变化均有行为对应（+15 判别、−1 虚构） |

### C段规划区间（非已实施测量，不作最终额度）

| 包 | 主要旧承担者（实测行块） | 接替新增 | 预计净 |
|---|---|---|---|
| C00 | 基线 cmd_complete 无候选/留存 | scope_step、候选校验、原 AR 留存与重试（06：历史净增百余行） | **+160～+190** |
| C01 | cmdInit 35 行；chapterSection+leadFigure ~40；DECISION_SHAPE 指路 | chapterDraft 渲染、decisions 形状守卫、来源必需性共享 ~+35 | **−40～−60** |
| C02 | ⑪ 形态守恒 81 行段内 slot/配额；slotCondition 30＋renderSlot 35 收窄；⑫c 段数；story-chapters.json form 类 ~176 raw | 必要 slots 子集保留 | **−120～−180** |
| C03 | check原检查提取为共用函数，原本没有两份可删 | chapterProblems 共用 + 首屏 NEXT/INPUT/RESULT | **+15～+30** |
| C04 | COPYEDIT_ROWS 11＋用途；⑫d 26；story_flow 台账 copyedit；作者页七行自证说明 | 无（02 §4.4 方法在 prompts） | **−55～−80** |
| C05 | ⑦ 50→薄比较；⑫b 41→共用比较；review 字段核与渲染两处 | 只读机器区比较＋review-render 共享入口 +15~20 | **−50～−85** |
| C06 | ④ 88 行段内副本特权/同字节重复任务；proseBlocks 第二段配额 | imageSection 按身份一图一任务（重写，净≈0） | **−25～−45** |
| C07 | 任务书另一套方法/结果示例 | 全文内联一次 +5、reviewVerdict +8 | **−10～+5** |
| C08 | story-write.md 289→六节 ~150-180；phases/spec.md 126 −10~20；SKILL/规则页/README 同步 | questions改写与boundary新增计入成本 | **−110～−170** |
| C09 | 无孤儿后收口 | — | **0** |
| **合计** | 按各包区间相加，重叠职责只算一次 | C00按历史修复规模校正；raw行不当预算行 | **净 −455～−175** |

### G1裁定

B段实施通过；G1依用户允许合理实现适当超限的原则通过，见独立裁定。上述C区间以N1=9575计，约对应N2=9120～9400，与8675优先目标差445～725；不是已获批最终额度。逐包实施时核实际新增、唯一承担者和旧机制退出，G2统一评估合理例外。

## 5. 未验证与边界

- 正常CLI未运行；下一包为C00，未提前实施。
- hooks_mjs及C00的进行中规模由G1具名登记，预算专用回归5项、16项子测试通过；此前全量结果不冒充调整后的新全量。
- B08知识正文变化与测试域不抵扣机制预算；B06–B09含返修完整字符较d1a0ece2增加3371（481879−478508）。

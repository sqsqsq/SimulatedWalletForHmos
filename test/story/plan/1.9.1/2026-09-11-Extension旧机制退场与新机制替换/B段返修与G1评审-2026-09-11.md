# B段返修及G1材料独立评审

对象：d1a0ece2（B01返修）、3f98307c（B06–B09）。本次核diff、作者输入、正常/异常合同并运行相关离线；未修改生产实现，未运行真实CLI。

## 1. 结论与规模原则

**B01–B05返修通过。B06–B09暂不通过，原因是下述正确性缺口，不是超过5行预算。G1暂不关闭。** 修完本组问题并更正估算后再交G1，不恢复旧函数、不另建协议。

按用户本轮明确要求：阈值用于促使尽可能删除被替代的旧机制，不得为凑数删除正常功能。明确合理且没有更简单完整替代的实现允许保留，必要时允许适当突破阈值。8675保留为优先收敛目标，不再作为不问职责的绝对否决线；9675仍是固定比较基线。

本次多AC完整覆盖、正确解析失败、候选逐对处理有真实职责，不能为了hooks_mjs多5行而删掉。超限应按正确实现复算、记录保留职责和更简单替代为何不足，再具名调整预算；不能把任意现值直接变成目标。当前尚有缺陷，先修正确性，不提前给未收口实现签终态规模。

## 2. R1 [P2] “无候选”被当成Plan擅自新增的模式

`hooks/plan/post_check.mjs`：specPatternHits跳过“无候选”，planPatternChoices却把它收入二层Map；新反向集合比较因此认为它是Spec未提出的模式。

复现：knowledge-use中U的candidate=无候选，Plan同U写“无候选／不选／不适用依据”，真实Plan hook报：

> plan 的设计模式选型表给「U」写了候选 无候选，spec 没有提出它

这是合法的零候选说明，不是新增模式。准确修法：两侧采用相同的无候选身份语义，说明行不进入实际模式集合；空候选不能让本来真实存在的候选漏核。保持“Spec有真实候选而Plan遗漏”与“Plan新增真实模式”两条检查。

现有test_no_candidate_rows_do_not_trigger修改的是旧Spec展示表，真正的knowledge-use真源未改，且只排除两个旧错误串；没有覆盖新反向检查。新增或迁移到真实knowledge-use无候选，并对本项所有错误断言不误拦。不要要求模型删掉合理说明来过检查。

## 3. R2 [P2] Plan侧重复二元身份仍后写覆盖前写

`planPatternChoices`的 `byPattern.set(pattern, ...)` 没检查已有键；只有Spec侧实现了重复检查。07要求同一unit+pattern重复报错，两侧均适用。

复现：Spec U/decision-tree一条；Plan两行同U/decision-tree，先“不选且空理由”，再“采用且有理由”。真实hook没有重复错误，也没有空理由错误，前行已经被覆盖。不能把这种结果称为逐条检查完成。

准确修法：Plan读取返回choices及problems，遇重复同一对报告并禁止覆盖；调用方接住。保留不同模式同单元的组合。现有重复测试只造了Spec重复，补Plan重复及正常双模式；不新增第三份解析器。遍历中未使用的row变量顺带删除。

## 4. R3 [P2] 数值审查示例本身换算错误

`rules/spec-rules.overlay.yaml`的新示例把“30分钟”与“180000ms”作为有依据换算，实际180000ms是3分钟，30分钟为1800000ms。该文字直接交给语义审查者，与本次准确性目的相反。

删除这段具体数字示例即可，保留“单位换算有真实依据才成立”的原则；如保留例子就改成正确数值。不要为此新增单位换算脚本或词表。B09退出字面includes裁决、保留来源格式并交语义审查的方向成立。

## 5. R4 [P2] facts原禁止句未被替换，新增说明与其冲突

`knowledge/facts/README.md`仍保留“无X（已核对）……不要选进新设计”，下一行才新增“本轮确需新建的……进入方案”。模型仍会同时收到禁止新设计和允许新建两种相反含义。这正是本轮要避免的保留旧规则后叠一条补充。

用一条完整说明替换原禁止句并删除新增的重复行：已确认不存在的能力不可当作已有能力复用；有需求/设计依据的新建应在方案明确。保留用前核实和真实知识，不改业务事实。这是B08原定范围，不是追加需求。

## 6. 其它实现判断与测试边界

B01公共featuresDir已补trim、非字符串/空值回默认；extensionRoot未扩大修改，缺清单注释与分项减量口径已纠正。

B06共享读取、一rule多AC、Spec只认criteria、UT/testing保留criteria+boundaries、错误不吞的方向和消费者接线成立。acceptanceEntries目前只返回带knowledge_rule的合法条目，当前消费者也只用于知识桥接；这是比原通用entries合同更窄的实际职责，不要求为了方案字面再扩成通用验收库。已在07明确该接口范围，避免未来调用者误当全量验收。

B07作者模板还写“逐个候选单元一行”，返修时同步为每个真实unit+candidate一行；无候选是说明，非真实模式。不让新脚本成为首次教作者多候选格式的位置。

本次四个测试文件（features_dir_config、neutral_knowledge、plan_pattern_crosscheck、knowledge_use）重跑：**71 passed / 6 subtests passed**。另用现有临时夹具实际运行Plan hook复现R1/R2；它们是在已有测试全绿后发现的缺口。未把夹具整体其它判项通过与否混作这两条规则结果，也未声称真实模型效果已验收。

## 7. G1估算需纠正，不能靠删功能兑现数字

交回表B01–B05后hooks_mjs仍写2984，而总计9534已包含R1多1行，该格应为2985。

C00–C09逐行区间按表相加得到**−515～−235**，不是合计行的−540～−380。以N1=9567计算，对应N2=**9052～9332**，与8675的差额为**377～657**。这是对交回估算算术的更正，不是本评审重新测得C的可靠实现预算。

还需纠正估算依据：C03是提取原检查并增加提交入口，原本没有两份局部规则，不能计两份删除；C01新增boundary并非基线已有；C02必须保留08的必要slots，不能拿全部176 raw行直接当预算行删除收益；各包重叠删除只算一次。

按最新规模原则，不再仅因无法预证≤8675否决G1。G1应核：B功能闭合、旧重复已退出、C完整实现及替换范围明确、成本估算可信；超目标部分逐职责论证有必要且无更简单完整替代。证据成立可具名接受合理例外，保持追踪目标差额；证据不成立则继续简化，不能只写“合理”就免审。

当前G1待R1–R4及估算更正后再审。没有要求为达892行净减去删正常验收桥接、源图保护或其它现行功能。

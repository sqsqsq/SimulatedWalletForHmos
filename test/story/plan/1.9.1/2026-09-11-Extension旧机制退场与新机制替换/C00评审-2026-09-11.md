# C00独立评审 · 2026-09-11

> 最新复审：ebdd317f已关闭R1，C00通过，见§8。下一包按新增10分册实施C01/C02，不沿旧大文件布局直接继续。

对象f9940d5c，基线706ebfe2；核08 §1、实际移植与测试。本次未改生产实现，未运行真实CLI。

## 1. 结论

**C00暂不通过：最后一段提交失败无法按同命令恢复。** 这是本包既定恢复合同缺口，不是因为净增172行或需要再拆文件。先修R1，再收口C00；不为解决它恢复2.3索引或增加并行事务台账。

候选独立落盘、留存原AR、范围判定提取、通常重试与其它材料变更拦截的方向成立。移植范围没有带回template/prepare等2.3机制。

## 2. R1 [P2] materials已刷新、流程契约未保存时，幂等重试失效

位置：story_flow.cmd_complete的 `materials.refresh`、`recorded.digest != base` 早退及最后 `save(feature_root, contract)`。

实际顺序：留存原件、覆盖AR、刷新materials.json后，才保存flow的新基准与complete。此时如果最后save失败，磁盘上materials是新基准，flow仍是旧基准。重跑命令在识别本次已完成写入之前，就因两个基准不同退出。

本次用test_s4_commit.S4Case正常走完前置，仅将最终save模拟成抛OSError，然后解除模拟，用真实CLI重试相同候选：

```text
首次：OSError simulated contract write failure
重试：exit 1，材料清单与本轮登记的基准对不上：先跑story_flow.py round...
磁盘flow状态：in_progress
```

最后save还在提交异常捕获范围外，因此首次失败也没有约定的“已完成动作／未收口／重跑同命令”提示。不能要求作者按错误指路开新轮；那会把自己的派生稿重新当作材料起点。

准确修复要求：

1. 将最终契约保存纳入提交失败处理，区分哪些写入已完成，保留原输入和候选。
2. 重试允许识别“materials刷新成功但flow尚未保存”的合法中间态。复用现有原AR留存、初始化骨架、已登记派生稿身份与digest_with；按旧flow基准证明除自己提交外的材料未变，再刷新并保存。不能简单取消清单/基准比对或无条件接受新清单。
3. 加此断点的测试：恢复后同一轮complete，origin仍指真实原件，不新增round、不把候选留成上游；同窗口另改RR/SR仍须拦。沿已有临时夹具与mock即可，不建新测试设施。

现有21条S4测试覆盖留存/覆盖中断，但没有这个刷新后、最终保存前的断点，因此全绿不证明整个提交顺序可恢复。此缺口也存在于历史移植方案，维护方案未把最后窗口逐项写清，我已同步08。

## 3. 原输入消费者：允许分包，但不能称全部完成

交回将08 §1.3解释为全部归C07/C08，与原方案并不一致。原件指针只落盘不被读，尚不能证明原始内容持续进入写作与审查。

按当前分包明确责任：C00只闭合留存/提交/恢复；**C01**作者输入接origin，**C06**源图与材料目录接origin，**C07**reader接origin。C08负责相关文字同步。08与交回已同步该分工；C00不能声称§1三节均已完成，C整链未接齐前不发布或跑CLI宣称效果。

这不是要求C00现在提前重构全部消费者，也不允许其后忘记接线。原件只有指针的中间态有明确后继与验收。

## 4. 两处注释随返修校正

- is_ar_skeleton仍写“后面的来源索引”，本轮没有2.3来源索引；改为实际作者/审查读取原件的职责。
- contract.design快照注释说“不作校验用”，但prior_ar及cmd_complete已用sha256识别登记派生稿。改为说明其身份用途，以及不能拿归档后的当前AR直接作冻结比对；不删除实际校验。

## 5. 独立验证与规模

相关四文件test_s4_commit、test_material_rounds、test_spec_story_gate、test_mechanism_budget重跑：**102 passed / 16 subtests passed**。另执行R1断点复现，确定重试失败。diff空白检查通过；实施者全量808项及其它检查没有在本次重复运行。

实测总量**9747**：data781、hooks3022、prompts1771、scripts_mjs2463、scripts_py1710；完整字符**492165**。与交回相同，净增172在既定范围内，当前无需为数字删功能或抬预算。

## 6. 下一步拆分约定

C00本次不强制拆文件。R1修复并通过后，**C01/C02启动前必须先完成并评审其按职责拆分细则**：入口只保留路由，各功能模块点名接口、直接消费者与旧位置退出；不先全量搬函数、不留双实现。原08尚未给出这一新增目录合同，执行者不能直接按旧C01/C02开始拆分，也不自行选目录结构。本维护会话负责在放行下一包前补齐方案。

## 7. 6d1b1549复审：R1只修了有本轮留存件的一支

**仍不通过，沿原R1返修，不另开新问题或新机制。** save已正确移入异常处理，两处注释已更正；有本轮真实原AR留存件时，刷新后保存失败可以同命令恢复。

但新增恢复识别只问本轮keep文件是否存在。原合同还有另外两种合法身份，它们本来就不生成本轮keep：

| 原AR身份 | 是否有本轮keep | 刷新后save失败再执行相同complete，本次实际结果 |
|---|---|---|
| 真实上游原件 | 有 | exit0，同轮收口 |
| init空骨架 | 无，且明确不应留存 | exit1，仍要求round |
| 上轮已登记派生稿，本轮沿旧origin | 无本轮rN，沿更早轮次原件 | exit1，仍要求round |

三种均用现有S4Case走正常前置，在最终save位置模拟OSError，再解除模拟并调用真实CLI重试；第二、三行均已复现，不是推测。

根因不是“又缺一个特殊判断”，而是同一原输入身份职责拆成两份：prior_ar原本认识三类，新增的清单已刷新恢复分支只认识keep。应将已有三种身份的解析归到同一处，供刷新前后恢复共用；提供可验证的原AR摘要与origin，用digest_with与旧flow基准比对其它来源。正常缺origin（空骨架）不等于身份未知。不得为了让分支通过把空骨架或派生稿留成上游，也不要在新分支再复制两份身份判据。

返修验收：三种身份均覆盖最终保存失败后同命令恢复，轮次数不变、原件不变、origin准确；同窗口的RR/SR真实变化仍拦。缺失或无法证明身份仍停，不通过简单取消基准比较放行。

本次相关三文件回归 **100 passed / 16 subtests passed**，仍漏了上述两支；不是全量效果已验证。实施者报告规模9752/峰值9800，本次不因规模否决，未修改生产代码或预算。

上轮我也应把同一断点与三种已声明身份组合核查，而非只验证真实原件一支。方案已把这组完成条件写明确；本次要求收敛身份解析职责，不继续叠特例。

## 8. ebdd317f最终复审：C00通过

ar_input_identities现在统一枚举本轮留存、初始化骨架（两种行尾）和已登记派生稿。prior_ar的origin定位与刷新后未保存状态的识别共用该枚举；前轮只认keep的恢复分支已被替换，没有在两个入口各复制骨架/派生稿判断。

恢复识别仍须用候选原摘要代回live材料复现旧flow基准；没有取消其它材料变化检查。空骨架的origin=None与身份未知分开处理；已登记派生稿沿旧origin，不把本轮候选另存为原材料。最终save仍在提交异常处理内。

本次独立运行test_s4_commit、test_material_rounds、test_mechanism_budget：**102 passed / 16 subtests passed**。新增两类恢复、已修的真实原件恢复及材料变化反例均在该集合。未再次运行实施者报告的全量812项、失效形态或适配检查，未运行真实CLI。diff空白检查通过。

实测总量**9764**，scripts_py **1727**，其它四类与交回一致；完整字符**493533**。C00及返修累计+189，在已有进行中预算内；没有为数字撤掉恢复职责。原件消费侧仍按C01/C06/C07后继接线，C00通过不代表成文整链已完成。

按用户此前要求，已新增 [C01/C02职责拆分与实施接线](10-C01-C02职责拆分与实施接线.md)，在该包动手前给出两个功能模块、入口职责、接口、消费者、同包退出和测试迁移。可以进入C01/C02，执行者须读10及08对应节；本次未实施拆分。

# A段独立评审 · 2026-09-11

对象：提交 `17d6aed8`，父提交 `33dd9123`；对照01回退合同、A段交回及实际Git对象、文件与备份。本次未修改Extension、未启动B/C、未运行真实CLI。

## 1. 结论

**A段通过，无实现阻断项，可以进入B段。** G1在B完成后裁决，G1通过才进入C；不能将G1作为启动B的前提。

A完成只说明回退基线及指定保留项成立，不说明旧机制清理完成，也不说明2.1/2.2的行为效果达成。S4缺口仍由C00负责，不在本次补实现。

## 2. 实际范围与保护件

| 检查 | 本次核实 |
|---|---|
| 提交基线 | HEAD=17d6aed8，父提交准确为33dd9123 |
| Extension差异 | 相对33dd9123只有rules/rules.md与story-adaptation/templates/plan.md两项删除；没有其它脚本、合同、提示词修改，未带回2.3/2.4实现 |
| Framework与Knowledge | 分别相对33dd9123核diff，均为空；不是只用 `git diff HEAD` 的干净工作区代替版本比较 |
| 保护tag | story-before-rebuild-20260911-171937可解析，指向5203ca30 |
| stash | f0a3f2ae可读；相对其第一父提交确含story-build.mjs、test/story/AGENTS.md、test_entry_switch.py三文件；旧生产/测试改动未取回 |
| 仓外备份 | recovery.json、design、spec可读；1048份design和15份spec逐文件按字节比对，当前同路径均存在且一致 |
| 保留资料 | 三份release资料、TEST、两份AR90006 Story文本、五张图片共11文件，与保护tag逐字节一致 |
| 特许资料差异 | AGENTS相对tag只增本轮正确入口；golden README只删未保留的两项2.3目录引用。基线AR90004/Review金样与公共图片无改动 |

Git本地记录与备份足以核回退恢复材料；本次未查询远端实时状态，不把本地工作区干净当作“证明没有推送”的证据。

## 3. 两项删除与补丁的职责

rules.md是当前送达路径之外的同义副本，Story、Spec与review_reflow已承担其有效方法；删除后议题定义的一致性检查保留两处实际送达面，失效形态两处出处指向现存方法。不是因为“没有测试”而删除。

adapt的plan模板属于已取消的旧适配方案流程。当前轻量化升级入口不生产或消费该方案模板，本步按明确保留删除项处理；不将安装器预算外删除算为本轮机制减量。

run_multi_case只移入live_pids的一行终态过滤，未整份恢复新版运行器。新增两条测试确实区分：终态记录的复用PID不拦、未终态存活PID仍拦；所有删除仅在测试自建临时目录发生，进程探测被mock，不清理实际suite。

金样测试只增加约定保留的七项登记及摘要，未改旧金样期望或降低其保护。新金样用于后续效果比较，不作为当前作者的输入答案。

## 4. 本次复算与验证

| 项 | 本次结果 |
|---|---|
| 预算行 | data 778；hooks_mjs 3009；prompts_md 1760；scripts_mjs 2488；scripts_py 1559；总计 **9594** |
| 固定原始基线与差额 | 9675−9594=81；差异对应rules.md，不能在B重复记收益 |
| 完整字符 | 同预算文件范围含注释/空行，**481000**，与交回一致 |
| 相关离线重跑 | test_multi_case_cli、test_writing_flow、test_golden_sample、test_mechanism_budget：**88 passed / 47 subtests passed** |
| 失效形态重跑 | 70条有效形态：**FAIL 0 / PASS 62 / 委派8**；未指定真实Story产物，不能作为行为验收 |
| 适配只读重跑 | adapt-scan --check --target . --package .，退出0 |

本次pytest初次在收集前因旧系统pytest临时目录WinError 5退出；换独立workspace临时目录后按原并行测试命令通过，没有修改测试或权限规则。交回中的764项全量结果是实施者报告，本次没有再全量重跑，不混称为本次独立实测。

## 5. 交回与入口的文字纠正

已直接纠正A交回两处：开头“G1前不启动B/C”改为“A通过后可进B，B完成后G1，G1通过才进C”；stash表中的test_story/AGENTS.md改为实际test/story/AGENTS.md。未改实施者原始测试数或实现记录。

总览更新A的实际状态，撤下仍写“HEAD未回退”的现时表述。后续按07实施B01–B09，按04交回G1；本评审不授权提前实施C或启动真实CLI。

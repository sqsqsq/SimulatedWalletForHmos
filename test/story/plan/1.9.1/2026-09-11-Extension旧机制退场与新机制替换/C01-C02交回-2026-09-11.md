# C01/C02 交回 · 起手、草稿与章节合同按职责归位 · 2026-09-11

> 状态：已实施，独立评审暂不通过，见 [C01/C02评审](C01-C02评审-2026-09-12.md)。下文保留实施者交回，当前数值与差额以评审实测为准。本包提交 240031a0（前序 ebdd317f）；branch story，未推送。
> 合同：10 分册（拆分与接线）＋ 08 §2–§3（功能合同）＋ 11 分册（目录与规模）。未启动 C03；不冒充章提交已有新检查。

## 1. 逐项：旧函数/判据 → 新唯一文件 → 全部消费者 → 旧位置删除

| 旧对象（story-build 入口） | 新唯一归属 | 消费者切换 | 旧位置处置 |
|---|---|---|---|
| draftPath / chapterDraft / writeDrafts / DRAFTS | story/drafts.mjs | cmdSkeleton 调 writeDrafts；无其它消费者 | 入口同名函数删除，不留包装器 |
| chapterSeed（真源打底） | chapter-contract.chapterSeedRows | drafts.chapterDraft 调用 | 入口删除；材料清单/术语仍由入口解析后经 facts 传入 |
| chapterSeed 的表渲染 | renderTable 迁 chapter-contract | 入口（appendix 投影、规约域表）改 import 同一导出 | 入口副本删除 |
| slotApplies / slotCondition 多条件实现 | 退出；siblings 判定收窄为入口 `siblingsFact(ctx)` 产生 facts.siblings，requiredSlots 消费 | chapter-contract.requiredSlots | 双函数删除；decisions/ui_images 分支未迁移 |
| chapterSeed / renderSlot 的 prose/list/ordered/labels/image/table_draft_only 分支 | 退出（08 §3 可选形式放开） | 无 | 不搬进新文件 |
| ⑪ 形态守恒的 form/sections/named/labels/ordered 检查 | chapter-contract.chapterStructureProblems（必要 H3/表/图） | check ⑪ 改为逐章调用；C03 的 chapterProblems 后续同调 | 旧 ⑪ 槽位循环删除 |
| tableHeaders / findSubsection / subsectionText / subsectionNames / norm | chapter-contract（纯文本辅助） | 入口仍用 subsectionText/subsectionNames/norm 的处（⑦/⑫/材料清单豁免）改 import 同一导出 | 入口副本删除 |
| cmdInit + COMMANDS init 分派 + 用法行 | 退出；decisions 骨架由 cmdSkeleton 严格读取接管 | 无（init_audit 测试助手改为补骨架，不跑命令） | cmdInit 删除；不留 init 壳 |
| author.chapterSection / leadFigureSection | 退出；章头由草稿唯一送达 | 任务包改为 `originalArSection`（原 AR 定位） | 删除，未改名迁移 |

## 2. 接口合同遵守（10 §3）

- chapter-contract：不读磁盘、不写文件、不输出 stdout、不导入入口；`requiredSlots(ch, facts)` / `chapterSeedRows(ch, facts)` / `chapterStructureProblems(ch, body, facts)` 三入口共用同一必要结构解释；facts 只含已解析数据（terms/materialListRows/materialListName/siblings）。
- drafts：`draftPath`/`chapterDraft`/`writeDrafts` 按合同签名；章头为 08 §2.2 的四行 `story-draft:guide`（换行折空格、`-->` 转义）+ H2 + 必要种子；chapterState 由入口提供 `{written, pending, hasStory}`；已有草稿零覆盖、已写缺章按现稿恢复、缺失章不凭空重建。
- 生产代码无反向 import：drafts/chapter-contract 只依赖 headings 与彼此（drafts→chapter-contract）。
- 规模：drafts 53 行、chapter-contract 109 行（≤500 ✓）；story-build 1555 行为中间态（11 分册 C07 收口）。

## 3. 08 §2.1 起手预检与 §4 原件接线

- cmdSkeleton 顺序：冻结守卫 → flow 读取（不存在/未收口/结构问题各自给责任动作）→ 材料基准对账 → scanSources 必需性判定（远程单按 `AR/detail.json` 识别；本地单 PRD/SE 降为可选并记一笔；缺失必备来源阻断）→ 严格 decisions 读取（缺失补 `{decisions: []}`，坏 JSON/坏形状报错，不用 readJson fallback 覆盖）→ facts/chapterState 计算 → 写缺失项。
- UX 正文来源入合同（`UX: ux-reference/README.md, required:false`）；材料贡献清单经同一 sources 表定位。
- flow-check 新增 `originalArSource(featureRoot)` → `{path, problem}`：无 origin=path null；越界/坏指针/读不到给具体 problem。author 任务包新增「4a 上游原 AR」节——区分留存原件与当前提取稿，problem 如实披露；人工补录仍指 upstream.md。C06 源图集合与 C07 reader 复用同一定位（后继接线）。
- flowStatus 路径修正为 `scripts/core/story_flow.py`（原路径缺 core，降级假成功）。

## 4. 测试迁移与新增

- test_story_build：init 用例迁 skeleton（空登记合法且不覆盖、登记后 skeleton 受冻结守卫、登记前一切照旧）；固定槽位/三标签/参与方表用例退役（08 §3 退场清单），改为守「可选形式分支不再预置/不再核」反例；新增章头承载合同问题、验收/交付种子保留、UX README 不再被提起（skeleton 版）用例。
- 夹具：R01-good 补最小 spec（SPEC 必备来源）；StoryBuildCase/RealRunCase 增加 `ensure_flow_state`（真实 story_flow 走 S1–S3 并收口，生成契约）。
- 失效形态：S09/S12/S13/S15/S19 按 08 §3 退役（status: retired，含 reason/approved_by）；S01 发现者针更新为新必要结构判据文案；check_failure_modes 起手步骤去掉 init。
- 全量：**807 passed / 227 subtests，0 失败**。失效形态 65 条：FAIL 0、委派 8、PASS 57。adapt-scan 通过；全部改动 .mjs node --check 过。

## 5. 数值

| 类 | C00 后 | C01/C02 后 |
|---|---:|---:|
| data | 781 | 716 |
| hooks_mjs | 3022 | 3008 |
| prompts_md | 1771 | 1771 |
| scripts_mjs | 2463 | **2507** |
| scripts_py | 1727 | 1727 |
| **总计** | **9747** | **9729** |

- 净 **−18**：新模块 +162（drafts 53＋chapter-contract 109），旧入口净删 −198（form/slots/init/重复函数），合同 form 数据 −65（data）。移动只计一次；story-build 1696→1555（中间态）。
- 完整字符 492886（+621）；完整功能目录与 500 行终态由 11 分册后续步骤收口，不以本包拆分冒称已简化。

## 6. 未验证与边界

- 正常 CLI 未运行；C03 前局部结构检查仍只在原 check 调用（10 §5 明确后继）。
- `originalArSource` 的 C06/C07 消费者未接（本包只接作者任务包）。
- 5 条失效形态退役、3 条用例退役、2 条用例改写——均对应 08 §3 退场清单，未删除仍有效的断言。

# A段交回 · 2026-09-11

> 状态：已实施，A段独立评审通过，见 [A段评审](A段评审-2026-09-11.md)。A通过后可进入B；B完成后进行G1，G1通过才进入C。

## 1. 提交与保护件

| 项 | 值 |
|---|---|
| 起点 HEAD | 5203ca30（branch story） |
| 回退目标 | 33dd9123（B=9675 原始基线） |
| A段提交 | 17d6aed8（一笔，未推送；无 force-push） |
| 保护 tag | story-before-rebuild-20260911-171937 → 5203ca30 |
| stash | f0a3f2ae2a70e10eac8b384784a036a95c99c83c「回退前现场 20260911-171937」，含 story-build.mjs、test/story/AGENTS.md、test_entry_switch.py 三处未提交改动；未 pop |
| 仓外备份 | `C:\Users\kangz\AppData\Local\Temp\opencode\story-recovery-20260911-171937`（status-before.txt、recovery.json、design 1048 份、spec 15 份，与在盘 ignored 数量核平） |

AGENTS 入口行从 stash diff 单独取用，指向已改为目录A（本目录 00-总览.md）；story-build.mjs 与 test_entry_switch.py 的旧实现改动留在 stash，未带回。撤销回退时先保护 A 段新增工作再回 tag；保护件不在本计划中删除。

## 2. 取回与退场逐项

- 取回（逐文件与 tag diff 核零差异）：release 三份、AGENTS.md、TEST.md、golden/README.md、story-金样-AR90006.md、story-金样-AR90006-说明.md、assets/AR90006 五张 PNG（detail-entry、disabled-state、manage-page、signup-page、verify-page）。基线自有 AR90004/Review 金样与公共图片未被覆盖。
- 退场（4e5a0615 按职责应用）：`skills/story/rules/rules.md`、`skills/story-adaptation/templates/plan.md` 删除；直接消费者同步——failure-modes.yaml 两处 clause 指向 story-write.md 现存原文（:162、:208 已核在），test_writing_flow 议题定义三处一致改两处一致。
- 补丁（d36edb70 精确应用）：run_multi_case.cleanup_previous_test_runs 的 live_pids 增加 `str(item.get("status")) not in TERMINAL_STATUS`；新增两条守卫测试（终态复用不拦、未终态存活仍拦），夹具沿用 WorkspaceBoundaryTest 模式，mock `_pid_alive`/`_process_inventory`，不触发真实清理。
- README 清理：删除指向未保留 2.3 金样（story-template-金样、source-selection）的两条失效目录项；Story 金样用途保留。
- 金样登记：test_golden_sample 的 GOLDEN_FINGERPRINTS 与 EXPECTED_CANONICAL_FILES 补 AR90006 七项（含指纹），原有合法金样未删。

## 3. 数值

- B = 9675（05 已独立复算）；A 段后实测 N0 = **9594**，与预计一致，差额恰为 rules.md 的 81 预算行，无未解释差异。
- 五类：data 778、hooks_mjs 3009、prompts_md 1760、scripts_mjs 2488、scripts_py 1559。
- 完整字符（含注释/空行）481000：data 32547、hooks_mjs 142521、prompts_md 73854、scripts_mjs 140141、scripts_py 91937。
- templates/plan.md 属安装器（classify 排除），不计预算收益。

## 4. 验证结果

- 全量离线：pytest 764 passed / 221 subtests passed，零失败（xdist loadscope）。
- check_failure_modes.py：FAIL 0，委派 8，exit 0。
- adapt-scan --check --target . --package .：通过，exit 0。
- `git diff HEAD -- framework` 与 `-- doc/extensions/knowledge` 均为空：Framework 与知识层零改动。

## 5. 未验证与边界

- 正常 CLI 未运行（A 段无此要求）；金样只作维护侧参照，未注入作者。
- 基线不含 S4 原AR留存/候选提交，该已知缺口按计划由 C00 修复并计入第三段，不得当作基线能力引用。
- TEST.md 为 tag 版维护约定，无指向已消失测试文件的引用；测试任务索引随 B/C 迁移按 09 处理。

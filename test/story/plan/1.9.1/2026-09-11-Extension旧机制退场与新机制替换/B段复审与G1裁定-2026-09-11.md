# B段复审与G1裁定 · 2026-09-11

对象：706ebfe2及B段完整提交链，承接上轮B段返修与G1评审。按用户授权，合理实现允许适当超出规模目标，不以删正常功能换数字。

## 1. 结论

**B01–B09实施评审通过，G1通过，可以进入C00。** C按08逐包实施与评审，本次不提前执行C，不启动真实CLI；不是认定整个Extension清理完成或Story效果达标。

上轮四个正确性问题已在原责任位置修复，没有增加第二套模式协议或恢复重复路径函数。B的规模收益有限，但多AC桥接和多模式处理是在修正常功能的数据丢失，不能为了净减重新退回单值Map。

## 2. 返修逐项核对

| 上轮项 | 706ebfe2实际修改 | 判定 |
|---|---|---|
| R1 无候选误拦 | Spec和Plan两侧均把无候选说明排除在真实模式集合之外；正反向真实候选集合核查仍在 | 通过；新测试修改knowledge-use真源，不再仅改旧展示表 |
| R2 Plan重复覆盖 | planPatternChoices返回choices/problems，重复同一对先报错再continue，首行不被覆盖；消费者合并问题 | 通过；测试同时发现重复与首行空理由，正常双模式仍合法 |
| R3 换算示例错误 | 删除错误数字示例，保留依据与单位换算的语义要求 | 通过；没有为一条例子新增换算器 |
| R4 新旧facts规则冲突 | 原禁止句与补充句合成一条“不能冒充已有／新建须有依据”的说明 | 通过；旧相反指令实际退出 |
| 作者入口同步 | plan-sections改为每个真实单元×候选一行，无候选只作说明 | 通过；方法在写前可见 |

本次改动范围与返修一致；Framework相对A段无修改。没有追加新的语义代理或根据测试数量删除正常功能。

## 3. 实测与交回数字更正

交回§2仍用3f98307c的9567/3015；706ebfe2已增加Plan重复诊断和模板说明，必须纳入本轮总量，不能只在文字里写“含返修”。本次用原measure/classify/code_lines复算：

| 类别 | A段N0 | B完成N1 | B净变化 |
|---|---:|---:|---:|
| data | 778 | 781 | +3 |
| hooks_mjs | 3009 | 3022 | +13 |
| prompts_md | 1760 | 1761 | +1 |
| scripts_mjs | 2488 | 2463 | −25 |
| scripts_py | 1559 | 1548 | −11 |
| 总计 | **9594** | **9575** | **−19** |

固定比较基线仍为9675：至此累计减少100，其中A的81与B的19分别计数。完整字符481879，比A的481000增加879；不能把预算行下降表述成全字符下降。

B06–B09含返修相对d1a0ece2：9534→9575，+41；完整字符478508→481879，+3371。本次已将交回主表更新为现值，不再保留两个冲突的“当前总量”。

独立重跑四个相关测试文件：**73 passed / 6 subtests passed**。预算文件调整后另跑其专用测试：**5 passed / 16 subtests passed**。diff空白检查通过。实施者报告的全量787项及失效形态/适配结果未在本次重复运行；真实模型与未知业务效果仍待C完成后的授权测试。

## 4. G1规模裁定与实际预算接线

由本评审依据用户2026-09-11明确授权裁定，写入现有mechanism-budget.yaml：

- hooks_mjs进行中峰值3010→**3030**：当前3022，保留多AC读取、候选逐对判别和重复错误传播；8行余量供本组小范围收口，不是新增功能配额。完成target暂3010，C作者/审查收敛后按实际职责再核。
- scripts_py进行中峰值1590→**1750**：当前1548，C00历史S4修复的Python净增约163行；需容纳候选校验、原输入留存、材料基准与幂等重试。保留小幅接线余量，不让1590迫使删除恢复能力；完成target暂1590，是否需合理例外归G2。
- 总进行中峰值**9800不变**；本轮requirement身份更新到当前重构，状态保持in_progress。总完成优先目标**8675**写入实际被测试读取的total.target。

额度和理由具名记录，不修改预算算法、不隐藏超限、不将业务机制移到预算外。用户允许合理超限不意味着最终可以自动按现值重签；G2继续核完整功能、旧承担者是否退净、更简单完整替代和实际成本。

## 5. C实施预期与G1边界

C原估算只能作为初步规划，不能当净减承诺。C00旧估+100～130偏乐观，已验证历史修复连Python与提示说明约百余行，规划改按**+160～190**；实际接替时优先复用已验证实现，不为落入估算裁掉失败恢复。其它包仍需按职责去重，raw行不能充当预算行，C03提取检查不能算删除两份规则，C01的boundary新增计成本。

沿交回其它区间暂算，合计净变化**−455～−175**，以9575计对应约**9120～9400**，超8675目标约445～725。它只是规划区间，**没有获批为新的最终上限**；本评审也不承诺C能净减900行。

G1允许进入C的依据是：B功能与接替闭合；C各包目标、旧承担者、保留保护和正常/恢复路径已明确；进行中额度可以承载第一包；目标差额不再被要求靠删正常功能消除。C每包交回实际增删与保留理由，最后统一判断合理例外。不能把必要slots全部删掉、把原图/人工区保护撤掉，或压短作者方法来兑现预计行数。

下一步只实施C00，范围与测试按08 §1、09；保留原材料与人工输入，退出直接覆盖原AR的旧写法。完成后交回评审，再进入下一包。

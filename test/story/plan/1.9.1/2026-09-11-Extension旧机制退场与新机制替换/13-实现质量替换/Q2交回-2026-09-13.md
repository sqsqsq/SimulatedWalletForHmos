# Q2 交回 · 章节解析与起手条件替换 · 2026-09-13

> 状态：**首版 58533614 评审待返修，5108b566 复审只剩围栏关闭一条，b55b9bbd 已补，等待复审**（见 [Q2评审](Q2评审-2026-09-13.md)、[Q2复审](Q2复审-5108b566-2026-09-13.md)）。合同：[00-执行总览](00-执行总览.md) 与 [02 分册](02-章节解析与起手条件替换.md)；
> 开工前已完整读取当前工作树 `test/story/AGENTS.md`（重点 §7.5 维护预算与分级复核）与 TEST 的离线分支。
> 基线 71c2be05（Q1 复审通过，9951 行 / 505888 字符）。首版 58533614、返修 5108b566、b55b9bbd；branch story，未推送；未进入 Q3，未跑真实 CLI。
> 关闭 12 的 **R2**（必需 Spec 被改成提示后继续）、**R4**（必要表只认第一张同主语表）、**R5**（总览图的 H3 位置规定，方案已收回）。

## 1. 解析与判断分开

新模块 `core/story/document.mjs`（67 有效行）：`parseChapter(text)` 扫一遍给出这一章的结构——原文、围栏范围与语言、H3 小节名/范围/正文、正文表头及**它所在的小节**。配套 `sectionBody`（先精确再包含）、`sectionNames`、`tablesIn`（按小节或全章）、`hasDiagram`、`norm`。只解析当前判据真要的那几样：不建通用 Markdown 语法树、不落盘、不跨命令缓存。

| 旧承担者 | 新承担者 | 旧位置处置 |
|---|---|---|
| `chapter-contract.views`（章首/去围栏两份视图） | `parseChapter` 的 `fences`/`sections` | 删除；`lead`（章首视图）随 R5 收回一并退出 |
| `chapter-contract.subsectionText/subsectionNames/findSubsection` | `sectionBody`/`sectionNames`（同一套「先精确再包含」） | 删除，不留包装壳 |
| `chapter-contract.tableHeaders` | `parseChapter` 的 `tables`（附带所在小节） | 删除；此前每次判一张表就重扫整章 |
| `chapter-contract.norm` | `document.norm` | 删除；`story-build` 改 import 同一处 |
| `story-build` 里按文本反复切附录小节（⑦、⑫ 的节名集合与逐节正文、⑫b 的行比对） | `viewOf(章名)` 的一份视图 | 调用点全部改为读视图 |

`chapter-contract` 141 → 85 行，只剩条件、结构规则与种子；它不再自己切文。

**一次 check 里每章只解析一次**：入口按章记忆视图，⑦/⑪/⑫/⑫b 读同一份。新用例把副本树的 `parseChapter` 记一笔后跑真 check：这份夹具有正文的两章各解析 **1** 次（共 2 次）——附录那一章一次 check 里被问十来次。

## 2. 必要结构的唯一解释

- **表：作用域内任意一张满足全部锚列即通过**（R4）。验收章前面先放一张「编号／说明」对照表、后面才是完整验收表，这一章是齐的；一张都不满足时，按缺得最少的那张说缺哪几列，作者改它就够了。不跨表拼列，不锁首张，不加表名豁免。
- **图：`structure.diagram` 由 `"chapter-start"` 改为 `true`**（R5）。只核这一章真有一张画图语言的围栏；总览在章首那段或一个「总览」小节里都合法，位置不再当成「它是不是总览」的代理，讲没讲清整条业务归语义审查。普通示例代码（`json` 这类围栏）不算图。
- h3 仍按 `{title, when}`，`siblings` 只在确为 true 时必需；种子与检查共享同一条件、标题与表位置；围栏里的标题与表不参加结构判定。

作者面同步：`story-write.md` 里「章首一张总览图，在第一个小节之前」改成「放在读者一进这一章就找得到的位置（章首那段，或一个总览小节）」——规则松了，作者那一句不能还是旧的严。

## 3. Spec 起手条件回到必需语义（R2）

`cmdSkeleton` 在写任何文件前核**本步真要读的那几节**在不在：术语映射表（术语章起始行从它派生）、§9.1–9.5（附录 A–C 从它投影）。缺了就回 Spec 并在报错里说清「这件事确实不涉及，就在那一节写『不涉及：<依据>』一行」——写出来的结论评审者读得到，没写到那儿的，起手这一步分不出是哪一种。不重跑 Framework 全套语义，不替这份需求判断它有没有接口，也不要求补空表。原来那条无条件「记一笔后继续」退出。

夹具按分册要求升级为**合法最小 Spec**（术语映射表一行 + §9 五节各一句「不涉及：<依据>」）：`R01-verdict-echo/good`、`test_author_task_package` 与 `test_image_registration` 各自的起手夹具。纯结构用例仍走离线/函数入口，没有为旧夹具降低运行合同。

## 4. 验证

新增 `test_chapter_view.py`（8 条）：原文逐字节不动；围栏里的标题与表不是结构、围栏之后的正文不被吞；表知道自己在哪一节；`tablesIn` 对「那一节缺席」返回 null、对「有节没表」返回空；先精确再包含；小节里的空行保留；只有画图语言算图；没闭合的围栏也看得见。

`test_story_build` 改/增：辅助表在前不再掩盖真正的验收表；列散在两张表仍失败；总览在独立小节合法、章内零图失败、`json` 围栏不算图；Spec 缺必要章**拦且文件集合不变**、显式「不涉及」合法。退役了「章首范围」那两条断言——它们测的是已被收回的规定。

失效形态：`S01-diagram-degraded` 的反例判据改核新报错（「没有图」）；它判的仍是「这一章一张真图都没有」，语义风险（图讲没讲清分支）仍由 `observed_by` 登记的读者审查承担。

离线：**pytest 864 passed**（+2 预算失败，见下）；失效形态 65 条 **FAIL 0**、委派 8、PASS 57；改动的 `.mjs` 全过 `node --check`；判据锚金样 `AR90004` 离线 check **通过（10 章）**；参照件 `AR90006` 从 5 处未过降到 4 处——「业务流程章首缺总览图」那一条随 R5 消失，余下 4 条（业务方案缺「参与方与分工」、材料清单 3 行）在 Q1 基线上就存在。

## 5. 规模：**超在途峰值，按 §7.5 交 reviewer**

| 类 | 71c2be05 | 本次 | 在途峰值 | 完成上限 |
|---|---:|---:|---:|---:|
| data | 719 | 719 | 800 | 800 |
| hooks_mjs | 3001 | 3001 | 3030 | 3010 |
| prompts_md | 1838 | 1839 | 2050 | 2050 |
| scripts_mjs | 2634 | **2652** | 2640（**超 12**） | 2510 |
| scripts_py | 1759 | 1759 | 1760 | 1590 |
| **总计** | **9951** | **9970** | 9960（**超 10**） | 8675 |

完整字符 505888 → 508288（+2400；口径：范围与预算 classify 一致，逐文件 `Path.read_text(encoding='utf-8')`）。

逐文件：`document.mjs` **+67**（新），`chapter-contract.mjs` 141→85（**−56**），`story-build.mjs` 1569→1576（**+7**），`story-write.md` +1。净 +19。

**新增与移动分开记**：67 行里约 55 行是从 `chapter-contract` 移过来的解析实现（移动不算新增也不算净减）；**真新增约 30 行**——一次扫描里把表归到它所在的小节、`tablesIn`/`hasDiagram` 的作用域、遍历全部候选表、入口按章记忆视图、Spec 必需预检。**真退出**：`views` 的章首视图、`chapter-start` 位置分支、`specSeedGaps` 的记一笔通道、每判一张表重扫整章。

按 §7.5：02 分册没有登记本包的新增实现预算，这里不自行补数，也没有改 `mechanism-budget.yaml` 的任何额度——**旧检查器的两条硬超限保留实测**（`scripts_mjs` 与总量），交 reviewer 按本节核必要性、定新增预算与适用起点。我没有为换绿删能力，也没有压行：可退的两处（未被消费的 `prose` 字段、`chapter-start` 分支）已经退了。

独立评审状态：58533614待返修，执行 [Q2分册](02-章节解析与起手条件替换.md) 与 [Q2评审](Q2评审-2026-09-13.md)。下述样本结构缺口判断已由评审§3纠正；原交回保留为实施者当时判断。

## 6. 边界与未验证

- `story-build` 里还有两条与章节解析相邻但不属本包的路径：`storySections`（H2 切章）与 `subsectionSpan`（机器区/材料清单要的原文行号）。功能目录把它们归 `document`，接线时点在 Q5（章提交）与 Q6（投影与人工区）；本包没有提前搬。
- `pipeTables` 读的是 `spec.md`，不是 story 的章，与本包的视图无关（Q6 的投影职责）。
- 真实 CLI 未运行；Q3 未开始。
- 工作树里 `test/story/AGENTS.md`、`TEST.md`、`mechanism-budget.yaml` 与 `test_mechanism_budget.py` 是维护侧未提交的规约更新：**原样保留，没有还原，也没有并进本包提交**——它们不是执行者的新实现。
- `AR90006` 余下的「业务方案缺『参与方与分工』」是那份参照件自身的结构缺口，不是判据问题；要不要按它改合同或改样本，请评审裁定。

## 7. 返修（对 Q2 评审的三条）

当前5108b566复审：R2/R3与残留清理通过，R1尚需限定关闭行尾随内容，见 [当前复审](Q2复审-5108b566-2026-09-13.md)。Q3不开始。

返修前重读了当前工作树 `test/story/AGENTS.md` §7.5 与 TEST 的离线分支。三条都是确定性边界，改在唯一解析职责与既有来源集合的量词上，没有加兜底检查、没有加模型自证步骤、没有按业务名或金样加豁免。

### R1 围栏关闭要匹配符号种类与长度

`parseChapter` 现在记住开启标记的**种类与长度**（`mark`），只有同种、且不短于它的那一行才关得上；关不上的围栏行仍算围栏里的内容。原来遇到任意围栏行就翻转开闭：一个反引号 `text` 块里贴一段波浪号样例，样例里的 `### 交付物` 与交付物表会被当成本章的正文结构，后面的范围也跟着错位。

保留原有行为：未闭合围栏仍在 `fences` 里看得见、原文逐字节不动、CRLF 照旧。

### R2 同名小节的正文与表来自同一个节

`tablesIn` 先用 `matchSection` 定出**同一个节**，再按它已有的 `[from, to)` 与表的 `line` 限定范围；表对象上那个按名字记的 `section` 字段随之成为冗余，核过唯一消费者后删掉（测试改核实际范围义务）。原来正文取第一个匹配节、表却按规范名过滤全章，第二个同名小节的表会补上第一个的缺口。

保持：重复原名、编号规范化后同名、精确优先于包含、缺节 `null` 与有节无表空数组、目标节之外的表不补缺口。

### R3 三份投影来源逐节核

`APPENDIX_FROM_SPEC` 的每个来源节各带自己的位置（`{at, re}`），`specGaps` 逐节核并报**准确缺哪几节**；`appendixTables` 与 `specNotApplicable` 同步改读新形状，没有另立第二份章节表。原来 `res.some(...)` 让「§9.2 在」就跳过整组，而附录那一节是从 §9.2/§9.3/§9.4 一起投出来的。显式「不涉及＋依据」继续合法，不要求补空表。

### 顺带退场

`chapter-contract` 里已无消费者的 `DIAGRAM_FENCE` 常量删除；`document.mjs` 的 `@returns` 与真实返回形状同步（`fences` 带 `mark`，`tables` 不再带 `section`）。

### 返修验证

新增 6 条用例：异种围栏不能关闭外层、较短同种标记不能关闭较长外层、真关闭之后结构照常被认出（且数据围栏不算图）、同名小节两向（正文与表同源／别的节自己那张仍查得到）、三份投影来源分别只缺一节都失败且不写盘。**拿返修前的实现跑过这 6 条，全部红**，确认它们测的是真实边界。

离线：**pytest 870 passed**（+2 预算硬失败，见下）；失效形态 65 条 FAIL 0、委派 8、PASS 57；金样 `AR90004` 通过（10 章），`AR90006` 仍 4 处（与返修前相同，本次未动那两类）；`adapt-scan --check` 通过；改动的 `.mjs` 全过 `node --check`。

### 返修后的规模

| 类 | 71c2be05 | 首版 58533614 | 返修 5108b566 | 在途峰值 |
|---|---:|---:|---:|---:|
| scripts_mjs | 2634 | 2652 | **2658** | 2640（超 18） |
| 总计 | 9951 | 9970 | **9976** | 9960（超 16） |

其余三类不变（prompts_md 1839、hooks_mjs 3001、data 719、scripts_py 1759）。完整字符 508805。

返修净 +6：`document.mjs` 67→70（围栏匹配 +2、范围限定 +1），`story-build.mjs` 1576→1580（来源节各带位置 +3、逐节核 +1），`chapter-contract.mjs` 85→84（`DIAGRAM_FENCE` 退场）。**这 6 行都是评审点名的确定性缺口的修法**，不是新能力。按 §7.5 仍未改 `mechanism-budget.yaml` 的任何额度，两条旧硬超限保留实测交 reviewer 定新增预算与起点。

## 8. 第二轮返修（对 5108b566 复审的唯一阻断项）

当前b55b9bbd已独立复审通过，可进入Q3；历史待返修状态已被 [当前复审](Q2复审-b55b9bbd-2026-09-13.md) 取代。

**关闭行与开启行不是同一种语法**。上一版的关闭谓词只比标记的种类与长度，于是 ```` ```markdown ```` 这一行（一段样例的**开启行**）被当成关闭符：样例里的 `###` 与表泄漏成本章结构，真正的关闭符又被当成新的开启，它后面的真正文跟着丢——泄漏与丢正文同时发生。

关闭谓词现在三条同时满足：**同种标记、不短于开启标记、标记之后到行末只有空格或 tab**。开启行继续允许语言信息；仍是同一个扫描器，没加事后过滤、样例白名单，也没有第二次解析。

复审给的那份输入现在的结果：`sections` 只有「真的节」，样例里的表不进 `tables`。

**验收按参数化边界覆盖**：尾随语言 / 普通文字 / 标点 / 语言加文字四种都不关闭；裸标记、只有空格、只有 tab、较长同种四种都关闭；较短同种与异种仍不关闭。每条都核**真关闭之后正文与表恢复**，不是只断言 `fences` 数量；原有用例全部保留。四条尾随内容用例拿返修前的实现跑过，全红。

离线：**pytest 873 passed**（+2 预算硬失败，按 §7.5 保留实测交 reviewer）；失效形态 65 条 FAIL 0、委派 8、PASS 57；金样 `AR90004` 通过（10 章）、`AR90006` 仍 4 处；`adapt-scan` 通过。

规模：`document.mjs` 70→**72**（关闭谓词一行常量 + 条件一行），scripts_mjs 2658→**2660**，总量 9976→**9978**，完整字符 509008。这 2 行是复审点名缺口的修法；仍未改 `mechanism-budget.yaml` 的任何额度（在途峰值超 20 / 18 行，保留实测）。

# Q3 验收读取：一次验证、一次分组

> 方案/实施/评审按 [维护预算目的](../../../../AGENTS.md#75-维护预算与分级复核) 核本包新旧职责：防走偏与旧机制残留，不以删正常功能换绿。

当前ec21d747已复审通过，见 [Q3评审](Q3评审-ec21d747-2026-09-13.md)，下一步Q4。这是用户授权对本轮已实现内容的质量替换，保持B06功能，不重新打开已确定的业务需求。

Q2已通过b55b9bbd，见 [当前复审](Q2复审-b55b9bbd-2026-09-13.md)。开工读取当前工作树AGENTS§7.5。顺带将test_chapter_view.py现有unittest.main块移至文件末尾，使直接执行也能发现全部测试；保持所有断言。

## 0. 实施前估算（Codex reviewer，2026-09-13）

主要口径为不含注释/空行的机制有效行，字符另报。起点b55b9bbd总9978、hooks_mjs3001。预计新knowledgeCriteria完整校验/分组约35–40行、Spec调用和集合消费约5行，新增实现估45行；退出acceptanceEntries、原knowledgeCriteria中转和Spec旧消费约50行，预计净减约5行、完成总量约9973。复用原校验语句计入替换实现成本，不用Git新增行代替；移动不算净减。测试入口整理不进入机制预算。

本分项进入整轮后续新增/退出账，不作为每包重置100%/125%/150%阈值的分母。原已实施部分不倒签，后续未实施包在各自开工前估算并汇总；分级机械接线未完成，不声称脚本已执行新比例。实际超过本估算先反思，预计扩大职责或需要超出现有在途额度时交reviewer裁定，不由实施者按实数改预算。允许按先删中转再替换的顺序实施，不需要长期双接口迁移期；当前在途hooks3030、总10000保持，完成目标8675不变。

## 1. 替换对象

当前contracts.acceptanceEntries先生成 `{section, criterion}` 数组，knowledgeCriteria再遍历分组；Spec只拿编号集合，UT/testing只拿byRule。section字段没有独立下游用途，中间包装和接口不必保留。

保留 `readAcceptance` 的文件读取与三态返回。将验证和分组收进现有 `knowledgeCriteria(acceptance, sections) -> {byRule, problems}`，直接在一次section/row遍历中校验并追加到rule数组。删除acceptanceEntries函数及导出，不新增另一个通用读取器。

## 2. 保持的行为

- sections由阶段显式传入：Spec只criteria；UT/testing是criteria+boundaries。
- 缺集合与当前合法空集合仍按既有约定处理；错误集合/行/knowledge_rule保持错误，不过滤后变成功。
- 普通业务AC没有knowledge_rule合法，不进入知识桥接，不被强迫关联规约。
- 同rule多个AC按原顺序全部保留；重复项不静默去重，不改变当前校验责任。
- readAcceptance解析失败、缺文件和合法空文档的现有区别保持；verify分派、缺ID和覆盖检查仍在各消费者，不搬进分组函数。

Spec.acceptanceCoverage改调用同一knowledgeCriteria，使用byRule.keys做原双向差集；先处理problems。UT/testing返回接口不变，继续逐条检查byRule下所有AC。不要要求调用方为了去掉中转又自行解析一次YAML。

## 3. 验证与完成

复用test_neutral_knowledge、test_contracts_single_source、test_spec_story_gate和知识相关测试，对同批输入比对旧/新byRule及problems含义：多AC、普通业务AC、criteria/boundaries分工、坏YAML、坏类型、空字段、缺ID、未覆盖与完整覆盖。

至少一条真实Spec hook及UT/testing调用贯通，不只测纯函数。新旧合法输入结果、阶段分派和失败含义相同，不修改已有正确测试期望来适配新算法。

完成：全仓生产/测试无acceptanceEntries引用；没有中间包装数组或第二套遍历；阶段只消费分组结果。减少的是接口和转换，不是校验条件。交回实际净变化，不事先承诺节省多少行。

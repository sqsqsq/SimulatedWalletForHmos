# Q5复审：239a025b

结论：**Q5通过，可进入Q6。** 清洗空行、重复围栏解析和重复编号编译三项已关闭，此前通过的写入保护、候选边界和接续恢复保持。实施者开工Q6前读当前工作树test/story/AGENTS.md、TEST离线分支及Q6分册；预算采用用户已澄清的功能/结构区分。

## 实现质量与退出

stripGuidance直接删除全局空行压缩，仅过滤自有指导；不再添加一层围栏特例补偿清洗。连续空行的原文断言与作者注释保护均通过。

fenceRanges成为唯一开闭判断，parseChapter、切章、区间定位、编号及指导清洗都消费其范围。旧fenceMask和chapter本地fencedLines退出；只需范围的消费者不再被迫构建小节与表头。多种非法关闭与合法关闭测试贯通实际消费者，且全篇check的章视图复用保持。

compileIdShapes在建立当前命令上下文时计算，在线和离线上下文都携带结果，各章与check只读；旧idShapes接口退出，没有全局缓存。源码证据支持“一次编译”；报错一次的测试本身不单独证明调用一次，两种证据分开看。

Q5整体已具备坏候选写前拦、原文目标区外保护、完整候选边界检查、单章/全篇局部判据共用、写后接续恢复。copyedit记录的生产/检查/冻结/指令退出，实际整稿方法保留。未发现需要继续阻断的实现问题。

非阻断整理：context.mjs的readRaw说明被放到readJson前、离线上下文说明被放到CORE_DIR前；document.mjs的parseChapter参数说明悬在fenceRanges前。Q6触及这些直接消费者时将说明归到真实对象，删掉仅叙述维护过程的游离说明即可；不为了注释另开返修或重跑全量。

## 独立验证

相关回归test_story_build、test_chapter_view、test_writing_flow、test_negative_guards：**240 passed、56 subtests passed，34.18秒**。提交diff检查通过。未重复实施者901条全量及形态检查，未启动真实CLI，不宣称模型整稿效果已验收。

独立规模：scripts_mjs2899、scripts_py1778、hooks_mjs2993、prompts_md1724、data714，总**10108有效行、518826字符**。字符按预算分类纳入文件的read_text长度计，本次与交回518490有336字符差异，以独立实测为本次记录，不把差额解释成运行效果。当前story各模块最大appendix381有效行，其余均低于500；结构拆分要求成立。

## 功能预算与结构成本裁定

必要import、导出、参数传递与模块边界不计功能预算比例，完整规模仍统计，原248%论证继续撤回。不能因结构增长要求功能超支人工确认。

交回§5.2列187行功能实现，却漏列本包实际替换的cmdChapter。独立按同一有效行口径，该函数66行；将整函数保守补计，功能实现估算约253行。保留原180估算，不为原约35行中可能含结构成本而事后缩小分母，也不扣掉返修的围栏/编译实现冒称无需预算。按现有分项估算约141%，属于reviewer复核范围；这是职责估算，不将Git行文本差额伪称精确功能比例。

Codex reviewer具名认可这些功能实现：完整候选写前保护、共用范围/编号解析和接续恢复均服务本包既定目标，所替换旧逻辑已退出。没有证据要求把必要功能删回180行，也不按本包重置整轮累计记录。后续继续分别核功能实现与结构接线，若实际功能预算达到需人工确认的档位，再按规约处理。

旧总量检查仍把结构计算在内。为使后续正常验证可用，认可Q5实测并调整在途scripts_mjs至2920、scripts_py至1800、总量至10150；其它类别及最终8675目标保持。这是已审结构与功能的具名在途规模裁定，不是扩展功能范围或永久提高完成目标。Q5比起点9831净增277行，不能宣称整轮缩减已达成。

## 交回与下一步

Q6只处理当前appendix/review中的投影计算和只读保护归一，不再搬一次Q5模块，不重建文档解析。重点验证“直接篡改盘上机器区后只读check能发现”，不能先project再比较制造必绿证据。更新后的Q6分册包含实施前估算；当前总览与交回入口已同步通过状态。

# Q2复审：b55b9bbd

结论：**Q2通过，可按Q3分册继续。** 本次关闭围栏尾随内容问题；前次已通过的同名小节范围、Spec逐来源预检和残留清理保持。维护实施者开始Q3前读取当前工作树test/story/AGENTS.md，重点§7.5及TEST离线分支，不沿用demo根规约或旧上下文。

## 实现与验证

document.mjs的关闭条件同时核标记种类、长度和整行尾随空白；开启行继续保留语言信息。修改位于现有扫描器内，没有事后过滤、业务例外、第二套解析或模型自证。尾随语言、文字、标点不会泄漏样例；真正关闭后正文和表恢复，裸标记、空格/tab及较长同种关闭保持合法。

独立相关回归：test_chapter_view、test_story_build、test_author_task_package、test_image_registration、test_writing_flow，**249 passed、54 subtests passed，30.54秒**。已读新增参数化断言，覆盖结构结果而非只数围栏。未重复实施者873条全量，也未启动真实CLI；不据此宣称模型成文达到金样。

Q2整体替换成立：旧重复章节解析退出；必要表遍历目标范围内全部候选；总览不再用H3前后位置代替语义；Spec必需输入逐节检查。表的冗余section字段和合同死图正则已删除。保留的H2切章/写入范围与Spec表读取有不同消费者，分别按Q5/Q6完成接线，不作为本包未退旧机制。

本次未发现新的阻断问题。一个非阻断测试组织问题：新测试类写在test_chapter_view.py的unittest.main()之后，pytest/discovery能收集，直接执行该文件则漏掉最后三条。Q3交回前将现有main块移至文件末尾即可；不改断言、不加新运行入口。这属于测试入口整理，不要求重做Q2或重复全量。

## 规模与在途预算裁定

独立计量：scripts_mjs2660、scripts_py1759、hooks_mjs3001、prompts_md1839、data719，总**9978有效行、509008字符**。相对Q1基线增加27行、3120字符；本次返修增加2行、203字符。document72、chapter-contract84有效行，完成模块满足500行要求；大入口仍按Q8/Q9归位。

增加部分承担范围索引、单次解析复用、候选表遍历和真实起手预检，旧承担者已退出。本包不是净减，但没有因预算删正常功能，也没有用新流程绕旧错误。基于用户允许合理实现适当超限及AGENTS§7.5，Codex reviewer具名认可Q2最终实测，并将旧检查器在途scripts_mjs额度2640调整为2680、总额度9960调整为10000，其余类别与完成目标8675不变。20/22行余量仅是当前替换接线的在途空间，不是新增功能授权；额度变化不证明实现合理性，后续仍核职责和退出。

这次是旧在途额度裁定，不用9978除以旧总峰值计算新规比例，也不倒签已完成Q2的“初始新增预算”。Q3分册补入尚未实施的新增/退出估算，计入本轮后续预算记录；它不是独立重置整个需求分级的分母。后续方案继续补齐各自未实施范围，在整体新增预算有可靠依据前，超出已估范围交reviewer判断，不由实施者回填实际值或宣称未触发150%。

## 后续边界

Q3仅执行验收读取的直接分组替换及上述测试入口整理。Q4继续处理金样有分工内容却因标题名不符被报缺失的问题；不改金样迎合脚本。Q2通过不代表Q3–Q11完成，更不代表本轮真实行为验收完成。

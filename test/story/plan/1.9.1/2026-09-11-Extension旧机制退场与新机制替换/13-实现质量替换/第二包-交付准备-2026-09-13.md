# 第二包 · 交付准备件（Q11 §1/§4 静态部分） · 2026-09-13

> 本文件是**候选**：按 [11-集成验收与本轮交付](11-集成验收与本轮交付.md) §4，正式迁入
> `test/story/` 与 requirement.closed 在真实验收与 G2 之后。这里只把 G2 之前必须备齐的
> 静态件放齐，并写清哪一部分按约定留到最终 HEAD。

## 1. 版本与发布说明（已落盘）

| 件 | 位置 | 本次动作 |
|---|---|---|
| 发布说明 | `test/story/release/1.9.1.md` | 新建。按本轮实际改变的**信息送达与放行判据**写，不声称表达质量提升 |
| 历史说明 | `test/story/release/1.9.0.md` | 原样保留，1.9.1 里链到它 |
| manifest 版本 | `doc/extensions/manifest.yaml` | `1.8.0` → `1.9.1`；`version:` 上面那 39 行版本注释**整段移除** |

**用户 2026-09-13 的两条指示**：版本继续按 1.9.x 演进（本轮 `1.9.1`，不跳 2.0.0）；
manifest 里的版本注释整段移除。

演进记录此后只有一处——`test/story/release/` 的发布说明。这与 adapt 的所有权规则一致：
`version:` 上面那一段本就归装它的工程（目标在那里写「我们这个仓怎么用它」），包不该占着它。
`test_the_manifest_version_covers_this_round` 改成守这一条：包再往那里写演进记录就红。
manifest 版本此前停在 `1.8.0` 而仓里已有 1.9.0 发布说明——注释整段移除后这个落差不再存在。

## 2. 指南与交付流程的局部对齐（已落盘）

三处此前与实现不一致，本次改到与现行行为一致：

| 位置 | 旧说法 | 现行行为 |
|---|---|---|
| `story使用指南.md` Q5 | 「story.md 不能手工润色，它由规格自动装配」 | 正文由 AI 按章写、经章提交命令落盘（章外字节原样保留）；**附录的机器投影区**不能手改，改了逐行报出并给出重新生成的出口 |
| `story使用指南.md` 保证 1 | 「评审的必然是最新的——由规格确定性装配」 | 评审看的是**登记那一版**：登记前重跑检查、按当时真源重投影；登记后 story 冻结，规格再改走评审回流 |
| `需求交付流程.md` 四 | 「评审叙事由规格确定性装配而成」 | 由 AI 按规格逐章写成，附录的契约部分由机器投影 |

头部的版本链接指向 1.9.1，历史版本括注保留。

## 3. 本轮旧机制退出清单 —— 移到映射里，这里只给指针

退出记录的唯一落点是 [FEATURE-MAP.md 的「本轮退出项」](FEATURE-MAP.md#本轮退出项唯一记录处)，
在那里与功能归属放在一起核，分成三类记：

| 类 | 判什么 |
|---|---|
| A 结构搬迁 | 同一份实现换了位置，**能力不变**；核「新唯一落点 / 消费者已切 / 旧位置已删」 |
| B 业务能力变化 | 有增有减，每一项写清**承接者是谁**；退出自证载体不等于退出职责 |
| C 新增能力 | 本轮四项，各自落在既有功能叶子里，写清替代了什么 |

本节此前另存过一份清单，与映射里的结论有两处对不上（`copyedit` 写成「无替代」、
归档副本特权写成被「上游原件留存」替代），按评审 R2 一并纠正后不再在两处维护。

## 4. FEATURE 三稿（已形成）

三份候选就在本目录，基线 `77314387`：

| 文件 | 回答 | 规模 |
|---|---|---|
| [FEATURES.md](FEATURES.md) | 有哪些功能：7组、43个细分功能项（含F2.4父项），各自的目的、输入、现行实现、消费者、失败出口、证据 | 约 330 行 |
| [FEATURE-MAP.md](FEATURE-MAP.md) | 功能 → 实现（文件·符号），外加本轮退出项三类 | 约 400 行 |
| [FEATURE-CONTENT-MAP.md](FEATURE-CONTENT-MAP.md) | 实现 → 功能：每个交付文件的每个顶层单元归哪个功能 | 约 480 行 |

**按符号定位，不写行号**：本轮之后还有返修与 G2，行号会整片作废而符号在返修中稳定；
确需行号时以声明的基线为准。上一轮三稿用的是 `d36edb70` 的行号锚，那套坐标在本轮已全部失效
（八个文件改了位置），所以不照搬。

### 4.1 双向覆盖是机械核过的

- **实现 → 功能**：`doc/extensions/` 下 54 个代码文件、**642 个顶层符号**，
  逐个与 FEATURE-CONTENT-MAP 里登记的名字比对，**差集为空**；`__pycache__` 不算交付内容。
  提示词与规则页按二级标题登记，合同与配置按顶层键登记。
- **功能 → 实现**：43个细分功能项每条都有承担文件与具体符号，没有「由子特性承担」之外的空条目。
- **逐领域点名**（评审要求的样本）：流程关卡 F1.6、材料身份 F1.3/F1.4、章提交 F2.4、
  投影 F2.5、图片 F2.4.2、审查 F5.5、知识 F4.1–F4.9、适配 F6.1–F6.4——
  每一条都能从功能找到现行实现，也能从实现反查回功能。
- **退出项**：A 类 8 行、B 类 7 行、C 类 4 行，各自的核对方式写在映射里。

### 4.2 功能树粒度

本轮沿用功能ID，既有能力的实现替换与能力增量在相应功能项下说明；粒度以职责为准，不为了证明“没有新增功能”限制后续拆分。当前定义43个细分功能项，其中F2.4有三个子项，统计父项与叶子时明确口径。

### 4.3 最终 HEAD 还要做的

验收有返修时**只更新受影响条目**：改了哪个符号就改那一行，基线声明换成新 HEAD。
全量重建只在符号大面积搬家时才需要，本轮之后不预期再有这种搬家。
正式迁入 `test/story/` 与更新 AGENTS 入口仍在最终验收与 G2 之后。

## 5. 本包的未验证项

- 真实 CLI 未跑（Q11 §2 才跑）。本包所有结论都是确定性接线与送达，**不声称模型语义质量**。
- 发布说明与指南的措辞未经真实读者（业务使用方）验证。
- FEATURE 三稿的**双向覆盖**是机械核过的（符号差集为空），但「归属判得对不对」是语义判断，
  由评审看；机械核只能证明没漏登。
- 三稿仍是候选：正式迁入 `test/story/`、更新 AGENTS 入口与 requirement.closed 在 G2 之后。

# Q3 交回 · 验收读取一次验证一次分组 · 2026-09-13

> 状态：**ec21d747独立评审通过**，见 [Q3评审](Q3评审-ec21d747-2026-09-13.md)。合同：[00-执行总览](00-执行总览.md) 与 [03 分册](03-验收读取单次分组.md)（含 §0 实施前估算）。
> 开工前读取当前工作树 `test/story/AGENTS.md`（重点 §7.5）与 TEST 的离线分支；预算额度用工作树里 reviewer 已调整的那一份（在途 `scripts_mjs` 2680、总 10000），未自行改数。
> 基线 b55b9bbd（Q2 复审通过，9978 行 / 509008 字符）。本次提交 ec21d747，branch story，未推送；未进入 Q4，未跑真实 CLI。

## 1. 旧承担者 → 新承担者

| 原来是什么 | 现在由谁承担 | 旧位置处置 |
|---|---|---|
| `acceptanceEntries(acceptance, sections)` 先摊成 `{section, criterion}` 数组并在遍历里校验 | `knowledgeCriteria(acceptance, sections)` 在**同一次遍历**里校验并直接追加到 rule 数组 | 函数与导出删除；全仓（生产与测试）零引用 |
| `knowledgeCriteria` 再遍历一遍中转数组分组 | 同上，只剩一次遍历 | 第二次遍历退出 |
| `spec/post_check` 拿 `entries` 再 `map` 出编号集合 | 直接用 `byRule.keys()` 做原来的双向差集 | 中转消费退出；`import` 改为 `knowledgeCriteria` |
| UT/testing 的 `knowledgeCriteria(...)` 调用 | 不变（接口与返回形状没动） | 无 |

`readAcceptance` 的文件读取与三态返回（缺文件／解析失败／合法空文档）一个字没改；没有新增第二个通用读取器。

## 2. 保持的行为（等价面）

- `sections` 仍由阶段显式传入：Spec 只 `criteria`，UT/testing 是 `criteria + boundaries`。
- 缺集合、合法空集合照旧；非数组集合、非对象成员、`knowledge_rule` 非字符串或空串仍各自报错，不过滤成功。
- 普通业务 AC（没有 `knowledge_rule`）合法，不进桥接、不被强迫关联规约。
- 同 rule 多个 AC 按原顺序全部保留，不去重。
- `problems` 的文案与触发条件逐条照搬，消费者仍先处理 `problems` 再查 `byRule`。
- 验收条件一条没减：减掉的是**接口与转换**，不是校验。

## 3. 验证

复用既有真实 hook 贯通用例（`test_neutral_knowledge` 用真 UT hook、真 Spec hook 跑）：同 rule 两条 AC 必须都被覆盖、两条全覆盖且普通业务 AC 不误报、缺 id 逐条点名、一条 criteria 桥一串编号被拒、坏 YAML 报解析失败、`boundaries` 计入 UT 桥但顶替不了 Spec 的桥。

新增 3 条（同样走真 UT hook）：集合写成一句话报「不是列表」并点名集合；条目写成裸值报「不是键值对象」并点名第几条；`knowledge_rule: ""` 报「不是一个编号」并点名 AC。**这三条在替换前后都通过**——它们是等价锁，不是区分力用例：Q3 是同语义替换，要守的正是「这些问题的含义一个都没变」。

离线：**pytest 877 passed / 240 subtests，0 失败**（全量为实施者运行记录；main移位不改变pytest收集，不能据此解释与基线的条数差）；失效形态 65 条 FAIL 0、委派 8、PASS 57；`adapt-scan --check` 通过；改动的 `.mjs` 过 `node --check`。

顺带按复审的非阻断项整理测试入口：`test_chapter_view.py` 的 `unittest.main()` 块移到文件末尾，直接执行 `python test/story/tests/test_chapter_view.py` 也能发现文件末尾新增测试；pytest及unittest导入发现原本不受main位置影响。断言一条没改，没有新增运行入口。

## 4. 规模

| 类 | b55b9bbd | 本次 | 在途峰值 | 完成上限 |
|---|---:|---:|---:|---:|
| data | 719 | 719 | 800 | 800 |
| hooks_mjs | 3001 | **2992** | 3030 | 3010 |
| prompts_md | 1839 | 1839 | 2050 | 2050 |
| scripts_mjs | 2660 | 2660 | 2680 | 2510 |
| scripts_py | 1759 | 1759 | 1760 | 1590 |
| **总计** | **9978** | **9969** | 10000 | 8675 |

完整字符 509008 → 508536（**−472**）。逐文件：`shared/contracts.mjs` 159→**151**（−8），`spec/post_check.mjs` 310→**309**（−1）。**净减 9 行**。

对照 03 分册 §0 的估算（新增实现约 45、退出约 50、净减约 5、完成总量约 9973）：实际**新增实现约 37 行**（合并后的 `knowledgeCriteria` 整体，校验语句按替换实现计入，不按 Git 新增行算）、**退出约 46 行**（`acceptanceEntries` 全体与 `knowledgeCriteria` 的中转遍历、Spec 侧的 `entries.map`）、净减 9、完成总量 9969——都在估算范围内，比估算多减 4 行、总量低 4 行。移动为零（没有跨文件搬运）。测试入口整理不进机制预算。

预算额度一个没改：reviewer 在工作树里已把在途 `scripts_mjs` 调到 2680、总量调到 10000，本包按那一份核，五类与总量全部在内，预算测试恢复全绿。

## 5. 边界与未验证

- 真实 CLI 未运行；Q4 未开始。
- `testing/post_check` 与 UT 走同一个调用与同一份返回形状，本包的贯通用例跑的是 UT 那一条（外加真 Spec hook）；testing 侧的分派与覆盖检查仍在它自己的判据里，本包没动。
- `verify` 分派、缺 ID 与覆盖检查仍在各消费者，没有被搬进分组函数。
- 工作树里 `test/story/AGENTS.md`、`TEST.md`、`mechanism-budget.yaml`、`test_mechanism_budget.py` 是维护侧未提交的规约与额度更新：原样保留，没还原，也没并进本包提交。

> 当前独立复审：b4984fd7第二包静态通过，见 [第二包当前评审](第二包评审-2026-09-13.md)。候选三稿已形成，下面旧版“留到最终HEAD”的延期表述已失效；只在最终落位时更新受影响定位。

> 当前独立评审：77314387代码迁移通过，静态交付准备待补齐，见 [第二包当前评审](第二包评审-2026-09-13.md)。下文延期FEATURE候选的提议未获接受。

> 维护实施者 → 维护设计者 / 评审。第二包（Q9 → Q10 → Q11 静态准备）已实施，等整包复审。
> 本包不自宣通过；返修更新本文件，不新开交回。
>
> **当前版本：第 3 版**（2026-09-13）。[第二包评审](第二包评审-2026-09-13.md) 已判
> **静态评审通过，进入最终真实验收准备**。本版只做三件事：把评审在候选稿里直接校正的
> 三处口径同步进本文件、记下调整后的在途额度、写清下一步在谁手上。**代码未变。**
>
> 通过不等于本轮需求交付完成：真实 CLI、模型效果评价、G2 与正式映射落位都还没做。

# 第二包交回 · 结构归位与静态交付准备 · 2026-09-13

基线 `c3819a30`（第一包终点，实测 10260 有效行 / 529235 字符）。

## 1. 提交范围

| 提交 | 内容 |
|---|---|
| `d2031781` | Q9：`story_flow.py` 拆进 `core/flow/`，`materials.py`/`import_sources.py` 归 `core/materials/`，`flow-check.mjs` → `core/flow/check.mjs` |
| `61ee01a9` | Q10：`knowledge-use.mjs` 拆成 document/validation/projection，五个调用方改直接 import；入口与提示三处同步 |
| `55ef0383` | Q11 静态：版本与发布说明、指南与交付流程三处对齐、三处失效指向修正 |
| `77314387` | 版本按用户指示走 1.9.x（本轮 `1.9.1`）；manifest 的版本注释整段移除 |
| `b4984fd7` | 第二包评审 R1/R2 返修：三份 FEATURE 候选、发布说明按真实能力改正、退出清单归口到映射（**无代码变化**） |
| — | 复审通过后同步评审校正的三处口径（只动本目录的候选与交回，无提交） |

branch `story`，**未推送**。工作树里 `mechanism-budget.yaml` 与 `test_mechanism_budget.py`
是维护侧的在途改动，原样保留，未并进任何提交。

## 2. 各 Q 的结果

### Q9 流程与材料功能目录归位

`story_flow.py` 1123 行按《功能目录与规模》§4 的归属拆开，入口只剩 68 行的参数解析、
分派与顶层 JSON 输出：

| 新落点 | 承担 | 有效行（估区间） |
|---|---|---|
| `flow/state.py` | 契约路径与关卡常量、`load/save/require`、轮次条目 | 63（70–140） |
| `flow/inputs.py` | 一次性侧车、本 AR 定位与选项、工作区骨架（`init`） | 309（200–350） |
| `flow/routing.py` | 材料事实、关卡状态、`next_step`/`scope_step`/`spec_stage_step` | 226（160–280） |
| `flow/decisions.py` | `decide` | 110（120–220） |
| `flow/rounds.py` | `round` / `reopen` | 105（100–190） |
| `flow/submission.py` | `complete`：候选核对、三身份、原件留存 | 181（180–300） |
| `flow/lifecycle.py` | `status` / `story` / `archived` | 121（120–220） |
| `story_flow.py` 入口 | 解析、分派、顶层输出 | 68（70–130） |
| `materials/importer.py` | 导入实现（`cmd_import` 承接原 main 的导入正文） | 355（380–470） |
| `materials/registry.py` | 材料身份、清单与摘要 | 194（180–260） |
| `import_sources.py` 入口 | 四种模式的分派与输出 | 91（30–70） |
| `flow/check.mjs` | JS 侧只读消费流程契约 | 276（200–350） |

`import_sources.py` 入口 91 行超估区间 30–70：原 main 有四条互不相同的模式（preview /
图片三态 / register-ux / import），每条都要自己的前置校验与输出。导入正文已经搬进
`cmd_import`，剩下的就是这四条分派；把它们再拆一层等于给作者多记一个命令，没有做。

**算法一行没改**：三级关卡值域、人签、材料版本判轮、C00 三身份与保存失败恢复都是原实现移位。

**资源定位收成一条规则**：`flow/state.py` 的 `SKILL_ROOT` 一处定位合同、模板与
`story-build.mjs`；默认工程根只在 `materials/importer.py` 定义一次，两个入口都引它——
此前两个入口各写一遍 `parents[6]`，往下挪一层文件只会有一个被改对。

### Q10 知识应用归位与入口同步

`knowledge-use.mjs` 499 行拆成三块：`document.mjs` 115（真源文件的定位、读取、规范化与骨架）、
`validation.mjs` 171（完备性与在册性）、`projection.mjs` 152（§10/§11 生成区的渲染、写入与
只读保护）；入口 70 行只剩 `init`/`render` 两条命令，**导出为空、不转发**。五个调用方
（plan/spec 两个 post_check、`story/appendix.mjs`、三份测试）改成直接 import 所属模块。

同一份取值规范化（`text`/`requirements`/`NO_CANDIDATE`）只在 `document.mjs` 维护，验证与
渲染读同一个结果。

入口与提示同步三处，都是**此前与实现不一致**的地方，不是措辞偏好：

| 位置 | 旧说法 | 现行行为 |
|---|---|---|
| SKILL / phases·spec | 交付门「核读者审查那一项写没写成形态」 | 核的是**实际结论**：判的不是 PASS 就不交付；没登记审查员时记一笔「未经读者语义审查即交付」（Q8 已是此行为） |
| rules·inbox_import | 登记命令用 `\` 续行 | 本项目 shell 是 PowerShell，那个反斜杠会被当字面参数；改成一行 |
| story-chapters.json ×3 / document.mjs ×1 | 指向早已退场的 `lint-rules.mjs`、`headings.mjs` | 指向 `core/story/language.mjs`、`core/story/document.mjs` |

失效形态 M06 的取材也改了：原来按**文件名**认「知识派生模块」，拆分之后会漏掉真正派生的
`knowledge-use/document.mjs`，而门禁照样报通过。改成按「谁把激活清单变成派生值」认，
并排除命令入口（入口的出声方式是非零退出码，不是 throw）。实测覆盖 1 个模块 → 2 个。

### Q11 §1 / §4 静态准备

见 [第二包-交付准备-2026-09-13.md](第二包-交付准备-2026-09-13.md)：版本与发布说明、
指南对齐、本轮旧机制退出清单、FEATURE 三稿的候选状态与现行实现清单。

两件具名交评审裁定：

1. **版本与 manifest 注释**（用户 2026-09-13 两条指示）：版本继续按 1.9.x 演进，本轮记
   `1.9.1`，不跳 2.0.0；manifest 里 `version:` 上面那 39 行版本注释整段移除。
   演进记录此后只在 `test/story/release/` 的发布说明里；`version:` 上面那一段按 adapt 的
   所有权规则本就归装它的工程（目标写「我们这个仓怎么用它」），用例改成守这一条：
   包再往那里写演进记录就红。manifest 版本此前停在 `1.8.0` 而仓里已有 1.9.0 发布说明，
   这个落差我没有倒签补注释——注释已整段移除，落差随之消失。
2. **FEATURE 三稿留到最终 HEAD**：11 §4 要求「以最终 HEAD 重建、行号取最终 HEAD」。G2 之前
   还会有返修，现在重建 4262 行的行号在 G2 后全部作废，而三稿的价值恰在行号能落到实处。
   本包备齐重建所需的现行实现清单与要改的三点，**不声称三稿已完成**。

### 第二包评审 R1/R2 返修（本版新增）

**R1 三份 FEATURE 候选已形成**（本目录，基线 `77314387`）：
[FEATURES.md](FEATURES.md) 7 组共 **43 个细分功能项**（F1.1–F7.2 四十项，F2.4 另有三个子项；统计含 F2.4 父项），每项写目的 / 输入 / 现行实现 / 消费者 / 失败出口 / 证据、
[FEATURE-MAP.md](FEATURE-MAP.md) 功能 → 实现（文件·符号）+ 本轮退出项、
[FEATURE-CONTENT-MAP.md](FEATURE-CONTENT-MAP.md) 实现 → 功能逐内容归属。

- **双向覆盖机械核过**：54 个代码文件、**642 个顶层符号**逐个比对，登记差集为空；
  提示词与规则页按二级标题、合同与配置按顶层键登记。核对脚本是一次性的，不留生成器、
  不建维护协议——评审明确不要新增这两样。
- **按符号定位不写行号**：上一轮三稿的 `d36edb70` 行号锚在本轮已整片失效（八个文件改了位置）。
  返修时只更新受影响条目，不预先假定全部作废——这一点是我上一版的判断错了：
  「将来会漂移」不构成现在不做完整性检查的理由，而完整性检查恰恰是本轮
  「新机制齐不齐、旧机制退没退」唯一能机械核的地方。
- **沿用功能 ID**：职责搬迁、判据修正与四处能力增量（F1.7 / F2.3 / F5.5 / F5.6）在既有功能项下
  标注，便于与上一轮逐条对照。粒度按真实职责定，**不以证明「没有加功能」为目的**——
  我上一版把它写成了论证目标，评审已在候选稿里校正；后续出现独立能力再调整粒度。

**R2 发布说明与退出清单按实际能力改正**：

| 改了什么 | 原来写的 | 实际 |
|---|---|---|
| `1.9.1.md` 升级一节 | 「业务对接实现一个字节不碰」 | **看来源**：Demo 来源不给也不覆盖（目标自己按合同实现），业务仓之间整份换成来源版本 |
| `1.9.1.md` 生成区一节与功能表 | 附录与评审记录统一写成「逐行与真源比对」 | 分开写：附录是 `appendixZoneProblems` 全篇检查时逐行比；评审记录是渲染时按摘要判动没动过、动过就拒绝重渲并说明撤销，人工区逐字节保留 |
| 退出清单 `copyedit` 行 | 「退场，无替代」 | 退的是**自证载体**（`COPYEDIT_ROWS`、check ⑫d、台账冻结项、七行自证）；整稿职责由 `story/check.mjs` 的跨章核对、`chapter.mjs::chapterProblems` 的写前核与作者面的整稿方法承接 |
| 退出清单归档副本行 | 「被上游原件留存替代」 | 两件不同的事：归档副本特权收敛到**登记身份规则**；上游原件成独立材料是 F1.7 的**新增**，不替代它 |
| 退出清单「有意保留」 | 列了三处，含 manifest 版本注释里的旧名字 | 那一段已整段移除，现在只剩一处：`drafts.mjs::chapterDraft`（同名不同物） |

退出清单不再在交付准备件与映射两处维护，**唯一记录处**是 FEATURE-MAP 的「本轮退出项」。

### 复审直接校正的三处（本版已同步）

评审在候选稿里改了三处口径，我把它们的理由记在这里，免得下一版又写回去：

| 校正 | 我原来写的 | 现在的口径 |
|---|---|---|
| 功能项计数 | 「7 组 36 个叶子」 | **43 个细分功能项**（F1.1–F7.2 四十项 + F2.4 的三个子项，统计含 F2.4 父项）；数叶子与数功能项是两种口径，要写明是哪一种 |
| 功能粒度的理由 | 「本轮没有新增功能叶子…写成新叶子会让『本轮是替换不是加功能』看不出来」 | 粒度按**真实职责**定，**不以证明「没有加功能」为目的**；沿用 ID 是为了逐条对照，后续出现独立能力再调粒度 |
| F5.5 的失败出口 | 「报告读不到、结构不全、结论取不到时 `reviewVerdict` 为 null」 | 两种要分开：报告缺失或结论格不可取得才是 null；**汇总状态已可读而后续证据/明细结构不完整时保留 verdict 并另报结构问题**——一概写成 null 会把「审查判了但报告没写全」误成「没判过」 |
| 知识内容的登记口径 | 「`knowledge/` 内容归目标工程，本稿只登记谁读它，不为正文分功能」 | 正文按文件、条目表/处置节与模式分节归入 F4 相应功能并列消费者；**内容由目标工程维护不代表它不在清单内**，只是不为每条业务知识另建机制功能 ID |

第四条与第三条同属「一面登记一面又声称不纳入」这一类，评审一并改了。

## 3. 旧承担者与退出

逐项清单在交付准备件 §3（15 行，每行都核过「新唯一落点 / 消费者已切 / 旧位置已删」）。
核对方式：在 `doc/extensions/` 全量搜 16 个已退场的名字，只剩一处**有意保留**——
`drafts.mjs` 里与 story-build 那个同名不同物的局部函数 `chapterDraft`。

另核一遍「每项保留能力有唯一现行实现和真实消费者」：`.mjs` 的导出**零个**没有消费者。
Python 侧有 22 个函数只在自己模块内被调用——那是模块内 helper，不是死代码（跨文件消费者为零
不等于没有调用者），所以不作为孤儿报出。

## 4. 验证

全部离线，按 TEST §7 并行跑（下表是第 1 版的结果；**R1/R2 返修没有代码变化**，
按评审「只做文档、映射与来源核对不重复刷全量」，本版未重跑）：

| 项 | 结果 |
|---|---|
| `pytest test/story/tests -n auto --dist loadscope` | **961 passed / 283 subtests**，约 39 秒；唯一红的是预算那 2 条（见 §5） |
| `check_failure_modes.py` | 形态 **64 条：FAIL 0、SKIP 0、委派 8、PASS 56** |
| `adapt-scan.mjs --check --target .` | 通过 |
| `story-build check --offline --story` 金样 | 通过（10 章） |
| 单文件规模 | `doc/extensions/` 下全部交付脚本**无一超过 500 有效行** |

新增用例（每条都做过反向验证——把改动撤掉，用例应当失败，逐条实测）：

| 用例 | 判什么 | 撤掉什么会红 |
|---|---|---|
| `test_flow_module_layout::TheCallGraphHasOneDirection` | 命令问路由、路由不问命令；功能模块不 import 入口；入口只留 `main` | 任一反向 import |
| `…::ImportingAModuleChangesNothing` | 9 个子模块单独 import：不输出、不写盘、退出 0 | import 期跑 CLI |
| `…::OneRuleFindsEveryResource` | 换 cwd + 工程根带空格下跑真命令：模板落地字节与 Skill 自带那份一致；关卡值域来自章节合同（侧车说合法也不算）；默认工程根只有一处定义 | `SKILL_ROOT` 数错一层 → 两条全红（实测） |
| `test_s4_commit::RegistrationRunsTheRealChecker` | 登记跑的是真 `story-build`，它的结论就是登记的结论 | `CORE_DIR` 数错一层 → 报 `Cannot find module`，用例红（实测） |
| `test_adapt_ownership::…python_package_arrives…` | 新 python 包真被装上、同名旧平铺文件真被清掉 | 少一半都红 |
| `test_knowledge_use::TheEntryIsOnlyACommand` | 入口导出为空、import 不跑命令、三模块导出边界、依赖单向 | 给入口加一行 `export {…}` → 红（实测） |

**一次真抓到的缺陷**：拆 `knowledge-use` 时入口尾部的自执行守卫被复制成了两份，
`init` 连跑两次（第一次写入、第二次报「已经在了」并退 1）。`test_empty_knowledge_repo`
当场变红；我把重复守卫删掉之后才绿。这条记在这里是因为它正是「只核能力存在就会漏」的形态。

## 5. 功能预算、结构成本与完整规模

实测（口径 `test_mechanism_budget.code_lines`）：

| 类 | c3819a30 | 现值 | 差 | 在途额度 |
|---|---|---|---|---|
| scripts_py | 1778 | **1823** | +45 | 1800（**超 23**） |
| hooks_mjs | 3041 | **3050** | +9 | 3060 ✓ |
| scripts_mjs | 2983 | 2983 | 0 | 3000 ✓ |
| prompts_md | 1724 | 1724 | 0 | 2050 ✓ |
| data | 734 | 734 | 0 | 800 ✓ |
| **总量** | 10260 | **10314** | **+54** | 10300（**超 14**） |
| 完整字符 | 529235 | 532247 | +3012 | — |

**三项相加与实测净增对得上**：

| 项 | 行 | 说明 |
|---|---|---|
| 功能新增 | **0** | 本包不新增业务功能。发布说明、版本注释、指南在 `doc/extensions` 之外或是注释，不进有效行 |
| 功能退出 | **0** | 本包的退出都是**移位**：同一份实现换了文件，能力一项没减 |
| 结构成本 | **+54** | Python：import 行 27 → 94（**+67**），正文净 **−22**（导入入口三处重复的工程根解析与 JSON 输出收成一处 `emit`）；知识应用：import 行 7 → 16（**+9**） |
| 合计 | **+54** | = 实测净增 ✓ |

字符 +3012 里绝大部分是新模块的模块说明（docstring 不计有效行）。manifest 里 39 行版本注释按用户 2026-09-13 的指示整段移除，字符净额已含这一减。

**交回时超在途额度、如实报、额度一字未改**：scripts_py 曾超 1800 共 23 行、总量超 10300
共 14 行，**全部是拆分接线**（上表的 +67 import 行）。我没有为换绿删任何能力、测试或说明，
也没有压行（长 import 折成括号形式本身算了 13 行进去，没有靠合并行掩盖）。

**复审裁定**：认可这笔结构成本，具名把在途额度调到 **scripts_py 1840、总量 10350**
（其余分类与最终目标 8675 不变）。现值 1823 / 10314 因此落在额度内——
**全量 962 passed / 284 subtests 全绿，本轮第一次没有红项**。
额度调整不代表缩减目标已达成，G2 仍按完整功能与实际效果裁决。

## 6. 未验证项

> 复审确认这一节就是最终验收要覆盖的范围；**第二包已无必须先补交的工作**。


- **真实 CLI 未跑**（Q11 §2 才跑）。本包所有结论都是确定性接线与送达，**不声称模型语义质量**。
- 发布说明与两份指南的措辞未经业务使用方验证。
- 默认工程根的**取值**没有行为用例：现有用例一律显式传 `--project-root`（隔离要求），
  所以只核到「定义唯一、两个入口都引它」。要核取值得在另一个工程根上跑真命令，本包没做。
- FEATURE三稿已形成当前候选，正式落位及验收返修后的定位更新留到最终HEAD。
- `story_flow.py` 的成文登记只核到「story-build 的结论就是登记的结论」这一路；
  **登记成功那一路**需要一份能过全部判据的 story，本包没造（金样不在 feature 根下）。

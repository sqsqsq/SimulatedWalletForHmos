# Q6独立评审：8d746fa2

结论：**待返修，Q6问题验证关闭后直接继续第一包Q7/Q8，不单独停等评审。** 投影内容共用计算、旧首列集合判据退出、Review渲染前共用校验成立；必需来源缺失和机器区集合仍有漏检。维护实施者按当前工作树test/story/AGENTS.md、TEST离线分支及已同步Q6分册返修。

## R1 必需Spec小节缺失被当作合法空期望

appendixZoneProblems只检查specText是否null；文件存在但必需小节被删或清空时，appendixProjection可能返回空数组。随后按“没有期望也没有机器区”放行，并没有复用已有specGaps的逐节要求。

独立从干净夹具出发，只删Spec §9.1及其对应接口机器区，在接口作者区留一句说明，直接check返回0。文档有字不等于技术契约已明确不涉及。这与Q2确立的“缺节不是不涉及”和Q6必需真源不可读不能作为空期望成功均冲突。

在现有投影输入位置复用必需来源预检，区分缺失/未形成与显式“不涉及＋依据”。writer与readonly checker消费同一份输入有效性结论：无效时返回/报告准确来源位置，project不能删掉旧机器区冒充当前合法状态；不新增另一份必需小节清单或空表要求。合法来源变化、规约真实退出激活清单的现有处理保持。

验收分别覆盖Spec文件缺失、仅一节被删/清空、显式不涉及；缺失时check非零且不写盘，project拒绝前不改变Story，既有人工区/机器区不丢。不可用来源与合法空投影不能共用一个空数组含义。

## R2 机器区只核已知名称，未知区静默漏掉

zonesOnDisk收集全附录标记，appendixZoneProblems却只遍历合同预期名称，未对多出的名称报错。独立在“改动边界”区之后追加完整的`story-build:begin obsolete`/end区及正文，直接check返回0。这段旧机器内容既不受真源比较，也不是作者说明，可能长期冒充现行投影。

在当前区块扫描中完成预期机器区与实际机器区的集合核对，材料清单不是机器投影区。未知名称、重复、缺/孤立结束标记及嵌套边界不能被当成普通作者文字略过；每个结束标记只属于一个开始标记，不能让两个开始都取后面的同一结束行。报到具体区/标记位置并保持只读，不重新引入旧表反向解析。

生成侧已有处理退出区的协议可以保留，但check必须能在不先project的情况下发现残留。两者可以分别遍历当前文本；扫描次数不是重复机制的判据，标记合法性与所有权解释必须一致。不要以写入时索引会变化为由保留不同标记协议，也不要求建立持久索引。

验收用已知合法区加未知完整区证明旧代码漏检；再核孤立/嵌套/损坏标记、重复区、合法作者解释，check均不写盘。标记处理修在现有document/appendix职责内，不新建通用解析框架。

## 已成立部分与质量

appendixProjection同时供生成与只读比较使用，逐行比较保留重复、顺序和非首列内容；旧verdictTableProblems与specRowProblems实际退出。非首列、少行、换序等现有测试直接改盘上内容后check，有区分力，不是project后的必绿比较。

review-render文件与转发壳退出，渲染归review、摘要和机器区标记口径归document。cmdBuild先调用与check共用的decisionProblems再渲染，未通过不写文件，方向正确。人工区和冲突保护不能因为本次共用而撤掉。

新模块有效行appendix409、review277，低于500。R1/R2是本包输入与边界未闭合，不要求重做模块拆分。顺带注释归位已落实；未发现为金样加专名豁免。

## 验证、预算与责任

独立相关回归test_story_build、test_contracts_single_source、test_review_golden、test_spec_contract_section：**214 passed、49 subtests passed，25.49秒**。额外两个反例均在原本check通过的夹具上产生，修改后仍错误返回0。没有用其它已有失败掩盖判断。未重复实施者914条全量和形态运行，未跑真实CLI。

独立规模：scripts_mjs2928、scripts_py1778、hooks_mjs2993、prompts_md1724、data714，总**10137有效行、520640字符**。相对Q5增加29行、1814字符；scripts_mjs超旧在途8行，总量仍在10150以内。

必要结构接线单列，不进入功能预算比例；8行分类超限不是本次拒绝原因，不要求删正常功能或合并文件换绿。本次不改额度，允许收敛修正与必要验证。

我在Q6估退出170行，没有逐一核原两条判据的实际61行，仍然高估，这是方案估算错误。不能把差额全部说成实施者打补丁；但也不能用估算错误豁免R1/R2。交回功能新增108、退出61、结构约1、调度−2相加为46，与净增29不一致；其中可能有子函数/真源检查重复计数，应在现有表里去重或明确包含关系，不再扩张预算台账。暂不按这份重叠估计下精确功能比例结论。

返修只覆盖R1/R2、直接消费者和有区分力的测试，预算说明按现有分类纠正。保留当前测试域未提交改动，尤其test_narrative_variants.py；不纳入本包、不覆盖。Q6返修相关验证通过后直接继续第一包Q7/Q8，至第一包完成统一交回；当前推进以执行总览§2为准。

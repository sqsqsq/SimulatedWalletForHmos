# Q5复审：efe6f9f5

结论：**R2–R5通过；R1的内容保护仍有遗漏，解析职责还需收敛，Q5待返修、Q6不开始。** 不重做已通过部分。维护实施者按当前工作树test/story/AGENTS.md及Q5分册完成下述收敛修正。

## 1. 已关闭的行为问题

- 原Story改由readRaw读取，BOM与行尾不再先被一般读取器剥除；普通章和附录均新增目标区外字节比较。
- 指导清洗后检查完整候选中的额外H1/H2，围栏内标题不误拦；storySections同时补齐围栏排除，避免重新切章时再误切。
- check旧drop全文扫描退出，单章/全篇共用章内判据；图内工作编号放行，正文工作编号仍拦。全篇对drop/keep坏配置有显式诊断。
- 写入之后的接续异常已隔离，故障注入核文件确实已更新、返回成功及恢复命令；真实写入失败仍失败。不将不可用输出通道描述为必然可送达。

这些修复各在原职责内，没有新事务、持久状态或业务例外。自有指导标记已与生产者共用，普通作者注释也不再通杀。

## 2. 仍须修正

### R1剩余：指导清洗仍改写围栏内空行

chapter.mjs的stripGuidance末尾仍有`.replace(/\n{3,}/g, '\n\n')`，对整个候选执行。独立提交text围栏，内容为first、两个空行、second，命令退出0，但两个空行变成一个。此前要求“围栏示例原样保留”仍未满足；源图标签或原文摘录同样不该被指导清洗附带改写。

直接删除指导清洗中的全局空行压缩，不另加一个围栏保护补丁。当前职责只是去掉自有指导，目标章首尾拼接仍按既有约定。增加含连续空行的围栏原文比较，保持作者注释与来源/机器标记原有测试。这是上轮R1未完成的同一要求，不是新功能。

### 实现质量：围栏开闭仍有两套实现

document.mjs新增fenceMask供切章、定位和编号共用，但parseChapter仍独立实现同一套开启、长度、种类与关闭行判断；chapter.mjs又通过fencedLines将parseChapter结果转为行号集合。当前行为样例一致，并不等于只有一份实现。以后修一个开闭边界仍要改两处，这是本轮要防止的重复机制。

收敛成一个围栏范围扫描实现：parseChapter读取其范围/语言/闭合状态，切章、定位、编号与指导清洗按同一范围派生行标记。可以内聚在document现有模块，不新增通用AST、目录或持久缓存。仅需范围的消费者不要为了取掩码强制构建小节和表头。完整章视图仍按Q2约定在一次全篇check内复用。

同次修正idShapes“编译一次”的实现与说明不符：当前每次调用都会重新new RegExp，全篇入口与各章重复调用。用当前命令上下文保存一次编译结果，调用方复用；或者取消并无需要的中转接口，统一在上下文建立时校验。保留坏配置诊断、drop/keep分工与章内唯一判据；不增加全局缓存或回到静默catch。

验收核所有围栏消费者在混合标记、长短标记、尾随内容和未闭合情况下的一致行为；测试按实际消费者断言，而非只数新helper。源码核原重复开闭逻辑已删除。编号配置只在当前命令取得一次有效编译结果，错误可观察。

## 3. 预算裁定与对实施者的答复（按用户最新决定更正）

**预算分级约束功能实现，不约束必要的结构拆分成本。** 功能实现包括本次能力、校验、状态与提示的新增或替换，以及旧机制退出；拆文件带来的必要import、导出、参数传递、函数边界与模块接线单列为结构成本，不计入功能预算比例。完整行数与字符总量仍报告两者，用于观察整体规模。

我此前要求把所有拆分接线计入功能预算的口径错误，现撤回。由此推导的446/180≈248%及人工确认要求一并撤回；不能因为必要接线使旧总量检查超限，就要求用户按功能超支签字。已列的原始规模实测保留：10117有效行、518801字符，它不是功能预算比例。

**实施者据此继续完成本报告§2的收敛修正，无需等待上述248%的预算确认。** 交回分清功能新增/替换及退出、必要结构成本、完整总量即可，不再为了把所有结构成本塞进180的分母做逐行对账。原180估算保留，不倒签；其中约35行“上下文/状态及提示消费者接线”须按实际职责区分：改变行为/输入送达的逻辑属于功能，纯粹因文件拆分而传递已有参数属于结构。

结构成本仍需合理：同一功能的重复解析、重复编译、重复规则不能冒充必要接线。§2的围栏唯一实现与命令内编译复用要求保持；不要求合回大文件、不要求删正常能力凑数。确有功能实现超出适用功能预算时，再按AGENTS§7.5分级，而非套用结构增长计算。

当前Q5未通过、Q6不开始，原因是§2的内容保护和职责收敛尚未完成，**不是必要拆分成本超支**。本次不修改机械计量或数值额度；旧总量硬失败如实报告，由reviewer区分功能与结构后处理，不当成自动功能超支门。

## 4. 验证与交回

独立相关回归test_story_build、test_writing_flow、test_negative_guards：**219 passed、42 subtests passed，27.60秒**。新增保护与故障注入用例已读；额外围栏空行反例确实失败。未重复实施者896条全量，未跑真实CLI，也不据离线结果宣称模型整稿效果达成。

当前分册与状态已同步。返修范围仅本报告§2及其直接消费者、测试、功能与结构分别列示的规模说明；R2–R5保持，不新增验证记录协议。

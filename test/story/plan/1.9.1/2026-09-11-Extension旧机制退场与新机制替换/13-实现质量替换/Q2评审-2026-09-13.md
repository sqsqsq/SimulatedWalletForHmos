# Q2 独立评审：58533614

结论：**待返修，Q3暂不开始。** 单次解析、全部候选表检查和图位置约束退出的方向成立；当前三个确定性边界仍未闭合。拒绝原因是实际行为错误，不是超出旧预算10行。维护实施者返修前完整读取当前工作树 `test/story/AGENTS.md`，重点§7.5及TEST离线分支；近期维护规则不在demo根AGENTS，不依赖旧上下文。

## 1. 必须修正

### R1 围栏关闭未匹配开启符号，样例能冒充正文结构

位置：`core/story/document.mjs:44–54`。当前遇到任意反引号或波浪号围栏就翻转开闭状态，没有匹配符号种类和长度。

复现：一个反引号text围栏内部放波浪号围栏样例，样例内有 `### 交付物` 和交付物表。`sectionNames`竟返回该标题，`tablesIn`返回该样例表；它们实际上全部在外层代码块内。这会让样例满足正文必要结构，也会污染后续范围。这个错误在原实现也存在，不冒称本次新引入，但Q2接替完整围栏解析职责后必须修正。

替换方式：当前open保存实际开启符号及长度，只用匹配的合法关闭行结束；其它围栏形状在已开启块内作为内容处理。沿用当前一次扫描及范围，不新建通用AST、第二套解析器或业务豁免。

验收：异种围栏不能关闭外层；较短同种标记不能关闭较长外层；真实关闭后正文继续被识别。保留现有未闭合围栏可见、原文保存、CRLF等行为，测试验证结构结果，不只验证记录了marker字段。

### R2 同名小节的表被合并，正文与表头的作用域不一致

位置：`core/story/document.mjs:97–102`。`sectionBody`通过matchSection取第一个匹配节，`tablesIn`却以规范化名称过滤全章表，包含后续同名节的表。

复现：第一个“交付物”小节只有“前节无表”，第二个同名小节有完整交付物表。查正文得到第一节；查表却得到第二节的表。缺表的小节因此可能通过，其它消费者仍读取缺表的第一节。

替换方式：所有按名称查询先用现有matchSection确定同一个节，再按其已有from/to与表line限定范围。不要新增ID台账，也不要用禁止同名标题或删除合法名称支持来绕开。若表上保存的section名称因此成为冗余，核直接消费者后去掉；测试改核实际范围义务。

验收：重复原名、编号规范化后同名、精确优先于包含、缺节null与有节无表空数组均保持一致。完整候选表在目标节内可通过，目标节之外的表不能补它的缺口。

### R3 Spec预检把三个必需来源节当成任选其一

位置：`core/story-build.mjs:2200–2202`。APPENDIX_FROM_SPEC将§9.2、§9.3、§9.4合并投影到一节，不表示三份输入可以互相替代。`res.some(...)`让任意一个有正文就跳过整个组的缺口。

独立复现：在合法最小夹具中完成流程前置后，只删除Spec §9.3、§9.4，保留术语、§9.1、§9.2、§9.5。真实skeleton命令退出0，继续生成草稿。这与交回所说的§9.1–9.5缺失即拦不一致。

替换方式：沿APPENDIX_FROM_SPEC逐一核它实际消费的来源节，合并输出准确的缺失位置；不增加另一份章节表。明确不涉及及依据继续合法，不要求补空表，不升级成Framework全套语义审查。

验收：分别只缺§9.2、§9.3、§9.4均失败，写前文件集合及已有内容不变；这些节全部显式不涉及可通过。不要只测删除整个§9组。

## 2. 实现质量与退场判断

本次不是纯拆文件：chapter-contract退出了重复切文，check内复用章节视图，表检查遍历同一范围内候选，总览位置不再用H3前后代替语义判断。职责拆分比原实现直接，应保留。

当前主要不足是索引虽然集中，索引内部对边界仍有两种解释。R1/R2应修正唯一解析职责，R3应修正既有来源集合的量词，不追加兜底检查或模型自证步骤。

`chapter-contract.mjs:49`的DIAGRAM_FENCE已无消费者，随本包删除并同步解析模块注释与真实返回形状。此处是零散死声明，不据此推断整套旧系统仍在。入口其它同名声明需各自核消费者，不按名字批量删除。

storySections的H2分章、subsectionSpan的写入范围仍按Q5/Q6接线；Spec的pipeTables承担另一输入职责。它们当前存在不能直接判为本包未退旧机制。两个大入口尚未拆完，期限仍为story-build在Q8、story_flow在Q9，新document 67、chapter-contract 85有效行符合本步模块规模要求。

过拟合审视：本包实际变化围绕Markdown结构、合同锚列与既有Spec来源，没有按AR编号、金样原句或业务专名加判据。R1–R3修法也须保持此边界；固定格式能被解析不代表业务表达已达标。

## 3. 金样问题的裁定

不接受交回将AR90006“缺参与方与分工标题”直接定为样本内容缺口。金样§4.1“业务结果以谁为准”已有钱包、支付部件、卡云分工正文及权威来源/钱包处理表，目标语义实际存在。当前标题检查测到的是名称不匹配，不能推导没有讲清分工。

本包已批准保留H3合同，故不把这项历史冲突伪称Q2新退化，也不为通过而改金样或新增它的标题别名。按Q4已有“固定标题代理退出、有效语义风险保留”的职责处理，已补入该分册：区分必要读者目标和字面标题，作者与独立审查仍核分工内容。其它三项材料清单报错不能仅凭这个判断一并豁免，本评审未裁定其内容正确性。

## 4. 验证与预算

独立相关回归：test_chapter_view、test_story_build、test_author_task_package、test_image_registration、test_writing_flow，共 **240 passed、41 subtests passed，33.05秒**。上述三个反例为额外离线复现，说明现有绿测没有覆盖这些边界。本次未启动真实CLI，不宣称证明模型首次成文或金样效果。交回全量864 passed、两项预算失败和65条形态结果属于实施者证据，本次未重复全量。

独立规模：prompts_md 1839、hooks_mjs 3001、data 719、scripts_mjs 2652、scripts_py 1759，总9970有效行、508288字符。相对71c2be05增加19行、2400字符；scripts_mjs超当前在途峰值12行，总量超10行。新增模块+67、原合同−56、入口+7、方法页+1，不能把移动55行算退场节省。

本包预先缺新增实现预算属于方案缺口，不能反推9970/9960当作新增预算比例，也不能把交回估计“真新增约30”直接充当初始批准预算。认可维护实施者保留硬失败并交reviewer的做法。当前允许完成R1–R3收敛修正与必要验证；本次不改额度，不要求为减少10行删正常能力，不批准据此进入Q3。

返修交回保留既有口径，报告实际新承担者、退出者及最终量；reviewer在放行下一包前补齐尚未实施范围的新增/退出预算及适用起点，历史已实现部分保留实测，不能倒签或重置累计量。最终8675目标没有因本次增长自动提高。

## 5. 返修交回

一次交回R1–R3及直接残留清理，附新增边界结果、相关回归、规模与消费者变化。原有三项正向替换保持，Q2通过后才进入Q3。当前总览与Q2/Q4分册已同步；无需实施者拼历史评审推导新要求。

# Q4 交回 · 作者说明保留语义、退出重复定义 · 2026-09-13

> 当前状态：**返修21a593f7独立复审通过**，见 [当前复审](Q4复审-21a593f7-2026-09-13.md)。下文为首版交回记录，当前规模与结论以复审为准。合同：[00-执行总览](00-执行总览.md)、[04 分册](04-作者说明同语义重写.md)（含 §0 估算）、
> 语义基线 [目标与写作语义](目标与写作语义.md)。开工前读取当前工作树 `test/story/AGENTS.md`（重点 §7.5）与 TEST 离线分支。
> 基线 ec21d747（9969 行 / 508536 字符）。本次提交 a4d45de2，branch story，未推送；未进入 Q5，未跑真实 CLI。

## 1. 旧段落 → 新唯一落点（等价合并）

`story-write.md` 354 → **242** 非空行，六部分：读者与通用组织 / 动笔前 / 写一章 / 写后核对 / 十章差异 / 归档与交回。

| 旧段落 | 新落点 | 变化类型 |
|---|---|---|
| 「写给这样的读者」十三条 | 一（读者原则） | 等价，「人读改写后再核对全部事实」并入四 |
| 成文两步框图 + 为什么不是一次写完 + 没有先分配 | 开头三段 | 等价合并 |
| 动笔前三处分工、未定只作边界、叙述不落附录（含附录五节与作者区） | 二 | 等价 |
| 同一件事讲透一处 + 必要重现不是重复 | 一（形式那一节） | 等价 |
| 「什么内容用什么形态」表 + 「哪种图讲什么」表 + 「章首那张图讲给评审者什么」三段 + 「上游那张在 story 有对应」 | 一「形式按内容的关系选」一张表加三段；逐章差异留在五 | **同义合并**（同一条「按关系选」定义原来讲了三遍） |
| 别写成长文两段 | 一（并入「不以数量代替判断」） | 等价 |
| 第一步按章写（命令、落盘唯一路径、中断恢复、每章输入表）、怎么写一章（写给人读、表转散文每列有落点、上游图搬运、图单向与登记、验收编号不自造后缀、规约判定一条一行、空章） | 三 | 等价 |
| 决策登记（议题定义、十一类扫描表、已定写取舍位置、澄清三段式、根据指名、标题陈述句、不写档位名） | 三·决策登记（**唯一维护处**） | 等价；`phases/spec.md` 的第二份改为指路 |
| 「机器不判、要你自己把关」三条 | 表前引导→四#5；小节名→一；位置那条见下 | 拆到它们各自该在的地方 |
| 第二步统稿七项 + copyedit 七行 | 四：三个动作，动作下七条与 copyedit 七行**一一对应** | 等价重组（协议按分册保留到 Q5） |
| 归档四条红线 + 需求不是演示 + 交回前 + 两条自检 | 六 | 等价；交回动作改为 `story_flow.py story`（它自带 number/project/check） |

**已批准的纠正（不是合并）**：

- 「同一件事只占一个结构位置——表、文、图是三种形态，不是三次机会」**退出**：与「图讲关系、表讲属性、文字讲理由是互补」相反，留着模型只能各取一条。
- 「②b/第二步是唯一一次通读全篇」**改写**为「把成稿从头读到尾的那一次」（`phases/spec.md` 同步）：逐章写时已经要求比对已写内容、只补独有信息。
- 已否决的配额**退出**：超过 12 步 / 少于三项 / 只有一列一行 / 字数段数句长门槛。
- 任务包（`author.mjs`）里的图片**方法**段退出（单向、引进正文再解释、附录不列图）：方法归作业书，任务包只给这一轮有哪几张图、每张的命令与指路；命令围栏标 `powershell`，并写明换 shell 要自己核引用规则。

## 2. 标题代理逐项审视（分册 §4）

| 合同项 | 它判的是什么 | 处置 | 直接消费者同步 |
|---|---|---|---|
| 范围「特性分工」 | 内容目标（分工讲清没有） | **退出机器核** | 结构检查、草稿种子、逐章方法、`questions`（已问「本部件不负责但其他部件仍需完成什么」） |
| 范围「交接约定」（`when: siblings`） | 内容目标（兄弟交接） | **退出** | 同上 + `questions`「兄弟 AR 各承载什么、边界、先后依赖」 |
| 业务方案「参与方与分工」 | 内容目标（评审点名的金样误报） | **退出** | 同上 + `questions`「全部参与系统及各自输入、输出、责任与失败影响」 |
| 交付与上线「回退设计」 | 内容目标（回退措施与依据） | **退出** | 同上 + `questions`「可独立控制的对象怎样启用、停用与恢复」 |
| 交付与上线「交付物」 | **机器接口**：交付物表的 `at` 定位范围 | 保留 | 不变 |
| 附录五节 | **机器接口**：投影区、材料清单集合、⑫ 节名集合 | 保留 | 不变 |

四个内容目标退场之后，`when: siblings` 再没有使用者——**条件机械随之退场**：合同的 `when`、`chapter-contract.applies`、`story-build` 的 `siblingsFact` / `offlineFacts` / `ctx.facts`。它唯一的用途就是那个条件标题，留着就是零消费者分支。`chapterStructureProblems(ch, view)` 少一个参数。

草稿种子跟着同一份解释走：只预置机器要定位的那一节（`### 交付物` 与它的表），内容目标那类不预置标题——名字由作者按业务起。

**金样**：`AR90006` 的「业务方案缺『参与方与分工』」这条误报消失（评审已裁定它讲清了分工）；判据锚 `AR90004` 仍通过（10 章）。没有改金样、没有加标题白名单。

**失效形态**：`S08-solution-chapter-flat` 按同一依据 `retired`（reason 写明纯标题代理为什么退、`approved_by` 记本分册批准），语义风险仍由 `observed_by`（读者审查按业务方案章的读者问题看参与方、取舍、风险）与合同 `verdicts.chapter_dimensions` 承担——不把「取舍无依据」一起退休。同源的 S09/S12/S13 此前已按 08 §3 与 C01/C02 退役，本包不动它们的台账。

## 3. 语义保留验收（独有义务逐项）

动作前可取到：十章与必要下级组织（合同 `questions`/`boundary` → 草稿头；逐章方法 → 作业书五）、无页面能力（作业书·功能说明明写「没有页面就写调用方、后台作业或管理动作」）、多对象恢复（交付与上线：对象成表、关闭入口不等于撤销业务）、跨章互补与必要重现（一）、全部有效来源与原图（任务包 §4/§4a/§4b/§4c）、真实判断与未决（三·决策登记）、可执行验收（五·验收）、交付时机（五·交付与上线：写在表里或上线路径里都行）、自包含与错误恢复（六）。

读者问题没有被抄成第二份：作业书不复述 `questions`，只讲组织方式。章头指向的「十章各自怎么组织」真实存在（第五节）。

## 4. 验证

- `test_writing_flow` 六条按**义务**改写（不是改字面）：写后核对锚到三个动作并核七条与 `COPYEDIT_ROWS` 一致；`copyedit` 恰好七行；议题定义**只维护一份**且阶段页指路（新断言：阶段页不得再出现定义正文）；总览图那一段核「主路径与全部分支去向 / 交接点 / 不为了与上游不同而刻意画不同」与形式表的三种关系；图片方法在作业书而命令在任务包；表前引导与小节命名各有其家，且「不是三次机会」这句不得再出现、「三者互补」必须在。
- `test_story_build`：围栏样例不顶替（现在只剩「交付物」一个要定位的小节）；**用业务名起的标题不再被报缺失**（范围与业务方案各起一个业务名小节，check 通过）；种子只预置机器要定位的那一节。
- `test_author_task_package`：任务包给数据与命令、指出方法在哪；方法本身在作业书。

离线：**pytest 876 passed / 240 subtests，0 失败**；失效形态 64 条（S08 退役后）**FAIL 0**、委派 8、PASS 56；`adapt-scan --check` 通过；改动的 `.mjs` 过 `node --check`；负面扫描无新增命中。

语义复核请评审直接比新旧说明：我给的是旧段落→新落点表与上面的义务清单，不声称测试能证明模型理解。真实 CLI 未运行，不承诺首稿效果或耗时变化。

## 5. 规模

| 类 | ec21d747 | 本次 | 在途峰值 | 完成上限 |
|---|---:|---:|---:|---:|
| data | 719 | **713** | 800 | 800 |
| hooks_mjs | 2992 | **2986** | 3030 | 3010 |
| prompts_md | 1839 | **1721** | 2050 | 2050 |
| scripts_mjs | 2660 | **2643** | 2680 | 2510 |
| scripts_py | 1759 | 1759 | 1760 | 1590 |
| **总计** | **9969** | **9822** | 10000 | 8675 |

完整字符 508536 → **503442**（−5094）。逐文件：`story-write.md` 354→242（−112）、`phases/spec.md` 129→123（−6）、`author.mjs` 264→258（−6）、`story-build.mjs` 1580→1568（−12）、`chapter-contract.mjs` 84→79（−5）、`story-chapters.json` 385→379（−6）、`SKILL.md` 208（措辞同步，行数不变）。**净减 147**。

对照 §0 估算（替换实现约 320、退出约 374、预计净减约 54、总量约 9915）：实际**净减 147、总量 9822**，比估算多减 93。差额不是删义务，来源有三笔：① 同一条「按关系选」定义原来讲了三遍，合并省下的比估算多；② 标题代理退场连带退了合同数据、`applies` 与 `siblings` 三处代码（估算只算了文字）；③ 议题定义与图片方法各退掉一份重复。上面第 3 节逐项列了义务的新落点——**全文更短但义务丢失不通过**，请按那张表核。

预算额度一个没改（reviewer 已调整的 2680 / 10000 保持），五类与总量全部在内。

## 6. 边界与未验证

- `copyedit` 七行协议与它的消费者（`COPYEDIT_ROWS`、⑫d、冻结台账）按分册保留，Q5 随实际整稿接续同包退出；本包没有偷改成另一套记录。
- 独立审查的全文输入、`reviewVerdict`、reader 方法归一仍按 Q8；本包只同步了与作者面相反的说法，没有动 overlay。
- 真实 CLI 未运行；Q5 未开始。
- 工作树里 `test/story/AGENTS.md`、`TEST.md`、`mechanism-budget.yaml`、`test_mechanism_budget.py` 是维护侧未提交的规约与额度更新：原样保留，未还原也未并入本包提交。

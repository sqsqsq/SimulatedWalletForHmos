# Q3独立评审：ec21d747

结论：**通过，可进入Q4。** 本包在保持功能的情况下删除无独立用途的中转，比原实现直接；未发现需要返修的生产行为问题。维护实施者进入Q4前读取当前工作树test/story/AGENTS.md（重点§7.5）、TEST离线分支及已更新Q4分册。

## 1. 方案与实现质量

acceptanceEntries函数及导出已删除，生产代码和测试无引用。knowledgeCriteria直接在一次section/row遍历里校验并按rule追加原条目，退出中间包装数组及第二次分组遍历；未增加兼容壳或新通用读取器。

Spec改用byRule.keys形成编号集合，双向差集逻辑保留；UT/testing继续消费同一个byRule接口，阶段显式选择的criteria/boundaries不变。readAcceptance未改，坏集合、坏条目和非法knowledge_rule的诊断条件与文案保留。普通业务AC不被强迫关联规约；同rule多条AC的顺序、重复和原对象均保留。

这是完整替换一项职责，不是删条件换预算，也不是把旧函数搬到另一文件。注释说明当前映射的必要性与中转无独立消费者的原因，没有混入案例编号或金样特例。无需为了再省一两行压缩校验分支或合并不同错误。

Q2留下的测试入口整理已完成，unittest.main位于文件末尾，所有测试断言保留。

## 2. 独立验证与证据边界

运行test_neutral_knowledge、test_contracts_single_source、test_spec_story_gate、test_chapter_view、test_mechanism_budget：**58 passed、26 subtests passed，18.20秒**。包含真实Spec/UT hook贯通、形状错误、多AC及阶段集合差异；同时检查testing消费者源码，其接口与分派未变。本次没有单独实跑testing hook，不把共享接口检查说成端到端实机证明。提交diff空白检查通过。

独立计量：hooks_mjs2992、scripts_mjs2660、scripts_py1759、prompts_md1839、data719，总**9969有效行、508536字符**。相对Q2净减9行、472字符；contracts151、Spec post_check309有效行，已改模块低于500行。预算测试通过，额度未由本包自行提高。

实施者报告877条全量及失效形态全绿属于其交回证据，本次未重复全量或启动真实CLI。交回对测试入口的解释有一处措辞需纠正：main位置影响直接执行test_chapter_view.py，不影响pytest或unittest导入发现；不能用main移位解释pytest总数增加。该说明错误不影响真实实现和上述独立结果。

## 3. 预算与后续

本包预计新增替换实现45行、退出约50行；交回约37/46的分项估计与实际净减方向一致，独立可精确确认的是净减9行。没有将旧校验语句保留误算成全新业务能力，也没有将文件移动算退场。分项估计不作为整轮预算比例的独立分母；保留当前总量目标与在途额度。

Q4进入作者说明同语义重写，成败须看独有义务是否完整、送达是否及时、固定形式代理是否退出，不能仅看提示词行数。Q4分册已补实施前估算及明确退出对象。copyedit仍按Q5退出，真实审查链仍按Q8实现，不借本次通过提前宣告它们完成。

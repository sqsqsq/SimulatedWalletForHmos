# Q1独立评审

> 最新复审：71c2be05通过，当前完整结论见 [独立复审报告](Q1复审-71c2be05-2026-09-12.md)；下文保留首轮发现与复核过程。**执行者在进入Q2前先完整读取当前工作树的test/story/AGENTS.md，重点§7.5，再读TEST对应预算/离线分支。** 规约只在Story维护目录，demo根AGENTS未增加本预算要求；不要沿旧会话记忆或只读HEAD版本。无需新增“已读”台账。

对象7f672e33，基线88d3a81a。本次核Q1合同、实际diff、消费者和正常路径；未进入Q2、未运行真实Story CLI。

## 1. 结论

**Q1暂不通过，限本包两处返修。** pending在round后仍阻断、查询透传工程根、status/complete复用材料事实、JS共用客户端的方向成立。未通过主要是等价优化没有覆盖全部调用路径，而不是因为净增57行或大文件尚未拆完。

## 2. R1：第二/三级decide仍重复读取同一时点材料

cmd_decide在非material_scope分支的前置检查调用 `next_step(feature_root, contract)`，未传manifest；保存关卡后又调用 `next_step(feature_root, contract, manifest)`。manifest只在第一级补料请求分支赋值，正常scope_decision时仍为None，两次next_step各自读盘。

本次用现有S4Case正常走到scope_decision，在单次cmd_decide调用中计数materials.build：**code=0、next=generate_design、build=2**。测试覆盖了status和complete，没有覆盖该正常关卡路径。

准确处理：在本命令首次需要材料时取得一份manifest，前置、驳回判断与末尾路由全部传同一份；只有契约变化时重新派生changed，不重读未发生写入的材料。不要只给scope_decision加例外，检查split_carrier的同一调用路径。保留写入材料前后不同快照的边界，不增加跨命令缓存。

验收：正常第二级、正常第三级、第一阶段补料接受/拒绝都各按实际读写时点计数；状态/决定/错误结果保持，status一次、complete写前与写后两次的已有断言继续成立。

## 3. R2：错误/未开始分支的重跑命令丢失参数引用

author.positionSection生成命令时直接拼 `--feature ${feature} --project-root ${projectRoot}`。新增的说明让模型自行给空格路径加引号，而本模块已经导入shellArg，正常图片命令也在使用它。

使用既有shellArg渲染参数，保持当前可用正式脚本路径；不要为这两处创建另一套命令构造器。带空格、美元符号等路径的正常查询虽已验证，错误分支给出的重跑命令也要按原样可执行，不能把确定性处理留给模型。

## 4. 已通过的部分与实现质量

- 两份解释器/JSON调用胶水已退出；flow/client有两个真实消费者，没有多套运行路径。
- pending/changed作为只读事实返回，skeleton不再猜单个next字符串；round不会把未导入变成已导入。
- status、round及complete的共用快照有实际接线；写入后refresh保留。客户端与调用者的阻断策略分开，优于将所有策略揉进共用函数。
- S4三种身份及恢复回归保持。GBK环境变量下另做对照，返回中文action与UTF-8一致：Python入口已有reconfigure，**不需要在JS另加一份编码补丁**。
- 新客户端49有效行属于有界共用能力；新增格式检查解决输出协议问题，工程根问题实际由显式参数和跨工作区调用解决，两者不能混称同一个因果。

因此不是单纯搬文件，但“整个decide只取一次”的交回结论过强，须按R1补齐后再宣告。解释器ENOENT后备分支尚无用例证据，可用隔离替身/mock补验证；环境有python不等于该分支不可测，不必新增测试设施。

## 5. 验证与预算

本次独立重跑flow_status_client、material_rounds、s4_commit、author_task_package、mechanism_budget：**150 passed / 19 subtests passed**。另复现第二级决定的两次读取；未重复运行交回的全量846项及其它静态门。

实测**9945**行：data719、hooks3000、prompts1838、scripts_mjs2633、scripts_py1755。按Path.read_text相同换行口径完整字符**505501**，交回505503有2字符差异，不影响功能裁定，但应标测量口径，不能使用不一致读法比较微小字符收益。

本包相对88d3a81a净+57，没有净减。共享快照与必要结果接口可以有成本，不能退回丢pending或错误工程根的实现凑数。依据用户合理例外授权，认可**进行中峰值scripts_mjs2640、scripts_py1760、总9960**用于本包收口，完成目标不变；这不是批准把重复扫描永久保留。预算认可与功能通过分开。

## 6. 脚本拆分实际进度

**尚未全部完成。** 当前core/story有drafts（54）和chapter-contract（141），core/flow只有client（49）。主脚本仍是story-build 1569、story_flow 1100有效行。

后续：Q2建立文档解析归属；Q5–Q8逐项归位成文职责，Q8结束story-build应成为≤500的小入口；Q9拆flow/materials，Q10拆knowledge-use并同步入口；Q11最终全量核每个交付脚本。knowledge-use目前499、import_sources461，也要按所属目录迁移，不能只看是否已低于500。

本次不为了看起来拆完先搬所有函数。先完成Q1返修并通过，再进入Q2；当前所有已落地模块均低于500，大入口属于有明确退出步骤的中间态。

## 7. 71c2be05复审：通过

R1：cmd_decide内部首次需要时取manifest，前置路由、补料驳回和最终路由共用局部snapshot；生命周期限本命令，没有全局缓存或为不同关卡复制分支。正常第一级、驳回、第二级、第三级均计数一次，status一次、complete写入前后两个时点保持。对同一manifest多次派生pending/changed不是重复磁盘扫描，不必为了调用次数再增加机制。

R2：重跑命令复用shellArg，并引用客户端真正调用的FLOW_SCRIPT绝对路径，支持跨工作区、含空格和美元符号的工程根。导出这一条路径服务实际命令渲染，不是为了保留旧测试的兼容壳。

本次独立重跑flow_status_client、material_rounds、s4_commit、author_task_package、mechanism_budget：**156 passed / 19 subtests passed**；diff空白检查通过。全量852项与其它静态门为实施者交回，本次未重复跑全量，未运行真实CLI。

实测9951有效行、505888字符；五类为data719、hooks3001、prompts1838、scripts_mjs2634、scripts_py1759。相对Q1基线净+63，没有净减，不能称规模优化已实现；但重复扫描/查询循环确已退出，两个原功能问题关闭，新增接口的用途与消费者明确。本包可通过，后续继续核等价精简收益，不要求为省行退回错误实现。

交回同时写“评审已认可额度”和“仍待用户签”相互冲突。该在途额度已按此前用户合理例外授权具名认可，本轮不重复请人工签。新规约的100%/125%/150%按新增实现预算计算，不能拿9951/9960套档；当前分级机械接线尚未改造，不宣称已经自动分级。旧在途额度与阶段状态保持，不回退重启。

**下一步：先读最新维护规约，再按Q2分册实施。** 方案制定/实现/reviewer都以防走偏、补丁叠加、旧机制残留与功能干扰为目的，超预算先审实现与方案，不删正常功能或只改数字换绿。相关规约目前含未提交工作区修改，实施者应保留，不误作本包新增代码或覆盖还原。

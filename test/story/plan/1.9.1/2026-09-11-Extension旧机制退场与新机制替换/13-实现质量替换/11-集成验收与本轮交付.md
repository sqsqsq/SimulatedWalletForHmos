# Q11 集成验收、G2与本轮交付

> 方案/实施/评审按 [维护预算目的](../../../../AGENTS.md#75-维护预算与分级复核) 核本包新旧职责：防走偏与旧机制残留，不以删正常功能换绿。

本分册分两次消费：§1与§4静态准备在第二包内接续Q10执行；§2真实测试、§3 G2及§4正式交付在第二包评审通过后执行。推进及交回边界见 [执行总览§2](00-执行总览.md#2-两包实施与一次最终验收当前唯一推进规则)。接替原C09及总方案G2/正式交付。目标是完整功能、实现质量与实际模型效果共同成立，不以“新目录已建”“测试全绿”“行数下降”单独收口。

## 1. 静态与正常路径集成

按test/story/TEST.md离线分支，跑一次必要全量、失效形态、适配只读核及Framework边界检查。相关包已验的不反复无条件重跑；新发现集成问题回真正责任包修后，按受影响范围重验。

验证完整链：原材料/范围→候选提交→知识判断/Spec→skeleton→各章→整稿→story登记→原生harness/verifier→回执/交付；正常、不适用、必需输入缺失、解析失败、返修与恢复各有正确出口。使用隔离合法输入，不在主业务feature造状态证明自己正确。

在当前HEAD按功能双向核：旧解析/循环/自证/配额/协议/转发层是否实际退出；每项保留能力是否有唯一现行实现和真实消费者；两种不同判断不按同名误合并。纯死代码、旧机制与测试设施分别解释，只有测试引用不自动判废弃。

## 2. 模型行为与金样

真实CLI按TEST由用户启动和确定Case/配置，实施者不能自行创建suite。正常输入从干净隔离工作区开始，不喂金样、历史Spec或维护答案；真实运行结束后再评价，不在运行中替作者改产物。

以现有events/runlog/草稿/Story/verifier报告还原：实际送达（含head/分页）、首稿、确定性返修、语义返修、整稿改动、审查实际发现、上下文与成本。文件存在/被装配不等于读到，自述不等于执行，构造反例不证明模型泛化。

对所有H2/H3/H4最小内容单元逐项评价，父节自身有内容也单列：读者问题、来源及独有信息、形式为何合适、遗漏/重复/矛盾、相对历史或金样的保留/改善/劣化。不得强制生成金样所有小节。

| 章 | 核心效果 |
|---|---|
| 背景 | 因果/目标有据，不提前完整复述方案 |
| 术语 | 能消歧，词典不承担唯一业务规则落点 |
| 范围 | 完整需求不做与本部件不负责分清；依赖、阻塞与交接有归属 |
| 方案 | 全局机制/真源/真实取舍明确，不虚构备选或反向指代 |
| 流程 | 总览易找到，调用/分支/回跳/终态接得上；标记齐不代替内容 |
| 功能 | 按能力表达输入/规则/数据/状态/结果；无页面不造页面 |
| 异常 | 正常受限/取消/故障分清，数据和在途结果、恢复可理解 |
| 验收 | 原编号独立，条件/结果/主责可执行，指标有口径与依据 |
| 交付 | 各真实控制对象、存量/在途/恢复及时机明确，不把业务停用冒充版本回退 |
| 附录 | 五节精确投影与作者解释共存，业务含义不被藏入附录 |

AR90006作为用户认可的表达目标，使用本轮有效事实，不抄历史业务值。总览在独立小节、合并表列等合法形式不因旧样例位置不同被否决。未知配置、无页面整体案例、未发生的恢复实跑如实待验，不用UI里有缓存小节代表全覆盖。

## 3. G2规模与质量裁决

固定比较基线9675；完整终态包含Q1–Q10全部实现、返修及预算范围内版本/manifest说明。优先目标≤8675；用户已允许合理实现适当超限，但须逐职责说明必要性、旧实现退出、更简单完整替代为何不足和实际差额，不按现值自动重签，不删正常功能凑数。

同时报五类有效行、完整字符、全部交付脚本单文件行数（含安装器/adapters的单文件审视）、实际模型输入/重复读取/返修与耗时。新/已完成脚本默认≤500有效行；合理例外另具名，不靠压行或迁预算外。

功能/质量/行为未达则不关闭。数字过但效果待验只能报“静态完成、行为待验”。G2由用户或指定评审会话裁决，实施者不自签；预算目标及requirement.closed只在真实收口后按裁定登记。不能将未完成功能另开轮次后称本轮完成。

## 4. 正式交付与长期映射

发布说明保留历史1.9.0，按既定版本策略准备本轮新说明（按用户已定版本1.9.1）；能力说明与使用指南分开，指南/交付流程局部对齐，不讲内部实现步骤给业务用户。版本/manifest等计量内改动在G2测终态前准备好，G2后不再悄悄补功能。

第二包先以当前实际实现HEAD形成三份完整FEATURE候选；真实验收发生返修时只更新受影响职责与定位，最终稳定后确认HEAD并正式落位。模块清单不替代候选，不能以未来行号可能变化为由推迟。当前返修要求见 [第二包评审](第二包评审-2026-09-13.md)。

以最终HEAD重建三份FEATURE稿：功能全集、功能到实现映射、全部内容到功能归属。每个叶子有目的、现行实现/输入/消费者/失败恢复/证据；每个函数、数据和提示段落有合理归属，可多归属不可遗漏。行号取最终HEAD，不取回旧d36edb70及2.3映射。先在当前目录形成，完整复核后正式放入test/story并更新AGENTS入口。

提交本轮已退/替换/保留清单、测试迁移、G1/G2与行为结果；不自动删除回退保护件、不强推、不自动部署或改远端需求数据。原目录B的知识协议、Review决策发现与会议转写另行在新基线上讨论和实施，本线路到本轮交付结束。

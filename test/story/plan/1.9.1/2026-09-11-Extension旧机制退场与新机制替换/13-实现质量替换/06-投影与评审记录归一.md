# Q6 投影计算与只读保护归一

> 方案/实施/评审按 [维护预算目的](../../../../AGENTS.md#75-维护预算与分级复核) 核本包新旧职责：防走偏与旧机制残留，不以删正常功能换绿。

第一包起步职责，接替原C05。包级推进见 [执行总览§2](00-执行总览.md#2-两包实施与一次最终验收当前唯一推进规则)。若Q5已迁入appendix模块，本包直接在该模块收敛，不再搬一遍。目标是同一真源只计算一次规则，生成后仍有有效只读保护。

Q5 239a025b已通过，见 [Q5当前复审](Q5复审-239a025b-2026-09-13.md)。开工读当前工作树AGENTS与TEST；必要结构接线单列，不进入功能预算比例。当前模块已经归位，Q5非阻断的注释对象错位在触及直接消费者时同步整理。

## 0. 实施前估算（Codex reviewer，2026-09-13）

起点10108有效行、518826字符。功能替换初估140有效行：附录期望结果及机器区比较约90，Review共用字段校验及调用约50；退出对应旧逐行还原/重复校验初估170行，功能净减约30。预计必要结构接线约20行，review-render纯迁移单列、净零，综合总量约10098。以上按职责估计，先核实际旧函数与保留职责，不把完整appendix381行误当成全可删除；不得删保护条件命中估数。

当前在途scripts_mjs2920、scripts_py1800、总10150，其余类别保持；最终8675不变。功能新增/替换、旧机制退出、必要结构成本和完整规模分别报告，分项不重置整轮预算比例。实现或估算出现实际缺口先交reviewer处理，不能把必要结构成本报成功能超支，也不能把重复计算当必要结构成本。

## 1. 附录

当前8d746fa2待返修，见 [Q6评审](Q6评审-8d746fa2-2026-09-13.md)，两个问题经相关验证关闭后直接进入同包Q7，不单独停等评审。旧退出170行估算高于实际已核两条判据61行，保留为原估算，不要求实施者凑删；交回功能/结构分项去除重复记账。

appendixProjection为唯一A–D机器内容计算；projectAppendix据此写入，check据同一结果与盘上机器区逐区比较。替换原⑦/⑫b的另一套手写逐行还原算法；保留它们不同于投影的有效职责，不能整块删判据窗口。

输出保留行序、全部单元格及重复行；比较仅归一CRLF/LF等无内容差别。不使用Set丢重复或只比首列。作者解释区在机器标记外，不参加相等比较，不被生成器重写。

缺/重复/损坏机器标记、少行、非首列变化、错误行序、真源变化均明确到区与责任真源。必需Spec不可读不能作为expected=[]成功；显式不涉及按已有合同投影。

必需来源预检覆盖文件存在但消费小节被删或清空，复用specGaps等现有来源合同，生成与只读检查共用有效性结论。无效输入不能作为合法空数组让project删除旧区；明确不涉及继续合法，失败不写盘。区块扫描同时核实际/预期机器区集合：未知区、孤立结束、嵌套和损坏边界必须报错，一个结束只能配一个开始；作者区合法说明不参加机器内容比较。

project仍遵循现有手改保护：生成区被人工改过先停；合法真源变化沿原协议刷新。check不能调用会写盘的cmdProject。调用者需要多个检查时复用本次已计算结果，不建持久投影缓存。

## 2. Review

将原review-render及decisionList/字段检查归入story/review.mjs，入口cmdBuild和只读check调用同一字段校验，返回问题而不写文件；合法才render。

保留议题身份、H1、decisions对应、冻结及人工表态；人工区按既有标记逐字节保留。生成过不代表之后没有被改，不能删只读检查换成“所有写入都走生成器”的假设。不新增第二份JSON schema。

同包迁移原review-render的实际消费者与测试，删除旧文件及转发壳。头部/区间定位复用Q2 document，避免每个生成器自写Markdown扫描。

## 3. 验收

生成器使用独立期望；只读保护测试**不先project**，直接改盘上非首列、删行、换序、破标记、改真源，check必须发现且文件不变。合法作者解释、格式样例和人工表态在project/build/number后保留。

复用test_contracts_single_source、test_story_build、test_review_golden、test_spec_contract_section及相关失效形态。只在project后立刻比较相等不能证明保护有区分力。本节相关验证通过后直接继续第一包Q7，至Q8完成再统一交回。

# C00 交回 · S4 候选提交与原输入留存 · 2026-09-11

> 独立复审：ebdd317f已通过，见 [C00评审§8](C00评审-2026-09-11.md)。下一包按10分册的职责拆分方案实施C01/C02。
> 状态：已实施（含 C00评审 R1 两轮返修：6d1b1549 修留存件支，ebdd317f 按复审 §7 补齐空骨架与已登记派生稿两支并统一身份解析），独立复审通过。提交链：706ebfe2 → f9940d5c（C00）→ 6d1b1549（C00-R1）→ ebdd317f（C00-R1b）；branch story，未推送。
> 范围：08 §1候选提交与原输入留存；消费侧按下述分包接线；按 G1 裁定的预算接线（评审已写入 mechanism-budget.yaml）实施。未动 sources 索引、template、prepare。

## 1. 移植范围与逐项

正确补丁来源 804db2d3 + eda9c282（非 c388922a）；基线缺 2.1/2.2 改动，逐 hunk 适配到基线结构，未整笔 cherry-pick。

| 项 | 内容 | 落点 |
|---|---|---|
| 常量 | `DESIGN_DRAFT`、`AR_SOURCES`、`S4_STEPS` | story_flow.py |
| scope_step | 从 next_step 提取范围判定（不看材料新鲜度），next_step 与 cmd_complete 复用同一函数 | story_flow.py |
| 候选解析 | `resolve_candidate`（相对需求根、越界拒绝）、`section_numbers`（围栏外五段）、`is_ar_skeleton`、`candidate_problems`（空稿/空骨架/五段结构，只核结构不核内容） | story_flow.py |
| 原输入定位 | `prior_ar` 三身份（本轮留存件 / init 空骨架 / 契约已登记提取稿沿其 origin），来历不明即停 | story_flow.py |
| cmd_complete | 固定顺序：留存原输入 → 覆盖 AR/design.md → materials.refresh 与本轮基准 → 登记 `design: {sha256, origin}`、design_generated_at、complete；预检失败零写入；执行中失败保留已完成动作并报「已完成/未收口/重跑同一条命令」；幂等重试判据 `retry = design_bytes == cand_bytes or kept_is_prior`（eda9c282 语义，含覆盖写到一半的第三态） | story_flow.py |
| 材料 | `materials.digest_with`（把 AR 换回登记版再算「其余材料动没动」）、`materials.source_sha`；收件箱 pending 仍拦收口 | materials.py |
| 交付面文字 | DOC_TARGET AR 注释（人工补料落 upstream.md 的理由）；SKILL S4 行与 description；ar_design_init.md 头部/§3/§4.4→5；init_analysis.md 命令行 | import_sources.py、SKILL.md、rules/ 两页 |

登记不新建并行状态：仍是 flow 合同的 `design` 字段。`design.origin` 的下游消费者（作者任务包、reader 任务、源图检查的原材料集合）按评审明确分包：C01接作者输入、C06接源图/材料目录、C07接reader，C08同步文字；本包只落指针，不将消费侧记成已完成。

## 1.1 返修记录（对照 C00评审 R1，提交 6d1b1549 与复审 §7，提交 ebdd317f）

- **最终保存纳入提交失败处理**：`save` 移入 try——保存失败时报「已完成：留存/覆盖/清单」，磁盘契约保持未收口，不再以裸 OSError 逸出。
- **重试识别「清单已刷新、流程契约未保存」的合法中间态**：把 AR 换回被覆盖那一份的摘要还能复现旧 flow 基准（`digest_with(live, rel, prior_sha) == base`）——即除自己提交外材料未变的确定性证据。其余来源真变化时证据不成立，照旧以「基准对不上」拦下。未取消清单/基准比对，未无条件接受新清单。
- **R1b（复审 §7）：身份解析统一，不再逐场景叠分支**。上轮只修了「有本轮留存件」一支；复审指出空骨架与已登记派生稿两类场景同一断点仍不可恢复。抽出共享枚举 `ar_input_identities`（留存件 / init 空骨架含行尾两形态 / 契约已登记提取稿沿其 origin），prior_ar 的 origin 定位与 cmd_complete 的中间态识别共用同一份；任一新场景只加在枚举一处。三类恢复各有用例锁定。
- **注释随事实校正**：is_ar_skeleton 的「来源索引」改为实际的作者/审查读原件职责；contract.design 登记说明改为身份用途（重试与跨轮 prior_ar 以 sha256 认已登记派生稿）＋归档后不得作冻结比对（冻结比对走 story_src_digests），实际校验未删。
- **断点用例四条**（真实文件级失败：契约文件只读 → PermissionError，全走真实 CLI）：保存失败报「已完成」且状态未收口；留存件场景恢复 origin 指真实原件、不新增轮、候选不留成上游；同窗口改 RR 以「对不上」拦下；空骨架场景恢复 origin=None 不留存指引；已登记派生稿场景恢复沿 r1 origin、不写 r2。

## 2. 测试

- `test_s4_commit.py` 25 条按 eda9c282 终态迁入＋R1 断点四条（含空骨架、已登记派生稿两类恢复），自建工作区驱动真实脚本，覆盖 09 要求的九类场景及最后保存窗口：正常提交（含 next 转 spec_、脚本不改一个字节）；空骨架不当证据留存；既有派生稿新轮沿 origin；重复命令幂等（committed=false、不多存 rN）；收口后换稿须 reopen；RR 真变化拦、改过的 AR 拦、重试中途真变化拦；缺 --from/候选缺失/越界/空稿/骨架/五段塌/围栏样例各拒绝且预检零写入；半提交原候选重试、改稿后重试、半写覆盖恢复、最终保存失败后三类场景恢复；只覆盖未留存报「来历」；留存冲突报「只有一份」；范围未定仍拦。
- 既有 material_rounds / spec_story_gate / multi_case_cli 全过（保留原材料与人工输入的语义未变）。
- 全量：**812 passed / 227 subtests，0 失败**。check_failure_modes FAIL 0；adapt-scan 通过；py_compile / node --check 过。

## 3. 数值（G1 裁定接线后的第一次实测）

| 类 | N1（B 完成） | C00 后 | R1 两轮返修后 |
|---|---:|---:|---:|
| data | 781 | 781 | 781 |
| hooks_mjs | 3022 | 3022 | 3022 |
| prompts_md | 1761 | 1771 | 1771 |
| scripts_mjs | 2463 | 2463 | 2463 |
| scripts_py | 1548 | **1710** | **1727** |
| **总计** | **9575** | **9747** | **9764** |

- C00 净 **+172**、两轮返修后累计 **+189**；scripts_py 1727 ≤ 峰值 1750，hooks_mjs 3022 ≤ 3030，总 9764 ≤ 9800。完整字符 493533（A 段 481000）。
- 评审写入的 mechanism-budget.yaml 接线（hooks_mjs 3030 / scripts_py 1750 / 总峰值 9800 / total.target 8675 / requirement in_progress）随 f9940d5c 一并入库。

## 4. 未验证与边界

- 正常 CLI 未运行；`design.origin` 消费侧未接（C01/C06/C07，C08同步文字）。
- 提交后 `AR/design.md` 为提取稿、原输入只在 sources/ar/rN.md——源图检查等现有读者在真实原 AR 场景下的行为差异，按 08 §1.3 随后续包处理。

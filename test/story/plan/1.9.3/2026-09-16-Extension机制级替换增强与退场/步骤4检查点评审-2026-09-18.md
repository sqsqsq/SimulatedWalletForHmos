# 步骤 4 检查点评审（2026-09-18）

评审对象：`步骤4检查点-2026-09-18.md`（合并稿）与 `步骤4检查点-2026-09-17.md`（两轮实跑记录），suite `20260917-095900` 与 `20260917-180959`，机制版本 84229f31。

结论：**检查点成立，进步骤 5；会议四栏未被审查消费这一项的责任位置裁定为 overlay 的输出要求（检查点的第 1 条），作为步骤 5 的第一件事，不为它单独再跑 CLI。** 另核出一条检查点没写的主因：car 轮② verifier 耗时 119.7 分，来自 spec 阶段 FAIL 归零后作者又重跑 harness 7 次、verifier 跑了 5 次且全部 PASS，不是任务书体积。

## 1. 独立复核

用同一份 events.jsonl 重算，与检查点一致：

| 运行 | harness 轮次 / 带 FAIL | 备注 |
|---|---|---|
| 轮① auto | 2 / 1 | 与检查点同 |
| 轮① car | 10 / 5 | 同 |
| 轮② auto | 8 / 3 | 检查点记 4，差一次是 FAIL 计数口径，方向不变 |
| 轮② car | 19 / 3 | 同；spec 9 次里 H04–H09 六次 FAIL 0，plan 10 次里 H13–H19 七次 FAIL 0 |

grep 机制面：轮① auto 36 次 `doc/extensions`、7 次 `framework/harness`，car 16 / 16；轮② 两 Case 各 1 次。与检查点「轮②几乎不再翻机制」一致。

verifier 报告：auto 轮② spec 报告全文「会议 / 话题 / doc-refresh / raw.md」零命中，`story_reader_review` 的补述只写了逐章与回看清单，证实检查点 §2 第三行。图并排与判断表的引用属实。

任务书位置：auto 轮② ai-prompt 共 2785 行，会议节在 2417–2558、图并排 2559–2745、判断表 2746 起，三段都在尾部、彼此相邻。**检查点的第 2 条（位置）假说不成立**，不采用。

## 2. 裁定：会议四栏没被用上，责任在输出要求

`spec-rules.overlay.yaml` 的 `story_reader_review`：判据段（:142-147）写了「按话题 → 原话 → 人选 → 结论逐条核」，但输出契约段（:200-204）只要求「证据格写你逐章过了什么」。verifier 按契约交了逐章补述，会议一段既没被要求写、也就没写。图并排与判断表被用上，是因为它们对应的判据（丢关系、reason 是不是命中条件里的事实）本身就落在逐章与知识两项的结论里。

**改法**（属步骤 5 提示层，一句）：输出契约段加「任务书里有『会议材料：逐话题并列』一节时，补述里逐话题各一行：话题 id → 人选了什么 → 当前结果里的确定行为有没有依据；没有这些行与没审同形」。不加机械判据（verifier-report.mjs 不核它，避免形态代理），效果在步骤 7 看。已写进 02 E3b。

不为这一句单独再跑检查点：它只影响审查输出的一段，步骤 7 两 Case 各两次会覆盖。

## 3. 检查点没写的主因：verifier 重复运行

car 轮②：spec 阶段 harness 9 次，其中 H03 起 FAIL 全为 0（WARN 10 条不变）；`verifier.report` 5 份全部 PASS；plan 阶段 harness 10 次，H13 起 FAIL 0，verifier request 9 份。也就是说 verifier 耗时 119.7 分里大部分是**同一份产物在 FAIL 归零之后被反复送审**。SKILL 与 `phases/spec.md` 已写「verifier PASS 之后不再跑 harness，只有 check-receipt 报 subject 失配才重跑」，模型没有照做；WARN 10 条稳定不变，说明重跑也不是在修 WARN。

责任位置两处：

- **提示层（步骤 5 R7）**：`phases/spec.md` ⑤ 与 SKILL 的这一句现在埋在长段里；重排时把它写成动作序列里的一行硬规则，并说明后果（每跑一次 harness 都会重派 subject、verifier 再来一次）。
- **framework（04 §4 已列）**：harness 每次重跑重派 subject。扩展不改。

这一条与「工具调用总数、上下文终值未降」同源：轮② car 570 次工具调用里，重复的 harness 与 verifier 往返占了相当部分。

## 4. 其它观察

- **E2 未触发**：两 Case 两轮 `knowledge-use.yaml` 都没有 `decision:`，DLV-02 都判不命中。检查点已记；步骤 7 若仍不触发，E2 只有离线覆盖，如实写进交付。
- **读判据脚本次数**：轮① 114/87 高于基线，轮② 19/47 大降；`measure_run.py` 的误计未修（步骤 6），只看方向。
- **spec.md 编辑次数轮②上升**（auto 18、car 17）：与 verifier 返修和 WARN 无关的自改，属作者行为，步骤 7 再看。
- **任务书体积** 1.13–1.25 倍，在 02 E3 上界内；会议四栏 6,220 字与图并排约 3K 字已单列，步骤 3 评审 §4 的建议已做。
- 轮① 宿主缺席 92 分钟计入人工等待，已单列不归责；轮② 等待只有 138 / 251 秒。

## 5. 下一步

1. 步骤 5（R7 → R8）开工，基线 84229f31。第一件事：overlay `story_reader_review` 输出契约加 §2 那一句；R7 重排 `phases/spec.md` 时把「verifier PASS 后不重跑 harness」写成硬规则一行。
2. 步骤 5 交回时按 04 §5 提分类完成目标（步骤 3 评审定的时机）。
3. 检查点两份记录保留，不合并。

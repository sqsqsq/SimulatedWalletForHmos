# 对三项质疑的答复（2026-09-18，步骤 7 后、步骤 8 前）

用户质疑：1.9.3 实现相对需求目标是否已完成、有无偏离；退场预算未达标是否真的合理；现有实现是否是正向设计。

## 1. 相对需求目标

需求目标（00 §1）：三类结构问题——扩展读法比 framework 严、形态代理语义与重复判据、模型判断时拿不到原义且提示层用解释代送达。效果目标：产物更稳、质量更高；完成判据用户定为人工审视，三轴辅助。

| 项 | 状态 | 证据 |
|---|---|---|
| R1–R4、R6 | 落地，零残留 | 步骤 1、2 评审 |
| R5 | 退 7 条不是 12 条（评审改判，保留能防手改契约的） | 步骤 1 评审 |
| R7、R8 | 落地，性质逐条核 | 步骤 6 评审 |
| E1、E3、E4、E6、E8、E9、E10 | 落地 | 步骤 3 评审 |
| E2 | 落地，三轮实跑未触发 | 检查点 §2 |
| E5 | 只改提示，三轮实跑无效 | 检查点、补跑 §4 |
| E7 | 撤回 | 第二轮评审 |
| E11 | 用户 09-18 加，落地 | 步骤 5 评审 |
| 03 退场 | 落地；⑬ ⑮ depends_on frontmatter 保留 | 步骤 7 评审 |
| 04 §2 效果验证 | 未做 | 步骤 8 |

偏离：效果目标至今只有过程指标证据（FAIL 轮次下降、扩展门禁 8→2–3、并列证据被引用两类），产物质量未评过；E5 无效说明「只改提示」对它不成立；轮中反应式加项四件（E11、裁定 A、overlay 会议句、检查点规则）。

## 2. 预算

退场账真实（每步 rg 零残留、framework 零改动）。不合理的两件：估算系统性失准（方案估 −1015，实 −342；退出量按原始行数与「一套一份」估、送达代码估 70 实 200+，三步同向）；完成目标跟着实测改三次（10600→10860→11500→11536），预算在 1.9.3 里没有起到约束作用。责任在设计者：应在步骤 1 交回时停下按 code_lines 口径重估，而不是逐步重签。

## 3. 正向设计

成立：步骤 1、2（从 framework 读法与一次解析正推，每项写替代对象）、步骤 5（按用户定的交互改流程）、步骤 6（提示层按读者与时机重排）。
打折：步骤 3 送达原则正向，实现是在现有任务书上加表加段，体积 1.13–1.25 倍；裁定 A 初版是补丁（已改为指向 framework 修正路径）；overlay 会议句是补齐输出契约但属反应式；E5 是「加一句提醒」。
未动的机制成本：投影加冻结让小改动整链重走；framework 重审整份粒度；验收条目漂移归上游。均已登记未解决。

## 4. 建议

步骤 8 前不再加任何东西；步骤 8 按人工审视两 Case 各两次；E5、E2 若仍不生效，交付说明如实写未生效，不补提示；交付说明写明估算失准与目标重签三次。

# 用户裁定：会议材料的交互（2026-09-18）

给设计者。用户在补跑讨论中给出期望交互（原话）：

> 会议材料是需求材料的一种，不会有一个单独提供会议材料的步骤。
> 我期望的交互是 1）AI问询是否补材料 2）人工提供需求材料（可能包括会议材料，也可能没有） 3）AI分析所有材料（包括会议，如果有的话），有会议时，存在需要人工确认的，进行一次确认，否则按原有流程进入特性拆分确认

## 与现状的差异（84229f31）

| 期望 | 现状 | 位置 |
|---|---|---|
| 人交料后不再回头确认材料 | 有会议时，第一级已定也再停一次（`unconfirmed` 非空即重摆第一级） | `flow/routing.py:405-414`、`flow/decisions.py:52-58` |
| 会议只在有待人定的话题时停一次 | 另有独立 `await_gate:meeting`；与第一级重摆能否合成一次只靠提示文字 | `flow/routing.py:360-367`，`story-chapters.json:314` 的 `flow.gates` 含 `meeting` |
| 没有会议时：问料 → 交料 → 分析 → 拆分确认 | 已一致 | 补跑 car：材料、范围两次停等 |

实跑：auto 轮①② 模型把会议与第一级重摆合成一问（3 次停等进 spec 前），补跑分成两问（4 次）。盘点 A §3 已记此矛盾；1.9.3 方案只在 01 R8 的 SKILL.md 停等表写「会议话题并入材料关卡同轮」，未含流程脚本动作。

## 待设计者定

是否纳入 1.9.3（步骤 5/6）或另开需求；交互目标以上面三步为准。

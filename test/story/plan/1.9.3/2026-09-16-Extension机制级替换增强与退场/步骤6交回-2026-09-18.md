# 步骤 6 交回：提示层按读者、时机、动作重排（R7）+ 写作单位与改章边界（R8）（2026-09-18）

状态：**评审通过（`步骤6评审-2026-09-18.md`），按评审 §2 第 3 条放回「关系图放正文、不搬进附录」一句后提交 97eaafab**。基线 bfa51fed。方案 `01` R7、R8，`00` §4 第 6 行（SKILL 停等表按 E11 后的行为写），步骤 5 评审 §6（spec.md ⑤ 修正入口与 SKILL 原 :106-110 合成一处）。本步只做离线门，不跑 CLI。

## 1. 逐文件改动

| 文件 | 现在只含 | 退出 / 迁走 |
|---|---|---|
| `SKILL.md`（285→184） | 链条图、各步规则指路、推进契约（授权两层 + 启动语义）、**停等真值表**、失败出口、停等消息写法、命令入口（初始化/归档/恢复/检视）、产物定位、token | 「停等点：只有两处」整节及其条件解释（并入真值表）；verifier 之后的顺序与 harness 重跑（归 phases/spec.md）；「成文到闭环的衔接链」；侧车写哪一级（scope_gate 与路由已有）；两张产物对比表合为一张 |
| `phases/spec.md`（173→118） | 上游必读、三份产物、阶段内六步、⑤ 修正入口硬规则（与 SKILL 原 :106-110 合一）、⑥ 交付门、「失败出口：verifier 报了阻断问题」小节、§9 取证、交付门之后的表态完成度 | :165-168 plan 期修正三问（review_reflow 已有）；原 :121-126 阻断返修链移入失败出口小节；①②③ 的长解释压成一行一条 |
| `phases/story-write.md`（453→303） | 按写章时机：起手输入 → 一、写给这样的读者（效果口径清单）→ 二、写作设计协议 → 三、写一章（**关系为写作单位**、画图、四条红线、需求不是演示、**改章两条路**）→ 决策登记 → 四、回看 → 五、登记 | §五 十章各自怎么组织（合同 questions/boundary 已铺进草稿头，其中来自用户批注的效果规则并进第一节清单，见 §3）；§六 自检两条（「独立可读」并进第一节首条，「有取舍」并进决策登记）；设计/完成两栏对照表；:206「已经写好的章不要重写」 |
| `templates/spec-sections.md`（178→78） | §9–§11 模板，每节一句「不写什么」；core 章节补充压成一段 | 头部原则段（spec 是什么、内容三分、形式、防重复、元叙述分层）——「只写与代码有关的要求、推演与生成说明不进正文」并入 `hooks/spec/author.md` 一句；防重复与形式已在 phases/spec.md |
| `templates/plan-sections.md`（181→91） | 知识决策章骨架 + 契约挂法样例（resource_keys 两层并进同一样例） | 头部原则段；must 五处、三字段表、verify 对照表、借挂反例、收尾——与 `hooks/plan/author.md` 同义；「挂给真正做事的实体」并入 author.md 一句 |
| 六份 `hooks/*/author.md` | 第 3 行换成本页读者与时机一句；coding/review/ut/testing 开头同义段统一为同一句 | 逐字相同的「推进不逐段问」blockquote（只留 SKILL 一处） |
| `rules/scope_gate.md` | 头注写读者、时机与「在哪停以 SKILL 真值表为准」；三级图下补会议有条件停一次的位置 | 「这两级是仅有的两处停等点、无条件」整节改为一句引用；第一级「请人陈述的是事实」一段（SKILL 已有）；呈现方式改为引用 SKILL |
| `rules/init_analysis.md` | 头注读者与时机；侧车示例第一级**只写 key**（与 scope_gate、`gates_note` 一致，矛盾 G） | 「关卡只认人签」的长解释改为一句引用；指向不存在的 SKILL 章「初析与流程契约」两处 |
| `rules/ar_design_init.md`、`inbox_import.md`、`review_reflow.md`、`reference/evidence-rules.md`、`phases/meeting-read.md` | 头注补读者与时机；ar_design_init 第 3 条注明「提取稿不搬图、成文时搬进 story」（矛盾 D） | 「停等点只有材料与范围两处」改为引用 SKILL 真值表 |
| `contracts/story-chapters.json` → **新建** `contracts/README.md` | 合同只剩数据键；19 个说明键按位置迁入 README（数据深相等核过） | 合同里全部 `note` / `*_note` / `_note` 键（方案写 14 个，实数 19 个，含 `note`、`_note` 与两处决策类别的 `banned_terms_note`） |
| `scripts/core/story/drafts.mjs` | 章头第三行：「写本章前先读已写章与本章共用的关系，再对照本章要用的原文」 | 「十章怎么组织见 story-write.md 第五节」（该节已退） |
| `scripts/core/story/context.mjs` | `refuseIfFrozen` 报错说出唯一的路：`reopen` → 照下一步走 → `story` 重新登记 | 重复的半句；「材料在定稿之后继续演化」一句 |
| `scripts/core/flow/check.mjs` | 注释指向 `rules/init_analysis.md` | 指向不存在的 SKILL 章 |

测试域：`test_writing_flow` 两处定位改到新位置（总览图要求从 §五「业务流程」移到第三节画图段；「只有一条合理路径」不再要求出现在已退的自检节）；`test_story_build` 冻结用例加锁「报错说出 `story_flow.py reopen`」；`check_failure_modes.py` F02 见 §5 第 2 条。

## 2. R7 性质逐条核

| 性质 | 结果 |
|---|---|
| 合同已定形态零复述 | 术语表头、交付物四列、附录五节名、验收表头、空节固定文案在提示层不再列出；附录「五节」只在合同与投影脚本 |
| 机器不核的项明说「由审查判」 | story-write 第一节头注「标机器核的有判据，其余由读者视角的语义审查判」；逐条标了机器核的有：编号、验收编号独立、四条红线、空章、上游图落点；「需求不是演示」「描述写得好不好」「资源是不是运行资源」明写由审查判 |
| 同一停等说明只在 SKILL 一处 | `grep -rn "必停\|无条件\|只认人签\|停等的开关"` 只剩 SKILL 真值表与 init_analysis 一句引用；`grep -rn "仅有的两处" doc/extensions` 零命中；scope_gate、init_analysis、ar_design_init 都指向「停等真值表」 |
| 停等表与 E11 一致 | 材料（第一轮必停，此后有侧车才停）、会议（有 `question` 才停，脚本枚举）、范围（必停，含 split 追问）；既有确认点：交付门后、查无此单、归档与恢复 |
| 每份文件头一句写明读者与时机 | 上表所列 20 份（`inbox-readme.md` 的读者是放料的人，首句即对他说，未改） |
| 引用的章节与行号有效 | 全部「文件「节名」」引用逐条核过存在：`story-write.md` 的「决策登记」「二、动笔前：先设计表达」、SKILL「停等真值表」「停等消息怎么写」「检视」、scope_gate「人提出自己的拆分诉求时」 |

体积（观察值，不是达标条件；`scratchpad/prompt_size.py`，HEAD 对工作区）：

| | 前 | 后 |
|---|---:|---:|
| 20 份提示文件 + 合同 README 行数 | 2656 | 2227 |
| 非空非围栏正文行 | 1744 | 1393 |
| 字符（含新建的合同 README 4129） | 87782 | 76107 |
| 字符（不含合同 README，作者起手会读的面） | 87782 | 71978 |

动作占比未按盘点 D 的人工分类口径复算（那是逐簇人判的，脚本复算不出同一口径），只记行数与字符。两 Case 起手读到的扩展提示总字符要在步骤 8 实跑里取。

## 3. 用户批注 12 条在第一节清单里的位置

| # | 批注 | story-write 第一节 |
|---|---|---|
| 1 | 分工用表、表前后一两句、单元格不成压缩袋 | 「表前有一句引导…单元格不塞成压缩袋」 |
| 2 | 方案 / 流程 / 功能最详细 | 「业务方案、业务流程、功能说明是全篇最详细的部分」 |
| 3 | 验收按要验的事分组、编号独立 | 「验收按『要验的事』分组…每个原始验收编号独立一行」 |
| 4 | 交付与上线要详细 | 「交付与上线要详细：上线路径、开关与兼容、回退设计及理由、交付物给谁做什么用、与兄弟单的上线顺序」 |
| 5 | 附录每节开头一句目的 | 「附录每节开头一句『这一节给评审者看什么』」 |
| 6 | 材料清单用列表 | 「材料清单用列表」 |
| 7 | 标题编号与业务命名 | 「标题用真实业务名…自造词、编号式与工程味命名…编号由登记时的 `number` 统一铺」 |
| 8 | 恢复类小节用「时机 / 方案 / 走向」短段 | 「恢复类内容用『时机 / 方案 / 走向』这类短段引导」 |
| 9 | 原型文案要求不单独成节 | 「照抄产品原型的『文案要求』不单独成节」 |
| 10 | 主叙事零工程标识、术语表收反复使用的概念 | 「主叙事零工程标识…术语表不收空转定义，主叙事反复使用的概念必须进术语表」 |
| 11 | 材料清单每行来源、文件名、提供方、贡献与原文链接，唯一允许仓内路径处 | 「每行写来源、《文件名》、提供方与方式、内容贡献，并带原文的相对链接——这是全篇唯一允许出现仓内路径的地方」 |
| 12 | H1 带需求编号 | 「H1 的单号由登记时的 `number` 统一铺」 |

## 4. R8 验收

- 提示层里改章只有两条路：story-write 第三节「落盘与改章只有两条路」——登记前在草稿上改、跑 `chapter` 重提（已写的章同样可以）；登记后先 `reopen`。与 `context.mjs::refuseIfFrozen`、`rounds.py::cmd_reopen` 一致；:206 绝对句与「十章各自重建理解」退出。
- 真实产物副本（`doc/features/ISSUE-206`，本轮补跑回灌件，`story_written`；`scratchpad/r8_check.py` 在临时工程根上跑）：
  - 登记后 `chapter` 重提第 3 章：退出码 1，报错点名 `story_flow.py reopen --feature <名>`（改前的报错没有指路，本步补上）；
  - `reopen` 之后同一章重提：退出码 0，story.md 只替换那一章，随后给出回看清单。
- drafts 章头加「写本章前先读已写章与本章共用的关系」，未加字段。

## 5. 读法偏差（请评审裁）

1. **十一类表保留**：R7 表把「十一类表全量」列为与合同重复的形态。合同 `decision_categories` 只有 `key` 与 `section`；表里「读到什么就检查」「撞的方向」两列在别处没有，任务包也只列 key。删了会丢扫描地图（`test_writing_flow` 专门锁「删的是骨架义务，不是扫描地图」）。所以表留着，表头说明「不是配额」保留。
2. **F02 失效形态检查**：它从 writer 作业书的「交回前自检」节取维度名，再核它们没泄漏到审查任务。自检节随 R7 退出后，它报「解析坏了」。改为：节还在而取不出维度才算解析坏了；节不在即该形态未启用。这是测试域判据跟着对象走，不是把产品改回去迎合检查。
3. **§五 里没有迁走的几句**：异常章「列名对准真实观察者」、验收「没有真实测量就不画结果图」、附录「决定行为理解的关系图放正文不搬进附录」。合同 questions/boundary 覆盖了这几章的主干，这三句不是用户批注、也不在合同里，按「为一次误判写的提醒」处理未保留。评审认为要留，放回第三节各一句。
4. **合同说明键实数 19 个**：方案写 14 个；多出的是顶层 `note`、`id_shapes._note`、`verdicts._note` 与两处决策类别的 `banned_terms_note`。全部零读者（代码与测试都不读），一并迁出。
5. **SKILL 的真值表把交付门后、查无此单、归档恢复并进同一张表**：R7 写「既有确认点：交付门后三选项、init 查无此单」，我把归档与恢复也列为一行（它们原来在「既有确认点」一节里）。

## 6. 离线门

| 门 | 结果 |
|---|---|
| `pytest test/story/tests -n auto --dist loadscope` | 1155 passed |
| `check_failure_modes.py` | FAIL 0（委派 8 项同前） |
| `adapt-scan --check --target .` | 通过（新建的合同 README 在包内） |
| `framework/` diff | 零 |
| `grep -rn "仅有的两处" doc/extensions` | 零命中 |

## 7. 预算与分类完成目标

| 类 | 基线 9b4637fa | 步骤 5 末 | 实测 | 本步净 | 在途峰值 |
|---|---:|---:|---:|---:|---:|
| prompts_md | 2012 | 2043 | 1681 | −362 | 2050 |
| hooks_mjs | 3214 | 3397 | 3397 | 0 | 3420 |
| data | 770 | 759 | 759 | 0 | 800 |
| scripts_py | 2375 | 2379 | 2379 | 0 | 2400 |
| scripts_mjs | 3507 | 3418 | 3417 | −1 | 3520 |
| 总 | 11878 | 11996 | **11633** | −363 | 12020 |

**完成目标 10860 按现有方案到不了**。步骤 7 剩下的退出按 04 §5 的归属账约为 hooks −30、MJS −60、Python −70、data +8，做完约 **11480**，比 10860 高约 620。差在三处：

| 类 | 04 §5 预计净 | 实际到步骤 6 末 | 差额来源 |
|---|---:|---:|---|
| hooks | −105 | +183 | 步骤 3 送达（E1–E3）实际 +200 多，已在步骤 3 签过在途额度 |
| MJS | −435 | −90 | R4/R6 实退比估的少（成文侧收一处后仍需的解析与红线扫描留着） |
| prompts | −425 | −331 | 十一类表、效果口径清单、SKILL 真值表新增；README 迁入按 .md 全额计 |

按 04 §5「若实账仍高于 10860，由用户在步骤 6 交回时定是重签还是继续退」，两个选项：

1. **重签完成目标**为步骤 7 做完后的实账（提案按类：prompts 1690 / hooks 3370 / data 770 / scripts_py 2310 / scripts_mjs 3360，合计 11500），步骤 7 交回时按实测收口。
2. **维持 10860**：需要设计者再指认约 620 行的退出对象；我手上没有已论证、可退而不丢能力的候选。

**用户 2026-09-18 选 1（重签为实账）**：已按类改 `test/story/regression/mechanism-budget.yaml` 的 target（3360 / 2310 / 3370 / 1690 / 770，总 11500），头注记签字与依据；在途额度不动；预算测试通过。

## 8. 未验证

- 重排后作者是否按新顺序读、SKILL 真值表是否让停等更少更稳，等步骤 8 实跑。
- 动作占比未按盘点口径复算（§2）。

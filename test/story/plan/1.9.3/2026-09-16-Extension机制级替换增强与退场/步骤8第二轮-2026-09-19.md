# 步骤 8 第二轮实跑记录（2026-09-18 晚 → 09-19 回灌）

| 项 | 值 |
|---|---|
| suite | `story-suite-20260918-175714` |
| 机制版本 | 1af7fdf7 **加两处未提交的知识改动**（见 §5） |
| 被测配置 | bailian-deepseek |
| 宿主 | 实施者会话；按用户 09-18 裁定只答模型所问，模型不提问一律不回 |
| 结果 | 两 Case 都在 19:26 自然闭环、`target_reached=true`（auto 到 spec，car 到 plan）；09-19 00:22 finalize 回灌，suite 状态 `harness_contaminated`（§5） |

## 1. 宿主回话（全部是模型所问）

| Case | 关卡 | 回话 |
|---|---|---|
| auto | 材料（缺原稿里的界面图） | 投原稿与会议记录两份 docx，「1」 |
| auto | 会议两处裁决（T1 更新接口、T3 AC-R1） | 「T1 选 1，T3 选 1」 |
| auto | 范围 | 「1」 |
| car | 材料（缺产品需求稿） | 投产品稿，「1」 |
| car | 范围 | 「1」 |
| car | 交付门（进 plan） | 「1」 |

两 Case 各停 3 次，没有一次「写成文字就停」（`invoke_stalls.py`：0 / 0）。没有说过任何规划外或未被问到的话；car 的 24 小时立场与评审意见都没有出现。

## 2. 新行为

| 行为 | auto-topup | car-key-sharing |
|---|---|---|
| E11 会议交互合一 | 停 3 次：问料 → 会议 2 个待裁话题一次问完（其余按会议采纳、两件未决照记）→ 范围；交料后没有再问材料 | 不适用 |
| E5 需求方提到的材料 | 材料关卡点名原稿里的界面图 | **没触发**：判「接受侧界面另行提供、不在本稿，属范围外」，没向宿主要（第一轮要了） |
| E2 评审动作落到议题 | **没触发**：DLV-02 无 `decision` | **触发**：DLV-02 `decision: DEC-DLV02-归档` |
| 撤销时限冲突 | — | 模型自己把 24h / 48h 登记为待评审未决；spec 首审 verifier 还抓到一处「撤销时限被写死」的阻断，模型改回未决边界 |
| 裁定 A（verifier 回复后闭环链不回头） | **没做到**：第 2 次 verifier PASS（0 阻断）后，模型认为 2 条 MAJOR「是真实缺陷、闭环前值得修」，改了产物、重跑 harness，又派两次 verifier（第 3 次因附录投影陈旧报阻断，第 4 次 PASS） | 做到：spec 首审有阻断 → 返修 → 再审 PASS，WARN 记 notes；plan PASS 后「1 MAJOR + 1 WARN 记 notes，本轮不阻断」 |
| 会议逐话题一行 | 没有：`story_reader_review` 证据格只写逐章，未逐话题 | 不适用 |

## 3. verifier 派发

| Case | 时刻 | 结论 | 原因 |
|---|---|---|---|
| auto | 18:55 | 1 阻断 | 首审（AC-R2 编号不一致）→ 返修，正常 |
| auto | 19:08 | PASS，0 阻断 | 返修后再审 |
| auto | 19:18 | 1 阻断 | **PASS 后改 MAJOR 引起**：改了 knowledge-use，story 附录 D 投影陈旧 |
| auto | 19:24 | PASS | — |
| car | 18:45 | 1 阻断 | 首审（撤销时限写死、AC-G4 编号错位）→ 返修，正常 |
| car | 19:01 | PASS | 再审 |
| car | 19:24 | plan PASS | — |

## 4. 过程指标（measure v2）

| 指标 | 第二轮基线 auto / car | 第一轮 auto / car | 本轮 auto / car |
|---|---|---|---|
| 实跑分钟 | 48.1 / 90.2 | 59.1 / 107.7* | 85.0 / 83.2 |
| harness 轮次 | 7 / 22 | 3 / 11 | 9 / 7 |
| 带 FAIL 的 harness 轮次 | 4 / 11 | 2 / 3 | 3 / 2 |
| verifier 派发 / 耗时（分） | 1·6.3 / 2–4·25.4 | 1·7.3 / 2·28.4 | 4·28.9 / 3·27.9 |
| 工具调用 | 305 / 约 400 | 313 / 451 | 370 / 343 |
| 上下文终值 | 411K / 509K | 474K / 562K | 525K / 484K |
| 读判据源码（v2） | 20 / 31 | 29 / 18 | 30 / 23 |
| 读规则文本（v2） | 49 / 88 | 75 / 90 | 70 / 71 |

\* 第一轮 car 含宿主自加的评审回流一段与 12 次「写成文字就停」，且 plan 未闭环，不可直接比。

auto 本轮耗时上升主要来自 §2 那一次「PASS 后改 MAJOR」引起的两次多余 verifier 与随后的 harness（19:08 PASS 之后到 19:26 收口共约 18 分钟）。car 本轮是四次 1.9.3 实跑里最快的一次，且 spec + plan 都闭环。

## 5. 机制污染

装置报 `harness_contaminated`：`doc/extensions/knowledge/design-patterns/decision-tree.md`、`page-interaction.md` 在 09-18 16:51 被改（未提交，非实施者、非被测模型所为），17:57 起跑时随工作区复制进了隔离环境。两处都是「§9 反模式」一节压短写法，不改机制代码。影响面：plan 阶段选型读到的反模式说明；car 本轮 plan 选型是否受影响未核。这两处改动仍在主工程工作区，实施者未动。

## 6. 两轮对照（供人工审视）

| 项 | 第一轮 | 第二轮 |
|---|---|---|
| auto 会议关卡 | 1 个待裁（AC-R1） | 2 个待裁（T1、T3） |
| car 范围 | 含被分享人接受页（交了接受侧说明） | 不含（没要接受侧说明） |
| car 撤销时限 | 宿主在交付门说出 24h（污染） | 模型自己登记为未决 |
| E2 | 两 Case 都触发 | 只有 car 触发 |
| 闭环后回头改 | 两 Case 都没有 | auto 回头改了一次 |

范围差异来自交料不同（car 第一轮交了接受侧说明），按 04 §2 属合法差异。04 §2 的逐类稳定性比较（规约判断、决策集合、未决承接、图关系、章内解释、会议去向）要逐类读两轮产物，尚未做；第一轮 car 的 plan 未闭环，plan 侧只能单轮看。

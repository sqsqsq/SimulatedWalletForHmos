# 步骤 8 第一轮实跑记录（2026-09-18）

| 项 | 值 |
|---|---|
| suite | `story-suite-20260918-140014` |
| 机制版本 | 1af7fdf7（步骤 7 末） |
| 被测配置 | bailian-deepseek |
| 宿主 | 实施者会话 |
| 结果 | auto-topup 15:14 自然结束（终点 spec 闭环）；car-key-sharing 16:08:54 起停在 plan 收尾前，用户 16:13 叫停（`stop`，无残留进程，装置记为 `stop_failed`），已 finalize 回灌 |

用户要求本轮只跑第一轮，第二轮未起。

## 1. 本轮要看的新行为

| 行为 | auto-topup | car-key-sharing |
|---|---|---|
| E11 会议交互合一 | 进 spec 前停 3 次：问料 → 会议仅 1 个待裁话题（AC-R1），其余按会议采纳 → 范围；交料后没有再问材料 | 无会议材料，不适用 |
| E5 需求方提到的材料 | 材料关卡点名 PRD 原稿里的界面图 | **第一次生效**：材料关卡点名「接受侧界面说明」，宿主据此交出；范围因此多了被分享人端接受页（前三轮都没要，补跑时判「不视为缺口」） |
| E2 评审动作落到议题 | **触发**：DLV-02 `decision: doc-archive` | **触发**：DLV-02 `decision: D5` |
| 裁定 A 闭环后不重派 | spec verifier 1 次，PASS 后没回头改 | spec verifier 1 次；交付门评审意见走 `--correction-init` → 改 spec/acceptance → `--revalidate`，回执 `completed_with_prior_review`，**没有重派 verifier**；plan verifier PASS（3 WARN）后说「按纪律 WARN 本轮不修，记入 notes」 |
| 会议逐话题一行（overlay 补句） | `story_reader_review` 证据格逐章写到了会议话题（T1/T2/T3/T5 回查 raw.md），但没有按「话题 id → 人选 → 依据」逐话题各一行 | 不适用 |

## 2. 过程指标（measure v2）

| 指标 | 第二轮基线 auto / car | 本轮 auto | 本轮 car |
|---|---|---:|---:|
| 实跑分钟（扣等人） | 48.1 / 90.2 | 59.1 | 107.7（含停等时宿主回话延迟，见 §4） |
| harness 轮次 | 7 / 22 | 3 | 11（含 spec 闭环后的 sync 与评审修正的 revalidate） |
| 带 FAIL 的 harness 轮次 | 4 / 11 | 2（FAIL 4→1→0） | spec 2（3、4）+ plan 1（5）= 3 |
| verifier 派发 / 耗时 | 1 次 6.3 分 / 2–4 次 25.4 分 | 1 次 7.3 分 | spec 1 + plan 1，28.4 分 |
| 工具调用 | 305 / 约 400 | 313 | 451 |
| 上下文终值 | 411K / 509K | 474K | 562K |
| 读判据源码（v2） | 20 / 31（09-16 重算） | 29 | 18 |
| 读规则文本（v2） | 49 / 88 | 75 | 90 |

car 的 plan 未闭环：verifier 报告未落盘、`check-receipt` 未跑，其余 plan 产物（plan.md、contracts.yaml、use-cases.yaml）已写。

## 3. 宿主回话

| Case | 回话 | 次数 | 说明 |
|---|---|---:|---|
| auto | 编号（材料「2」+ 两份 docx、会议「1」、范围「1」、术语「1」） | 4 | 均为模型所问 |
| auto | 「继续」 | 1 | 14:07 模型只留了一句自述、没提问 |
| car | 交产品稿一句、材料「1」+ 接受侧说明、范围「1」、术语一句 | 4 | 均为模型所问 |
| car | 交付门「评审通过：撤销时限按产品稿 24 小时，撤销中的用户文案改为『正在收回』，未定两件保持未定。」 | 1 | **宿主自加（improvised）**：模型只问「确认进入 plan 吗」；24 小时与评审意见都不是它问的。用户 09-18 裁定：模型没问的不说。这一句让 car 在已闭环的 spec 上走了一次修正（15:18–15:29），这段改动与时长单列，不算被测模型自发行为；补跑那一轮同样受此影响 |
| car | 「进入 plan。」 | 1 | 模型所问 |
| car | 「继续」 | 12 | 见 §4；用户 09-18 裁定此后模型不提问一律不回 |

## 4. 异常：car 把工具调用写成文字后本轮结束

car 18 次停等中 12 次是同一形态：模型最后一段输出把命令写成 `<invoke name="…">…</invoke>` 文字，CLI 这一轮随即以 `succeeded` 结束。逐次核事件流（`scratchpad/stall_audit.py`）：

- 停等到宿主回话之间工具事件全部为 0——命令没有执行；
- 回话后执行的第一个工具正是停等前写成文字的那条（例：15:18:53 写 `--correction-init`，15:20:53 回话，15:21:13 执行）。

分布：前 4 次停等都是正常提问；从 15:13 spec 闭环之后开始几乎每次都是这一形态，直到 plan。此前 7 次运行（09-14 至 09-18 上午）与本轮 auto 均为 0 次。只出现在同一会话的后半段、越来越频繁，推测与被测会话上下文变长后的输出格式退化有关（car 上下文终值 562K，为历次最高之一）——未验证。

用户 16:13 叫停时 car 停在：plan verifier PASS 后「写报告」一步写成文字、本轮结束。

## 5. 未验证 / 留给第二轮

- car 的 plan 闭环（报告落盘与回执）本轮未完成。
- 「写成文字就停」的成因（被测模型、CLI 适配或上下文长度）未定。
- 两次稳定性比较（04 §2 比较单元）要第二轮产物。
- 按用户裁定，第二轮宿主只答模型所问；car 交付门若只问进不进 plan，只回「进入 plan」。

# 步骤 6 评审：工作区未提交版（R7 提示层重排 + R8 写作单位与改章边界）

结论：**静态通过，一处小返修后提交。** R7 的性质逐条核过成立，R8 两条改章路径与代码一致，五条读法偏差里四条采纳、一条部分采纳（放回一句）。预算完成目标已由用户 2026-09-18 重签为 11500，yaml 已改，本评审只核账目一致。

## 1. 独立复核

| 门 | 结果 |
|---|---|
| `pytest test/story/tests -n auto --dist loadscope` | **1155 passed / 356 subtests**，与交回一致 |
| `check_failure_modes.py` | FAIL 0（委派 8） |
| `adapt-scan --check --target .` | 通过（新建 `contracts/README.md` 在包内） |
| `framework/` | 零 |
| `grep -rn "仅有的两处\|只有这两处" doc/extensions` | 零命中 |
| 六份 author.md 的「推进不逐段问」blockquote | 零命中 |

行数（非空行，HEAD → 工作区）：SKILL 211→140、story-write 355→234、spec 146→93、spec-sections 131→53、plan-sections 137→71；rules 与 author.md 基本不动。作为观察值记录，不作达标条件。

按 01 R7 的性质逐条核：

| 性质 | 核到的事实 |
|---|---|
| 合同已定形态零复述 | story-write 里术语表头、交付物四列、附录五节名、验收表头不再出现；十一类只留「从下面十一类里挑一个」（见 §2 第 1 条） |
| 机器不核的项明说由审查判 | 第一节头注「标机器核的有判据，其余由语义审查判」；「需求不是演示」明写不设词表由审查判 |
| 停等说明只在 SKILL 一处 | SKILL §「停等真值表」六行：材料（无条件）、会议（有 `question` 才停，脚本枚举）、范围（无条件）、交付门后、查无此单、归档恢复；scope_gate、init_analysis、ar_design_init 改为引用 |
| 停等表与 E11 一致 | 会议一行的条件与 `routing.py::meeting_step` 一致 |
| 金样 12 条批注可指认 | 交回 §3 表逐条对上第一节清单，抽核第 1、3、8、11、12 条属实 |
| ⑤ 修正入口与 SKILL 原 :106-110 合一 | `phases/spec.md` ⑤ 一处：闭环链不回头、真实问题走 `--correction-init` / `--revalidate`、不手动派 verifier；SKILL 不再复述 |
| 每份文件头一句读者与时机 | 抽核 scope_gate、init_analysis、contracts/README 属实 |

R8：story-write 第三节「落盘与改章只有两条路」（登记前 `chapter` 重提，登记后 `reopen`）；`context.mjs::refuseIfFrozen` 报错点名 `story_flow.py reopen`；「写作单位是一条业务关系」与 drafts 章头一句到位；:206 绝对句退出。真实产物副本上登记后重提被拒并指路、reopen 后重提只替换那一章，交回 §4 已验。

合同 `story-chapters.json` 只剩数据键，19 个说明键迁入 `contracts/README.md`，按位置排列。E5 那一小节仍在 init_analysis ⑤2 与 scope_gate 缺口句里，没有在重排中丢掉。

## 2. 读法偏差裁定（交回 §5）

| 项 | 裁定 |
|---|---|
| 1 十一类表保留 | 采纳：「读到什么就检查 / 撞的方向」两列只有这里有，是扫描地图不是配额 |
| 2 F02 判据跟着对象走 | 采纳 |
| 3 §五 三句未保留 | **部分采纳**：「决定行为理解的关系图放正文，不搬进附录」放回第三节画图段一句——它是 ⑫「附录里不放图」在作者侧的正向说法，也是 E3c 关系承接的前提；另两句（列名对准真实观察者、没有真实测量不画结果图）归审查判，不放回 |
| 4 合同说明键实数 19 | 采纳 |
| 5 归档恢复并进真值表 | 采纳：既有确认点一并列出才是全表 |

## 3. 预算

本步 −363（prompts −362），累计相对 9b4637fa 为 −245。用户 2026-09-18 已选「重签为实账」并签分类目标 3360 / 2310 / 3370 / 1690 / 770、总 11500，yaml 头注记签字。核过：分类之和 11500；步骤 7 剩余退出约 −150，做完约 11480 在目标内。

顺带记一笔供步骤 8 交付时如实写：整个 1.9.3 相对 1.9.2 收口 11878 预计净 −400 左右。方案 04 §5 估的 −1015 没有兑现，三步的估算口径都偏乐观，确定性替换实退少、送达实增多，这一点已在各步评审逐笔记账。

## 4. 下一步

1. 实施者放回 §2 第 3 条那一句，跑离线门，提交本步（提交说明指本评审）。
2. 步骤 7 退场，基线为本步提交；清单以 03 为准，含步骤 2 评审加的 hooks 侧定位与围栏。
3. 步骤 8 前把 04 §2 的比较单元口径再读一遍：会议逐话题一行、E5 枚举、闭环后不重派三项是本轮实跑要看的新行为。

# 步骤 2 评审：f7ef4433（成文侧收敛与红线）

结论：**静态通过，步骤 2 关闭。** R4 与 R6 按方案落地，退出项零残留，四道门独立复核全绿，十一条读法偏差全部采纳，其中第 10 条转成步骤 6 的一项退场（已写进 03 §4）。分类完成目标的签字改到步骤 3 交回时一次签（见 §3）。

## 1. 独立复核

| 门 | 本评审运行 | 结果 |
|---|---|---|
| `pytest test/story/tests -n auto --dist loadscope` | 跑了 | **1137 passed / 356 subtests，54 秒**，与交回一致 |
| `check_failure_modes.py` | 跑了 | 64 形态 FAIL 0（委派 8） |
| `adapt-scan --check --target .` | 跑了 | 通过 |
| `framework/` | `git diff --stat f99026e9 f7ef4433 -- framework` | 零 |
| 退出残留 | rg `matchSection / pipeTables / subHeadingsUnder / scanDanglingRefs / EXEMPT_LINE_PATTERNS / harness_terms / ai_heading / search_phrase / placeholder_heading / inFence` | `core/story/` 零命中；合同 `language_redline.kinds` 只剩四类，`harness_terms` 键不在 |

逐文件读过 diff：`document.mjs`（`parseDocument / fenceRanges / findByName / headingEnd / tableCells / tablesWithin / zonesByLine`）、`language.mjs`（重写后全文）、`check.mjs` ⑨/⑩/⑩b、`appendix.mjs`、`images.mjs`、`recheck.mjs`、`chapter-contract.mjs`、`sources.mjs`、合同 diff、24 个夹具的标记改动、`test_chapter_view.py::OneRuleOnEveryPath`、`test_story_build.py` 机器区两条、`test_negative_guards.py`。

按方案逐项核：

| 项 | 核到的事实 |
|---|---|
| R4 一次解析 | `parseDocument` 一次给围栏（三条件关闭）、各级标题、只认「表头 + 分隔行」的表；`findByName` 先精确再包含、包含只在唯一命中时成立、两个以上给出原名报歧义；`headingEnd` 是唯一节尾规则；`tableCells` 转义竖线留在格里 |
| R4 消费者 | 附录 `specSection` 与投影插入、附录围栏语言判、`diagramsOf`、`screeningDoubts`、`chapter-contract` 子节名全部改调 `document.mjs`；`test_the_open_close_rule_exists_in_exactly_one_place` 改为扫 `core/story/*.mjs` 全部模块 |
| R6 四类 | 合同 `kinds` 只剩 `repo_identifier / rule_id / doc_coordinate / source_tag`，`source_tags` 五词进合同，「灰度」改法去掉本工程业务词；不认得的类别抛错；一行一类一条，`spec §5.1` 已报就不再单列 `§5.1`；HTML 注释行不扫 |
| R6 文档坐标 | 悬空引用与裸文件名并入，按形态各带修法；作用域 `all`；review 只判文档坐标（原 ⑨ 对 review 的覆盖等价） |
| R6 机器区报到真源 | `zonesByLine` 从起始标记读区名与真源；⑨ 与 ⑩ 的 story 命中按行分到作者区与机器区，机器区收进 `⑩b` 一条，给真源与 `story-build.mjs project`；`zoneSpan` 仍按区名匹配，旧产物的旧真源标签照样被认（标签是自由文本），摘要不含标记行 |

## 2. 读法偏差裁定（交回 §5）

| 项 | 裁定 |
|---|---|
| 1 `§x.y` 全篇判为文档坐标 | 采纳：`renumberStory` 只重编标题不改正文引用，写号必漂；修法「改用本文章节名」正确 |
| 2 文件名形态只认文档与知识/框架产物名 | 采纳：资源文件名是工程标识，附录是它的落点 |
| 3 文档坐标作用域 `all` | 采纳：与原 ⑨ 扫全篇一致 |
| 4 HTML 注释行四类都不扫 | 采纳 |
| 5 机器区报到真源覆盖 ⑨ 的仓内路径与禁用词 | 采纳：方案本就没限定类别 |
| 6 投影真源标签改名、夹具同步、历史产物不动 | 采纳：区按名认，标签只进报错文案 |
| 7 「关卡」零命中只核 `language_redline` 一段 | 采纳：`gates_note` 里的「关卡」是流程概念不是词表 |
| 8 歧义只在必要 H3 一处报 | 采纳 |
| 9 表只认带分隔行的 | 采纳；交回已列为未验证项，步骤 4 检查点看有没有无分隔行的 §9 表 |
| 10 `hooks/spec/post_check.mjs` 自己的 `findHeading / sectionRange / scanDocCoords`（`:88-150`）与 `verifier-report.mjs::yamlBlocks`（`:87`）是 hooks 侧的第二份定位与围栏 | 采纳，**转成步骤 6 退场项**：已写进 03 §4——`findHeading/sectionRange` 改调 `document.mjs`，`scanDocCoords` 改用 `language.mjs` 的文档坐标形态，`yamlBlocks` 改用 `fenceRanges`；只换读法不改判据 |
| 11 顺带做了 03 §3 的两件 | 采纳 |

## 3. 预算：本步实账通过，分类目标改为步骤 3 交回时一次签

本步 −102（MJS −79、data −23），五类都在在途额度内。差额 213 行的三条解释核过：六套定位、五套围栏、两套表解析每套只有 5–18 行，方案按「一套一份实现」记退出量偏高；机器区报到真源是新职责（+34），方案 R6 没记。成立。

累计相对 9b4637fa：−124。按现值加剩余计划再算一次终值：

| 类 | 现值 | 剩余计划 | 预计终值 |
|---|---:|---:|---:|
| hooks | 3189 | E1 +10、E2 +25、E3 +70、03 −30 | 3264 |
| MJS | 3402 | E4 +15、E6 +5、03 −60 | 3362 |
| Python | 2386 | E8 +10、03 −70 | 2326 |
| prompts | 2025 | R7/R8 −480、README +40、E4/E5 +15 | 1600 |
| data | 752 | ⑬ +8 | 760 |
| 合计 | 11754 | −442 | **约 11310** |

步骤 1 交回的 11110 提案已过时。两步的估算口径偏差方向一致（退出量按原始行数或按「一套一份」估），再按剩余计划提一版仍会在步骤 3 后变。**改为：分类完成目标在步骤 3 交回时按三步实账与剩余计划一次提出、一次签**；到那时只剩 R7/R8（prompts）与 03 两块估算，误差可控。签之前 yaml 不动。用户此前定的「步骤 1 交回时分账再签」按此调整，00 §7 同步。

## 4. 观察项（不要求动作）

- `docCoordinateForms` 的知识文件名派生失败时退回「任意 .md 与框架产物名」并 stderr 出声，不静默；可接受。
- `source_tag` 只认括号里以登记词起头的那种，表格格子不判；与原实现口径一致。
- 步骤 1 评审记的「值里英文冒号加空格要加引号」仍挂在步骤 3 E1。

## 5. 下一步

1. 步骤 3（送达与登记：E1 → E2 → E3 → E4 → E5 → E6 → E8 → E9），基线 f7ef4433；开工前先按 02 E3「体积决定」用两 Case 第二轮产物副本各装配一次记任务书字符数基线。
2. 步骤 3 交回时一并提分类完成目标。

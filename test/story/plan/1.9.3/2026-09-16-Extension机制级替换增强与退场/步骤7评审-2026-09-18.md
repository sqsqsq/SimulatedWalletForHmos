# 步骤 7 评审：工作区未提交版（退场）

结论：**静态通过，步骤 7 关闭，可提交。** 03 的退出项全部落地且零残留，保留项未动，hooks 侧第二份定位与围栏收进 `document.mjs` / `language.mjs`，度量算法 v2 与人工核对一致。五条读法偏差全部采纳。完成目标已由用户按实测收口为 11536。

## 1. 独立复核

| 门 | 结果 |
|---|---|
| `pytest test/story/tests -n auto --dist loadscope` | **1152 passed / 356 subtests**（比步骤 6 少 3：退出的专用用例） |
| `check_failure_modes.py` | 64 形态 FAIL 0（委派 8） |
| `adapt-scan --check --target .` | 通过 |
| `framework/` | 零 |
| 退出项残留 | `imported / materials_changed_after_complete / reopened / scope_source / SCOPE_SOURCES / ANALYSIS / scope-text / topics.json / danglingFigures / REVIEW_BANNED_LINES / PER_UNIT_TABLE_RE / DOC_COORD_RE / rowCells / obj.notes` 在 `doc/extensions` 机制层零命中（合同 README 的说明文字除外） |
| 保留项 | ⑬ 改读合同 `review_form_banned_lines`（合同 :238）、⑮ 仍在 `check.mjs:284`、`depends_on` 仍在 `inputs.py:191/320`、frontmatter 未动 |
| hooks 侧收口 | `spec/post_check.mjs` 走 `parseDocument/headingEnd/tableCells` 与 `CROSS_DOC_COORDINATES`；plan/review 用 `cellByHeader/tableCells`；hooks 里自翻围栏零命中；`test_chapter_view` 的「开闭规则只有一处」已扩到扩展全部 `.mjs` |
| 改读合同 | `PENDING_MARK` 从合同 `pending_mark` 读、读不出响亮；`upstreamDocs` 从合同 `sources.SE/SPEC.path` 取 |
| 测试域 | retired 六个 checker 函数已删；`measure_run.py` v2 按管道与连接符切段、只计直接作用于路径的读取动词，输出带算法版本与未分类数；`度量重算-读取算法v2-2026-09-18.md` 对 09-16 那轮算出 auto 20 / car 31，与当时人工核的 19 / 31 一致 |

逐文件读过 diff 摘要（44 个文件，+298 / −516）：流程 Python 六份、成文 MJS 八份、hooks 十份、合同与 README、提示层三份、测试域六份。

## 2. 读法偏差裁定（交回 §4）

| 项 | 裁定 |
|---|---|
| 1 切列收到 `document.mjs` 不是 `knowledge.mjs` | 采纳：步骤 2 已把切法收成 `tableCells` 一处，再在 knowledge 起一份就是第二份；03 那行按实际改 |
| 2 spec 门禁的文档坐标加认 `PRD §` | 采纳：与成文红线同一份形态，原则一致；文件名与裸 `§N` 不并进 spec，正确 |
| 3 `scanNumericSources` 的围栏也收了 | 采纳：它是 hooks 里最后一处自翻围栏 |
| 4 UT 桥接用例反转 | 采纳：原用例锁的正是 03 要消掉的两种读法 |
| 5 `recommended` 只退文档与骨架 | 采纳：透传字段不值得加剥离代码 |

## 3. 预算

本步 −98，累计相对 9b4637fa 为 −342；实测 11536，比 09-18 上午签的 11500 高 36（Python 32、MJS 20），原因核过：字段多只占一两行、改读合同是加行。用户同日选「按实测收口」，yaml target 已改为 scripts_py 2342 / scripts_mjs 3380 / 总 11536，头注记签字。整个 1.9.3 相对 1.9.2 收口 11878 净 −342。

## 4. 下一步

1. 实施者提交本步（提交说明指本评审）。
2. 步骤 8 真实验证：两 Case 各两次，按 04 §2 的比较单元与完成判据（人工审视为准，三轴辅助）；本轮新行为三项要看：会议逐话题一行、需求方材料枚举（E5）、闭环后不重派；过程指标用 measure v2 与检查点并列；E2 是否触发如实记。
3. 交付：发布说明 1.9.3、使用指南差异、FEATURES 迁移单列不在本需求。

# 步骤 1 交接：读法与门禁（R1 → R2 → R3 → R5 → E10）

给维护实施者。方案真源在 `test/story/spec/2026-09-16-Extension机制级替换增强与退场/`，本文只把步骤 1 要读的段、已定的决定、开工前复核出的事实差异和交回要求收在一处，不复述方案正文。设计与实施分会话；实施者只能声明「已实施，等待评审」。

## 0. 开工前

1. 工作区里有两份 1.9.2 收口的受跟踪改动未提交：`test/story/release/1.9.2.md`、`test/story/release/story使用指南.md`。先按既有纪律单独提交它们（说明指 `plan/2026-09-11-知识协议-Review决策-会议转写/1.9.2收口-2026-09-16.md`），再在其上开步骤 1。
2. 必读：`test/story/AGENTS.md`（§3、§5）、`TEST.md §7`；方案 `00` §4/§6/§7、`01` R1/R2/R3/R5、`02` E10、`03` §2/§6/§7、`04` §1/§5/§6。行号以 9b4637fa 为准，本文 §2 列了开工前重核出的差异。
3. 本步只做离线门（04 §1），不跑 CLI；CLI 检查点在步骤 4。

## 1. 本步承担什么、替换什么、保留什么

| 项 | 新承担者 | 被替换的旧内容 | 保留的能力 |
|---|---|---|---|
| R1 | 新 `hooks/shared/yaml.mjs`：`parseYaml(text)`，借 `framework/harness` 的 `yaml` 包，签名不变 | `yaml-lite.mjs` 整文件；`contracts.mjs::flowStyleProblems` 与 `plan/post_check.mjs:237` 调用；`plan/author.md:52`「只读块式」；`test_knowledge_protocol.py` 里的流式拒收用例 | 「解析失败响亮、不吞成空对象」（新封装抛 `yaml` 包原始错误）；`test_yaml_lite_scalars.py` 改指新读取器后原样保留 |
| R2 | `contracts.mjs::resourceEntries`，`resource_keys.<module>.<category>.<key>` 精确 ref | `contracts.mjs:106-111` 模糊分支对 `resource_keys` 的适用；`plan-sections.md`、`plan/author.md` 里的平铺示例 | `files` 的路径尾匹配不动 |
| R3 | spec/plan 两份 post_check 按 01 R3 表分六组，各组自判前置、能跑的全跑、一次 `gate()` | `spec/post_check.mjs` 三处提前 return（缺章、`UseError`、`coverageProblems` 非空）；`plan/post_check.mjs:205/223/226` 整体返回 | 组间依赖用现有 `skipped` 表达；仍是一个 check id |
| R5 | `story-chapters.json` 新增 `flow: {schema, gates, carry_all}`，Python（`state.py:54`、`importer.py:60` 收一处）与 `flow/check.mjs` 都从它读 | `FLOW_*` 字面常量；两处读文件与两种 BOM 剥法（`check.mjs:44` vs `:96`）；01 R5 表标「退出」的 7 条判据与只为它们写的用例；复述阶段顺序的文案 | 01 R5 表标「保留」的判据（含 `chosen∈options`、第二级 ⊆ 分析选项、split carrier 恰一份、positioning 三项） |
| E10 | `⑫d 上游图承接` 独立标签，一图一行，离线整类不判 | `carriedDiagramProblems` 挂在 ⑫b（`story/check.mjs:248`）与多图重复的报错 | 判据本身不变 |
| 测试域 | 公共 helper：临时工程根下建 `framework/harness` → 真实目录的 junction | 各套件各自复制扩展的写法不动，只加一行接 helper | 不复制 node_modules，不退回旧读法 |
| 安装期 | `adapt-scan.mjs --apply/--check` 核 `<目标根>/framework/harness/node_modules/yaml` 存在，缺则提示先装 harness | 无 | 不计入机制预算 |

## 2. 开工前复核出的事实差异（已改进 01 R1）

- yaml-lite 的第六个直接消费者是 `skills/story-adaptation/scripts/adapt-scan.mjs:25`，不是「间接的 acceptance 读取」；成文侧 `core/story/{appendix,context,delivery,language}.mjs` 经 hooks 间接读。所以 R1 落地后 `story-build` 全链也依赖新读取器。
- 把扩展 `copytree` 进临时根的套件是 18 个，不止 03 §6 列的六个。不预先猜名单：R1 落地后跑离线全量，凡因找不到 `framework/harness` 抛错的套件逐个接 helper。抛错文案要带 `cd framework/harness && npm install`，这是 04 §1 的验收项之一。
- `test_yaml_lite_scalars.py` 不是「针对 yaml-lite 的专用测试」：它锁 adapt 用 `name` 相等判来源时几种合法写法读出同值，是仍有效的回归覆盖，改指 `yaml.mjs` 保留，并把 01 R1 的四类样本深相等比对用例写在它旁边。
- 安装器就是 `adapt-scan.mjs --apply`（story-adaptation SKILL 第 56 行），安装期检查落在那里，不另起脚本。
- `framework/harness/node_modules/yaml` 当前在位，版本 2.9.0，`package.json:46` 声明 `^2.7.0`。

## 3. 已定的决定（不再问）

- R1 运行期只有一条路径：找不到根或包就在第一次 `parseYaml` 抛错；不下载、不双读、不退回。根从 `yaml.mjs` 自身位置向上找 `framework.config.json`。
- R5 退 7 条、保留 6 类，以 01 R5 表为准；03 §2 与 00 §3 引用它。
- 本步不碰 R4/R6/R7/R8 与 03 其余退场项；R2 需要改的两处提示示例（`plan-sections.md`、`plan/author.md`）与 R1 的 `author.md:52` 一句属本步。
- 预算完成目标总量 10860（用户 2026-09-16 晚同意按 04 §5 实估重签，原 10600 作废）；实账若仍高于它，由用户在步骤 6 交回时定。

## 4. 本步预算

按 04 §5 的归属账，本步预计：hooks −180（R1 −230、R3 +40 −30、R2 +40）、MJS −90（R5 +20 −120、E10 +10）、data +10（R5 `flow`）。合计约 −260，五类都在现行在途额度内（scripts_py 2400 / scripts_mjs 3520 / hooks 3220 / prompts 2050 / data 800 / 总 11950），本步不需要新签在途额度。

交回时按实账报五类实测与差额原因；同时按 04 §5 提出**分类完成目标**（合计 10860）供用户签，签后由实施者改 `regression/mechanism-budget.yaml` 的 target 并在头注记用户签字与日期，不在签之前改。测试域 helper 与 adapt-scan 改动不入机制预算。

## 5. 离线门与本步验收

| 门 | 要求 |
|---|---|
| `pytest test/story/tests -n auto --dist loadscope` | 全绿 |
| `check_failure_modes.py` | FAIL 0 |
| `adapt-scan --check --target .` | 通过 |
| `test_mechanism_budget.py` | 全绿（本步净减） |
| `framework/` | `git diff --stat` 零 |
| R1 | 四类样本（framework `contracts-template.yaml`、1.9.1 car 的 `contracts.yaml`、当前 `knowledge-use.yaml` 骨架、任一 verifier 报告的 YAML 围栏）新读取器与 framework `YAML.parse` 深相等；临时根缺 `framework/harness` 时第一次解析抛错且文案含 `npm install`；`grep -rn "yaml-lite\|flowStyleProblems" doc/extensions test/story` 零命中（拒收用例除外） |
| R2 | 夹具两模块、各两分类、一个 key 带点：must 全派生且 ref 唯一；分类层 must 被点名 |
| R3 | spec：三类独立问题同轮同现；缺「规约约束要求」章时投影组 skipped、术语组照报。plan：两类同现；契约解析失败时义务组 skipped、章节组照报 |
| R5 | `grep -rn "carry_all\|material_scope" doc/extensions --include=*.mjs --include=*.py` 只剩合同读取处；坏 JSON / schema 错 / status 值域外三种仍被报 |
| E10 | auto 第二轮 4 图缺承接的场景重放为夹具：4 行不是 16 行，标签 ⑫d |
| 过拟合自查 | 新增文案零 Case 专名；`adapt-scan` 通过即机械部分完成，语义部分交回里写一句 |

## 6. 交回

写 `步骤1交回-2026-09-16.md`（本目录）。列：每项实际 diff 位置与替代关系；接了 helper 的套件清单与原因；离线门结果；R1 四类样本比对结果；预算五类实测、与 §4 预计的差额及原因、分类完成目标提案；与方案读法不同之处逐条列在「读法偏差」一节给评审裁；未验证项。不写自评分，不催 CLI。

# 步骤 8 第一轮评审（2026-09-18）

评审对象：`步骤8第一轮-2026-09-18.md`，suite `20260918-140014`，机制版本 1af7fdf7（步骤 7 末）。第二轮未起。

结论：**记录属实；本轮要看的四项新行为三项成立、一项部分成立；未决写成已定这一 1.9.2 的首要缺陷在 auto 上没有复现。** car 的 plan 未闭环是宿主叫停，不是机制；car 的「工具调用写成文字就停」是被测模型或 CLI 的输出退化，不归扩展。第二轮按用户裁定的宿主纪律再跑一次，再做 04 §2 的比较。

## 1. 独立复核（事件流重算）

| 指标 | 第二轮基线 auto / car | 本轮 auto / car |
|---|---|---|
| harness 轮次 | 7 / 22 | **3 / 11** |
| 带 FAIL 轮次 | 4 / 11 | **2 / 3** |
| 同一 check 重复 FAIL 最多 | 3 | **1**（两 Case 每条都只 FAIL 一次） |
| verifier 派发（task 事件） | 1 / 2–4 | **1 / 2**（spec 各 1，car plan 1），PASS 后零重派 |
| spec 闭环后的修正 | 重跑 harness 换 subject | car 走 `--correction-init` → `--revalidate`，回执 `completed_with_prior_review`，没有派 verifier |
| 进 spec 前停等 | — | auto 3 次（材料 → 会议 1 个待裁话题 → 范围）；car 2 次（材料 → 范围）；交料后没有再问材料 |
| 读判据源码（v2） | 20 / 31 | 29 / 18 |
| 读规则文本 | 49 / 88 | 75 / 90 |
| 上下文终值 | 411K / 509K | 474K / 562K |
| 工具调用 | 305 / 约 400 | 313 / 451 |

门禁、重派、停等次数这三组按方案方向下降；上下文与读规则文本没有降，car 的一部分来自 §3 的异常。

## 2. 四项新行为

| 行为 | 结论 | 证据 |
|---|---|---|
| E11 会议交互合一 | **成立** | auto `status` 的 next 依次 `await_gate:material_scope` → `await_gate:meeting` → `await_gate:scope_decision`；关卡记录 material_scope / meeting T3 / scope_decision 各一笔；交料后未再问材料 |
| E5 需求方提到的材料 | **首次成立** | car `init-analysis.md:71` 列出 `inbox/车钥匙接受侧界面说明.docx` 已导入；前三轮都没要 |
| E2 评审动作落到议题 | **首次触发** | auto `knowledge-use.yaml:129 decision: "doc-archive"`；car `:117 decision: D5` |
| 裁定 A 闭环后不重派 | **成立** | 见 §1 派发与修正两行 |
| overlay 会议逐话题一行 | **部分** | auto 报告的 `story_reader_review` 证据格逐章提到 T1/T2/T3/T5 并回查 raw.md，但没有按「话题 id → 人选 → 依据」逐话题各一行。审查读了会议、判了去向，只是输出形态没照契约 |

会议逐话题行这一项不再加提示：判断已经发生（1.9.2 时是审查压根没读会议），差的只是形态；步骤 8 第二轮看它是否稳定，交付说明如实写「部分」。

## 3. 产物抽核（供用户人工审视用，不替代）

auto 的两件在 1.9.2 两轮里被写成已定的未决，本轮保持未决且三处一致：

- 面额调高是否重验：`doc-refresh.md` T2 标未决；`story.md:330` 交付章「未决，正文按边界处理，联调前确认」；`review.md` 1.1 待确认区决策点、依据引澄清会「只改门限不重验（已定）、面额上调待支付与风控确认」。
- 建约失败重试与协议号有效期：`doc-refresh.md` T7 标未决、明写不按十分钟写；`story.md:275` 异常表「复用窗口结论未决，见决策登记」；`review.md` 1.2 待确认。

已定的写成已定：`story.md:245`「只改门限不涉及额度，无需重新验证」与会议原话一致。review 的「已定事项」四条有内容，不再是空节。这是 1.9.2 首要缺陷（未决夹带默认实现）在同一材料上的一次反例，稳定性要第二轮再看。

## 4. 异常与宿主

- **写成文字就停**：car 13 次 text 事件里含 `<invoke name="bash">` 原文，每次工具事件为零、宿主说「继续」后才执行同一条命令；auto 0 次。样本里命令前有一个孤立的「course」字样，形态是被测模型把 CLI 的工具调用标记当正文输出。与扩展无关；只在 car 会话后半段、上下文 500K 以上出现。记入 TEST 装置观察；用户已裁定模型不提问一律不回，第二轮照此执行。
- **宿主自加评审意见**：car 交付门那句「24 小时、正在收回」是模型没问的，引起一次 spec 修正与 15:18–15:29 的时长，已单列；`interaction-script.yaml` 已加「没问不说」。第二轮执行。
- car plan 未闭环（verifier 报告未落盘、check-receipt 未跑）是 16:13 叫停所致。

## 5. 与方案的对照

04 §2 要求两 Case 各两次、按类型比较单元做稳定性比较，本轮只有一次，比较未做。步骤 8 检查点最小规则里的两条（过程指标下降、verifier 引用并列证据）本轮都成立。完成判据是用户人工审视，本评审只给事实。

## 6. 第二轮

1. 同机制版本 1af7fdf7、同配置；宿主只答模型所问，car 交付门若只问进不进 plan 只回「进入 plan」；模型不提问不回话。
2. 跑完做 04 §2 的六类比较单元两两比：规约判断 T/F 与理由类别、决策集合、未决承接、图关系、章内独有解释、会议话题去向。
3. car 跑到 plan 闭环；若「写成文字」再现，记次数与上下文位置，不改机制。

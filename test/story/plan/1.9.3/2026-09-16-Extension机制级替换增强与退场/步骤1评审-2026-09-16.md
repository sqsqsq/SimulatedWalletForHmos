# 步骤 1 评审：f99026e9（读法与门禁）

结论：**静态通过，步骤 1 关闭。** 五项都按方案落地，退出项零残留，四道门独立复核全绿，七条读法偏差全部采纳。**分类完成目标提案退回重算**：它与剩余步骤的计划账对不上（详见 §3），签字前要按现值加剩余计划重列。这不是代码返修，不阻断步骤 2 开工。

## 1. 独立复核

| 门 | 本评审运行 | 结果 |
|---|---|---|
| `pytest test/story/tests -n auto --dist loadscope` | 跑了 | **1131 passed / 356 subtests，52 秒**，与交回一致 |
| `check_failure_modes.py` | 跑了 | 64 形态 FAIL 0（委派 8） |
| `adapt-scan --check --target .` | 跑了 | 通过 |
| `test_mechanism_budget` | 在全量内 | 绿 |
| `framework/` | `git diff --stat 9b4637fa f99026e9 -- framework`、`git status` | 零；`node_modules/ts-node/dist/bin.js` 与 `scripts/check-receipt.ts` 已核在位（junction 事故后恢复属实） |

逐文件读过 diff：`yaml.mjs`（新）、`gate.mjs`、`spec/post_check.mjs::knowledgeExitGroups`、`plan/post_check.mjs` 五组、`contracts.mjs::resourceEntries/resolveEntityRef`、`obligations.mjs`、`flow/check.mjs`、`state.py::contract_flow`、`story-chapters.json.flow`、`story/check.mjs` ⑫d、`images.mjs`、`adapt-scan.mjs`、`ext_workspace.py`、`test_yaml_reader.py`、`test_gate_groups.py`、`test_golden_sample.py` E10 用例。

按方案逐项核：

| 项 | 核到的事实 |
|---|---|
| R1 | 根从 `yaml.mjs` 自身向上找 `framework.config.json`，不用 cwd；首次 `parseYaml` 才取包；找不到根或包抛错且文案带 `cd framework/harness && npm install`；`yaml-lite.mjs`、`flowStyleProblems` 全仓零命中；`test_yaml_reader::SameObjectAsFramework` 四类样本与 `yaml` 包直接 `parse` 深相等，样本缺席时只核模板不假造 |
| R2 | `resourceEntries` 按两层对象摊平、形状错报到模块或分类；`resolveEntityRef` 对 `resource_keys` 整串精确匹配；`misplacedMust` 对模块层与分类层的 must 点名；`files` 的路径尾匹配未动 |
| R3 | spec 四组、plan 五组各自判前置；`coverage` 有问题时只投影组 skipped，桥接组照跑；契约解析失败时只义务与集合组 skipped，章节组照报；仍是一个 check id，`gate` 按组分节、编号跨组连续 |
| R5 | `flow` 常量入合同，`state.py` 与 `flow/check.mjs` 都读它，读不出响亮报错；保留的判据与 01 R5 表一致（gate 合法、options 在、chosen∈options、第一级 chosen 值域、第二级选项 ⊆ 分析选项、split 定案关卡记录与 carrier 唯一、positioning 三项、materials.digest、收口区间、design_generated_at、三件齐备）；退出 7 条属实 |
| E10 | `⑫d 上游图承接` 独立标签，离线整类不判，一图一行 |

## 2. 读法偏差裁定（交回 §5）

| 项 | 裁定 |
|---|---|
| 1 只链接 `yaml` 包一个目录 | **采纳**，且是必须的：整目录 junction 让替身写进真实 harness，已恢复；`ext_workspace.py` 文件头把这条教训写下了，够 |
| 2 helper 顺手写空 `framework.config.json` | 采纳：空配置对扩展等于默认路径 |
| 3 安装期检查只在目标有 harness 时 die | 采纳：没有 harness 归 framework-init |
| 4 `FLOW_FILE / FLOW_FIX / FLOW_STATES` 保留 | 采纳：合同里没有对应键，`FLOW_STATES` 是 JS 侧区间判的顺序，Python 侧 `after_complete` 只写状态名，两边语义一致；不入合同 |
| 5 与 framework 同一读法带来的两处严格化 | 采纳。**登记为步骤 3 E1 的必做项**：骨架注释与 spec 作者页各写一句「值里有英文冒号加空格要加引号」；步骤 4 检查点若出现 `knowledge-use.yaml 解析失败`，先看是不是这条 |
| 6 `test_yaml_lite_scalars` 改名并按 framework 判法改两条 | 采纳 |
| 7 `gate` 渲染组名一行 | 采纳 |

## 3. 预算：本步实账通过，分类目标提案退回

本步实测 11856（−22），五类都在在途额度内，不需要新签；差额 238 行的五条解释逐笔核过（`yaml-lite` 按 `code_lines` 口径 146 行、7 条判据本就一行一条、分组要各自判前置、`state.py` 从合同读三个常量），成立。

**分类目标提案对不上剩余计划。** 用现值加 04 §5 剩余各项算：

| 类 | 现值 | 剩余计划 | 预计终值 | 交回提案 |
|---|---:|---:|---:|---:|
| hooks | 3189 | E1 +10、E2 +25、E3 +70、03 −30 = +75 | 3264 | 3050 |
| MJS | 3481 | R4 −150、R6 −155、E4 +15、E6 +5、03 −60 = −345 | 3136 | 3100 |
| Python | 2386 | E8 +10、03 −70 = −60 | 2326 | 2330 |
| prompts | 2025 | R7/R8 −480、README +40、E4/E5 +15 = −425 | 1600 | 1600 |
| data | 775 | R6 −10、⑬ +8 = −2 | 773 | 780 |
| 合计 | 11856 | −757 | **11099** | 10860 |

hooks 提案 3050 比预计终值低 214、MJS 低 36，合起来正好是本步少退的行数被「分摊」进了目标，而剩余步骤没有对应的退出项承接它。提案的加总凑到了 10860，数字本身站不住。

处置：实施者按上表重列分类目标（合计约 11100），差额 240 行与 10860 的关系如实写明「本步估算口径失准造成，剩余步骤无对应退出项」，交用户签；用户可选重签 11100 或在步骤 6 交回时定继续退。不在签之前改 yaml。

## 4. 观察项（不要求动作）

- `yaml.mjs` 把空文档读成 `{}`（framework 的 `YAML.parse('')` 是 `null`）。调用方都按对象用，这一处差异有意为之且文件头写了理由；深相等用例对空文档也按 `?? {}` 比，口径一致。
- `test_yaml_reader::SameObjectAsFramework` 的三类真实样本从 `output/story` 取，目录被清理后只剩模板一类仍能过；不算问题，但届时深相等覆盖会缩到一类，交回里写一句即可。

## 5. 下一步

1. 实施者按 §3 重列分类目标交用户签；签后改 `mechanism-budget.yaml` target 与头注。
2. 步骤 2（成文侧收敛与红线：R4 → R6）开工，基线 f99026e9；开工前重核 01 R4 表列的六套定位与五套围栏的行号。
3. 步骤 3 E1 加上 §2 第 5 条的一句。

# 会话交接（2026-09-18）

给接手的维护实施者会话。方案真源在 `test/story/spec/2026-09-16-Extension机制级替换增强与退场/`（00–04 分册）；本目录放过程件，不入库。本文只写当前状态、已定决定、在途事项和坑，不复述方案。

## 1. 当前状态

| 项 | 状态 |
|---|---|
| 分支 | `story`，工作区干净（本目录不入库） |
| 步骤 1–3 | 已提交：f99026e9、f7ef4433、84229f31；各有交回与评审（本目录） |
| 预算 | 在途额度已签（用户 09-17「OK」）：hooks_mjs 3420、总 12020，写在 `test/story/regression/mechanism-budget.yaml` 头注；完成目标 09-18 重签为按类实账，步骤 7 交回时再按实测收口为 scripts_mjs 3380 / scripts_py 2342 / hooks_mjs 3370 / prompts_md 1690 / data 770，总 11536，已写进 yaml |
| 步骤 4 检查点 | 两轮实跑：suite `20260917-095900`（轮①）、`20260917-180959`（轮②），机制版本 84229f31，宿主由用户另选的模型担任。记录 `步骤4检查点-2026-09-17.md`、`步骤4检查点-2026-09-18.md`，评审 `步骤4检查点评审-2026-09-18.md` 裁定**成立、进步骤 5** |
| 下一步 | **1.9.3 已收口发布**：机制 1af7fdf7，发布说明与指南提交 85884077（`test/story/release/1.9.3.md`）。效果遗留三项（规约命中条件措辞、评审动作被写成已定、会议逐话题一行未生效）立案在 `test/story/spec/1.9.4/2026-09-20-效果侧遗留三项/`，与同目录残留清点 P1–P6 一起进 1.9.4，顺序与预算由设计者定。工作区两处知识 §9 改动与 EVOLUTION 改动非实施者所为，未动 |

## 2. 步骤 5 开工时的第一件事（评审已定）

1. `spec-rules.overlay.yaml` 的 `story_reader_review` 输出契约段（约 :200-204）加一句：任务书里有「会议材料：逐话题并列」一节时，补述逐话题各一行「话题 id → 人选了什么 → 当前结果里的确定行为有没有依据」；没有这些行与没审同形。不加机械判据。已写进 02 E3b。
2. R7 重排 `phases/spec.md` 时，把「verifier PASS 后不再跑 harness，只有 check-receipt 报 subject 失配才重跑」写成动作序列里的一行硬规则，并说明后果（每跑一次 harness 都重派 subject、verifier 再来一次）。
3. 检查点 §6/§8 提的「闭环后 WARN 级修改记进 notes、不改产物、不重派」与「回执『未经重审』MAJOR WARN 登进 04 §4 framework 缺口」**尚未经设计者裁定**，不要自行落地；交回时列为待裁项。

## 3. 耗时问题的归因（已答用户，别再重议）

用户问 1.9.3 耗时增长是 1.9.3 修改导致还是被测模型不稳定。已答：**主要是被测模型不稳定**。

- 同一机制版本 84229f31、同一被测配置 bailian-deepseek：car verifier 轮① 4 次 / 38 分，轮② 12 次 / 120 分。
- 1.9.3 之前同配置 car verifier 派发 2、4、4、2 次；轮① 在区间内，轮② 离群。
- 触发重审的环节（投影 + 冻结的 reopen 重走、framework 整份材料指纹、1.9.2 步骤 7 的「WARN 照常改、改完重绑」）都早于 1.9.3。
- 1.9.3 可量化的影响：任务书 1.13–1.25 倍；car 单次 verifier 未变慢（12.7 → 约 10 分）；门禁侧带 FAIL 轮次四次运行全降。
- 未定部分：每 Case 只两个样本，不排除 1.9.3 对单次 verifier 有小幅影响。
- 用户明确批评过「典型的打补丁方案」：回答归因类问题时只给证据和结论，不附改法清单。

另：宿主模型确有偏差（从未说出 24h 立场、交付门带评审意见、该回编号时回整句），但轮② 的 12 次派发都发生在宿主沉默时，与宿主无关（检查点 §7）。

## 4. 补跑（已完成）

用户 09-18 要求本会话自己当宿主再跑一次：suite `story-suite-20260918-084325`，已 finalize 回灌。结果见 `步骤4检查点-2026-09-18-补跑.md`：car verifier 4 次、实跑 105.7 分，轮② 的 12 次没复现；auto 出现 2 次「修 WARN 后自发重审」。car 终态 `gate_failed` 是装置收尾时 story 检查进程启动失败（0xC0000142），手动重跑通过。

## 4a. 待设计者裁定的新输入

- `用户裁定-会议交互-2026-09-18.md`：用户给定会议材料的期望交互（会议是需求材料的一种；交料后不再重问材料；有待人定的会议话题才确认一次）。现状差异在流程脚本，1.9.3 方案未覆盖。
- 「verifier PASS 后修 WARN 再重审」与步骤 5 硬规则的冲突（见补跑记录 §2–3、检查点 §6）。

## 5. 坑与约束

- `framework/` 只读；CLI 测试一律非沙箱。
- 测试临时根只 link `framework/harness/node_modules/yaml`（`test/story/tests/ext_workspace.py::link_harness_yaml`）。**不要把整个 `framework/harness` junction 进临时根**：桩测试会覆盖真实的 ts-node 与 `check-receipt.ts`（出过事，靠 git checkout + `npm install` 恢复）。
- Bash heredoc 里写带反斜杠的 Python 会坏；脚本先用 Write 写进 scratchpad 再跑。
- 评审会话不提交；提交只由实施者做，提交信息不加署名行；不用 `git reset --hard`。
- 在途额度内超出任何一类即停，按 test/story/AGENTS §5 找用户签，签前不提交。
- 度量脚本：`test/story/scripts/measure_run.py`（`--json`）；读判据脚本次数有已知误计（步骤 6 修），只看方向。

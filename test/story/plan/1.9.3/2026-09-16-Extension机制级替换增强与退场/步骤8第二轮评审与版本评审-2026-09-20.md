# 步骤 8 第二轮评审与 1.9.3 版本评审（2026-09-20）

评审对象：`步骤8第二轮-2026-09-19.md`、suite `story-suite-20260918-175714`（机制版本 1af7fdf7 加两处未提交知识改动）、第一轮 suite `20260918-140014` 的产物（已迁到 `E:\Project\bak\Story-Features-20260918-180000`）。两轮产物按 04 §2 六类比较单元逐类比过；事件流独立重算。用户四问逐一作答，完成判据按用户 09-16 裁定以人工审视为准，本文给事实与建议。

结论一句话：**1.9.3 的需求目标已实现，首要缺陷「未决写成已定」在四次实跑的业务未决上零复现；机制层四次实跑无崩溃、无门禁循环；产物按六类单元两轮基本一致，摇摆集中在三条规约判断、一张承接图和一件评审动作的状态；遗留八项均不阻断发布，建议按本文 §5 发布 1.9.3，三项进 1.9.4。**

## 1. 第二轮记录复核

| 项 | 实施者记录 | 独立重算 | 结论 |
|---|---|---|---|
| 自然闭环 | 两 Case 19:26 闭环 | 事件流末条 19:26:04 / 19:25:55，car `next=run_archived` 后进 plan 且 plan check-receipt 走完 | 属实 |
| harness 轮次 auto / car | 9 / 7 | 9 / 6（只计带 `--phase` 的调用） | 属实 |
| 带 FAIL 轮次 | 3 / 2 | 3 / 2；同一 check 重复 FAIL 最多 1 | 属实 |
| verifier 派发 | 4 / 3 | auto 4 次（18:55、19:08、19:18、19:24）；car spec 2 次 + plan 1 次 | 属实 |
| 写成文字就停 | 0 / 0 | 0 / 0 | 属实 |
| 停等 | 各 3 次 | auto：材料 → 会议（T1、T3）→ 范围；car：材料 → 范围 → 交付门 | 属实，与 SKILL 停等真值表一致 |
| 扩展脚本异常 | 未记 | 0；auto seq 595 的 Traceback 是模型自己用 `python -c` 解析 reopen 输出失败，不是扩展脚本 | 补记 |
| 机制污染 | 两处知识 §9 反模式压短 | diff 只改说明文字（−9 +7 行），不改机制；car plan 选型段没有引用反模式一节 | 对本轮结果无可见影响 |

## 2. 六类比较单元（两轮）

### 2.1 规约适用判断

| Case | 条目 | 第一轮 | 第二轮 | 哪次对 | 摇摆根因 |
|---|---|---|---|---|---|
| auto | DFX-02 | false（「不构成需量化的体积增量」） | true（新增字符串资源，量化记录） | 第二轮。命中条件是「需求新增资源或依赖」，第一轮拿处置结果否定命中，正是知识文件头禁止的写法 | 模型 |
| auto | DLV-02 | true（新增页面与 5 支端云接口） | false（「应用内导航页、钱包端私有消费协议」） | 定不了。命中条件「对外开放页面或接口」没有定义「对外开放」；car 两轮都把端云接口判为对外开放，auto 第二轮判为私有 | 知识措辞 |
| car | COMPAT-02 | false（只新增错误码不改旧码） | true（「新增错误码只扩增不修改」） | 定不了。命中条件「需求改动错误码」没说新增算不算改动 | 知识措辞 |
| 其余 14 条 | 两轮 T/F 一致，reason 引用的事实属于命中条件 | | | | |

1.9.2 的摇摆是 DFX/RES/COMPAT，原因是作者看不到处置列；1.9.3 送达处置后，剩下的摇摆两条落在命中条件本身的措辞上，一条是模型违反读法。这是知识内容问题，不是送达机制问题，修法在 `deliverables.md` 与 `compatibility-checklist.md` 的命中条件列各补一句定义。

### 2.2 决策集合

- auto：两轮业务决策七件一一对应（整体承载、update 接口、AC-R1 修订、开关只管新入口、单日 200、面额调高重验未决、协议号复用未决），状态全部一致。第二轮多一件「端云接口本地仓储模拟」登记为工程事实 confirm，少一件 DLV-02 归档（因判不命中）。合法差异。
- car：三件业务未决（24/48 小时、转分享、强制下线）两轮都是 open。第二轮多两件范围决策（整体承载、接受侧界面范围外），少一件本地模拟工程事实。合法差异。
- **一处状态错误**：car 第二轮 `DEC-DLV02-归档` 写 `status: settled`、decider「开发侧，确认归档动作已安排」，标题却是「归档状态待评审确认」。知识条目写的是「（评审动作）由负责归档的人在评审记录里表态」，第一轮登记为 open、decider「归档负责人（评审时表态）」是对的。第二轮把一件要人表态的评审动作自己定成已定，review.md 因此把它排进「已定事项」。verifier `story_reader_review` 没拦。

### 2.3 未决承接

| Case | 未决 | 第一轮 | 第二轮 |
|---|---|---|---|
| auto | T2 面额调高是否重验 | doc-refresh 未决、story 交付章未决、review 待确认 | doc-refresh 未决、story 4.4 风险 / 5.4 未决分支 / 6.3 / 9.4 四处未决、review 待确认 |
| auto | T6 协议号复用 | 同上 | 同上，7.3「不按十分钟写死」 |
| car | 撤销时限 24/48 | **宿主在交付门说出 24 小时，review 写「定案为 24 小时」**（第一轮污染） | 模型自己登记为 open；story 4.3、8.1 AC-K5「时限待裁决」；spec AC-6 同；首审 verifier 抓到一处「撤销时限被写死」阻断，返修后未决 |
| car | 转分享、强制下线 | open，story 3.2 / 4.2 不当已定 | open，story 3.2 / 4.3 不当已定 |

四次实跑的业务未决零次被写成已定；car 第一轮的 24 小时是宿主输入，不是模型改写。1.9.2 两轮各一次的首要缺陷，在 1.9.3 同材料上没有复现。

### 2.4 图关系

| 图 | 第一轮 | 第二轮 | 判断 |
|---|---|---|---|
| auto SR §1 #1 签约 | 承接并展开（入口三分支、验证失败回签约页、已有签约转管理页，11 条边） | 原样承接（4 条边），分支另画在 5.1 总览与 5.4 未决分支 | 都保持源图路径；分布不同，合法 |
| auto SR §1 #2 管理 | 原样 | 原样 | 一致 |
| car SR §2 #1 发起（时序图） | 原样时序图：车主、钱包、车云、车厂云、接受方钱包五个参与者，alt 签发成功/失败，失败由车云回滚 | **改画成流程图**：参与者消失，车厂云并进「车云+车厂云都成功?」一个判断，接受方钱包变成「推送送达」一个节点，「车厂云签发失败 → 车云回滚」的失败责任变成「整体回滚」 | 第二轮丢了参与者与失败责任归属。verifier 写「三图承接上游关系完整」是误判 |
| car SR §3 #1 状态机 | 六态七迁移加终态 | 六态七迁移，无终态；入口标签「车主发出」比源图「创建分享成功」宽（创建失败会回滚，不进待接受） | 小偏差 |

### 2.5 章内独有解释

两 Case 两轮的主要机制（auto：实名引导、开关管控、缓存；car：幂等创建与草稿续办、状态机、权限随对象）都在正文功能章或流程章，不是只在附录；章位置有差（auto 第一轮实名在 6.1 表内，第二轮独立 6.5）。合法差异。

### 2.6 会议话题去向（auto）

- 第一轮停 1 个话题（AC-R1），第二轮停 2 个（T1 update 接口、T3 AC-R1）。T1 改的是 SR 接口契约，问人有依据；哪些话题要人定这件事两轮不一致，但都在「脚本只枚举带 question 的话题、模型写 question」这条机制内。
- 两轮人选都是采纳，doc-refresh 与人选一致，未选项没有被采用。第二轮少列了第一轮的「连续失败停用无变化」一话题（原话 L33），该话题对产物无影响。
- verifier **两轮都没有逐话题各一行**。第二轮任务书 `ai-prompt.md:2561` 确有「会议材料：逐话题并列」一节，overlay :202 的输出要求也在任务书 :624；verifier 读了会议、判了未决，就是不按那个形态写。0/2。

## 3. 四项新行为与裁定 A 的两轮稳定性

| 行为 | 第一轮 | 第二轮 | 四次实跑合计 |
|---|---|---|---|
| E11 会议交互合一 | 成立 | 成立（两话题一次问完，交料后不再问料） | 2/2 |
| E5 需求方提到的材料 | car 成立 | auto 成立（点名原稿界面图）；car 未触发，模型按 PRD「另行提供、不在本稿」判范围外并登记为决策 | 从 1.9.3 检查点算起 2/5；第二轮 car 的处理有依据，不算错，但行为不稳 |
| E2 评审动作落到议题 | 两 Case 触发 | car 触发（DLV-02 命中）；auto 未触发（DLV-02 判不命中，逻辑一致） | 触发与判断一致；触发后状态见 §2.2 的错误 |
| 裁定 A 闭环链不回头 | car 走 correction-init → revalidate | car：PASS 后 1 MAJOR + 1 WARN 记 notes；**auto：第 2 次 verifier PASS（0 阻断）后模型认为 2 条 MAJOR「是真实缺陷、闭环前值得修」，改产物、重跑 harness、再派两次** | 3/4。`phases/spec.md:69`「⑤ 之后闭环链不回头（硬规则）：有阻断项才返修」写得明确，auto 第二轮是模型没照做；它修的两条（不存在的 `CardNumberMaskUtil`、编造的 ≤1.5 秒）确是真实缺陷，代价是 18 分钟与两次 verifier |
| 会议逐话题一行 | 部分 | 未做 | 0/2 |

## 4. 四问作答

### 4.1 需求目标是否已实现

00 §1 定的三类结构问题：

| 问题 | 机制动作 | 实跑证据（1.9.3 四次） |
|---|---|---|
| 扩展读法比 framework 严 | R1 R2 R3 R5 | YAML 拒收、resource_keys、洋葱式暴露零出现；同一 check 重复 FAIL 最多 1；post_check 报「以下 N 处需要修正（一次列全）」 |
| 形态代理语义、重复判据 | R4 R6 退场 | 语言红线四次只命中一次（auto 第二轮 §10 投影里模型自己写的「spec §9 立名」，命中正确）；投影区问题报到真源 |
| 判断时拿不到原义 | E1 E3 E4 E11 | 业务未决四次零改写；verifier 报告引用并列证据判 24/48 未写死并抓到一次写死；choice 全部有建议段 |

1.9.2 移交的八条缺口：六条落地并有实跑证据；「规约适用判断摇摆」从「看不到处置列」变成「命中条件措辞」，性质变了但没消失；「acceptance.yaml 与 spec §8 漂移」归 framework，car 第二轮 verifier 仍报。

效果目标「产物更稳、质量更高」：以 04 §2 单元看，决策集合与未决承接两轮一致，规约判断 14/17 一致，图关系 3/4 一致。**目标已实现；未完全的是 §2.1 两条知识措辞、§2.2 一件评审动作状态、§2.6 逐话题行。**

### 4.2 可靠性与稳定性

- 四次实跑（检查点两次、补跑两次、步骤 8 两次，后四次为最终版本 1af7fdf7）全部自然闭环，car 第一轮到 plan 是宿主叫停。
- 扩展脚本零崩溃；门禁零循环；停等次数与 SKILL 真值表逐次一致。
- verifier 派发按规则 1–2 次的有 3/4，auto 第二轮 4 次是模型违反硬规则。
- 没有降下来的：上下文终值 474K–562K（基线 411K/509K），实跑 59–107 分钟（TEST 期望半小时）。1.9.3 不以这两项为目标，但性能轴仍在 70–89。
- 装置：第一轮 car 的 13 次「工具调用写成文字」第二轮零次，归被测模型或 CLI，非扩展。

满足「不崩、不循环、停等可预期、闭环可达」这一层的要求；性能层未达 TEST 期望，与 1.9.2 持平。

### 4.3 知识传递与四类产物

| 产物 | 符合期望 | 不符合 |
|---|---|---|
| spec | §10/§11 由 knowledge-use 投影，verifier 两 Case 都核到 requirement 落到具体接口名、存储键、事件名，无规约转述；候选单元两条各有 signal | auto 第二轮 knowledge-use 改后附录 D 投影陈旧一次（verifier 抓到阻断，模型 reopen 重登记修掉）；car 第二轮把「Scope 声明」编成 §2，后文出现两个「## 8.」 |
| plan（car） | 扩展门禁一轮列出 12 处（含「判了命中契约里没有实体扛着：COMPAT-01/02、DLV-01」），一轮修完；decision-tree / page-interaction 有实体承载（`KeySharingCoordinator`、`ShareSetupPage`） | must 夹带：「开关关闭不进发起场景」挂在 RES-02 名下（verifier MAJOR FAIL，非阻断，记 notes）；facts 复用理由写「relationalStore 零命中」与已登记事实矛盾（WARN） |
| story | 未决四处一致；图源标记齐；主要机制在正文 | car 第二轮发起图丢参与者与失败责任（§2.4）；状态机入口标签放宽 |
| review | 议题由决策登记表生成、带 sha；choice 全部有建议段与理由；已定/待确认分区正确，已定事项不再是空节 | car 第二轮 DLV-02 评审动作被写成已定（§2.2）；auto 第二轮 DEC-006 建议「需重验」而第一轮「信息不够推荐不出来」，属建议差异 |

会议链：auto 两轮话题去向、未决保持、禁项不出现都成立；逐话题一行 0/2。

建议分（TEST §10，仅辅助，未经用户确认）：

| Case（第二轮） | 产物 | 性能 | 知识 | 主要扣分 |
|---|---|---|---|---|
| auto-topup | 90 | 72 | 86 | 性能：PASS 后回头改致 9 轮 harness、4 次 verifier、85 分钟、525K；知识：DLV-02 摇摆、附录 D 陈旧一次 |
| car-key-sharing | 84 | 80 | 80 | 产物：DLV-02 已定、发起图丢参与者、spec 编号重复；知识：COMPAT-02 摇摆、must 夹带、facts 失真 |

对照 1.9.2 第二轮 88/75/86、83/70/85：产物与性能持平或略升，知识持平；仍无一轴稳定过 90。

### 4.4 遗留问题与发布

| # | 遗留 | 归属 | 阻断发布？ | 去向 |
|---|---|---|---|---|
| 1 | DLV-02「对外开放」、COMPAT-02「改动错误码」命中条件无定义，判断摇摆 | 知识内容 | 否 | 1.9.4：两条命中条件各补一句定义（知识文件，不动机制） |
| 2 | 评审动作（DLV-02）被模型登记为 settled，verifier 未拦 | E2 只给 decision id，不约束状态 | 否，标题仍写「待评审确认」 | 1.9.4：评审动作类议题的 status 由 review_mode / decider 决定，或审查判据加「评审动作没有人表态不能是已定」一句 |
| 3 | verifier 会议逐话题一行 0/2 | overlay 输出契约 | 否，判断已发生 | 1.9.4：要么退掉那一句，要么由装配把表画好只让 verifier 填「是否有依据」 |
| 4 | 裁定 A 硬规则 1/4 未遵守（auto 第二轮 PASS 后改 MAJOR） | 模型 | 否 | 观察，不加提示 |
| 5 | E5 需求方材料 2/5 触发 | 提示层 | 否 | 观察；car 第二轮的判法有 PRD 依据 |
| 6 | car 第二轮承接图改画流程图丢参与者与失败责任，verifier 判「完整」 | 模型 + 审查判据 | 否 | 1.9.4 看是否复现；E10 独立标签已在，判据是否够具体再定 |
| 7 | spec 章节编号重复（car 第二轮两个 §8） | framework check-spec | 否 | 登记 04 §4 |
| 8 | 上下文 ~500K、实跑 60–85 分钟未降；framework 七条缺口未变 | 性能 / framework | 否 | 后续版本 |

发布前要做的：实施者提交 `interaction-script.yaml` 的宿主纪律一行；工作区两处知识 §9 压短由用户定提交或还原（非实施者所改，与 1.9.3 无关）；发布说明 1.9.3 与指南差异按本文 §4 如实写（含 E5 不稳、逐话题行未做、预算目标重签三次与实测 11536、framework 缺口）。

**建议：1.9.3 以 1af7fdf7 发布；1、2、3 三项进 1.9.4，其余记观察。** 判定权在用户人工审视。

用户已建 `test/story/spec/1.9.4/2026-09-18-Extension残留清点与正向性复查/`，其 P1–P6 是代码侧残留（章节合同访问器、hooks 自研定位、pending_mark 双写、images 旧分支、提示层手抄副本、review 按行禁写），建议顺序第 1 条「完成步骤 8 第二轮与人工审视」即本文。本文 1–3 是效果侧问题，与 P1–P6 不重叠，建议并入 1.9.4 范围一起定案。

## 5. 与方案的对照

04 §2 要求两 Case 各两次，已做；六类单元两两比已做（§2）；步骤 8 失败路径三条（性能归门禁、产物归送达、提示层回退）均未触发：性能变差归因到模型回头改，不归门禁；产物差异归因到知识措辞与模型判断，不归 E3 体积（任务书未触上界）；作者行为与检查点时一致。不回退。
